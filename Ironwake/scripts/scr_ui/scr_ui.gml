// =============================================================================
// GUI CANVAS CONSTANTS (SYSTEMS_RESOLUTION.md) - native 1920x1080 GUI layer.
// Drawn 1:1 to a 1080p display (clean 1.5x over the old 1280x720). Use these in
// place of bare 1920/1080/960/540 literals as each screen is rescaled, so a future
// resolution change is one edit here. Macros are global regardless of which script
// declares them; they live in the UI module since they're UI-canvas constants.
// =============================================================================
#macro GUI_W  1920
#macro GUI_H  1080
#macro GUI_CX (GUI_W / 2)   // 960 - horizontal center
#macro GUI_CY (GUI_H / 2)   // 540 - vertical center

// =============================================================================
// WIDE-ASPECT GUTTERS (ANDROID_PORT_PLAN.md chunk 8b) - phones are 19.5:9-21:9,
// wider than the 16:9 canvas. gui_geometry_apply() centers the 1920x1080 band on
// the screen (uniform scale, no distortion) and records how far the visible
// screen extends PAST the band on each side, in GUI units. ALL layout stays in
// 1920x1080 coordinates; only full-bleed drawing (backgrounds, dim scrims,
// vignettes) spans GUI_XL..GUI_XR so the side strips never show raw black.
// On any 16:9-or-narrower window the gutter is 0 and GUI_XL/GUI_XR collapse to
// exactly 0/1920 - the PC and HTML5 builds render identically to before.
// =============================================================================
#macro GUI_GUTTER (variable_global_exists("gui_gutter") ? global.gui_gutter : 0)
#macro GUI_XL (-GUI_GUTTER)          // left  edge of the visible screen, GUI units
#macro GUI_XR (GUI_W + GUI_GUTTER)   // right edge of the visible screen, GUI units

// Fit the GUI layer to the current window. 16:9 or narrower keeps the shipped
// path (display_set_gui_size stretch, gutter 0). Wider windows get the band
// treatment: uniform scale locked to window height, centered with equal side
// gutters via display_set_gui_maximise (mouse/touch -> GUI mapping follows it).
// Called from obj_game_controller Create and re-applied by its Step whenever the
// window dimensions change (F11 fullscreen, F7 aspect lever, Android fold/boot).
function gui_geometry_apply() {
    zoom_state_init();
    global.gui_gutter = 0;
    var _ww = max(1, window_get_width());
    var _wh = max(1, window_get_height());
    var _zs = global.zoom;
    // HTML5 keeps the shipped stretch-to-canvas behavior untouched (scr_stats
    // video_apply drives the canvas size there). Pinch zoom is not supported in
    // the browser (touch_pinch_update also gates on os_browser).
    if (os_browser != browser_not_a_browser) {
        _zs.z = 1; _zs.vx = 0; _zs.vy = 0; _zs.active = false;
        display_set_gui_size(GUI_W, GUI_H);
        return;
    }
    if (_ww / _wh <= (GUI_W / GUI_H) + 0.002) {
        _zs.sx = _ww / GUI_W;  _zs.sy = _wh / GUI_H;   // stretch scales (16:9 = uniform)
        _zs.ox = 0;  _zs.l0 = 0;  _zs.vis_w = GUI_W;
    } else {
        var _s  = _wh / GUI_H;                    // uniform scale, height-locked
        var _ox = (_ww - GUI_W * _s) / 2;         // centering offset, window px
        global.gui_gutter = _ox / _s;             // one side's gutter, GUI units
        _zs.sx = _s;  _zs.sy = _s;  _zs.ox = _ox;
        _zs.l0 = -global.gui_gutter;              // GUI x visible at the window's left edge
        _zs.vis_w = _ww / _s;                     // full visible width incl both gutters
    }
    // Window shape changed = the pan/scale anchor math is stale; snap out of
    // zoom rather than guess (a fold/rotate mid-zoom just resets to 1.0).
    _zs.z = 1;  _zs.vx = 0;  _zs.vy = 0;  _zs.active = false;
    zoom_apply();
}

// =============================================================================
// PINCH ZOOM - mobile magnifier (SYSTEMS_PINCH_ZOOM.md, M design-locked 07-28).
// ONE state (global.zoom) drives the GUI transform every frame. The whole game
// renders on the GUI layer (no Draw_0 events exist), so zooming the GUI layer
// zooms everything - no room-camera half needed. gui_geometry_apply() records
// the BASE window fit (sx/sy scale, ox centering offset, l0 = GUI x at the
// window's left edge, vis_w = visible GUI width incl gutters); zoom composes
// on top: window_x = gui_x*sx*z + ox*z - vx*sx*z, where (vx,vy) is the pan in
// GUI units from the base view origin. device_mouse_*_to_gui() passes through
// display_set_gui_maximise, so every existing hit-test keeps working untouched.
// Gesture tracking lives in touch_pinch_update (scr_input); the on-screen
// controls stay screen-FIXED via the zgx/zgy/ziv helpers below.
// =============================================================================
#macro ZOOM_MAX 2.5

function zoom_state_init() {
    if (variable_global_exists("zoom")) return;
    global.zoom = {
        z: 1, vx: 0, vy: 0,               // current zoom + pan (GUI units)
        sx: 1, sy: 1, ox: 0,              // base window fit (gui_geometry_apply)
        l0: 0, vis_w: GUI_W,              // base view origin / width, GUI units
        active: false,                    // a two-finger pinch is in progress
        d0: 1, z0: 1, g0x: 0, g0y: 0,     // gesture anchors (start dist/zoom/midpoint)
        cool: 0                           // frames to keep the tap classifier muted
    };
}

// Push the current zoom state onto the GUI transform. At 1.0 this restores the
// exact shipped calls (display_set_gui_size / centered maximise) byte-identically.
function zoom_apply() {
    zoom_state_init();
    if (os_browser != browser_not_a_browser) return;
    var _zs = global.zoom;
    if (_zs.z <= 1.0005) {
        _zs.z = 1;  _zs.vx = 0;  _zs.vy = 0;
        if (global.gui_gutter > 0) display_set_gui_maximise(_zs.sx, _zs.sy, _zs.ox, 0);
        else                       display_set_gui_size(GUI_W, GUI_H);
        return;
    }
    display_set_gui_maximise(_zs.sx * _zs.z, _zs.sy * _zs.z,
                             _zs.ox * _zs.z - _zs.vx * _zs.sx * _zs.z,
                                            - _zs.vy * _zs.sy * _zs.z);
}

// Clamp the pan so the zoomed view never shows anything the unzoomed view
// wouldn't (locked decision #5: the view never leaves the canvas/band).
function zoom_pan_clamp() {
    var _zs = global.zoom;
    _zs.vx = clamp(_zs.vx, 0, _zs.vis_w * (1 - 1 / _zs.z));
    _zs.vy = clamp(_zs.vy, 0, GUI_H     * (1 - 1 / _zs.z));
}

function zoom_active() {
    return variable_global_exists("zoom") && global.zoom.z > 1.0005;
}

// Screen-FIXED control mapping (locked decision #4): the d-pad, back chip and
// action chips keep their physical screen position/size while the UI zooms
// beneath them. These map a NOMINAL GUI coordinate to the zoomed-GUI coordinate
// that renders at the same physical pixel; the mapping is affine, so mapping a
// rect's corners also shrinks it by 1/z - draw text inside with ziv() scale.
function zgx(_gx) {
    if (!zoom_active()) return _gx;
    var _zs = global.zoom;
    return (_zs.l0 + _zs.vx) + (_gx - _zs.l0) / _zs.z;
}
function zgy(_gy) {
    if (!zoom_active()) return _gy;
    return global.zoom.vy + _gy / global.zoom.z;
}
function ziv() {
    return zoom_active() ? 1 / global.zoom.z : 1;
}

// =============================================================================
// draw_text_outline(x, y, str, [outline_col], [fill_col])
// Draws str with a solid black (or given) outline so light hint/footer text and
// lore flavor stay legible over busy backgrounds. Uses the CURRENT draw color as
// the fill unless fill_col is supplied, and respects the current font / halign /
// valign / alpha. Leaves the draw color set to the fill afterwards.
// =============================================================================
function draw_text_outline(x, y, str, outline_col = c_black, fill_col = undefined) {
    var _fill = is_undefined(fill_col) ? draw_get_color() : fill_col;
    draw_set_color(outline_col);
    draw_text(x - 1, y - 1, str);
    draw_text(x,     y - 1, str);
    draw_text(x + 1, y - 1, str);
    draw_text(x - 1, y,     str);
    draw_text(x + 1, y,     str);
    draw_text(x - 1, y + 1, str);
    draw_text(x,     y + 1, str);
    draw_text(x + 1, y + 1, str);
    draw_set_color(_fill);
    draw_text(x, y, str);
}

// ---------------------------------------------------------------------------
// ui_draw_key_legend(cx, y, txt, label_col)
// Footer key-legend renderer (M 2026-07-06): draws a centred hint line with
// the KEY tokens in outlined gold and the labels dim, so the legend reads as
// quick reference instead of a jumble. Segments split on runs of 2+ spaces;
// within a segment the key is a leading "[...]" chunk or everything up to the
// first ":" (parenthesised asides stay all-dim). Sets fnt_ui_small and leaves
// halign fa_left. Honors the caller's valign.
// ---------------------------------------------------------------------------
function ui_draw_key_legend(cx, y, txt, label_col = undefined, translate = true, max_w = 0) {
    // max_w (08-16, M: Bairc's legend "runs out of the border popup box"): when
    // the row is wider than this, the gap tightens first, then the segments
    // split into TWO rows stacked upward so the bottom row stays on the
    // caller's y and nothing runs past the panel. 0 = no limit (old behaviour).
    // Touch (M 07-17): key legends are keyboard-speak ("[G] Gender", "W/S...") -
    // useless on a phone and just wasted footer space. On-screen actions live in the
    // tappable chip bar / buttons instead, so draw nothing here on touch. (Gamepad
    // still gets the pad-glyph translation below; keyboard is unchanged.)
    if (input_device() == 2) return;
    if (label_col == undefined) label_col = make_color_rgb(150, 158, 182);
    var _key_col = make_color_rgb(255, 205, 90);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_halign(fa_left);

    // Font-size guard (M 08-14): caller y-values are tuned for the default
    // font, so the taller Large variant ran under the gothic frame band
    // (full-screen panels open y30..1050 - Sable/Maren/Vex/Vael cut-off shots).
    // Clamp the text bottom to the frame opening when the caller aimed inside
    // it, else to the screen edge. Honors the active valign.
    var _lh  = string_height(txt);
    var _cap = (y <= 1040) ? 1046 : GUI_H - 4;
    var _va  = draw_get_valign();
    var _bot = (_va == fa_middle) ? y + _lh * 0.5 : ((_va == fa_bottom) ? y : y + _lh);
    if (_bot > _cap) y -= (_bot - _cap);

    // Split into segments on runs of 2+ spaces.
    var _segs  = [];
    var _len   = string_length(txt);
    var _start = 1, _i = 1;
    while (_i <= _len) {
        if (string_char_at(txt, _i) == " " && _i < _len && string_char_at(txt, _i + 1) == " ") {
            if (_i > _start) array_push(_segs, string_copy(txt, _start, _i - _start));
            while (_i <= _len && string_char_at(txt, _i) == " ") _i++;
            _start = _i;
        } else {
            _i++;
        }
    }
    if (_start <= _len) array_push(_segs, string_copy(txt, _start, _len - _start + 1));

    // Split each segment into key / label; measure for centring.
    // Gamepad (chunk 7b): key tokens are translated to pad text chips via
    // input_legend_key (scr_input); segments whose action has no pad binding
    // are hidden. Keyboard/mouse rendering is byte-identical to before.
    // Callers that already built a pad string pass translate = false.
    var _pad_ui = (input_device() == 1) && translate;
    var _parts  = [];
    var _gap    = 34;
    var _total  = 0;
    for (var _s = 0; _s < array_length(_segs); _s++) {
        var _seg = _segs[_s];
        var _key = "", _lab = _seg;
        var _bracketed = false;
        if (string_char_at(_seg, 1) == "[") {
            var _cb = string_pos("]", _seg);
            if (_cb > 0) { _key = string_copy(_seg, 1, _cb); _lab = string_delete(_seg, 1, _cb); _bracketed = true; }
        } else if (string_char_at(_seg, 1) != "(") {
            var _cp = string_pos(":", _seg);
            if (_cp > 0 && _cp <= 16) { _key = string_copy(_seg, 1, _cp); _lab = string_delete(_seg, 1, _cp); }
        }
        if (_pad_ui && _key != "") {
            var _kin = _bracketed
                ? string_copy(_key, 2, string_length(_key) - 2)    // strip [ ]
                : string_copy(_key, 1, string_length(_key) - 1);   // strip trailing :
            var _pk = input_legend_key(_kin);
            if (_pk == "") continue;   // no pad equivalent - hide the segment
            _key = _bracketed ? ("[" + _pk + "]") : (_pk + ":");
        }
        array_push(_parts, { key: _key, lab: _lab });
        _total += string_width(_key) + string_width(_lab);
    }
    var _np = array_length(_parts);
    var _row_total = _total + _gap * max(0, _np - 1);
    if (max_w > 0 && _row_total > max_w) {
        _gap = 18;   // tighten first
        _row_total = _total + _gap * max(0, _np - 1);
    }
    var _rows = [];   // each = { first, last } part index range
    if (max_w > 0 && _row_total > max_w && _np > 1) {
        // Split at the part where the running width crosses half - two rows.
        var _half = _total / 2, _run = 0, _split = 1;
        for (var _q = 0; _q < _np; _q++) {
            _run += string_width(_parts[_q].key) + string_width(_parts[_q].lab);
            if (_run >= _half) { _split = clamp(_q + 1, 1, _np - 1); break; }
        }
        array_push(_rows, { first: 0, last: _split });
        array_push(_rows, { first: _split, last: _np });
    } else {
        array_push(_rows, { first: 0, last: _np });
    }
    // Rows stack UPWARD from the caller's y (bottom row = y).
    var _lh_row = string_height("Ag") + 4;
    for (var _ri = 0; _ri < array_length(_rows); _ri++) {
        var _rw = _rows[_ri];
        var _ry = y - (array_length(_rows) - 1 - _ri) * _lh_row;
        var _rt = 0;
        for (var _p = _rw.first; _p < _rw.last; _p++) _rt += string_width(_parts[_p].key) + string_width(_parts[_p].lab);
        _rt += _gap * max(0, (_rw.last - _rw.first) - 1);
        var _x = cx - _rt / 2;
        for (var _p = _rw.first; _p < _rw.last; _p++) {
            var _pt = _parts[_p];
            if (_pt.key != "") {
                draw_set_color(_key_col);
                draw_text_outline(_x, _ry, _pt.key);
                _x += string_width(_pt.key);
            }
            draw_set_color(label_col);
            draw_text(_x, _ry, _pt.lab);
            _x += string_width(_pt.lab) + _gap;
        }
    }
}

// ---------------------------------------------------------------------------
// ui_draw_touch_back() - universal Back/close chip (chunk 8c minimal).
// Call LAST in every room controller's Draw GUI event, on every device (it
// also pumps the touch key releaser). On touch devices it draws an X button
// top-right; tapping fires a simulated Esc, so each screen's EXISTING cancel
// handler reacts (shop closes, modal backs out, hub opens the pause menu).
// ---------------------------------------------------------------------------
// _force_x: the caller says "an overlay of MINE is open, so Esc will CLOSE it,
// not open a menu". Needed because some overlays live on the room controller
// rather than the game controller, so __input_ctx() still reports the base
// screen - combat's ability_detail_open is the case M hit 07-18: the chip showed
// the three-bar MENU glyph while it was actually the popup's only exit.
// Deliberately NOT folded into ui_input_blocked(): that function gates real
// input all over the codebase, and widening it to satisfy a glyph would risk
// changing combat's input handling.
function ui_draw_touch_back(_top = 108, _force_x = false) {
    // default y=108 clears the gold/dust readouts that live top-right on the
    // vendor screens (M 07-08: the chip covered them); combat passes 24 to stay
    // above the enemy-bar grid and shifts its awakening label left instead.
    // Glyph is context-aware (M 07-08 "X opens settings reads wrong"): at a
    // TOP-LEVEL screen Esc opens the pause menu, so the chip shows a menu
    // glyph (three bars); inside an overlay Esc closes it, so it shows X.
    touch_sim_pump();
    if (input_device() != 2) return;
    var _ctx = __input_ctx();
    // Event choice = forced (Esc is dead there); a close chip would promise an
    // exit that can't happen, so draw nothing (M softlocked on it 07-08).
    if (_ctx == "event") return;
    var _is_menu = !_force_x
        && (_ctx == "hub" || _ctx == "floor" || _ctx == "combat" || _ctx == "loot");
    var _s  = 78;
    // Anchored to the SCREEN's right edge (GUI_XR), not the 1920 band's (GUI_W),
    // so on a letterboxed phone it sits out in the gutter instead of on top of
    // the content (M 07-18: in the loadout it covered the topmost ability /
    // companion depending on tab). On PC the gutter is 0 and GUI_XR == GUI_W,
    // so this is byte-identical to the old placement - no desktop change.
    var _x2 = GUI_XR - 24,     _y1 = _top;
    var _x1 = _x2 - _s,        _y2 = _top + _s;
    // PINCH ZOOM (locked decision #4): the chip stays screen-FIXED. Mapping the
    // rect's corners through zgx/zgy also shrinks it by 1/z (affine), so the
    // same coords drive draw + hit-test; glyph metrics scale via _iz.
    var _iz = ziv();
    _x1 = zgx(_x1);  _x2 = zgx(_x2);  _y1 = zgy(_y1);  _y2 = zgy(_y2);
    // Pressed-state: acknowledge the finger the moment it lands on the chip
    // (no hover/key-click on glass - an unacknowledged tap reads as a miss).
    var _bk_press = mouse_check_button(mb_left)
        && device_mouse_x_to_gui(0) >= _x1 && device_mouse_x_to_gui(0) <= _x2
        && device_mouse_y_to_gui(0) >= _y1 && device_mouse_y_to_gui(0) <= _y2;
    draw_set_alpha(_bk_press ? 0.92 : 0.72);
    draw_set_color(_bk_press ? make_color_rgb(52, 44, 26) : make_color_rgb(14, 16, 24));
    draw_rectangle(_x1, _y1, _x2, _y2, false);
    draw_set_alpha(1.0);
    draw_set_color(_bk_press ? make_color_rgb(245, 195, 80) : make_color_rgb(150, 130, 90));
    draw_rectangle(_x1, _y1, _x2, _y2, true);
    if (_is_menu) {
        // three-bar menu glyph (drawn, not a font glyph - Castellar has no such char)
        draw_set_color(make_color_rgb(228, 205, 140));
        var _bcx = (_x1 + _x2) / 2, _bcy = (_y1 + _y2) / 2;
        for (var _b = -1; _b <= 1; _b++) {
            draw_rectangle(_bcx - 18 * _iz, _bcy + (_b * 15 - 3) * _iz,
                           _bcx + 18 * _iz, _bcy + (_b * 15 + 3) * _iz, false);
        }
    } else {
        draw_set_font(ui_font(fnt_ui));
        draw_set_halign(fa_center); draw_set_valign(fa_middle);
        draw_set_color(make_color_rgb(228, 205, 140));
        draw_text_transformed((_x1 + _x2) / 2, (_y1 + _y2) / 2 + 2 * _iz, "X", _iz, _iz, 0);
        draw_set_halign(fa_left); draw_set_valign(fa_top);
        draw_set_color(c_white); draw_set_font(-1);
    }
    // Suppress the back-tap while the action menu owns input (it has its own Close).
    var _amenu_up = variable_global_exists("touch_amenu") && global.touch_amenu.open;
    if (!_amenu_up && touch_tapped(_x1, _y1, _x2, _y2)) touch_press(vk_escape);
}

// ---------------------------------------------------------------------------
// ui_draw_touch_chips() - the per-context ACTION CHIP bar (chunk 8d: the key
// legends' touch replacement - punch items 2/10). Touch only. Draws tappable
// chips along the bottom for the actions that have no on-screen path (the
// keyboard letter hotkeys); a tap fires the same simulated key the legend
// would name. Contexts derive from __input_ctx, same as the pad map.
// Call from the room controller's Draw GUI right before ui_draw_touch_back.
// ---------------------------------------------------------------------------
function ui_draw_touch_chips() {
    if (input_device() != 2) return;
    // While the action menu is up it's modal - hide the chip bar under it.
    if (variable_global_exists("touch_amenu") && global.touch_amenu.open) return;
    var _ctx   = __input_ctx();
    var _chips = [];
    switch (_ctx) {
        case "hub":
            array_push(_chips, { lbl: "STASH",    key: ord("T"), hot: false });
            array_push(_chips, { lbl: "JOURNAL",  key: ord("J"), hot: journal_any_badge() });
            array_push(_chips, { lbl: "HERO",     key: ord("I"), hot: false });
            array_push(_chips, { lbl: "HISTORY",  key: ord("H"), hot: false });
            if (variable_global_exists("pending_perm_points") && global.pending_perm_points > 0) {
                array_push(_chips, { lbl: "UPGRADE", key: ord("P"), hot: true });
            }
            array_push(_chips, { lbl: "SETTINGS", key: ord("O"), hot: false });
            break;
        case "floor":
            array_push(_chips, { lbl: "JOURNAL", key: ord("J"), hot: journal_any_badge() });
            array_push(_chips, { lbl: "HERO",    key: ord("I"), hot: false });
            // 07-24 audit: the mid-run P perm-points overlay was keyboard-only.
            if (variable_global_exists("pending_perm_points") && global.pending_perm_points > 0) {
                array_push(_chips, { lbl: "UPGRADE", key: ord("P"), hot: true });
            }
            // Escape-item chip only when one is actually carried (mirrors the
            // "[G] Use ..." keyboard hint; a dead LAMP button confused M 07-08).
            // Named after the item so DEVIL WINE doesn't masquerade as a lamp.
            if (variable_global_exists("consumable_inventory")) {
                var _esc_lbl = "";
                for (var _eci = 0; _eci < array_length(global.consumable_inventory); _eci++) {
                    var _ect = global.consumable_inventory[_eci].effect_type;
                    if (_ect == "escape_lamp") { _esc_lbl = "LAMP"; break; }   // lamp wins over wine
                    if (_ect == "escape_wine" && _esc_lbl == "") _esc_lbl = "WINE";
                }
                if (_esc_lbl != "") array_push(_chips, { lbl: _esc_lbl, key: ord("G"), hot: false });
            }
            // EXTRACT only once the floor boss is dead - the E key is a no-op
            // before that (M 07-08: "why is there an extract button?"). Mirrors
            // the keyboard footer's [Defeat the boss first] gate.
            var _fc_x = instance_exists(obj_floor_controller) ? instance_find(obj_floor_controller, 0) : noone;
            if (_fc_x != noone) {
                for (var _bci = 0; _bci < array_length(_fc_x.current_rooms); _bci++) {
                    if (_fc_x.current_rooms[_bci].type == "boss" && _fc_x.current_rooms[_bci].cleared) {
                        array_push(_chips, { lbl: "EXTRACT", key: ord("E"), hot: false });
                        break;
                    }
                }
            }
            break;
        case "loadout":
            // The loadout had NO chips at all (it fell through to `default:
            // return`), so two keyboard-only verbs were invisible on a phone:
            // Tab (ability details) and M (spend a mastery notch). M 07-18 hit
            // both - "no touch method for tab for abilities in loadout" and
            // "no touch command for mastery/notch [M]".
            // Both are pool-tab verbs only (loadout_tab == 0), so gate them on
            // the SAME conditions the Step handler checks - no dead chips.
            var _ld_gc = instance_exists(obj_game_controller) ? instance_find(obj_game_controller, 0) : noone;
            if (_ld_gc != noone && _ld_gc.loadout_tab == 0) {
                // (DETAILS chip dropped 08-18: hold-a-row IS details on touch, and the chip
                //  bar landed on top of the confirm bar - M's phone screenshot.)
                // WEB opens the talent web for any pool row (it's a real screen
                // now, not just a spend prompt); lit hot when a point waits.
                var _ld_pool = abilities_class_pool(
                    variable_global_exists("chosen_class") ? global.chosen_class : 0);
                if (_ld_gc.loadout_cursor < array_length(_ld_pool)) {
                    var _ld_nm = _ld_pool[_ld_gc.loadout_cursor].name;
                    array_push(_chips, { lbl: "WEB", key: ord("M"), hot: ability_web_mp_pending(_ld_nm) > 0 });
                }
            }
            // Companion tab (2): B cycles the highlighted pet's stance - pad had RT,
            // touch had nothing (07-24 audit). Gated to a pet row that actually HAS
            // stances (Fortune pets return an empty list), so it's never a dead chip.
            if (_ld_gc != noone && _ld_gc.loadout_tab == 2) {
                var _ld_row = 0;
                for (var _ldp = 0; _ldp < pet_count(); _ldp++) {
                    if (global.pet_roster[_ldp].is_egg) continue;
                    if (_ld_row == _ld_gc.loadout_cursor) {
                        if (array_length(pet_stance_list(global.pet_roster[_ldp])) > 0) {
                            array_push(_chips, { lbl: "STANCE", key: ord("B"), hot: false });
                        }
                        break;
                    }
                    _ld_row++;
                }
            }
            if (array_length(_chips) == 0) return;
            break;
        case "kb":
            // Knucklebones: play is pure d-pad nav, but the H rules overlay had no
            // touch path (07-24 audit). Same chip toggles it closed again.
            array_push(_chips, { lbl: "RULES", key: ord("H"), hot: false });
            break;
        case "board":
            array_push(_chips, { lbl: "KNUCKLEBONES", key: ord("K"), hot: false });
            array_push(_chips, { lbl: "HIGH TABLE",   key: ord("T"), hot: false });
            array_push(_chips, { lbl: "REROLL",       key: ord("R"), hot: false });
            break;
        case "shop":
        case "bairc":
            // NPC screens: a single ACTIONS chip opens the long-press action menu
            // (Deepen Bond / Gift / Reforge / Cancel). Only shown when at least one
            // verb is actually available, so it's never a dead button. key -1 is the
            // sentinel handled in the tap loop below (opens the menu, not a keypress).
            if (array_length(touch_action_menu_items()) > 0) {
                array_push(_chips, { lbl: "ACTIONS", key: -1, hot: false });
            }
            if (array_length(_chips) == 0) return;
            break;
        default: return;   // other screens: direct taps + the X chip cover them
    }

    draw_set_font(ui_font(fnt_ui_small));
    // measure, then draw centred along the bottom edge. Chips sized up for thumbs
    // (M 07-17: prioritise menu-button size on mobile) - taller + more side padding.
    var _pad_x = 34, _gap = 15, _ch_h = 66;
    var _tot = 0;
    for (var _i = 0; _i < array_length(_chips); _i++) {
        _chips[_i].w = string_width(_chips[_i].lbl) + _pad_x * 2;
        _tot += _chips[_i].w + ((_i > 0) ? _gap : 0);
    }
    var _cx = GUI_CX - _tot / 2;
    var _cy = GUI_H - _ch_h - 9;
    // LOADOUT (08-18): the bottom band is the confirm bar + swipe hint - park the
    // (situational) WEB / STANCE chips top-right under the X chip instead.
    if (_ctx == "loadout") { _cx = GUI_XR - 24 - _tot; _cy = 200; }
    draw_set_halign(fa_center); draw_set_valign(fa_middle);
    var _pmx = device_mouse_x_to_gui(0), _pmy = device_mouse_y_to_gui(0);
    var _pdn = mouse_check_button(mb_left);
    // PINCH ZOOM (locked decision #4): chips stay screen-FIXED - each chip's
    // nominal rect maps through zgx/zgy (which also shrinks it 1/z), and the
    // label draws at _iz scale so it keeps its physical size inside the box.
    var _iz = ziv();
    for (var _i = 0; _i < array_length(_chips); _i++) {
        var _c = _chips[_i];
        var _flash = _c.hot && ((current_time div 400) mod 2 == 0);
        var _zx1 = zgx(_cx),         _zy1 = zgy(_cy);
        var _zx2 = zgx(_cx + _c.w),  _zy2 = zgy(_cy + _ch_h);
        // Pressed-state: brighten while the finger is down on this chip so the
        // tap is visibly acknowledged (no hover on glass).
        var _press = _pdn && _pmx >= _zx1 && _pmx <= _zx2
                          && _pmy >= _zy1 && _pmy <= _zy2;
        draw_set_alpha(_press ? 0.95 : 0.82);
        draw_set_color(_press ? make_color_rgb(52, 44, 26) : make_color_rgb(14, 16, 24));
        draw_rectangle(_zx1, _zy1, _zx2, _zy2, false);
        draw_set_alpha(1.0);
        draw_set_color((_flash || _press) ? make_color_rgb(245, 195, 80) : make_color_rgb(110, 100, 75));
        draw_rectangle(_zx1, _zy1, _zx2, _zy2, true);
        draw_set_color((_flash || _press) ? make_color_rgb(245, 195, 80) : make_color_rgb(215, 200, 165));
        draw_text_transformed((_zx1 + _zx2) / 2, (_zy1 + _zy2) / 2 + _iz, _c.lbl, _iz, _iz, 0);
        if (touch_tapped(_zx1, _zy1, _zx2, _zy2)) {
            if (_c.key == -1) {
                if (!variable_global_exists("touch_amenu")) global.touch_amenu = { open: false, open_t0: -1 };
                global.touch_amenu.open    = true;
                global.touch_amenu.open_t0 = variable_global_exists("tg") ? global.tg.t0 : -1;
                audio_play_sound(snd_page, 1, false);
            } else {
                touch_press(_c.key);
            }
        }
        _cx += _c.w + _gap;
    }
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white); draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_touch_action_menu() - the NPC-screen long-press ACTION MENU (M 07-17).
// Modal list of the current context's letter-hotkey verbs; a tapped row fires the
// same simulated key the keyboard uses (handled next frame). Opened by long-press
// or the ACTIONS chip (both set global.touch_amenu.open); the tap/close INPUT is
// owned by obj_game_controller Step (which exits while open so the screen beneath
// is frozen) - this function only DRAWS, using the shared touch_amenu_rects layout
// so hit-test and render can't drift. Call LAST in the room controller's Draw GUI,
// after ui_draw_touch_chips / ui_draw_touch_back, so it lands on top.
// ---------------------------------------------------------------------------
function ui_draw_touch_action_menu() {
    if (input_device() != 2) return;
    if (!variable_global_exists("touch_amenu") || !global.touch_amenu.open) return;
    var _items = touch_action_menu_items();
    var _n = array_length(_items);
    if (_n == 0) { global.touch_amenu.open = false; return; }
    var _lay = touch_amenu_rects(_n);

    // Full-bleed scrim (spans the wide-aspect gutters).
    draw_set_alpha(0.62); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    // Panel + gold frame.
    draw_set_color(make_color_rgb(16, 18, 26));
    draw_rectangle(_lay.x1, _lay.y1, _lay.x2, _lay.y2, false);
    draw_set_color(make_color_rgb(150, 130, 85));
    draw_rectangle(_lay.x1, _lay.y1, _lay.x2, _lay.y2, true);

    draw_set_halign(fa_center); draw_set_valign(fa_middle);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(240, 220, 170));
    draw_text(GUI_CX, _lay.y1 + _lay.title_h / 2 + 4, "ACTIONS");

    var _pmx = device_mouse_x_to_gui(0), _pmy = device_mouse_y_to_gui(0);
    var _pdn = mouse_check_button(mb_left);

    // Action rows.
    for (var _i = 0; _i < _n; _i++) {
        var _r  = _lay.rows[_i];
        var _pr = _pdn && _pmx >= _lay.x1 && _pmx <= _lay.x2 && _pmy >= _r.y1 && _pmy <= _r.y2;
        draw_set_color(_pr ? make_color_rgb(52, 44, 26) : make_color_rgb(26, 28, 38));
        draw_rectangle(_lay.x1 + _lay.pad, _r.y1, _lay.x2 - _lay.pad, _r.y2, false);
        draw_set_color(_pr ? make_color_rgb(245, 195, 80) : make_color_rgb(120, 108, 82));
        draw_rectangle(_lay.x1 + _lay.pad, _r.y1, _lay.x2 - _lay.pad, _r.y2, true);
        draw_set_color(make_color_rgb(232, 222, 198));
        draw_text(GUI_CX, (_r.y1 + _r.y2) / 2 + 1, _items[_i].lbl);
    }

    // Close row.
    var _rc  = _lay.close;
    var _prc = _pdn && _pmx >= _lay.x1 && _pmx <= _lay.x2 && _pmy >= _rc.y1 && _pmy <= _rc.y2;
    draw_set_color(_prc ? make_color_rgb(52, 26, 26) : make_color_rgb(22, 20, 26));
    draw_rectangle(_lay.x1 + _lay.pad, _rc.y1, _lay.x2 - _lay.pad, _rc.y2, false);
    draw_set_color(make_color_rgb(150, 92, 82));
    draw_rectangle(_lay.x1 + _lay.pad, _rc.y1, _lay.x2 - _lay.pad, _rc.y2, true);
    draw_set_color(make_color_rgb(222, 184, 172));
    draw_text(GUI_CX, (_rc.y1 + _rc.y2) / 2 + 1, "Close");

    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white); draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_touch_gamepad() - on-screen GAMEPAD overlay for touch (M 07-17). Since
// the whole game is operable from the (tested) gamepad backend - everything reachable
// by 4 directions + confirm - this is just a touch skin over those existing inputs:
// each button fires a simulated key that nav_* / input_confirm already handle, so it
// drives EVERY screen for free with zero per-screen work. Mounted in the reclaimed
// left GUTTER (8b letterbox space); skipped on ~16:9 devices with no gutter room.
// ADDITION, not a replacement - direct tap still works. Hold a direction to repeat
// (self-timed so it can't edge-churn the key). Call LAST in each room controller's
// Draw GUI. Toggle-off via global.touch_gamepad_off (a Settings row can flip it).
// v1 = d-pad + confirm; back/tab/examine buttons come after the feel is tuned.
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// touch_pad_geom() - the on-screen d-pad's LAYOUT, split out of the draw call
// (M 07-18) so the hit-test can be asked about BEFORE the pad draws.
//
// Why this exists: the pad is drawn LAST in every room controller's Draw GUI so
// it sits on top, but touch_tapped() is NON-CONSUMING - so a press on the pad
// ALSO fired the tap handler of whatever overlay had drawn underneath it
// earlier in the same frame. At Bairc that underlying handler is tap-to-close,
// which is why M saw "dpad exits the specific pet menu you pull up"; in the
// Journal the presses were being eaten by the panel beneath. Exposing the rects
// lets touch_tapped() mask the pad's own footprint out for everyone else.
//
// Returns undefined when no pad is on screen (all three early-outs below), so
// callers can treat "no pad" as "nothing to mask".
// ---------------------------------------------------------------------------
function touch_pad_geom() {
    if (input_device() != 2) return undefined;
    touch_settings_init();
    if (global.touch_gamepad_off) return undefined;
    if (GUI_GUTTER < 120) return undefined;   // needs letterbox gutter room

    var _scl  = clamp(global.touch_pad_scale, TOUCH_PAD_SCALE_MIN, TOUCH_PAD_SCALE_MAX);
    var _bs   = 46 * _scl;                            // d-pad button HALF-size
    var _step = _bs * 2.35;                           // centre-to-centre: > 2*_bs = real gap
    var _half = _step + _bs;                          // cross half-extent
    var _abs  = _bs * 1.25;                           // OK button half-size

    // GUI_XL is NEGATIVE (-gutter) and GUI_XR is 1920+gutter - they're the visible
    // screen EDGES, not widths. An earlier pass added GUI_GUTTER to GUI_XR to get a
    // "full width", which double-counted the right gutter and let the OK button be
    // clamped to a point off the right of the screen. Clamp to GUI_XR directly.
    var _cy   = GUI_H - _half - 30;                                 // low, for thumb reach
    // Sit as far LEFT as the screen allows so the cross uses the gutter rather
    // than the play area (cross left edge lands 10px inside the screen edge).
    // With no gutter GUI_XL is 0 and this is just _half+10, as before.
    var _lcx  = GUI_XL + _half + 10;                                // LEFT  -> d-pad (left thumb)
    var _rcx  = min((GUI_W + GUI_XR) / 2, GUI_XR - _abs - 10);      // RIGHT -> OK    (right thumb)

    // PINCH ZOOM (locked decision #4): the pad stays screen-FIXED while the UI
    // zooms beneath it. Map the centres through zgx/zgy and scale the extents
    // by 1/z; every consumer (draw + hit-test + footprint mask) reads THIS
    // struct, so draw and hit-test stay in lockstep automatically.
    if (zoom_active()) {
        var _iz = ziv();
        _lcx  = zgx(_lcx);   _rcx = zgx(_rcx);   _cy = zgy(_cy);
        _bs  *= _iz;  _step *= _iz;  _half *= _iz;  _abs *= _iz;
    }

    return {
        bs: _bs, step: _step, half: _half, abs: _abs,
        cy: _cy, lcx: _lcx, rcx: _rcx,
        tgl: _bs * 0.44,                              // triangle glyph half-size
        // Masking rects: the cross bbox and the OK button bbox.
        cx1: _lcx - _half, cy1: _cy - _half, cx2: _lcx + _half, cy2: _cy + _half,
        ox1: _rcx - _abs,  oy1: _cy - _abs,  ox2: _rcx + _abs,  oy2: _cy + _abs
    };
}

// True when a GUI-space point lands on the on-screen pad (either the direction
// cross or the OK button). touch_tapped() uses this to keep pad presses from
// leaking through to the UI underneath.
function touch_over_pad(_mx, _my) {
    var _g = touch_pad_geom();
    if (is_undefined(_g)) return false;
    if (point_in_rectangle(_mx, _my, _g.cx1, _g.cy1, _g.cx2, _g.cy2)) return true;
    return point_in_rectangle(_mx, _my, _g.ox1, _g.oy1, _g.ox2, _g.oy2);
}

function ui_draw_touch_gamepad() {
    var _g = touch_pad_geom();
    if (is_undefined(_g)) return;

    // GEOMETRY (rewritten 07-18, M on S25: "too small and too close together").
    // The v2 bug was arithmetic, not taste: _bs is a HALF-size (buttons are 2*_bs
    // wide) but centre-to-centre was only 1.7*_bs, so adjacent buttons physically
    // OVERLAPPED by ~30% - hence "too close together". Spacing must exceed 2*_bs.
    // Size is no longer clamped to the gutter (that cap is what made it tiny: a
    // ~210px S25 gutter yielded _bs=35); it's now a player-scaled value that may
    // bleed over the low-left play area, which is the only way to be thumb-sized.
    // Position is clamped to the screen so it can never run off-edge (the v1 bug).
    // Layout now lives in touch_pad_geom() so touch_tapped() can mask these rects.
    var _bs = _g.bs, _step = _g.step, _tgl = _g.tgl, _abs = _g.abs;
    var _cy = _g.cy, _lcx = _g.lcx, _rcx = _g.rcx;

    var _mx = device_mouse_x_to_gui(0), _my = device_mouse_y_to_gui(0);
    var _down = mouse_check_button(mb_left);
    if (!variable_global_exists("tg_pad_hold")) global.tg_pad_hold = { key: -1, t: 0 };
    var _hold = global.tg_pad_hold;
    var _any_dir = false;

    var _dirs = [[0, -1, vk_up], [0, 1, vk_down], [-1, 0, vk_left], [1, 0, vk_right]];
    for (var _i = 0; _i < 4; _i++) {
        var _bx = _lcx + _dirs[_i][0] * _step;
        var _by = _cy  + _dirs[_i][1] * _step;
        var _key = _dirs[_i][2];
        var _on  = _down && point_in_rectangle(_mx, _my, _bx - _bs, _by - _bs, _bx + _bs, _by + _bs);
        draw_set_alpha(_on ? 0.92 : 0.46);
        draw_set_color(_on ? make_color_rgb(58, 50, 30) : make_color_rgb(16, 18, 26));
        draw_roundrect(_bx - _bs, _by - _bs, _bx + _bs, _by + _bs, false);
        draw_set_alpha(_on ? 1.0 : 0.72);
        draw_set_color(_on ? make_color_rgb(245, 205, 90) : make_color_rgb(150, 140, 110));
        draw_roundrect(_bx - _bs, _by - _bs, _bx + _bs, _by + _bs, true);
        draw_set_color(_on ? make_color_rgb(255, 230, 150) : make_color_rgb(200, 190, 160));
        var _t = _tgl;
        if (_dirs[_i][1] == -1)      draw_triangle(_bx, _by - _t, _bx - _t, _by + _t, _bx + _t, _by + _t, false);
        else if (_dirs[_i][1] == 1)  draw_triangle(_bx, _by + _t, _bx - _t, _by - _t, _bx + _t, _by - _t, false);
        else if (_dirs[_i][0] == -1) draw_triangle(_bx - _t, _by, _bx + _t, _by - _t, _bx + _t, _by + _t, false);
        else                         draw_triangle(_bx + _t, _by, _bx - _t, _by - _t, _bx - _t, _by + _t, false);
        if (_on) {
            _any_dir = true;
            if (_hold.key != _key) { _hold.key = _key; _hold.t = 0; touch_press(_key); }
            else { _hold.t++; if (_hold.t >= 18 && ((_hold.t - 18) mod 5) == 0) touch_press(_key); }
        }
    }
    if (!_any_dir) _hold.key = -1;

    // OK / confirm button in the RIGHT gutter (right thumb) - a bit larger (_abs above).
    var _aon = _down && point_in_rectangle(_mx, _my, _rcx - _abs, _cy - _abs, _rcx + _abs, _cy + _abs);
    draw_set_alpha(_aon ? 0.95 : 0.5);
    draw_set_color(_aon ? make_color_rgb(24, 52, 26) : make_color_rgb(16, 22, 18));
    draw_roundrect(_rcx - _abs, _cy - _abs, _rcx + _abs, _cy + _abs, false);
    draw_set_alpha(1.0);
    draw_set_color(_aon ? make_color_rgb(150, 240, 150) : make_color_rgb(110, 180, 120));
    draw_roundrect(_rcx - _abs, _cy - _abs, _rcx + _abs, _cy + _abs, true);
    draw_set_halign(fa_center); draw_set_valign(fa_middle);
    draw_set_font(ui_font(fnt_ui)); draw_set_color(c_white);
    // ziv(): keep the label's PHYSICAL size constant while zoomed (the button
    // rect is already 1/z in GUI units - full-size text would overflow it).
    draw_text_transformed(_rcx, _cy + 1, "OK", ziv(), ziv(), 0);
    draw_set_halign(fa_left); draw_set_valign(fa_top); draw_set_font(-1);
    // true = "this IS the pad" - bypass the pad-footprint mask in touch_tapped.
    if (touch_tapped(_rcx - _abs, _cy - _abs, _rcx + _abs, _cy + _abs, true)) touch_press(vk_enter);

    draw_set_alpha(1.0); draw_set_color(c_white);
}

// ---------------------------------------------------------------------------
// ui_draw_touch_continue(cx, y) - framed CONTINUE button for touch, replacing
// "Press Enter to continue" prompt text (M 07-08: "should be a bordered bigger
// button than ambiguous text"). Draws a 420x72 gold-framed button with the
// pressed-state flash; tapping it fires a simulated Enter. Most owning popups
// also close on tap-anywhere - the button is the visible control either way.
// Caller is responsible for gating on input_device() == 2.
// ---------------------------------------------------------------------------
function ui_draw_touch_continue(_cx, _cy, _lbl = "CONTINUE") {
    var _w  = 420, _h = 72;
    var _x1 = _cx - _w / 2, _y1 = _cy;
    var _x2 = _cx + _w / 2, _y2 = _cy + _h;
    var _pr = mouse_check_button(mb_left)
        && device_mouse_x_to_gui(0) >= _x1 && device_mouse_x_to_gui(0) <= _x2
        && device_mouse_y_to_gui(0) >= _y1 && device_mouse_y_to_gui(0) <= _y2;
    draw_set_alpha(_pr ? 0.95 : 0.85);
    draw_set_color(_pr ? make_color_rgb(52, 44, 26) : make_color_rgb(20, 22, 34));
    draw_rectangle(_x1, _y1, _x2, _y2, false);
    draw_set_alpha(1.0);
    draw_set_color(_pr ? make_color_rgb(245, 195, 80) : make_color_rgb(150, 130, 90));
    draw_rectangle(_x1, _y1, _x2, _y2, true);
    draw_rectangle(_x1 + 3, _y1 + 3, _x2 - 3, _y2 - 3, true);
    draw_set_font(ui_font(fnt_ui));
    draw_set_halign(fa_center); draw_set_valign(fa_middle);
    draw_set_color(_pr ? make_color_rgb(245, 195, 80) : make_color_rgb(228, 215, 180));
    draw_text(_cx, (_y1 + _y2) / 2 + 1, _lbl);
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white); draw_set_font(-1);
    if (touch_tapped(_x1, _y1, _x2, _y2)) touch_press(vk_enter);
}

// draw_text_ext_outline(x, y, str, sep, w, [outline_col], [fill_col])
// Wrapped (draw_text_ext) variant of draw_text_outline - for multi-line flavor /
// lore text that needs both word-wrap and a legibility outline (e.g. the camp line).
function draw_text_ext_outline(x, y, str, sep, w, outline_col = c_black, fill_col = undefined) {
    var _fill = is_undefined(fill_col) ? draw_get_color() : fill_col;
    draw_set_color(outline_col);
    draw_text_ext(x - 1, y - 1, str, sep, w);
    draw_text_ext(x,     y - 1, str, sep, w);
    draw_text_ext(x + 1, y - 1, str, sep, w);
    draw_text_ext(x - 1, y,     str, sep, w);
    draw_text_ext(x + 1, y,     str, sep, w);
    draw_text_ext(x - 1, y + 1, str, sep, w);
    draw_text_ext(x,     y + 1, str, sep, w);
    draw_text_ext(x + 1, y + 1, str, sep, w);
    draw_set_color(_fill);
    draw_text_ext(x, y, str, sep, w);
}

// =============================================================================
// scr_ui.gml
// Combat HUD drawing functions for Ironwake.
// Room dimensions: 1920 x 1080 (GUI layer).
//
// All functions are pure draw calls - they read state but never mutate it.
// Call every function from a Draw event (or a dedicated Draw GUI event).
//
// Draw call order in ui_draw_combat_hud():
//   1. Turn queue          - top-center
//   2. Player HP bar       - top-left
//   3. Energy pips         - below HP bar
//   4. Secondary resource  - below energy pips
//   5. Level + XP bar      - below secondary resource
//   6. Player buff icons   - below XP bar
//   7. Ability buttons     - bottom-center
//   8. Combat log          - bottom-left
//   9. Telegraph warning   - overlaid at top (only when active)
//  Enemy debuff icons are drawn in Draw_64.gml below each enemy HP bar.
// =============================================================================

// ---------------------------------------------------------------------------
// ui_item_stat_str(item)
// Builds a compact stat string from an item's primary stat + affixes array.
// E.g.  "+4 STR   +2 CON  +5% Crit"
// ---------------------------------------------------------------------------
function ui_item_stat_str(item) {
    // Weapons lead with their flat reach-gated damage ("+N dmg"); any +stat the weapon
    // also carries (global, as before) follows it. Non-weapons just show their stat.
    var _s = "";
    if (variable_struct_exists(item, "weapon_damage") && item.weapon_damage > 0) {
        // Caster ranged weapons name their rolled school ("+3 Fire dmg"); martial
        // weapons stay untyped (physical).
        var _wpn_sch = (variable_struct_exists(item, "wpn_school") && item.wpn_school != "")
            ? (school_label(item.wpn_school) + " ") : "";
        _s = "+" + string(item.weapon_damage) + " " + _wpn_sch + "dmg";
        // Tag the weapon's reach (Melee/Ranged - which abilities its damage feeds)
        // and hand requirement so the role and the offhand trade-off are both visible.
        var _reach_word = (variable_struct_exists(item, "slot") && item.slot == "ranged_weapon") ? "Ranged" : "Melee";
        var _hand_word  = (variable_struct_exists(item, "two_handed") && item.two_handed) ? "2H" : "1H";
        _s += "  (" + _reach_word + ", " + _hand_word + ")";
    }
    if (variable_struct_exists(item, "stat_value") && variable_struct_exists(item, "stat_name") && item.stat_value != 0) {
        _s += (_s == "" ? "" : "   ") + "+" + string(item.stat_value) + " " + item.stat_name;
    }
    if (variable_struct_exists(item, "affixes")) {
        var _aff = item.affixes;
        var _alen = array_length(_aff);
        for (var _i = 0; _i < _alen; _i++) {
            var _af = _aff[_i];
            var _asn = _af.stat_name;
            // Signed formatting (07-28): CURSED relics carry NEGATIVE affixes -
            // a bare "+" prefix printed "+-12". Negative values keep their own sign.
            var _sv = ((_af.stat_value >= 0) ? "+" : "") + string(_af.stat_value);
            if (_asn == "bonus_max_hp") {
                _s += "   " + _sv + " HP";
            } else if (_asn == "crit_flat") {
                _s += "   " + _sv + "% Crit (all)";     // 07-31: unlabeled crit read ambiguous
            } else if (_asn == "crit_spell") {
                _s += "   " + _sv + "% Spell Crit";
            } else if (_asn == "crit_phys") {
                _s += "   " + _sv + "% Phys Crit";
            } else if (_asn == "dodge_flat") {
                _s += "   " + _sv + " Dodge";
            } else if (_asn == "gold_find") {
                _s += "   " + _sv + "% Gold";
            } else if (_asn == "armor") {
                _s += "   " + _sv + " Armor";        // capitalized (M 07-31)
            } else if (_asn == "el_resist") {
                _s += "   " + _sv + " Elem. Resist";
            } else if (string_copy(_asn, 1, 7) == "school_") {
                // "+X <school> damage" gear affix (SYSTEMS_ELEMENT_SCHOOLS.md §C).
                var _sch_name = string_copy(_asn, 8, string_length(_asn) - 7);
                _s += "   " + _sv + " " + school_label(_sch_name) + " dmg";
            } else {
                _s += "   " + _sv + " " + _asn;
            }
        }
    }
    // Elemental affix (small elemental damage + a setup status). (§C)
    if (variable_struct_exists(item, "elem_affix") && item.elem_affix != undefined) {
        var _eaff_slot = variable_struct_exists(item, "slot") ? item.slot : "";
        _s += (_s == "" ? "" : "   ") + elem_affix_describe(item.elem_affix, _eaff_slot);
    }
    // Weight-class baseline Armor / El Resist (M-approved 07-31): inherent to the
    // piece (plate/leather/cloth), shown apart from rolled affixes.
    var _ba = item_base_armor(item);
    if (_ba > 0) _s += (_s == "" ? "" : "   ") + "+" + string(_ba) + " Armor";
    var _be = item_base_el_resist(item);
    if (_be > 0) _s += (_s == "" ? "" : "   ") + "+" + string(_be) + " Elem. Resist";
    // Tempering + dormancy tags (08-04): quality below 100 and dormancy scale
    // the item's positives at APPLICATION time - the numbers above are the
    // item's full rolls, so say what the piece is delivering right now.
    if (variable_struct_exists(item, "dormant") && item.dormant)
        _s += (_s == "" ? "" : "   ") + "[DORMANT - Maren can wake it]";
    // FINISH: quality's own flat HP grant - only ever nonzero once the smith has
    // actually tempered the piece (quality_base gate, M 08-11). Shown plain: the
    // "(Tempering bonus)" tag was clutter; the before/after story lives in Dorn's
    // temper preview, the stat line just states what the piece grants.
    var _fin = item_finish_hp(item);
    if (_fin > 0) _s += (_s == "" ? "" : "   ") + "+" + string(_fin) + " HP";
    if (variable_struct_exists(item, "quality") && item.quality < 100)
        _s += (_s == "" ? "" : "   ") + "[Quality " + string(item.quality) + "%]";
    return _s;
}

// ---------------------------------------------------------------------------
// ui_draw_stat_line_fit(x, y, txt, max_w)
// Draws a pre-built stat string on ONE line, shrinking it uniformly only when it's
// wider than max_w, so a busy many-affix item can't overrun a row's right-hand
// column (loot screen, equip picker). Caller sets font/colour/halign first.
// ---------------------------------------------------------------------------
function ui_draw_stat_line_fit(x, y, txt, max_w) {
    var _w = string_width(txt);
    if (_w > max_w && _w > 0) {
        var _sc = max_w / _w;
        draw_text_transformed(x, y, txt, _sc, _sc, 0);
    } else {
        draw_text(x, y, txt);
    }
}

// STAT STRINGS STACK, THEY DON'T SHRINK (M 08-11 screenshot: a long affix
// line shrink-to-fitted into unreadably small text - "insanely small on
// mobile... stack it vertically not just horizontally to preserve text size").
// One line when it fits (y_one - the classic layout); otherwise a 2-line wrap
// at (near-)full size anchored at y_two, only easing down to 80% if even two
// wrapped lines overflow. Returns the line count so the caller can yield the
// flavor line's room when the second line needs it.
function ui_draw_stat_str_stack(x, y_one, y_two, txt, max_w) {
    if (string_width(txt) <= max_w) { draw_text(x, y_one, txt); return 1; }
    var _sc = 1.0;
    while (_sc > 0.80 && string_height_ext(txt, 24, max_w / _sc) * _sc > 56) _sc -= 0.05;
    draw_text_ext_transformed(x, y_two, txt, 24, max_w / _sc, _sc, _sc, 0);
    return 2;
}

// ---------------------------------------------------------------------------
// ui_str_hash(s) - small deterministic string hash (djb-ish, 31-mult). Used to
// give Rare+ swords a STABLE per-item-identity icon (same base name -> same icon).
// ---------------------------------------------------------------------------
function ui_str_hash(s) {
    var _h = 0;
    for (var _i = 1; _i <= string_length(s); _i++) {
        _h = ((_h * 31) + ord(string_char_at(s, _i))) & 0x7fffffff;
    }
    return _h;
}

// ---------------------------------------------------------------------------
// ui_icon_seq_index(bucket_id, base_name, len)
// Collision-minimizing icon variant pick (07-29, M: two items sharing the same
// art reads as "false depth"). The old hash pick let different names collide
// on the same variant; now every base item gets the NEXT free slot in its
// bucket, assigned in CATALOG ORDER on first use (ui_icon_assign_warm walks
// the codex master list) so the mapping is deterministic across sessions.
// The raw sequence is stored and wrapped mod len at READ time, so importing
// more variant art later automatically re-spreads without code changes.
// With more items than sprites a shared icon is pigeonhole-unavoidable -
// the sequential walk guarantees the minimum possible number of shares.
// ---------------------------------------------------------------------------
// ui_icon_item_key(item) - the stable per-item identity used for variant
// hashing. 08-04 (M): transforming an item RE-ROLLS its look - services that
// remake the piece (re-attune, cursed rebirth, future tempering/awakening)
// stamp a saved `icon_seed`, which shifts this key so the item claims a fresh
// variant slot and keeps it forever after. Unseeded items keep their original
// look (no visual churn on existing saves).
function ui_icon_item_key(item) {
    var _k = string_lower(item_base_name(item));
    if (variable_struct_exists(item, "icon_seed")) _k += "#" + string(item.icon_seed);
    return _k;
}

function ui_icon_seq_index(bucket_id, base_name, len) {
    if (len <= 1) return 0;
    if (!variable_global_exists("__icon_assign") || !is_struct(global.__icon_assign)) {
        global.__icon_assign = {};
        global.__icon_assign_ready = false;
    }
    if (!global.__icon_assign_ready) ui_icon_assign_warm();
    var _b;
    if (!variable_struct_exists(global.__icon_assign, bucket_id)) {
        _b = { count: 0, map: {} };
        variable_struct_set(global.__icon_assign, bucket_id, _b);
    } else {
        _b = variable_struct_get(global.__icon_assign, bucket_id);
    }
    var _k = string_lower(base_name);
    if (variable_struct_exists(_b.map, _k)) return variable_struct_get(_b.map, _k) mod len;
    var _idx = _b.count;
    variable_struct_set(_b.map, _k, _idx);
    _b.count += 1;
    return _idx mod len;
}

// Walks the codex master list once, in catalog order, resolving every bucket-
// picked icon so each base item claims its sequence slot deterministically.
// Ready-flag is set BEFORE the walk: the resolvers below call back into
// ui_icon_seq_index and must register, not re-warm. Items with dedicated art
// (legendary unique_effects, the named-unique overrides) never reach a bucket
// picker, so they are skipped rather than wasting a slot.
function ui_icon_assign_warm() {
    if (variable_global_exists("__icon_assign_warming") && global.__icon_assign_warming) return;
    global.__icon_assign_warming = true;
    global.__icon_assign_ready   = true;
    var _lst = item_codex_master_list();
    for (var _i = 0; _i < array_length(_lst); _i++) {
        var _it = _lst[_i];
        if (codex_entry_is_header(_it)) continue;
        if (variable_struct_exists(_it, "unique_effect") && _it.unique_effect != "") continue;
        var _bn = item_base_name(_it);
        if (_bn == "Void Scepter" || _bn == "Stormcaller Staff" || _bn == "Runed Scepter"
            || _bn == "Crystal Wand" || _bn == "Vaultwood Bow") continue;
        switch (_it.slot) {
            case "weapon": case "ranged_weapon": ui_weapon_icon_sprite(_it); break;
            case "chest":  ui_chest_icon_sprite(_it);  break;
            case "helm":   ui_helm_icon_sprite(_it);   break;
            case "gloves": ui_gloves_icon_sprite(_it); break;
            case "boots":  ui_boots_icon_sprite(_it);  break;
            // offhand / ring / amulet resolve to single keyword sprites - no buckets
        }
    }
    global.__icon_assign_warming = false;
}

// ---------------------------------------------------------------------------
// ui_sentence(s) - sentence-case a flavor/description fragment for STANDALONE
// display: capitalize the first letter (skipping leading quotes/spaces) and
// append a period when the text doesn't already end in punctuation. The
// authored catalog strings stay lowercase fragments; this normalizes them only
// at the render sites where they stand alone as their own line. (Sentence-cap
// copy-edit, deferred 2026-06-30, executed 2026-07-06. Ability descriptions
// are deliberately NOT routed through this - the 07-05 flavor pass hand-set
// their formatting.)
// ---------------------------------------------------------------------------
function ui_sentence(s) {
    if (!is_string(s) || s == "") return s;
    var _len = string_length(s);
    // First letter: skip leading quote marks and whitespace.
    var _i = 1;
    while (_i <= _len) {
        var _c = string_char_at(s, _i);
        if (_c != "\"" && _c != "'" && _c != " ") break;
        _i++;
    }
    if (_i <= _len) {
        s = string_copy(s, 1, _i - 1) + string_upper(string_char_at(s, _i))
            + string_copy(s, _i + 1, _len - _i);
    }
    // Terminal punctuation (closing quotes/parens count as already terminated).
    var _last = string_char_at(s, string_length(s));
    if (string_pos(_last, ".!?\"')") == 0) s += ".";
    return s;
}

// ---------------------------------------------------------------------------
// ui_weapon_icon_sprite(item)
// Returns the asset index of the correct weapon icon based on name keywords.
// Keyword map: wand/focus/scepter->wand, bow->bow, sickle->sickle, spear/reach->spear.
// Everything else is treated as a sword (also the default). Rare+ swords (rarity
// >= 2) get a theme- and rarity-specific icon via ui_sword_icon_rare; Common/
// Uncommon swords keep the single plain spr_icon_weapon_sword.
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// ui_icon_proxy(item) - Pattern Book art assignment (08-11): a crafted item
// carrying `icon_as` LOOKS like the unlocked art it was assigned. The proxy
// struct holds name/base_name/rarity/icon_seed - everything the resolvers
// read - so every icon draw site inherits the assigned face for free. Items
// without icon_as pass through untouched.
// ---------------------------------------------------------------------------
function ui_icon_proxy(item) {
    if (is_struct(item) && variable_struct_exists(item, "icon_as") && is_struct(item.icon_as)) return item.icon_as;
    return item;
}

function ui_weapon_icon_sprite(item) {
    item = ui_icon_proxy(item);
    var _n = string_lower(item.name);
    // Caster weapons (incl. staff/rod) use the wand icon. Without staff/rod here a
    // "Stormcaller Staff" fell through to the sword bucket and showed a sword icon
    // (looked like a melee sword even though it's a ranged caster weapon). Matches
    // weapon_required_stat's caster keyword list.
    // Each family has 3 icon variants (base / _b / _c) so weapons of the same type
    // don't all look identical. ui_weapon_icon_variant hashes the base name to pick
    // one deterministically (a given weapon always shows the same icon).
    if (string_pos("wand",   _n) > 0 || string_pos("focus", _n) > 0 || string_pos("scepter", _n) > 0
        || string_pos("staff", _n) > 0 || string_pos("rod", _n) > 0) {
        var _wb = [spr_icon_weapon_wand, spr_icon_weapon_wand_b, spr_icon_weapon_wand_c,
                   spr_icon_weapon_wand_d, spr_icon_weapon_wand_e];
        // 07-29 collision pass: 6 catalog wands over 5 sprites - the 6th
        // variant joins by string once its art is imported (+__sprite_includes).
        var _wf = asset_get_index("spr_icon_weapon_wand_f");
        if (_wf != -1 && sprite_exists(_wf)) array_push(_wb, _wf);
        return ui_weapon_icon_variant(item, _wb, "wpn_wand");
    }
    if (string_pos("bow",    _n) > 0)
        return ui_weapon_icon_variant(item, [spr_icon_weapon_bow, spr_icon_weapon_bow_b, spr_icon_weapon_bow_c,
                                             spr_icon_weapon_bow_d, spr_icon_weapon_bow_e], "wpn_bow");
    if (string_pos("sickle", _n) > 0)
        return ui_weapon_icon_variant(item, [spr_icon_weapon_sickle, spr_icon_weapon_sickle_b,
                                             spr_icon_weapon_sickle_c, spr_icon_weapon_sickle_d], "wpn_sickle");
    if (string_pos("spear",  _n) > 0 || string_pos("reach", _n) > 0)
        return ui_weapon_icon_variant(item, [spr_icon_weapon_spear, spr_icon_weapon_spear_b, spr_icon_weapon_spear_c,
                                             spr_icon_weapon_spear_d, spr_icon_weapon_spear_e], "wpn_spear");

    var _rar = variable_struct_exists(item, "rarity") ? item.rarity : 0;
    if (_rar >= 2) return ui_sword_icon_rare(item, _n, _rar);
    // Common/Uncommon swords pick from the plain steel variants.
    var _swb = [spr_icon_weapon_sword, spr_icon_weapon_sword_b, spr_icon_weapon_sword_c,
                spr_icon_weapon_sword_d, spr_icon_weapon_sword_e];
    // 07-29 collision pass: 6 c/u blades over 5 sprites - see the wand note.
    var _swf = asset_get_index("spr_icon_weapon_sword_f");
    if (_swf != -1 && sprite_exists(_swf)) array_push(_swb, _swf);
    return ui_weapon_icon_variant(item, _swb, "wpn_sword_cu");
}

// ui_weapon_icon_variant(item, bucket, bucket_id) - deterministically pick one
// icon from a family's variant list. Sequence-assigned in catalog order
// (ui_icon_seq_index) so different weapons never share a variant while sprites
// last; a given weapon always shows the same icon.
function ui_weapon_icon_variant(item, bucket, bucket_id) {
    var _len = array_length(bucket);
    if (_len <= 1) return bucket[0];
    return bucket[ui_icon_seq_index(bucket_id, ui_icon_item_key(item), _len)];
}

// ui_armor_icon_variant(item, key_base) - RARITY-BANDED armor variant buckets
// (M 07-29 loot-variance pass: every robe showed THE SAME robe icon; art now
// splits by rarity - "less intricate more plain the lower rarity, more
// intricate more elaborate exotic higher rarity").
// Variant sprites are named <key>_<band>[n]: bands c/u/r/e/l for the 5
// rarities, n = 2,3,... for extra members (e.g. spr_icon_chest_robe_l2).
// The item's rarity picks its band; empty bands fall DOWN toward common so
// every rarity always resolves. The family's original base sprite joins its
// own intricacy band (home map below). Within a band, a hash of the base name
// spreads different items across members while a given item stays stable.
// String-resolved: compiles before the art lands; new variants appear once
// imported + listed in global.__sprite_includes. -1 = no variant art yet.
function ui_armor_icon_variant(item, key_base) {
    var _rar   = clamp(variable_struct_exists(item, "rarity") ? item.rarity : 0, 0, 4);
    var _bands = ["c", "u", "r", "e", "l"];
    // The pre-existing family art's own intricacy band (ornate robe/plate read
    // as Rare-grade; plain leather/hood read as Common).
    var _home = 0;
    switch (key_base) {
        case "spr_icon_chest_robe":  _home = 2; break;
        case "spr_icon_chest_plate": _home = 2; break;
    }
    var _list = [];
    var _band_used = "";
    for (var _bi = _rar; _bi >= 0 && array_length(_list) == 0; _bi--) {
        for (var _n = 1; _n <= 8; _n++) {
            var _s = asset_get_index(key_base + "_" + _bands[_bi] + ((_n == 1) ? "" : string(_n)));
            if (_s != -1 && sprite_exists(_s)) array_push(_list, _s);
        }
        if (array_length(_list) > 0) _band_used = _bands[_bi];
        if (_bi == _home && array_length(_list) > 0) {
            var _b0 = asset_get_index(key_base);
            if (_b0 != -1 && sprite_exists(_b0)) array_push(_list, _b0);
        }
    }
    var _len = array_length(_list);
    if (_len == 0) {
        // Family has no banded variants yet (void chest, circlet, gloves,
        // boots...) - resolve the plain themed key like the pre-band code did,
        // or the themed art would silently fall through to the generic icon.
        var _plain = asset_get_index(key_base);
        if (_plain != -1 && sprite_exists(_plain)) return _plain;
        return -1;
    }
    if (_len == 1) return _list[0];
    return _list[ui_icon_seq_index(key_base + "|" + _band_used, ui_icon_item_key(item), _len)];
}

// ---------------------------------------------------------------------------
// ui_sword_icon_rare(item, _n, _rar)
// Picks an individual sword icon for a Rare+ sword. Theme comes from the base
// name (the elemental identity - affixes are stat words, not elements) with the
// full affixed name adding tints (Ghost->void, Gilded->radiant, Arcane/Runed->arcane).
// Within a theme, rarity selects the lower (Rare) or upper (Epic+) half of the
// theme's icon list - fancier art at higher rarity - and a base-name hash picks
// within that half so each distinct sword always shows the same icon.
// ---------------------------------------------------------------------------
function ui_sword_icon_rare(item, _n, _rar) {
    var _base = string_lower(item_base_name(item));
    var _bucket; var _theme;
    if      (string_pos("ash",    _base) > 0 || string_pos("ember", _n) > 0 || string_pos("flame", _n) > 0
          || string_pos("scorch", _n) > 0    || string_pos("sear",  _n) > 0 || string_pos("magma", _n) > 0
          || string_pos("cinder", _n) > 0    || string_pos("fire",  _n) > 0)
        { _bucket = [spr_icon_sword_fire_a, spr_icon_sword_fire_b, spr_icon_sword_fire_c]; _theme = "fire"; }
    else if (string_pos("frost", _n) > 0 || string_pos("froze", _n) > 0 || string_pos("ice",   _n) > 0
          || string_pos("glaci", _n) > 0 || string_pos("rime",  _n) > 0 || string_pos("chill", _n) > 0)
        { _bucket = [spr_icon_sword_frost_a, spr_icon_sword_frost_b]; _theme = "frost"; }
    else if (string_pos("ghost",  _n) > 0 || string_pos("void",  _n) > 0 || string_pos("shadow", _n) > 0
          || string_pos("wraith", _n) > 0 || string_pos("abyss", _n) > 0)
        { _bucket = [spr_icon_sword_void_a, spr_icon_sword_void_b]; _theme = "void"; }
    else if (string_pos("vampir",  _n) > 0 || string_pos("blood", _n) > 0 || string_pos("sanguine", _n) > 0
          || string_pos("crimson", _n) > 0 || string_pos("gore",  _n) > 0 || string_pos("ruin",     _n) > 0)
        { _bucket = [spr_icon_sword_blood_a, spr_icon_sword_blood_b]; _theme = "blood"; }
    else if (string_pos("gild", _n) > 0 || string_pos("lucky",  _n) > 0 || string_pos("radian", _n) > 0
          || string_pos("holy", _n) > 0 || string_pos("divine", _n) > 0 || string_pos("sacred", _n) > 0)
        { _bucket = [spr_icon_sword_radiant_a, spr_icon_sword_radiant_b]; _theme = "radiant"; }
    else if (string_pos("arcane", _n) > 0 || string_pos("rune", _n) > 0 || string_pos("storm", _n) > 0)
        { _bucket = [spr_icon_sword_arcane_a, spr_icon_sword_arcane_b]; _theme = "arcane"; }
    else
        { _bucket = [spr_icon_sword_steel_a, spr_icon_sword_steel_b, spr_icon_sword_steel_c, spr_icon_sword_steel_d]; _theme = "steel"; }

    var _len  = array_length(_bucket);
    var _half = max(1, _len div 2);
    var _start, _size;
    if (_rar >= 3) { _start = _half; _size = _len - _half; }   // Epic+ -> upper (fancier) half
    else           { _start = 0;     _size = _half;        }   // Rare  -> lower half
    var _sw_bucket = "sword_" + _theme + ((_rar >= 3) ? "_hi" : "_lo");
    return _bucket[_start + ui_icon_seq_index(_sw_bucket, ui_icon_item_key(item), _size)];
}

// ---------------------------------------------------------------------------
// ui_offhand_icon_sprite(item)
// Off-hand subtype by name keyword (mirrors the weapon resolver) so off-hands
// no longer all read as one shield. Shields/bucklers/bulwarks use sliced
// medieval art; caster off-hands (totem/orb/stone/focus) use distinct arcane
// tome art. Falls back to the generic spr_icon_offhand for unmatched names.
// ---------------------------------------------------------------------------
function ui_offhand_icon_sprite(item) {
    item = ui_icon_proxy(item);
    var _n = string_lower(item.name);
    // _b = the 08-15 approved regen (M shot "messed up icon for shield");
    // the original spr_icon_offhand_buckler stays in the project untouched.
    if (string_pos("buckler",   _n) > 0)                                  return spr_icon_offhand_buckler_b;
    if (string_pos("bulwark",   _n) > 0 || string_pos("ironhide", _n) > 0) return spr_icon_offhand_bulwark_b;   // 08-15 regen (M-approved), original kept
    if (string_pos("shield",    _n) > 0 || string_pos("aegis",    _n) > 0) return spr_icon_offhand_shield_b;    // 08-15 regen (M-approved), original kept
    // 08-16 (M: "my offhand is Runed Soulbound Orb which shows a book"): the
    // shipped offhand icons were MISLABELED - spr_icon_offhand_orb / _totem /
    // _stone are all books. Orb / totem / focus now resolve to icons derived
    // 2:1 from the codex splash art (tools/import_offhand_icons_from_art_0816.py,
    // string-resolved so this compiles before the import lands; falls back to
    // the old sprite until then). Books stay for tome / grimoire only.
    if (string_pos("totem",     _n) > 0 || string_pos("idol",     _n) > 0) {
        var _tb = asset_get_index("spr_icon_offhand_totem_b");
        return (_tb >= 0 && sprite_exists(_tb)) ? _tb : spr_icon_offhand_totem;
    }
    if (string_pos("orb",       _n) > 0 || string_pos("sphere",   _n) > 0) {
        var _ob = asset_get_index("spr_icon_offhand_orb_b");
        return (_ob >= 0 && sprite_exists(_ob)) ? _ob : spr_icon_offhand_orb;
    }
    if (string_pos("soulstone", _n) > 0 || string_pos("fragment", _n) > 0 || string_pos("stone", _n) > 0) return spr_icon_offhand_stone_b;     // 08-15 regen (M-approved), original kept
    if (string_pos("tome",      _n) > 0 || string_pos("book",     _n) > 0 || string_pos("grimoire", _n) > 0) return spr_icon_offhand_focus;   // the purple book - the one true book icon
    if (string_pos("focus",     _n) > 0 || string_pos("runic",    _n) > 0) {
        var _fc = asset_get_index("spr_icon_offhand_focus_c");   // pedestal focus from the codex art
        return (_fc >= 0 && sprite_exists(_fc)) ? _fc : spr_icon_offhand_focus_b;
    }
    return spr_icon_offhand;
}

// ---------------------------------------------------------------------------
// ui_chest_icon_sprite(item)
// Chest subtype by name keyword so chests don't all read as one sprite (mirrors the
// off-hand/weapon resolvers). Resolves themed sprites BY STRING (asset_get_index) so
// it compiles + runs even before the art is imported - until those sprites exist it
// falls back to the single spr_icon_chest (current look), then upgrades automatically
// once the chest-icon art pass adds spr_icon_chest_{robe,plate,leather,void}.
// ---------------------------------------------------------------------------
function ui_chest_icon_sprite(item) {
    item = ui_icon_proxy(item);
    var _n = string_lower(item.name);
    var _key = "";
    if      (string_pos("void", _n) > 0 || string_pos("shadowthread", _n) > 0 || string_pos("shadowcloth", _n) > 0)
        _key = "spr_icon_chest_void";
    else if (string_pos("robe", _n) > 0 || string_pos("vestment", _n) > 0 || string_pos("mantle", _n) > 0
          || string_pos("spellweave", _n) > 0 || string_pos("acolyte", _n) > 0)
        _key = "spr_icon_chest_robe";
    else if (string_pos("plate", _n) > 0 || string_pos("cuirass", _n) > 0 || string_pos("chain", _n) > 0
          || string_pos("brigandine", _n) > 0 || string_pos("ironveil", _n) > 0 || string_pos("aegis", _n) > 0)
        _key = "spr_icon_chest_plate";
    else if (string_pos("coat", _n) > 0 || string_pos("vest", _n) > 0 || string_pos("tunic", _n) > 0
          || string_pos("wrap", _n) > 0 || string_pos("robes", _n) > 0)
        _key = "spr_icon_chest_leather";
    if (_key != "") {
        var _s = ui_armor_icon_variant(item, _key);
        if (_s != -1) return _s;
    }
    return spr_icon_chest;   // graceful fallback until themed chest art is imported
}

// ---------------------------------------------------------------------------
// ui_helm_icon_sprite / ui_gloves_icon_sprite / ui_boots_icon_sprite
// Armor-piece subtype icons by name keyword (mirror the chest/off-hand resolvers).
// Themed sprites are resolved BY STRING so they compile + run before the art is
// imported, falling back to the single base sprite until then; once the icon art
// pass adds them, variety appears automatically. Keyword order = priority.
// ---------------------------------------------------------------------------
function ui_helm_icon_sprite(item) {
    item = ui_icon_proxy(item);
    var _n = string_lower(item.name);
    var _key = "";
    if      (string_pos("hood", _n) > 0 || string_pos("cowl", _n) > 0 || string_pos("hat", _n) > 0)
        _key = "spr_icon_helm_hood";
    else if (string_pos("circlet", _n) > 0 || string_pos("crown", _n) > 0 || string_pos("diadem", _n) > 0
          || string_pos("tiara", _n) > 0 || string_pos("coronet", _n) > 0)
        _key = "spr_icon_helm_circlet";
    else  // cap / skullcap / visor / helm / helmet / casque / circlet handled above
        _key = "spr_icon_helm_plate";
    var _s = ui_armor_icon_variant(item, _key);
    if (_s != -1) return _s;
    return spr_icon_helm;
}

function ui_gloves_icon_sprite(item) {
    item = ui_icon_proxy(item);
    var _n = string_lower(item.name);
    var _key = "";
    if      (string_pos("sage", _n) > 0 || string_pos("rune", _n) > 0 || string_pos("arcane", _n) > 0
          || string_pos("mage", _n) > 0 || string_pos("spell", _n) > 0)
        _key = "spr_icon_gloves_arcane";
    else if (string_pos("wrap", _n) > 0 || string_pos("nimble", _n) > 0 || string_pos("whisper", _n) > 0
          || string_pos("fleet", _n) > 0 || string_pos("silk", _n) > 0)
        _key = "spr_icon_gloves_cloth";
    else  // gauntlet / irongrip / crusher / iron / steel / plated default to heavy
        _key = "spr_icon_gloves_plate";
    var _s = ui_armor_icon_variant(item, _key);
    if (_s != -1) return _s;
    return spr_icon_gloves;
}

function ui_boots_icon_sprite(item) {
    item = ui_icon_proxy(item);
    var _n = string_lower(item.name);
    var _key = "";
    if      (string_pos("greave", _n) > 0 || string_pos("stomper", _n) > 0 || string_pos("ironshod", _n) > 0
          || string_pos("stoneguard", _n) > 0 || string_pos("colossus", _n) > 0 || string_pos("plate", _n) > 0
          || string_pos("stone", _n) > 0)
        _key = "spr_icon_boots_plate";
    else if (string_pos("wrap", _n) > 0 || string_pos("dustwalker", _n) > 0 || string_pos("sandal", _n) > 0
          || string_pos("cloth", _n) > 0 || string_pos("silk", _n) > 0)
        _key = "spr_icon_boots_cloth";
    else  // boots / treads / strider / step / leather default to leather
        _key = "spr_icon_boots_leather";
    var _s = ui_armor_icon_variant(item, _key);
    if (_s != -1) return _s;
    return spr_icon_boots;
}

// ---------------------------------------------------------------------------
// ui_ring_icon_sprite(item) / ui_amulet_icon_sprite(item)
// Per-identity jewelry icons by name keyword (themed art from the accessories
// pack). Keyword order matters where a name contains several keywords (e.g.
// "Wraithbone Signet" -> wraith, "Soul-Linked Talisman" -> soul). Fall back to
// the generic ring/amulet sprite for unmatched names.
// ---------------------------------------------------------------------------
function ui_ring_icon_sprite(item) {
    item = ui_icon_proxy(item);
    var _n = string_lower(item.name);
    if (string_pos("void",   _n) > 0)                                    return spr_icon_ring_void;
    // 07-29 icon-collision pass: split the pact/blood and venom/signet ties.
    // String-resolved so this compiles before the art lands; until then each
    // falls through to the old shared icon. Once imported, list the sprites in
    // global.__sprite_includes or the compiler strips them.
    if (string_pos("pact", _n) > 0) {
        var _sp = asset_get_index("spr_icon_ring_pact");
        if (_sp != -1 && sprite_exists(_sp)) return _sp;
    }
    if (string_pos("venom", _n) > 0) {
        var _sv = asset_get_index("spr_icon_ring_venom");
        if (_sv != -1 && sprite_exists(_sv)) return _sv;
    }
    if (string_pos("blood",  _n) > 0 || string_pos("pact",      _n) > 0) return spr_icon_ring_blood;
    if (string_pos("ember",  _n) > 0)                                    return spr_icon_ring_ember;
    if (string_pos("wraith", _n) > 0)                                    return spr_icon_ring_wraith;
    if (string_pos("bone",   _n) > 0)                                    return spr_icon_ring_bone;
    if (string_pos("signet", _n) > 0 || string_pos("copper",    _n) > 0) return spr_icon_ring_signet;
    if (string_pos("band",   _n) > 0 || string_pos("tarnished", _n) > 0) return spr_icon_ring_band;
    return spr_icon_ring;
}

function ui_amulet_icon_sprite(item) {
    item = ui_icon_proxy(item);
    var _n = string_lower(item.name);
    // 07-29 icon-collision pass: Emberheart Talisman gets its own art instead
    // of sharing the bone-talisman icon (string-resolved, see ring note above).
    if (string_pos("emberheart", _n) > 0) {
        var _se = asset_get_index("spr_icon_amulet_emberheart");
        if (_se != -1 && sprite_exists(_se)) return _se;
    }
    if (string_pos("eye",       _n) > 0)                                  return spr_icon_amulet_eye;
    if (string_pos("soul",      _n) > 0)                                  return spr_icon_amulet_soul;
    if (string_pos("medallion", _n) > 0)                                  return spr_icon_amulet_medallion;
    if (string_pos("sentry",    _n) > 0 || string_pos("pendant",  _n) > 0) return spr_icon_amulet_sentry;
    if (string_pos("chain",     _n) > 0 || string_pos("silver",   _n) > 0) return spr_icon_amulet_chain;
    if (string_pos("bone",      _n) > 0 || string_pos("talisman", _n) > 0) return spr_icon_amulet_bone;
    if (string_pos("dusty",     _n) > 0)                                  return spr_icon_amulet_dusty;
    return spr_icon_amulet;
}

// ---------------------------------------------------------------------------
// awakening_label() - formatted "Awakening A2 - Brutal" string for the current
// run difficulty (global.selected_ascendance). Mirrors the hub's tier names so
// the combat and dungeon screens can show the awakening tier as a reference.
// ---------------------------------------------------------------------------
function awakening_label() {
    var _asc = variable_global_exists("selected_ascendance") ? global.selected_ascendance : 0;
    // THE DESCENT: the fractional floor-scaled tier reads as its floor.
    if (variable_global_exists("descent_active") && global.descent_active) {
        return "THE DESCENT - Floor " + string(variable_global_exists("descent_floor") ? global.descent_floor : 1);
    }
    // A6+ endless tiers (SYSTEMS_ENDLESS.md §2).
    if (_asc > 5) return "Awakening A" + string(round(_asc)) + " - Beyond Infernal";
    var _names = ["Normal", "Hardened", "Brutal", "Relentless", "Nightmare", "Infernal"];
    _asc = clamp(_asc, 0, array_length(_names) - 1);
    return "Awakening A" + string(_asc) + " - " + _names[_asc];
}

// ---------------------------------------------------------------------------
// ui_draw_item_icon(x, y, sz, item, framed)
// Draws a pixel art icon for the item, scaled to szxsz.
// Legendary items are detected via unique_effect; weapons use name-keyword
// subtype detection. Falls back to colored box + abbreviation if the sprite
// has not yet been imported into the project.
// framed=false skips the dark panel + rarity border - for callers that already
// drew their own frame around the icon (#10: the codex art box drew an icon
// box INSIDE its box for splash-less items like Chipped Spear).
// ---------------------------------------------------------------------------
function ui_draw_item_icon(x, y, sz, item, framed = true) {
    var _slot = variable_struct_exists(item, "slot") ? item.slot : "";
    var _rar  = variable_struct_exists(item, "rarity") ? item.rarity : 0;
    var _rcol = item_rarity_color(_rar);

    // Resolve icon sprite - -1 means "not found, use fallback"
    var _spr = -1;

    // Legendary items identified by unique_effect field.
    // CURSED items skip their dedicated art (M 08-04 screenshot: the Brand's
    // pinned legendary icon ignored the reforge seed and "reverted") - the
    // dark REMADE the piece, so it falls to the seeded variant buckets below.
    // Tempering/awakening never unpins dedicated art; only the curse does.
    if (variable_struct_exists(item, "unique_effect")
        && !(variable_struct_exists(item, "cursed") && item.cursed)) {
        switch (item.unique_effect) {
            case "gatewarden_brand":  _spr = spr_icon_legendary_gatewarden_brand;  break;
            case "heartstone_aegis":  _spr = spr_icon_legendary_heartstone_aegis;  break;
            case "crown_hollow_king": _spr = spr_icon_legendary_crown_hollow_king; break;
            case "thief_of_hours":    _spr = spr_icon_legendary_thief_of_hours;    break;
            // 07-28 expansion (imported via tools/import_legendary_icons.py -
            // Batareya pack picks + themed accessories + bare-art gens).
            case "duelists_rebuke":     _spr = spr_icon_legendary_duelists_rebuke;     break;
            case "gravewalker_treads":  _spr = spr_icon_legendary_gravewalker_treads;  break;
            case "sanguine_chalice":    _spr = spr_icon_legendary_sanguine_chalice;    break;
            case "stormcallers_loop":   _spr = spr_icon_legendary_stormcallers_loop;   break;
            case "misers_blade":        _spr = spr_icon_legendary_misers_blade;        break;
            case "veil_patient_dark":   _spr = spr_icon_legendary_veil_patient_dark;   break;
            case "longshots_memory":    _spr = spr_icon_legendary_longshots_memory;    break;
            case "aegis_unbroken_line": _spr = spr_icon_legendary_aegis_unbroken_line; break;
            case "hollow_kings_signet": _spr = spr_icon_legendary_hollow_kings_signet; break;
            case "ember_saints_censer": _spr = spr_icon_legendary_ember_saints_censer; break;
            case "oathbreakers_shard":  _spr = spr_icon_legendary_oathbreakers_shard;  break;
            case "lantern_last_door":   _spr = spr_icon_legendary_lantern_last_door;   break;
            case "crownfire_diadem":    _spr = spr_icon_legendary_crownfire_diadem;    break;
            case "beggars_fortune":     _spr = spr_icon_legendary_beggars_fortune;     break;
            case "kindled_reliquary":   _spr = spr_icon_legendary_kindled_reliquary;   break;
            // 08-12 batch (M approved all): dueling relics + mutator legendaries.
            case "duel_pin":            _spr = spr_icon_legendary_duel_pin;            break;
            case "duel_parry":          _spr = spr_icon_legendary_duel_parry;          break;
            case "duel_widow":          _spr = spr_icon_legendary_duel_widow;          break;
            case "stormskip_band":      _spr = spr_icon_legendary_stormskip_band;      break;
            case "twinned_prism":       _spr = spr_icon_legendary_twinned_prism;       break;
            case "second_toll":         _spr = spr_icon_legendary_second_toll;         break;
            case "smolderbrand":        _spr = spr_icon_legendary_smolderbrand;        break;
        }
    }

    // Named-unique overrides (2026-07-06 icon pass): dedicated art for authored
    // caster/ranged pieces that the keyword resolvers could only serve generically.
    // Base name (affix prefixes/suffixes stripped); string-refs live in
    // global.__sprite_includes so the compiler keeps the sprites.
    if (_spr == -1) {
        switch (item_base_name(item)) {
            case "Void Scepter":      _spr = asset_get_index("spr_icon_unique_void_scepter");      break;
            case "Stormcaller Staff": _spr = asset_get_index("spr_icon_unique_stormcaller_staff"); break;
            case "Runed Scepter":     _spr = asset_get_index("spr_icon_unique_runed_scepter");     break;
            case "Crystal Wand":      _spr = asset_get_index("spr_icon_unique_crystal_wand");      break;
            case "Vaultwood Bow":     _spr = asset_get_index("spr_icon_unique_vaultwood_bow");     break;
        }
    }

    // Equipment slots (weapons dispatch to subtype helper)
    if (_spr == -1) {
        switch (_slot) {
            case "weapon":        _spr = ui_weapon_icon_sprite(item);  break;
            case "ranged_weapon": _spr = ui_weapon_icon_sprite(item);  break;
            case "offhand": _spr = ui_offhand_icon_sprite(item); break;
            case "helm":    _spr = ui_helm_icon_sprite(item);    break;
            case "chest":   _spr = ui_chest_icon_sprite(item);   break;
            case "gloves":  _spr = ui_gloves_icon_sprite(item);  break;
            case "boots":   _spr = ui_boots_icon_sprite(item);   break;
            case "amulet":  _spr = ui_amulet_icon_sprite(item);  break;
            case "ring":    _spr = ui_ring_icon_sprite(item);    break;
        }
    }

    // Force full opacity - icons are never meant to inherit a caller's dimmed alpha
    draw_set_alpha(1.0);

    // Dark panel background + rarity border (skipped when the caller framed it, #10)
    if (framed) {
        draw_set_color(make_color_rgb(12, 14, 22));
        draw_rectangle(x, y, x + sz, y + sz, false);
        draw_set_color(_rcol);
        draw_rectangle(x, y, x + sz, y + sz, true);
    }

    if (_spr != -1 && sprite_exists(_spr)) {
        // 2px inset keeps the rarity border visible around the icon
        draw_sprite_stretched(_spr, 0, x + 2, y + 2, sz - 4, sz - 4);
    } else {
        // Fallback: slot-colored fill + 3-letter abbreviation
        var _bg;
        switch (_slot) {
            case "weapon":        _bg = make_color_rgb(80, 38, 38); break;
            case "ranged_weapon": _bg = make_color_rgb(80, 56, 30); break;
            case "offhand": _bg = make_color_rgb(32, 55, 80); break;
            case "helm":    _bg = make_color_rgb(48, 48, 72); break;
            case "chest":   _bg = make_color_rgb(36, 58, 44); break;
            case "gloves":  _bg = make_color_rgb(62, 50, 32); break;
            case "boots":   _bg = make_color_rgb(44, 38, 62); break;
            case "amulet":  _bg = make_color_rgb(66, 38, 66); break;
            case "ring":    _bg = make_color_rgb(70, 58, 22); break;
            default:        _bg = make_color_rgb(36, 38, 52); break;
        }
        var _abbrev;
        switch (_slot) {
            case "weapon":        _abbrev = "MEL"; break;
            case "ranged_weapon": _abbrev = "RWP"; break;
            case "offhand": _abbrev = "OFF"; break;
            case "helm":    _abbrev = "HLM"; break;
            case "chest":   _abbrev = "CHT"; break;
            case "gloves":  _abbrev = "GLV"; break;
            case "boots":   _abbrev = "BTS"; break;
            case "amulet":  _abbrev = "AMU"; break;
            case "ring":    _abbrev = "RNG"; break;
            default:        _abbrev = "???"; break;
        }
        draw_set_color(_bg);
        draw_rectangle(x + 1, y + 1, x + sz - 1, y + sz - 1, false);
        draw_set_halign(fa_center);
        draw_set_valign(fa_middle);
        draw_set_color(make_color_rgb(195, 200, 220));
        draw_text_transformed(x + sz / 2, y + sz / 2, _abbrev, 0.68, 0.68, 0);
        draw_set_halign(fa_left);
        draw_set_valign(fa_top);
    }
}

// ---------------------------------------------------------------------------
// ui_consumable_icon_sprite(cname)
// Returns the asset index of a consumable's icon by exact name, or -1 if none.
// Exact-match (not substring) is required: "Greater Healing Salve" contains
// "Healing Salve" and would otherwise collide.
// ---------------------------------------------------------------------------
function ui_consumable_icon_sprite(cname, item = undefined) {
    switch (cname) {
        case "Healing Salve":         return spr_icon_consumable_healing_salve;
        case "Antidote":              return spr_icon_consumable_antidote;
        case "Energy Tonic":          return spr_icon_consumable_energy_tonic;
        case "Smelling Salts":        return spr_icon_consumable_smelling_salts;
        case "Greater Healing Salve": return spr_icon_consumable_greater_healing_salve;
        case "Purification Draught":  return spr_icon_consumable_purification_draught;
        case "Adrenaline Vial":       return spr_icon_consumable_adrenaline_vial;
        case "Warden's Tonic":        return spr_icon_consumable_wardens_tonic;
        // Sable's brewed (alchemy-exclusive) potions.
        case "Aegis Draught":          return spr_icon_consumable_aegis_draught;
        case "Master Healing Draught": return spr_icon_consumable_master_healing_draught;
        case "Phoenix Tonic":          return spr_icon_consumable_phoenix_tonic;
        case "Cleansing Philter":      return spr_icon_consumable_cleansing_philter;
        case "Ley Battery":            return spr_icon_consumable_ley_battery;
        case "Goldfinger Elixir":      return spr_icon_consumable_goldfinger_elixir;
        case "Faerie's Tear":          return spr_icon_consumable_faeries_tear;
        // Devil Wine: string-ref so the game compiles before/without the sprite
        // import (asset_get_index returns -1, matching this function's contract).
        // Once imported, the sprite must sit in global.__sprite_includes or the
        // compiler strips it (string-only refs; see the pet-sprite note there).
        case "Devil Wine":             return asset_get_index("spr_icon_consumable_devil_wine");
        case "Genie Lamp":             return asset_get_index("spr_icon_consumable_genie_lamp");
        // Chaotic Brew (M 07-28: "its own fun chaotic icon of smashing potions
        // together"). 08-15 batch added 3 bottle VARIANTS (a swirl / a split /
        // lashed vials) - a cheap desc hash keys a stable per-brew pick, so the
        // same brew always wears the same bottle. Name-only callers (no struct)
        // fall back to the original shared icon.
        case "Chaotic Brew": {
            // Instances carry .description, catalog defs .desc (see
            // ui_draw_consumable_entry) - hash whichever is present.
            var _cbd = (item != undefined && is_struct(item))
                     ? (variable_struct_exists(item, "description") ? item.description
                     : (variable_struct_exists(item, "desc") ? item.desc : ""))
                     : "";
            if (_cbd != "") {
                var _cbh = 0;
                var _cbs = string(_cbd);
                for (var _cbi = 1; _cbi <= string_length(_cbs); _cbi++) _cbh += ord(string_char_at(_cbs, _cbi));
                switch (_cbh mod 3) {
                    case 0:  return spr_icon_consumable_chaotic_brew_a;
                    case 1:  return spr_icon_consumable_chaotic_brew_b;
                    default: return spr_icon_consumable_chaotic_brew_c;
                }
            }
            return asset_get_index("spr_icon_consumable_chaotic_brew");
        }
        // Lesser Aegis Draught (07-28, M: "a rock potion cause its a shield") -
        // string-ref, listed in global.__sprite_includes.
        case "Lesser Aegis Draught":   return asset_get_index("spr_icon_consumable_lesser_aegis");
        // VALUABLES (08-18): sell-only trinkets - string-refs so the game compiles before
        // the icon import; once imported they sit in global.__sprite_includes.
        case "Tarnished Locket":         return asset_get_index("spr_icon_valuable_tarnished_locket");
        case "Silver Reliquary":         return asset_get_index("spr_icon_valuable_silver_reliquary");
        case "Sovereign's Signet":       return asset_get_index("spr_icon_valuable_sovereigns_signet");
        case "Star-Iron Idol":           return asset_get_index("spr_icon_valuable_star_iron_idol");
        case "Crown Shard of Ironwake":  return asset_get_index("spr_icon_valuable_crown_shard");
    }
    return -1;
}

// ---------------------------------------------------------------------------
// ui_draw_consumable_icon(x, y, sz, item)
// Cyan-bordered icon badge for a consumable, mirroring ui_draw_item_icon's look
// for gear. Draws the consumable's sprite if one exists, else just the panel.
// Used by shops and the loot screen so consumables show icons like gear does.
// ---------------------------------------------------------------------------
function ui_draw_consumable_icon(x, y, sz, item) {
    var _cname = variable_struct_exists(item, "name") ? item.name : "";
    var _spr   = ui_consumable_icon_sprite(_cname, item);   // struct enables brew variants
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(12, 14, 22));
    draw_rectangle(x, y, x + sz, y + sz, false);
    draw_set_color(make_color_rgb(80, 200, 200));
    draw_rectangle(x, y, x + sz, y + sz, true);
    if (_spr != -1 && sprite_exists(_spr)) {
        draw_sprite_stretched(_spr, 0, x + 2, y + 2, sz - 4, sz - 4);
    }
}

// ---------------------------------------------------------------------------
// ui_draw_consumable_entry(x, ty, item, name_col)
// Draws a potion as an equipment-style row: icon badge on the left, NAME on the top
// line and its EFFECT description below - mirroring how gear reads. `item` may be a
// brew-catalog def (uses .desc) or a consumable instance (uses .description). `ty` is
// the row text baseline returned by ui_maren_row.
// ---------------------------------------------------------------------------
function ui_draw_consumable_entry(_x, _ty, item, _name_col = make_color_rgb(150, 215, 200), _desc_max_w = -1) {
    var _ry  = _ty - 15;
    var _isz = 48;
    ui_draw_consumable_icon(_x + 6, _ry + 9, _isz, item);
    var _tx  = _x + 6 + _isz + 18;
    var _nm  = variable_struct_exists(item, "name") ? item.name : "Potion";
    var _ds  = variable_struct_exists(item, "desc")        ? item.desc
             : (variable_struct_exists(item, "description") ? item.description : "");
    draw_set_halign(fa_left);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_name_col);
    draw_text(_tx, _ry + 4, _nm);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(170, 185, 180));
    // Long descs (Devil Wine, chaotic brews) used to SHRINK to 0.72 and then
    // truncate - unreadable (M 08-16). Now they WRAP onto a second line inside
    // the caller's measured clear width; the vendor row pitch reserves two
    // body lines. Past two lines the tail is trimmed at a word with "..".
    var _cbd = ui_vendor_body_dy();   // measured (M 08-15: hard +38 rode borders)
    if (_desc_max_w > 0) {
        var _lh = ui_vendor_body_lh();
        if (string_height_ext(_ds, _lh, _desc_max_w) > _lh * 2 + 2) {
            var _guard = 0;
            while (string_height_ext(_ds + "..", _lh, _desc_max_w) > _lh * 2 + 2 && _guard++ < 80) {
                var _sp = 0;
                for (var _ci = string_length(_ds); _ci >= 1; _ci--) {
                    if (string_char_at(_ds, _ci) == " ") { _sp = _ci; break; }
                }
                if (_sp <= 1) { _ds = string_copy(_ds, 1, max(1, string_length(_ds) - 6)); }
                else          { _ds = string_copy(_ds, 1, _sp - 1); }
            }
            _ds += "..";
        }
        draw_text_ext(_tx, _ry + _cbd, _ds, _lh, _desc_max_w);
    } else {
        draw_text(_tx, _ry + _cbd, _ds);
    }
    draw_set_font(ui_font(fnt_ui));
}

// ---------------------------------------------------------------------------
// ui_ability_icon_sprite(ability)
// Resolves the 64x64 icon sprite for an ability by name (Pass 2 icon initiative).
// Returns -1 when no icon is mapped so callers fall back to text-only buttons.
// All 49 Phase 1 abilities are mapped; add a case here when a new ability ships.
// ---------------------------------------------------------------------------
function ui_ability_icon_sprite(ability) {
    var _name = (is_struct(ability) && variable_struct_exists(ability, "name")) ? ability.name : ability;
    switch (_name) {
        // --- Arcanist ---
        case "Soulfire":         return spr_ability_soulfire;
        case "Void Drain":       return spr_ability_void_drain;
        case "Arcane Burst":     return spr_ability_arcane_burst;
        case "Soul Harvest":     return spr_ability_soul_harvest;
        case "Blink":            return spr_ability_blink;
        case "Curse":            return spr_ability_curse;
        case "Soul Shield":      return spr_ability_soul_shield;
        case "Entropy":          return spr_ability_arcane_echo; // remap 07-17: old Arcane Echo art reads as the chaos-swirl Entropy wants; Arcane Echo gets new 3-ring art. spr_ability_entropy retired.
        case "Rift":             return spr_ability_rift;
        case "Soulbind":         return spr_ability_soulbind;
        // --- Arcanist melee kit (#26, 2026-07-08) ---
        case "Blazing Palm":     return spr_ability_blazing_palm;
        case "Gravewrack Grip":  return spr_ability_gravewrack_grip;
        case "Soul Rend":        return spr_ability_soul_rend;
        // --- D SS4 frost/shock/ramp kit (2026-07-10) ---
        case "Hoarfrost Lance":  return spr_ability_hoarfrost_lance;
        case "Glacial Ward":     return spr_ability_glacial_ward;
        case "Static Arc":       return spr_ability_static_arc;
        case "Soul Engine":      return spr_ability_soul_engine;
        // --- 08-17 control pair (string-resolved: sprites import after M approves the
        //     icon sheet; until then they borrow the nearest shipped icon) ---
        case "Paralytic Pulse":  { var _ppi = asset_get_index("spr_ability_paralytic_pulse");  return (_ppi >= 0) ? _ppi : spr_ability_static_arc; }
        case "Call of the Void": { var _cvi = asset_get_index("spr_ability_call_of_the_void"); return (_cvi >= 0) ? _cvi : spr_ability_curse; }
        // --- Bloodwarden ---
        case "Blood Leech":      return spr_ability_blood_leech;
        case "Iron Skin":        return spr_ability_iron_skin;
        case "Gore Strike":      return spr_ability_gore_strike;
        case "Blood Surge":      return spr_ability_blood_surge;
        case "Marrow Crush":     return spr_ability_marrow_crush;
        case "Vital Theft":      return spr_ability_vital_theft;
        case "Bloodthorn Aura":  return spr_ability_bloodthorn_aura;
        case "Undying":          return spr_ability_undying_rise; // 07-17: new wounded-warrior-rising art in its own sprite
        case "Bulwark Slam":     return spr_ability_undying;    // remap 07-17: reuses the OLD Undying art (M pick); spr_ability_undying now belongs to Bulwark Slam alone.
        case "Plague Touch":     return spr_ability_plague_touch;
        case "Bloodfeast":       return spr_ability_bloodfeast;
        case "Galvanize":        return spr_ability_galvanize;   // D SS4 2026-07-10
        // --- Shadowstrider ---
        case "Snipe":            return spr_ability_snipe;
        case "Bear Trap":        return spr_ability_bear_trap;
        case "Shadow Step":      return spr_ability_shadow_step;
        case "Poison Dart":      return spr_ability_poison_dart;
        case "Smoke Bomb":       return spr_ability_smoke_bomb;
        case "Frost Shot":       return spr_ability_crippling_shot;   // renamed 07-16; sprite asset keeps its old name
        case "Warpath":          return spr_ability_warpath;            // 07-16 combo batch (M pick #13)
        case "Compounding Dread": return spr_ability_compounding_dread; // 07-16 combo batch (M pick #7)
        case "Spike Trap":       return spr_ability_spike_trap;
        case "Marked for Death": return spr_ability_marked_for_death;
        case "Evasive Roll":     return spr_ability_evasive_roll;
        case "Death Snare":      return spr_ability_death_snare;
        case "Tripline":         return spr_ability_tripline;       // 08-09 trap rework icons
        case "Warding Chime":    return spr_ability_warding_chime;  // 08-09 trap rework icons
        case "Wire Snare":       return spr_ability_wire_snare;     // 08-09 trap rework icons
        case "Caltrops":         return spr_ability_caltrops;       // 08-09 trap rework icons
        case "Winter's Bite":    return spr_ability_winters_bite; // D SS4 2026-07-10
        case "Counterblade":     return spr_ability_counterblade; // 07-17 combat plan v2 (new riposte verb)
        // Mutator v2 carriers (08-11): bespoke icons are in the approved art
        // batch - string-resolved so they light up on import; until then each
        // REMAPS to the closest shipped identity (icon rule: prefer remap).
        case "Ricochet Shot": {
            var _ric = asset_get_index("spr_ability_ricochet_shot");
            if (_ric != -1 && sprite_exists(_ric)) return _ric;
            return spr_ability_snipe;
        }
        case "Bouncing Bomb": {
            var _bbm = asset_get_index("spr_ability_bouncing_bomb");
            if (_bbm != -1 && sprite_exists(_bbm)) return _bbm;
            return spr_ability_smoke_bomb;
        }
        case "Gout of Rot": {
            var _gor = asset_get_index("spr_ability_gout_of_rot");
            if (_gor != -1 && sprite_exists(_gor)) return _gor;
            return spr_ability_plague_touch;
        }
        case "Measured Riposte": return spr_ability_measured_riposte; // bespoke icon (08-15 batch, M-approved)
        // 08-15 approved batch: weapon strikes + the 3 Arcanist summons.
        case "Weapon Strike":    return spr_ability_weapon_strike;
        case "Weapon Shot":      return spr_ability_weapon_shot;
        case "Magma Golem":      return spr_ability_magma_golem;
        case "Warding Effigy":   return spr_ability_warding_effigy;
        case "Static Husk":      return spr_ability_static_husk;
        // --- General ---
        case "Strike":           return spr_ability_strike;
        case "Field Dressing":   return spr_ability_field_dressing;
        case "Second Wind":      return spr_ability_second_wind;
        case "Adrenaline Rush":  return spr_ability_adrenaline_rush;
        case "Mana Sever":       return spr_ability_mana_sever;
        case "Arcane Echo":      return spr_ability_arcane_echo_rings; // 07-17: new 3-ring echo art; spr_ability_arcane_echo (old art) now belongs to Entropy
        case "Event Horizon":    return spr_ability_singularity;   // 08-15 rename kept the art
        case "Sanguine Pact":    return spr_ability_sanguine_pact;
        case "Bonebreaker":      return spr_ability_bonebreaker;
        case "Crimson Apex":     return spr_ability_crimson_apex;
        case "Flurry":           return spr_ability_flurry;
        case "Vanish":           return spr_ability_vanish;
        case "Killing Spree":    return spr_ability_killing_spree;
        case "Scorch":           return spr_ability_scorch;
        case "Soul Nova":        return spr_ability_soul_nova;
        case "Cleave":           return spr_ability_cleave;
        case "Rupture":          return spr_ability_rupture;
        case "Throat Slit":      return spr_ability_throat_slit;
        case "Assassinate":      return spr_ability_assassinate;
        case "Devil's Flip":     return spr_ability_devils_flip;  // D SS4 2026-07-10
    }
    return -1;
}

// ---------------------------------------------------------------------------
// ui_draw_ability_icon(x, y, sz, ability)
// Draws an ability's icon in a square badge at (x,y). Falls back to a plain
// dark panel when the ability has no mapped sprite, so callers never crash on
// an unmapped ability. No border is drawn here - callers own their framing.
// ---------------------------------------------------------------------------
function ui_draw_ability_icon(x, y, sz, ability) {
    var _spr = ui_ability_icon_sprite(ability);
    if (_spr != -1 && sprite_exists(_spr)) {
        // Uses the caller's current draw alpha so dimmed buttons dim the icon too.
        draw_sprite_stretched(_spr, 0, x, y, sz, sz);
    }
}

// ---------------------------------------------------------------------------
// ui_draw_coin_burst(coins, floor_y)
// Gold-burst celebration: coins tossed up, falling with gravity, one
// bounce-free settle into a pyramid pile centred on GUI_CX at floor_y.
// Pure draw-side sim - mutates the coin structs in place, no state elsewhere.
// Seed `coins` with {x, y, vx, vy, spin, slot, grounded:false} structs.
// Shared by the event-result panel and the treasure popup (#19 polish).
// ---------------------------------------------------------------------------
function ui_draw_coin_burst(coins, floor_y) {
    for (var _cn = 0; _cn < array_length(coins); _cn++) {
        var _co = coins[_cn];
        if (!_co.grounded) {
            _co.vy  += 0.45;
            _co.x   += _co.vx;
            _co.y   += _co.vy;
            _co.spin += 0.35;
            // Pyramid rest slot from the coin's index: rows of 7/5/4/3/2/2...
            var _rows  = [7, 5, 4, 3, 2, 2, 1];
            var _ri    = 0, _acc = 0, _slot = _co.slot;
            while (_ri < array_length(_rows) - 1 && _slot >= _acc + _rows[_ri]) { _acc += _rows[_ri]; _ri++; }
            var _in_row = _slot - _acc;
            var _rest_y = floor_y - _ri * 13;
            var _rest_x = GUI_CX + (_in_row - (_rows[_ri] - 1) / 2) * 26 + ((_ri mod 2 == 1) ? 7 : -7);
            if (_co.vy > 0 && _co.y >= _rest_y) {
                _co.x = _rest_x; _co.y = _rest_y; _co.grounded = true;
            }
        }
        // Coin: dark bronze rim, gold face, top-left glint. Airborne coins
        // squash on x with their spin so they read as tumbling discs.
        var _cxs = _co.grounded ? 1.0 : max(0.25, abs(cos(_co.spin)));
        draw_set_color(make_color_rgb(96, 62, 18));
        draw_ellipse(_co.x - 10 * _cxs, _co.y - 9, _co.x + 10 * _cxs, _co.y + 9, false);
        draw_set_color(make_color_rgb(232, 186, 74));
        draw_ellipse(_co.x - 8 * _cxs, _co.y - 7, _co.x + 8 * _cxs, _co.y + 7, false);
        draw_set_color(make_color_rgb(255, 232, 150));
        draw_ellipse(_co.x - 4 * _cxs, _co.y - 5, _co.x - 4 * _cxs + 4, _co.y - 1, false);
    }
}

// ui_seed_coin_burst(n, origin_x, origin_y) - build the coin array for
// ui_draw_coin_burst: n coins tossed upward from around (origin_x, origin_y).
function ui_seed_coin_burst(n, origin_x, origin_y) {
    var _coins = [];
    for (var _ci = 0; _ci < n; _ci++) {
        array_push(_coins, {
            x: origin_x + random_range(-40, 40),
            y: origin_y,
            vx: random_range(-4.2, 4.2),
            vy: random_range(-9, -4),
            spin: random_range(0, pi * 2),
            slot: _ci,           // index into the pile rest positions
            grounded: false
        });
    }
    return _coins;
}

// ---------------------------------------------------------------------------
// ui_trait_icon_sprite(tr)
// Resolves the 64x64 icon sprite for a trait by its effect_id. Returns -1 when
// no icon is mapped so callers fall back to a plain badge. 18 icons come from
// the framed warrior/mage library packs, 7 from PixelLab gap-fills - together
// they cover all 25 traits. Add a case here when a new trait ships.
// ---------------------------------------------------------------------------
function ui_trait_icon_sprite(tr) {
    var _eid = (is_struct(tr) && variable_struct_exists(tr, "effect_id")) ? tr.effect_id : tr;
    switch (_eid) {
        // --- Universal ---
        case "sense":            return spr_icon_trait_sense;
        case "scavenger":        return spr_icon_trait_scavenger;
        case "thick_skin":       return spr_icon_trait_thick_skin;
        case "quick_recovery":   return spr_icon_trait_quick_recovery;
        case "treasure_hunter":  return spr_icon_trait_treasure_hunter;
        // 07-08 identity split: the clover moved to the NEW Lucky Find (gold/loot);
        // Blessed Thirst (old lucky_find id, the no-consume trait) got its own
        // pouring-potion icon (M-approved candidate, tools/gen_reward_icons.py).
        case "lucky_find":       return spr_icon_trait_blessed_thirst;
        case "lucky_find_gold":  return spr_icon_trait_lucky_find;
        case "battle_hardened":  return spr_icon_trait_battle_hardened;
        case "salvager":         return spr_icon_trait_salvager;
        case "iron_will":        return spr_icon_trait_iron_will;
        case "expanded_arsenal": return spr_icon_trait_expanded_arsenal;
        case "prospector":       return spr_icon_trait_prospector;
        case "pack_rat":         return spr_icon_trait_pack_rat;
        case "last_stand":       return spr_icon_trait_last_stand;
        // --- AoE-themed ---
        case "focused_power":    return spr_icon_trait_focused_power;
        case "chain_caster":     return spr_icon_trait_chain_caster;
        case "plaguebearer":     return spr_icon_trait_plaguebearer;
        // --- Arcanist ---
        case "soul_siphon":      return spr_icon_trait_soul_siphon;
        case "ley_tap":          return spr_icon_trait_ley_tap;
        case "arcane_surge":     return spr_icon_trait_arcane_surge;
        // --- Bloodwarden ---
        case "crimson_reserve":  return spr_icon_trait_crimson_reserve;
        case "vampiric_edge":    return spr_icon_trait_vampiric_edge;
        case "berserker_rage":   return spr_icon_trait_berserker_rage;
        case "relentless":       return spr_icon_trait_relentless;   // M pick 07-16 (berserk scream #09)
        // --- Shadowstrider ---
        case "phantom_step":     return spr_icon_trait_phantom_step;
        case "shadow_meld":      return spr_icon_trait_shadow_meld;
        case "serrated_strikes": return spr_icon_trait_serrated_strikes;
        // Bespoke crown-over-hand icon (08-15 batch, M-approved). HARD RULE
        // stands: never reuse equipment/item art for trait or ability icons.
        case "mandate_heaven":   return spr_icon_trait_mandate_heaven;
    }
    return -1;
}

// ---------------------------------------------------------------------------
// ui_draw_trait_icon(x, y, sz, tr)
// Draws a trait's icon in a square badge at (x,y). When the trait has no mapped
// sprite, draws a plain dark slate badge with a bronze border so the row keeps
// a consistent icon column. Uses the caller's current alpha (so dimmed rows dim
// their icon). Callers own any outer framing/selection glow.
// ---------------------------------------------------------------------------
function ui_draw_trait_icon(x, y, sz, tr) {
    var _spr = ui_trait_icon_sprite(tr);
    // Badge backdrop draws for EVERY trait (not just the missing-icon fallback)
    // so remapped unframed sprites (Mandate's diadem) sit in the same bordered
    // style as the framed library icons, which simply cover it.
    var _a = draw_get_alpha();
    draw_set_color(make_color_rgb(20, 22, 32));
    draw_rectangle(x, y, x + sz, y + sz, false);
    draw_set_color(make_color_rgb(120, 84, 40));
    draw_rectangle(x, y, x + sz, y + sz, true);
    draw_set_alpha(_a);
    if (_spr != -1 && sprite_exists(_spr)) {
        draw_sprite_stretched(_spr, 0, x, y, sz, sz);
    }
}

// ---------------------------------------------------------------------------
// item_splash_sprite(base_name)
// Returns the codex splash-art sprite for a base item, or -1 if none exists yet.
// Art is generated in batches (PixelLab) and wired here as each sprite lands;
// until then the codex detail pane falls back to the scaled item icon. Add a
// case per generated sprite: case "Iron Sword": return spr_item_art_iron_sword;
// ---------------------------------------------------------------------------
function item_splash_sprite(base_name) {
    switch (base_name) {
        // --- Legendaries ---
        case "Gatewarden's Brand":      return spr_item_art_gatewarden_brand;
        case "Heartstone Aegis":        return spr_item_art_heartstone_aegis;
        case "Crown of the Hollow King": return spr_item_art_crown_hollow_king;
        case "Thief of Hours":          return spr_item_art_thief_of_hours;
        // 07-28 expansion (tools/import_legendary_icons.py splash batch)
        case "Duelist's Rebuke":           return spr_item_art_duelists_rebuke;
        case "Gravewalker Treads":         return spr_item_art_gravewalker_treads;
        case "Sanguine Chalice":           return spr_item_art_sanguine_chalice;
        case "Stormcaller's Loop":         return spr_item_art_stormcallers_loop;
        case "Miser's Blade":              return spr_item_art_misers_blade;
        case "Veil of the Patient Dark":   return spr_item_art_veil_patient_dark;
        case "Longshot's Memory":          return spr_item_art_longshots_memory;
        case "Aegis of the Unbroken Line": return spr_item_art_aegis_unbroken_line;
        case "Hollow King's Signet":       return spr_item_art_hollow_kings_signet;
        case "Ember Saint's Censer":       return spr_item_art_ember_saints_censer;
        case "Oathbreaker's Shard":        return spr_item_art_oathbreakers_shard;
        case "Lantern of the Last Door":   return spr_item_art_lantern_last_door;
        case "Crownfire Diadem":           return spr_item_art_crownfire_diadem;
        case "Beggar's Fortune":           return spr_item_art_beggars_fortune;
        case "Kindled Reliquary":          return spr_item_art_kindled_reliquary;
        // --- Common ---
        case "Ashen Blade": return spr_item_art_ashen_blade;
        case "Worn Shortbow": return spr_item_art_worn_shortbow;
        case "Cracked Focus": return spr_item_art_cracked_focus;
        case "Cracked Shield": return spr_item_art_cracked_shield;
        case "Ash Totem": return spr_item_art_ash_totem;
        case "Soulstone Fragment": return spr_item_art_soulstone_fragment;
        case "Ashen Hood": return spr_item_art_ashen_hood;
        case "Bone Cap": return spr_item_art_bone_cap;
        case "Tarnished Visor": return spr_item_art_tarnished_visor;
        case "Tattered Robes": return spr_item_art_tattered_robes;
        case "Rusted Chainshirt": return spr_item_art_rusted_chainshirt;
        case "Shadowcloth Tunic": return spr_item_art_shadowcloth_tunic;
        case "Worn Gauntlets": return spr_item_art_worn_gauntlets;
        case "Nimble Wraps": return spr_item_art_nimble_wraps;
        case "Sage's Gloves": return spr_item_art_sages_gloves;
        case "Worn Treads": return spr_item_art_worn_treads;
        case "Ironshod Boots": return spr_item_art_ironshod_boots;
        case "Dustwalker Wraps": return spr_item_art_dustwalker_wraps;
        case "Dusty Amulet": return spr_item_art_dusty_amulet;
        case "Bone Talisman": return spr_item_art_bone_talisman;
        case "Silver Chain": return spr_item_art_silver_chain;
        case "Bone Ring": return spr_item_art_bone_ring;
        case "Copper Signet": return spr_item_art_copper_signet;
        case "Tarnished Band": return spr_item_art_tarnished_band;
        // --- Uncommon ---
        case "Gravelstone Sword": return spr_item_art_gravelstone_sword;
        case "Vaultstone Wand": return spr_item_art_vaultstone_wand;
        case "Shadow Sickle": return spr_item_art_shadow_sickle;
        case "Warden's Buckler": return spr_item_art_wardens_buckler;
        case "Runic Focus": return spr_item_art_runic_focus;
        case "Watcher's Cowl": return spr_item_art_watchers_cowl;
        case "Iron Skullcap": return spr_item_art_iron_skullcap;
        case "Shadowthread Vest": return spr_item_art_shadowthread_vest;
        case "Ashwarden Coat": return spr_item_art_ashwarden_coat;
        case "Irongrip Gauntlets": return spr_item_art_irongrip_gauntlets;
        case "Fleethand Wraps": return spr_item_art_fleethand_wraps;
        case "Vaultstrider Boots": return spr_item_art_vaultstrider_boots;
        case "Stoneguard Greaves": return spr_item_art_stoneguard_greaves;
        case "Sentry's Pendant": return spr_item_art_sentrys_pendant;
        case "Soul-Linked Talisman": return spr_item_art_soul_linked_talisman;
        case "Ember Ring": return spr_item_art_ember_ring;
        case "Voidtouched Ring": return spr_item_art_voidtouched_ring;
        // --- Rare ---
        case "Ashkeeper Blade": return spr_item_art_ashkeeper_blade;
        case "The Ashen Blade": return spr_item_art_ashkeeper_blade;   // Duelist Arts prize - REMAP (bespoke splash = M-approved gen later)
        case "Void Scepter": return spr_item_art_void_scepter;
        case "Serpent's Reach": return spr_item_art_serpents_reach;
        case "Soulbound Orb": return spr_item_art_soulbound_orb;
        case "Ironhide Bulwark": return spr_item_art_ironhide_bulwark;
        case "Forsaken Circlet": return spr_item_art_forsaken_circlet;
        case "Thornwarden Helm": return spr_item_art_thornwarden_helm;
        case "Voidskin Coat": return spr_item_art_voidskin_coat;
        case "Ironveil Plate": return spr_item_art_ironveil_plate;
        case "Crushers": return spr_item_art_crushers;
        case "Whispergloves": return spr_item_art_whispergloves;
        case "Shadowstep Boots": return spr_item_art_shadowstep_boots;
        case "Colossus Stompers": return spr_item_art_colossus_stompers;
        case "Enduring Medallion": return spr_item_art_medallion_of_endurance;
        case "Warden's Eye": return spr_item_art_wardens_eye;
        case "Wraithbone Signet": return spr_item_art_wraithbone_signet;
        case "Bloodpact Ring": return spr_item_art_bloodpact_ring;
    }
    return -1;
}

// ---------------------------------------------------------------------------
// ui_room_icon_sprite(room_type)
// Returns the asset index of a floor-map room-type icon, or -1 if none exists
// for that type. Supply/armory/reliquary have art; the old trap icon is reused
// for event rooms (the trap room was folded into the event system).
// ---------------------------------------------------------------------------
function ui_room_icon_sprite(room_type) {
    switch (room_type) {
        case "treasure_heal":  return spr_icon_room_supply;
        case "treasure_vault": return spr_icon_room_armory;
        case "treasure_rare":  return spr_icon_room_reliquary;
        case "event":          return spr_icon_room_trap;
    }
    return -1;
}

// ---------------------------------------------------------------------------
// ui_input_blocked()
// Returns true when any full-screen overlay managed by obj_game_controller is
// open. Call as the very first line of room-controller Step events so their
// regular input does not bleed through while overlays are active.
// gc Step is intentionally excluded - it must keep running to handle overlays.
// ---------------------------------------------------------------------------
// True while any typed-text entry is capturing keyboard_string (pet naming at Bairc,
// hero naming at char select). Global hotkeys must stand down while this is true, or
// typing a name with 'i' in it opens the inventory (M's bug report 2026-07-03).
function text_entry_active() {
    if (instance_exists(obj_game_controller)) {
        var _gc = instance_find(obj_game_controller, 0);
        if (variable_instance_exists(_gc, "bairc_naming") && _gc.bairc_naming) return true;
    }
    if (instance_exists(obj_char_select)) {
        var _cs = instance_find(obj_char_select, 0);
        if (variable_instance_exists(_cs, "naming_active") && _cs.naming_active) return true;
    }
    // THE LEGENDARY FORGE naming phase (M locked 07-28) - typing the piece's name.
    if (instance_exists(obj_game_controller)) {
        var _fgc = instance_find(obj_game_controller, 0);
        if (variable_instance_exists(_fgc, "forge_open") && _fgc.forge_open
            && variable_instance_exists(_fgc, "forge_phase") && _fgc.forge_phase == 2) return true;
        // PATTERN CRAFT naming phase (08-11) - typing the crafted piece's name.
        if (variable_instance_exists(_fgc, "pb_craft_open") && _fgc.pb_craft_open
            && variable_instance_exists(_fgc, "pb_craft_phase") && _fgc.pb_craft_phase == 5) return true;
    }
    return false;
}

// True while the shared forge-result reveal popup is up (07-31). Its close
// handling lives in obj_game_controller Step; every craft screen's input block
// stands down on this so the close press can't fall through.
function forge_result_up() {
    return variable_global_exists("forge_result") && global.forge_result != undefined;
}

function ui_input_blocked() {
    if (text_entry_active())  return true;  // typing a name - every hotkey stands down
    if (tutorial_is_active()) return true;  // onboarding coach-mark is modal
    if (forge_result_up())    return true;  // forge-result reveal popup (07-31)
    if (!instance_exists(obj_game_controller)) return false;
    var _gc = instance_find(obj_game_controller, 0);
    if (_gc.menu_open)       return true;   // character menu (I)
    if (_gc.stash_mode_open) return true;   // stash screen (T)
    if (_gc.shop_open != -1) return true;   // Petra / Dorn shops
    if (variable_instance_exists(_gc, "trainer_open") && _gc.trainer_open) return true;   // Vex the Trainer
    if (variable_instance_exists(_gc, "maren_open")   && _gc.maren_open)   return true;   // Maren the Runesmith
    if (variable_instance_exists(_gc, "sable_open")   && _gc.sable_open)   return true;   // Sable the Alchemist
    if (variable_instance_exists(_gc, "vael_open")    && _gc.vael_open)    return true;   // Vael the Aesthete
    if (variable_instance_exists(_gc, "bairc_open")   && _gc.bairc_open)   return true;   // Bairc the Creature Keeper
    if (variable_instance_exists(_gc, "bairc_intro_open") && _gc.bairc_intro_open) return true;   // Bairc first-talk dialogue
    if (variable_instance_exists(_gc, "level_alloc_open") && _gc.level_alloc_open) return true;
    if (variable_instance_exists(_gc, "loadout_open")     && _gc.loadout_open)     return true;
    if (variable_instance_exists(_gc, "journal_open")     && _gc.journal_open)     return true;   // J Journal (Phase 4a)
    if (variable_instance_exists(_gc, "codex_open")       && _gc.codex_open)       return true;   // full Item Codex gallery (opens anywhere since 07-28)
    if (variable_instance_exists(_gc, "pet_inspect_open") && _gc.pet_inspect_open) return true;   // P companion inspect
    if (variable_instance_exists(_gc, "tavern_board_open") && _gc.tavern_board_open) return true; // Tavern Requests board (Phase 4b)
    if (variable_instance_exists(_gc, "kb_open") && _gc.kb_open) return true;                     // Knucklebones (expression #1)
    return false;
}

// ---------------------------------------------------------------------------
// bairc_pad_menu_items(pet, level)
// Entries for the gamepad action submenu at Bairc (chunk 7b). Level 0 lists the
// highlighted creature's actions; level 1 lists the owned feeds. Each entry is
// { label, tag } - the tag is the synthetic hotkey injected on pick ("bairc:N"),
// consumed by the unchanged letter handlers in obj_game_controller/Step.
// "feed_menu" descends a level; "" is a disabled info row. Shared by the Step
// handler (counts/tags) and the Draw modal (labels) so they can never disagree.
// ---------------------------------------------------------------------------
function bairc_pad_menu_items(_pet, _level) {
    var _items = [];
    if (_level == 1) {
        // ALL owned feeds, absolute-indexed (07-24 audit: the old min(6) cap +
        // page-relative feedN tags hid feed types 7+ from pad/touch; the menu's
        // height auto-scales, and the Step consumer reads feedabs<i> directly).
        var _owned = pet_feed_owned_list();
        for (var _i = 0; _i < array_length(_owned); _i++) {
            var _cnt = pet_feed_pouch_count(_owned[_i].id);
            array_push(_items, { label: _owned[_i].name + "  x" + string(_cnt),
                                 tag:   "bairc:feedabs" + string(_i) });
        }
        if (array_length(_items) == 0) {
            array_push(_items, { label: "No feed on hand - visit Petra", tag: "" });
        }
        return _items;
    }
    if (_pet.is_egg && !pet_egg_identified(_pet)) {
        array_push(_items, { label: "Identify Egg (" + string(pet_egg_identify_cost()) + "g)", tag: "bairc:I" });
    } else if (_pet.is_egg) {
        array_push(_items, { label: "Hatch", tag: "bairc:confirm" });
    } else {
        array_push(_items, { label: "Set Active Companion", tag: "bairc:confirm" });
        array_push(_items, { label: "Feed...", tag: "feed_menu" });
        if (pet_capstone_can_pick(_pet) || pet_splash_can_pick(_pet)) {
            array_push(_items, { label: "Choose Its Gift", tag: "bairc:G" });
        }
        if (pet_corr_state(_pet) == "pushing") {
            array_push(_items, { label: "Cure Corruption", tag: "bairc:C" });
        }
        array_push(_items, { label: pet_named(_pet) ? "Rename (20 dust)" : "Name", tag: "bairc:N" });
    }
    // Touch/pad path to the Tab detail popup (07-24: Tab-only = invisible on a
    // phone; M hit it on the S25). Same popup the keyboard opens.
    array_push(_items, { label: "Details", tag: "bairc:detail" });
    array_push(_items, { label: "Donate to the Garden", tag: "bairc:R" });
    return _items;
}

// ---------------------------------------------------------------------------
// ui_draw_bairc_screen()
// Bairc the Creature Keeper's stable / hatchery (Pets Phase 2). Roster on the left
// (cursor + active marker), the selected creature's detail on the right. Enter hatches
// an egg or sets the active companion (handled in obj_game_controller/Step). No-op
// unless bairc_open. See PETS_DESIGN.md.
// ---------------------------------------------------------------------------
// Full-screen egg-hatch cutscene, drawn over the Bairc screen. Three phases driven by
// gc state (see hatch_cutscene_step in scr_stats): 0 shake (trembling egg + darkening),
// 1 crack (the egg's hatch animation plays out with a growing glow), 2 reveal (a white
// flash clears to the baby scaling in with its name/archetype/egg-benefit caption).
function hatch_cutscene_draw() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "hatch_active") || !_gc.hatch_active) return;
    var _pet = _gc.hatch_pet;
    if (!is_struct(_pet)) return;

    var _phase = _gc.hatch_phase;
    var _t     = _gc.hatch_t;
    var _cx    = GUI_CX;
    var _base_y = GUI_CY + 140;   // subjects sit a little below center

    // Backdrop: fades to FULLY black through the shake, holds opaque afterwards. Must
    // be opaque so the garden's pet-detail panel behind (centered differently) doesn't
    // bleed through and clash with the centered hatch caption (M 07-17: looked like the
    // text "switched alignment" across phases - it was the garden showing through 0.95).
    var _bg_a = (_phase == 0) ? lerp(0.30, 1.0, _t / HATCH_SHAKE_LEN) : 1.0;
    draw_set_alpha(_bg_a); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);
    draw_set_halign(fa_center); draw_set_valign(fa_middle);

    if (_phase <= 1) {
        var _spr, _fr, _jx = 0, _jy = 0, _glow = 0;
        if (_phase == 0) {
            _spr = pet_sprite(_pet, "s");
            _fr  = 0;
            var _mag = lerp(1, 11, _t / HATCH_SHAKE_LEN);   // rumble grows toward the crack
            _jx  = round((random(2) - 1) * _mag);
            _jy  = round((random(2) - 1) * _mag);
        } else {
            _spr  = hatch_crack_sprite(_pet);
            _fr   = clamp(_gc.hatch_frame, 0, max(0, sprite_get_number(_spr) - 1));
            _glow = (sprite_get_number(_spr) > 1) ? (_fr / max(1, sprite_get_number(_spr) - 1)) : 0.5;
        }
        if (_spr >= 0) {
            // Light spilling from the shell during the crack phase.
            if (_glow > 0) {
                draw_set_alpha(0.35 * _glow);
                draw_set_color(make_color_rgb(255, 244, 205));
                draw_circle(_cx + _jx, _base_y - 60, 90 + 140 * _glow, false);
                draw_set_alpha(1.0);
            }
            var _sc = 360 / max(1, sprite_get_height(_spr));   // ~360px tall
            // Anchor the VISIBLE egg's center at the stage point - padded sprite
            // canvases were drifting the egg well above it (and away from the glow).
            var _vl = sprite_get_bbox_left(_spr);
            var _vt = sprite_get_bbox_top(_spr);
            var _vcx = _vl + (sprite_get_bbox_right(_spr)  - _vl + 1) / 2;
            var _vcy = _vt + (sprite_get_bbox_bottom(_spr) - _vt + 1) / 2;
            var _dx = _cx     + _jx + (sprite_get_xoffset(_spr) - _vcx) * _sc;
            var _dy = _base_y + _jy + (sprite_get_yoffset(_spr) - _vcy) * _sc;
            draw_sprite_ext(_spr, _fr, _dx, _dy, _sc, _sc, 0, c_white, 1);
        }
        // LIGHT WASH (M 08-21): from crack frame 5 a warm-white flood ramps over the
        // egg and drowns the shell-burst frames; the reveal's white flash then takes
        // over at full white, so the hatchling emerges out of the light. Drawn under
        // the caption so "It's hatching!" stays readable.
        if (_phase == 1 && _spr >= 0) {
            var _nf   = max(1, sprite_get_number(_spr) - 1);
            var _wash = clamp((_fr - 4) / max(1, _nf - 4), 0, 1);   // f4 -> 0, last frame -> 1
            if (_wash > 0) {
                draw_set_alpha(0.95 * _wash);
                draw_set_color(make_color_rgb(255, 246, 222));
                draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
                draw_set_alpha(1.0);
            }
        }
        draw_set_font(fnt_ui_title);
        draw_set_color(make_color_rgb(210, 200, 225));
        draw_text(_cx, GUI_CY - 300, (_phase == 0) ? "The shell trembles..." : "It's hatching!");
    } else {
        // REVEAL: baby scales in with a slight overshoot; a white flash clears over it.
        var _bsp  = pet_sprite(_pet, "s");
        var _grow = clamp(_t / 22, 0, 1);
        var _pop  = 1 + 0.14 * sin(_grow * pi);             // subtle bounce
        if (_bsp >= 0) {
            // #16: reveal height keyed to the VISIBLE creature, not the padded canvas.
            var _bvl = sprite_get_bbox_left(_bsp);
            var _bvt = sprite_get_bbox_top(_bsp);
            var _bvw = max(1, sprite_get_bbox_right(_bsp)  - _bvl + 1);
            var _bvh = max(1, sprite_get_bbox_bottom(_bsp) - _bvt + 1);
            var _sc  = (360 / _bvh) * (0.25 + 0.75 * _grow) * _pop;
            // Anchor the VISIBLE creature's center at the stage point too - padded
            // canvases were drawing the body up into the caption text (07-13 report).
            var _dx = _cx     + (sprite_get_xoffset(_bsp) - (_bvl + _bvw / 2)) * _sc;
            var _dy = _base_y + (sprite_get_yoffset(_bsp) - (_bvt + _bvh / 2)) * _sc;
            draw_sprite_ext(_bsp, pet_anim_frame(_bsp), _dx, _dy, _sc, _sc, 0, c_white, 1);
        }
        // Caption
        draw_set_font(fnt_ui_title);
        draw_set_color(make_color_rgb(245, 230, 250));
        draw_text(_cx, GUI_CY - 300, _pet.name);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(190, 210, 235));
        draw_text(_cx, GUI_CY - 250, "a " + pet_archetype_name(_pet.archetype) + " hatchling");
        var _el = pet_egg_label(_pet);
        if (_el != "") {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(150, 200, 140));
            draw_text(_cx, GUI_CY - 212, _el);
        }
        if (_t >= HATCH_REVEAL_MIN) {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(160, 165, 185));
            draw_text(_cx, GUI_CY + 340, "Press Enter");
        }
        // White flash clearing over the first ~18 frames.
        var _flash = clamp(1 - _t / 18, 0, 1);
        if (_flash > 0) {
            draw_set_alpha(_flash); draw_set_color(c_white);
            draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
            draw_set_alpha(1.0);
        }
    }

    // Restore shared draw state.
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_alpha(1.0); draw_set_color(c_white); draw_set_font(-1);
}

// Bairc first-talk dialogue popup: a portrait + speech panel shown once, before his
// station opens (gc.bairc_intro_open). Modeled on ui_draw_tutorial_tip. Dismiss handled
// in obj_game_controller/Step (opens the station + hatch prompt).
function ui_draw_bairc_intro() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "bairc_intro_open") || !_gc.bairc_intro_open) return;
    ui_draw_bairc_dialogue("Hmph. You again, stranger."
        + "\n\n...That egg you carry - it began to stir the very moment you drew near. How strange."
        + "\n\nSet it down here in my garden. Let us see what wakes.");
}

// Bairc lore-fragment popup (design §10): the oldest queued one-time line, shown over his
// garden (gc.bairc_lore_open). Dismiss handled in obj_game_controller/Step (pops the queue).
function ui_draw_bairc_lore() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "bairc_lore_open") || !_gc.bairc_lore_open) return;
    var _q = bairc_lore_queue();
    if (array_length(_q) == 0) return;
    var _lines = bairc_lore_lines();
    if (!variable_struct_exists(_lines, _q[0])) return;
    ui_draw_bairc_dialogue(variable_struct_get(_lines, _q[0]));
}

// Shared Bairc portrait + speech panel (intro + lore fragments): near-opaque backdrop,
// portrait left, name + divider, wrapped body, any-key footer.
function ui_draw_bairc_dialogue(_txt) {
    draw_set_alpha(0.93); draw_set_color(make_color_rgb(6, 8, 14));   // near-opaque so the hub behind (incl. Bairc's own portrait panel) can't bleed past the modal edges
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    var _bw   = 1240;
    var _tx0  = 320;                  // body/name left edge (clears the portrait)
    var _wrap = _bw - _tx0 - 60;
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui));
    var _body_h = string_height_ext(_txt, -1, _wrap);
    var _bh = 130 + max(260, _body_h) + 40;
    var _bx = GUI_CX - _bw / 2;
    var _by = GUI_CY - _bh / 2;

    draw_set_color(make_color_rgb(18, 20, 30));
    draw_rectangle(_bx, _by, _bx + _bw, _by + _bh, false);

    // Portrait (left column)
    var _port = asset_get_index("spr_npc_bairc_portrait");
    if (_port < 0) _port = asset_get_index("spr_npc_bairc_idle");
    if (_port >= 0) {
        var _psz = 250;
        var _psc = _psz / max(1, max(sprite_get_width(_port), sprite_get_height(_port)));
        // The portrait art uses a centre origin (and the idle fallback a bottom origin), so
        // offset the draw point by the scaled origin to land the art's top-left at (+45,+55)
        // instead of spilling out the panel's upper-left corner.
        var _pox = sprite_get_xoffset(_port) * _psc;
        var _poy = sprite_get_yoffset(_port) * _psc;
        draw_sprite_ext(_port, 0, _bx + 45 + _pox, _by + 55 + _poy, _psc, _psc, 0, c_white, 1);
    }

    // Name + divider. Scale the name down if it would run past the panel's right edge
    // (fnt_ui_title is large; "Bairc, the Creature Keeper" overflowed the old width).
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(228, 200, 130));
    var _nm     = "Bairc, the Creature Keeper";
    var _navail = (_bx + _bw - 45) - (_bx + _tx0);
    var _nw     = string_width(_nm);
    var _nsc    = (_nw > _navail) ? (_navail / _nw) : 1;
    draw_text_transformed(_bx + _tx0, _by + 48, _nm, _nsc, _nsc, 0);
    draw_set_color(make_color_rgb(70, 64, 48));
    draw_line(_bx + _tx0, _by + 104, _bx + _bw - 45, _by + 104);

    // Speech body
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(210, 214, 226));
    draw_text_ext(_bx + _tx0, _by + 128, _txt, -1, _wrap);

    // Footer prompt
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(120, 128, 150));
    draw_text(GUI_CX, _by + _bh - 40, "Press any key or click to continue");

    ui_draw_gothic_frame(_bx, _by, _bx + _bw, _by + _bh, 30);
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_alpha(1.0); draw_set_color(c_white); draw_set_font(-1);
}

// Capstone PICK modal: a raised Adult chooses its permanent Stage-3 gift. Two cards (the
// archetype's capstones); A/D highlights, Enter opens a Yes/No confirm. Drawn over the
// Bairc screen. State on gc: bairc_capstone_open / _sel / _confirm.
function ui_draw_bairc_capstone() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "bairc_capstone_open") || !_gc.bairc_capstone_open) return;

    var _n_pets = pet_count();
    var _pet    = (_n_pets > 0) ? global.pet_roster[clamp(_gc.bairc_cursor, 0, _n_pets - 1)] : undefined;
    if (!is_struct(_pet)) return;
    // Mode: Stage-3 capstone pick, or the Stage-4 Awakened SPLASH pick (off-archetype pool).
    var _splash = variable_instance_exists(_gc, "bairc_capstone_mode") && _gc.bairc_capstone_mode == "splash";
    var _pool = _splash ? pet_splash_pool(_pet.archetype) : pet_archetype_capstones(_pet.archetype);
    var _np   = array_length(_pool);
    if (_np == 0) return;
    var _sel = clamp(_gc.bairc_capstone_sel, 0, _np - 1);

    // Backdrop.
    draw_set_alpha(0.88); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    // 08-16 (M shot "pet capstone overruns its boxes"): panel widened 1000 ->
    // 1280 so four cards get ~270px of wrap width, and the card height is
    // MEASURED from the longest description below (was a fixed 300px box the
    // Executioner text ran straight through, into the footer).
    var _px1 = 320, _py1 = 230, _px2 = 1600, _py2 = 850;
    draw_set_color(make_color_rgb(16, 18, 22));
    draw_rectangle(_px1, _py1, _px2, _py2, false);
    ui_draw_gothic_frame(_px1, _py1, _px2, _py2, 36);

    draw_set_halign(fa_center); draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(230, 210, 150));
    draw_text((_px1 + _px2) / 2, _py1 + 34, _splash
        ? ("Choose " + _pet.name + "'s Awakened Splash")
        : ("Choose " + _pet.name + "'s Gift"));
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 160, 185));
    draw_text((_px1 + _px2) / 2, _py1 + 92, _splash
        ? ("The crossing: one permanent touch of ANOTHER archetype - choose well.")
        : ("A permanent " + pet_archetype_name(_pet.archetype) + " capstone - choose well."));

    // Cards - height measured from the tallest desc, capped above the footer;
    // if even the cap can't hold it the desc line pitch tightens (never overruns).
    var _cy1 = _py1 + 150;
    var _gap = 40, _cw = ((_px2 - _px1 - 90) - _gap * (_np - 1)) / _np;
    var _dlh = 30, _dmax = 0;
    draw_set_font(ui_font(fnt_ui_small));
    for (var _mi = 0; _mi < _np; _mi++) _dmax = max(_dmax, string_height_ext(_pool[_mi].desc, _dlh, _cw - 44));
    var _cy2_cap = _py2 - 76;
    if (_cy1 + 104 + _dmax + 20 > _cy2_cap) {
        _dlh = 26; _dmax = 0;
        for (var _mi = 0; _mi < _np; _mi++) _dmax = max(_dmax, string_height_ext(_pool[_mi].desc, _dlh, _cw - 44));
    }
    var _cy2 = min(_cy2_cap, max(_cy1 + 220, _cy1 + 104 + _dmax + 20));
    for (var _i = 0; _i < _np; _i++) {
        var _e   = _pool[_i];
        var _cx1 = _px1 + 45 + _i * (_cw + _gap);
        var _cx2 = _cx1 + _cw;
        var _hot = (_i == _sel);
        draw_set_color(_hot ? make_color_rgb(40, 44, 58) : make_color_rgb(26, 28, 36));
        draw_rectangle(_cx1, _cy1, _cx2, _cy2, false);
        // accent border (brighter when highlighted)
        draw_set_color(_hot ? make_color_rgb(235, 205, 120) : make_color_rgb(70, 74, 92));
        draw_rectangle(_cx1, _cy1, _cx2, _cy2, true);
        draw_rectangle(_cx1 + 1, _cy1 + 1, _cx2 - 1, _cy2 - 1, true);

        var _mid = (_cx1 + _cx2) / 2;
        draw_set_halign(fa_center); draw_set_valign(fa_top);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_hot ? make_color_rgb(245, 230, 250) : make_color_rgb(200, 195, 215));
        draw_text(_mid, _cy1 + 26, _e.name);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(150, 200, 140));
        draw_text(_mid, _cy1 + 64, _e.kind);
        draw_set_color(make_color_rgb(185, 190, 205));
        draw_text_ext(_mid, _cy1 + 104, _e.desc, _dlh, _cw - 44);
        // Touch / mouse (08-16, touch rule - the modal had no tap path): tap a
        // card to highlight it; tap the highlighted card to open the confirm;
        // tap it again to lock. Step consumes capstone:pick<i> / capstone:lock.
        if (touch_tapped(_cx1, _cy1, _cx2, _cy2)) {
            if (_gc.bairc_capstone_confirm && _hot)      input_inject("capstone:lock");
            else if (_hot)                               input_inject("capstone:confirm");
            else                                         input_inject("capstone:pick" + string(_i));
        }
    }

    // Footer hint or confirm bar.
    draw_set_halign(fa_center); draw_set_valign(fa_bottom);
    draw_set_font(ui_font(fnt_ui_small));
    if (_gc.bairc_capstone_confirm) {
        draw_set_color(make_color_rgb(235, 205, 120));
        draw_text((_px1 + _px2) / 2, _py2 - 34, (input_device() == 2)
            ? ("Lock in " + _pool[_sel].name + "?   Tap it again = Yes    Back = No")
            : ("Lock in " + _pool[_sel].name + "?   Enter = Yes    Esc = No"));
    } else {
        draw_set_color(make_color_rgb(155, 160, 180));
        draw_text((_px1 + _px2) / 2, _py2 - 34, (input_device() == 2)
            ? "Tap a gift to highlight it  -  tap it again to choose"
            : "A / D  select      Enter  choose      Esc  cancel");
    }

    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_alpha(1.0); draw_set_color(c_white); draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// JOURNAL (Phase 4a, PHASE4A_SPEC.md): the player character's personal journal.
// J-key overlay (hub + floor map; gc owns input). Two tabs, master-detail:
//   0 Relationships - met NPCs, profile, tier + diegetic progress, blurb, taste
//     slots (all "???" until gifts land in 4b), interaction ledger, pinned quest.
//   1 Quests - Active / Available / Completed, detail + hub-only actions.
// ---------------------------------------------------------------------------
function journal_npc_portrait(id) {
    switch (id) {
        case "dorn":  return Blacksmith_1__Dark_Gritty_;
        case "sable": return Alcehmist_2__Flirty_;
        case "maren": return Runesmith_3__Facewrap_;
        case "vex":   return Trainer_2__Sullen_;
        case "petra": return Merchant_7__Voluptuous_;
        case "vael":  return Aesthete_2__Gothic_;
        case "bairc":
            var _b = asset_get_index("spr_npc_bairc_portrait");
            if (_b < 0) _b = asset_get_index("spr_npc_bairc_idle");
            return _b;
    }
    return -1;
}

// One-line reward description for a quest def (mirrors quest_turn_in's grants).
function journal_quest_reward_text(def) {
    // Gate quests (4c): the reward is the relationship tier itself.
    if (variable_struct_exists(def, "kind") && def.kind == "gate") {
        return npc_display_name(def.npc) + " -> " + affinity_tier_name_for(def.gate_tier);
    }
    var _r = def.reward; var _out = "";
    if (_r.gold > 0) _out += string(_r.gold) + "g";
    if (_r.feed != "" && _r.feed_n > 0) {
        var _fd = pet_feed_get(_r.feed);
        _out += (_out != "" ? ", " : "") + string(_r.feed_n) + "x " + ((_fd != undefined) ? _fd.name : _r.feed);
    }
    if (_r.rune_id != "" && _r.rune_tier > 0) {
        var _rd = rune_get(_r.rune_id);
        _out += (_out != "" ? ", " : "") + ((_rd != undefined) ? _rd.name : _r.rune_id) + " " + rune_tier_roman(_r.rune_tier) + " rune";
    }
    // Board-request extras (BOARD_REQUESTS_SPEC.md): dust / item roll / Reforge Chit.
    if (variable_struct_exists(_r, "dust") && _r.dust > 0) _out += (_out != "" ? ", " : "") + string(_r.dust) + " dust";
    if (variable_struct_exists(_r, "item") && _r.item)     _out += (_out != "" ? " " : "") + "+ item";
    if (variable_struct_exists(_r, "chit") && _r.chit > 0) _out += (_out != "" ? " " : "") + "+ " + item_rarity_name(variable_struct_exists(_r, "chit_tier") ? clamp(_r.chit_tier, 0, 4) : 0) + " Reforge Ingot";
    return (_out == "") ? "Their thanks" : _out;
}

// ---------------------------------------------------------------------------
// ui_draw_item_codex() - RETIRED IN PLACE (07-29 M pass): the Journal's codex
// tab is the full inline codex now (bestiary-style list + detail), the Enter-hop
// that opened this overlay is gone, and codex_open is never set true. Kept one
// F5-verified session for safety; delete together with the gc Step block. Note
// this overlay's list also silently OMITTED epics (it summed 4 loot tables and
// epics are affixed rare bases) - the inline tab fixes that via best-rarity-seen.
// ---------------------------------------------------------------------------
function ui_draw_item_codex() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "codex_open") || !_gc.codex_open) return;

    // Full-screen dark cover
    draw_set_alpha(0.97);
    draw_set_color(make_color_rgb(10, 12, 20));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    // Title
    draw_set_font(fnt_ui_title);
    draw_set_halign(fa_center);
    draw_set_color(c_white);
    draw_text(GUI_CX, 30, "ITEM CODEX");
    draw_set_halign(fa_left);

    // Build master item list (common -> uncommon -> rare -> legendary)
    var _gal_all = [];
    if (variable_global_exists("loot_table_common"))    { for (var _gi = 0; _gi < array_length(global.loot_table_common);    _gi++) array_push(_gal_all, global.loot_table_common[_gi]);    }
    if (variable_global_exists("loot_table_uncommon"))  { for (var _gi = 0; _gi < array_length(global.loot_table_uncommon);  _gi++) array_push(_gal_all, global.loot_table_uncommon[_gi]);  }
    if (variable_global_exists("loot_table_rare"))      { for (var _gi = 0; _gi < array_length(global.loot_table_rare);      _gi++) array_push(_gal_all, global.loot_table_rare[_gi]);      }
    if (variable_global_exists("loot_table_legendary")) { for (var _gi = 0; _gi < array_length(global.loot_table_legendary); _gi++) array_push(_gal_all, global.loot_table_legendary[_gi]); }
    var _gal_count   = array_length(_gal_all);
    var _gal_visible = 12;
    var _row_h       = 69;
    var _list_y0     = 120;

    // Build discovered set for fast lookup
    var _disc_set = [];
    if (variable_global_exists("items_discovered")) {
        for (var _di = 0; _di < array_length(global.items_discovered); _di++) {
            array_push(_disc_set, global.items_discovered[_di]);
        }
    }
    var _disc_count = array_length(_disc_set);

    // -------------------------------------------------------------------------
    // LEFT PANEL - scrollable item list (x=30, w=1080)
    // -------------------------------------------------------------------------
    for (var _ri = 0; _ri < _gal_visible; _ri++) {
        var _abs_i = _gc.codex_scroll + _ri;
        if (_abs_i >= _gal_count) break;

        var _it    = _gal_all[_abs_i];
        var _ry    = _list_y0 + _ri * _row_h;
        var _is_cur = (_abs_i == _gc.codex_cursor);

        // Check discovered
        var _disc = false;
        for (var _dci = 0; _dci < _disc_count; _dci++) {
            if (_disc_set[_dci] == _it.name) { _disc = true; break; }
        }

        // Row background
        var _bg_col = _is_cur ? make_color_rgb(28, 38, 65) : make_color_rgb(14, 16, 28);
        draw_set_alpha(_disc ? 1.0 : 0.55);
        draw_set_color(_bg_col);
        draw_rectangle(30, _ry, 1110, _ry + _row_h - 3, false);
        draw_set_alpha(1.0);
        var _is_equipped_gal = false;
        if (_disc && variable_global_exists("inventory")) {
            for (var _ei = 0; _ei < array_length(global.inventory); _ei++) {
                if (global.inventory[_ei] != undefined && global.inventory[_ei].name == _it.name) {
                    _is_equipped_gal = true; break;
                }
            }
        }
        var _bord_col = _is_cur        ? make_color_rgb(70, 100, 200)
            : (_is_equipped_gal        ? make_color_rgb(55, 185, 95)
            :                            make_color_rgb(35, 40, 62));
        draw_set_color(_bord_col);
        draw_rectangle(30, _ry, 1110, _ry + _row_h - 3, true);

        // Rarity color badge (left 6px strip)
        var _rar_col = item_rarity_color(_it.rarity);
        draw_set_alpha(_disc ? 1.0 : 0.4);
        draw_set_color(_rar_col);
        draw_rectangle(30, _ry, 36, _ry + _row_h - 3, false);
        draw_set_alpha(1.0);

        // Item name or ???
        draw_set_font(ui_font(fnt_ui));
        draw_set_halign(fa_left);
        draw_set_valign(fa_top);
        if (_disc) {
            draw_set_color(_rar_col);
            draw_text(45, _ry + 8, _it.name);
        } else {
            draw_set_color(make_color_rgb(55, 60, 85));
            draw_text(45, _ry + 8, "???");
        }

        // Slot label (right side)
        var _slot_str = string_upper(item_slot_title(_it.slot));   // was raw "RANGED_WEAPON" (M 08-14 shot)
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_halign(fa_right);
        draw_set_color(_disc ? make_color_rgb(140, 150, 190) : make_color_rgb(40, 45, 68));
        draw_text(1103, _ry + 8, _slot_str);

        // Stat preview (second line, only if discovered)
        if (_disc) {
            draw_set_halign(fa_left);
            draw_set_color(make_color_rgb(100, 110, 145));
            draw_text(45, _ry + 38, _it.effect_desc);
        }
    }

    // Scroll indicator
    if (_gal_count > _gal_visible) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_halign(fa_center);
        draw_set_color(make_color_rgb(70, 80, 110));
        draw_text(570, _list_y0 + _gal_visible * _row_h + 6,
            string(_gc.codex_scroll + 1) + " - " + string(min(_gc.codex_scroll + _gal_visible, _gal_count))
            + " of " + string(_gal_count) + "  (W/S or mouse wheel)");
    }

    // Discovered count
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_halign(fa_right);
    draw_set_color(make_color_rgb(80, 100, 150));
    draw_text(1103, 78, string(_disc_count) + " / " + string(_gal_count) + " discovered");

    // -------------------------------------------------------------------------
    // RIGHT PANEL - detail view (x=1140, w=750) or empty state
    // -------------------------------------------------------------------------
    var _dp_x = 1140;
    var _dp_y = 120;
    var _dp_w = 750;
    var _dp_h = 840;

    draw_set_alpha(0.85);
    draw_set_color(make_color_rgb(12, 14, 26));
    draw_rectangle(_dp_x, _dp_y, _dp_x + _dp_w, _dp_y + _dp_h, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(45, 55, 90));
    draw_rectangle(_dp_x, _dp_y, _dp_x + _dp_w, _dp_y + _dp_h, true);

    if (_gc.codex_detail_item != undefined) {
        var _d   = _gc.codex_detail_item;
        var _dx  = _dp_x + 27;
        var _txw = _dp_w - 54;
        var _is_leg = (variable_struct_exists(_d, "rarity") && _d.rarity == 4);

        // Rarity strip at top of detail panel
        draw_set_color(item_rarity_color(_d.rarity));
        draw_rectangle(_dp_x, _dp_y, _dp_x + _dp_w, _dp_y + 6, false);

        draw_set_halign(fa_left);
        draw_set_valign(fa_top);

        // --- Splash art box (splash sprite if it exists, else scaled item icon) ---
        var _art_sz = 198;
        var _art_x  = _dp_x + (_dp_w - _art_sz) / 2;
        var _art_y  = _dp_y + 21;
        draw_set_color(make_color_rgb(8, 10, 18));
        draw_rectangle(_art_x, _art_y, _art_x + _art_sz, _art_y + _art_sz, false);
        draw_set_color(item_rarity_color(_d.rarity));
        draw_rectangle(_art_x, _art_y, _art_x + _art_sz, _art_y + _art_sz, true);
        var _splash = item_splash_sprite(item_base_name(_d));
        if (_splash != -1 && sprite_exists(_splash)) {
            ui_draw_sprite_cover(_splash, 0, _art_x + 3, _art_y + 3, _art_sz - 6, _art_sz - 6, 1.0);
        } else {
            // Fallback: enlarge the item icon, centered in the box. Unframed (#10):
            // the art box above IS the frame - the icon's own box read as an
            // icon-within-an-icon (Chipped Spear et al.).
            ui_draw_item_icon(_art_x + (_art_sz - 150) / 2, _art_y + (_art_sz - 150) / 2, 150, _d, false);
        }

        // --- Name + rarity/slot ---
        var _ly = _art_y + _art_sz + 18;
        draw_set_font(fnt_ui_title);
        draw_set_halign(fa_center);
        draw_set_color(item_rarity_color(_d.rarity));
        draw_text_ext(_dp_x + _dp_w / 2, _ly, _d.name, -1, _txw);
        _ly += string_height_ext(_d.name, -1, _txw) + 6;
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(130, 140, 185));
        var _rs_line = string_upper(item_rarity_name(_d.rarity)) + "  *  " + string_upper(item_slot_title(_d.slot));
        draw_text(_dp_x + _dp_w / 2, _ly, _rs_line);
        _ly += string_height(_rs_line) + 12;
        draw_set_halign(fa_left);

        // Divider
        draw_set_color(make_color_rgb(45, 55, 90));
        draw_line(_dx, _ly, _dp_x + _dp_w - 27, _ly);
        _ly += 15;

        // --- Lore (legendary, gold) OR generic description ---
        if (_is_leg && variable_struct_exists(_d, "lore") && _d.lore != "") {
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(235, 205, 120));
            draw_text_ext(_dx, _ly, _d.lore, -1, _txw);
            _ly += string_height_ext(_d.lore, -1, _txw) + 12;
        } else {
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(170, 185, 215));
            var _gdesc = item_generic_desc(_d);
            draw_text_ext(_dx, _ly, _gdesc, -1, _txw);
            _ly += string_height_ext(_gdesc, -1, _txw) + 6;
            // one-line flavor from the item's effect_desc, when present
            if (variable_struct_exists(_d, "effect_desc") && _d.effect_desc != "") {
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(110, 122, 150));
                var _flav = "\"" + ui_sentence(_d.effect_desc) + "\"";
                draw_text_ext(_dx, _ly, _flav, -1, _txw);
                _ly += string_height_ext(_flav, -1, _txw) + 9;
            }
        }

        // --- Stat ranges reference ---
        draw_set_color(make_color_rgb(45, 55, 90));
        draw_line(_dx, _ly, _dp_x + _dp_w - 27, _ly);
        _ly += 15;
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(150, 165, 200));
        draw_text(_dx, _ly, "Rolls & Stats");
        _ly += string_height("Rolls & Stats") + 6;
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(190, 200, 225));
        var _ranges = item_stat_ranges_text(_d);
        draw_text_ext(_dx, _ly, _ranges, -1, _txw);
        _ly += string_height_ext(_ranges, -1, _txw) + 12;

        // --- Unique effect (legendary) ---
        if (variable_struct_exists(_d, "unique_desc") && _d.unique_desc != "") {
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(255, 200, 60));
            draw_text(_dx, _ly, "Unique Effect");
            _ly += string_height("Unique Effect") + 6;
            draw_set_color(make_color_rgb(255, 220, 100));
            draw_text_ext(_dx + 9, _ly, _d.unique_desc, -1, _txw - 9);
        }

        // Gold value
        draw_set_color(make_color_rgb(45, 55, 90));
        draw_line(_dx, _dp_y + _dp_h - 90, _dp_x + _dp_w - 27, _dp_y + _dp_h - 90);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(200, 170, 60));
        draw_text(_dx, _dp_y + _dp_h - 69, "Value:  " + string(_d.gold_value) + "g");

        // Close hint
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_halign(fa_right);
        draw_set_color(make_color_rgb(70, 80, 118));
        draw_text_outline(_dp_x + _dp_w - 27, _dp_y + _dp_h - 69, "Esc / click to close");
        draw_set_halign(fa_left);

    } else {
        // Empty state
        draw_set_font(ui_font(fnt_ui));
        draw_set_halign(fa_center);
        draw_set_valign(fa_middle);
        draw_set_color(make_color_rgb(45, 52, 82));
        draw_text(_dp_x + _dp_w / 2, _dp_y + _dp_h / 2, "Select a discovered item\nto view details");
        draw_set_valign(fa_top);
    }

    // Footer hint
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_halign(fa_center);
    draw_set_color(make_color_rgb(60, 68, 100));
    ui_draw_key_legend(GUI_CX, 1044, (input_device() == 2)
        ? "Tap a row to inspect   -   CLOSE or Back to exit"
        : "W/S: Navigate   Enter: Inspect   G / Esc: Close Gallery");
    draw_set_halign(fa_left);

    // CLOSE button, top-right - every device (08-19 mouse pass: desktop had Esc only).
    {
        var _cx1 = 1758, _cy1 = 27, _cx2 = 1890, _cy2 = 79;
        var _c_on = mouse_check_button(mb_left)
            && point_in_rectangle(device_mouse_x_to_gui(0), device_mouse_y_to_gui(0), _cx1, _cy1, _cx2, _cy2);
        draw_set_alpha(_c_on ? 0.95 : 0.82);
        draw_set_color(_c_on ? make_color_rgb(52, 44, 26) : make_color_rgb(14, 16, 24));
        draw_rectangle(_cx1, _cy1, _cx2, _cy2, false);
        draw_set_alpha(1.0);
        draw_set_color(_c_on ? make_color_rgb(245, 195, 80) : make_color_rgb(150, 130, 90));
        draw_rectangle(_cx1, _cy1, _cx2, _cy2, true);
        draw_set_halign(fa_center); draw_set_valign(fa_middle);
        draw_set_color(_c_on ? make_color_rgb(245, 205, 120) : make_color_rgb(215, 200, 165));
        draw_text((_cx1 + _cx2) / 2, (_cy1 + _cy2) / 2 + 1, "CLOSE");
        draw_set_halign(fa_left); draw_set_valign(fa_top);
        if (touch_tapped(_cx1, _cy1, _cx2, _cy2)) touch_press(vk_escape);
    }

    draw_set_color(c_white);
    draw_set_font(-1);
}

function ui_draw_journal() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "journal_open") || !_gc.journal_open) return;
    var _at_hub = (room == rm_hub);

    // Dim + panel + frame (vendor-screen footprint).
    draw_set_alpha(0.86); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);
    var _x1 = 120, _y1 = 66, _x2 = 1800, _y2 = 1020;
    draw_set_color(make_color_rgb(18, 17, 24));
    draw_rectangle(_x1, _y1, _x2, _y2, false);
    ui_draw_gothic_frame(_x1, _y1, _x2, _y2, 36);

    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(228, 215, 180));
    draw_text(_x1 + 45, _y1 + 36, "Journal");

    // Tab chips (7 since 08-13 - BLUEPRINTS added, the Pattern Book's
    // read-anywhere reference copy).
    var _tabs = ["RELATIONSHIPS", "QUESTS", "COMPENDIUM", "ITEM CODEX", "BESTIARY", "CREATURES", "BLUEPRINTS"];
    var _tx = _x1 + 340;
    draw_set_font(ui_font(fnt_ui_small));
    for (var _t = 0; _t < 7; _t++) {
        var _hot = (_gc.journal_tab == _t);
        // 08-06: padding trimmed 48->36 (and the gap 24->16 below) so six chips
        // still clear _x2 at 1920x1080 with the longest labels.
        var _tw  = string_width(_tabs[_t]) + 36;
        draw_set_color(_hot ? make_color_rgb(52, 48, 66) : make_color_rgb(26, 26, 34));
        draw_rectangle(_tx, _y1 + 36, _tx + _tw, _y1 + 84, false);
        draw_set_color(_hot ? make_color_rgb(228, 205, 140) : make_color_rgb(70, 72, 92));
        draw_rectangle(_tx, _y1 + 36, _tx + _tw, _y1 + 84, true);
        draw_set_color(_hot ? make_color_rgb(240, 232, 250) : make_color_rgb(140, 145, 165));
        draw_set_halign(fa_center);
        draw_text(_tx + _tw / 2, _y1 + 45, _tabs[_t]);
        draw_set_halign(fa_left);
        _tx += _tw + 16;
    }

    var _list_x1 = _x1 + 45,  _list_x2 = _x1 + 640;
    var _det_x1  = _x1 + 680, _det_x2  = _x2 - 45;
    var _top     = _y1 + 130, _bot     = _y2 - 84;
    // Sub-panels.
    draw_set_color(make_color_rgb(12, 12, 18));
    draw_rectangle(_list_x1 - 14, _top - 12, _list_x2 + 14, _bot, false);
    draw_rectangle(_det_x1 - 14,  _top - 12, _det_x2 + 14,  _bot, false);
    draw_set_color(make_color_rgb(52, 54, 74));
    draw_rectangle(_list_x1 - 14, _top - 12, _list_x2 + 14, _bot, true);
    draw_rectangle(_det_x1 - 14,  _top - 12, _det_x2 + 14,  _bot, true);

    if (_gc.journal_tab == 0) {
        // ============ RELATIONSHIPS ============
        var _ids = journal_met_ids();
        var _n   = array_length(_ids);
        var _cur = clamp(_gc.journal_cursor, 0, max(0, _n - 1));
        if (_n == 0) {
            draw_set_font(ui_font(fnt_ui)); draw_set_color(make_color_rgb(150, 156, 175));
            draw_text(_list_x1, _top + 12, "No one yet. Talk to the townsfolk.");
        }
        // Windowed like the quests list (M 07-20): the met-NPC roster (7+ hub
        // townsfolk) overran the panel bottom with no scroll. Keyboard-only nav,
        // so windowing touches the draw only. 7 portrait rows fit _top.._bot.
        var _row_h = 96;
        var _rl_vis   = 7;
        var _rl_first = ui_list_window("journal_rels", _cur, _n, _rl_vis);
        var _rl_last  = min(_n, _rl_first + _rl_vis);
        var _rl_y0    = _top;
        if (_rl_first > 0) {
            draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
            draw_set_halign(fa_center);
            ui_draw_scroll_more((_list_x1 + _list_x2) / 2, _rl_y0, true, string(_rl_first) + " more");
            draw_set_halign(fa_left);
            _rl_y0 += 26;
        }
        for (var _i = _rl_first; _i < _rl_last; _i++) {
            var _id = _ids[_i];
            var _ry = _rl_y0 + (_i - _rl_first) * _row_h;
            var _hot = (_i == _cur);
            draw_set_color(_hot ? make_color_rgb(40, 42, 58) : make_color_rgb(20, 21, 30));
            draw_rectangle(_list_x1 - 6, _ry, _list_x2 + 6, _ry + _row_h - 10, false);
            draw_set_color(_hot ? make_color_rgb(210, 185, 120) : make_color_rgb(52, 56, 76));
            draw_rectangle(_list_x1 - 6, _ry, _list_x2 + 6, _ry + _row_h - 10, true);
            // Portrait thumb.
            var _ps = journal_npc_portrait(_id);
            if (_ps >= 0) ui_draw_sprite_cover(_ps, 0, _list_x1 + 2, _ry + 7, 72, 72, 1.0, 0);
            draw_set_color(make_color_rgb(52, 56, 76));
            draw_rectangle(_list_x1 + 2, _ry + 7, _list_x1 + 74, _ry + 79, true);
            // Name + tier.
            draw_set_font(ui_font(fnt_ui)); draw_set_color(_hot ? c_white : make_color_rgb(205, 208, 222));
            draw_text(_list_x1 + 94, _ry + 12, npc_display_name(_id));
            draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(210, 190, 130));
            draw_text(_list_x1 + 94, _ry + 50, affinity_tier_name(_id));
            // Update badge.
            if (journal_npc_badged(_id)) {
                draw_set_color(make_color_rgb(235, 180, 80));
                draw_circle(_list_x2 - 16, _ry + 22, 7, false);
            }
        }
        if (_rl_last < _n) {
            draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
            draw_set_halign(fa_center);
            ui_draw_scroll_more((_list_x1 + _list_x2) / 2, _rl_y0 + (_rl_last - _rl_first) * _row_h, false, string(_n - _rl_last) + " more");
            draw_set_halign(fa_left);
        }
        // ---- Profile pane ----
        if (_n > 0) {
            var _pid = _ids[_cur];
            var _px  = _det_x1, _py = _top + 6;
            var _pspr = journal_npc_portrait(_pid);
            if (_pspr >= 0) ui_draw_sprite_cover(_pspr, 0, _px, _py, 210, 210, 1.0, 0);
            draw_set_color(make_color_rgb(60, 64, 88));
            draw_rectangle(_px, _py, _px + 210, _py + 210, true);
            draw_set_font(ui_font(fnt_ui)); draw_set_color(c_white);
            draw_text(_px + 240, _py + 6, npc_display_name(_pid));
            draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(210, 190, 130));
            draw_text(_px + 240, _py + 48, affinity_tier_name(_pid));
            // Diegetic progress bar toward the next gate (never the raw score).
            if (affinity_tier(_pid) < 4) {
                var _bw2 = 380, _bh2 = 12, _bx2 = _px + 240, _by2 = _py + 92;
                draw_set_color(make_color_rgb(40, 44, 56));
                draw_rectangle(_bx2, _by2, _bx2 + _bw2, _by2 + _bh2, false);
                draw_set_color(make_color_rgb(210, 190, 130));
                draw_rectangle(_bx2, _by2, _bx2 + _bw2 * affinity_progress_frac(_pid), _by2 + _bh2, false);
                draw_set_color(make_color_rgb(90, 96, 110));
                draw_rectangle(_bx2, _by2, _bx2 + _bw2, _by2 + _bh2, true);
                draw_set_color(make_color_rgb(140, 145, 165));
                draw_text(_bx2, _by2 + 20, affinity_gate_ready(_pid)
                    ? "Ready to grow closer - see them at the hub."
                    : "Growing closer...");
            } else {
                draw_set_color(make_color_rgb(226, 150, 150));
                draw_text(_px + 240, _py + 92, "Yours, and you theirs.");
            }
            // Lore blurb.
            var _by3 = _py + 232;
            draw_set_color(make_color_rgb(185, 190, 205));
            draw_text_ext(_px, _by3, journal_npc_blurb(_pid), 27, _det_x2 - _px - 10);
            _by3 += string_height_ext(journal_npc_blurb(_pid), 27, _det_x2 - _px - 10) + 22;
            // Gift tastes (Phase 4b live): revealed cells show the category, the rest
            // stay ??? - counts make the collection progress visible without spoiling.
            draw_set_color(make_color_rgb(120, 200, 140));
            draw_text(_px, _by3, "TASTES"); _by3 += 30;
            var _tcats  = ["weapons", "armor", "jewelry", "potions", "runes", "feed"];
            var _tnames = ["Weapons", "Armor", "Jewelry", "Potions", "Runes", "Pet Feed"];
            var _tline1 = "", _tline2 = "";
            var _bands  = ["loved", "liked", "neutral", "disliked", "hated"];
            var _blabel = ["Loves", "Likes", "Shrugs at", "Dislikes", "Hates"];
            for (var _tb = 0; _tb < 5; _tb++) {
                var _cell = "";
                var _hidden = 0;
                for (var _tc = 0; _tc < 6; _tc++) {
                    if (gift_taste(_pid, _tcats[_tc]) != _bands[_tb]) continue;
                    if (gift_taste_known(_pid, _tcats[_tc])) _cell += (_cell != "" ? ", " : "") + _tnames[_tc];
                    else _hidden++;
                }
                if (_hidden > 0) _cell += (_cell != "" ? ", " : "") + "???";
                if (_cell == "") continue;   // no categories in this band
                var _frag = _blabel[_tb] + ": " + _cell;
                if (_tb < 2) _tline1 += (_tline1 != "" ? "      " : "") + _frag;
                else         _tline2 += (_tline2 != "" ? "      " : "") + _frag;
            }
            draw_set_color(make_color_rgb(140, 145, 165));
            draw_text(_px, _by3, _tline1); _by3 += 28;
            draw_text(_px, _by3, _tline2); _by3 += 40;
            // Interaction ledger (newest last; show the most recent 6).
            draw_set_color(make_color_rgb(120, 200, 140));
            draw_text(_px, _by3, "MY NOTES"); _by3 += 30;
            var _led = npc_ledger(_pid);
            var _ln  = array_length(_led);
            if (_ln == 0) {
                draw_set_color(make_color_rgb(120, 126, 148));
                draw_text(_px, _by3, "Nothing worth writing down yet.");
                _by3 += 30;
            } else {
                var _l0 = max(0, _ln - 6);
                for (var _li = _l0; _li < _ln; _li++) {
                    draw_set_color(make_color_rgb(170, 176, 195));
                    draw_text_ext(_px, _by3, "- " + _led[_li].text, 24, _det_x2 - _px - 10);
                    _by3 += string_height_ext("- " + _led[_li].text, 24, _det_x2 - _px - 10) + 6;
                }
            }
            // Pinned active quest with this NPC.
            var _pq = "";
            var _qrows = journal_quest_rows();
            for (var _qi = 0; _qi < array_length(_qrows); _qi++) {
                var _qd2 = quest_def(_qrows[_qi]);
                var _qs2 = quest_state(_qrows[_qi]);
                if (_qd2 != undefined && _qd2.npc == _pid && _qs2.status == "active") { _pq = _qrows[_qi]; break; }
            }
            if (_pq != "") {
                var _pqd = quest_def(_pq); var _pqs = quest_state(_pq);
                _by3 += 10;
                draw_set_color(make_color_rgb(228, 205, 140));
                draw_text(_px, _by3, "QUEST:  " + _pqd.name + "   ("
                    + string(min(_pqs.progress, _pqd.obj_target)) + " / " + string(_pqd.obj_target) + ")");
            }
        }
    } else if (_gc.journal_tab == 1) {
        // ============ QUESTS ============
        var _rows = journal_quest_rows();
        var _qn   = array_length(_rows);
        var _qcur = clamp(_gc.journal_cursor, 0, max(0, _qn - 1));
        if (_qn == 0) {
            draw_set_font(ui_font(fnt_ui)); draw_set_color(make_color_rgb(150, 156, 175));
            draw_text(_list_x1, _top + 12, "No quests yet.");
        }
        // Windowed list (M 07-20 screenshot: a full COMPLETED history ran off the
        // bottom of the panel with no scroll). Edge-triggered window follows the
        // cursor; drawn-triangle "N more" indicators show what's clipped. Mirrors
        // the tavern-board list above. No mouse hit-test here (W/S nav only), so the
        // window only affects the draw.
        // 7 (not 8): the window can straddle ALL THREE group headers at once
        // (ACTIVE + AVAILABLE + COMPLETED = 99px), which at 8 rows shoved the "N more"
        // down-hint below the panel border (M 07-20 "3 more" shot). 7 rows + 3 headers
        // + both hints still fit _top.._bot (196..936).
        var _ql_vis   = 7;
        var _ql_first = ui_list_window("journal_quests", _qcur, _qn, _ql_vis);
        var _ql_last  = min(_qn, _ql_first + _ql_vis);
        var _qy = _top;
        if (_ql_first > 0) {
            draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
            draw_set_halign(fa_center);
            ui_draw_scroll_more((_list_x1 + _list_x2) / 2, _qy, true, string(_ql_first) + " more");
            draw_set_halign(fa_left);
            _qy += 26;
        }
        var _last_status = "";
        for (var _i2 = _ql_first; _i2 < _ql_last; _i2++) {
            var _qid = _rows[_i2];
            var _qd  = quest_def(_qid);
            var _qs  = quest_state(_qid);
            // Group header when the status band changes (rows are pre-grouped).
            // The "" seed forces a header on the first WINDOWED row too, so a window
            // that opens mid-COMPLETED still shows which section you're in.
            if (_qs.status != _last_status) {
                _last_status = _qs.status;
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(120, 200, 140));
                var _glabel = (_qs.status == "active") ? "ACTIVE" : ((_qs.status == "available") ? "AVAILABLE" : "COMPLETED");
                draw_text(_list_x1, _qy, _glabel);
                _qy += 33;
            }
            var _qhot = (_i2 == _qcur);
            draw_set_color(_qhot ? make_color_rgb(40, 42, 58) : make_color_rgb(20, 21, 30));
            draw_rectangle(_list_x1 - 6, _qy, _list_x2 + 6, _qy + 66, false);
            draw_set_color(_qhot ? make_color_rgb(210, 185, 120) : make_color_rgb(52, 56, 76));
            draw_rectangle(_list_x1 - 6, _qy, _list_x2 + 6, _qy + 66, true);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_qs.status == "done" ? make_color_rgb(130, 136, 155) : (_qhot ? c_white : make_color_rgb(205, 208, 222)));
            draw_text(_list_x1 + 10, _qy + 6, _qd.name);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(140, 145, 165));
            draw_text(_list_x1 + 10, _qy + 40, npc_display_name(_qd.npc)
                + ((_qs.status == "active") ? ("   " + string(min(_qs.progress, _qd.obj_target)) + " / " + string(_qd.obj_target)
                    + (quest_is_complete(_qid) ? "  -  READY" : "")) : ""));
            if (journal_quest_badged(_qid)) {
                draw_set_color(make_color_rgb(235, 180, 80));
                draw_circle(_list_x2 - 16, _qy + 16, 7, false);
            }
            _qy += 76;
        }
        if (_ql_last < _qn) {
            draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
            draw_set_halign(fa_center);
            ui_draw_scroll_more((_list_x1 + _list_x2) / 2, _qy, false, string(_qn - _ql_last) + " more");
            draw_set_halign(fa_left);
        }
        // ---- Quest detail pane ----
        if (_qn > 0) {
            var _sid = _rows[_qcur];
            var _sd  = quest_def(_sid);
            var _ss  = quest_state(_sid);
            var _dx2 = _det_x1, _dy2 = _top + 6;
            draw_set_font(ui_font(fnt_ui)); draw_set_color(c_white);
            draw_text(_dx2, _dy2, _sd.name); _dy2 += 46;
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(210, 190, 130));
            draw_text(_dx2, _dy2, "For " + npc_display_name(_sd.npc)); _dy2 += 40;
            draw_set_color(make_color_rgb(205, 208, 222));
            draw_text(_dx2, _dy2, "Objective:  " + _sd.objective
                + "   (" + string(min(_ss.progress, _sd.obj_target)) + " / " + string(_sd.obj_target) + ")"); _dy2 += 36;
            draw_set_color(make_color_rgb(185, 200, 160));
            draw_text(_dx2, _dy2, "Reward:  " + journal_quest_reward_text(_sd)); _dy2 += 48;
            draw_set_color(make_color_rgb(170, 176, 195));
            draw_text_ext(_dx2, _dy2, _sd.flavor, 27, _det_x2 - _dx2 - 10);
            _dy2 += string_height_ext(_sd.flavor, 27, _det_x2 - _dx2 - 10) + 30;
            // Status line (view-only since Phase 4b - the Tavern board owns the actions).
            if (_ss.status == "done") {
                draw_set_color(make_color_rgb(130, 136, 155));
                draw_text(_dx2, _dy2, "Fulfilled.");
            } else if (quest_is_complete(_sid)) {
                draw_set_color(make_color_rgb(120, 220, 140));
                draw_text(_dx2, _dy2, "Done - report to the tavern's request board.");
            } else if (_ss.status == "active") {
                draw_set_color(make_color_rgb(140, 145, 165));
                draw_text(_dx2, _dy2, "Underway - progress ticks as you play.");
            } else {
                draw_set_color(make_color_rgb(228, 205, 140));
                draw_text(_dx2, _dy2, "Posted at the tavern's request board.");
            }
        }
    } else if (_gc.journal_tab == 2) {
        // ============ COMPENDIUM (moved from the character menu, 2026-07-04) ======
        var _cs   = ui_compendium_sections();
        var _ccount = array_length(_cs);
        var _csel = clamp(_gc.compendium_section, 0, _ccount - 1);
        // Windowed like the other journal lists (M 07-20): 13+ sections overran the
        // panel bottom (this list never had scrolling - it just grew past the frame
        // as sections were added). Cursor is _csel (its own nav var); no headers,
        // no mouse hit-test. 10 rows fit _top.._bot.
        var _cp_vis   = 10;
        var _cp_first = ui_list_window("journal_compendium", _csel, _ccount, _cp_vis);
        var _cp_last  = min(_ccount, _cp_first + _cp_vis);
        var _cy0 = _top;
        if (_cp_first > 0) {
            draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
            draw_set_halign(fa_center);
            ui_draw_scroll_more((_list_x1 + _list_x2) / 2, _cy0, true, string(_cp_first) + " more");
            draw_set_halign(fa_left);
            _cy0 += 26;
        }
        for (var _ci = _cp_first; _ci < _cp_last; _ci++) {
            var _cy  = _cy0 + (_ci - _cp_first) * 66;
            var _chot = (_ci == _csel);
            draw_set_color(_chot ? make_color_rgb(40, 42, 58) : make_color_rgb(20, 21, 30));
            draw_rectangle(_list_x1 - 6, _cy, _list_x2 + 6, _cy + 56, false);
            draw_set_color(_chot ? make_color_rgb(210, 185, 120) : make_color_rgb(52, 56, 76));
            draw_rectangle(_list_x1 - 6, _cy, _list_x2 + 6, _cy + 56, true);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_chot ? c_white : make_color_rgb(180, 186, 205));
            draw_text(_list_x1 + 12, _cy + 12, _cs[_ci].title);
        }
        if (_cp_last < _ccount) {
            draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
            draw_set_halign(fa_center);
            ui_draw_scroll_more((_list_x1 + _list_x2) / 2, _cy0 + (_cp_last - _cp_first) * 66, false, string(_ccount - _cp_last) + " more");
            draw_set_halign(fa_left);
        }
        // Detail: the section's entries (term + text lines).
        var _cdy = _top + 6;
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(228, 205, 140));
        draw_text(_det_x1, _cdy, _cs[_csel].title); _cdy += 48;
        draw_set_font(ui_font(fnt_ui_small));
        var _ces = _cs[_csel].entries;
        for (var _ce = 0; _ce < array_length(_ces); _ce++) {
            if (_cdy > _bot - 40) break;
            var _cent = _ces[_ce];
            draw_set_color(make_color_rgb(200, 210, 235));
            draw_text(_det_x1, _cdy, _cent.term);
            draw_set_color(make_color_rgb(160, 166, 186));
            var _ctw = _det_x2 - _det_x1 - 260;
            draw_text_ext(_det_x1 + 250, _cdy, _cent.text, 25, _ctw);
            _cdy += max(33, string_height_ext(_cent.text, 25, _ctw) + 12);
        }
    } else if (_gc.journal_tab == 3) {
        // ============ ITEM CODEX (rebuilt inline 07-29 - M: the old tab was an
        // empty stub with an Enter-hop into a separate gallery, "redundant and
        // not fluid". Now mirrors the Bestiary: list left, detail right.) ======
        var _cdx  = item_codex_master_list();
        var _cn   = array_length(_cdx);
        var _ccu  = clamp(_gc.journal_cursor, 0, max(0, _cn - 1));
        // Draw-side normalization: the frame the journal opens, Step hasn't
        // hopped the cursor off the leading section header yet.
        var _cnrm = 0;
        while (_ccu < _cn - 1 && codex_entry_is_header(_cdx[_ccu]) && _cnrm++ < _cn) _ccu++;
        var _crow = 46;
        var _cvis = max(1, floor((_bot - _top - 10) / _crow));
        var _cfirst = clamp(_ccu - (_cvis - 1), 0, max(0, _cn - _cvis));
        if (_ccu < _cfirst) _cfirst = _ccu;
        var _clast  = min(_cn, _cfirst + _cvis);
        // Discovered counter runs over ITEM rows only (headers aren't entries).
        var _disc_n = 0, _cn_items = 0;
        for (var _dn = 0; _dn < _cn; _dn++) {
            if (codex_entry_is_header(_cdx[_dn])) continue;
            _cn_items++;
            if (codex_entry_discovered(_cdx[_dn])) _disc_n++;
        }
        for (var _ci2 = _cfirst; _ci2 < _clast; _ci2++) {
            var _ce2   = _cdx[_ci2];
            var _cy2   = _top + (_ci2 - _cfirst) * _crow;
            // Section header row: a rarity-tinted band title, not selectable.
            if (codex_entry_is_header(_ce2)) {
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(item_rarity_color(_ce2.rarity));
                draw_text(_list_x1 + 2, _cy2 + 10, "- " + _ce2.title + " -");
                draw_set_color(make_color_rgb(45, 55, 90));
                draw_line(_list_x1 + 2, _cy2 + _crow - 8, _list_x2 + 6, _cy2 + _crow - 8);
                continue;
            }
            var _chot2 = (_ci2 == _ccu);
            var _cdisc = codex_entry_discovered(_ce2);
            draw_set_color(_chot2 ? make_color_rgb(40, 42, 58) : make_color_rgb(20, 21, 30));
            draw_rectangle(_list_x1 - 6, _cy2, _list_x2 + 6, _cy2 + _crow - 6, false);
            draw_set_color(_chot2 ? make_color_rgb(210, 185, 120) : make_color_rgb(52, 56, 76));
            draw_rectangle(_list_x1 - 6, _cy2, _list_x2 + 6, _cy2 + _crow - 6, true);
            // Rarity strip: the ROW'S band color (EPIC rows are purple even as
            // ???), dimmed until discovered.
            draw_set_alpha(_cdisc ? 1.0 : 0.35);
            draw_set_color(item_rarity_color(_ce2.rarity));
            draw_rectangle(_list_x1 - 6, _cy2, _list_x1, _cy2 + _crow - 6, false);
            draw_set_alpha(1.0);
            draw_set_font(ui_font(fnt_ui_small));
            if (_cdisc) {
                draw_set_color(_chot2 ? c_white : merge_color(item_rarity_color(_ce2.rarity), c_white, 0.35));
                draw_text(_list_x1 + 10, _cy2 + 8, _ce2.name);
            } else {
                draw_set_color(make_color_rgb(78, 82, 104));
                draw_text(_list_x1 + 10, _cy2 + 8, "???");
            }
            draw_set_halign(fa_right);
            draw_set_color(_cdisc ? make_color_rgb(130, 136, 158) : make_color_rgb(60, 64, 84));
            draw_text(_list_x2 - 6, _cy2 + 8, string_upper(item_slot_title(_ce2.slot)));
            draw_set_halign(fa_left);
            // Touch (same-task parity): tap a row to select it.
            if (touch_tapped(_list_x1 - 6, _cy2, _list_x2 + 6, _cy2 + _crow - 6)) {   // click too (08-19 mouse pass)
                _gc.journal_cursor = _ci2;
            }
        }
        if (_cfirst > 0) {
            draw_set_color(make_color_rgb(120, 140, 170));
            ui_draw_scroll_more(_list_x1, _top - 30, true, "more");
        }
        if (_clast < _cn) {
            draw_set_color(make_color_rgb(120, 140, 170));
            ui_draw_scroll_more(_list_x1, _bot - 30 + 6, false, string(_cn - _clast) + " more");
        }
        // Detail pane - full record for a discovered entry, a tease otherwise.
        if (_cn > 0 && !codex_entry_is_header(_cdx[_ccu])) {
            var _d    = _cdx[_ccu];
            var _ddis = codex_entry_discovered(_d);
            var _depc = (variable_struct_exists(_d, "codex_epic") && _d.codex_epic);
            var _dx   = _det_x1;
            var _txw  = _det_x2 - _det_x1 - 10;
            var _dcx  = (_det_x1 + _det_x2) / 2;
            // Discovered counter owns the pane's top-right corner.
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_halign(fa_right);
            draw_set_color(make_color_rgb(110, 125, 165));
            draw_text(_det_x2, _top - 4, string(_disc_n) + " / " + string(_cn_items) + " discovered");
            draw_set_halign(fa_left);
            if (!_ddis) {
                draw_set_font(ui_font(fnt_ui));
                draw_set_halign(fa_center);
                draw_set_color(make_color_rgb(90, 96, 122));
                draw_text(_dcx, _top + 160, "???");
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(120, 126, 152));
                var _dtease = _depc
                    ? "\n\nUndiscovered - this base can drop as EPIC, carrying two greater affixes. Find one and the codex will record it."
                    : "\n\nUndiscovered - find or buy one and the codex will record it.";
                draw_text_ext(_dcx, _top + 220,
                    string_upper(item_rarity_name(_d.rarity)) + "  *  " + string_upper(item_slot_title(_d.slot))
                    + _dtease, 30, _txw - 80);
                draw_set_halign(fa_left);
            } else {
                var _is_leg = (_d.rarity == 4);
                // Splash art (or the item icon enlarged) in a rarity-rimmed box.
                var _art_sz = 172;
                var _art_x  = _dcx - _art_sz / 2;
                var _art_y  = _top + 4;
                draw_set_color(make_color_rgb(8, 10, 18));
                draw_rectangle(_art_x, _art_y, _art_x + _art_sz, _art_y + _art_sz, false);
                draw_set_color(item_rarity_color(_d.rarity));
                draw_rectangle(_art_x, _art_y, _art_x + _art_sz, _art_y + _art_sz, true);
                var _splash = item_splash_sprite(item_base_name(_d));
                if (_splash != -1 && sprite_exists(_splash)) {
                    ui_draw_sprite_cover(_splash, 0, _art_x + 3, _art_y + 3, _art_sz - 6, _art_sz - 6, 1.0);
                } else {
                    ui_draw_item_icon(_art_x + (_art_sz - 130) / 2, _art_y + (_art_sz - 130) / 2, 130, _d, false);
                }
                var _ly = _art_y + _art_sz + 12;
                draw_set_font(ui_font(fnt_ui));
                draw_set_halign(fa_center);
                draw_set_color(item_rarity_color(_d.rarity));
                draw_text_ext(_dcx, _ly, _d.name, -1, _txw);
                _ly += string_height_ext(_d.name, -1, _txw) + 4;
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(130, 140, 185));
                // Rarity line. Rare rows point at their purple twin in the EPIC
                // band; epic rows say what an epic IS.
                var _rs_line = string_upper(item_rarity_name(_d.rarity)) + "  *  " + string_upper(item_slot_title(_d.slot));
                if (_d.rarity == 2) _rs_line += "  *  drops up to EPIC";
                if (_depc)          _rs_line += "  *  two greater affixes";
                draw_text(_dcx, _ly, _rs_line);
                _ly += string_height(_rs_line) + 4;
                draw_set_halign(fa_left);
                draw_set_color(make_color_rgb(45, 55, 90));
                draw_line(_dx, _ly, _det_x2, _ly);
                _ly += 12;
                // Lore (legendary) or the generic description + flavor.
                draw_set_font(ui_font(fnt_ui_small));
                if (_is_leg && variable_struct_exists(_d, "lore") && _d.lore != "") {
                    draw_set_color(make_color_rgb(235, 205, 120));
                    draw_text_ext(_dx, _ly, _d.lore, 26, _txw);
                    _ly += string_height_ext(_d.lore, 26, _txw) + 10;
                } else {
                    draw_set_color(make_color_rgb(170, 185, 215));
                    var _gdesc = item_generic_desc(_d);
                    draw_text_ext(_dx, _ly, _gdesc, 26, _txw);
                    _ly += string_height_ext(_gdesc, 26, _txw) + 6;
                    if (variable_struct_exists(_d, "effect_desc") && _d.effect_desc != "") {
                        draw_set_color(make_color_rgb(110, 122, 150));
                        var _flav = "\"" + ui_sentence(_d.effect_desc) + "\"";
                        draw_text_ext(_dx, _ly, _flav, 26, _txw);
                        _ly += string_height_ext(_flav, 26, _txw) + 10;
                    }
                }
                // Stat-ranges reference + legendary unique effect, clipped to the pane.
                if (_ly < _bot - 120) {
                    draw_set_color(make_color_rgb(45, 55, 90));
                    draw_line(_dx, _ly, _det_x2, _ly);
                    _ly += 12;
                    draw_set_color(make_color_rgb(150, 165, 200));
                    draw_text(_dx, _ly, "Rolls & Stats");
                    _ly += 32;
                    draw_set_color(make_color_rgb(190, 200, 225));
                    var _ranges = item_stat_ranges_text(_d);
                    draw_text_ext(_dx, _ly, _ranges, 26, _txw);
                    _ly += string_height_ext(_ranges, 26, _txw) + 10;
                }
                if (variable_struct_exists(_d, "unique_desc") && _d.unique_desc != "" && _ly < _bot - 70) {
                    draw_set_color(make_color_rgb(255, 200, 60));
                    draw_text(_dx, _ly, "Unique Effect");
                    _ly += 30;
                    draw_set_color(make_color_rgb(255, 220, 100));
                    draw_text_ext(_dx + 9, _ly, _d.unique_desc, 26, _txw - 9);
                }
                // Gold value pinned to the pane's bottom line.
                draw_set_color(make_color_rgb(200, 170, 60));
                draw_text(_dx, _bot - 40, "Value:  " + string(_d.gold_value) + "g");
            }
        }
    } else if (_gc.journal_tab == 4) {
        // ============ BESTIARY (new 2026-07-04): per-species lore ================
        // 08-08 fix: this was a bare `else`, so it drew for EVERY tab it didn't
        // recognise - including CREATURES (tab 5), which was added later and
        // renders its own pane further down. The result was both panes stacked:
        // an enemy's name printed through the creature's name, an Elite mob's
        // lore under "You have not raised one of these", and the bestiary's
        // undiscovered SILHOUETTE floating up in the tab bar (M screenshot).
        // Tabs own their branch explicitly - never let one be the fallback.
        var _bst  = bestiary_catalog();
        var _bn   = array_length(_bst);
        var _bcur = clamp(_gc.journal_cursor, 0, max(0, _bn - 1));
        var _brow = 46;
        var _bvis = max(1, floor((_bot - _top - 10) / _brow));
        var _bfirst = clamp(_bcur - (_bvis - 1), 0, max(0, _bn - _bvis));
        if (_bcur < _bfirst) _bfirst = _bcur;
        var _blast  = min(_bn, _bfirst + _bvis);
        var _lastfam = "";
        for (var _bi = _bfirst; _bi < _blast; _bi++) {
            var _be = _bst[_bi];
            var _by = _top + (_bi - _bfirst) * _brow;
            var _bhot = (_bi == _bcur);
            draw_set_color(_bhot ? make_color_rgb(40, 42, 58) : make_color_rgb(20, 21, 30));
            draw_rectangle(_list_x1 - 6, _by, _list_x2 + 6, _by + _brow - 6, false);
            draw_set_color(_bhot ? make_color_rgb(210, 185, 120) : make_color_rgb(52, 56, 76));
            draw_rectangle(_list_x1 - 6, _by, _list_x2 + 6, _by + _brow - 6, true);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(_bhot ? c_white : make_color_rgb(190, 195, 212));
            draw_text(_list_x1 + 10, _by + 8, _be.name);
            draw_set_halign(fa_right);
            draw_set_color(make_color_rgb(130, 136, 158));
            draw_text(_list_x2 - 6, _by + 8, _be.family);
            draw_set_halign(fa_left);
        }
        if (_bfirst > 0) {
            draw_set_color(make_color_rgb(120, 140, 170));
            ui_draw_scroll_more(_list_x1, _top - 30, true, "more");
        }
        if (_blast < _bn) {
            draw_set_color(make_color_rgb(120, 140, 170));
            ui_draw_scroll_more(_list_x1, _bot - 30 + 6, false, string(_bn - _blast) + " more");
        }
        // Detail pane: name, family, creature portrait, lore.
        if (_bn > 0) {
            var _bd  = _bst[_bcur];
            var _bdy = _top + 6;
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(c_white);
            draw_text(_det_x1, _bdy, _bd.name); _bdy += 46;
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(210, 190, 130));
            // Family immunities ride the header line (M-locked 08-17): "Ashen Vault - Elite  |  IMMUNE: Bleed, Stun".
            var _bimt = enemy_immunity_text(_bd.name);
            draw_text(_det_x1, _bdy, _bd.family + "  -  " + _bd.kind + ((_bimt != "") ? ("   |   IMMUNE: " + _bimt) : "")); _bdy += 44;
            // #8: the creature itself, SOUTH-facing (frame 0 of the 8-dir sheet),
            // centred between the header and the lore with a footing shadow.
            var _bsm = enemy_sprite_map();
            if (variable_struct_exists(_bsm, _bd.name)) {
                var _bspr = variable_struct_get(_bsm, _bd.name);
                var _bsc  = 180 / max(1, sprite_get_height(_bspr));
                var _bw   = sprite_get_width(_bspr) * _bsc;
                var _bcx  = (_det_x1 + _det_x2) / 2;
                ui_draw_ground_shadow(_bcx, _bdy + 180 * 0.94, _bw);
                draw_sprite_ext(_bspr, 0, _bcx - _bw / 2, _bdy, _bsc, _bsc, 0, c_white, 1);
                _bdy += 202;
            }
            draw_set_color(make_color_rgb(170, 176, 195));
            draw_text_ext(_det_x1, _bdy, _bd.lore, 28, _det_x2 - _det_x1 - 10);
        }
    }

    if (_gc.journal_tab == 5) {
        // ============ CREATURES (08-06, DESIGN_WORLD_EXPANSION_0806.md §10) =====
        // The collection view. Undiscovered species stay in the list as blacked
        // out silhouettes - seeing the SHAPE of what you are missing is the whole
        // hook, and it mirrors how the Item Codex gates its rows.
        var _kc   = compendium_catalog();
        var _kn   = array_length(_kc);
        var _kcur = clamp(_gc.journal_cursor, 0, max(0, _kn - 1));
        var _krow = 46;
        var _kvis = max(1, floor((_bot - _top - 10) / _krow));
        var _kfirst = clamp(_kcur - (_kvis - 1), 0, max(0, _kn - _kvis));
        if (_kcur < _kfirst) _kfirst = _kcur;
        var _klast = min(_kn, _kfirst + _kvis);

        // Progress header, drawn above the list panel.
        var _kpg = compendium_progress(false);
        var _kps = compendium_progress(true);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(150, 156, 178));
        draw_text(_list_x1 - 6, _top - 40,
                  "CREATURES " + string(_kpg.found) + " / " + string(_kpg.total)
                  + "      SCIONS " + string(_kps.found) + " / " + string(_kps.total));

        for (var _ki = _kfirst; _ki < _klast; _ki++) {
            var _ke  = _kc[_ki];
            var _ky  = _top + (_ki - _kfirst) * _krow;
            var _khot = (_ki == _kcur);
            var _kgot = compendium_discovered(_ke.id);
            draw_set_color(_khot ? make_color_rgb(40, 42, 58) : make_color_rgb(20, 21, 30));
            draw_rectangle(_list_x1 - 6, _ky, _list_x2 + 6, _ky + _krow - 6, false);
            draw_set_color(_khot ? make_color_rgb(210, 185, 120) : make_color_rgb(52, 56, 76));
            draw_rectangle(_list_x1 - 6, _ky, _list_x2 + 6, _ky + _krow - 6, true);
            draw_set_font(ui_font(fnt_ui_small));
            if (_kgot) {
                draw_set_color(_khot ? c_white : make_color_rgb(190, 195, 212));
                draw_text(_list_x1 + 10, _ky + 8, _ke.name);
            } else {
                draw_set_color(make_color_rgb(96, 100, 120));
                draw_text(_list_x1 + 10, _ky + 8, "? ? ?");
            }
            if (_ke.scion) {
                draw_set_halign(fa_right);
                draw_set_color(_kgot ? make_color_rgb(198, 168, 110) : make_color_rgb(84, 76, 58));
                draw_text(_list_x2 - 6, _ky + 8, "SCION");
                draw_set_halign(fa_left);
            }
        }
        // Visible scrollbar (HARD rule) - the roster is 60+ rows and never fits.
        // Same idiom as the Bairc roster bar above.
        if (_kn > _kvis) {
            var _ksb_x0 = _list_x2 + 11, _ksb_x1 = _ksb_x0 + 6;
            var _ksb_y0 = _top, _ksb_y1 = _top + _kvis * _krow - 10;
            draw_set_color(make_color_rgb(30, 34, 48));
            draw_rectangle(_ksb_x0, _ksb_y0, _ksb_x1, _ksb_y1, false);
            var _ksb_h  = max(36, (_ksb_y1 - _ksb_y0) * (_kvis / _kn));
            var _ksb_ty = _ksb_y0 + ((_ksb_y1 - _ksb_y0) - _ksb_h) * (_kfirst / max(1, _kn - _kvis));
            draw_set_color(make_color_rgb(90, 150, 210));
            draw_rectangle(_ksb_x0, _ksb_ty, _ksb_x1, _ksb_ty + _ksb_h, false);
        }

        // Touch parity (HARD rule: hit-tests live in DRAW, beside the geometry
        // they mirror). Tap a row to select it; the list scrolls with the cursor.
        if (mouse_check_button_pressed(mb_left)) {
            var _kmx = device_mouse_x_to_gui(0);
            var _kmy = device_mouse_y_to_gui(0);
            if (_kmx >= _list_x1 - 6 && _kmx <= _list_x2 + 6) {
                for (var _kti = _kfirst; _kti < _klast; _kti++) {
                    var _kty = _top + (_kti - _kfirst) * _krow;
                    if (_kmy >= _kty && _kmy <= _kty + _krow - 6) { _gc.journal_cursor = _kti; break; }
                }
            }
        }

        // ---- Detail pane ----
        if (_kn > 0) {
            var _kd   = _kc[_kcur];
            var _kgot2 = compendium_discovered(_kd.id);
            var _kdy  = _top + 6;
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(228, 215, 180));
            draw_text(_det_x1, _kdy, _kgot2 ? _kd.name : "Undiscovered"); _kdy += 46;
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(210, 190, 130));
            if (_kgot2 && _kd.scion && _kd.boss != "") {
                draw_text(_det_x1, _kdy, "Scion - kin of " + _kd.boss); _kdy += 40;
            } else if (_kd.scion) {
                draw_text(_det_x1, _kdy, "Scion"); _kdy += 40;
            }

            // ---- FORM SLOTS (M 08-13): baby / young adult / adult / awakened,
            // each revealed only once one of YOURS has reached that stage. A/D
            // (or tapping a chip) cycles; unrevealed forms stay "? ? ?".
            var _kmax    = _kgot2 ? compendium_stage_max(_kd.id) : -1;
            var _kstages = [PET_STAGE_BABY, PET_STAGE_YOUNGADULT, PET_STAGE_ADULT, PET_STAGE_AWAKENED];
            var _knames  = ["BABY", "YOUNG ADULT", "ADULT", "AWAKENED"];
            var _kform   = variable_instance_exists(_gc, "journal_form") ? clamp(_gc.journal_form, 0, 3) : 3;
            while (_kform > 0 && _kstages[_kform] > _kmax) _kform -= 1;   // clamp DOWN to a revealed slot
            _gc.journal_form = _kform;

            // Portrait: the chosen form's art (walking DOWN when a stage has no
            // bespoke sprite yet - awakened shows adult art in-game too), real
            // when discovered, flat silhouette when not. pet_sprite_fit
            // feet-anchors + fits the visible bbox (08-08 float fix).
            var _kspr = -1;
            if (_kgot2) {
                for (var _kfs = _kform; _kfs >= 0; _kfs--) {
                    _kspr = compendium_stage_sprite(_kd.id, _kstages[_kfs]);
                    if (_kspr >= 0) break;
                }
            }
            if (_kspr < 0) _kspr = compendium_sprite(_kd.id);
            if (_kspr >= 0) {
                var _kcx  = (_det_x1 + _det_x2) / 2;
                // Stage-scaled (08-19): the form being shown sets the size (baby small, adult full).
                var _kmul = _kgot2 ? pet_stage_size_mult(_kstages[_kform]) : 1.0;
                var _kfit = pet_sprite_fit(_kspr, _kcx, _kdy + 180, 180 * _kmul, ((_det_x2 - _det_x1) - 40) * _kmul);
                if (_kgot2) {
                    ui_draw_ground_shadow(_kcx, _kdy + 180, sprite_get_width(_kspr) * _kfit.scale);
                    draw_sprite_ext(_kspr, 0, _kfit.x, _kfit.y, _kfit.scale, _kfit.scale, 0, c_white, 1);
                } else {
                    // Silhouette: same sprite drawn flat black. Shows the shape of
                    // what you're missing without revealing the creature.
                    draw_sprite_ext(_kspr, 0, _kfit.x, _kfit.y, _kfit.scale, _kfit.scale, 0, c_black, 0.92);
                }
                _kdy += 202;
            }

            // The 4-slot cycle bar (discovered species only).
            if (_kgot2) {
                // Chip width MEASURED off the longest label (M 08-18 shot: "YOUNG ADULT" spilled
                // out of the fixed 150px box in Large font); the four still fit the column.
                draw_set_font(ui_font(fnt_ui_small));
                var _kch_w = 150, _kch_h = 40, _kch_gap = 12;
                for (var _kwi = 0; _kwi < 4; _kwi++) _kch_w = max(_kch_w, string_width(_knames[_kwi]) + 28);
                _kch_w = min(_kch_w, floor(((_det_x2 - _det_x1) - 3 * _kch_gap) / 4));
                var _kch_x = (_det_x1 + _det_x2) / 2 - (4 * _kch_w + 3 * _kch_gap) / 2;
                for (var _kci = 0; _kci < 4; _kci++) {
                    var _kcx0 = _kch_x + _kci * (_kch_w + _kch_gap);
                    var _krev = (_kstages[_kci] <= _kmax);
                    var _ksel = (_kci == _kform);
                    draw_set_color(_ksel ? make_color_rgb(40, 42, 58) : make_color_rgb(18, 19, 27));
                    draw_rectangle(_kcx0, _kdy, _kcx0 + _kch_w, _kdy + _kch_h, false);
                    draw_set_color(_ksel ? make_color_rgb(210, 185, 120) : make_color_rgb(52, 56, 76));
                    draw_rectangle(_kcx0, _kdy, _kcx0 + _kch_w, _kdy + _kch_h, true);
                    draw_set_halign(fa_center);
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(_krev ? (_ksel ? c_white : make_color_rgb(170, 176, 195))
                                         : make_color_rgb(84, 88, 106));
                    draw_text(_kcx0 + _kch_w / 2, _kdy + 9, _krev ? _knames[_kci] : "? ? ?");
                    draw_set_halign(fa_left);
                    // Touch parity (HARD rule): tap a revealed chip to view it.
                    if (_krev && mouse_check_button_pressed(mb_left)) {
                        var _kmx2 = device_mouse_x_to_gui(0), _kmy2 = device_mouse_y_to_gui(0);
                        if (_kmx2 >= _kcx0 && _kmx2 <= _kcx0 + _kch_w
                            && _kmy2 >= _kdy && _kmy2 <= _kdy + _kch_h) _gc.journal_form = _kci;
                    }
                }
                _kdy += _kch_h + 14;
                // Legend hint (M 08-14 fix-now #10: asked how to rotate forms -
                // nothing on screen said A/D cycles them).
                draw_set_halign(fa_center);
                draw_set_color(make_color_rgb(120, 126, 146));
                draw_text((_det_x1 + _det_x2) / 2, _kdy, "A/D or tap a form to view it");
                draw_set_halign(fa_left);
                _kdy += 32;
            }

            draw_set_color(make_color_rgb(170, 176, 195));
            if (_kgot2) {
                draw_text_ext(_det_x1, _kdy, pet_species_lore(_kd.id), 28, _det_x2 - _det_x1 - 10);
                _kdy += 34 * (1 + string_count("\n", pet_species_lore(_kd.id)));
            } else {
                draw_set_color(make_color_rgb(120, 124, 145));
                draw_text_ext(_det_x1, _kdy, "You have not raised one of these. Hatch it to record what it is.",
                              28, _det_x2 - _det_x1 - 10);
            }
        }
    }

    if (_gc.journal_tab == 6) {
        // ============ BLUEPRINTS (M 08-13): the Pattern Book, read-only ========
        // Dorn's [B] overlay stays the working shortcut; this is the reference
        // copy you can check from anywhere. Same family rows, list + detail.
        pattern_book_ensure();
        var _pbc   = pattern_family_catalog();
        var _pbn   = array_length(_pbc);
        var _pbcur = clamp(_gc.journal_cursor, 0, max(0, _pbn - 1));
        var _pbrow = 46;
        var _pbvis = max(1, floor((_bot - _top - 10) / _pbrow));
        var _pbfirst = clamp(_pbcur - (_pbvis - 1), 0, max(0, _pbn - _pbvis));
        if (_pbcur < _pbfirst) _pbfirst = _pbcur;
        var _pblast = min(_pbn, _pbfirst + _pbvis);

        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(150, 156, 178));
        draw_text(_list_x1 - 6, _top - 40, "Blueprints are learned at Dorn's by SMELTING gear."
            + "   Art page: " + string(array_length(global.pattern_book.art)) + " icons.");

        for (var _pbi = _pbfirst; _pbi < _pblast; _pbi++) {
            var _pbe   = _pbc[_pbi];
            var _pby   = _top + (_pbi - _pbfirst) * _pbrow;
            var _pbhot = (_pbi == _pbcur);
            var _pbt   = pattern_fam_tier(_pbe.stat_name);
            draw_set_color(_pbhot ? make_color_rgb(40, 42, 58) : make_color_rgb(20, 21, 30));
            draw_rectangle(_list_x1 - 6, _pby, _list_x2 + 6, _pby + _pbrow - 6, false);
            draw_set_color(_pbhot ? make_color_rgb(210, 185, 120) : make_color_rgb(52, 56, 76));
            draw_rectangle(_list_x1 - 6, _pby, _list_x2 + 6, _pby + _pbrow - 6, true);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color((_pbt > 0) ? (_pbhot ? c_white : make_color_rgb(190, 195, 212))
                                      : make_color_rgb(110, 112, 130));
            draw_text(_list_x1 + 10, _pby + 8, _pbe.label);
            draw_set_halign(fa_right);
            draw_set_color((_pbt >= 3) ? make_color_rgb(230, 195, 120) : make_color_rgb(140, 145, 165));
            draw_text(_list_x2 - 6, _pby + 8, pattern_fam_progress_text(_pbe.stat_name));
            draw_set_halign(fa_left);
        }
        if (_pbn > _pbvis) {
            var _pbsb_x0 = _list_x2 + 11, _pbsb_x1 = _pbsb_x0 + 6;
            var _pbsb_y0 = _top, _pbsb_y1 = _top + _pbvis * _pbrow - 10;
            draw_set_color(make_color_rgb(30, 34, 48));
            draw_rectangle(_pbsb_x0, _pbsb_y0, _pbsb_x1, _pbsb_y1, false);
            var _pbsb_h  = max(36, (_pbsb_y1 - _pbsb_y0) * (_pbvis / _pbn));
            var _pbsb_ty = _pbsb_y0 + ((_pbsb_y1 - _pbsb_y0) - _pbsb_h) * (_pbfirst / max(1, _pbn - _pbvis));
            draw_set_color(make_color_rgb(90, 150, 210));
            draw_rectangle(_pbsb_x0, _pbsb_ty, _pbsb_x1, _pbsb_ty + _pbsb_h, false);
        }
        // Touch parity: tap a row to select it.
        if (mouse_check_button_pressed(mb_left)) {
            var _pbmx = device_mouse_x_to_gui(0), _pbmy = device_mouse_y_to_gui(0);
            if (_pbmx >= _list_x1 - 6 && _pbmx <= _list_x2 + 6) {
                for (var _pbti = _pbfirst; _pbti < _pblast; _pbti++) {
                    var _pbty = _top + (_pbti - _pbfirst) * _pbrow;
                    if (_pbmy >= _pbty && _pbmy <= _pbty + _pbrow - 6) { _gc.journal_cursor = _pbti; break; }
                }
            }
        }

        // ---- Detail pane ----
        if (_pbn > 0) {
            var _pbd  = _pbc[_pbcur];
            var _pbdt = pattern_fam_tier(_pbd.stat_name);
            var _pbdy = _top + 6;
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(228, 215, 180));
            draw_text(_det_x1, _pbdy, _pbd.label); _pbdy += 46;
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(210, 190, 130));
            draw_text(_det_x1, _pbdy, (_pbd.kind == "school") ? "Caster Affix (amulet/ring)" : "Stat Affix"); _pbdy += 38;
            ui_pb_pips(_det_x1, _pbdy + 4, _pbdt); _pbdy += 44;
            draw_set_color(make_color_rgb(170, 176, 195));
            var _pbdd = pattern_family_desc(_pbd.stat_name);
            if (_pbdd != "") { draw_text_ext(_det_x1, _pbdy, _pbdd, 28, _det_x2 - _det_x1 - 10); _pbdy += 48; }
            draw_set_color(make_color_rgb(150, 156, 178));
            draw_text(_det_x1, _pbdy, "Progress: " + pattern_fam_progress_text(_pbd.stat_name)); _pbdy += 38;
            draw_set_color(make_color_rgb(120, 124, 145));
            draw_text_ext(_det_x1, _pbdy,
                "One study unlocks Uncommon crafts, 3 unlock Rare, 6 unlock Epic - rarer fodder teaches faster. Deeper study rolls better numbers at Dorn's CRAFT.",
                28, _det_x2 - _det_x1 - 10);
        }
    }

    // Footer.
    draw_set_halign(fa_center); draw_set_valign(fa_bottom);
    draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
    ui_draw_key_legend((_x1 + _x2) / 2, _y2 - 30, "W/S: Rows     Q/E: Tab     J / Esc: Close");
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_alpha(1.0); draw_set_color(c_white); draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// GIFT RESULT POPUP (Phase 4b UX): reaction line + bond delta + live tier and
// progress bar. Modal over the giver's window; gc dismisses on any confirm key.
// ---------------------------------------------------------------------------
function ui_draw_gift_popup() {
    if (!variable_global_exists("gift_popup") || global.gift_popup == undefined) return;
    var _gp  = global.gift_popup;
    var _pos = (_gp.delta >= 0);

    draw_set_alpha(0.55); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);
    var _w = 860, _h = 380;
    var _x1 = GUI_CX - _w / 2, _y1 = GUI_CY - _h / 2, _x2 = _x1 + _w, _y2 = _y1 + _h;
    draw_set_color(make_color_rgb(20, 19, 28));
    draw_rectangle(_x1, _y1, _x2, _y2, false);
    ui_draw_gothic_frame(_x1, _y1, _x2, _y2, 30);
    draw_set_color(_pos ? make_color_rgb(120, 200, 140) : make_color_rgb(210, 110, 100));
    draw_rectangle(_x1, _y1, _x2, _y1 + 4, false);

    draw_set_halign(fa_center); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(228, 215, 180));
    draw_text(GUI_CX, _y1 + 30, npc_display_name(_gp.npc));
    // Reaction line, wrapped.
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(205, 208, 222));
    draw_text_ext(GUI_CX, _y1 + 84, _gp.line, 27, _w - 120);
    // Bond delta.
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_pos ? make_color_rgb(120, 220, 140) : make_color_rgb(225, 110, 100));
    draw_text(GUI_CX, _y1 + 186, (_gp.delta >= 0 ? "Bond +" : "Bond ") + string(_gp.delta));
    // Tier + live progress bar (diegetic - the bar, never the number).
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(210, 190, 130));
    draw_text(GUI_CX, _y1 + 240, affinity_tier_name(_gp.npc)
        + (affinity_gate_ready(_gp.npc) ? "   -   ready to grow closer" : ""));
    var _bw = 420, _bx = GUI_CX - _bw / 2, _by = _y1 + 282, _bh = 14;
    draw_set_color(make_color_rgb(40, 44, 56));
    draw_rectangle(_bx, _by, _bx + _bw, _by + _bh, false);
    draw_set_color(make_color_rgb(210, 190, 130));
    draw_rectangle(_bx, _by, _bx + _bw * affinity_progress_frac(_gp.npc), _by + _bh, false);
    draw_set_color(make_color_rgb(90, 96, 110));
    draw_rectangle(_bx, _by, _bx + _bw, _by + _bh, true);

    draw_set_color(make_color_rgb(140, 145, 165));
    draw_text(GUI_CX, _y2 - 48, (input_device() == 2) ? "Tap Back to close" : "Enter / Esc - close");
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_alpha(1.0); draw_set_color(c_white); draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// TAVERN REQUESTS BOARD (Phase 4b UX): the diegetic quest surface - a posting
// board. List of requests grouped Active / Available / Fulfilled; Enter takes an
// available job or turns in a finished one. The Journal only TRACKS quests.
// ---------------------------------------------------------------------------
function ui_draw_tavern_board() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "tavern_board_open") || !_gc.tavern_board_open) return;

    draw_set_alpha(0.86); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);
    var _x1 = 240, _y1 = 66, _x2 = 1680, _y2 = 1020;
    // Board backdrop: the tavern-board art fills the panel (cover-crop), darkened so
    // the parchment rows stay readable. Wooden tones as the pre-import fallback.
    // NOTE: spr_tavern_board is a NEW resource - GameMaker must RELOAD the project
    // once to pick it up; until then the fallback draws.
    var _tb_bg = asset_get_index("spr_tavern_board");
    if (_tb_bg >= 0) {
        ui_draw_sprite_cover(_tb_bg, 0, _x1, _y1, _x2 - _x1, _y2 - _y1, 1.0);
        draw_set_alpha(0.52); draw_set_color(c_black);
        draw_rectangle(_x1, _y1, _x2, _y2, false);
        draw_set_alpha(1.0);
    } else {
        draw_set_color(make_color_rgb(38, 26, 18));
        draw_rectangle(_x1, _y1, _x2, _y2, false);
    }
    draw_set_color(make_color_rgb(24, 16, 11));
    draw_rectangle(_x1 + 18, _y1 + 18, _x2 - 18, _y2 - 18, true);
    ui_draw_gothic_frame(_x1, _y1, _x2, _y2, 36);

    draw_set_halign(fa_center); draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(230, 210, 160));
    draw_text((_x1 + _x2) / 2, _y1 + 33, "TAVERN REQUESTS");
    draw_set_font(ui_font(fnt_ui_small));
    // High Table invitation (dice v2) replaces the subtitle while it stands.
    kb_tourney_ensure();
    if (global.kb_tourney_ready) {
        draw_set_color(make_color_rgb(255, 205, 90));
        draw_text((_x1 + _x2) / 2, _y1 + 87, "The HIGH TABLE is set - [T] to sit. "
            + string(kb_tourney_buyin()) + "g buy-in, three seats, one pot.");
    } else {
        draw_set_color(make_color_rgb(170, 150, 120));
        draw_text((_x1 + _x2) / 2, _y1 + 87, "Jobs, hunts and favors posted by the townsfolk.");
    }
    draw_set_halign(fa_left);

    // Board rows = active + available only. Fulfilled requests come off the board
    // (their notes are "taken down") and live in the Journal's Completed group.
    var _rows = tavern_board_rows();
    var _n    = array_length(_rows);
    var _cur  = clamp(_gc.tavern_board_cursor, 0, max(0, _n - 1));
    var _lx = _x1 + 66, _rx = _x2 - 66;
    var _qy = _y1 + 144;
    if (_n == 0) {
        draw_set_font(ui_font(fnt_ui)); draw_set_color(make_color_rgb(160, 140, 115));
        draw_text(_lx, _qy, "The board is bare.");
    }
    // Windowed list (M 07-09 screenshot: with 7+ notes the last row ran under the
    // key-legend footer). 6 notes + their group headers fit between the subtitle
    // and the footer band; the window follows the cursor, drawn-triangle
    // indicators show what's clipped.
    var _bl_vis   = 6;
    var _bl_first = ui_list_window("tavern_board", _cur, _n, _bl_vis);
    var _bl_last  = min(_n, _bl_first + _bl_vis);
    if (_bl_first > 0) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(190, 170, 130));
        draw_set_halign(fa_right);
        ui_draw_scroll_more(_rx, _qy - 27, true, string(_bl_first) + " more");
        draw_set_halign(fa_left);
    }
    var _last_status = "";
    for (var _i = _bl_first; _i < _bl_last; _i++) {
        var _qid = _rows[_i];
        var _qd  = quest_def(_qid);
        var _qs  = quest_state(_qid);
        if (_qs.status != _last_status) {
            _last_status = _qs.status;
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(150, 190, 130));
            draw_text(_lx, _qy, (_qs.status == "active") ? "TAKEN BY YOU" : "POSTED");
            _qy += 33;
        }
        var _hot = (_i == _cur);
        var _is_board = quest_is_board(_qd);
        var _is_urgent  = _is_board && _qd.urgent;
        var _is_special = _is_board && board_is_special(_qd);
        // Pinned-note row: parchment tint, brighter when highlighted; urgent offers
        // get an ember-red cast so they read as "grab this before it's gone"; the
        // rotating SPECIAL posting (v2) gets a gilded cast + wax-gold pin.
        if      (_is_special) draw_set_color(_hot ? make_color_rgb(72, 58, 22)  : make_color_rgb(54, 44, 18));
        else if (_is_urgent)  draw_set_color(_hot ? make_color_rgb(74, 42, 30)  : make_color_rgb(56, 30, 22));
        else                  draw_set_color(_hot ? make_color_rgb(62, 50, 34)  : make_color_rgb(46, 36, 26));
        draw_rectangle(_lx - 12, _qy, _rx + 12, _qy + 96, false);
        if      (_is_special) draw_set_color(_hot ? make_color_rgb(255, 205, 90) : make_color_rgb(170, 130, 50));
        else if (_is_urgent)  draw_set_color(_hot ? make_color_rgb(235, 120, 80) : make_color_rgb(140, 62, 44));
        else                  draw_set_color(_hot ? make_color_rgb(220, 190, 130) : make_color_rgb(80, 64, 46));
        draw_rectangle(_lx - 12, _qy, _rx + 12, _qy + 96, true);
        // Touch (8d, punch item 4): tap a note to highlight it, tap the
        // highlighted note again to take / turn in (simulated Enter).
        if (touch_tapped(_lx - 12, _qy, _rx + 12, _qy + 96)) {   // click too (08-19 mouse pass): click selects, click again takes
            if (_i == _cur) touch_press(vk_enter);
            else { _gc.tavern_board_cursor = _i; _cur = _i; }
        }
        // "Pin"
        draw_set_color(_is_special ? make_color_rgb(255, 205, 90)
                     : (_is_urgent ? make_color_rgb(235, 80, 55) : make_color_rgb(180, 60, 50)));
        draw_circle(_lx + 6, _qy + 12, 5, false);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_qs.status == "done" ? make_color_rgb(140, 125, 105) : (_hot ? make_color_rgb(240, 228, 200) : make_color_rgb(205, 190, 165)));
        draw_text(_lx + 24, _qy + 9, (_is_special ? "SPECIAL: " : (_is_urgent ? "URGENT: " : "")) + _qd.name);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(160, 145, 120));
        var _sub = npc_display_name(_qd.npc) + "   -   " + _qd.objective;
        if (_qs.status == "active") _sub += "   (" + string(min(_qs.progress, _qd.obj_target)) + " / " + string(_qd.obj_target)
            + (quest_is_complete(_qid) ? "  -  DONE" : "") + ")";
        draw_text(_lx + 24, _qy + 48, _sub);
        draw_set_halign(fa_right);
        draw_set_color(make_color_rgb(185, 200, 160));
        draw_text(_rx, _qy + 9, journal_quest_reward_text(_qd));
        // Expiry tag (board requests): run-count clock, red when this is the last run.
        // A fulfilled-awaiting-turn-in request never rots, so no tag once it's DONE.
        if (_is_board && !quest_is_complete(_qid)) {
            if (_qd.expires <= 1) {
                draw_set_color(make_color_rgb(235, 110, 80));
                draw_text(_rx, _qy + 48, "THIS RUN ONLY");
            } else {
                draw_set_color(make_color_rgb(200, 170, 110));
                draw_text(_rx, _qy + 48, string(_qd.expires) + " runs left");
            }
        }
        draw_set_halign(fa_left);
        if (journal_quest_badged(_qid)) {
            draw_set_color(make_color_rgb(235, 180, 80));
            // "unseen" dot sits in the empty gap BETWEEN the reward row (y+9) and
            // the expiry row (y+48) so it can't overlap the right-aligned expiry
            // text - "2 runs lef[dot]" collision, M 07-20. y+60 put it into the
            // expiry line; y+38 is clear of both rows.
            draw_circle(_rx + 2, _qy + 38, 6, false);
        }
        _qy += 108;
    }
    if (_bl_last < _n) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(190, 170, 130));
        draw_set_halign(fa_right);
        ui_draw_scroll_more(_rx, _qy + 3, false, string(_n - _bl_last) + " more");
        draw_set_halign(fa_left);
    }

    // Feedback + footer.
    draw_set_halign(fa_center); draw_set_valign(fa_bottom);
    draw_set_font(ui_font(fnt_ui_small));
    if (_gc.tavern_board_note != "") {
        draw_set_color(make_color_rgb(230, 210, 150));
        draw_text((_x1 + _x2) / 2, _y2 - 72, _gc.tavern_board_note);
    }
    if (input_device() == 2) {
        // Touch: rows are tappable (tap again = take/turn in); the chip bar
        // (ui_draw_touch_chips "board" ctx) carries Knucklebones/Table/Reroll.
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(160, 145, 120));
        draw_set_halign(fa_center);
        draw_text((_x1 + _x2) / 2, _y2 - 30, "Tap a note to read it  -  tap again to take / turn in");
    } else {
        ui_draw_key_legend((_x1 + _x2) / 2, _y2 - 30, "W/S: Browse    Enter: Take / Turn in    R: Reroll ("
            + string(board_reroll_cost()) + "g)    K: Knucklebones    T: High Table    Esc: Leave    (done: Journal, J)",
            make_color_rgb(160, 145, 120));
    }
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_alpha(1.0); draw_set_color(c_white); draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_knucklebones() - the tavern dice game (expression #1). Two 3x3 boards,
// opponent on top; geometry is self-contained. Input lives in the gc Step kb
// block; this only renders gc.kb.
// ---------------------------------------------------------------------------
function ui_draw_knucklebones() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "kb_open") || !_gc.kb_open || _gc.kb == undefined) return;
    var _g = _gc.kb;

    // Near-opaque dim so the tavern board / hub text can't ghost through at the edges.
    draw_set_alpha(0.96); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);
    var _x0 = 510, _y0 = 54, _x1 = 1410, _y1 = 1026;
    draw_set_color(make_color_rgb(30, 22, 15));
    draw_rectangle(_x0, _y0, _x1, _y1, false);
    ui_draw_gothic_frame(_x0, _y0, _x1, _y1, 30);

    draw_set_halign(fa_center); draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    // High Table bracket (dice v2): gild the title and show the match count.
    var _kb_ht = variable_instance_exists(_gc, "kb_tourney") ? _gc.kb_tourney : undefined;
    if (_kb_ht != undefined) {
        draw_set_color(make_color_rgb(255, 205, 90));
        draw_text(GUI_CX, _y0 + 21, "THE HIGH TABLE");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(220, 185, 110));
        draw_text(GUI_CX, _y0 + 66, "Match " + string(_kb_ht.stage + 1) + " of 3   -   pot: "
            + string(kb_tourney_pot()) + "g + a curiosity");
        draw_set_color(make_color_rgb(170, 150, 120));
        draw_text(GUI_CX, _y0 + 96, npc_display_name(_g.foe) + " sits across from you.   " + kb_foe_line(_g.foe));
    } else {
        draw_set_color(make_color_rgb(230, 210, 160));
        draw_text(GUI_CX, _y0 + 21, "KNUCKLEBONES");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(170, 150, 120));
        draw_text(GUI_CX, _y0 + 78, npc_display_name(_g.foe) + " sits across from you.   " + kb_foe_line(_g.foe));
    }

    // --- Stake phase: pick the wager ---
    if (_g.phase == "stake") {
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(c_white);
        draw_text(GUI_CX, 420, "The stake:");
        draw_set_font(fnt_ui_title);
        draw_set_color(make_color_rgb(240, 205, 110));
        draw_text(GUI_CX, 480, "< " + string(_g.stake) + "g >");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(160, 150, 130));
        draw_text(GUI_CX, 570, "Winner takes double. Equal dice in a column multiply; matching their column smashes their dice.");
        if (_g.msg != "") { draw_set_color(make_color_rgb(220, 140, 120)); draw_text(GUI_CX, 630, _g.msg); }
        draw_set_color(make_color_rgb(150, 140, 120));
        ui_draw_key_legend(GUI_CX, _y1 - 60, "A/D: Stake     Enter: Sit down     H: Rules     Esc: Not tonight",
            make_color_rgb(150, 140, 120));
        draw_set_halign(fa_center);
        draw_set_halign(fa_left); draw_set_valign(fa_top); draw_set_font(-1);
        ui_draw_knucklebones_rules(_g);
        return;
    }

    // --- Boards. Foe on top (their slot 0 nearest the middle), player below.
    //     Foe board starts at 174 so the subtitle line above clears it. ---
    var _cell = 92, _gap = 10;
    var _colx = [GUI_CX - (_cell + _gap) - _cell / 2, GUI_CX - _cell / 2, GUI_CX + (_cell + _gap) + _cell / 2 - _cell];
    var _foe_y0  = 174;   // foe cells grow DOWN toward the middle
    var _mine_y0 = 610;

    for (var _c = 0; _c < 3; _c++) {
        // Foe column (drawn top-down, slot 2 at top so slot 0 sits by the middle).
        for (var _r = 0; _r < 3; _r++) {
            var _fv = _g.foes[_c][2 - _r];
            var _fx = _colx[_c], _fy = _foe_y0 + _r * (_cell + 6);
            draw_set_color((_fv > 0) ? make_color_rgb(64, 48, 40) : make_color_rgb(38, 30, 24));
            draw_rectangle(_fx, _fy, _fx + _cell, _fy + _cell, false);
            draw_set_color(make_color_rgb(90, 70, 52));
            draw_rectangle(_fx, _fy, _fx + _cell, _fy + _cell, true);
            if (_fv > 0) {
                draw_set_font(fnt_ui_title);
                draw_set_color(make_color_rgb(235, 215, 180));
                draw_set_valign(fa_middle);
                draw_text(_fx + _cell / 2, _fy + _cell / 2, string(_fv));
                draw_set_valign(fa_top);
            }
        }
        // Foe column score (just under their stack, above the divider).
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(190, 160, 120));
        draw_text(_colx[_c] + _cell / 2, _foe_y0 + 3 * (_cell + 6) + 3, string(kb_col_score(_g.foes, _c)));

        // Player column score above their stack.
        draw_set_color(make_color_rgb(190, 200, 160));
        draw_text(_colx[_c] + _cell / 2, _mine_y0 - 30, string(kb_col_score(_g.mine, _c)));
        // Player cursor chevron.
        if (_g.phase == "play" && _g.my_turn && _g.cursor == _c) {
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(255, 205, 90));
            draw_text(_colx[_c] + _cell / 2, _mine_y0 - 63, "v");
        }
        // Player column (slot 0 at top, growing down).
        for (var _r2 = 0; _r2 < 3; _r2++) {
            var _mv = _g.mine[_c][_r2];
            var _mx = _colx[_c], _my = _mine_y0 + _r2 * (_cell + 6);
            draw_set_color((_mv > 0) ? make_color_rgb(40, 52, 44) : make_color_rgb(26, 32, 28));
            draw_rectangle(_mx, _my, _mx + _cell, _my + _cell, false);
            draw_set_color(make_color_rgb(70, 96, 76));
            draw_rectangle(_mx, _my, _mx + _cell, _my + _cell, true);
            if (_mv > 0) {
                draw_set_font(fnt_ui_title);
                draw_set_color(make_color_rgb(200, 235, 205));
                draw_set_valign(fa_middle);
                draw_text(_mx + _cell / 2, _my + _cell / 2, string(_mv));
                draw_set_valign(fa_top);
            }
        }
    }

    // --- Middle band: totals + whose turn + the live die ---
    draw_set_font(ui_font(fnt_ui));
    draw_set_halign(fa_left);
    draw_set_color(make_color_rgb(200, 170, 130));
    draw_text(_x0 + 45, 500, npc_display_name(_g.foe) + ":  " + string(kb_total(_g.foes)));
    draw_set_color(make_color_rgb(170, 220, 180));
    draw_text(_x0 + 45, 540, "You:  " + string(kb_total(_g.mine)));
    // Stake sits ABOVE the die box on the right so the two never collide.
    draw_set_halign(fa_right);
    draw_set_color(make_color_rgb(240, 205, 110));
    draw_text(_x1 - 45, 442, "Stake: " + string(_g.stake) + "g");
    draw_set_halign(fa_center);

    if (_g.phase == "play") {
        var _die_v = _g.my_turn ? _g.die : _g.foe_die;
        var _dx = GUI_CX + 300, _dy = 495;
        draw_set_color(_g.my_turn ? make_color_rgb(46, 58, 48) : make_color_rgb(64, 48, 40));
        draw_rectangle(_dx, _dy, _dx + 66, _dy + 66, false);
        draw_set_color(make_color_rgb(240, 230, 200));
        draw_rectangle(_dx, _dy, _dx + 66, _dy + 66, true);
        draw_set_font(fnt_ui_title);
        draw_set_valign(fa_middle);
        draw_text(_dx + 33, _dy + 33, string(_die_v));
        draw_set_valign(fa_top);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(160, 150, 130));
        draw_text(_dx + 33, _dy + 75, _g.my_turn ? "your die" : "their die");
        draw_set_color(make_color_rgb(150, 140, 120));
        if (_g.my_turn) {
            ui_draw_key_legend(GUI_CX, _y1 - 60, "A/D: Column     Enter: Place     H: Rules     Esc: Concede",
                make_color_rgb(150, 140, 120));
            draw_set_halign(fa_center);
        } else {
            draw_text(GUI_CX, _y1 - 60, npc_display_name(_g.foe) + " considers...");
        }
    } else {   // over
        draw_set_font(ui_font(fnt_ui));
        var _col_res = (_g.result == "win") ? make_color_rgb(150, 230, 150)
                     : ((_g.result == "tie") ? make_color_rgb(220, 210, 160) : make_color_rgb(225, 140, 120));
        draw_set_color(_col_res);
        draw_text(GUI_CX, _y1 - 96, _g.msg);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(150, 140, 120));
        draw_text(GUI_CX, _y1 - 54, "Enter: Leave the table");
    }

    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_alpha(1.0); draw_set_color(c_white); draw_set_font(-1);

    // Rules overlay last, over the whole table (auto-open on first sit-down; H toggles).
    ui_draw_knucklebones_rules(_g);
}

// ---------------------------------------------------------------------------
// ui_draw_knucklebones_rules(g) - the HOW TO PLAY panel. Drawn over the table
// when g.help is up (input frozen by the gc Step kb block meanwhile).
// ---------------------------------------------------------------------------
function ui_draw_knucklebones_rules(_g) {
    if (!_g.help) return;
    draw_set_alpha(0.82); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);
    var _hx0 = 480, _hy0 = 216, _hx1 = 1440, _hy1 = 864;
    draw_set_color(make_color_rgb(26, 20, 14));
    draw_rectangle(_hx0, _hy0, _hx1, _hy1, false);
    draw_set_color(make_color_rgb(230, 200, 130));
    draw_rectangle(_hx0, _hy0, _hx1, _hy1, true);

    draw_set_halign(fa_center); draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(235, 210, 150));
    draw_text(GUI_CX, _hy0 + 27, "HOW TO PLAY");

    draw_set_halign(fa_left);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(215, 205, 185));
    var _rl = [
        "1.  You and your opponent take turns. Each turn you roll a die and",
        "     place it in one of YOUR three columns (A/D to aim, Enter to place).",
        "2.  Matching dice in a column MULTIPLY each other: EACH matching die",
        "     counts its value x the number of matches. Two 4s count 8 apiece",
        "     (16 total); three 4s count 12 apiece (36 total).",
        "3.  Placing a die DESTROYS every opponent die of the same value in the",
        "     facing column - those points simply vanish (nothing moves to your",
        "     side). Wreck their stacks, but stacking your own invites reprisal.",
        "4.  When either board fills, the game ends - the higher total takes",
        "     the whole pot (double the stake).",
        "5.  A won game warms your opponent toward you. Esc concedes the stake."
    ];
    for (var _i = 0; _i < array_length(_rl); _i++) {
        draw_text(_hx0 + 60, _hy0 + 93 + _i * 45, _rl[_i]);
    }

    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 140, 120));
    ui_draw_key_legend(GUI_CX, _hy1 - 51, "H / Enter / Esc: Back to the table");
    draw_set_halign(fa_center);
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white); draw_set_font(-1);
}

// Archetype accent colour (Boon gold / Combatant red / Guardian green) for chips & bars.
function pet_arch_color(archetype) {
    switch (archetype) {
        case PET_ARCH_COMBATANT: return make_color_rgb(215, 110, 95);
        case PET_ARCH_GUARDIAN:  return make_color_rgb(110, 190, 160);
    }
    return make_color_rgb(228, 190, 90);   // Boon
}

// Small labelled pill chip (dark fill, accent border + text, fnt_ui_small). Returns the
// x just past its right edge so pills can be laid out in a row.
function ui_bairc_pill(_x, _y, _txt, _col) {
    draw_set_font(ui_font(fnt_ui_small));
    var _w = string_width(_txt) + 26;
    draw_set_color(make_color_rgb(16, 18, 26));
    draw_rectangle(_x, _y, _x + _w, _y + 36, false);
    draw_set_color(_col);
    draw_rectangle(_x, _y, _x + _w, _y + 36, true);
    draw_set_halign(fa_center);
    draw_text(_x + _w / 2, _y + 6, _txt);
    draw_set_halign(fa_left);
    return _x + _w;
}

// Floating equipment-style tooltip explaining a creature stat (PWR/SPR/LCK): what it
// governs, the live effect on THIS creature, and where each point comes from (mirrors
// pet_stat_breakdown, so it can never disagree with the real math). Follows the
// char-menu stat popup pattern; self-sizes and clamps to the screen.
function ui_draw_pet_stat_tooltip(mx, my, pet, which) {
    var _val = pet_stat(pet, which);
    var _gov = (which == pet_stat_primary(pet.archetype));
    var _lines = [];   // { txt, col }
    array_push(_lines, { txt: pet_stat_name(which) + "  " + string(_val), col: c_white });
    // Universal role: every stat does this on EVERY creature (mirrors the pet_active_*
    // helpers in scr_stats - keep the numbers in sync).
    switch (which) {
        case "pow":
            array_push(_lines, { txt: "Presence: you take " + string_format(min(8, _val * 0.5), 0, 1) + "% less damage.", col: make_color_rgb(180, 200, 235) });
            break;
        case "spr":
            array_push(_lines, { txt: "Hardy spirit: " + string(min(50, _val * 5)) + "% chance to shrug off an injury.", col: make_color_rgb(180, 200, 235) });
            break;
        default:
            array_push(_lines, { txt: "Fortune: +" + string_format(_val * 0.5, 0, 1) + "% gold, +" + string_format(_val * 0.3, 0, 1) + " loot find.", col: make_color_rgb(180, 200, 235) });
            break;
    }
    var _arch_of = (which == "pow") ? "Warrior" : ((which == "spr") ? "Guardian" : "Fortune");
    var _gift    = (which == "pow") ? "strike damage" : ((which == "spr") ? "heals & shields" : "boon (gold & loot)");
    // BAIRC'S LEDGER (Station rank 1, M 08-15): once his pens are upgraded,
    // every stat hover carries the FULL accounting - the source breakdown for
    // ALL stats (was governing-only) plus exact per-point impact lines.
    var _ledger = (npc_rank("bairc") >= 1);
    if (_gov) {
        array_push(_lines, { txt: "Governing stat: its " + _gift + " scales +" + string(round(_val * PET_STAT_COEFF * 100))
                                + "%  (each point = +" + string(round(PET_STAT_COEFF * 100)) + "%).", col: make_color_rgb(150, 210, 160) });
    }
    if (_gov || _ledger) {
        var _bd  = pet_stat_breakdown(pet, which);
        var _src = "Base " + string(_bd.base) + "   Stage +" + string(_bd.stage) + "   Talent +" + string(_bd.talent);
        if (_bd.bond > 0)      _src += "   Bond +" + string(_bd.bond);
        if (_bd.signature > 0) _src += "   Scion +" + string(_bd.signature);
        array_push(_lines, { txt: _src, col: make_color_rgb(170, 178, 198) });
    }
    if (_ledger) {
        switch (which) {
            case "pow":
                array_push(_lines, { txt: "Each point: -0.5% damage taken (caps at 8%).", col: make_color_rgb(150, 210, 160) });
                break;
            case "spr":
                array_push(_lines, { txt: "Each point: +5% injury shrug (caps at 50%).", col: make_color_rgb(150, 210, 160) });
                break;
            default:
                array_push(_lines, { txt: "Each point: +0.5% gold find, +0.3 loot find.", col: make_color_rgb(150, 210, 160) });
                // Loot-find points also feed the Banshee bottle chest roll
                // (+1% per 3 points, banshee_chest_try - keep in sync).
                array_push(_lines, { txt: "Songs: +" + string(floor(_val * 0.3 / 3))
                                        + "% Banshee bottle chance from chests (as companion, fed).", col: make_color_rgb(150, 210, 160) });
                break;
        }
        if (!_gov) {
            array_push(_lines, { txt: "If it governed: " + _gift + " would scale +" + string(round(_val * PET_STAT_COEFF * 100)) + "%.", col: make_color_rgb(140, 150, 175) });
        }
        array_push(_lines, { txt: "Bairc's ledger - full accounting (Station rank 1).", col: make_color_rgb(110, 160, 120) });
    }
    array_push(_lines, { txt: "Grows fastest on " + _arch_of + " creatures.", col: make_color_rgb(140, 145, 165) });
    draw_set_font(ui_font(fnt_ui_small));
    var _w = 0;
    for (var _i = 0; _i < array_length(_lines); _i++) _w = max(_w, string_width(_lines[_i].txt));
    _w += 36;
    var _h  = 24 + array_length(_lines) * 30;
    var _tx = mx + 24, _ty = my + 12;
    if (_tx + _w > GUI_W - 6) _tx = GUI_W - _w - 6;
    if (_ty + _h > GUI_H - 6) _ty = GUI_H - _h - 6;
    draw_set_alpha(0.95);
    draw_set_color(make_color_rgb(18, 20, 32));
    draw_rectangle(_tx, _ty, _tx + _w, _ty + _h, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(120, 140, 190));
    draw_rectangle(_tx, _ty, _tx + _w, _ty + _h, true);
    var _ly = _ty + 12;
    for (var _i = 0; _i < array_length(_lines); _i++) {
        draw_set_color(_lines[_i].col);
        draw_text(_tx + 18, _ly, _lines[_i].txt);
        _ly += 30;
    }
    draw_set_color(c_white);
}

// Three compact PWR/SPR/LCK chips (governing stat gold, hovered chip's border lightens).
// Returns the stat key under the mouse ("" if none) so the CALLER can pop
// ui_draw_pet_stat_tooltip after everything else - tooltips must draw last.
// ---------------------------------------------------------------------------
// ui_draw_pet_corruption_fx(pet, spr, frame, x, y, xs, ys, cx, cy)
// Code-drawn corruption dressing layered over a pet sprite (07-09 art track):
//   pushing   - an irregular violet flicker (the corruption gnawing at it)
//   fulfilled - a steady dark-violet aura + slow orbiting motes (grand power)
// x/y/xs/ys = the exact transform the base sprite was drawn with (the tint
// layers redraw the sprite, so they self-align at any site); cx/cy = the body
// CENTER the caller knows (feet-anchored sites pass feet_y - height/2), used
// only for the orbiting motes. No-op for uncorrupted pets. Caller's blendmode
// and alpha are restored.
// ---------------------------------------------------------------------------
function ui_draw_pet_corruption_fx(pet, spr, frame, x, y, xs, ys, cx, cy) {
    var _cst = pet_corr_state(pet);
    if (_cst != "pushing" && _cst != "fulfilled") return;
    var _t   = current_time / 1000;
    var _vio = make_color_rgb(165, 70, 210);
    gpu_set_blendmode(bm_add);
    if (_cst == "pushing") {
        // Mostly dormant, surging every couple of seconds - reads as "unstable".
        var _fk = sin(_t * 3.1) * sin(_t * 1.7 + 1.3);
        if (_fk > 0.55) {
            draw_sprite_ext(spr, frame, x, y, xs, ys, 0, _vio, 0.30 * ((_fk - 0.55) / 0.45));
        }
    } else {
        // Fulfilled: the corruption owns it. Steady aura...
        var _pl = 0.22 + 0.10 * sin(_t * 2.4);
        draw_sprite_ext(spr, frame, x, y + 2, xs * 1.10, ys * 1.10, 0, _vio, _pl);
        draw_sprite_ext(spr, frame, x, y, xs * 1.04, ys * 1.04, 0, _vio, _pl * 0.8);
        // ...plus slow orbiting motes around the body center.
        var _orb_r = max(24, sprite_get_height(spr) * ys * 0.42);
        for (var _mi = 0; _mi < 5; _mi++) {
            var _ma = _t * (0.8 + 0.15 * _mi) + _mi * 1.2566;   // 2pi/5 spacing
            var _mx = cx + cos(_ma) * _orb_r;
            var _my = cy + sin(_ma) * _orb_r * 0.45;            // flattened orbit
            draw_set_alpha(0.35 + 0.20 * sin(_t * 3 + _mi));
            draw_set_color(_vio);
            draw_circle(_mx, _my, 2.5, false);
        }
        draw_set_alpha(1.0);
    }
    gpu_set_blendmode(bm_normal);
}

// AWAKENED FX v2 (08-13, M direction: "holographic pulse / puff of smoke /
// aura glow" - the old 20%-alpha halo didn't read). One shared helper so the
// station, hub carousel and combat companion all sell Stage 4 identically.
// Draw AFTER the base sprite (all passes are additive). Layers:
//   1. breathing hologram echo (two ghost passes, offset pulse phases)
//   2. periodic PULSE RING - a holographic shockwave every ~2.2s
//   3. rising aura motes - ember/smoke specks drifting up around the body
// Color comes from pet_aura_color (archetype: gold/red/blue); callers skip
// entirely when it returns -1 (not Awakened).
function ui_draw_pet_awakened_fx(pet, spr, frame, x, y, xs, ys, cx, cy) {
    var _au = pet_aura_color(pet);
    if (_au < 0) return;
    var _t = current_time / 1000;
    gpu_set_blendmode(bm_add);
    // 1. Breathing hologram echo.
    draw_sprite_ext(spr, frame, x, y + 1, xs * 1.06, ys * 1.06, 0, _au, 0.30 + 0.14 * sin(_t * 3.3));
    draw_sprite_ext(spr, frame, x, y + 2, xs * (1.15 + 0.03 * sin(_t * 2.3)), ys * (1.15 + 0.03 * sin(_t * 2.3)), 0, _au, 0.13 + 0.07 * sin(_t * 2.3 + 1.3));
    // 2. Holographic pulse ring: expands and fades on a fixed cycle.
    var _pp = (current_time mod 2200) / 2200;
    if (_pp < 0.55) {
        var _pr = _pp / 0.55;
        draw_sprite_ext(spr, frame, x, y, xs * (1.0 + 0.34 * _pr), ys * (1.0 + 0.34 * _pr), 0, _au, 0.38 * (1 - _pr) * (1 - _pr));
    }
    // 3. Rising aura motes - smoke-puff specks born low, drifting up and out.
    var _mh = max(40, sprite_get_height(spr) * ys * 0.6);
    for (var _mi = 0; _mi < 6; _mi++) {
        var _ph = (_t * 0.45 + _mi * 0.37) mod 1;   // 0 birth (low) -> 1 gone (high)
        var _mx = cx + sin(_ph * 5 + _mi * 2.1) * (14 + _mi * 4);
        var _my = cy + _mh * 0.45 - _mh * 1.1 * _ph;
        draw_set_alpha((1 - _ph) * (0.55 + 0.25 * sin(_t * 4 + _mi)));
        draw_set_color(_au);
        draw_circle(_mx, _my, 1.5 + 1.2 * (1 - _ph), false);
    }
    draw_set_alpha(1.0);
    gpu_set_blendmode(bm_normal);
}

function ui_draw_pet_stat_chips(_x, _y, _pet, _chip_w, _chip_h) {
    var _pri  = pet_stat_primary(_pet.archetype);
    var _keys = ["pow", "spr", "lck"];
    var _mx = device_mouse_x_to_gui(0), _my = device_mouse_y_to_gui(0);
    var _hover = "";
    for (var _i = 0; _i < 3; _i++) {
        var _cx  = _x + _i * (_chip_w + 12);
        var _hot = (_keys[_i] == _pri);
        var _hov = (_mx >= _cx && _mx <= _cx + _chip_w && _my >= _y && _my <= _y + _chip_h);
        if (_hov) _hover = _keys[_i];
        draw_set_color(_hot ? make_color_rgb(32, 30, 20) : make_color_rgb(18, 20, 30));
        draw_rectangle(_cx, _y, _cx + _chip_w, _y + _chip_h, false);
        draw_set_color(_hov ? make_color_rgb(200, 215, 245) : (_hot ? make_color_rgb(210, 175, 90) : make_color_rgb(50, 56, 76)));
        draw_rectangle(_cx, _y, _cx + _chip_w, _y + _chip_h, true);
        draw_set_halign(fa_center); draw_set_valign(fa_middle);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(_hot ? make_color_rgb(232, 202, 122) : make_color_rgb(170, 178, 198));
        draw_text(_cx + _chip_w / 2, _y + _chip_h / 2, pet_stat_name(_keys[_i]) + "  " + string(pet_stat(_pet, _keys[_i])));
        draw_set_halign(fa_left); draw_set_valign(fa_top);
    }
    return _hover;
}

function ui_draw_bairc_screen() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "bairc_open") || !_gc.bairc_open) return;

    // Dim + panel + frame.
    draw_set_alpha(0.82); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);
    var _x1 = 120, _y1 = 66, _x2 = 1860, _y2 = 1020;   // vendor-width so Bairc gets the same 300px actor column
    draw_set_color(make_color_rgb(16, 18, 22));
    draw_rectangle(_x1, _y1, _x2, _y2, false);
    ui_draw_gothic_frame(_x1, _y1, _x2, _y2, 36);

    var _pad = 45;
    draw_set_halign(fa_left); draw_set_valign(fa_top);

    // Header: title only. Bairc himself stands full-size (300px) in the reserved
    // right-hand actor column below, matching every other vendor screen.
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(225, 215, 235));
    draw_text(_x1 + _pad, _y1 + _pad + 6, "Bairc's Garden");
    draw_set_font(ui_font_dense(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 160, 185));
    draw_text(_x1 + _pad, _y1 + _pad + 66, "He tends the creatures you carry up from below.");
    // VISIT THE GARDEN (M design-locked 08-15): walk the full grounds - the
    // chip taps through to the same [V] the keyboard uses.
    ui_garden_chip(1330, _y1 + _pad + 4, "VISIT THE GARDEN  [V]", "bairc:garden");

    var _roster = pet_roster();
    var _n      = array_length(_roster);
    var _cur    = clamp(_gc.bairc_cursor, 0, max(0, _n - 1));

    var _list_x = _x1 + _pad;
    var _list_y = _y1 + 236;
    var _row_h  = 78;   // taller: icon box + two text lines
    var _list_w = 560;

    // Left column sub-panel: the STABLE roster (and HIS GARDEN below it) sits in its own
    // inset panel now - the video-gamey pass frames every functional area like the
    // equipment screens do.
    var _lp_x0 = _list_x - 16, _lp_y0 = _list_y - 56, _lp_x1 = _list_x + _list_w + 16, _lp_y1 = _y2 - 120;
    draw_set_color(make_color_rgb(12, 14, 20));
    draw_rectangle(_lp_x0, _lp_y0, _lp_x1, _lp_y1, false);
    draw_set_color(make_color_rgb(52, 58, 80));
    draw_rectangle(_lp_x0, _lp_y0, _lp_x1, _lp_y1, true);

    // SCROLL WINDOW (08-01, M: an expanded roster spilled past the panel and over
    // the footer - "implicitly add scrolling to menus that extend like this").
    // Capacity is MEASURED from the panel; the window follows the cursor, so
    // W/S (keyboard, pad, and the touch on-screen d-pad) reach every row.
    // 08-11 (M): HIS GARDEN owns a permanently reserved band at the panel foot -
    // the roster scrolls in what's left instead of ever pushing the garden off.
    var _gard_h = 190;
    var _vis = max(3, ((_lp_y1 - _gard_h - 8) - _list_y) div _row_h);
    if (!variable_instance_exists(_gc, "bairc_scroll")) _gc.bairc_scroll = 0;
    _gc.bairc_scroll = clamp(_gc.bairc_scroll, 0, max(0, _n - _vis));
    if (_cur < _gc.bairc_scroll)            _gc.bairc_scroll = _cur;
    if (_cur > _gc.bairc_scroll + _vis - 1) _gc.bairc_scroll = _cur - _vis + 1;
    var _scr = _gc.bairc_scroll;
    // Mouse wheel over the panel steps the cursor (desktop nicety; same modal
    // guards as the tap handler below).
    if (_n > 0 && !_gc.bairc_pad_menu_open && !_gc.bairc_detail_open && !_gc.bairc_naming
        && !_gc.bairc_release_confirm && !_gc.bairc_capstone_open && !_gc.hatch_active) {
        var _whx = device_mouse_x_to_gui(0);
        var _why = device_mouse_y_to_gui(0);
        if (_whx >= _lp_x0 && _whx <= _lp_x1 && _why >= _lp_y0 && _why <= _lp_y1) {
            if (mouse_wheel_down()) _gc.bairc_cursor = min(_n - 1, _cur + 1);
            if (mouse_wheel_up())   _gc.bairc_cursor = max(0, _cur - 1);
        }
    }

    // Column headers. The counter reads stabled/capacity (active companion exempt) and
    // turns amber when Bairc's stable is crowded - feed potency drops (soft cap, §6).
    draw_set_font(ui_font_dense(fnt_ui_small));
    var _stb_ovf = pet_stable_overflow();
    draw_set_color(_stb_ovf > 0 ? make_color_rgb(230, 160, 90) : make_color_rgb(150, 200, 140));
    draw_text(_list_x, _list_y - 36, "STABLE  (" + string(pet_stabled_count()) + "/" + string(pet_stable_capacity()) + ")"
        + (_stb_ovf > 0 ? "  -  crowded" : ""));

    if (_n == 0) {
        draw_set_font(ui_font_dense(fnt_ui));
        draw_set_color(make_color_rgb(150, 156, 175));
        draw_text(_list_x, _list_y + 12, "No creatures yet.");
    }

    // Roster rows: icon box (the creature's own sprite, shrunk to fit) + two text
    // lines. Only the scroll window's rows draw - nothing can spill the panel.
    for (var _i = _scr; _i < min(_n, _scr + _vis); _i++) {
        var _ry  = _list_y + (_i - _scr) * _row_h;
        var _sel = (_i == _cur);
        var _pet = _roster[_i];
        var _is_active = (variable_global_exists("active_pet") && global.active_pet == _i && !_pet.is_egg);

        draw_set_color(_sel ? make_color_rgb(34, 42, 56) : make_color_rgb(20, 23, 32));
        draw_rectangle(_list_x, _ry, _list_x + _list_w, _ry + _row_h - 10, false);
        draw_set_color(_sel ? make_color_rgb(90, 150, 210) : make_color_rgb(44, 52, 70));
        draw_rectangle(_list_x, _ry, _list_x + _list_w, _ry + _row_h - 10, true);

        // Active companion: the same steady bright-green double frame the loadout
        // COMPANION tab uses, so "equipped" reads identically on both screens.
        if (_is_active) {
            draw_set_color(make_color_rgb(120, 230, 150));
            draw_rectangle(_list_x, _ry, _list_x + _list_w, _ry + _row_h - 10, true);
            draw_rectangle(_list_x + 1, _ry + 1, _list_x + _list_w - 1, _ry + _row_h - 11, true);
        }

        // Archetype accent bar down the row's left edge (gold Boon / red Combatant /
        // green Guardian) so the roster reads at a glance.
        draw_set_color(pet_arch_color(_pet.archetype));
        draw_rectangle(_list_x + 2, _ry + 3, _list_x + 7, _ry + _row_h - 13, false);

        // Icon box: the actual creature sprite, fit by width+height (bottom-centre origin).
        var _ibs  = _row_h - 22;
        var _ibx0 = _list_x + 12, _iby0 = _ry + 6, _ibx1 = _ibx0 + _ibs, _iby1 = _iby0 + _ibs;
        draw_set_color(make_color_rgb(14, 16, 24));
        draw_rectangle(_ibx0, _iby0, _ibx1, _iby1, false);
        draw_set_color(_sel ? make_color_rgb(80, 130, 190) : make_color_rgb(40, 48, 64));
        draw_rectangle(_ibx0, _iby0, _ibx1, _iby1, true);
        var _isp = pet_sprite(_pet, "s");
        if (_isp >= 0) {
            // #16: fit the VISIBLE creature (bbox) into the icon box, not the canvas.
            var _ifit = pet_sprite_fit(_isp, (_ibx0 + _ibx1) / 2, _iby1 - 5, _ibs - 8, _ibs - 10);
            draw_sprite_ext(_isp, pet_anim_frame(_isp), _ifit.x, _ifit.y, _ifit.scale, _ifit.scale, 0, c_white, 1);
        } else {
            // Species art not imported yet (expansion species via the F12 dev
            // lever) - a "?" placeholder so the row reads intentional, not broken.
            draw_set_halign(fa_center); draw_set_valign(fa_middle);
            draw_set_font(ui_font_dense(fnt_ui));
            draw_set_color(make_color_rgb(110, 120, 145));
            draw_text((_ibx0 + _ibx1) / 2, (_iby0 + _iby1) / 2, "?");
            draw_set_halign(fa_left); draw_set_valign(fa_top);
            draw_set_font(ui_font_dense(fnt_ui_small));
        }

        var _tx = _ibx1 + 14;

        // Line 1: name (left, green when it is the active companion - matches the
        // loadout screen; the old "* " asterisk marker is gone) + stage/egg-type PILL (right).
        draw_set_font(ui_font_dense(fnt_ui));
        draw_set_color(_is_active ? make_color_rgb(150, 235, 170)
                     : (_pet.is_egg ? make_color_rgb(210, 195, 130) : make_color_rgb(220, 226, 238)));
        draw_text(_tx, _ry + 10, (_pet.is_egg ? (_pet.name + " Egg") : _pet.name));
        var _pill_txt = _pet.is_egg ? pet_egg_label(_pet) : pet_stage_name(_pet.stage);
        draw_set_font(ui_font_dense(fnt_ui_small));
        var _pl_w  = string_width(_pill_txt) + 22;
        var _pl_x1 = _list_x + _list_w - 12, _pl_x0 = _pl_x1 - _pl_w;
        var _pl_y0 = _ry + 9, _pl_y1 = _pl_y0 + 33;
        draw_set_color(_pet.is_egg ? make_color_rgb(44, 38, 20) : make_color_rgb(20, 32, 24));
        draw_rectangle(_pl_x0, _pl_y0, _pl_x1, _pl_y1, false);
        draw_set_color(_pet.is_egg ? make_color_rgb(160, 140, 70) : make_color_rgb(90, 150, 100));
        draw_rectangle(_pl_x0, _pl_y0, _pl_x1, _pl_y1, true);
        draw_set_halign(fa_center);
        draw_set_color(_pet.is_egg ? make_color_rgb(225, 205, 140) : make_color_rgb(170, 220, 170));
        draw_text((_pl_x0 + _pl_x1) / 2, _pl_y0 + 5, _pill_txt);
        draw_set_halign(fa_left);

        // Line 2 right edge: ACTIVE tag for the equipped companion (loadout parity).
        if (_is_active) {
            draw_set_halign(fa_right);
            draw_set_font(ui_font_dense(fnt_ui_small));
            draw_set_color(make_color_rgb(120, 230, 150));
            draw_text(_list_x + _list_w - 12, _ry + 40, "ACTIVE");   // same baseline as line 2 (was +46: read as a third, staggered line)
            draw_set_halign(fa_left);
        }

        // Line 2: type (+ species only when a custom name hides it, + egg gift + any
        // injury/corruption tags), muted. Raised so it can't kiss the row border.
        draw_set_font(ui_font_dense(fnt_ui_small));
        draw_set_color(make_color_rgb(150, 160, 185));
        var _l2 = pet_archetype_name(_pet.archetype);
        if (pet_named(_pet)) _l2 = pet_species_get(_pet.species).name + "  -  " + _l2;
        if (_pet.is_egg) _l2 = "unhatched  -  " + _l2;
        else {
            var _regl = pet_egg_label(_pet);
            if (_regl != "") _l2 += "  -  " + _regl;
            // FOUND creatures (08-16 M: "a pet hatched itself... a Stormkirin was
            // just there") arrive alive from ~15% of egg sources - say so here,
            // where the player actually looks, not only in the hub notice.
            if (variable_struct_exists(_pet, "raised") && !_pet.raised) _l2 += "  -  found alive, never an egg";
            _l2 += pet_injury_tag(_pet) + pet_corruption_tag(_pet);
        }
        // Truncate against the stage pill / ACTIVE tag on the right - a long tag
        // cluster ("Fortune - Vital Egg [CORRUPTING 1/3]") ran into both (M 07-09).
        var _l2_lim = min(_pl_x0, _list_x + _list_w - 12 - (_is_active ? string_width("ACTIVE") + 18 : 0));
        draw_text(_tx, _ry + 40, ui_truncate(_l2, _l2_lim - _tx));
    }

    // Visible scrollbar (HARD rule: scrollable lists show their bar) - right
    // gutter between the rows and the panel edge, only when the roster overflows.
    if (_n > _vis) {
        var _sb_x0 = _list_x + _list_w + 5, _sb_x1 = _sb_x0 + 6;
        var _sb_y0 = _list_y, _sb_y1 = _list_y + _vis * _row_h - 10;
        draw_set_color(make_color_rgb(30, 34, 48));
        draw_rectangle(_sb_x0, _sb_y0, _sb_x1, _sb_y1, false);
        var _sb_h  = max(36, (_sb_y1 - _sb_y0) * (_vis / _n));
        var _sb_ty = _sb_y0 + ((_sb_y1 - _sb_y0) - _sb_h) * (_scr / max(1, _n - _vis));
        draw_set_color(make_color_rgb(90, 150, 210));
        draw_rectangle(_sb_x0, _sb_ty, _sb_x1, _sb_ty + _sb_h, false);
    }

    // Touch (8d, M 07-08 "cant hatch egg"): tap a roster row to select it; tap
    // the SELECTED row again to open the action menu (Hatch / Set Active /
    // Feed... - same submenu the gamepad uses). Gated off while any Bairc
    // modal owns input so a behind-the-modal tap can't double-act.
    if (input_device() == 2 && _n > 0 && mouse_check_button_pressed(mb_left)
        && !_gc.bairc_pad_menu_open && !_gc.bairc_detail_open && !_gc.bairc_naming
        && !_gc.bairc_release_confirm && !_gc.bairc_capstone_open && !_gc.hatch_active) {
        var _brx = device_mouse_x_to_gui(0);
        var _bry = device_mouse_y_to_gui(0);
        if (_brx >= _list_x && _brx <= _list_x + _list_w) {
            // Visible window only - taps map through the scroll offset.
            for (var _bri = _scr; _bri < min(_n, _scr + _vis); _bri++) {
                var _bry0 = _list_y + (_bri - _scr) * _row_h;
                if (_bry >= _bry0 && _bry <= _bry0 + _row_h - 10) {
                    if (_bri == _cur) touch_press(vk_enter);
                    else _gc.bairc_cursor = _bri;
                    break;
                }
            }
        }
    }

    // HIS GARDEN - every creature ever entrusted to Bairc lives in the plot at
    // the panel's foot (design §6/§11: donation is visible care, not deletion).
    // 08-11 rework (M: "it should always show"): the band is ALWAYS drawn - the
    // roster window above was shrunk to guarantee it the room - and the plot is
    // a small SCENE now: a pond, a back and a front lane for depth, and
    // creatures that idle in place then now and then stroll to a new spot.
    // Everything is a deterministic function of index + clock - the garden
    // lives without any stored state.
    var _dn  = array_length(bairc_donated());
    var _mn  = array_length(bairc_memorials());   // memorial stones (08-01, pillar A)
    var _gy0 = _lp_y1 - _gard_h + 6;
    draw_set_font(ui_font_dense(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 185, 120));
    // The garden BLESSING reads in the header - donation visibly tends back.
    var _gbl = bairc_garden_blessing_pct();
    draw_text(_list_x, _gy0, "HIS GARDEN  (" + string(_dn) + ")"
        + ((_gbl > 0) ? "  +" + string(_gbl) + "% growth" : ""));
    if (_dn > 10) {
        draw_set_halign(fa_right);
        draw_set_color(make_color_rgb(110, 130, 100));
        draw_text(_list_x + _list_w, _gy0, "+" + string(_dn - 10) + " more");
        draw_set_halign(fa_left);
    }
    draw_set_color(make_color_rgb(40, 48, 38));
    draw_line(_list_x, _gy0 + 30, _list_x + _list_w, _gy0 + 30);

    // Grounds: a faint earth band, and the pond off-centre left (the memorial
    // stones own the right edge).
    var _gfl_b = _lp_y1 - 60;   // back-lane floor (drawn first = behind)
    var _gfl_f = _lp_y1 - 14;   // front-lane floor
    draw_set_alpha(0.35);
    draw_set_color(make_color_rgb(26, 34, 24));
    draw_rectangle(_list_x, _gy0 + 31, _list_x + _list_w, _lp_y1 - 2, false);
    draw_set_alpha(1.0);
    // The pond only exists once the garden is LIVED IN (M 08-11 screenshot: a
    // bare ellipse in an empty plot read as "a random oval"), and it reads as
    // water now - layered tones + a moving glint - not a flat shape.
    if (_dn > 0) {
        var _pd_cx = _list_x + 140, _pd_cy = _lp_y1 - 32;
        draw_set_color(make_color_rgb(22, 36, 48));
        draw_ellipse(_pd_cx - 66, _pd_cy - 14, _pd_cx + 66, _pd_cy + 12, false);
        draw_set_color(make_color_rgb(38, 62, 80));
        draw_ellipse(_pd_cx - 54, _pd_cy - 10, _pd_cx + 54, _pd_cy + 9, false);
        draw_set_color(make_color_rgb(80, 120, 146));
        draw_ellipse(_pd_cx - 66, _pd_cy - 14, _pd_cx + 66, _pd_cy + 12, true);
        var _pd_gx = _pd_cx - 20 + 14 * sin(current_time / 900);
        draw_set_alpha(0.5);
        draw_set_color(make_color_rgb(140, 180, 205));
        draw_line(_pd_gx - 10, _pd_cy - 3, _pd_gx + 10, _pd_cy - 3);
        draw_line(_pd_gx - 4, _pd_cy + 3, _pd_gx + 14, _pd_cy + 3);
        draw_set_alpha(1.0);
    }

    if (_dn == 0 && _mn == 0) {
        draw_set_color(make_color_rgb(110, 125, 105));
        draw_text(_list_x, _gy0 + 44, "The plot lies quiet. Creatures you entrust to Bairc live on here.");
    }

    // MEMORIAL STONES (08-01): pets lost to the injury ladder rest at the
    // garden's right edge - small headstones, newest nearest the creatures.
    // Drawn before the front lane so the living walk in front of the stones.
    if (_mn > 0) {
        var _mshow = min(_mn, 4);
        for (var _mi = 0; _mi < _mshow; _mi++) {
            var _mx = _list_x + _list_w - 16 - _mi * 26;
            draw_set_color(make_color_rgb(66, 70, 82));
            draw_roundrect_ext(_mx - 8, _gfl_b + 20, _mx + 8, _gfl_b + 40, 7, 7, false);
            draw_set_color(make_color_rgb(30, 33, 42));
            draw_roundrect_ext(_mx - 8, _gfl_b + 20, _mx + 8, _gfl_b + 40, 7, 7, true);
            draw_line(_mx - 4, _gfl_b + 28, _mx + 4, _gfl_b + 28);   // the engraving
        }
        if (_mn > _mshow) {
            draw_set_color(make_color_rgb(110, 115, 130));
            draw_text(_list_x + _list_w - 16 - _mshow * 26 - 6, _gfl_b + 18, "+" + string(_mn - _mshow));
        }
    }

    // Creatures: alternate indices split back/front lane - the split reads as
    // depth, not rows. Even per-lane spacing plus a stroll amplitude under half
    // a lane keeps neighbours from ever overlapping (the collision M asked to
    // keep). The stroll itself: a slow sine pushed through smoothstep, clamped
    // so it DWELLS at each end - creatures stand idling most of the time and
    // occasionally walk to their other spot.
    var _gshow  = min(_dn, 10);
    var _lane_bn = _gshow div 2;         // back-lane population (odd indices)
    var _lane_fn = _gshow - _lane_bn;    // front-lane population (even indices)
    for (var _pass = 0; _pass < 2; _pass++) {
        var _ln = (_pass == 0) ? _lane_bn : _lane_fn;
        if (_ln <= 0) continue;
        var _li = 0;
        for (var _gi = 0; _gi < _gshow; _gi++) {
            if ((_gi mod 2 == 0) == (_pass == 0)) continue;   // pass 0 = odd = back
            var _d  = bairc_donated()[_gi];
            // A stand-in struct is all pet_sprite needs (species + stage + not-an-egg).
            var _gp  = { is_egg:false, species:_d.species, stage:_d.stage };
            var _gsp = pet_sprite(_gp, "s");
            var _slot_w = (_list_w - 80) / _ln;
            var _lane   = _list_x + 40 + (_li + 0.5) * _slot_w;
            _li++;
            if (_gsp < 0) continue;
            var _t   = current_time / 1000;
            var _sw  = sin(_t * (0.16 + 0.05 * (_gi mod 3)) + _gi * 2.13);
            var _mv  = clamp((_sw + 0.55) / 1.10, 0, 1);
            _mv = _mv * _mv * (3 - 2 * _mv);   // ease in/out of the stroll
            var _amp = min(30, _slot_w * 0.35);
            var _wx  = lerp(-_amp, _amp, _mv);
            // #16: fit the VISIBLE creature; back lane smaller for depth.
            var _gfit = pet_sprite_fit(_gsp, _lane + _wx,
                (_pass == 0) ? _gfl_b : _gfl_f, ((_pass == 0) ? 40 : 54) * pet_stage_size_mult(_d.stage));
            draw_sprite_ext(_gsp, pet_anim_frame(_gsp), _gfit.x, _gfit.y,
                _gfit.scale, _gfit.scale, 0, c_white, (_pass == 0) ? 0.92 : 1);
        }
    }

    // Detail panel (right) for the highlighted creature - its own inset profile panel.
    var _dx = _x1 + 660;
    var _dy = _y1 + 174;
    var _dw = 1500 - _dx;   // stop short of the reserved Bairc actor column (x1530+)
    draw_set_color(make_color_rgb(12, 14, 20));
    draw_rectangle(_dx - 24, _dy - 20, _x1 + 1516, _y2 - 60, false);
    draw_set_color(make_color_rgb(52, 58, 80));
    draw_rectangle(_dx - 24, _dy - 20, _x1 + 1516, _y2 - 60, true);

    var _stat_hover = "";   // stat chip under the mouse; its tooltip draws LAST (on top)
    var _growth_hover_txt = "";   // READY-bar hover: full evolve requirement, tooltip drawn last
    if (_n > 0) {
        var _p = _roster[_cur];
        var _p_known = pet_egg_identified(_p);   // false only for unidentified eggs
        var _p_title = !_p_known
            ? ("Mysterious " + (pet_egg_label(_p) != "" ? pet_egg_label(_p) : "Egg"))
            : (_p.is_egg ? (_p.name + " Egg") : _p.name);
        draw_set_font(fnt_ui_title);
        draw_set_color(c_white);
        draw_text(_dx, _dy, _p_title);

        // STATUS pills only (ACTIVE / INJURED / SCION), spaced to the right of the
        // name - identity facts (species/type/stage) are labelled rows in the PROFILE
        // box below, Pokemon-style, so nothing reads as a floating unexplained label.
        var _plx = _dx + string_width(_p_title) + 34;
        if (global.active_pet == _cur)    _plx = ui_bairc_pill(_plx, _dy + 6, "ACTIVE", make_color_rgb(120, 210, 150)) + 12;
        if (!_p.is_egg && _p.injured > 0) _plx = ui_bairc_pill(_plx, _dy + 6, "INJURED", make_color_rgb(220, 120, 90)) + 12;
        if (_p.signature)                 _plx = ui_bairc_pill(_plx, _dy + 6, "SCION", make_color_rgb(200, 160, 235)) + 12;

        // ---- PROFILE box: Species / Type / Life Stage / Growth / Bond rows ----
        var _pa_y0 = _dy + 64, _pa_y1 = _dy + 254;
        draw_set_color(make_color_rgb(15, 17, 24));
        draw_rectangle(_dx - 14, _pa_y0, _dx + _dw, _pa_y1, false);
        draw_set_color(make_color_rgb(52, 58, 80));
        draw_rectangle(_dx - 14, _pa_y0, _dx + _dw, _pa_y1, true);

        // Pet sprite (or a framed placeholder until per-stage pet art is generated),
        // bottom-anchored inside the profile box's right side.
        var _psp = pet_sprite(_p, "s");
        var _spx = _dx + _dw - 90, _spb = _pa_y1 - 12;
        if (_psp >= 0) {
            // M 08-13 ("each model should fit the window... nightowl is perfect
            // filling its frame but crypt bat looks tiny"): the profile portrait
            // fills its frame at EVERY life stage - the stage-scaled height
            // ([78..116] x stage scale) made babies rattle around in the box,
            // and the Life Stage text row already tells the stage story.
            // 08-19 (M): stage-scaled again - babies at ~62% so they read as babies
            // (pet_stage_size_mult); adults still fill the frame.
            var _th    = 150 * pet_stage_size_mult(_p.stage);
            // #16: fit by VISIBLE content, not the padded canvas (bonehound/hollow
            // pup drew tiny). Width-capped so wide species stay inside the panel.
            var _pfit  = pet_sprite_fit(_psp, _spx, _spb, _th, 170 * pet_stage_size_mult(_p.stage));
            var _psc   = _pfit.scale;
            draw_sprite_ext(_psp, pet_anim_frame(_psp), _pfit.x, _pfit.y, _psc, _psc, 0, c_white, 1);
            // Awakened FX v2 (08-13): hologram echo + pulse ring + rising motes.
            ui_draw_pet_awakened_fx(_p, _psp, pet_anim_frame(_psp),
                _pfit.x, _pfit.y, _psc, _psc, _spx, _spb - _th * 0.5);
            // Corruption dressing (07-09 art track): flicker / dark aura + motes.
            ui_draw_pet_corruption_fx(_p, _psp, pet_anim_frame(_psp),
                _pfit.x, _pfit.y, _psc, _psc, _spx, _spb - _th * 0.5);
        } else {
            // Species art not imported yet (08-01: the raw "Stage 0" label here
            // read as a bug when the F12 dev eggs hatched) - proper stage name
            // plus an honest "art soon" note.
            draw_set_color(make_color_rgb(28, 32, 44));
            draw_rectangle(_spx - 48, _spb - 92, _spx + 48, _spb, false);
            draw_set_color(make_color_rgb(70, 80, 105));
            draw_rectangle(_spx - 48, _spb - 92, _spx + 48, _spb, true);
            draw_set_halign(fa_center);
            draw_set_font(ui_font_dense(fnt_ui));
            draw_set_color(make_color_rgb(120, 130, 160));
            draw_text(_spx, _spb - 84, "?");
            draw_set_font(ui_font_dense(fnt_ui_small));
            draw_text(_spx, _spb - 52, _p.is_egg ? "egg" : pet_stage_name(_p.stage));
            draw_set_color(make_color_rgb(90, 98, 122));
            draw_text(_spx, _spb - 27, "art soon");
            draw_set_halign(fa_left);
        }

        // Identity rows: grey label column, coloured value column.
        draw_set_font(ui_font_dense(fnt_ui_small));
        var _idr = !_p_known
            ? [ // Unidentified egg: only the shell is visible. Pay Bairc to learn the rest.
                ["Species:",    "??",                                                     make_color_rgb(140, 146, 165)],
                ["Type:",       "??",                                                     make_color_rgb(140, 146, 165)],
                ["Life Stage:", "Egg (unidentified - [I] Identify, " + string(pet_egg_identify_cost()) + "g)", make_color_rgb(200, 170, 240)],
              ]
            : [
                ["Species:",    pet_species_get(_p.species).name,                         make_color_rgb(220, 226, 238)],
                ["Type:",       pet_archetype_name(_p.archetype),                         pet_arch_color(_p.archetype)],
                // Hunger rides the Life Stage row (07-08): state + meter, colored by
                // severity so a starving pet reads at a glance. HP moved OFF this row
                // to the stat-chip band (M 07-09: the row ran under the sprite).
                ["Life Stage:", _p.is_egg ? "Egg (unhatched)"
                    : (pet_stage_name(_p.stage) + "   -   " + pet_hunger_state_label(_p)
                       + " (" + string(pet_hunger(_p)) + "/100)"),
                 // GML requires nested ternaries fully parenthesized - a bare
                 // a ? b : c ? d : e chain is a compile error.
                 (_p.is_egg ? make_color_rgb(228, 190, 90)
                    : ((pet_hunger_state(_p) == "starving") ? make_color_rgb(235, 90, 80)
                     : ((pet_hunger_state(_p) == "hungry")  ? make_color_rgb(230, 150, 90)
                      : ((pet_hunger_state(_p) == "peckish") ? make_color_rgb(215, 200, 120)
                       : make_color_rgb(228, 190, 90)))))],
              ];
        for (var _ri = 0; _ri < 3; _ri++) {
            draw_set_color(make_color_rgb(150, 160, 185));
            draw_text(_dx, _dy + 76 + _ri * 30, _idr[_ri][0]);
            draw_set_color(_idr[_ri][2]);
            // Truncated against the sprite's corner of the profile box (M 07-09:
            // long stage+hunger values ran under the creature art).
            draw_text(_dx + 158, _dy + 76 + _ri * 30, ui_truncate(_idr[_ri][1], (_dw - 190) - 158));
        }
        // Hovering the Type value explains what that type DOES (M 07-08: e.g.
        // Boon - "grants passive boons, never fights"). Reuses the growth-bar
        // hover channel so the tooltip draws above everything at the end.
        if (_p_known) {
            var _tyhx = device_mouse_x_to_gui(0), _tyhy = device_mouse_y_to_gui(0);
            var _ty_ry = _dy + 76 + 30;   // row 1 = Type
            if (_tyhx >= _dx + 158 && _tyhx <= _dx + 158 + string_width(pet_archetype_name(_p.archetype)) + 12
                && _tyhy >= _ty_ry - 4 && _tyhy <= _ty_ry + 28) {
                _growth_hover_txt = pet_archetype_name(_p.archetype) + " type: " + pet_archetype_blurb(_p.archetype);
            }
        }

        if (!_p.is_egg) {
            // Growth row + ticked bar (or the grown/READY status).
            if (_p.stage >= pet_max_stage()) {
                draw_set_color(make_color_rgb(210, 180, 120));
                draw_text(_dx, _dy + 168, "Fully grown (Awakened).");
            } else {
                var _need  = pet_growth_needed(_p.stage);
                var _ready = pet_growth_ready(_p);
                // The Adult -> Awakened crossing has its own gate (full clear @ A5, Soul-bound).
                // The full ACTIVE-companion requirement is a HOVER TOOLTIP on the bar now
                // (M 2026-07-04: the spelled-out line ran across the pet sprite). The line
                // itself stays short; the (hover) nudge marks that there's more to read.
                var _p_is_active = (variable_global_exists("active_pet") && pet_active() == _p);
                draw_set_color(_ready ? make_color_rgb(120, 210, 150) : make_color_rgb(170, 180, 200));
                draw_text(_dx, _dy + 168, _ready
                    ? ("READY " + ((_p.stage == PET_STAGE_ADULT) ? "to Awaken" : ("to become " + pet_stage_name(_p.stage + 1))) + "  (hover)")
                    : ("Growth to " + pet_stage_name(_p.stage + 1) + ":  " + string(_p.growth) + " / " + string(_need)));
                var _bx = _dx, _gby = _dy + 196, _bw = 420, _bh = 16;
                // Hover capture: the READY line + the bar row both count as the hot zone.
                if (_ready) {
                    var _ghmx = device_mouse_x_to_gui(0), _ghmy = device_mouse_y_to_gui(0);
                    if (_ghmx >= _dx && _ghmx <= _dx + _bw && _ghmy >= _dy + 162 && _ghmy <= _gby + _bh + 4) {
                        _growth_hover_txt = (_p.stage == PET_STAGE_ADULT)
                            ? "To Awaken: full-clear a dungeon at Awakening A5 with this creature as your ACTIVE companion while it is Soul-bound (Bond 18)."
                            : ("To reach " + pet_stage_name(_p.stage + 1) + ": complete a run with it as your ACTIVE companion."
                               + (_p_is_active ? "" : "  It is NOT active now - equip it at the dungeon gate > Companion."));
                    }
                }
                draw_set_color(make_color_rgb(40, 44, 56));
                draw_rectangle(_bx, _gby, _bx + _bw, _gby + _bh, false);
                draw_set_color(_ready ? make_color_rgb(120, 210, 150) : make_color_rgb(210, 190, 130));
                draw_rectangle(_bx, _gby, _bx + _bw * (_p.growth / max(1, _need)), _gby + _bh, false);
                draw_set_color(make_color_rgb(24, 27, 36));
                for (var _tk = 1; _tk < _need; _tk++) {
                    var _tkx = _bx + _bw * (_tk / _need);
                    draw_line(_tkx, _gby + 2, _tkx, _gby + _bh - 2);
                }
                draw_set_color(make_color_rgb(90, 96, 110));
                draw_rectangle(_bx, _gby, _bx + _bw, _gby + _bh, true);
            }
            // Bond row (07-29 rework - M: the tier circles + "total 8" readout was
            // incoherent). Tier name + an explicit "8 / 10 to Devoted" fraction,
            // then a FULL-SCALE progress bar (0 -> Soul-bound 18) with gold tick
            // marks at each milestone - the growth bar's idiom, one row down.
            var _bt    = pet_bond_tier(_p);
            var _bval  = pet_bond(_p);
            var _bnext = pet_bond_next_at(_p);
            draw_set_color(make_color_rgb(226, 150, 150));
            var _btxt = "Bond:  " + pet_bond_tier_name(_bt);
            draw_text(_dx, _dy + 224, _btxt);
            draw_set_color(make_color_rgb(140, 132, 150));
            draw_text(_dx + string_width(_btxt) + 20, _dy + 224, (_bnext > 0)
                ? (string(_bval) + " / " + string(_bnext) + " to " + pet_bond_tier_name(_bt + 1) + "   (hover)")
                : "its heart is yours entirely");
            var _bms  = pet_bond_milestones();
            var _bcap = _bms[array_length(_bms) - 1].at;      // Soul-bound = the full scale
            var _bby  = _dy + 248, _bbw = 420, _bbh = 10;
            draw_set_color(make_color_rgb(40, 44, 56));
            draw_rectangle(_dx, _bby, _dx + _bbw, _bby + _bbh, false);
            draw_set_color(make_color_rgb(214, 120, 120));
            draw_rectangle(_dx, _bby, _dx + _bbw * clamp(_bval / _bcap, 0, 1), _bby + _bbh, false);
            for (var _bmi = 0; _bmi < array_length(_bms); _bmi++) {
                var _bmx = _dx + _bbw * (_bms[_bmi].at / _bcap);
                draw_set_color((_bval >= _bms[_bmi].at) ? make_color_rgb(255, 205, 120) : make_color_rgb(24, 27, 36));
                draw_line_width(_bmx, _bby - 2, _bmx, _bby + _bbh + 2, 2);
            }
            draw_set_color(make_color_rgb(90, 96, 110));
            draw_rectangle(_dx, _bby, _dx + _bbw, _bby + _bbh, true);
            var _bhx = device_mouse_x_to_gui(0), _bhy = device_mouse_y_to_gui(0);
            if (_bhx >= _dx && _bhx <= _dx + _dw - 190 && _bhy >= _dy + 220 && _bhy <= _bby + _bbh + 4) {
                _growth_hover_txt = "Bond deepens by +1 on each survived run with it as your ACTIVE companion (ending the run with a full belly adds another +1). Treats add bond directly, up to 2 per run. Milestones: Trusting at 4, Devoted at 10, Soul-bound at 18.";
            }
        } else {
            // Eggs: what the creature inside will do, in the growth/bond rows' place.
            draw_set_color(make_color_rgb(180, 188, 206));
            draw_text_ext(_dx, _dy + 172, pet_archetype_blurb(_p.archetype), 28, _dw - 200);
        }

        // Stat chips (PWR/SPR/LCK, governing gold). Hovering one explains it - the
        // tooltip itself draws at the end of the screen so nothing overdraws it.
        if (!_p.is_egg) {
            _stat_hover = ui_draw_pet_stat_chips(_dx, _dy + 266, _p, 128, 42);
            // HP lives with the stats now (M 07-09: it rode the Life Stage row and
            // ran under the sprite). Red when knocked out.
            draw_set_font(ui_font_dense(fnt_ui));
            if (pet_hp(_p) <= 0) {
                draw_set_color(make_color_rgb(230, 95, 85));
                draw_text(_dx + 3 * 140 + 24, _dy + 274, "HP 0/" + string(pet_max_hp(_p)) + "  KO");
            } else {
                draw_set_color(make_color_rgb(140, 205, 150));
                draw_text(_dx + 3 * 140 + 24, _dy + 274, "HP " + string(pet_hp(_p)) + "/" + string(pet_max_hp(_p)));
            }
        }

        // ---- GRANTS box: what this creature does for you - type gift, egg gift,
        // corruption state, origin, signature kinship. A plain labelled header (no
        // floating type tags - the Type: row above explains the type); the outline is
        // drawn after the content, once its height is known.
        draw_set_font(ui_font_dense(fnt_ui_small));
        var _gb_y0 = _p.is_egg ? (_dy + 266) : (_dy + 322);
        draw_set_color(make_color_rgb(120, 200, 140));
        draw_text(_dx, _gb_y0 + 12, "GRANTS");
        var _by  = _gb_y0 + 46;
        var _eff = pet_effect_body(_p);
        if (!_p.is_egg && _eff != "") {
            draw_set_color(make_color_rgb(150, 210, 150));
            draw_text_ext(_dx, _by, _eff, 26, _dw - 14);
            _by += string_height_ext(_eff, 26, _dw - 14) + 10;
        }
        var _cst  = pet_corr_state(_p);
        var _elab = pet_egg_label(_p);
        if (_cst == "pushing") {
            draw_set_color(make_color_rgb(205, 120, 225));
            draw_text(_dx, _by + 2, "Corrupting " + string(pet_corr_runs(_p)) + "/3  -  costs you -20% max HP, -10% dmg.");
            draw_set_color(make_color_rgb(150, 210, 150));
            draw_text(_dx, _by + 32, ui_hint("[C] Cure now", "Tap it, then Cure Corruption") + " (keep +" + string(pet_corr_runs(_p) * 15) + "%, forfeit grand power)");
            _by += 66;
        } else if (_cst == "fulfilled") {
            draw_set_color(make_color_rgb(210, 120, 230));
            draw_text(_dx, _by + 2, "FULLY CORRUPTED  -  grand power unleashed (+45%).");
            _by += 34;
        } else {
            if (_elab != "") {
                // The egg's carried gift - shown on the egg itself AND the hatchling that keeps it.
                // Role-aware (08-17): show what the gift DOES for THIS creature.
                var _etd = pet_egg_effect_for(_p);
                var _egtxt = _elab + " - " + ((_etd == undefined) ? "" : (_etd.desc + ((_etd.note != "") ? ("  (" + _etd.note + ")") : "")));
                draw_set_color(make_color_rgb(228, 190, 90));
                draw_text_ext(_dx, _by, _egtxt, 26, _dw - 14);
                _by += string_height_ext(_egtxt, 26, _dw - 14) + 10;
            } else {
                var _origin = _p.raised ? "Hatched and raised by you." : "Found already living in the dark.";
                if (_cst == "cured") _origin = "Purged of corruption (+" + string(pet_corr_runs(_p) * 15) + "% retained).";
                draw_set_color(make_color_rgb(150, 160, 185));
                draw_text(_dx, _by + 2, _origin);
                _by += 32;
            }
            if (_p.signature) {
                draw_set_color(make_color_rgb(210, 180, 120));
                var _sig_sp = pet_species_get(_p.species);
                draw_text(_dx, _by + 2, variable_struct_exists(_sig_sp, "boss")
                    ? ("A scion of " + _sig_sp.boss + " - its like is found nowhere else.")
                    : "A boss's scion - its like is found nowhere else.");
                _by += 32;
            }
        }
        draw_set_color(make_color_rgb(52, 58, 80));
        draw_rectangle(_dx - 14, _gb_y0, _dx + _dw, _by + 6, true);
        var _after = _by + 24;

        // Eggs keep an explicit hatch prompt (their one action here); live creatures'
        // set-active/donate/etc. hints live in the shared footer + ACTIVE pill.
        if (_p.is_egg) {
            draw_set_font(ui_font_dense(fnt_ui_small));
            draw_set_color(make_color_rgb(120, 210, 150));
            draw_text(_dx, _after, ui_hint("[Enter] Hatch this egg", "Tap the egg to hatch it"));
            _after += 36;
        }

        // Capstone pick prompt (a raised Adult owes its permanent gift), or the Awakened
        // splash pick once it crosses. Same [G] key, same modal (bairc_capstone_mode).
        if (pet_capstone_can_pick(_p)) {
            draw_set_font(ui_font_dense(fnt_ui));
            draw_set_color(make_color_rgb(235, 205, 120));
            draw_text(_dx, _after, ui_hint("[G] Choose its Capstone", "Tap it, then Choose Its Gift") + "  -  a permanent gift awaits");
            _after += 44;
        } else if (pet_splash_can_pick(_p)) {
            draw_set_font(ui_font_dense(fnt_ui));
            draw_set_color(make_color_rgb(235, 205, 120));
            draw_text(_dx, _after, ui_hint("[G] Choose its Splash", "Tap it, then Choose Its Gift") + "  -  the Awakened crossing awaits");
            _after += 44;
        }

        // ---- FEED box (ALL live creatures - fully grown pets still get hungry and
        // hurt, and pet_feed_apply always fed them; hiding the box at max stage was a
        // visual oversight, M 07-27): OWNED feed from the pouch, bought at Petra;
        // number key applies it. Outlined like the other groups; row count adapts to
        // the space the boxes above left (overflow shows "+N more" - hotkeys 1-6
        // still address the full pouch list, as with the old 6-row cap).
        if (!_p.is_egg) {
            var _grown  = (_p.stage >= pet_max_stage());
            var _owned  = pet_feed_owned_list();
            var _fready = pet_growth_ready(_p);
            var _fy0    = _after, _fy1 = _y2 - 66;
            draw_set_color(make_color_rgb(15, 17, 24));
            draw_rectangle(_dx - 14, _fy0, _dx + _dw, _fy1, false);
            draw_set_color(make_color_rgb(52, 58, 80));
            draw_rectangle(_dx - 14, _fy0, _dx + _dw, _fy1, true);
            draw_set_font(ui_font_dense(fnt_ui_small));
            var _crowd = pet_feed_crowd_mult(_p);
            draw_set_color(make_color_rgb(150, 200, 140));
            draw_text(_dx, _fy0 + 12, _grown
                ? "FEED  -  sates hunger & heals  (buy from Petra)"
                : "FEED  -  fills growth  (buy from Petra)");
            // Right-side status note: growth-full beats the crowding warning (it is the
            // reason the rows below are greyed - feed can't help until a run evolves it).
            // A fully-grown pet has no growth to warn about (crowding only dampens
            // growth), so it just states its state when it has no reason to eat.
            if (_grown) {
                if (pet_hunger(_p) >= 100 && pet_hp(_p) >= pet_max_hp(_p)) {
                    draw_set_halign(fa_right);
                    draw_set_color(make_color_rgb(120, 210, 150));
                    draw_text(_dx + _dw - 14, _fy0 + 12, "fully grown - well-fed");
                    draw_set_halign(fa_left);
                }
            } else if (_fready) {
                draw_set_halign(fa_right);
                draw_set_color(make_color_rgb(120, 210, 150));
                draw_text(_dx + _dw - 14, _fy0 + 12, "growth FULL - run to evolve");
                draw_set_halign(fa_left);
            } else if (_crowd < 1.0) {
                draw_set_halign(fa_right);
                draw_set_color(make_color_rgb(230, 160, 90));
                draw_text(_dx + _dw - 14, _fy0 + 12, "crowded stable - feed at " + string(round(_crowd * 100)) + "%");
                draw_set_halign(fa_left);
            }
            var _rows_y = _fy0 + 46;
            if (array_length(_owned) == 0) {
                draw_set_color(make_color_rgb(120, 124, 138));
                draw_text(_dx, _rows_y, "Feed pouch empty - visit Petra the Trader.");
            }
            // Fit as many 54px rows as the box allows (max 6 = the hotkey range),
            // reserving a line for the overflow note when the pouch is deeper.
            // Pages are sized by the MEASURED capacity, not a flat 6 (M 07-27: when
            // the boxes above squeezed this one to <6 rows, ceil(owned/6) said "one
            // page" and the tail said "+N more" but could never be shown). Draw
            // publishes the capacity in _gc.bairc_feed_vis; the Step pager and the
            // hotkey mapping MUST read it so hotkey [n] = visible row n.
            var _avail = _fy1 - _rows_y - 10;
            var _fcap  = min(6, max(1, _avail div 54));
            if (array_length(_owned) > _fcap) _fcap = min(_fcap, max(1, (_avail - 30) div 54));
            _gc.bairc_feed_vis = _fcap;
            var _fp_total = max(1, ceil(array_length(_owned) / _fcap));
            var _fpage    = clamp(_gc.bairc_feed_page, 0, _fp_total - 1);
            var _fp_off   = _fpage * _fcap;
            var _fmax     = min(_fcap, array_length(_owned) - _fp_off);
            for (var _fi = 0; _fi < _fmax; _fi++) {
                var _ff  = _owned[_fp_off + _fi];
                var _cnt = pet_feed_pouch_count(_ff.id);
                // Species favorites only feed their own species; a match glows gold.
                var _is_pref = variable_struct_exists(_ff, "species");
                var _fav     = _is_pref && (_ff.species == _p.species);
                // Favored TREATS (08-17) glow gold too - +2 bond for this creature.
                var _tfav    = pet_treat_is_favored(_ff, _p);
                if (_tfav) _fav = true;
                // Edibility mirrors pet_feed_apply EXACTLY (M 07-09: rows greyed on
                // "growth full" alone, but food still feeds a hungry or hurt pet at
                // full growth, and treats are bond-only - growth never gates them).
                var _is_treat = variable_struct_exists(_ff, "bond") && _ff.bond > 0;
                var _edible;
                if (_is_treat) {
                    _edible = (pet_treats_left() > 0);
                } else {
                    var _fd_grow_ok = (_p.stage < pet_max_stage()) && !_fready;
                    _edible = _fd_grow_ok || (pet_hunger(_p) < 100) || (pet_hp(_p) < pet_max_hp(_p));
                }
                var _can = (_cnt > 0) && _edible && (!_is_pref || _fav);
                var _fby = _rows_y + _fi * 54;
                var _fbh = 48;
                draw_set_color(_fav && _can ? make_color_rgb(40, 36, 22) : (_can ? make_color_rgb(26, 36, 30) : make_color_rgb(20, 22, 28)));
                draw_rectangle(_dx, _fby, _dx + _dw - 14, _fby + _fbh, false);
                draw_set_color(_fav && _can ? make_color_rgb(210, 175, 90) : (_can ? make_color_rgb(90, 170, 110) : make_color_rgb(44, 50, 60)));
                draw_rectangle(_dx, _fby, _dx + _dw - 14, _fby + _fbh, true);
                // Feed icon (left), name + growth, owned count (right).
                var _ffic = pet_feed_icon(_ff.id);
                var _txt_x = _dx + 14;
                if (_ffic >= 0) {
                    var _ffs = 40 / max(1, max(sprite_get_width(_ffic), sprite_get_height(_ffic)));
                    draw_sprite_ext(_ffic, 0, _dx + 26, _fby + _fbh / 2, _ffs, _ffs, 0, c_white, _can ? 1 : 0.5);
                    _txt_x = _dx + 52;
                }
                draw_set_font(ui_font_dense(fnt_ui));
                draw_set_color(_fav && _can ? make_color_rgb(235, 210, 140) : (_can ? make_color_rgb(222, 228, 240) : make_color_rgb(110, 114, 128)));
                // Crowded stable: show what the feed ACTUALLY grants ("+3 -> +2").
                // Treats are bond-only - label them "+1 bond", never "+0 grow" (#4).
                // Fully grown: growth is meaningless, so show the hunger the meal
                // sates (mirrors pet_feed_apply: 10 + growth*10).
                var _fg_eff = pet_feed_effective_growth(_p, _ff);
                var _fg_txt = (variable_struct_exists(_ff, "bond") && _ff.bond > 0)
                    ? ("+" + string(pet_treat_bond_for(_ff, _p)) + " bond")
                    : (_grown ? ("+" + string(10 + _ff.growth * 10) + " hunger")
                    : ((_fg_eff < _ff.growth) ? ("+" + string(_ff.growth) + " -> +" + string(_fg_eff)) : ("+" + string(_ff.growth))));
                draw_text(_txt_x, _fby + 8, "[" + string(_fi + 1) + "]  " + _ff.name + "   " + _fg_txt + (_tfav ? "   - a favored treat!" : (_fav ? "   - its favorite!" : "")));
                if (_tfav) ui_draw_heart(_txt_x + string_width("[" + string(_fi + 1) + "]  " + _ff.name) + 12, _fby + 24, 7, make_color_rgb(235, 110, 140));
                draw_set_font(ui_font_dense(fnt_ui_small));
                draw_set_halign(fa_right);
                draw_set_color(_can ? make_color_rgb(150, 205, 150) : make_color_rgb(90, 110, 96));
                draw_text(_dx + _dw - 30, _fby + 14, "x" + string(_cnt));
                draw_set_halign(fa_left);
            }
            if (_fp_total > 1) {
                draw_set_font(ui_font_dense(fnt_ui_small));
                draw_set_color(make_color_rgb(150, 160, 140));
                var _fp_after = array_length(_owned) - (_fp_off + _fmax);
                draw_text(_dx, _rows_y + _fmax * 54 + 4, ui_hint("[A]/[D] page ", "Page ") + string(_fpage + 1) + "/" + string(_fp_total)
                    + (_fp_after > 0 ? ("  -  " + string(_fp_after) + " more in the pouch") : ""));
                // Vertical scroll bar (M 07-27 standing rule: overflowing lists get a
                // visible scroll indicator, not just a "+N more" line). Track spans the
                // row area on the box's inner-right edge; thumb = this page's window.
                var _fsb_x0 = _dx + _dw - 8, _fsb_x1 = _dx + _dw - 2;
                var _fsb_y0 = _rows_y, _fsb_y1 = _rows_y + _fcap * 54 - 6;
                draw_set_color(make_color_rgb(34, 38, 50));
                draw_rectangle(_fsb_x0, _fsb_y0, _fsb_x1, _fsb_y1, false);
                var _fsb_h  = _fsb_y1 - _fsb_y0;
                var _fsb_t0 = _fsb_y0 + _fsb_h * (_fp_off / array_length(_owned));
                var _fsb_t1 = _fsb_y0 + _fsb_h * (min(array_length(_owned), _fp_off + _fmax) / array_length(_owned));
                draw_set_color(make_color_rgb(120, 150, 130));
                draw_rectangle(_fsb_x0, _fsb_t0, _fsb_x1, _fsb_t1, false);
            }
        }
    }

    // Bairc stands full-size (300px) in his reserved right-hand column, like every vendor.
    ui_draw_npc_column("bairc");

    // Notification + footer controls.
    if (variable_instance_exists(_gc, "bairc_notification") && _gc.bairc_notification != "") {
        draw_set_font(ui_font_dense(fnt_ui));
        draw_set_color(c_yellow);
        // Sits above the (now 2-row) key legend: legend rows are y2-42 and
        // ~y2-70, so the notification bottom must clear y2-74 (measured).
        draw_text(_x1 + _pad, _y2 - 78 - string_height("Ag"), _gc.bairc_notification);
    }
    draw_set_halign(fa_center);
    draw_set_font(ui_font_dense(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 160, 190));
    // [G] only appears when the selected pet actually owes a pick, with the exact
    // word for it - keeps the footer short (it was crowding the panel) and clear
    // (the old always-on "[G] Its Gift" read as nonsense out of context).
    // Chunk 7b: on a gamepad the letters live in the A action submenu, so the
    // footer collapses to the four real buttons instead of a wall of dead keys.
    var _bfoot;
    if (input_device() == 1) {
        _bfoot = "[D-Pad] Browse  [A] Actions  [RT] Gift Bairc  [L3] Station  [Y] Details  [B] Leave";
    } else {
        _bfoot = "[W/S] Browse  [1-6] Feed";
        if (_n > 0) {
            var _fp = _roster[_cur];
            if (is_struct(_fp) && !_fp.is_egg) {
                if (variable_struct_exists(_fp, "capstone_pending") && _fp.capstone_pending)   _bfoot += "  [G] Capstone";
                else if (variable_struct_exists(_fp, "splash_pending") && _fp.splash_pending)  _bfoot += "  [G] Splash";
            }
        }
        _bfoot += "  [V] Garden  [F] Gift Bairc  [U] Station  [N] Name  [C] Cure  [R] Donate  [Tab] Details  [Enter] Hatch/Active  [Esc] Leave";   // [V] added 08-16 (M: "I didn't even see it")
    }
    ui_draw_key_legend((_x1 + 1500) / 2, _y2 - 42, _bfoot, undefined, false, (1500 - _x1) - 40);   // wraps to 2 rows inside the panel (M 08-16 shot)
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white);
    draw_set_font(-1);

    // Stat hover tooltip - drawn after the actor column and footer so it tops everything;
    // the modals below dim the whole screen, so they rightly cover it when open.
    if (_stat_hover != "" && _n > 0) {
        ui_draw_pet_stat_tooltip(device_mouse_x_to_gui(0), device_mouse_y_to_gui(0), _roster[_cur], _stat_hover);
    }

    // READY-bar hover tooltip: the full evolve requirement, wrapped (drawn last, on top).
    if (_growth_hover_txt != "") {
        var _gt_mx = device_mouse_x_to_gui(0), _gt_my = device_mouse_y_to_gui(0);
        draw_set_font(ui_font_dense(fnt_ui_small));
        var _gt_w = 520;
        var _gt_h = string_height_ext(_growth_hover_txt, 27, _gt_w - 36) + 36;
        var _gt_x = min(_gt_mx + 24, GUI_W - _gt_w - 12);
        var _gt_y = min(_gt_my + 18, GUI_H - _gt_h - 12);
        draw_set_alpha(0.96);
        draw_set_color(make_color_rgb(18, 20, 32));
        draw_rectangle(_gt_x, _gt_y, _gt_x + _gt_w, _gt_y + _gt_h, false);
        draw_set_alpha(1.0);
        draw_set_color(make_color_rgb(120, 210, 150));
        draw_rectangle(_gt_x, _gt_y, _gt_x + _gt_w, _gt_y + _gt_h, true);
        draw_set_color(make_color_rgb(210, 218, 230));
        draw_text_ext(_gt_x + 18, _gt_y + 18, _growth_hover_txt, 27, _gt_w - 36);
        draw_set_font(-1);
        draw_set_color(c_white);
    }

    // Tab detail popup over the station (the highlighted creature's full kit breakdown).
    if (variable_instance_exists(_gc, "bairc_detail_open") && _gc.bairc_detail_open && _n > 0) {
        ui_draw_pet_detail(_roster[_cur]);
    }

    // Donate confirm modal (design §6: donation, not release - it lives on in his garden).
    if (variable_instance_exists(_gc, "bairc_release_confirm") && _gc.bairc_release_confirm && _n > 0) {
        var _rp = _roster[_cur];
        var _rmx1 = 560, _rmy1 = 372, _rmx2 = 1360, _rmy2 = 636;
        draw_set_alpha(0.9); draw_set_color(c_black);
        draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false); draw_set_alpha(1.0);
        draw_set_color(make_color_rgb(20, 18, 14));
        draw_rectangle(_rmx1, _rmy1, _rmx2, _rmy2, false);
        ui_draw_gothic_frame(_rmx1, _rmy1, _rmx2, _rmy2, 30);
        draw_set_halign(fa_center); draw_set_valign(fa_top);
        draw_set_font(fnt_ui_title); draw_set_color(make_color_rgb(228, 200, 130));
        // Shrink-to-fit so a long pet name can't run past the modal frame (M 07-17).
        var _et_txt = "Entrust " + (_rp.is_egg ? (_rp.name + " Egg") : _rp.name) + " to Bairc?";
        var _et_sc  = min(1, ((_rmx2 - _rmx1) - 80) / max(1, string_width(_et_txt)));
        draw_text_transformed(GUI_CX, _rmy1 + 34, _et_txt, _et_sc, _et_sc, 0);
        draw_set_font(ui_font_dense(fnt_ui)); draw_set_color(make_color_rgb(205, 198, 208));
        draw_text_ext(GUI_CX, _rmy1 + 104, _rp.is_egg
            ? "He will keep the egg warm and hatch it in his own time. It will not be yours."
            : "It stays in his garden for good - cared for, visible, but no longer yours to raise.",
            34, (_rmx2 - _rmx1) - 120);
        draw_set_font(ui_font_dense(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
        draw_text(GUI_CX, _rmy2 - 48, ui_hint("[Enter] Donate     [Esc] Keep", "OK: Donate     X: Keep"));
        draw_set_halign(fa_left); draw_set_valign(fa_top); draw_set_color(c_white); draw_set_font(-1);
    }

    // Naming modal (typed text entry over the station).
    if (variable_instance_exists(_gc, "bairc_naming") && _gc.bairc_naming && _n > 0) {
        var _np = _roster[_cur];
        // Android (8c): lift the modal clear of the OS keyboard while it's up.
        var _bn_off = (variable_global_exists("osk_shown") && global.osk_shown) ? -210 : 0;
        var _nx1 = 540, _ny1 = 354 + _bn_off, _nx2 = 1380, _ny2 = 666 + _bn_off;
        draw_set_alpha(0.9); draw_set_color(c_black);
        draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false); draw_set_alpha(1.0);
        draw_set_color(make_color_rgb(16, 18, 26));
        draw_rectangle(_nx1, _ny1, _nx2, _ny2, false);
        ui_draw_gothic_frame(_nx1, _ny1, _nx2, _ny2, 30);
        draw_set_halign(fa_center); draw_set_valign(fa_top);
        draw_set_font(fnt_ui_title); draw_set_color(make_color_rgb(220, 210, 235));
        draw_text(GUI_CX, _ny1 + 28, pet_named(_np) ? "Rename your creature" : "Name your creature");
        draw_set_font(ui_font_dense(fnt_ui_small)); draw_set_color(make_color_rgb(175, 165, 205));
        draw_text(GUI_CX, _ny1 + 82, "This name will now be known to them...");
        // Input field + blinking caret.
        var _ntxt = keyboard_string;
        if ((current_time div 450) mod 2 == 0) _ntxt += "_";
        draw_set_color(make_color_rgb(28, 32, 44));
        draw_rectangle(_nx1 + 90, _ny1 + 124, _nx2 - 90, _ny1 + 184, false);
        draw_set_color(make_color_rgb(90, 110, 150));
        draw_rectangle(_nx1 + 90, _ny1 + 124, _nx2 - 90, _ny1 + 184, true);
        draw_set_font(ui_font_dense(fnt_ui)); draw_set_color(c_white);
        draw_text(GUI_CX, _ny1 + 138, _ntxt);
        draw_set_font(ui_font_dense(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
        draw_text(GUI_CX, _ny1 + 208, (pet_named(_np) ? "Rename: 20 dust" : "First name: free")
            + ((input_device() == 2) ? "" : "        [Enter] Confirm     [Esc] Cancel"));
        // Deck/controller players have no physical keyboard - point at the Steam OSK
        // (STEAM_DECK_NOTES.md; same hint as the char-create naming modal).
        if (input_device() == 1) {
            draw_set_color(make_color_rgb(100, 110, 130));
            draw_text(GUI_CX, _ny1 + 248, "No keyboard? Steam + X opens the on-screen keyboard.");
        }
        // DONE button (fires the same Enter path) - every device (08-19 mouse pass).
        {
            var _bdx1 = GUI_CX - 150, _bdy1 = _ny1 + 238;
            var _bdx2 = GUI_CX + 150, _bdy2 = _bdy1 + 57;
            draw_set_color(make_color_rgb(20, 34, 58));
            draw_rectangle(_bdx1, _bdy1, _bdx2, _bdy2, false);
            draw_set_color(make_color_rgb(90, 110, 150));
            draw_rectangle(_bdx1, _bdy1, _bdx2, _bdy2, true);
            draw_set_font(ui_font_dense(fnt_ui)); draw_set_valign(fa_middle);
            draw_set_color(c_white);
            draw_text(GUI_CX, (_bdy1 + _bdy2) / 2, "DONE");
            draw_set_valign(fa_top);
            if (touch_tapped(_bdx1, _bdy1, _bdx2, _bdy2)) touch_press(vk_enter);
        }
        draw_set_halign(fa_left); draw_set_valign(fa_top); draw_set_color(c_white); draw_set_font(-1);
    }

    // Gamepad action submenu (chunk 7b): the highlighted creature's actions,
    // opened with A on a roster row (obj_game_controller/Step owns the input).
    // Level 1 is the feed list. Drawn last so it tops the tooltips.
    if (variable_instance_exists(_gc, "bairc_pad_menu_open") && _gc.bairc_pad_menu_open && _n > 0) {
        var _pmp = _roster[_cur];
        var _pmi = bairc_pad_menu_items(_pmp, _gc.bairc_pad_menu_level);
        var _pmn = array_length(_pmi);
        var _bm_touch = (input_device() == 2);
        var _pmw      = _bm_touch ? 564 : 480;
        var _bm_pitch = _bm_touch ? 72 : 48;   // row spacing - bigger tap targets on touch
        var _bm_rowh  = _bm_touch ? 60 : 42;   // highlight / tap-zone height
        // Donate (tag "bairc:R") is destructive - set it apart with an extra gap +
        // divider + red tint so it can't be fat-fingered next to Feed/Name (M 07-17).
        var _bm_dgap  = 30;
        var _bm_has_donate = false;
        for (var _dq = 0; _dq < _pmn; _dq++) if (_pmi[_dq].tag == "bairc:R") _bm_has_donate = true;
        var _pmh = 100 + _pmn * _bm_pitch + (_bm_has_donate ? _bm_dgap : 0);
        var _pmx = GUI_CX - _pmw / 2;
        var _pmy = GUI_CY - _pmh / 2;
        draw_set_alpha(0.55); draw_set_color(c_black);
        draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
        draw_set_alpha(0.96); draw_set_color(make_color_rgb(14, 16, 24));
        draw_rectangle(_pmx, _pmy, _pmx + _pmw, _pmy + _pmh, false);
        draw_set_alpha(1.0);
        ui_draw_gothic_frame(_pmx, _pmy, _pmx + _pmw, _pmy + _pmh, 24);
        draw_set_halign(fa_center); draw_set_valign(fa_top);
        draw_set_font(ui_font_dense(fnt_ui)); draw_set_color(make_color_rgb(228, 205, 140));
        draw_text(GUI_CX, _pmy + 20, (_gc.bairc_pad_menu_level == 1)
            ? ("Feed " + (_pmp.is_egg ? "the egg" : _pmp.name))
            : (_pmp.is_egg ? (_pmp.name + " Egg") : _pmp.name));
        draw_set_valign(fa_middle);
        for (var _pr = 0; _pr < _pmn; _pr++) {
            var _pit    = _pmi[_pr];
            var _is_don = (_pit.tag == "bairc:R");
            var _pry    = _pmy + 56 + _pr * _bm_pitch + (_is_don ? _bm_dgap : 0);   // row TOP
            var _is_cur = (_pr == _gc.bairc_pad_menu_cursor);
            if (_is_don) {   // divider above the set-apart Donate row
                draw_set_color(make_color_rgb(92, 54, 48));
                draw_rectangle(_pmx + 30, _pry - _bm_dgap + 9, _pmx + _pmw - 30, _pry - _bm_dgap + 11, false);
            }
            if (_is_cur) {
                draw_set_color(_is_don ? make_color_rgb(58, 26, 24) : make_color_rgb(46, 42, 28));
                draw_rectangle(_pmx + 18, _pry, _pmx + _pmw - 18, _pry + _bm_rowh, false);
                draw_set_color(_is_don ? make_color_rgb(240, 120, 100) : make_color_rgb(255, 224, 120));
                draw_rectangle(_pmx + 18, _pry, _pmx + _pmw - 18, _pry + _bm_rowh, true);
            }
            draw_set_color((_pit.tag == "") ? make_color_rgb(110, 114, 128)
                          : (_is_don ? (_is_cur ? make_color_rgb(255, 185, 170) : make_color_rgb(214, 130, 116))
                                     : (_is_cur ? c_white : make_color_rgb(200, 208, 222))));
            draw_text(GUI_CX, _pry + _bm_rowh / 2, _pit.label);
        }
        draw_set_valign(fa_top);
        draw_set_font(ui_font_dense(fnt_ui_small)); draw_set_color(make_color_rgb(150, 158, 182));
        ui_draw_key_legend(GUI_CX, _pmy + _pmh - 34,
            (input_device() == 2)
                ? "Tap an action  -  tap outside to close"
                : ((_gc.bairc_pad_menu_level == 1) ? "[A] Feed  [B] Back" : "[A] Select  [B] Close"),
            undefined, false);
        // Touch (8d): tap an entry to pick it (cursor + simulated Enter); tap
        // anywhere outside the panel to back out (simulated Esc).
        if (mouse_check_button_pressed(mb_left)) {
            var _pmmx = device_mouse_x_to_gui(0);
            var _pmmy = device_mouse_y_to_gui(0);
            var _pm_hit = false;
            for (var _pt = 0; _pt < _pmn; _pt++) {
                var _pty = _pmy + 56 + _pt * _bm_pitch + ((_pmi[_pt].tag == "bairc:R") ? _bm_dgap : 0);
                if (_pmmx >= _pmx + 18 && _pmmx <= _pmx + _pmw - 18
                    && _pmmy >= _pty && _pmmy <= _pty + _bm_rowh) {
                    _gc.bairc_pad_menu_cursor = _pt;
                    touch_press(vk_enter);
                    _pm_hit = true;
                    break;
                }
            }
            // 08-18 (M on phone: "the OK button just exits the hatch popup"): the on-screen
            // pad's OK / d-pad sit OUTSIDE the panel, so their press was also read as a
            // tap-outside -> Esc, and the Esc beat the Enter. Pad footprint is exempt.
            if (!_pm_hit && !touch_over_pad(_pmmx, _pmmy)
                && (_pmmx < _pmx || _pmmx > _pmx + _pmw || _pmmy < _pmy || _pmmy > _pmy + _pmh)) {
                touch_press(vk_escape);
            }
        }
        draw_set_halign(fa_left); draw_set_valign(fa_top); draw_set_color(c_white); draw_set_font(-1);
    }
}

// ---------------------------------------------------------------------------
// ui_draw_pet_detail(pet)
// Full-screen "press Tab" breakdown for a creature - mirrors ui_draw_ability_detail:
// sprite, stage/archetype, role + live effect, and the named KIT (abilities & traits)
// with descriptions, plus injury/corruption status. Opened from Bairc's station and the
// Gate Companion tab; the caller gates input while it's up. (Pets §5)
// ---------------------------------------------------------------------------
function ui_draw_pet_detail(pet, inline = false) {
    // inline (#1): drawn as a seamless PAGE inside the character menu's Companion
    // tab - no screen dim, a plain panel border instead of the modal gothic frame,
    // and no "[Tab]/[Esc] Close" footer (the tab's own footer covers input).
    if (!is_struct(pet)) return;
    var _pd_stat_hover = "";   // stat chip under the mouse; tooltip drawn last
    var _pd_type_hover = "";   // archetype name under the mouse; free-text tooltip drawn last
    if (!inline) {
        draw_set_alpha(0.84); draw_set_color(c_black);
        draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
        draw_set_alpha(1.0);
    }
    var _x1 = 360, _y1 = 96, _x2 = 1560, _y2 = 1002;
    draw_set_color(make_color_rgb(16, 17, 26));
    draw_rectangle(_x1, _y1, _x2, _y2, false);
    if (inline) {
        draw_set_color(make_color_rgb(52, 58, 80));
        draw_rectangle(_x1, _y1, _x2, _y2, true);
    } else {
        ui_draw_gothic_frame(_x1, _y1, _x2, _y2, 36);
    }

    var _pad = 45, _lx = _x1 + _pad, _rx = _x2 - _pad, _y = _y1 + _pad;
    draw_set_halign(fa_left); draw_set_valign(fa_top);

    // Header: framed sprite box (left) + name + stage/archetype (right of it). The
    // creature art (esp. wide quadrupeds) is boxed and fit by BOTH width and height so it
    // can never bleed into the text column; the new sprites use a bottom-centre origin.
    var _hbx0 = _lx, _hby0 = _y, _hbx1 = _lx + 140, _hby1 = _y + 150;
    draw_set_color(make_color_rgb(24, 26, 38));
    draw_rectangle(_hbx0, _hby0, _hbx1, _hby1, false);
    draw_set_color(make_color_rgb(70, 78, 104));
    draw_rectangle(_hbx0, _hby0, _hbx1, _hby1, true);
    var _spr = pet_sprite(pet, "s");
    if (_spr >= 0) {
        // #16: fit the VISIBLE creature (bbox) into the portrait box, not the canvas.
        var _hfit = pet_sprite_fit(_spr, (_hbx0 + _hbx1) / 2, _hby1 - 9, ((_hby1 - _hby0) - 18) * pet_stage_size_mult(pet.stage), ((_hbx1 - _hbx0) - 22) * pet_stage_size_mult(pet.stage));   // stage-scaled (08-19)
        draw_sprite_ext(_spr, pet_anim_frame(_spr), _hfit.x, _hfit.y, _hfit.scale, _hfit.scale, 0, c_white, 1);
    }
    var _htx = _hbx1 + 26;
    draw_set_font(fnt_ui_title); draw_set_color(c_white);
    draw_text(_htx, _y + 3, pet.is_egg ? (pet.name + " Egg") : pet.name);
    draw_set_font(ui_font(fnt_ui)); draw_set_color(make_color_rgb(190, 160, 240));
    // SPECIES leads this line (M 08-08). The sheet never stated WHAT the creature
    // was anywhere - and a NAMED pet hid it completely, since the header above
    // shows the given name. Eggs stay a mystery until they hatch. The separator
    // narrows if the longest combination would run past the panel's right edge.
    var _pd_arch_nm = pet_archetype_name(pet.archetype);
    var _pd_sep     = "   -   ";
    var _pd_head    = pet.is_egg ? pet_stage_name(pet.stage)
                    : (pet_species_get(pet.species).name + _pd_sep + pet_stage_name(pet.stage));
    if (string_width(_pd_head + _pd_sep + _pd_arch_nm) > (_rx - _htx)) {
        _pd_sep  = " - ";
        _pd_head = pet.is_egg ? pet_stage_name(pet.stage)
                 : (pet_species_get(pet.species).name + _pd_sep + pet_stage_name(pet.stage));
    }
    draw_text(_htx, _y + 62, _pd_head + _pd_sep + _pd_arch_nm);
    // Hover the archetype name for what the type DOES (M 07-08 - e.g. Boon:
    // "grants passive boons, never fights"). Measured with fnt_ui (still set).
    if (!pet.is_egg) {
        var _pd_arch_x0 = _htx + string_width(_pd_head + _pd_sep);
        var _pd_arch_w  = string_width(_pd_arch_nm);
        var _pd_hmx = device_mouse_x_to_gui(0), _pd_hmy = device_mouse_y_to_gui(0);
        if (_pd_hmx >= _pd_arch_x0 && _pd_hmx <= _pd_arch_x0 + _pd_arch_w + 10
            && _pd_hmy >= _y + 58 && _pd_hmy <= _y + 96) {
            _pd_type_hover = pet_archetype_name(pet.archetype) + " type: " + pet_archetype_blurb(pet.archetype);
        }
    }
    draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
    // (The egg gift moved from this header into PASSIVES & TRAITS, where its effect
    // text sits under it - a bare "Gilded Egg" here communicated nothing.)
    var _flavtag = (pet.raised ? "Raised from the egg." : "Found already living.")
                 + pet_injury_tag(pet) + pet_corruption_tag(pet);
    // Combat move pool (08-17): what this creature can DO in a fight, so the log
    // lines ("uses REND") have a home the player can read before the fight.
    if (!pet.is_egg && pet_move_pool_text(pet) != "") _flavtag += "  Moves: " + pet_move_pool_text(pet) + " (picked by need).";
    draw_text_ext(_htx, _y + 100, _flavtag, 26, _rx - _htx);

    _y += 168;
    draw_set_color(make_color_rgb(60, 64, 90)); draw_line(_lx, _y, _rx, _y); _y += 18;

    if (pet.is_egg) {
        draw_set_font(ui_font(fnt_ui)); draw_set_color(make_color_rgb(150, 156, 175));
        draw_text_ext(_lx, _y, "An unhatched egg. Hatch it at Bairc to reveal the creature within - its stats, passives and abilities all appear once it is born.", 34, _rx - _lx);
    } else {
        // ---- STATS ----
        draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(120, 200, 140));
        draw_text(_lx, _y, "STATS"); _y += 34;
        // Growth + bond (the stage itself already sits in the header - don't repeat it).
        draw_set_font(ui_font(fnt_ui)); draw_set_color(make_color_rgb(210, 214, 230));
        var _statline = (pet.stage < pet_max_stage())
            ? ("Growth " + string(pet.growth) + " / " + string(pet_growth_needed(pet.stage)) + "  to " + pet_stage_name(pet.stage + 1))
            : "Fully grown";
        _statline += "      Bond: " + pet_bond_tier_name(pet_bond_tier(pet));
        _statline += "      Hunger: " + string(pet_hunger(pet)) + "/100 (" + pet_hunger_state_label(pet) + ")";
        _statline += "      HP: " + ((pet_hp(pet) <= 0) ? "KO"
            : (string(pet_hp(pet)) + "/" + string(pet_max_hp(pet))));   // #20 pool
        draw_text(_lx, _y, _statline); _y += 42;
        // Three stat chips (the governing stat for this archetype is highlighted gold).
        // Hovering a chip pops the stat's explanation tooltip (drawn last, at the bottom
        // of this function, so nothing overdraws it).
        var _skeys = ["pow", "spr", "lck"];
        var _pri   = pet_stat_primary(pet.archetype);
        var _cw    = (_rx - _lx) / 3;
        var _pd_mx = device_mouse_x_to_gui(0), _pd_my = device_mouse_y_to_gui(0);
        for (var _si = 0; _si < 3; _si++) {
            var _sk  = _skeys[_si];
            var _cx0 = _lx + _si * _cw;
            var _hot = (_sk == _pri);
            var _hov = (_pd_mx >= _cx0 && _pd_mx <= _cx0 + _cw - 14 && _pd_my >= _y && _pd_my <= _y + 84);
            if (_hov) _pd_stat_hover = _sk;
            draw_set_color(_hot ? make_color_rgb(32, 30, 20) : make_color_rgb(18, 20, 30));
            draw_rectangle(_cx0, _y, _cx0 + _cw - 14, _y + 84, false);
            draw_set_color(_hov ? make_color_rgb(200, 215, 245) : (_hot ? make_color_rgb(210, 175, 90) : make_color_rgb(50, 56, 76)));
            draw_rectangle(_cx0, _y, _cx0 + _cw - 14, _y + 84, true);
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small)); draw_set_color(_hot ? make_color_rgb(232, 202, 122) : make_color_rgb(150, 160, 185));
            draw_text(_cx0 + (_cw - 14) / 2, _y + 6, pet_stat_name(_sk));
            // Big number vertically centred in the chip's LOWER half (taller chip + lower
            // centre so the title-font digits can't collide with the abbreviation above).
            draw_set_font(fnt_ui_title); draw_set_valign(fa_middle); draw_set_color(c_white);
            draw_text(_cx0 + (_cw - 14) / 2, _y + 56, string(pet_stat(pet, _sk)));
            draw_set_valign(fa_top); draw_set_halign(fa_left);
        }
        _y += 98;
        draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(140, 150, 175));
        draw_text(_lx, _y, pet_stat_name(_pri) + " governs its gift - and every stat helps any creature. Hover a chip for details."); _y += 36;

        // ---- PASSIVES (left column) & ABILITIES (right column) ----
        draw_set_color(make_color_rgb(60, 64, 90)); draw_line(_lx, _y, _rx, _y); _y += 16;
        var _mid  = (_lx + _rx) / 2 + 20;   // right column start
        var _colw = _mid - _lx - 40;        // wrap width per column
        var _cyL  = _y, _cyR = _y;

        draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(190, 160, 240));
        draw_text(_lx, _cyL, "PASSIVES & TRAITS"); _cyL += 36;
        draw_set_color(make_color_rgb(120, 200, 140));
        draw_text(_mid, _cyR, "ABILITIES"); _cyR += 36;

        // Passives (left).
        var _pl = pet_passive_list(pet);
        if (array_length(_pl) == 0) {
            draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 156, 175));
            draw_text_ext(_lx, _cyL, "None yet - it earns traits as it grows.", 28, _colw);
            _cyL += 54;
        } else {
            // Tightened inter-entry gaps (was +2 / +14) so a max-trait pet - the
            // 5-trait fully-corrupted Fortune - keeps its last trait clear of the
            // "[Tab] - Close" footer instead of spilling into it (M 07-20). Line
            // spacing (30/26) is unchanged, so no desc lines touch.
            for (var _pi = 0; _pi < array_length(_pl); _pi++) {
                var _pe = _pl[_pi];
                draw_set_font(ui_font(fnt_ui)); draw_set_color(make_color_rgb(228, 190, 90));
                draw_text_ext(_lx, _cyL, _pe.name, 30, _colw);
                _cyL += string_height_ext(_pe.name, 30, _colw) + 1;
                draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(195, 200, 216));
                draw_text_ext(_lx + 18, _cyL, _pe.desc, 26, _colw - 18);
                _cyL += string_height_ext(_pe.desc, 26, _colw - 18) + 6;
            }
        }

        // Abilities (right).
        var _al = pet_ability_list(pet);
        if (array_length(_al) == 0) {
            draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 156, 175));
            draw_text_ext(_mid, _cyR, "None - its strength is its passive.", 28, _colw);
            _cyR += 54;
        } else {
            for (var _ai = 0; _ai < array_length(_al); _ai++) {
                var _ae = _al[_ai];
                draw_set_font(ui_font(fnt_ui)); draw_set_color(make_color_rgb(150, 210, 235));
                draw_text_ext(_mid, _cyR, _ae.name, 30, _colw);
                _cyR += string_height_ext(_ae.name, 30, _colw) + 1;   // gaps tightened to match the passives column (M 07-20)
                draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(195, 200, 216));
                draw_text_ext(_mid + 18, _cyR, _ae.desc, 26, _colw - 18);
                _cyR += string_height_ext(_ae.desc, 26, _colw - 18) + 6;
            }
        }
        // These footer teasers are decorative - only draw them when the trait
        // columns leave room above the "[Tab] - Close" hint (_y2 - 39). A maxed
        // pet whose traits fill the card simply omits the teaser rather than
        // overrunning the footer (M 07-20). Traits themselves always fit now.
        var _teaser_y = max(_cyL, _cyR) + 6;
        if (_teaser_y <= _y2 - 90) {
            if (pet.stage < PET_STAGE_ADULT) {
                draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
                draw_text(_lx, _teaser_y, "On reaching Adult it gains a capstone ability.");
            } else if (pet.stage == PET_STAGE_ADULT) {
                // Tease the last rung: the Stage-4 crossing and its gate (design 2026-07-03).
                draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
                draw_text(_lx, _teaser_y, "Beyond lies the Awakened crossing - full-clear at Awakening A5, Soul-bound.");
            }
        }
    }

    if (!inline) {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(150, 160, 190));
        draw_text_outline((_x1 + _x2) / 2, _y2 - 39, ((input_device() == 2) ? "Tap Back to close" : "[Tab] or [Esc] - Close"));
        draw_set_halign(fa_left); draw_set_valign(fa_top);
    }

    // Hovered stat chip's explanation - very last, so it tops the whole popup.
    if (_pd_stat_hover != "") ui_draw_pet_stat_tooltip(device_mouse_x_to_gui(0), device_mouse_y_to_gui(0), pet, _pd_stat_hover);

    // Archetype hover tooltip - free text box (same style as the Bairc READY-bar one).
    if (_pd_type_hover != "") {
        var _pt_mx = device_mouse_x_to_gui(0), _pt_my = device_mouse_y_to_gui(0);
        draw_set_font(ui_font(fnt_ui_small));
        var _pt_w = 520;
        var _pt_h = string_height_ext(_pd_type_hover, 27, _pt_w - 36) + 36;
        var _pt_x = min(_pt_mx + 24, GUI_W - _pt_w - 12);
        var _pt_y = min(_pt_my + 18, GUI_H - _pt_h - 12);
        draw_set_alpha(0.96);
        draw_set_color(make_color_rgb(18, 20, 32));
        draw_rectangle(_pt_x, _pt_y, _pt_x + _pt_w, _pt_y + _pt_h, false);
        draw_set_alpha(1.0);
        draw_set_color(make_color_rgb(190, 160, 240));
        draw_rectangle(_pt_x, _pt_y, _pt_x + _pt_w, _pt_y + _pt_h, true);
        draw_set_color(make_color_rgb(210, 214, 230));
        draw_text_ext(_pt_x + 18, _pt_y + 18, _pd_type_hover, 27, _pt_w - 36);
        draw_set_font(-1);
    }

    draw_set_color(c_white); draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// dungeon_bg_sprite(surface)
// Returns the sprite index for the current dungeon's themed background on a
// given surface, or -1 if that art hasn't been imported yet.
//   surface "combat"   -> spr_combatbg_<tag>_<floor>  (per-floor; floor 1-3 =
//                          the progression layer, so the arena escalates).
//   surface "floormap" -> spr_floormap_<tag>          (one per dungeon).
// <tag> is ashen | scorched | tundra. Uses asset_get_index (string lookup) so it
// compiles cleanly before the assets exist. NOTE: once imported, these string-only
// sprite refs MUST be added to global.__sprite_includes or the compiler strips them.
// ---------------------------------------------------------------------------
function dungeon_bg_sprite(surface) {
    var _d = variable_global_exists("selected_dungeon") ? global.selected_dungeon : "ashen_vault";
    var _tag = "ashen";
    if      (_d == "scorched_depths") _tag = "scorched";
    else if (_d == "tundra_tomb")     _tag = "tundra";

    var _name;
    if (surface == "combat") {
        var _fl = variable_global_exists("current_floor") ? global.current_floor : 1;
        _fl = clamp(_fl, 1, 3);
        _name = "spr_combatbg_" + _tag + "_" + string(_fl);
    } else {
        _name = "spr_floormap_" + _tag;
    }
    return asset_get_index(_name);
}

// ---------------------------------------------------------------------------
// dungeon_bg_draw(surface, scrim_alpha)
// Draws the themed dungeon background stretched to the 1280x720 GUI, with a dark
// scrim on top so overlaid UI (text, HP bars, node graph, sprites) stays legible
// regardless of the art's brightness. Returns false (drawing nothing) when the art
// isn't imported yet, so callers fall back to their flat fill - a safe no-op until
// the MidJourney backgrounds land. See [[project]] SYSTEMS / MISC_TASKS §5.
// ---------------------------------------------------------------------------
// FAUX-2.5D COMBAT EXPERIMENT (M 08-11, reversible lever). ON: the combat
// backdrop is drawn to a perspective-tilted quad (top edge pinched, bottom
// bulged - reads as a camera looking down the arena floor) and the enemy row
// scales by depth. OFF (settings.ini [ui] combat_25d=0, or F7 in combat):
// every code path is byte-identical to the shipped flat look.
function combat_25d() {
    return variable_global_exists("combat_25d") && global.combat_25d;
}

// ---------------------------------------------------------------------------
// ui_draw_cast_shadow(cx, feet_y, width, dir) - actor shadow for the combat
// scene. Flat mode: the shipped contact ellipse, untouched. 2.5D v3 (M 08-13:
// "the shadows being behind them instead of below them would help create the
// illusion"): an oblique pool CAST BACK along the floor toward the horizon -
// offset behind the actor (dir: +1 = behind is screen-right, enemies face
// left; -1 = behind is screen-left, the player faces right) and stretched
// up-screen so it reads as lying ON the receding plane.
// ---------------------------------------------------------------------------
function ui_draw_cast_shadow(_cx, _feet_y, _w, _dir = 1) {
    if (!combat_25d()) { ui_draw_ground_shadow(_cx, _feet_y, _w); return; }
    // Round 3 (M shot: "2.5d shadows are too big"): ~40% of the old footprint,
    // lighter, hugging the feet - a grounding accent, not a puddle.
    // Round 9 (M shots: "shadow IS NOT CLIPPED TO MODEL"): callers now pass the
    // VISIBLE model width + true visible-feet y, so the pool sits flush at the
    // feet (no upward drift) and is proportioned to the model it grounds.
    var _sx = _cx + _dir * _w * 0.08;
    var _sy = _feet_y - _w * 0.006;
    draw_set_color(c_black);
    draw_set_alpha(0.20);
    draw_ellipse(_sx - _w * 0.36, _sy - _w * 0.05, _sx + _w * 0.36, _sy + _w * 0.04, false);
    draw_set_alpha(0.09);
    draw_ellipse(_sx - _w * 0.46, _sy - _w * 0.07, _sx + _w * 0.46, _sy + _w * 0.055, false);
    draw_set_alpha(1.0);
    draw_set_color(c_white);
}

// ---------------------------------------------------------------------------
// ui_draw_25d_props(pass) - VOLUMETRIC PROPS (M 08-13, Gen1Recomp reference:
// "objects that look 3d... magnify this"). The illusion is prop-driven -
// each placeholder is drawn WITH a visible top face (the baked-3/4 read the
// reference's fence posts have). Stations ride the stage layout. pass 0 =
// behind the actors (incl. wall dressing), pass 1 = near foreground, drawn
// after the actors. Generated prop art replaces these draws 1:1 later.
// ---------------------------------------------------------------------------
function ui_draw_25d_props(_pass) {
    // 08-18 (M shot "pillars in combat from outdated background creation": "I thought we
    // removed the cheap looking polygon... they clash with the much better backgrounds"):
    // the hand-drawn polygon props are RETIRED. The painted backgrounds carry the
    // architecture now. Table + drawing kept below behind a dev lever
    // (global.combat_25d_props = true) in case a painted-prop pass wants the stations.
    if (!variable_global_exists("combat_25d_props") || !global.combat_25d_props) return;
    if (!combat_25d() || !instance_exists(obj_combat_controller)) return;
    var _cc  = instance_find(obj_combat_controller, 0);
    var _lay = variable_instance_exists(_cc, "stage_layout") ? _cc.stage_layout : 0;
    // Stations: {k, x, feet, d (depth size factor), near}. Kept clear of the
    // enemy stations, the tooltip card (x1395+ y655+), and the combat lanes.
    static _P = [
        [ {k:"pillar",x:900,feet:598,d:0.62,near:false},  {k:"sconce",x:520,feet:315,d:1,near:false},
          {k:"brazier",x:1835,feet:640,d:0.62,near:false},{k:"rubble",x:150,feet:1035,d:1.30,near:true} ],
        [ {k:"pillar",x:1800,feet:565,d:0.50,near:false}, {k:"sconce",x:1080,feet:315,d:1,near:false},
          {k:"brazier",x:850,feet:610,d:0.55,near:false}, {k:"pillar",x:90,feet:1060,d:1.35,near:true} ],
        [ {k:"pillar",x:660,feet:560,d:0.50,near:false},  {k:"sconce",x:1520,feet:315,d:1,near:false},
          {k:"rubble",x:1850,feet:620,d:0.60,near:false}, {k:"brazier",x:210,feet:1010,d:1.25,near:true} ],
        [ {k:"brazier",x:960,feet:588,d:0.55,near:false}, {k:"sconce",x:330,feet:315,d:1,near:false},
          {k:"pillar",x:1830,feet:610,d:0.60,near:false}, {k:"rubble",x:1680,feet:1050,d:1.30,near:true} ]
    ];
    // Round 7 (M shot: "looks pretty random"): the sconce / brazier / rubble
    // PLACEHOLDERS read as floating junk, not architecture - only the full
    // pillars earned their place. Everything else waits for real art.
    if (_pass == 1) return;
    var _st = _P[clamp(_lay, 0, 3)];
    for (var _i = 0; _i < array_length(_st); _i++) {
        var _p = _st[_i];
        if (_p.near != (_pass == 1)) continue;
        if (_p.k != "pillar") continue;
        var _d = _p.d;
        if (_p.k == "pillar") {
            var _pw = 88 * _d;
            var _broken = (_d >= 1.0);
            var _top = _broken ? (_p.feet - 430 * _d) : -40;   // full pillars hold the unseen ceiling
            // Shaft with cylinder shading: dark left edge, lit right band.
            draw_set_color(make_color_rgb(52, 49, 58));
            draw_rectangle(_p.x - _pw / 2, _top, _p.x + _pw / 2, _p.feet, false);
            draw_set_color(make_color_rgb(38, 36, 44));
            draw_rectangle(_p.x - _pw / 2, _top, _p.x - _pw / 2 + _pw * 0.22, _p.feet, false);
            draw_set_color(make_color_rgb(72, 68, 78));
            draw_rectangle(_p.x + _pw * 0.10, _top, _p.x + _pw * 0.30, _p.feet, false);
            // Mortar courses.
            draw_set_color(make_color_rgb(28, 26, 33));
            for (var _my2 = _p.feet - 34 * _d; _my2 > max(_top, 0); _my2 -= 76 * _d)
                draw_rectangle(_p.x - _pw / 2, _my2, _p.x + _pw / 2, _my2 + 3, false);
            if (_broken) {
                // The TOP FACE - the volume sliver that sells the 3/4 read.
                draw_set_color(make_color_rgb(88, 84, 96));
                draw_ellipse(_p.x - _pw * 0.56, _top - _pw * 0.16, _p.x + _pw * 0.56, _top + _pw * 0.12, false);
                draw_set_color(make_color_rgb(60, 57, 66));
                draw_ellipse(_p.x - _pw * 0.42, _top - _pw * 0.10, _p.x + _pw * 0.42, _top + _pw * 0.07, false);
            }
            // Base plinth + contact shadow.
            draw_set_color(make_color_rgb(46, 44, 52));
            draw_rectangle(_p.x - _pw * 0.62, _p.feet - 16 * _d, _p.x + _pw * 0.62, _p.feet, false);
            ui_draw_cast_shadow(_p.x, _p.feet + 6, _pw * 1.6, 1);
        } else if (_p.k == "brazier") {
            var _bw = 70 * _d;
            draw_set_color(make_color_rgb(44, 42, 50));
            draw_rectangle(_p.x - _bw * 0.18, _p.feet - 52 * _d, _p.x + _bw * 0.18, _p.feet, false);
            // Bowl with a RIM top face.
            draw_set_color(make_color_rgb(56, 52, 60));
            draw_ellipse(_p.x - _bw * 0.55, _p.feet - 70 * _d, _p.x + _bw * 0.55, _p.feet - 40 * _d, false);
            draw_set_color(make_color_rgb(30, 26, 30));
            draw_ellipse(_p.x - _bw * 0.42, _p.feet - 66 * _d, _p.x + _bw * 0.42, _p.feet - 50 * _d, false);
            // Fire: additive breathing glow.
            var _fp = 0.5 + 0.5 * sin(current_time / 240 + _p.x);
            gpu_set_blendmode(bm_add);
            draw_set_alpha(0.30 + 0.18 * _fp);
            draw_set_color(make_color_rgb(235, 140, 50));
            draw_circle(_p.x, _p.feet - 78 * _d, _bw * (0.45 + 0.10 * _fp), false);
            draw_set_alpha(0.75);
            draw_set_color(make_color_rgb(255, 210, 120));
            draw_circle(_p.x, _p.feet - 74 * _d, _bw * 0.16, false);
            gpu_set_blendmode(bm_normal);
            draw_set_alpha(1.0);
            ui_draw_cast_shadow(_p.x, _p.feet + 4, _bw * 1.3, 1);
        } else if (_p.k == "rubble") {
            var _rw = 60 * _d;
            // Tumbled blocks, each with a lit top edge (the volume read).
            for (var _rb = 0; _rb < 4; _rb++) {
                var _rh2 = __ui_25d_hash(_rb + 3, _lay + 11);
                var _rx2 = _p.x + (_rb - 1.5) * _rw * 0.55 + _rh2 * 14;
                var _rs2 = _rw * (0.38 + 0.30 * _rh2);
                var _ry2 = _p.feet - _rs2 * 0.7 - _rh2 * 8;
                draw_set_color(make_color_rgb(48, 45, 53));
                draw_rectangle(_rx2 - _rs2 * 0.5, _ry2, _rx2 + _rs2 * 0.5, _p.feet, false);
                draw_set_color(make_color_rgb(78, 74, 84));
                draw_rectangle(_rx2 - _rs2 * 0.5, _ry2, _rx2 + _rs2 * 0.5, _ry2 + _rs2 * 0.18, false);
            }
            ui_draw_cast_shadow(_p.x, _p.feet + 5, _rw * 2.0, 1);
        } else if (_p.k == "sconce") {
            // Wall-mounted: bracket + flame, ON the back wall (above horizon).
            draw_set_color(make_color_rgb(40, 38, 46));
            draw_rectangle(_p.x - 7, _p.feet, _p.x + 7, _p.feet + 52, false);
            draw_set_color(make_color_rgb(58, 54, 64));
            draw_rectangle(_p.x - 16, _p.feet + 46, _p.x + 16, _p.feet + 56, false);
            var _sp2 = 0.5 + 0.5 * sin(current_time / 210 + _p.x * 2);
            gpu_set_blendmode(bm_add);
            draw_set_alpha(0.26 + 0.16 * _sp2);
            draw_set_color(make_color_rgb(235, 150, 60));
            draw_circle(_p.x, _p.feet - 12, 34 + 7 * _sp2, false);
            draw_set_alpha(0.8);
            draw_set_color(make_color_rgb(255, 215, 130));
            draw_circle(_p.x, _p.feet - 8, 7, false);
            gpu_set_blendmode(bm_normal);
            draw_set_alpha(1.0);
        }
    }
    draw_set_color(c_white);
}

// ---------------------------------------------------------------------------
// __ui_25d_plane_surfaces() - PROCEDURAL TEST PLANES for the 2.5D stage
// (M 08-13 round 2: "we need 2 different backgrounds... just make simple
// cheap new ones for testing"). A slab floor and a brick wall, deterministic,
// cached on surfaces (rebuilt on device loss), tinted per dungeon at draw
// time. These exist so the two-plane ILLUSION can be judged without touching
// the shipped paintings; approved painted pairs replace them per scene.
// ---------------------------------------------------------------------------
function __ui_25d_hash(_a, _b) {
    return ((_a * 374761 + _b * 668265) mod 1024) / 1024;
}

function __ui_25d_plane_surfaces() {
    if (!variable_global_exists("ui_25d_floor_surf")) global.ui_25d_floor_surf = -1;
    if (!variable_global_exists("ui_25d_wall_surf"))  global.ui_25d_wall_surf  = -1;

    if (!surface_exists(global.ui_25d_floor_surf)) {
        global.ui_25d_floor_surf = surface_create(512, 512);
        surface_set_target(global.ui_25d_floor_surf);
        // Round 3: floor runs LIGHTER than the wall on purpose - the value gap
        // is what makes two planes read where pattern alone failed (M shot).
        draw_clear(make_color_rgb(36, 32, 38));                    // mortar
        for (var _ty = 0; _ty < 8; _ty++) {
            for (var _tx = 0; _tx < 8; _tx++) {
                var _h = __ui_25d_hash(_tx + 7, _ty + 13);
                var _g = 68 + _h * 22;
                draw_set_color(make_color_rgb(_g + 4, _g, _g + 6));
                draw_rectangle(_tx * 64 + 3, _ty * 64 + 3, _tx * 64 + 61, _ty * 64 + 61, false);
                // Worn top edge highlight + occasional crack, so slabs read as stone.
                draw_set_color(make_color_rgb(_g + 22, _g + 17, _g + 20));
                draw_rectangle(_tx * 64 + 3, _ty * 64 + 3, _tx * 64 + 61, _ty * 64 + 7, false);
                if (_h > 0.72) {
                    draw_set_color(make_color_rgb(18, 16, 20));
                    draw_line(_tx * 64 + 12 + _h * 20, _ty * 64 + 10,
                              _tx * 64 + 40, _ty * 64 + 52);
                }
            }
        }
        surface_reset_target();
    }

    if (!surface_exists(global.ui_25d_wall_surf)) {
        global.ui_25d_wall_surf = surface_create(1024, 512);
        surface_set_target(global.ui_25d_wall_surf);
        draw_clear(make_color_rgb(16, 14, 19));                    // mortar
        for (var _by = 0; _by < 8; _by++) {
            var _bo = (_by % 2 == 0) ? 0 : 64;                     // running bond
            for (var _bx = -1; _bx < 9; _bx++) {
                var _h2 = __ui_25d_hash(_bx + 31, _by + 5);
                var _g2 = 30 + _h2 * 14;
                draw_set_color(make_color_rgb(_g2 + 2, _g2, _g2 + 5));
                draw_rectangle(_bx * 128 + _bo + 3, _by * 64 + 3,
                               _bx * 128 + _bo + 125, _by * 64 + 61, false);
            }
        }
        // Ceiling gloom: the wall darkens toward the top, floorward rows stay lit.
        for (var _gy = 0; _gy < 10; _gy++) {
            draw_set_alpha(0.34 * (1 - _gy / 10));
            draw_set_color(c_black);
            draw_rectangle(0, _gy * 34, 1024, (_gy + 1) * 34, false);
        }
        draw_set_alpha(1.0);
        surface_reset_target();
    }
}

function dungeon_bg_draw(surface, scrim_alpha) {
    var _spr = dungeon_bg_sprite(surface);
    if (_spr == -1 || !sprite_exists(_spr)) return false;
    draw_set_color(c_white);
    draw_set_alpha(1.0);
    if (surface == "combat" && combat_25d()) {
        // ===== 2.5D v3 round 2 (M 08-13): TWO SEPARATE PLANES. =====
        // A dedicated WALL image stands flat above the horizon; a dedicated
        // FLOOR image lies below it, strip-projected (Mode-7 row compression)
        // so it recedes INTO the wall at the seam. Actors then stand on the
        // floor like paper cutouts. Planes are the procedural TEST stones for
        // now (M: "simple cheap new ones for testing"), tinted to the dungeon;
        // approved painted pairs slot straight into the same two draws.
        __ui_25d_plane_surfaces();
        var _hz = 470;                        // horizon: where floor meets wall
        var _vw = GUI_XR - GUI_XL;
        // Round 3 contrast: wall dims hard (cold, receding), floor stays lit -
        // the value gap between the planes IS the seam read.
        var _d25 = variable_global_exists("selected_dungeon") ? global.selected_dungeon : "ashen_vault";
        var _tw = make_color_rgb(105, 108, 138);   // wall mood tint (ashen default)
        var _tf = make_color_rgb(235, 232, 244);   // floor tint - near full value
        if (_d25 == "scorched_depths") { _tw = make_color_rgb(135, 92, 72); _tf = make_color_rgb(255, 208, 165); }
        else if (_d25 == "tundra_tomb") { _tw = make_color_rgb(92, 118, 158); _tf = make_color_rgb(215, 235, 255); }

        // PAINTED PLANE PAIRS (round 11, M-approved: "1 for each floor and for
        // each biome" + a Descent set): spr_wall25_<tag>_<fl> (authored ~4:1,
        // fills the plane above the horizon) + spr_floor25_<tag>_<fl> (square
        // tileable top-down texture for the strip projection). String lookup =
        // compiles before the art exists; each import must also be added to
        // global.__sprite_includes or the compiler strips it. Painted planes
        // draw UNTINTED (the painting carries its own palette); the procedural
        // stones + dungeon tints remain the fallback per scene.
        var _tag25 = "ashen";
        if      (_d25 == "scorched_depths") _tag25 = "scorched";
        else if (_d25 == "tundra_tomb")     _tag25 = "tundra";
        if (variable_global_exists("descent_active") && global.descent_active) _tag25 = "descent";
        var _fl25 = clamp(variable_global_exists("current_floor") ? global.current_floor : 1, 1, 3);
        var _wspr25 = asset_get_index("spr_wall25_"  + _tag25 + "_" + string(_fl25));
        var _fspr25 = asset_get_index("spr_floor25_" + _tag25 + "_" + string(_fl25));
        var _wall_painted  = (_wspr25 >= 0 && sprite_exists(_wspr25));
        var _floor_painted = (_fspr25 >= 0 && sprite_exists(_fspr25));

        // Motion parallax (round 6, Gen1Recomp homework): a slow micro-drift
        // where the wall slides LESS than the floor - the relative motion only
        // two genuinely separate planes can produce.
        var _swy = 3 * sin(current_time / 2600);

        // WALL: flat vertical plane, full width down to the horizon (drawn a
        // few px oversized so the parallax drift never exposes an edge).
        if (_wall_painted) {
            // Painted walls are authored ~400x192 and HORIZONTALLY SEAMLESS:
            // cover-scale to the horizon height and tile sideways (a raw
            // stretch to full width would fatten the art ~5x).
            var _wsc25 = _hz / max(1, sprite_get_height(_wspr25));
            var _wtw25 = max(32, sprite_get_width(_wspr25) * _wsc25);
            var _wx25  = GUI_XL - 5 + _swy * 0.35;
            while (_wx25 > GUI_XL) _wx25 -= _wtw25;
            for (; _wx25 < GUI_XR; _wx25 += _wtw25)
                draw_sprite_ext(_wspr25, 0, _wx25, 0, _wsc25, _wsc25, 0, c_white, 1.0);
        } else {
            draw_surface_stretched_ext(global.ui_25d_wall_surf, GUI_XL - 5 + _swy * 0.35, 0, _vw + 10, _hz, _tw, 1.0);
        }

        // FLOOR: strip-projected slab texture. Vertical: v = 1-(1-t)^2.2 packs
        // far rows against the seam. Horizontal (round 3 - THE missing
        // ingredient): each strip is also magnified about screen center by a
        // 1/z-flavored factor, so the slab grout lines CONVERGE toward the
        // horizon instead of running parallel - without this the "floor" read
        // as one giant wall (M screenshot 08-13). Far strips draw narrower and
        // tile sideways to keep full coverage.
        var _strips = 48;
        // Painted floor: source rows come from the sprite's own dimensions.
        var _fsw25 = _floor_painted ? sprite_get_width(_fspr25)  : 512;
        var _fsh25 = _floor_painted ? sprite_get_height(_fspr25) : 512;
        for (var _st = 0; _st < _strips; _st++) {
            var _t0 = _st / _strips;
            var _t1 = (_st + 1) / _strips;
            // Round 7 (M: "the floor curve looks bad idk why its still
            // there"): DEAD FLAT - perfectly linear row mapping, zero
            // magnification bend. Depth = line convergence + actor scale only.
            var _v0 = _t0;
            var _v1 = _t1;
            var _sy0 = _v0 * _fsh25;
            var _sy1 = _v1 * _fsh25;
            var _dy0 = _hz + _t0 * (GUI_H - _hz);
            var _dy1 = _hz + _t1 * (GUI_H - _hz);
            var _ysc = (_dy1 - _dy0) / max(1, _sy1 - _sy0);
            // Convergence: LINEAR in screen height (round 8 - THE curve M kept
            // seeing: the old hyperbolic factor bowed every vertical grout
            // line outward as it descended. Straight world lines project to
            // straight screen lines only when this factor is linear). 0.52 at
            // the horizon -> 1.0 at the near edge.
            var _cv  = lerp(0.52, 1.0, _t1);
            var _dw  = _vw * _cv;
            // Center-anchored sideways tiling covers the screen at every depth
            // (one spare tile each side so the parallax drift never gaps).
            var _fx0 = GUI_CX - _dw / 2 + _swy;
            while (_fx0 > GUI_XL - _dw * 0.5) _fx0 -= _dw;
            for (var _fx = _fx0; _fx < GUI_XR; _fx += _dw) {
                if (_floor_painted) {
                    draw_sprite_part_ext(_fspr25, 0, 0, _sy0, _fsw25, max(1, _sy1 - _sy0),
                        _fx, _dy0, _dw / _fsw25, _ysc, c_white, 1.0);
                } else {
                    draw_surface_part_ext(global.ui_25d_floor_surf, 0, _sy0, 512, max(1, _sy1 - _sy0),
                        _fx, _dy0, _dw / 512, _ysc, _tf, 1.0);
                }
            }
        }
        // Near warm light: the camp-side rows catch lamplight, pulling the
        // floor plane apart from the cold wall in VALUE, not just pattern.
        gpu_set_blendmode(bm_add);
        for (var _wl = 0; _wl < 8; _wl++) {
            draw_set_alpha(0.030 * (_wl + 1));
            draw_set_color(make_color_rgb(120, 80, 40));
            draw_rectangle(GUI_XL, GUI_H - (8 - _wl) * 52, GUI_XR, GUI_H - (7 - _wl) * 52, false);
        }
        gpu_set_blendmode(bm_normal);
        draw_set_alpha(1.0);

        // The seam: a hard baseboard line where wall meets floor, then soft
        // distance haze rising off it - the "meet at an angle" read.
        draw_set_color(c_black);
        draw_set_alpha(0.55);
        draw_rectangle(GUI_XL, _hz - 3, GUI_XR, _hz + 2, false);
        draw_set_alpha(0.28);
        draw_rectangle(GUI_XL, _hz + 2, GUI_XR, _hz + 26, false);
        draw_set_alpha(0.16);
        draw_rectangle(GUI_XL, _hz - 40, GUI_XR, _hz - 3, false);
        // Side vignette (round 6): a soft lens edge pulls the eye into the
        // diorama and hides the parallax drift at the borders.
        for (var _vg = 0; _vg < 4; _vg++) {
            draw_set_alpha(0.05);
            draw_rectangle(GUI_XL, 0, GUI_XL + 46 + _vg * 44, GUI_H, false);
            draw_rectangle(GUI_XR - 46 - _vg * 44, 0, GUI_XR, GUI_H, false);
        }
        draw_set_alpha(1.0);
        draw_set_color(c_white);
    } else if (GUI_GUTTER <= 0) {
        // 16:9 (PC/HTML5): the shipped stretch, byte-for-byte.
        draw_sprite_stretched(_spr, 0, 0, 0, GUI_W, GUI_H);
    } else {
        // Wide-aspect gutters (8b): uniform cover-scale over the full visible
        // width so phone side strips show more art instead of raw black.
        var _bg_w = sprite_get_width(_spr);
        var _bg_h = sprite_get_height(_spr);
        var _bg_s = max((GUI_XR - GUI_XL) / max(1, _bg_w), GUI_H / max(1, _bg_h));
        draw_sprite_ext(_spr, 0, GUI_CX - _bg_w * _bg_s / 2, GUI_CY - _bg_h * _bg_s / 2,
            _bg_s, _bg_s, 0, c_white, 1.0);
    }
    draw_set_color(c_black);
    draw_set_alpha(scrim_alpha);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);
    draw_set_color(c_white);
    return true;
}

// ---------------------------------------------------------------------------
// ui_draw_gothic_frame(x1, y1, x2, y2, band)
// Draws an ornate gothic 9-slice border (spr_ui_frame: carved stone + gold
// filigree) SURROUNDING the rect (x1,y1)-(x2,y2) - the band extends OUTWARD so
// the rect itself is the clear inner opening and panel content is never covered.
// The four corner ornaments draw undistorted; the edges stretch between them; the
// center (the inner opening) is skipped. Implemented manually with
// draw_sprite_part_ext so it needs no GameMaker nineSlice config.
//   band = drawn border thickness in px (default 26). Source ornate band is 42px;
//   corners scale band/42 so the filigree stays crisp at any panel size.
// For full-screen overlays pass a slightly inset rect (e.g. 30,30,1890,1050) so the
// outward band stays on-screen. Call AFTER the panel's fill + content.
// ---------------------------------------------------------------------------
function ui_draw_gothic_frame(x1, y1, x2, y2, band = 26, _alpha = 1.0) {
    var _s  = spr_ui_frame;
    if (!sprite_exists(_s)) return;
    var _sw = sprite_get_width(_s);    // 192
    var _sb = 42;                      // source border (ornate band incl. corners)
    var _pw = x2 - x1;
    var _ph = y2 - y1;
    if (_pw < 8 || _ph < 8) return;

    var _cs  = band / _sb;             // corner scale (keeps filigree crisp)
    var _se  = _sw - _sb * 2;          // source edge length (between corners) = 108
    var _exs = _pw / _se;              // top/bottom edge x-stretch (spans the opening)
    var _eys = _ph / _se;              // left/right edge y-stretch

    draw_set_color(c_white);
    draw_set_alpha(_alpha);

    // --- Corners (undistorted, scaled by _cs), sitting just OUTSIDE each corner ---
    draw_sprite_part_ext(_s, 0, 0,          0,          _sb, _sb, x1 - band, y1 - band, _cs, _cs, c_white, _alpha); // TL
    draw_sprite_part_ext(_s, 0, _sw - _sb,  0,          _sb, _sb, x2,        y1 - band, _cs, _cs, c_white, _alpha); // TR
    draw_sprite_part_ext(_s, 0, 0,          _sw - _sb,  _sb, _sb, x1 - band, y2,        _cs, _cs, c_white, _alpha); // BL
    draw_sprite_part_ext(_s, 0, _sw - _sb,  _sw - _sb,  _sb, _sb, x2,        y2,        _cs, _cs, c_white, _alpha); // BR

    // --- Edges (stretched along each side of the opening) ---
    draw_sprite_part_ext(_s, 0, _sb,        0,          _se, _sb, x1,        y1 - band, _exs, _cs, c_white, _alpha); // top
    draw_sprite_part_ext(_s, 0, _sb,        _sw - _sb,  _se, _sb, x1,        y2,        _exs, _cs, c_white, _alpha); // bottom
    draw_sprite_part_ext(_s, 0, 0,          _sb,        _sb, _se, x1 - band, y1,        _cs, _eys, c_white, _alpha); // left
    draw_sprite_part_ext(_s, 0, _sw - _sb,  _sb,        _sb, _se, x2,        y1,        _cs, _eys, c_white, _alpha); // right
    draw_set_alpha(1.0);
}

// ---------------------------------------------------------------------------
// ui_list_window_first(cursor, count, max_vis)
// Returns the index of the first row to draw for a scrolling list so that the
// cursor always stays on screen. Centers the cursor within the visible window
// (stateless), clamped to valid bounds. Draw + mouse hit-testing must both use
// this so a windowed list stays click-accurate.
// ---------------------------------------------------------------------------
function ui_list_window_first(cursor, count, max_vis) {
    if (count <= max_vis) return 0;
    return clamp(cursor - floor(max_vis / 2), 0, count - max_vis);
}

/// ui_list_window(list_id, cursor, count, max_vis)
/// Edge-triggered sibling of ui_list_window_first: keeps a persistent per-list
/// scroll offset (keyed by list_id) so the SELECTOR moves within the visible window
/// and the list only shifts once the cursor reaches the top/bottom edge - instead of
/// the centered "cursor floats, list recenters" feel. Draw + mouse hit-test for the
/// SAME list MUST pass the SAME list_id or clicks misalign. Self-corrects to 0 when a
/// list opens (cursor resets to 0 -> cursor < first -> first = 0), so stale offsets
/// from a previous visit never persist.
function ui_list_window(list_id, cursor, count, max_vis) {
    if (count <= max_vis) return 0;
    if (!variable_global_exists("list_scroll_state")) global.list_scroll_state = {};
    var _prev  = variable_struct_exists(global.list_scroll_state, list_id)
               ? variable_struct_get(global.list_scroll_state, list_id) : 0;
    var _first = clamp(_prev, 0, count - max_vis);
    if (cursor < _first)            _first = cursor;
    if (cursor >= _first + max_vis) _first = cursor - (max_vis - 1);
    _first = clamp(_first, 0, count - max_vis);
    variable_struct_set(global.list_scroll_state, list_id, _first);
    return _first;
}

// ui_draw_scroll_more(x, y, up, label)
// #9: windowed lists used the LETTERS "^" / "v" as scroll markers, which read as
// keyboard keys. Draws a real filled triangle at a text line anchored at (x, y)
// (top-left of the line, like draw_text), then the label ("19 more") beside it.
// Uses the caller's current font/color; honors the caller's halign for the whole
// arrow+label block, restoring it afterwards.
function ui_draw_scroll_more(x, y, up, label = "") {
    var _aw = 14, _ah = 12;               // arrow size, tuned to fnt_ui_small lines
    var _ty = y + 4;                       // arrow's top inside the text line box
    var _lw = (label != "") ? string_width("  " + label) : 0;
    var _bw = _aw + _lw;
    var _x0 = x;
    var _prev_ha = draw_get_halign();
    switch (_prev_ha) {
        case fa_center: _x0 = x - _bw / 2; break;
        case fa_right:  _x0 = x - _bw;     break;
    }
    if (up) draw_triangle(_x0, _ty + _ah, _x0 + _aw, _ty + _ah, _x0 + _aw / 2, _ty, false);
    else    draw_triangle(_x0, _ty,       _x0 + _aw, _ty,       _x0 + _aw / 2, _ty + _ah, false);
    if (label != "") {
        draw_set_halign(fa_left);
        draw_text(_x0 + _aw, y, "  " + label);
        draw_set_halign(_prev_ha);
    }
}

// ui_draw_sable_scroll_hint(right_x, first, last, count)
// "N more" arrow indicators for a windowed ui_maren_row list (rows based at
// y285, 72px tall). right_x = the right edge to right-align against. No-op when nothing
// is clipped above or below the visible window.
function ui_draw_sable_scroll_hint(right_x, first, last, count) {
    if (first <= 0 && last >= count) return;
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_halign(fa_right);
    draw_set_color(make_color_rgb(150, 160, 140));
    if (first > 0)    ui_draw_scroll_more(right_x, 262, true,  string(first) + " more");
    if (last < count) ui_draw_scroll_more(right_x, 952, false, string(count - last) + " more");
    draw_set_halign(fa_left);
}

// ---------------------------------------------------------------------------
// status_icon_color(name, effect_type)
// Returns a color for a given status effect name, falling back to effect_type.
// ---------------------------------------------------------------------------
function status_icon_color(sname, etype) {
    switch (sname) {
        case "Gore Strike":      return make_color_rgb(200,  50,  50);  // bleed
        case "Poison Dart":      return make_color_rgb( 80, 190,  60);  // poison
        case "Curse":            return make_color_rgb(160,  50, 210);  // curse
        case "Marrow Crush":     return make_color_rgb(210, 130,  40);  // weaken
        case "Smoke Bomb":       return make_color_rgb(150, 150, 155);  // blind
        case "Frost Shot":       return make_color_rgb( 90, 180, 230);  // frost primer (renamed 07-16)
        case "Warpath":          return make_color_rgb(200,  70,  60);  // BW ramp (07-16)
        case "Compounding Dread": return make_color_rgb(120,  80, 170); // SS trap ramp (07-16)
        case "Plague Touch":     return make_color_rgb( 90, 200, 110);  // plague
        case "Marked for Death": return make_color_rgb(220, 200,  40);  // marked
    }
    if (etype == "dot")    return make_color_rgb(210, 120,  40);
    if (etype == "debuff") return make_color_rgb(170,  70,  70);
    return make_color_rgb(120, 120, 130);
}

// ---------------------------------------------------------------------------
// status_icon_label(name, effect_type)
// Returns a 2-3 char abbreviation for a status effect.
// ---------------------------------------------------------------------------
function status_icon_label(sname, etype) {
    switch (sname) {
        case "Gore Strike":      return "BLD";
        case "Poison Dart":      return "PSN";
        case "Curse":            return "CRS";
        case "Marrow Crush":     return "WKN";
        case "Smoke Bomb":       return "SMK";
        case "Frost Shot":       return "FRS";
        case "Warpath":          return "WAR";
        case "Compounding Dread": return "DRD";
        case "Plague Touch":     return "PLG";
        case "Marked for Death": return "MFD";
    }
    if (etype == "dot")    return "DoT";
    if (etype == "debuff") return "DEB";
    return "?";
}

// ---------------------------------------------------------------------------
// status_icon_style(se) - KIND-BASED badge style for an applied status struct.
// Resolves {label, color} from the status `kind` (combat_status_kind_of) so EVERY
// status tags consistently (BLND/WKN/VUL/STUN/ROOT/SIL + PSN/BLEED/BRN/DOT),
// with the DoT sub-type chosen by name keyword. Falls back to the legacy
// name-keyed label/color for anything without a typed kind.
// ---------------------------------------------------------------------------
function status_icon_style(se) {
    var _kind = combat_status_kind_of(se);
    var _name = string_lower(variable_struct_exists(se, "name") ? se.name : "");
    // Elemental weapon-affix statuses get a distinct badge keyed off their element
    // even though they piggyback existing kinds (frost=weaken, shock=vulnerable). (§C)
    var _elem = (is_struct(se) && variable_struct_exists(se, "element")) ? se.element : "";
    switch (_elem) {
        case "burn":  return { label: "BRN",  color: make_color_rgb(225, 130,  40) };
        case "frost": return { label: "FRST", color: make_color_rgb( 90, 180, 230) };
        case "shock": return { label: "SHK",  color: make_color_rgb(230, 210,  70) };
    }
    switch (_kind) {
        case "dot":
            // ELEMENT wins over name keywords (M 08-13: Blood Leech's Seeping
            // Wound linger is element "bleed" but named "Lingering Blood" -
            // it badged as generic DoT).
            var _dot_el = combat_status_element(se);
            if (_dot_el == "bleed")  return { label: "BLEED", color: make_color_rgb(175,  35,  35) };
            if (_dot_el == "burn")   return { label: "BRN",   color: make_color_rgb(225, 130,  40) };
            if (_dot_el == "poison") return { label: "PSN",   color: make_color_rgb( 90, 200,  90) };
            if (string_pos("bleed", _name) || string_pos("gore", _name) || string_pos("rend", _name) || string_pos("hemor", _name))
                return { label: "BLEED", color: make_color_rgb(175,  35,  35) };
            if (string_pos("burn", _name) || string_pos("cinder", _name) || string_pos("scorch", _name) || string_pos("flame", _name) || string_pos("ignit", _name) || string_pos("ember", _name) || string_pos("sear", _name) || string_pos("brand", _name))
                return { label: "BRN",   color: make_color_rgb(225, 130,  40) };
            if (string_pos("poison", _name) || string_pos("venom", _name) || string_pos("plague", _name) || string_pos("toxic", _name))
                return { label: "PSN",   color: make_color_rgb( 90, 200,  90) };
            return { label: "DOT",   color: make_color_rgb(210, 160,  60) };
        case "vulnerable": return { label: "VUL",  color: make_color_rgb(185,  65, 120) };
        case "hexed":      return { label: "HEX",  color: make_color_rgb(150,  70, 200) };
        // Scorch's mark: every hit on the target deals bonus TRUE FIRE damage. Distinct
        // from VUL (which is typeless) - its own badge so the player reads it as fire.
        case "firemark":   return { label: "Fire+", color: make_color_rgb(225, 130,  40) };
        case "weaken":     return { label: "WKN",  color: make_color_rgb(175, 110,  55) };
        case "blind":      return { label: "BLND", color: make_color_rgb(120, 125, 145) };
        case "mortality":  return { label: "MORT", color: make_color_rgb(120, 175,  90) };
        case "stun":       return { label: "STUN", color: make_color_rgb(228, 200,  60) };
        case "root":       return { label: "ROOT", color: make_color_rgb( 55, 160, 150) };
        case "silence":    return { label: "SIL",  color: make_color_rgb(125,  90, 205) };
        case "regen":      return { label: "HEAL", color: make_color_rgb( 90, 200, 120) };
        case "soulbind":   return { label: "BOND", color: make_color_rgb(170, 110, 215) };
        // Enemy self-buff chips (dormant-mechanics batch, 08-27).
        case "fortify":     return { label: "FORT",  color: make_color_rgb(150, 155, 175) };
        case "phased":      return { label: "PHASE", color: make_color_rgb(140, 150, 220) };
        case "retribution": return { label: "RET+",  color: make_color_rgb(190, 140,  70) };
    }
    return {
        label: status_icon_label(se.name, se.effect_type),
        color: status_icon_color(se.name, se.effect_type)
    };
}

// ---------------------------------------------------------------------------
// status_icons_from(status_effects) - build a [{label,color,duration}] icon list
// from a combatant's status_effects[], using kind-based styling. Shared by both
// the enemy bar row and the player buff row.
// ---------------------------------------------------------------------------
function status_icons_from(status_effects) {
    var _icons = [];
    for (var _i = 0; _i < array_length(status_effects); _i++) {
        var _se = status_effects[_i];
        var _st = status_icon_style(_se);
        array_push(_icons, {
            label:    _st.label,
            color:    _st.color,
            duration: variable_struct_exists(_se, "duration") ? _se.duration : 0,
            se:       _se   // source status, so the icon row can hover-explain it
        });
    }
    return _icons;
}

// ---------------------------------------------------------------------------
// status_fx_sprite_for(se) - maps a status to its looping VFX sprite (or -1).
// kind/name -> spr_fx_*; resolved by string so a missing sprite simply no-ops
// (the sprites are registered in global.__sprite_includes to survive the build).
// ---------------------------------------------------------------------------
function status_fx_sprite_for(se) {
    var _kind = combat_status_kind_of(se);
    var _name = string_lower(variable_struct_exists(se, "name") ? se.name : "");
    // Elemental affix statuses use a dedicated aura; until dedicated frost/shock art
    // exists, fall back to the weaken/stun auras so there's still a visible mark. (§C)
    var _elem = (is_struct(se) && variable_struct_exists(se, "element")) ? se.element : "";
    if (_elem == "burn")  return asset_get_index("spr_fx_burn");
    if (_elem == "frost") { var _ff = asset_get_index("spr_fx_frost"); return (_ff >= 0) ? _ff : asset_get_index("spr_fx_root_ice"); }   // 08-18: ice block, not the drip cloud
    if (_elem == "shock") { var _fs = asset_get_index("spr_fx_shock"); return (_fs >= 0) ? _fs : asset_get_index("spr_fx_stun"); }
    var _key  = "";
    switch (_kind) {
        case "dot":
            // ELEMENT wins over name keywords, mirroring status_icon_style
            // (M 08-13: "Lingering Blood" is element "bleed" but its name has
            // no bleed keyword - it drew the POISON gas).
            var _dot_fel = combat_status_element(se);
            if (_dot_fel == "bleed" || string_pos("bleed", _name) || string_pos("gore", _name) || string_pos("rend", _name) || string_pos("hemor", _name) || string_pos("blood", _name))
                _key = "spr_fx_bleed";
            else if (_dot_fel == "burn" || string_pos("burn", _name) || string_pos("cinder", _name) || string_pos("scorch", _name) || string_pos("flame", _name) || string_pos("ignit", _name) || string_pos("ember", _name))
                _key = "spr_fx_burn";
            else
                _key = "spr_fx_poison";   // poison / plague / venom / generic dot
            break;
        // 08-18 (M: "weaken is just a recolored blind... a flat ceiling plane... doesn't
        // convey its effect"): the drip-cloud family is retired. Weaken / Vulnerable /
        // Mortality wear the Gigapack down-arrow debuff loop in their badge colour,
        // Blind a dark converging mote-swirl, Root an ice-lock at the FEET.
        case "blind":      _key = "spr_fx_blind_dark";    break;
        case "stun":       _key = "spr_fx_stun";          break;
        case "weaken":     _key = "spr_fx_debuff_violet"; break;
        case "vulnerable": _key = "spr_fx_debuff_red";    break;
        case "mortality":  _key = "spr_fx_debuff_green";  break;
        case "root":       _key = "spr_fx_root_ice";      break;
        case "silence":    _key = "spr_fx_debuff_violet"; break;   // (silence draws its own "..." bubble; this is the fallback only)
    }
    if (_key == "") return -1;
    return asset_get_index(_key);
}

// item_slot_label(slot) - display name for an equipment slot key (weapon->"Weapon").
function item_slot_label(slot) {
    switch (slot) {
        case "weapon":        return "Melee Weapon";
        case "ranged_weapon": return "Ranged Weapon";
        case "offhand": return "Offhand";
        case "helm":    return "Helm";
        case "chest":   return "Chest";
        case "gloves":  return "Gloves";
        case "boots":   return "Boots";
        case "amulet":  return "Amulet";
        case "ring":    return "Ring";
    }
    return (is_string(slot) && slot != "") ? string_upper(string_copy(slot, 1, 1)) + string_copy(slot, 2, string_length(slot) - 1) : "Gear";
}

// status_effect_plain_text(se) - short plain-language description of what a status
// does, for the menu "Boons & Effects" panel (e.g. "12 dmg/turn", "-accuracy").
function status_effect_plain_text(se) {
    var _kind = combat_status_kind_of(se);
    var _val  = variable_struct_exists(se, "effect_value") ? se.effect_value : 0;
    var _elem = (is_struct(se) && variable_struct_exists(se, "element")) ? se.element : "";
    if (_elem == "burn")  return string(_val) + " fire dmg/turn";
    if (_elem == "frost") return "chilled (-" + string(round(_val * 100)) + "% enemy dmg)";
    if (_elem == "shock") return "shocked (+" + string(_val) + " dmg taken)";
    switch (_kind) {
        case "dot":        return string(_val) + " dmg/turn";
        case "blind":      return "reduced accuracy";
        case "weaken":     return "weaker attacks";
        case "vulnerable": return "+" + string(_val) + " dmg taken";
        case "firemark":   return "+" + string(_val) + " fire dmg/hit";
        case "mortality":  return "reduced healing";
        case "stun":       return "cannot act";
        case "root":       return "cannot melee";
        case "silence":    return "cannot cast";
        case "regen":      return "+" + string(_val) + " HP/turn";
        case "soulbind":   return "shares " + string(round(_val * 100)) + "% of your pain";
        case "fortify":     return "takes only " + string(round(_val * 100)) + "% damage";
        case "phased":      return "untargetable - blows pass through";
        case "retribution": return "+" + string(_val) + " armor (repeated damage type)";
    }
    return (se.effect_type == "dot") ? (string(_val) + " dmg/turn") : "debuff";
}

// combatant_has_status_kind(c, kind) - true if any active status on c is of `kind`.
function combatant_has_status_kind(c, kind) {
    if (!variable_struct_exists(c, "status_effects")) return false;
    for (var _i = 0; _i < array_length(c.status_effects); _i++)
        if (combat_status_kind_of(c.status_effects[_i]) == kind) return true;
    return false;
}

// status_fx_anchor(se) - where on the combatant the fx sits: 0 head, 1 center, 2 feet.
function status_fx_anchor(se) {
    var _kind = combat_status_kind_of(se);
    if (_kind == "blind" || _kind == "stun") return 0;   // around the head
    // Root binds the FEET (08-11, M: root read like blind - both hovered at
    // the body/head). Grounding it is the tell; no root-specific art exists in
    // any owned pack yet, so placement carries the meaning.
    if (_kind == "root") return 2;
    var _name = string_lower(variable_struct_exists(se, "name") ? se.name : "");
    if (_kind == "dot" && (string_pos("burn", _name) || string_pos("cinder", _name) || string_pos("scorch", _name) || string_pos("flame", _name) || string_pos("ignit", _name) || string_pos("ember", _name)))
        return 2;   // flames at the feet
    return 1;       // gas / blood / aura at the body
}

// ---------------------------------------------------------------------------
// ui_draw_status_fx(cx, top_y, draw_h, status_effects) - overlay looping VFX
// sprites for every active status on a combatant, drawn over the sprite.
// cx = horizontal centre; top_y = sprite top; draw_h = displayed sprite height.
// De-dupes by sprite so stacked statuses of one type show a single effect.
// ---------------------------------------------------------------------------
function ui_draw_status_fx(cx, top_y, draw_h, status_effects) {
    var _drawn = [];
    var _fx_h  = draw_h * 0.55;
    // The poison cloud art fills its whole 64x64 canvas edge-to-edge, so at the
    // shared 55%-of-height size it blanketed the enemy. Shrink + fade ONLY the
    // poison sprite so the model stays visible; all other FX keep their size.
    var _spr_poison = asset_get_index("spr_fx_poison");
    draw_set_alpha(0.85);
    for (var _i = 0; _i < array_length(status_effects); _i++) {
        var _se  = status_effects[_i];
        var _kind_fx = combat_status_kind_of(_se);

        // SILENCE (M 08-13: "in most rpgs theres a '...' that hovers like a
        // text bubble - we should mimic that"): a classic speech bubble with
        // three pulsing dots by the head, code-drawn - no more borrowing the
        // weaken tendril cloud.
        if (_kind_fx == "silence") {
            var _dup_s = false;
            for (var _d = 0; _d < array_length(_drawn); _d++) if (_drawn[_d] == "#silence") { _dup_s = true; break; }
            if (_dup_s) continue;
            array_push(_drawn, "#silence");
            var _bob = 3 * sin(current_time / 260);
            var _bw  = max(46, draw_h * 0.22);
            var _bh  = _bw * 0.52;
            var _bx  = cx + draw_h * 0.24;
            var _by  = top_y - _bh * 0.30 + _bob;
            draw_set_alpha(0.88);
            draw_set_color(make_color_rgb(26, 26, 38));
            draw_roundrect_ext(_bx, _by, _bx + _bw, _by + _bh, 10, 10, false);
            // tail toward the head
            draw_triangle(_bx + _bw * 0.20, _by + _bh - 1, _bx + _bw * 0.42, _by + _bh - 1,
                          _bx + _bw * 0.12, _by + _bh + 9, false);
            draw_set_color(make_color_rgb(125, 90, 205));   // SIL badge purple
            draw_roundrect_ext(_bx, _by, _bx + _bw, _by + _bh, 10, 10, true);
            // three dots, slow "..." typing pulse
            var _don = 1 + ((current_time div 420) mod 3);
            for (var _dt = 0; _dt < 3; _dt++) {
                draw_set_alpha((_dt < _don) ? 0.95 : 0.30);
                draw_set_color(make_color_rgb(214, 204, 240));
                draw_circle(_bx + _bw * (0.26 + 0.24 * _dt), _by + _bh * 0.52, max(2.5, _bw * 0.055), false);
            }
            draw_set_alpha(0.85);   // restore the loop's baseline alpha
            continue;
        }

        // ROOT (M 08-18: "the ice cube only makes sense for an ice root - a Bear Trap
        // should be the trap closed around the base of the model"): a root set by a
        // TRAP draws that trap's own prop, clamped shut at the feet (name == the trap,
        // as the trigger block stamps it); frost roots keep the ice-lock; every other
        // root (Gravewrack Grip, the void roll, enemy holds) wears the lock tinted
        // stone-grey so it never reads as ice.
        var _root_trap = -1, _root_tint = c_white;
        if (_kind_fx == "root") {
            var _rt_nm = string_lower(variable_struct_exists(_se, "name") ? _se.name : "");
            var _rt_el = combat_status_element(_se);
            if (string_pos("trap", _rt_nm) > 0 || string_pos("snare", _rt_nm) > 0 || string_pos("tripline", _rt_nm) > 0 || string_pos("caltrop", _rt_nm) > 0) {
                _root_trap = trap_prop_sprite(variable_struct_exists(_se, "name") ? _se.name : "");
                if (_root_trap < 0) _root_trap = asset_get_index("spr_trap_prop_bear");
            } else if (!(_rt_el == "frost" || string_pos("frost", _rt_nm) > 0 || string_pos("ice", _rt_nm) > 0 || string_pos("rime", _rt_nm) > 0
                         || string_pos("hoar", _rt_nm) > 0 || string_pos("glacial", _rt_nm) > 0 || string_pos("freez", _rt_nm) > 0 || string_pos("chill", _rt_nm) > 0)) {
                _root_tint = make_color_rgb(150, 150, 140);   // stone grip, not ice
            }
        }
        if (_root_trap >= 0) {
            var _dup_t = false;
            for (var _d = 0; _d < array_length(_drawn); _d++) if (_drawn[_d] == "#roottrap") { _dup_t = true; break; }
            if (_dup_t) continue;
            array_push(_drawn, "#roottrap");
            // Prop art is 64px side-on; clamp it across the feet at ~half the body width,
            // a tiny bite-pulse so it reads as SHUT on the leg, not lying on the floor.
            var _tp_sc = max(1.0, (draw_h * 0.42) / 64) * (1 + 0.03 * sin(current_time / 140));
            var _tp_w  = sprite_get_width(_root_trap) * _tp_sc, _tp_h = sprite_get_height(_root_trap) * _tp_sc;
            // Squashed a little (0.72) so the jaws read as CLAMPED round the leg rather than lying open.
            draw_sprite_ext(_root_trap, 0, cx - _tp_w * 0.5, top_y + draw_h - _tp_h * 0.72 * 0.95, _tp_sc, _tp_sc * 0.72, 0, c_white, 0.95);
            continue;
        }

        var _spr = status_fx_sprite_for(_se);
        if (_spr == -1) continue;
        // Shock (no dedicated art yet) reuses the stun sprite but with its OWN
        // treatment - electric-yellow tint, body anchor, fast flicker-jitter - so
        // it never reads as a second stun (M 07-08). De-dupe keyed separately so
        // a stunned AND shocked enemy shows both effects.
        var _is_shock = (combat_status_element(_se) == "shock");
        // String key: sprite refs are typed handles in LTS2026 - arithmetic on
        // them (ref + real) throws "DoAdd: Malformed variable" at runtime.
        // The four debuff kinds that share the weaken aura art each get their
        // badge color as a heavy tint + their OWN dedupe key (M 08-13: "weaken
        // should have its own... if multiple are on the same target their vfx
        // just stack") - so WKN / VUL / MORT / ROOT read as different marks,
        // same-kind repeats still collapse to one draw.
        var _fx_tint = _root_tint;   // 08-18: the new debuff loops are pre-coloured (no heavy tint); non-ice roots tint the lock grey
        var _dd_kind = "";
        switch (_kind_fx) {
            case "weaken":     _dd_kind = "#wkn";  break;
            case "vulnerable": _dd_kind = "#vul";  break;
            case "mortality":  _dd_kind = "#mort"; break;
            case "root":       _dd_kind = "#root"; break;
        }
        var _dd_key   = string(_spr) + (_is_shock ? "#shock" : _dd_kind);
        var _dup = false;
        for (var _d = 0; _d < array_length(_drawn); _d++) if (_drawn[_d] == _dd_key) { _dup = true; break; }
        if (_dup) continue;
        array_push(_drawn, _dd_key);

        if (_is_shock) {
            var _sc_sh = (_fx_h / max(1, sprite_get_height(_spr))) * 0.8;
            var _jit   = ((current_time div 55) mod 2 == 0) ? 5 : -5;
            var _fn_sh = max(1, sprite_get_number(_spr));
            var _fr_sh = (current_time div 55) mod _fn_sh;   // twice the normal frame rate
            var _ay_sh = top_y + draw_h * 0.5;
            draw_sprite_ext(_spr, _fr_sh,
                cx + _jit - (sprite_get_width(_spr)  * _sc_sh) * 0.5,
                _ay_sh   - (sprite_get_height(_spr) * _sc_sh) * 0.5,
                _sc_sh, _sc_sh, 0, make_color_rgb(255, 235, 90), 0.9);
            continue;
        }

        var _is_poison = (_spr == _spr_poison);
        var _size_mult = _is_poison ? 0.62 : 1.0;   // ~34% of enemy height instead of 55%
        var _fx_alpha  = _is_poison ? 0.65 : 1.0;   // let the enemy show through the gas

        var _anchor = status_fx_anchor(_se);
        var _ay = top_y + draw_h * (_anchor == 0 ? 0.16 : (_anchor == 2 ? 0.82 : 0.50));
        var _sc = (_fx_h / max(1, sprite_get_height(_spr))) * _size_mult;
        var _fn = max(1, sprite_get_number(_spr));
        var _fr = (current_time div 110) mod _fn;
        draw_sprite_ext(_spr, _fr,
            cx - (sprite_get_width(_spr)  * _sc) * 0.5,
            _ay - (sprite_get_height(_spr) * _sc) * 0.5,
            _sc, _sc, 0, _fx_tint, _fx_alpha);
    }
    draw_set_alpha(1.0);
}

// ---------------------------------------------------------------------------
// ui_draw_status_icon_row(x, y, icon_list)
// Draws a horizontal row of small badge-style status icons.
// icon_list: array of { label, color, duration }
// Each badge is 48x24px with a 5px gap; duration shown in small text below.
// (48 wide fits 4-char labels like "STUN"/"WEAK" at fnt_ui_small without clipping.)
// ---------------------------------------------------------------------------
function ui_draw_status_icon_row(x, y, icon_list, max_w = -1) {
    var _iw  = 48;
    var _ih  = 24;
    var _gap = 5;
    var _ix  = x;
    // Width cap: when max_w is given and the badges won't fit, draw as many as fit
    // (reserving one slot) then a "+N" overflow badge - the row never runs into
    // neighbouring UI. Hover the enemy's bar to inspect everything it carries.
    var _n_draw = array_length(icon_list);
    var _n_over = 0;
    if (max_w > 0) {
        var _fit = max(1, floor((max_w + _gap) / (_iw + _gap)));
        if (_n_draw > _fit) { _n_draw = _fit - 1; _n_over = array_length(icon_list) - _n_draw; }
    }
    // Mouse (GUI space) for hover-to-explain. The hovered status is stashed in a
    // global and drawn as a tooltip later by obj_combat_controller (so it lands on
    // top of every bar/row). Reset each combat Draw frame.
    var _mx = device_mouse_x_to_gui(0);
    var _my = device_mouse_y_to_gui(0);
    draw_set_halign(fa_center);
    draw_set_valign(fa_middle);
    draw_set_font(ui_font(fnt_ui_small));
    for (var _i = 0; _i < _n_draw; _i++) {
        var _ic = icon_list[_i];
        // Hover hit-test (badge rect). Only entries carrying their source `se` are
        // explainable (ad-hoc buff badges without one are skipped).
        if (variable_struct_exists(_ic, "se")
            && _mx >= _ix && _mx < _ix + _iw && _my >= y && _my < y + _ih) {
            global.combat_status_tip = { se: _ic.se, x: _mx, y: _my };
        }
        // filled badge
        draw_set_alpha(0.88);
        draw_set_color(_ic.color);
        draw_roundrect(_ix, y, _ix + _iw, y + _ih, false);
        // subtle dark border
        draw_set_alpha(1.0);
        draw_set_color(make_color_rgb(10, 10, 18));
        draw_roundrect(_ix, y, _ix + _iw, y + _ih, true);
        // Detonation pulse (07-16 combo legibility): the badge the selected
        // ability would react with breathes a bright ring - cast now to pop it.
        if (variable_struct_exists(_ic, "pulse") && _ic.pulse) {
            draw_set_alpha(0.5 + 0.5 * sin(current_time / 140));
            draw_set_color(c_white);
            draw_roundrect(_ix - 2, y - 2, _ix + _iw + 2, y + _ih + 2, true);
            draw_roundrect(_ix - 1, y - 1, _ix + _iw + 1, y + _ih + 1, true);
            draw_set_alpha(1.0);
        }
        // Label + turns-left as ONE string INSIDE the badge (M 08-13 screenshot:
        // even the 08-11 "tucked tight" counter still kissed the next bar row's
        // intent chip in the stacked grid - nothing may hang below the badge).
        // Shrinks to fit so "BLEED 4" never spills the 48px chip.
        draw_set_color(c_white);
        var _bd_txt = _ic.label + ((_ic.duration > 0) ? " " + string(_ic.duration) : "");
        var _bd_tw  = string_width(_bd_txt);
        if (_bd_tw > _iw - 6) {
            var _bd_sc = (_iw - 6) / _bd_tw;
            draw_text_transformed(_ix + _iw * 0.5, y + _ih * 0.5, _bd_txt, _bd_sc, _bd_sc, 0);
        } else {
            draw_text(_ix + _iw * 0.5, y + _ih * 0.5, _bd_txt);
        }
        _ix += _iw + _gap;
    }
    if (_n_over > 0) {
        draw_set_alpha(0.88);
        draw_set_color(make_color_rgb(70, 75, 95));
        draw_roundrect(_ix, y, _ix + _iw, y + _ih, false);
        draw_set_alpha(1.0);
        draw_set_color(make_color_rgb(10, 10, 18));
        draw_roundrect(_ix, y, _ix + _iw, y + _ih, true);
        draw_set_color(c_white);
        draw_text(_ix + _iw * 0.5, y + _ih * 0.5, "+" + string(_n_over));
    }
    draw_set_font(-1);
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
}

// ---------------------------------------------------------------------------
// status_detonation_text(se) - the detonation reaction a status enables when a
// detonator (Snipe/Assassinate/Arcane Burst/Soul Nova) hits a foe carrying it.
// "" if the status doesn't detonate. Mirrors combat_detonator_pick + the reaction
// values in obj_combat_controller/Step_0. See SYSTEMS_VIABILITY_PASS.md.
// ---------------------------------------------------------------------------
function status_detonation_text(se) {
    if (!is_struct(se)) return "";
    var _k  = variable_struct_exists(se, "kind") ? se.kind
            : (variable_struct_exists(se, "effect_type") ? se.effect_type : "");
    var _el = combat_status_element(se);   // explicit element, else inferred (dot -> bleed)
    if (_k == "stun")                            return "Detonate: guaranteed critical hit.";
    if (_el == "frost")                          return "Detonate: +30% damage (shatters).";
    if (_k == "root")                            return "Detonate: +30% damage (shatters).";
    if (_el == "burn")                           return "Detonate: +40% crit chance.";
    if (_el == "shock")                          return "Detonate: arcs ~33% damage to other foes (or +25% crit if alone).";
    if (_k == "vulnerable" || _k == "firemark")  return "Detonate: +12 bonus damage.";
    if (_k == "dot" && _el == "bleed")           return "Detonate: bursts all remaining bleed ticks (+5 each).";
    if (_k == "dot" && _el == "poison")          return "Detonate: applies Mortality (-40% healing, 4 turns).";
    if (_k == "dot" && _el == "void")            return "Detonate: heals you 30% of the damage dealt.";
    if (_k == "weaken")                          return "Detonate: +15% damage.";
    if (_k == "blind")                           return "Detonate: cannot miss.";
    return "";
}

// status_tooltip_desc(se) - one-line "what it does" explanation incl. turns left.
function status_tooltip_desc(se) {
    var _k    = (variable_struct_exists(se, "kind") && se.kind != "") ? se.kind
              : (variable_struct_exists(se, "effect_type") ? se.effect_type : "");
    var _val  = variable_struct_exists(se, "effect_value") ? se.effect_value : 0;
    var _dur  = variable_struct_exists(se, "duration") ? se.duration : 0;

    // A badge can carry its own one-line explanation (player buffs like Iron Skin /
    // Blink / Vanish that live on the player struct, not in status_effects[], so they
    // have no typed `kind` to switch on). It supplies its own duration noun too
    // ("charge" for stacked-evasion buffs, "turn" otherwise).
    if (variable_struct_exists(se, "desc")) {
        var _noun = variable_struct_exists(se, "dur_noun") ? se.dur_noun : "turn";
        var _suf  = (_dur > 0) ? ("  (" + string(_dur) + " " + _noun + (_dur == 1 ? "" : "s") + " left)") : "";
        return se.desc + _suf;
    }

    var _el   = combat_status_element(se);
    var _rawel = (is_struct(se) && variable_struct_exists(se, "element")) ? se.element : "";
    var _turns = (_dur > 0) ? ("  (" + string(_dur) + " turn" + (_dur == 1 ? "" : "s") + " left)") : "";
    var _base;
    if (_rawel == "frost")      _base = "Chilled: deals " + string(round(_val * 100)) + "% less damage.";
    else if (_rawel == "shock") _base = "Shocked: takes +" + string(_val) + " damage per hit.";
    else switch (_k) {
        case "dot":        _base = "Damage over time: " + string(_val) + " " + (_el != "" ? _el + " " : "") + "damage each turn."; break;
        case "vulnerable": _base = "Exposed: takes +" + string(_val) + " damage per hit."; break;
        case "hexed":      _base = "Hexed: takes +" + string(_val) + " damage per hit; detonations on it are DOUBLED and spread +2 damage-taken to all other enemies."; break;
        case "firemark":   _base = "Seared: every hit on it deals +" + string(_val) + " fire damage."; break;
        case "weaken":     _base = "Weakened: deals " + string(round(_val * 100)) + "% less damage."; break;
        case "blind":      _base = "Blinded: -" + string(round(_val * 100)) + "% accuracy."; break;
        case "mortality":  _base = "Mortality: healing received reduced " + string(round(_val * 100)) + "%."; break;
        case "stun":       _base = "Stunned: cannot act."; break;
        case "root":       _base = "Rooted: melee can't reach (ranged still acts)."; break;
        case "silence":    _base = "Silenced: cannot cast spells."; break;
        case "marked":     _base = "Marked for Death: once below half HP, takes +30% damage from ALL sources."; break;
        case "regen":      _base = "Regenerating: restores " + string(_val) + " HP each turn."; break;
        case "soulbind":   _base = "Soulbound: suffers " + string(round(_val * 100)) + "% of the damage you take, healing you the same. Lasts the whole combat."; break;
        case "overwhelm":  _base = "Overwhelmed: carrying 2 or more DIFFERENT status effects - takes +15% damage from ALL sources while they last."; break;
        default:           _base = "Active effect."; break;
    }
    return _base + _turns;
}

// ---------------------------------------------------------------------------
// ui_draw_status_tooltip(mx, my, se) - floating popup explaining a status icon:
// name, what it does (+turns left), and its detonation reaction (if any). Mirrors
// the gear tooltip; self-sizes and clamps to the screen.
// ---------------------------------------------------------------------------
function ui_draw_status_tooltip(mx, my, se) {
    if (!is_struct(se)) return;
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));

    var _pad = 15, _lh = 27, _w = 540, _iw = _w - _pad * 2;
    // Header/accent colour: a badge may supply its own (player buffs); otherwise derive
    // it from the typed status style. (Avoids running status_icon_style on a buff struct
    // that has no `kind`/`effect_type`.)
    var _st_color = variable_struct_exists(se, "color") ? se.color : status_icon_style(se).color;
    var _name = variable_struct_exists(se, "name") ? se.name : "Status";
    var _desc = status_tooltip_desc(se);
    var _det  = status_detonation_text(se);

    // Height = pad + name line + desc(wrapped) + optional detonation(wrapped) + pad.
    var _h = _pad * 2 + _lh;
    _h += string_height_ext(_desc, _lh, _iw);
    if (_det != "") _h += string_height_ext(_det, _lh, _iw) + 6;

    var _x = mx + 24, _y = my + 18;
    if (_x + _w > 1905) _x = mx - _w - 18;
    if (_x < 6) _x = 6;
    if (_y + _h > 1065) _y = 1065 - _h;
    if (_y < 6) _y = 6;

    draw_set_alpha(0.96);
    draw_set_color(make_color_rgb(12, 14, 26));
    draw_rectangle(_x, _y, _x + _w, _y + _h, false);
    draw_set_alpha(1.0);
    draw_set_color(_st_color);
    draw_rectangle(_x, _y, _x + _w, _y + _h, true);
    draw_rectangle(_x, _y, _x + _w, _y + 4, false);   // accent strip

    var _cx = _x + _pad, _cy = _y + _pad;
    draw_set_color(_st_color);
    draw_text(_cx, _cy, _name);
    _cy += _lh;
    draw_set_color(make_color_rgb(215, 218, 230));
    draw_text_ext(_cx, _cy, _desc, _lh, _iw);
    _cy += string_height_ext(_desc, _lh, _iw);
    if (_det != "") {
        _cy += 6;
        draw_set_color(make_color_rgb(255, 205, 90));
        draw_text_ext(_cx, _cy, _det, _lh, _iw);
    }

    draw_set_font(-1);
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_alpha(1.0);
}

// ---------------------------------------------------------------------------
// ui_draw_enemy_inspect_tooltip(mx, my, enemy)
// Hover panel for an enemy: its attack class (reach x kind) and which control
// effects stop it - Root halts melee, Silence stops spells, Stun stops anything.
// Lets the player tell BEFORE acting whether e.g. a Bear Trap (root) will work on
// a foe (ranged foes ignore root by design). See SYSTEMS_ATTACK_CLASS.md.
// ---------------------------------------------------------------------------
function ui_draw_enemy_inspect_tooltip(mx, my, enemy) {
    if (!is_struct(enemy)) return;
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));

    var _reach     = variable_struct_exists(enemy, "reach") ? enemy.reach : "melee";
    var _kind      = variable_struct_exists(enemy, "kind")  ? enemy.kind  : "attack";
    var _is_ranged = (_reach == "ranged");
    var _is_spell  = (_kind  == "spell");
    var _name      = variable_struct_exists(enemy, "name") ? enemy.name : "Enemy";
    var _class_str = (_is_ranged ? "Ranged" : "Melee") + " - " + (_is_spell ? "Spell" : "Phys");

    var _green = make_color_rgb(120, 210, 120);
    var _muted = make_color_rgb(180, 110, 110);
    var _root_ok = !_is_ranged;
    var _sil_ok  = _is_spell;
    var _lines = [
        { t: "Root:     " + (_root_ok ? "stops it" : "no effect (ranged)"), c: _root_ok ? _green : _muted },
        { t: "Silence:  " + (_sil_ok  ? "stops it" : "no effect (phys)"),   c: _sil_ok  ? _green : _muted },
        { t: "Stun:     stops it",                                          c: _green },
    ];
    // School weakness spelled out (M 08-14 fix-now #8: the diamond alone never
    // said what it MEANS) - mirrors the +30%/+1 AP hook in the combat Step.
    var _wk_i = enemy_weak_school(_name);
    if (_wk_i != "") {
        array_push(_lines, { t: "Weak to " + school_label(_wk_i) + ": +30% damage, 1st hit +1 AP",
                             c: school_color(_wk_i) });
    }
    // Family immunities (M-locked 08-17) - what this foe SHRUGS entirely.
    var _imt = enemy_immunity_text(_name);
    if (_imt != "") array_push(_lines, { t: "IMMUNE to " + _imt, c: make_color_rgb(200, 200, 215) });

    var _pad = 15, _lh = 27, _w = 420;
    var _h = _pad * 2 + _lh * (2 + array_length(_lines)) + 6;   // name + class + control lines + gap

    var _x = mx + 24, _y = my + 18;
    if (_x + _w > 1905) _x = mx - _w - 18;   // enemies sit top-right; flip left near the edge
    if (_x < 6) _x = 6;
    if (_y + _h > 1065) _y = 1065 - _h;
    if (_y < 6) _y = 6;

    var _accent = _is_ranged ? make_color_rgb(220, 170, 80) : make_color_rgb(150, 165, 195);
    draw_set_alpha(0.96);
    draw_set_color(make_color_rgb(12, 14, 26));
    draw_rectangle(_x, _y, _x + _w, _y + _h, false);
    draw_set_alpha(1.0);
    draw_set_color(_accent);
    draw_rectangle(_x, _y, _x + _w, _y + _h, true);
    draw_rectangle(_x, _y, _x + _w, _y + 4, false);   // accent strip

    var _cx = _x + _pad, _cy = _y + _pad;
    draw_set_color(c_white);
    draw_text(_cx, _cy, _name);
    _cy += _lh;
    draw_set_color(_accent);
    draw_text(_cx, _cy, _class_str);
    _cy += _lh + 6;
    for (var _li = 0; _li < array_length(_lines); _li++) {
        draw_set_color(_lines[_li].c);
        draw_text(_cx, _cy, _lines[_li].t);
        _cy += _lh;
    }

    draw_set_font(-1);
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_alpha(1.0);
}

// ---------------------------------------------------------------------------
// ui_draw_enemy_status_icons(x, y, status_effects)
// Draws status icons for an enemy's status_effects array inline with their bar.
// Placed to the right of the HP bar; call from Draw_64 during the bar loop.
// ---------------------------------------------------------------------------
function ui_draw_enemy_status_icons(x, y, status_effects, react_se = undefined, overwhelmed = false) {
    if (array_length(status_effects) == 0) exit;
    // Capped to the HP-bar width so a heavily-debuffed enemy's row can't collide
    // with the neighbouring column or the row below (overflow shows as "+N").
    var _icons = status_icons_from(status_effects);
    // Combo legibility (07-16): the status the SELECTED ability would detonate
    // pulses (react_se = that status struct, matched by reference).
    if (react_se != undefined) {
        for (var _gi = 0; _gi < array_length(_icons); _gi++) {
            if (_icons[_gi].se == react_se) { _icons[_gi].pulse = true; break; }
        }
    }
    // OVERWHELM (07-16 core rule): 2+ distinct statuses -> +15% damage taken.
    // Shown as a gold badge at the row's end; hover explains it like any status.
    if (overwhelmed) {
        array_push(_icons, {
            label: "OVW+", color: make_color_rgb(255, 200, 70), duration: 0,
            se: { name: "Overwhelmed", kind: "overwhelm", effect_value: 0.15, duration: 0,
                  color: make_color_rgb(255, 200, 70) }
        });
    }
    ui_draw_status_icon_row(x, y, _icons, 400);
}

// ---------------------------------------------------------------------------
// ui_draw_item_tooltip(ttx, tty, item, compared_item)
// Draws a floating tooltip near the cursor for an equipment or consumable item.
// compared_item: currently equipped item in that slot (for Replaces line), or undefined.
// ---------------------------------------------------------------------------
function ui_draw_item_tooltip(ttx, tty, item, compared_item) {
    var _pad  = 15;
    var _lh   = 27;
    // Width is now adaptive: it grows to fit the longest line up to a cap, and any
    // line wider than the inner text area wraps to multiple lines (draw_text_ext).
    var _tw_min = 450;
    var _tw_max = 705;

    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));

    var _has_rarity = variable_struct_exists(item, "rarity");
    var _rcol  = _has_rarity ? item_rarity_color(item.rarity) : make_color_rgb(80, 210, 210);
    var _rname = _has_rarity ? item_rarity_name(item.rarity)  : "";

    var _flavor = "";
    if      (variable_struct_exists(item, "effect_desc") && item.effect_desc != "")
        _flavor = item.effect_desc;
    else if (variable_struct_exists(item, "description"))
        _flavor = item.description;
    _flavor = ui_sentence(_flavor);   // authored fragments stand alone here

    var _has_unique  = variable_struct_exists(item, "unique_desc")  && item.unique_desc  != "";
    var _has_compare = (compared_item != undefined);
    var _has_flavor  = (_flavor != "");

    // Build the row list. kind: "text" (txt+col, optional right-aligned tag) or "divider".
    var _rows = [];
    array_push(_rows, { kind: "text", txt: item.name, col: _rcol, tag: _rname, tagcol: make_color_rgb(130, 140, 165) });
    array_push(_rows, { kind: "text", txt: variable_struct_exists(item, "slot") ? string_upper(item_slot_title(item.slot)) : " ",
                        col: make_color_rgb(100, 110, 140), tag: "", tagcol: c_white });
    // Class restriction (gold = your class can use it, red = locked to another class).
    var _it_cr = variable_struct_exists(item, "class_req") ? item.class_req : -1;
    if (_it_cr != -1) {
        var _cr_names = ["Arcanist", "Bloodwarden", "Shadowstrider"];
        var _cr_name  = (_it_cr >= 0 && _it_cr <= 2) ? _cr_names[_it_cr] : "Unknown";
        var _my_cl    = variable_global_exists("chosen_class") ? global.chosen_class : -1;
        var _cr_col   = (_it_cr == _my_cl) ? make_color_rgb(210, 175, 90) : make_color_rgb(225, 80, 80);
        array_push(_rows, { kind: "text", txt: _cr_name + " only", col: _cr_col, tag: "", tagcol: c_white });
    }
    array_push(_rows, { kind: "text", txt: ui_item_stat_str(item), col: c_white, tag: "", tagcol: c_white });
    // Gear stat requirement (SYSTEMS_WEAPON_ROLES.md §D3): red if the wearer doesn't
    // meet it (equipping is hard-blocked), dim green if satisfied.
    var _req = item_stat_requirement(item);
    if (_req.value > 0 && _req.stat != "") {
        var _req_met = (player_base_stat(_req.stat) >= _req.value);
        var _req_col = _req_met ? make_color_rgb(110, 170, 110) : make_color_rgb(225, 80, 80);
        array_push(_rows, { kind: "text", txt: "Requires " + string(_req.value) + " " + _req.stat, col: _req_col, tag: "", tagcol: c_white });
    }
    // Permanent-level gate reads like a stat requirement (M 08-15: was hidden
    // until an equip attempt bounced).
    var _plr = item_perm_level_req(item);
    if (_plr > 0) {
        var _plr_met = (player_permanent_level() >= _plr);
        array_push(_rows, { kind: "text", txt: "Requires Permanent Level " + string(_plr),
            col: _plr_met ? make_color_rgb(110, 170, 110) : make_color_rgb(225, 80, 80), tag: "", tagcol: c_white });
    }
    if (_has_unique) array_push(_rows, { kind: "text", txt: item.unique_desc, col: make_color_rgb(255, 200, 50), tag: "", tagcol: c_white });
    // Socketed runes - list each rune's effect so equipped/inspected gear shows what
    // its sockets are contributing (these feed apply_equipment_stats but weren't visible).
    if (variable_struct_exists(item, "runes") && array_length(item.runes) > 0) {
        for (var _rune_ri = 0; _rune_ri < array_length(item.runes); _rune_ri++) {
            array_push(_rows, { kind: "text", txt: "* " + rune_describe(item.runes[_rune_ri]),
                                col: make_color_rgb(150, 200, 255), tag: "", tagcol: c_white });
        }
    }
    if (_has_flavor) {
        array_push(_rows, { kind: "divider" });
        array_push(_rows, { kind: "text", txt: _flavor, col: make_color_rgb(110, 120, 145), tag: "", tagcol: c_white });
    }
    if (_has_compare) {
        array_push(_rows, { kind: "divider" });
        array_push(_rows, { kind: "text", txt: "Replaces: " + compared_item.name, col: make_color_rgb(150, 160, 180), tag: "", tagcol: c_white });
        array_push(_rows, { kind: "text", txt: ui_item_stat_str(compared_item), col: make_color_rgb(110, 120, 140), tag: "", tagcol: c_white });
    }

    // Pass 1 - pick the panel width from the widest single-line content (name line
    // also reserves room for the right-aligned rarity tag), clamped to [min, max].
    var _maxw = _tw_min - _pad * 2;
    for (var _ri = 0; _ri < array_length(_rows); _ri++) {
        var _row = _rows[_ri];
        if (_row.kind != "text" || _row.txt == "") continue;
        var _w = string_width(_row.txt);
        if (_row.tag != "") _w += string_width(_row.tag) + 36;
        _maxw = max(_maxw, _w);
    }
    var _tw = clamp(_maxw + _pad * 2, _tw_min, _tw_max);
    var _ww = _tw - _pad * 2;   // inner wrap width

    // Pass 2 - measure height with wrapping applied.
    var _th = _pad * 2;
    for (var _ri = 0; _ri < array_length(_rows); _ri++) {
        var _row = _rows[_ri];
        if (_row.kind == "divider") { _th += 18; continue; }
        _th += (_row.txt == "") ? _lh : string_height_ext(_row.txt, _lh, _ww);
    }

    // Screen-clamp
    if (ttx + _tw > 1905) ttx -= _tw + 36;
    if (ttx < 6)          ttx  = 6;
    if (tty + _th > 1065) tty  = 1065 - _th;
    if (tty < 6)          tty  = 6;

    // Panel background
    draw_set_alpha(0.95);
    draw_set_color(make_color_rgb(12, 14, 26));
    draw_rectangle(ttx, tty, ttx + _tw, tty + _th, false);
    draw_set_alpha(1.0);
    draw_set_color(_rcol);
    draw_rectangle(ttx, tty, ttx + _tw, tty + _th, true);
    draw_rectangle(ttx, tty, ttx + _tw, tty + 4, false);

    // Pass 3 - render rows (wrapped via draw_text_ext).
    var _cx = ttx + _pad;
    var _cy = tty + _pad;
    for (var _ri = 0; _ri < array_length(_rows); _ri++) {
        var _row = _rows[_ri];
        if (_row.kind == "divider") {
            draw_set_color(make_color_rgb(50, 55, 80));
            draw_line(_cx, _cy + 9, ttx + _tw - _pad, _cy + 9);
            _cy += 18;
            continue;
        }
        draw_set_color(_row.col);
        draw_text_ext(_cx, _cy, _row.txt, _lh, _ww);
        if (_row.tag != "") {
            draw_set_halign(fa_right);
            draw_set_color(_row.tagcol);
            draw_text(ttx + _tw - _pad, _cy, _row.tag);
            draw_set_halign(fa_left);
        }
        _cy += (_row.txt == "") ? _lh : string_height_ext(_row.txt, _lh, _ww);
    }

    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// combat_hp_vis(c) - eased DISPLAYED HP for combat bars (08-04 conveyance).
// The real HP snaps instantly at resolution; the bar drains toward it over a
// few frames, and a projectile in flight holds the drain (c.hp_hold, set at
// cast) so the bar visibly moves when the bolt LANDS, not when the math ran.
// Call exactly once per frame per combatant (it ticks the hold/ease).
function combat_hp_vis(_c) {
    if (!variable_struct_exists(_c, "hp_vis")) _c.hp_vis = _c.HP;
    if (variable_struct_exists(_c, "hp_hold") && _c.hp_hold > 0) {
        _c.hp_hold--;
    } else {
        var _d = _c.HP - _c.hp_vis;
        if (abs(_d) <= 0.75) _c.hp_vis = _c.HP;
        else _c.hp_vis += _d * 0.22;
    }
    return round(_c.hp_vis);
}

// combat_slide_px(t, dur, peak) - offset curve for the dodge sidestep / hit
// recoil (08-04 conveyance): fast out over the first 30% of the anim, ease
// back over the rest. t is the countdown (dur -> 0); returns pixels.
function combat_slide_px(_t, _dur, _peak) {
    var _p = clamp((_dur - _t) / _dur, 0, 1);
    return _peak * ((_p < 0.3) ? (_p / 0.3) : (1 - (_p - 0.3) / 0.7));
}

// ui_draw_hp_bar(x, y, width, height, current_hp, max_hp, label)
// Draws a filled HP bar with a label and "current / max" readout.
// Color zones: green >=50%, yellow 25-50%, red <25%.
// ---------------------------------------------------------------------------
function ui_draw_hp_bar(x, y, width, height, current_hp, max_hp, label, ornate = false) {
    var ratio = (max_hp > 0) ? clamp(current_hp / max_hp, 0, 1) : 0;

    // Ornate gothic trim (enemy bars): a bronze frame with darker inset + small
    // corner ticks, drawn just OUTSIDE the bar so it reads as a forged metal plate.
    if (ornate) {
        draw_set_color(make_color_rgb(58, 44, 24));
        draw_rectangle(x - 4, y - 4, x + width + 4, y + height + 4, true);
        draw_set_color(make_color_rgb(150, 120, 62));
        draw_rectangle(x - 3, y - 3, x + width + 3, y + height + 3, true);
        draw_set_color(make_color_rgb(205, 170, 95));   // corner ticks
        var _ck = 8;
        draw_line(x - 3, y - 3, x - 3 + _ck, y - 3);  draw_line(x - 3, y - 3, x - 3, y - 3 + _ck);
        draw_line(x + width + 3, y - 3, x + width + 3 - _ck, y - 3);  draw_line(x + width + 3, y - 3, x + width + 3, y - 3 + _ck);
        draw_line(x - 3, y + height + 3, x - 3 + _ck, y + height + 3);  draw_line(x - 3, y + height + 3, x - 3, y + height + 3 - _ck);
        draw_line(x + width + 3, y + height + 3, x + width + 3 - _ck, y + height + 3);  draw_line(x + width + 3, y + height + 3, x + width + 3, y + height + 3 - _ck);
    }

    // Background track
    draw_set_color(c_dkgray);
    draw_rectangle(x, y, x + width, y + height, false);

    // Fill color based on HP ratio
    var fill_color;
    if (ratio >= 0.5) {
        fill_color = c_green;
    } else if (ratio >= 0.25) {
        fill_color = c_yellow;
    } else {
        fill_color = c_red;
    }

    var fill_width = floor(width * ratio);
    if (fill_width > 0) {
        draw_set_color(fill_color);
        draw_rectangle(x, y, x + fill_width, y + height, false);
    }

    // Thin border over the bar
    draw_set_color(c_black);
    draw_rectangle(x, y, x + width, y + height, true);

    // HP readout (right-aligned). Measured first so the label can be truncated to
    // whatever space is left, instead of running into the numbers (font-agnostic).
    draw_set_font(ui_font(fnt_ui));
    var _hp_str = string(current_hp) + " / " + string(max_hp);
    var _label_max = width - 12 - string_width(_hp_str) - 12;   // l-pad + readout + gap

    // Label (left-aligned, vertically centered on the bar), truncated to fit. Black
    // outline so the white name + HP readout stay legible over the yellow/green/red
    // fill (white-on-yellow was nearly unreadable at mid HP).
    draw_set_halign(fa_left);
    draw_set_valign(fa_middle);
    draw_set_color(c_white);
    draw_text_outline(x + 6, y + height / 2, ui_truncate(label, max(20, _label_max)));

    draw_set_halign(fa_right);
    draw_text_outline(x + width - 6, y + height / 2, _hp_str);

    // Reset alignment to safe defaults
    draw_set_font(-1);
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
}

// ---------------------------------------------------------------------------
// ui_draw_energy_pips(x, y, current_energy, max_energy)
// Draws energy as a row of small squares.
// Lit pips: bright yellow. Empty pips: dark gray.
// Each pip is 24x24 with a 6px gap between them.
// ---------------------------------------------------------------------------
function ui_draw_energy_pips(x, y, current_energy, max_energy) {
    var pip_size = 24;
    var pip_gap  = 6;

    // Burst AP (from Energy Tonic / Adrenaline Vial / Ley Battery) can push current
    // above the normal cap - draw extra pips so the surplus is visible, tinted orange.
    var pip_count = max(max_energy, current_energy);

    for (var i = 0; i < pip_count; i++) {
        var px = x + i * (pip_size + pip_gap);

        // Fill: lit pips are yellow, burst pips (beyond max) orange, empty dark gray.
        if (i < current_energy) {
            draw_set_color((i >= max_energy) ? make_color_rgb(245, 160, 40) : c_yellow);
        } else {
            draw_set_color(c_dkgray);
        }
        draw_rectangle(px, y, px + pip_size, y + pip_size, false);

        // Border
        draw_set_color(c_black);
        draw_rectangle(px, y, px + pip_size, y + pip_size, true);
    }

    // Label to the right of the pips
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(c_white);
    draw_set_halign(fa_left);
    draw_set_valign(fa_middle);
    var label_x = x + pip_count * (pip_size + pip_gap) + 6;
    draw_text(label_x, y + pip_size / 2, "AP");
    draw_set_font(-1);
    draw_set_valign(fa_top);
}

// ---------------------------------------------------------------------------
// ui_draw_secondary_resource(x, y, current, maximum, resource_name, color)
// Draws a slim labeled bar for Souls / Blood / Preparation.
// The bar fill uses the passed color; background is dark gray.
// ---------------------------------------------------------------------------
function ui_draw_secondary_resource(x, y, current, maximum, resource_name, color) {
    var width  = 375;
    var height = 24;
    var ratio  = (maximum > 0) ? clamp(current / maximum, 0, 1) : 0;

    // Background
    draw_set_color(c_dkgray);
    draw_rectangle(x, y, x + width, y + height, false);

    // Fill
    var fill_width = floor(width * ratio);
    if (fill_width > 0) {
        draw_set_color(color);
        draw_rectangle(x, y, x + fill_width, y + height, false);
    }

    // Border
    draw_set_color(c_black);
    draw_rectangle(x, y, x + width, y + height, true);

    // OVERCHARGE glow (07-16): a FULL reserve pulses gold - the next eligible
    // spender drains everything for +2 per point (see the Compendium entry).
    if (maximum > 0 && current >= maximum) {
        draw_set_alpha(0.45 + 0.4 * sin(current_time / 160));
        draw_set_color(make_color_rgb(255, 210, 80));
        draw_rectangle(x - 2, y - 2, x + width + 2, y + height + 2, true);
        draw_rectangle(x - 1, y - 1, x + width + 1, y + height + 1, true);
        draw_set_alpha(1.0);
    }

    // Resource name and value. Value measured first so the name truncates to the
    // remaining space instead of colliding it (font-agnostic).
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(c_white);
    draw_set_valign(fa_middle);
    var _val_str = string(current) + " / " + string(maximum)
                 + ((maximum > 0 && current >= maximum) ? "  FULL" : "");
    var _name_max = width - 12 - string_width(_val_str) - 12;

    draw_set_halign(fa_left);
    draw_text(x + 6, y + height / 2, ui_truncate(resource_name, max(20, _name_max)));

    draw_set_halign(fa_right);
    draw_text(x + width - 6, y + height / 2, _val_str);

    draw_set_font(-1);
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
}

// ---------------------------------------------------------------------------
// ui_draw_turn_queue(x, y, combat_state)
// Draws the initiative order as a horizontal row of name boxes.
// Active combatant: bright white border. Player boxes: teal. Enemy: orange.
// Names are truncated to 8 characters to fit the box.
// ---------------------------------------------------------------------------
function ui_draw_turn_queue(x, y, combat_state) {
    // Compact boxes: the old 120x48 strip at y15 reached down to y63 and sat on
    // top of the first enemy row's intent chips (top ~y56). 36 tall clears them.
    var box_width  = 100;
    var box_height = 36;
    var box_gap    = 6;

    var count = array_length(combat_state.combatants);

    for (var i = 0; i < count; i++) {
        var c  = combat_state.combatants[i];
        var bx = x + i * (box_width + box_gap);

        // Skip defeated combatants - show a dim slot instead
        if (c.is_defeated) {
            draw_set_alpha(0.3);
        }

        // Box fill - teal for player, orange for enemies
        if (c.is_player) {
            draw_set_color(c_teal);
        } else {
            draw_set_color(c_orange);
        }
        draw_rectangle(bx, y, bx + box_width, y + box_height, false);

        // Border - bright white for the active combatant, black otherwise
        if (i == combat_state.turn_index) {
            draw_set_color(c_white);
        } else {
            draw_set_color(c_black);
        }
        draw_rectangle(bx, y, bx + box_width, y + box_height, true);

        // Name - truncate to the box PIXEL width (font-agnostic; old 8-char cap
        // overflowed with wider fonts). Small font so more of the name fits.
        draw_set_font(ui_font(fnt_ui_small));
        var display_name = ui_truncate(c.name, box_width - 12);
        draw_set_color(c_white);
        draw_set_halign(fa_center);
        draw_set_valign(fa_middle);
        draw_text(bx + box_width / 2, y + box_height / 2, display_name);

        draw_set_alpha(1.0);
    }

    // Reset alignment
    draw_set_font(-1);
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
}

// ---------------------------------------------------------------------------
// ui_draw_ground_shadow(cx, baseline_y, sprite_display_width)
// Soft elliptical drop-shadow under a combat battler so the sprite reads against
// busy dungeon backgrounds. cx = horizontal centre, baseline_y = the sprite's feet
// (its bottom edge), width scales to the sprite. Three stacked ellipses (wide soft
// halo -> mid -> dense core) fake a soft edge and stay visible on dark floors. Call
// BEFORE the sprite so it sits beneath it.
// ---------------------------------------------------------------------------
function ui_draw_ground_shadow(cx, baseline_y, sprite_display_width) {
    var _w  = sprite_display_width * 0.46;   // tighter than before (was 0.66) - smaller pool
    var _h  = _w * 0.26;                      // squashed: ground perspective
    var _cy = baseline_y - _h * 0.5;          // sit just above the passed feet baseline
    var _prev_a = draw_get_alpha();
    draw_set_color(c_black);
    // Three stacked ellipses (soft halo -> mid -> dense core). Sized down so the
    // contact shadow hugs the feet instead of spreading into a big pool, while the
    // graded alphas keep it readable on dark arena floors.
    draw_set_alpha(0.30);
    draw_ellipse(cx - _w * 0.62, _cy - _h * 0.62, cx + _w * 0.62, _cy + _h * 0.62, false);
    draw_set_alpha(0.48);
    draw_ellipse(cx - _w * 0.44, _cy - _h * 0.44, cx + _w * 0.44, _cy + _h * 0.44, false);
    draw_set_alpha(0.66);
    draw_ellipse(cx - _w * 0.28, _cy - _h * 0.28, cx + _w * 0.28, _cy + _h * 0.28, false);
    draw_set_alpha(_prev_a);
    draw_set_color(c_white);
}

// ---------------------------------------------------------------------------
// ui_draw_ability_buttons(x, y, ability_array, selected_index, caster)
// Draws a row of ability buttons showing name and energy cost.
// Uncastable abilities are dimmed. The selected ability gets a bold, bright gold
// ring (over its role-coloured border) so it's unmistakable while scrolling.
// Each button: 240x75 with 12px gap.
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// combat_ability_geom(count) - SHARED source for the combat ability button row so
// the render (ui_draw_ability_buttons), the tap/click hit-test (combat Step), and
// the USED overlay (combat Draw) always agree. Desktop/pad = the shipped 240x75 @
// x240 pitch252 layout, byte-identical. Touch (M 07-17: prioritise ability-button
// size on mobile) FILLS the screen width so each button is as wide as the count
// allows - wider for the usual 4-6 abilities - and a little taller; it also keeps a
// high ability count ON-SCREEN instead of running off the right edge like the fixed
// 252 pitch did. Returns {x0, w, gap, pitch, y, h}.
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// combat_items_button_geom() - the single source of truth for the combat ITEMS
// button rect, shared by the draw (Draw_64) and the click hit-test (Step_0),
// which previously duplicated the literals in both files.
//
// On TOUCH with a letterbox gutter the button moves OUT of the play area and
// sits in the right gutter above the on-screen OK button (M 07-18: "items can
// go above ok button on mobile we have that extra width and space"). That frees
// the whole 1920 band for the ability row, which at 5 abilities (Expanded
// Arsenal) was otherwise running underneath it.
//
// `.gutter` tells combat_ability_geom whether the band is clear.
// ---------------------------------------------------------------------------
function combat_items_button_geom() {
    if (input_device() == 2) {
        var _pg = touch_pad_geom();   // undefined when no pad / no gutter room
        if (!is_undefined(_pg)) {
            var _gw = clamp(GUI_GUTTER - 28, 141, 210);
            var _gh = 76;
            return {
                x1: _pg.rcx - _gw / 2,
                // Stacked above the OK button, with a clear gap between them.
                y1: _pg.cy - _pg.abs - 28 - _gh,
                w: _gw, h: _gh, gutter: true
            };
        }
    }
    return { x1: 1767, y1: 990, w: 141, h: 63, gutter: false };
}

function combat_ability_geom(_count) {
    if (input_device() == 2 && _count > 0) {
        // The row is centred inside a band that STOPS SHORT of the ITEMS button
        // (obj_combat_controller Draw_64: x1767..1908, y990..1053). With a
        // symmetric 42px margin the row ran to x1878, so at 5 abilities
        // (Expanded Arsenal) the last button sat under ITEMS - and taps being
        // non-consuming, using ability 5 also opened the item menu. M 07-18.
        var _gap   = 14;
        var _lm    = 42;                              // left margin
        // Only reserve room on the right when ITEMS is still IN the band. With a
        // gutter it moves out above the OK button and the full width is ours.
        var _rm    = combat_items_button_geom().gutter ? 42 : 165;
        var _avail = GUI_W - _lm - _rm;
        var _w     = min((_avail - (_count - 1) * _gap) / _count, 360);   // cap so a few aren't absurd
        var _pitch = _w + _gap;
        var _x0    = _lm + (_avail - (_pitch * _count - _gap)) / 2;       // centre within the band
        return { x0: _x0, w: _w, gap: _gap, pitch: _pitch, y: 984, h: 87 };
    }
    return { x0: 240, w: 240, gap: 12, pitch: 252, y: 990, h: 75 };
}

function ui_draw_ability_buttons(x, y, ability_array, selected_index, caster) {
    // Geometry from the shared source (touch widens/fills; desktop = the passed
    // 240,990 exactly). Override the params so the existing draw math below is
    // untouched.
    var _abg = combat_ability_geom(array_length(ability_array));
    x = _abg.x0;
    y = _abg.y;
    var btn_width  = _abg.w;
    var btn_height = _abg.h;
    var btn_gap    = _abg.gap;

    var count = array_length(ability_array);

    draw_set_font(ui_font(fnt_ui_small));
    for (var i = 0; i < count; i++) {
        var ab = ability_array[i];
        var bx = x + i * (btn_width + btn_gap);
        // Same-category synergy discount (SYSTEMS_ABILITY_SYNERGY.md): the displayed AP
        // and the affordability gate both use the effective (discounted) cost, so a
        // button the discount makes castable lights up. Secondary resource checked apart.
        var _eff_cost  = ability_effective_cost(ab, caster);
        var _discounted = (_eff_cost < ab.energy_cost);
        var castable    = (caster.energy >= _eff_cost) && ability_secondary_ok(ab, caster);

        // Per-ability cooldown (Blink / Shadow Step). On cooldown == not usable.
        var _cd = (variable_struct_exists(caster, "ability_cd") && i < array_length(caster.ability_cd))
                  ? caster.ability_cd[i] : 0;

        // Dim uncastable or cooling-down buttons
        if (!castable || _cd > 0) {
            draw_set_alpha(0.45);
        }

        // Button background - dark fill
        draw_set_color(make_color_rgb(40, 40, 55));
        draw_rectangle(bx, y, bx + btn_width, y + btn_height, false);

        // Role-category border on EVERY button (offense red / defense blue / support
        // green / control purple) for at-a-glance role reading (SYSTEMS_ABILITY_SYNERGY.md).
        // Drawn 3px thick (stacked inward outlines) so the role colour is noticeable in
        // combat and stays readable next to the gold selection ring below.
        draw_set_color(ability_category_color(ability_category(ab)));
        for (var _b = 0; _b < 3; _b++) {
            draw_rectangle(bx + _b, y + _b, bx + btn_width - _b, y + btn_height - _b, true);
        }

        // OVERCHARGE (07-16): the reserve is FULL and this ability would drain it -
        // a pulsing gold ring over the role border flags the cash-out cast.
        if (ability_overcharge_eligible(ab, caster)) {
            var _oc_prev_a = draw_get_alpha();
            draw_set_alpha(0.55 + 0.45 * sin(current_time / 160));
            draw_set_color(make_color_rgb(255, 210, 80));
            for (var _ocb = 0; _ocb < 3; _ocb++) {
                draw_rectangle(bx + _ocb, y + _ocb, bx + btn_width - _ocb, y + btn_height - _ocb, true);
            }
            draw_set_alpha(_oc_prev_a);
        }

        // Selected ability: a bold, bright gold ring drawn OUTSIDE the role border so
        // the active choice is unmistakable even between same-coloured neighbours (the
        // old 1px white border was ambiguous while scrolling). Full alpha so it stays
        // bright even when the ability is dimmed for being uncastable; thickened by
        // stacking outlines that expand outward (buttons have a 12px gap, no overlap).
        if (i == selected_index) {
            var _prev_sel_a = draw_get_alpha();
            draw_set_alpha(1.0);
            draw_set_color(make_color_rgb(255, 224, 120));
            for (var _t = 1; _t <= 3; _t++) {
                draw_rectangle(bx - _t, y - _t, bx + btn_width + _t, y + btn_height + _t, true);
            }
            draw_set_alpha(_prev_sel_a);
        }

        // Ability icon - 60x60 badge on the left (inherits the dim alpha above)
        var _icon_sz = 60;
        ui_draw_ability_icon(bx + 8, y + 8, _icon_sz, ab);

        // Ability name (centered in the area right of the icon, upper half).
        // Wraps onto two lines and scales down to fit so long names (e.g.
        // "Adrenaline Rush") stay whole and inside the button. A detonator
        // reserves 16px on the right for its diamond so the two never collide.
        var _is_det    = ability_is_detonator(ab);
        var _name_left  = bx + 8 + _icon_sz + 5;
        var _name_right = (bx + btn_width) - 6 - (_is_det ? 16 : 0);
        var _name_w     = _name_right - _name_left;
        draw_set_color(c_white);
        draw_set_halign(fa_center);
        draw_set_valign(fa_middle);
        ui_draw_label_fit(_name_left + (_name_right - _name_left) / 2, y + 26, ab.name, _name_w, 45);

        // DETONATOR diamond (07-31, M: detonators weren't visible at a glance).
        // Same purple as the reactions table in the detail popup; top-right corner.
        if (_is_det) {
            var _dcx = bx + btn_width - 13, _dcy = y + 16;
            draw_set_color(make_color_rgb(190, 160, 240));
            draw_triangle(_dcx, _dcy - 7, _dcx - 7, _dcy, _dcx + 7, _dcy, false);
            draw_triangle(_dcx - 7, _dcy, _dcx + 7, _dcy, _dcx, _dcy + 7, false);
        }

        // Cooldown badge - overrides the AP pips while the ability is recharging.
        if (_cd > 0) {
            draw_set_color(make_color_rgb(120, 160, 255));
            draw_text(bx + btn_width / 2, y + btn_height - 18, "CD " + string(_cd));
            draw_set_alpha(1.0);
            continue;
        }

        // Energy cost pips in bottom half - small 12x12 squares. BASE cost pips are
        // always drawn so the real price stays legible; when a discount is active the
        // waived pips render as hollow green outlines at the end of the row ("you're
        // not paying these"), and the payable pips light green so the saving pops
        // (07-09: showing only the discounted count made a synergy-discounted
        // Singularity read as "2 AP ability" with no hint it was really 3).
        var pip_size = 12;
        var pip_gap  = 5;
        var _base_cost = variable_struct_exists(ab, "energy_cost") ? ab.energy_cost : 0;
        var pip_count = max(_base_cost, _eff_cost);   // 0 stays 0 (free)
        var pip_total_width = pip_count * (pip_size + pip_gap) - pip_gap;
        var pip_start_x = bx + (btn_width - pip_total_width) / 2;
        var pip_y       = y + btn_height - 21;
        var _lit_color  = _discounted ? make_color_rgb(120, 230, 140) : c_yellow;

        for (var p = 0; p < pip_count; p++) {
            var px = pip_start_x + p * (pip_size + pip_gap);
            if (p >= _eff_cost) {
                // Discount-waived pip: hollow green outline, full alpha so it reads
                // even on a dimmed button. Part of the base price, not paid this cast.
                var _pip_prev_a2 = draw_get_alpha();
                draw_set_alpha(1.0);
                draw_set_color(make_color_rgb(120, 230, 140));
                draw_rectangle(px, pip_y, px + pip_size, pip_y + pip_size, true);
                draw_line(px, pip_y + pip_size, px + pip_size, pip_y);   // struck through
                draw_set_alpha(_pip_prev_a2);
            } else if (p < caster.energy) {
                // Lit if the caster has enough energy to cover pips up to this one
                draw_set_color(_lit_color);
                draw_rectangle(px, pip_y, px + pip_size, pip_y + pip_size, false);
            } else {
                // Unaffordable pips still count toward the shown cost - at the dim
                // button alpha a dark-gray fill vanished into the background, so a
                // 2-AP ability with 1 AP left read as "costs 1 AP". Full alpha +
                // red outline = "this pip is part of the price, you lack the AP".
                var _pip_prev_a = draw_get_alpha();
                draw_set_alpha(1.0);
                draw_set_color(make_color_rgb(70, 75, 95));
                draw_rectangle(px, pip_y, px + pip_size, pip_y + pip_size, false);
                draw_set_color(make_color_rgb(205, 95, 80));
                draw_rectangle(px, pip_y, px + pip_size, pip_y + pip_size, true);
                draw_set_alpha(_pip_prev_a);
            }
        }

        draw_set_alpha(1.0);
    }

    // Reset alignment
    draw_set_font(-1);
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
}

// ---------------------------------------------------------------------------
// ui_draw_intent_chip(x, bottom_y, c) - the enemy's telegraphed NEXT action
// (INTENT_SPEC.md), drawn as a compact plate above its HP bar. Left edge at x;
// bottom_y = the chip's bottom edge. Tint by kind: red attack (with approx
// damage band), purple spell, green heal, amber control/debuff. While a control
// status would cancel the action the chip greys out with a strike-through.
// A re-rolled intent pulses briefly so the change is visible.
// ---------------------------------------------------------------------------
function ui_draw_intent_chip(x, bottom_y, c) {
    if (!variable_struct_exists(c, "intent") || c.intent == undefined) return;
    var _it = c.intent;

    var _txt = "";
    var _col = make_color_rgb(225, 90, 75);              // attack red
    switch (_it.kind) {
        case "attack":
            _txt = "ATK " + ((_it.lo == _it.hi) ? ("~" + string(_it.lo))
                                                 : ("~" + string(_it.lo) + "-" + string(_it.hi)));
            if (_it.x2) _txt += "  x2";
            break;
        case "spell":
            _col = make_color_rgb(180, 120, 235);        // spell purple
            _txt = "CAST ~" + string(_it.lo);
            break;
        case "heal":
            _col = make_color_rgb(105, 205, 125);        // heal/buff green
            _txt = string_upper(_it.label);
            break;
        default:                                          // control / debuff / dot
            _col = make_color_rgb(228, 178, 62);         // control amber
            _txt = string_upper(_it.label);
            break;
    }

    var _blocked = (enemy_intent_blocked(c) != "");
    if (_blocked) _col = make_color_rgb(118, 124, 136);  // greyed: action cancelled

    // Re-roll pulse: brief scale-up so a changed telegraph catches the eye.
    var _sc = 1.0;
    if (_it.pulse > 0) { _sc = 1.0 + 0.22 * (_it.pulse / 30.0); _it.pulse--; }

    draw_set_font(ui_font(fnt_ui_small));
    var _tw  = string_width(_txt)  * _sc;
    var _th  = string_height(_txt) * _sc;
    var _pad = 9;
    var _x0 = x, _x1 = x + _tw + _pad * 2;
    var _y1 = bottom_y, _y0 = bottom_y - _th - 8;
    var _cy = (_y0 + _y1) * 0.5;

    draw_set_alpha(0.75);
    draw_set_color(make_color_rgb(12, 12, 18));
    draw_rectangle(_x0, _y0, _x1, _y1, false);
    draw_set_alpha(1.0);
    draw_set_color(_col);
    draw_rectangle(_x0, _y0, _x1, _y1, true);

    draw_set_halign(fa_center);
    draw_set_valign(fa_middle);
    draw_text_transformed((_x0 + _x1) * 0.5, _cy, _txt, _sc, _sc, 0);
    if (_blocked) draw_line_width(_x0 + 5, _cy, _x1 - 5, _cy, 3);   // strike-through

    // School WEAKNESS gem (P2, 08-01): a small school-colored diamond on the
    // chip's right edge - perfect-information telegraph for the +30% / once-
    // per-enemy AP-refund window. Persists while the action is blocked (the
    // weakness doesn't go anywhere).
    var _wk = enemy_weak_school(variable_struct_exists(c, "name") ? c.name : "");
    if (_wk != "") {
        var _gx = _x1 + 11, _gr = 6;
        draw_set_color(school_color(_wk));
        draw_triangle(_gx - _gr, _cy, _gx, _cy - _gr - 2, _gx + _gr, _cy, false);
        draw_triangle(_gx - _gr, _cy, _gx, _cy + _gr + 2, _gx + _gr, _cy, false);
        draw_set_color(make_color_rgb(232, 234, 242));
        draw_triangle(_gx - _gr, _cy, _gx, _cy - _gr - 2, _gx + _gr, _cy, true);
        draw_triangle(_gx - _gr, _cy, _gx, _cy + _gr + 2, _gx + _gr, _cy, true);
        // Hover-explain (M 08-14 fix-now #8): stash a tip for the post-HUD pass
        // (status-tip idiom) - the gem alone never said what the weakness does.
        var _wmx = device_mouse_x_to_gui(0), _wmy = device_mouse_y_to_gui(0);
        if (point_distance(_wmx, _wmy, _gx, _cy) <= _gr + 10) {
            global.combat_weak_tip = { x: _wmx, y: _wmy, school: _wk,
                ename: (variable_struct_exists(c, "name") ? c.name : "Enemy") };
        }
    }

    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_weakness_tooltip(mx, my, school, ename) - hover-explain for the
// school-weakness diamond beside an enemy intent chip (M 08-14 fix-now #8: the
// gem alone never said what it MEANS). Same look as the status tooltip.
// ---------------------------------------------------------------------------
function ui_draw_weakness_tooltip(mx, my, school, ename) {
    if (school == "") return;
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));
    var _pad = 15, _lh = 27, _w = 540, _iw = _w - _pad * 2;
    var _col = school_color(school);
    var _hdr = "WEAK TO " + string_upper(school_label(school));
    var _bod = "Your " + school_label(school) + " abilities deal +30% damage to " + ename
             + ". The first " + school_label(school) + " hit on it each fight also refunds 1 AP.";
    var _h = _pad * 2 + _lh + string_height_ext(_bod, _lh, _iw);
    var _x = mx + 24, _y = my + 18;
    if (_x + _w > 1905) _x = mx - _w - 18;
    if (_x < 6) _x = 6;
    if (_y + _h > 1065) _y = 1065 - _h;
    if (_y < 6) _y = 6;
    draw_set_alpha(0.96);
    draw_set_color(make_color_rgb(12, 14, 26));
    draw_rectangle(_x, _y, _x + _w, _y + _h, false);
    draw_set_alpha(1.0);
    draw_set_color(_col);
    draw_rectangle(_x, _y, _x + _w, _y + _h, true);
    draw_rectangle(_x, _y, _x + _w, _y + 4, false);   // accent strip
    var _cx = _x + _pad, _cy = _y + _pad;
    draw_set_color(_col);
    draw_text(_cx, _cy, _hdr);
    _cy += _lh;
    draw_set_color(make_color_rgb(215, 218, 230));
    draw_text_ext(_cx, _cy, _bod, _lh, _iw);
    draw_set_font(-1);
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_alpha(1.0);
}

// Accuracy-row hover popup for the STATS page (M 08-17): shows the flat to-hit bonus and the
// socketed Hunter / Seer rune totals against their +12% caps, so the cap is discoverable
// somewhere the player already looks. Hover OR touch-hold on the row (hit-test in draw).
// ARM here (hit-test at the row), DRAW via ui_acc_cap_popup_flush() at the END of the
// character menu so later panels never paint over it and its font/colour changes
// never leak into the rows drawn after the Accuracy line.
function ui_acc_cap_popup(_x, _y, _w) {
    var _mx = device_mouse_x_to_gui(0), _my = device_mouse_y_to_gui(0);
    if (!(_mx >= _x && _mx <= _x + _w && _my >= _y - 4 && _my <= _y + 30)) return;
    global.acc_cap_popup_armed = true;
}
function ui_acc_cap_popup_flush() {
    if (!variable_global_exists("acc_cap_popup_armed") || !global.acc_cap_popup_armed) return;
    global.acc_cap_popup_armed = false;
    var _mx = device_mouse_x_to_gui(0), _my = device_mouse_y_to_gui(0);
    var _hun = rune_acc_total("ranged_acc"), _see = rune_acc_total("spell_acc");
    var _l1 = "Accuracy runes stack to a CAP";
    var _l2 = "Hunter (ranged attacks):  +" + string(_hun) + "% / +" + string(RUNE_ACC_CAP) + "%";
    var _l3 = "Seer (spells):            +" + string(_see) + "% / +" + string(RUNE_ACC_CAP) + "%";
    var _l4 = "Past the cap, extra runes add nothing.";
    draw_set_font(ui_font(fnt_ui_small));
    var _pw = max(string_width(_l1), string_width(_l2), string_width(_l3), string_width(_l4)) + 36;
    var _ph = 138;
    var _px = _mx + 24, _py = _my + 12;
    if (_px + _pw > GUI_W) _px = GUI_W - _pw - 6;
    if (_py + _ph > GUI_H) _py = GUI_H - _ph - 6;
    draw_set_alpha(0.95); draw_set_color(make_color_rgb(18, 20, 32));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, false);
    draw_set_alpha(1.0); draw_set_color(make_color_rgb(120, 140, 190));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, true);
    draw_set_color(c_white);                       draw_text(_px + 18, _py + 12, _l1);
    draw_set_color(make_color_rgb(150, 210, 160)); draw_text(_px + 18, _py + 42, _l2);
    draw_set_color(make_color_rgb(150, 190, 240)); draw_text(_px + 18, _py + 69, _l3);
    draw_set_color(make_color_rgb(140, 145, 165)); draw_text(_px + 18, _py + 99, _l4);
    draw_set_color(c_white);
}

// ---------------------------------------------------------------------------
// TAB HOVER QUICK-REF (M 08-15): mousing over any titular function tab shows a
// one-paragraph reminder of what lives there - a quick reference so nobody has
// to reset tutorials to remember what REWORK GEAR does. Pattern: each tab draw
// calls ui_tab_hover_stash() with its rect + copy; the screen calls
// ui_draw_tab_tip() LAST so the popup rides above everything on that screen.
// Touch: a finger resting on a tab sets the same hover, so the reminder shows
// while a tab is being pressed (mobile keyword-tap idea = later experiment).
// ---------------------------------------------------------------------------
// Procedural heart (two lobes + a point), centre (x,y), radius r. Used for the
// hub carousel bond row - spr_heart_fx is a 48px canvas with an ~8px heart in
// it, unreadable at chip size (M 08-15: "fix the bond hearts").
function ui_draw_heart(_x, _y, _r, _col, _a = 1.0) {
    draw_set_alpha(_a);
    draw_set_color(_col);
    draw_circle(_x - _r * 0.55, _y - _r * 0.4, _r * 0.62, false);
    draw_circle(_x + _r * 0.55, _y - _r * 0.4, _r * 0.62, false);
    draw_triangle(_x - _r * 1.12, _y - _r * 0.14, _x + _r * 1.12, _y - _r * 0.14, _x, _y + _r * 1.05, false);
    draw_set_alpha(1.0);
}

function ui_tab_hover_stash(x0, y0, x1, y1, title, body) {
    var _mx = device_mouse_x_to_gui(0), _my = device_mouse_y_to_gui(0);
    if (_mx >= x0 && _mx <= x1 && _my >= y0 && _my <= y1) {
        global.ui_tab_tip = { x: _mx, y: _my, title: title, body: body };
    }
}
function ui_draw_tab_tip() {
    if (!variable_global_exists("ui_tab_tip") || global.ui_tab_tip == undefined) return;
    var _t = global.ui_tab_tip;
    global.ui_tab_tip = undefined;   // one frame's stash, consumed here
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));
    var _pad = 15, _lh = 27, _w = 560, _iw = _w - _pad * 2;
    var _h = _pad * 2 + _lh + 6 + string_height_ext(_t.body, _lh, _iw);
    var _x = _t.x + 24, _y = _t.y + 20;
    if (_x + _w > 1905) _x = _t.x - _w - 18;
    if (_y + _h > 1065) _y = 1065 - _h;
    draw_set_alpha(0.96);
    draw_set_color(make_color_rgb(12, 14, 26));
    draw_rectangle(_x, _y, _x + _w, _y + _h, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(200, 170, 110));
    draw_rectangle(_x, _y, _x + _w, _y + _h, true);
    draw_rectangle(_x, _y, _x + _w, _y + 4, false);
    draw_set_color(make_color_rgb(255, 225, 150));
    draw_text(_x + _pad, _y + _pad, _t.title);
    draw_set_color(make_color_rgb(215, 218, 230));
    draw_text_ext(_x + _pad, _y + _pad + _lh + 6, _t.body, _lh, _iw);
}
// The authored quick-ref copy, keyed "<npc>:<tab label>". "" = no tip.
function ui_tab_hint_body(key) {
    switch (key) {
        case "dorn:BUY":     return "Dorn's rotating stock - weapons, armor and gear. Restocks between runs; his Awakening-scaled shelf improves as you climb. Station rank 1 widens the stall by 2 pieces.";
        case "dorn:SELL":    return "Sell unequipped gear from your pack and stash for gold.";
        case "dorn:REFORGE": return "Two crafts. REWORK GEAR: spend a Reforge Ingot of the item's tier or higher to reroll its affixes - same item, fresh stats. SMELT: destroy a piece for an ingot - Dorn studies one of its affixes into the PATTERN BOOK and keeps its ICON for the art page. CRAFT builds an item to your own design from studied blueprints (mastered blueprints roll PAST natural drops), wearing any smelted icon that fits the slot - then HONE buys +1s for gold.";
        case "dorn:TEMPER":  return "Work rough gear toward its true potential: each step is +10% QUALITY (gold + rune dust), plus a little bonus max HP. A raw legendary barely beats a finished epic. Station rank 2 makes each step +12%.";
        case "maren:Socket Gear":   return "Socket GEAR runes into equipped items with open sockets. Runes come back out freely - nothing is lost by experimenting. Station rank 2 unlocks DEEP SOCKET [D]: +1 socket, once per run.";
        case "maren:Aspects":       return "Socket ASPECT runes into your character's aspect slots - per-school damage (Ember, Rime...), Serration, Surge and the flagships live here.";
        case "maren:Runesmithing":  return "Maren's craft menu: COMBINE three identical runes into one a tier higher, SPLIT a rune back down, AWAKEN a dormant legendary, forge FLAGSHIP runes, and seal her RUNEHEART CORE share of the Legendary Forge.";
        case "maren:Runes":         return "Your full rune pouch - every unsocketed gear and aspect rune you carry.";
        case "maren:Spirits":       return "Bottled spirits from the dark. Maren frees them - their parting songs join your jukebox.";
        case "sable:Brew":     return "Sable brews potions from her book - and the CHAOTIC BREW fuses potions you already know into something unstable and new.";
        case "sable:Salvage":  return "Break unequipped gear down into RUNE DUST - rarer pieces pay more dust.";
        case "sable:Upgrade":  return "Sable's refinements: TRANSMUTE three runes into a different rune of the same tier, and improve what she already knows how to make.";
        case "sable:Rebirth":  return "Sable's dark exchange: CURSED REBIRTH feeds a legendary to the dark - it comes back stronger, but branded with a real curse. QUINTESSENCE distills her share of the Legendary Forge here too.";
        case "petra:BUY":      return "Supplies for the road - potions, creature feed and consumables. Restocks between runs; her SPECIAL shelf sometimes carries something rarer, and a LIMITED lot of one dungeon reagent rotates each run.";
        case "petra:SELL":     return "Sell unequipped gear from your pack and stash for gold.";
        case "petra:TRADE":    return "The treasure ladder: 3 same-tier items trade up to the next tier, delivered after floor clears. [R] cycles RUNE mode (5 same-tier runes buy a BLUEPRINT rune of your choosing) and REAGENT mode (swap 2 of one dungeon reagent for 1 of another). Traded goods are destroyed. Station rank 1 opens a SECOND ledger line.";
    }
    return "";
}

// ---------------------------------------------------------------------------
// ui_draw_telegraph_warning(enemy_name, message)
// Draws a red banner sized to its text, centered, parked just ABOVE the combat
// log panel (log top = y735) and drawn after it so it can never be buried.
// Only called when enemy_should_telegraph() returns true for any enemy.
// ---------------------------------------------------------------------------
function ui_draw_telegraph_warning(enemy_name, message) {
    var room_w      = GUI_W;
    var banner_h    = 54;
    var banner_y    = 669;   // bottom edge 12px above the log panel top (735)

    // Fall back to a generic wind-up warning when an enemy has no authored message
    // (most bosses set telegraph_turn/damage but inherit an empty message from their
    // base clone, so the banner used to show just the name).
    var _msg = (is_string(message) && message != "")
             ? message : "is charging a devastating attack!";
    var warning_text = enemy_name + " " + _msg;

    // Size the banner to the text (centered) instead of spanning the whole screen.
    draw_set_font(ui_font(fnt_ui));
    draw_set_halign(fa_center);
    draw_set_valign(fa_middle);
    var _pad_x = 42;
    var _bw    = string_width(warning_text) + _pad_x * 2;
    var _bx1   = room_w / 2 - _bw / 2;
    var _bx2   = room_w / 2 + _bw / 2;
    var mid_y  = banner_y + banner_h / 2;

    // Semi-transparent dark red backing
    draw_set_alpha(0.88);
    draw_set_color(make_color_rgb(160, 20, 20));
    draw_rectangle(_bx1, banner_y, _bx2, banner_y + banner_h, false);

    // Solid red border
    draw_set_alpha(1.0);
    draw_set_color(c_red);
    draw_rectangle(_bx1, banner_y, _bx2, banner_y + banner_h, true);

    // Warning text - fake bold via a 1px dark-red shadow under white.
    draw_set_color(make_color_rgb(80, 0, 0));
    draw_text(room_w / 2 + 2, mid_y + 2, warning_text);
    draw_set_color(c_white);
    draw_text(room_w / 2, mid_y, warning_text);

    draw_set_font(-1);
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
}

// ---------------------------------------------------------------------------
// ui_draw_combat_log(x, y, width, height, log_array)
// Fills the panel from bottom up, most recent entry at the bottom.
// Each entry reserves height proportional to its estimated wrapped line count
// so wrapped text never overlaps the entry below it.
// Stops drawing when the next entry would reach the top padding boundary.
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// ui_draw_log_line(x, y, str, max_w)
// Draws one combat-log line word-by-word, tinting the operative words so key
// events pop: CRIT (gold), MISS / DODGE (cool gray-blue / cyan), and the eight
// element schools (school_color). A number immediately tied to one of those
// events inherits its color too. Everything else stays the caller's color.
// Truncates with "..." at max_w. Caller sets font + alpha first.
// ---------------------------------------------------------------------------
function ui_draw_log_line(x, y, str, max_w) {
    var _COL_BASE  = make_color_rgb(225, 225, 230);
    var _COL_CRIT  = make_color_rgb(255, 205,  70);
    var _COL_MISS  = make_color_rgb(150, 165, 195);
    var _COL_DODGE = make_color_rgb( 95, 210, 220);
    var _COL_GOLD  = make_color_rgb(240, 200,  70);
    var _COL_DUST  = make_color_rgb( 90, 195, 185);
    var _COL_HEAL  = make_color_rgb(120, 220, 140);   // matches the heal damage-popup green

    // Companion (pet) actions are tagged "[Companion]" at the push sites and tinted
    // wholesale in the pet's green, so its turn reads at a glance among enemy lines.
    if (string_pos("[Companion]", str) == 1) _COL_BASE = make_color_rgb(150, 215, 150);

    var _COL_ENEMY = make_color_rgb(235, 120, 110);
    var _COL_DMG   = make_color_rgb(240, 130,  85);

    var _words = string_split_words_log(str);
    var _n = array_length(_words);
    var _cx = x;
    var _space_w = string_width(" ");

    // Pre-scan: pick the color a number in this line should take, even when the
    // keyword comes AFTER the number (the log writes "... for 42 damage (CRIT!)").
    // Priority: crit > school > damage > gold. Gold also covers loot/economy lines.
    var _num_col   = -1;
    var _gold_line = false;
    var _dmg_line  = false;
    for (var _ps = 0; _ps < _n; _ps++) {
        var _pc = string_lower(string_trim_punct_log(_words[_ps]));
        if (_pc == "crit" || _pc == "critical" || _pc == "crits") { _num_col = _COL_CRIT; }
        if (_num_col == -1) {
            var _ps_sch = log_word_school(_pc);
            if (_ps_sch != "" && !log_word_enemy_masked(_words, _ps)) _num_col = school_color(_ps_sch);
        }
        if (_pc == "gold" || log_word_is_goldnum(_pc)) _gold_line = true;
        if (_pc == "damage" || _pc == "blocked") _dmg_line = true;
    }
    if (_num_col == -1 && _dmg_line)  _num_col = _COL_DMG;
    if (_num_col == -1 && _gold_line) _num_col = _COL_GOLD;

    // Enemy names (M 08-13): tint every live combatant's name a hostile red so
    // actors read at a glance. Matched as CONSECUTIVE word runs against the
    // current fight's enemy list (covers summons, wardens, the duelist - any
    // name the log can print), possessives stripped, so "frost damage" never
    // false-hits on "Frost Shard".
    var _emk = array_create(_n, false);
    if (instance_exists(obj_combat_controller)) {
        var _lcc = instance_find(obj_combat_controller, 0);
        if (variable_instance_exists(_lcc, "combat_state") && is_struct(_lcc.combat_state)
            && variable_struct_exists(_lcc.combat_state, "combatants")) {
            var _lcbs = _lcc.combat_state.combatants;
            for (var _le = 0; _le < array_length(_lcbs); _le++) {
                if (_lcbs[_le].is_player) continue;
                var _nmw = string_split(string_lower(_lcbs[_le].name), " ");
                var _nw  = array_length(_nmw);
                if (_nw == 0) continue;
                for (var _ls = 0; _ls + _nw <= _n; _ls++) {
                    var _hit = true;
                    for (var _lq = 0; _lq < _nw; _lq++) {
                        var _lw = string_lower(string_trim_punct_log(_words[_ls + _lq]));
                        if (string_length(_lw) > 2 && string_copy(_lw, string_length(_lw) - 1, 2) == "'s")
                            _lw = string_copy(_lw, 1, string_length(_lw) - 2);
                        if (_lw != _nmw[_lq]) { _hit = false; break; }
                    }
                    if (_hit) for (var _lq = 0; _lq < _nw; _lq++) _emk[_ls + _lq] = true;
                }
            }
        }
    }

    for (var _i = 0; _i < _n; _i++) {
        var _w    = _words[_i];
        var _core = string_lower(string_trim_punct_log(_w));
        var _col  = _COL_BASE;
        var _is_num = (_core != "" && string_digits(_core) == _core);

        // Heals first (most specific): only heal lines put a number beside the
        // word HP ("you recover 12 HP"), so that pair goes green - including in
        // lines that also carry a damage number (Soulbind's lifelink).
        var _prev_core = (_i > 0)      ? string_lower(string_trim_punct_log(_words[_i - 1])) : "";
        var _next_core = (_i + 1 < _n) ? string_lower(string_trim_punct_log(_words[_i + 1])) : "";
        if (_is_num && _next_core == "hp") {
            _col = _COL_HEAL;
        } else if (_core == "hp" && _prev_core != "" && string_digits(_prev_core) == _prev_core) {
            _col = _COL_HEAL;
        } else if (_core == "crit" || _core == "critical" || _core == "crits") {
            _col = _COL_CRIT;
        } else if (_core == "miss" || _core == "missed" || _core == "misses" || _core == "missing") {
            _col = _COL_MISS;
        } else if (_core == "dodge" || _core == "dodged" || _core == "dodges") {
            _col = _COL_DODGE;
        } else if (_emk[_i]) {
            _col = _COL_ENEMY;
        } else if (log_word_school(_core) != "" && !log_word_enemy_masked(_words, _i)) {
            _col = school_color(log_word_school(_core));
        } else if (_core == "gold" || log_word_is_goldnum(_core)) {
            _col = _COL_GOLD;
        } else if (_core == "dust" || _core == "rune") {
            _col = _COL_DUST;
        } else if (_core == "common")      { _col = make_color_rgb(165, 170, 180);
        } else if (_core == "uncommon")    { _col = make_color_rgb(100, 205, 100);
        } else if (_core == "rare")        { _col = make_color_rgb( 90, 155, 235);
        } else if (_core == "epic")        { _col = make_color_rgb(190, 110, 235);
        } else if (_core == "legendary")   { _col = make_color_rgb(235, 150,  60);
        } else if (_core == "consumable")  { _col = make_color_rgb( 90, 210, 210);
        } else if (_is_num && _num_col != -1) {
            _col = _num_col;
        }

        // Word + trailing space; stop with an ellipsis if we'd overflow.
        var _seg_w = string_width(_w);
        if (_cx + _seg_w > x + max_w) {
            draw_set_color(_COL_BASE);
            draw_text(_cx, y, "...");
            return;
        }
        draw_set_color(_col);
        draw_text(_cx, y, _w);
        _cx += _seg_w + _space_w;
    }
}

// log_word_enemy_masked(words, i) - TRUE when words[i] is a school word that is
// really part of an ENEMY NAME (task #20: "Frost Shard hits you for 12" tinted
// "Frost" and the 12 like frost damage). Matched as adjacent word pairs against
// the school-colliding names in the enemy catalog - extend the list when a new
// school-named enemy ships. "'s" is stripped so possessives still match.
function log_word_enemy_masked(_words, _i) {
    static _pairs = [["fire", "drake"], ["frost", "shard"], ["eternal", "frost"]];
    var _strip = function(_w) {
        var _c = string_lower(string_trim_punct_log(_w));
        if (string_length(_c) > 2 && string_copy(_c, string_length(_c) - 1, 2) == "'s")
            _c = string_copy(_c, 1, string_length(_c) - 2);
        return _c;
    };
    var _n    = array_length(_words);
    var _c    = _strip(_words[_i]);
    var _next = (_i + 1 < _n) ? _strip(_words[_i + 1]) : "";
    var _prev = (_i > 0)      ? _strip(_words[_i - 1]) : "";
    for (var _p = 0; _p < array_length(_pairs); _p++) {
        if (_c == _pairs[_p][0] && _next == _pairs[_p][1]) return true;
        if (_c == _pairs[_p][1] && _prev == _pairs[_p][0]) return true;
    }
    return false;
}

// log_word_school(core) - the element school a lowercased log word invokes: an
// exact school word ("fire") or a compound with one at its start or end
// ("soulfire", "firemark", "bloodthorn"), so school-flavored ability names tint
// like their school. Prefix/suffix only - an interior match would false-hit
// ordinary words ("avoided" contains "void"). Returns "" for no school.
function log_word_school(_core) {
    static _log_schools = ["fire", "frost", "shock", "arcane", "blood", "void", "shadow", "poison"];
    var _cl = string_length(_core);
    for (var _i = 0; _i < array_length(_log_schools); _i++) {
        var _s  = _log_schools[_i];
        var _sl = string_length(_s);
        if (_cl < _sl) continue;
        if (string_copy(_core, 1, _sl) == _s) return _s;
        if (string_copy(_core, _cl - _sl + 1, _sl) == _s) return _s;
    }
    return "";
}

// log_word_is_goldnum(core) - true for a "12g" style gold amount (digits + a
// trailing 'g'), so the combat log can tint gold drops in gold.
function log_word_is_goldnum(_core) {
    var _len = string_length(_core);
    if (_len < 2) return false;
    if (string_char_at(_core, _len) != "g") return false;
    var _digits = string_copy(_core, 1, _len - 1);
    return (string_digits(_digits) == _digits);
}

// Split a log line on spaces (helper kept local so the renderer stays self-
// contained). Returns an array of words with empties dropped.
function string_split_words_log(_s) {
    var _out = [];
    var _cur = "";
    for (var _i = 1; _i <= string_length(_s); _i++) {
        var _c = string_char_at(_s, _i);
        if (_c == " ") {
            if (_cur != "") { array_push(_out, _cur); _cur = ""; }
        } else {
            _cur += _c;
        }
    }
    if (_cur != "") array_push(_out, _cur);
    return _out;
}

// Strip leading/trailing punctuation from a word so keyword matching is clean
// ("(CRIT!)" -> "crit!" stays, but "42," -> "42"). Keeps inner characters.
function string_trim_punct_log(_w) {
    var _p = "(),.:;'\"+-[]!?";
    var _s = _w;
    while (string_length(_s) > 0 && string_pos(string_char_at(_s, 1), _p) > 0)
        _s = string_delete(_s, 1, 1);
    while (string_length(_s) > 0 && string_pos(string_char_at(_s, string_length(_s)), _p) > 0)
        _s = string_delete(_s, string_length(_s), 1);
    return _s;
}

function ui_draw_combat_log(x, y, width, height, log_array) {
    // Row pitch is MEASURED from the active log font so the Large font-size
    // setting gets fewer, taller rows instead of overlapping lines (the band
    // stays 198px; scrollback already covers the shorter window). Default
    // font measures 27 -> the shipped 29px pitch, bit-identical.
    draw_set_font(ui_font(fnt_ui_small));
    var line_h  = max(29, string_height("Ag") + 2);
    var padding = 12;

    // Background panel + the gothic frame every other combat box wears (M 08-13)
    draw_set_alpha(0.7);
    draw_set_color(make_color_rgb(15, 15, 25));
    draw_rectangle(x, y, x + width, y + height, false);
    draw_set_alpha(1.0);
    ui_draw_gothic_frame(x, y, x + width, y + height, 22);

    var log_count = array_length(log_array);
    if (log_count == 0) return;

    draw_set_font(ui_font(fnt_ui_small));
    var max_width = width - (padding * 2) - 21;   // leave room for a scrollbar gutter
    // One compact line per entry (truncated, never wrapped) so many fit; this lets
    // the player see far more history at once and pairs with mouse-wheel scrollback.
    var _visible_rows = floor((height - padding * 2) / line_h);

    // Scrollback offset (0 = pinned to newest). Read from the combat controller so
    // the mouse wheel - handled in obj_combat_controller Step - can drive it.
    var _scroll = 0;
    // Per-line damage breakdowns for the hover tooltip (Task 1). Empty/disabled = no hover.
    var _detail_arr = [];
    var _bd_enabled = variable_global_exists("combat_log_breakdowns") && global.combat_log_breakdowns;
    if (instance_exists(obj_combat_controller)) {
        var _cc = instance_find(obj_combat_controller, 0);
        if (variable_instance_exists(_cc, "combat_log_scroll")) {
            var _max_scroll = max(0, log_count - _visible_rows);
            _scroll = clamp(_cc.combat_log_scroll, 0, _max_scroll);
            _cc.combat_log_scroll = _scroll;   // keep it bounded as the log grows
        }
        if (_bd_enabled && variable_instance_exists(_cc, "combat_log_detail")) _detail_arr = _cc.combat_log_detail;
    }

    draw_set_halign(fa_left);
    draw_set_valign(fa_top);

    // Hover state: a damage line under the mouse, surfaced as a math tooltip below.
    var _bd_mx = device_mouse_x_to_gui(0);
    var _bd_my = device_mouse_y_to_gui(0);
    var _bd_hover = undefined;

    // Newest at the bottom. Index of the bottom-most visible entry, offset by scroll.
    var _bottom_idx = log_count - 1 - _scroll;
    for (var _row = 0; _row < _visible_rows; _row++) {
        var _idx = _bottom_idx - _row;
        if (_idx < 0) break;
        var _ly = y + height - padding - line_h - _row * line_h;

        // A breakdown exists for this line? Mark it (subtle underline) and test hover.
        var _row_detail = (_bd_enabled && _idx < array_length(_detail_arr)) ? _detail_arr[_idx] : undefined;
        if (is_struct(_row_detail)) {
            if (_bd_mx >= x && _bd_mx <= x + width && _bd_my >= _ly && _bd_my < _ly + line_h) {
                _bd_hover = _row_detail;
                draw_set_alpha(0.18);
                draw_set_color(make_color_rgb(120, 150, 200));
                draw_rectangle(x + padding - 3, _ly - 1, x + max_width, _ly + line_h - 3, false);
                draw_set_alpha(1.0);
            }
        }

        // Fade by age relative to the newest visible line
        draw_set_alpha(lerp(1.0, 0.45, min(_row / 8.0, 1.0)));
        // Keyword/number color coding (crit/miss/dodge/schools); truncates internally.
        ui_draw_log_line(x + padding, _ly, log_array[_idx], max_width);
    }
    draw_set_alpha(1.0);

    // Draw the hovered line's damage breakdown tooltip last (on top of everything).
    if (is_struct(_bd_hover)) ui_draw_log_breakdown(_bd_mx, _bd_my, _bd_hover);

    // Scroll indicators + a simple scrollbar when there's history to scroll
    if (log_count > _visible_rows) {
        draw_set_halign(fa_right);
        draw_set_color(make_color_rgb(120, 140, 170));
        if (_scroll > 0)                                 ui_draw_scroll_more(x + width - 9, y + 3, true, "older");
        if (_scroll < log_count - _visible_rows)         ui_draw_scroll_more(x + width - 9, y + height - 27, false, "newer");
        draw_set_halign(fa_left);

        // Scrollbar track + thumb on the right gutter
        var _bar_x = x + width - 9;
        draw_set_color(make_color_rgb(40, 45, 60));
        draw_line_width(_bar_x, y + 3, _bar_x, y + height - 3, 5);
        var _track_h = height - 6;
        var _thumb_h = max(18, _track_h * (_visible_rows / log_count));
        // _scroll 0 = bottom; map to a thumb position (bottom = newest)
        var _frac    = (log_count - _visible_rows) > 0 ? (_scroll / (log_count - _visible_rows)) : 0;
        var _thumb_y = (y + height - 3 - _thumb_h) - _frac * (_track_h - _thumb_h);
        draw_set_color(make_color_rgb(110, 130, 160));
        draw_line_width(_bar_x, _thumb_y, _bar_x, _thumb_y + _thumb_h, 5);
    }
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_log_breakdown(mx, my, detail)
// Hover tooltip for a combat-log damage line: a DnD-style "dice roll" of the
// components that produced the hit (base, stat bonuses, crit, weapon flat, element
// riders, %-multipliers) and the post-armor total. detail is the struct attached at
// the log push (Task 1). Drawn near the cursor, clamped on-screen.
// ---------------------------------------------------------------------------
function ui_draw_log_breakdown(mx, my, detail) {
    if (!is_struct(detail)) return;
    var _lines = variable_struct_exists(detail, "lines") ? detail.lines : [];
    var _n     = array_length(_lines);

    var _pw    = 444;
    var _row_h = 30;
    var _head  = 48;
    var _foot  = 42;
    var _ph    = _head + _n * _row_h + _foot;

    // Anchor above-right of the cursor; clamp inside the GUI.
    var _px = mx + 18;
    var _py = my - _ph - 12;
    if (_px + _pw > GUI_W) _px = GUI_W - _pw - 6;
    if (_px < 6) _px = 6;
    if (_py < 6) _py = my + 18;
    if (_py + _ph > GUI_H) _py = GUI_H - _ph - 6;

    // Panel
    draw_set_alpha(0.96);
    draw_set_color(make_color_rgb(14, 15, 23));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, false);
    draw_set_alpha(1.0);
    var _accent = school_color(variable_struct_exists(detail, "school") ? detail.school : "");
    if (_accent == c_white) _accent = make_color_rgb(120, 150, 200);
    draw_set_color(_accent);
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, true);

    var _lx = _px + 18;
    var _rx = _px + _pw - 18;
    var _y  = _py + 12;

    // Header: ability name (+ CRIT tag)
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_accent);
    var _ttl = variable_struct_exists(detail, "title") ? detail.title : "Damage";
    if (variable_struct_exists(detail, "crit") && detail.crit) _ttl += "  (CRIT)";
    draw_text(_lx, _y, _ttl);
    _y += _head - 12;

    draw_set_color(make_color_rgb(60, 64, 90));
    draw_line(_lx, _y, _rx, _y);
    _y += 6;

    // Component rows: label left, value right.
    draw_set_font(ui_font(fnt_ui_small));
    for (var _i = 0; _i < _n; _i++) {
        var _ln = _lines[_i];
        draw_set_halign(fa_left);
        draw_set_color(make_color_rgb(180, 188, 206));
        draw_text(_lx, _y, variable_struct_exists(_ln, "label") ? _ln.label : "");
        draw_set_halign(fa_right);
        draw_set_color(make_color_rgb(225, 228, 240));
        draw_text(_rx, _y, variable_struct_exists(_ln, "val") ? string(_ln.val) : "");
        _y += _row_h;
    }

    // Footer: post-armor total dealt.
    draw_set_color(make_color_rgb(60, 64, 90));
    draw_line(_lx, _y + 2, _rx, _y + 2);
    _y += 8;
    draw_set_font(ui_font(fnt_ui));
    draw_set_halign(fa_left);
    draw_set_color(make_color_rgb(120, 200, 140));
    draw_text(_lx, _y, "Damage dealt");
    draw_set_halign(fa_right);
    draw_set_color(c_white);
    draw_text(_rx, _y, string(variable_struct_exists(detail, "total") ? detail.total : 0));

    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_ability_tooltip(x, anchor_bottom, ability, caster)
// Draws a tooltip panel for the currently selected ability (name, costs,
// damage, effect, accuracy). Width is fixed (480); HEIGHT is measured from the
// content and the panel is anchored by its BOTTOM at anchor_bottom, growing
// UPWARD. This guarantees long wrapped descriptions never spill below the panel
// onto the ability button row. The tooltip is on the GUI layer (drawn over the
// enemy sprites), so growing up simply covers empty arena space.
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// ui_truncate(str, max_w)
// Returns str clipped (with an ellipsis) to fit within max_w pixels at the
// current draw font. Keeps single-line labels from overflowing their box.
// ---------------------------------------------------------------------------
function ui_truncate(str, max_w) {
    if (string_width(str) <= max_w) return str;
    var _s = str;
    while (string_length(_s) > 1 && string_width(_s + "...") > max_w) {
        _s = string_copy(_s, 1, string_length(_s) - 1);
    }
    return _s + "...";
}

// =============================================================================
// ui_draw_option_row(x1, y1, x2, y2, opt) - THE vendor menu row (M 08-08).
//
// M's report: "wall of text issues, need more text color variance... maren has a
// tab that is just a list of all monochrome options that feel unappealing to read
// through." Every vendor had grown its own hand-rolled two-line row, and all of
// them drew the ACTION and its EXPLANATION in near-identical greys, so a craft
// menu read as one undifferentiated block of prose.
//
// This is the single standard. The visual grammar:
//   TITLE   accent-coloured, fnt_ui        - the verb. What you are choosing.
//   body    dim grey, fnt_ui_small         - the explanation. Never competes.
//   cost    right-aligned chips, per-currency colour; RED when unaffordable
//   tag     optional right-hand status word (READY / LOCKED / N held)
// Selection is carried by an accent left-bar + tinted fill + brighter title, so
// the row you are on is obvious without changing the text's meaning.
//
// opt = {
//   title:  string                (required)
//   body:   string                ("" for none - the body line is then skipped)
//   sel:    bool                  (cursor is on this row)
//   gold:   real                  (-1 / omitted = no gold cost)
//   dust:   real                  (-1 / omitted = no dust cost)
//   cost:   string                (free-text cost, used when it isn't gold/dust)
//   afford: bool                  (false paints every cost chip red; default true)
//   tag:    string                ("" for none)
//   tag_col:color                 (omitted = neutral blue-grey)
//   accent: color                 (omitted = the parchment gold the game uses for
//                                  actionable nouns; pass a vendor colour to theme)
//   dim:    bool                  (unavailable option - whole row desaturates)
// }
// Draws inside the given box; the caller owns geometry and hit-testing (hit-tests
// live in DRAW per the input-parity rule, so callers already know these bounds).
// =============================================================================
// Tab count for a shop screen. shop_open: 0 = Petra (BUY/SELL/TRADE),
// 1 = Dorn (BUY/SELL/REFORGE/TEMPER - Temper moved here from Maren on 08-08).
// Single source of truth: the tab bar in ui_draw_shop_screen AND all three
// click hit-tests in obj_game_controller/Step lay out from this, so they can
// never drift apart.
function shop_tab_count(_shop_open) {
    return (_shop_open == 1) ? 4 : 3;
}

// =============================================================================
// TRAPS ON THE FIELD (08-08 v2). M: "what i want is for the trap to physically
// sit in the middle of the screen between the player and the enemy like an
// actual trap, that springs when it goes off by an enemy."
//
// The first pass drew a row of slot CHIPS at y690 - including EMPTY ones - which
// covered the combat log, the pet and the player sprite and killed exactly the
// immersion this rework exists for. Replaced by three pieces:
//   1. ui_draw_trap_field - the trap OBJECT, on the ground mid-battlefield
//   2. trap buff chips    - status in the left buff row, reading like SS
//   3. ui_draw_trap_info  - the "blocks melee" reminder, left column under buffs
// Empty slots are not drawn at all any more: capacity is a number in the info
// block, not furniture on the battlefield.
//
// Ground band: y690-740, x560-860 - between the player (x330) and the enemy
// grid. Nothing else owns that strip. See UI_BANDS.md.
// =============================================================================
function trap_short_label(_name) {
    switch (_name) {
        case "Bear Trap":     return "BT";
        case "Spike Trap":    return "SPK";
        case "Death Snare":   return "DS";
        case "Tripline":      return "TRP";
        case "Warding Chime": return "WC";
        case "Wire Snare":    return "WS";
        case "Caltrops":      return "CLT";
    }
    return "TRP";
}

function trap_filter_color(_f) {
    switch (_f) {
        case "melee":  return make_color_rgb(214, 138, 84);
        case "ranged": return make_color_rgb(126, 186, 214);
        case "spell":  return make_color_rgb(178, 140, 226);
    }
    return make_color_rgb(226, 196, 120);
}

// Is any living enemy about to do something this trap answers? Shared by the
// field glyph, the buff chip and the info block so they can never disagree.
function trap_is_armed(_trap, _cs) {
    if (!is_struct(_cs)) return false;
    for (var _i = 0; _i < array_length(_cs.combatants); _i++) {
        var _c = _cs.combatants[_i];
        if (_c.is_player || _c.is_defeated) continue;
        if (!variable_struct_exists(_c, "intent") || _c.intent == undefined) continue;
        var _eab = _c.intent.eab;
        var _hos = (_eab == undefined) || (_eab.kind != "heal");
        var _dmg = (_eab == undefined) || (_eab.kind == "spell");
        var _rch = variable_struct_exists(_c, "reach") ? _c.reach : "melee";
        if (_eab != undefined && variable_struct_exists(_eab, "reach") && _eab.reach != "") _rch = _eab.reach;
        var _spl = (_eab != undefined && _eab.kind == "spell");
        if (trap_matches(_trap, _hos, _dmg, _rch, _spl)) return true;
    }
    return false;
}

// ---- 1. The trap object, sitting on the ground where it was thrown ----
// Code-drawn for now: sprung jaws in the filter's colour with a ground shadow, so
// it reads as resting ON the floor rather than floating on the HUD. The art that
// replaces this glyph is specced in SYSTEMS_TRAPS.md 8 (not generated yet).
// trap_field_pos(slot, cap) - the STATIC station where a trap in that slot sits.
// M 08-11: "2nd trap moves 1st trap - they should be in static assignable
// locations." A trap is stamped with its slot at DEPLOY time and never moves;
// stations exist for the whole fight whether occupied or not. Stations count
// from mid-corridor TOWARD the enemy (f = (slot+1)/cap) - the player end of
// the old diagonal (f<~0.4) sat on the pet companion's row (M's screenshot:
// chime prop drawn over the dog), so no station is placed there.
// The line runs up-and-right between player and enemy rows, the one genuinely
// empty corridor on the combat screen. Clearances, all measured - do not move
// these without re-checking every one:
//   combat log   x30-1200,  y735-945  -> lowest trap foot is 700 (35px clear)
//   enemy bars   x990-1885, y86-432   -> highest prop top is 468 (36px clear)
//   pet companion  feet up to ~(720, 726) -> nearest station (cap 2, f=.5) left
//                  prop edge is 806
//   enemy sprites  x>=1143 at 4 wide  -> rightmost prop edge is 1104
function trap_field_pos(_slot, _cap) {
    var _ax = 700, _ay = 700;            // player end of the diagonal (unused f=0)
    var _bx = 1040, _by = 596;           // enemy end
    _cap = max(1, _cap);
    var _f = (_slot + 1) / _cap;
    return { x: lerp(_ax, _bx, _f), y: lerp(_ay, _by, _f) };
}

// A trap's slot: stamped at deploy since 08-11; traps restored from older
// checkpoints fall back to their array index so they still spread out.
function trap_slot_of(_tr, _idx) {
    return (is_struct(_tr) && variable_struct_exists(_tr, "slot")) ? _tr.slot : _idx;
}

// The deployed prop art. Sprites are 64x64 authored side-on and drawn at 2x, the
// same chunky scale the 97px enemy sprites use at 3x. Returns -1 for any trap
// whose prop has not been authored yet - the caller falls back to the procedural
// jaw glyph, so a missing sprite degrades instead of crashing.
function trap_prop_sprite(_name) {
    switch (_name) {
        case "Bear Trap":     return spr_trap_prop_bear;
        case "Spike Trap":    return spr_trap_prop_spike;
        case "Death Snare":   return spr_trap_prop_snare;
        case "Tripline":      return spr_trap_prop_tripline;
        case "Warding Chime": return spr_trap_prop_chime;
        case "Wire Snare":    return spr_trap_prop_wire;
        case "Caltrops":      return spr_trap_prop_caltrops;
    }
    return -1;
}

function ui_draw_trap_field(_p, _cs) {
    if (!is_struct(_p) || !variable_struct_exists(_p, "traps") || !is_array(_p.traps)) return;
    var _n = array_length(_p.traps);
    if (_n <= 0) return;

    var _cap = trap_slots_max(_p);
    for (var _t = 0; _t < _n; _t++) {
        var _tr  = _p.traps[_t];
        var _pos = trap_field_pos(trap_slot_of(_tr, _t), _cap);
        var _cx  = _pos.x;
        var _gy  = _pos.y;               // ground line for THIS trap (the diagonal)
        var _col   = trap_filter_color(_tr.filter);
        var _armed = trap_is_armed(_tr, _cs);
        var _flash = (variable_struct_exists(_tr, "flash") && _tr.flash > 0);

        // Throw arc (P5, 08-11): a just-deployed trap FLIES from the player's
        // hands to its station on a lobbed arc instead of popping into place.
        // Draw-owned transient (the checkout-VFX idiom): advanced and cleared
        // here, so no Step timing changes. The armed ring waits until it lands.
        var _fly = false;
        var _sh_y = _gy;                 // where the ground shadow falls
        if (variable_global_exists("trap_throw") && global.trap_throw != undefined
            && global.trap_throw.name == _tr.name
            && global.trap_throw.slot == trap_slot_of(_tr, _t)) {
            var _th = global.trap_throw;
            var _ft = clamp(_th.t / _th.tmax, 0, 1);
            _cx   = lerp(660, _pos.x, _ft);
            _sh_y = lerp(690, _pos.y, _ft);                  // ground under the arc
            _gy   = _sh_y - 360 * _ft * (1 - _ft);           // peak ~90px mid-arc
            _fly = (_ft < 1);
            _th.t += 1;
            if (_th.t >= _th.tmax) global.trap_throw = undefined;
        }

        ui_draw_ground_shadow(_cx, _sh_y + 4, 54);

        // Armed: a slow breathing ring on the ground. This is the tell that an
        // enemy intends something this trap will catch.
        if ((_armed || _flash) && !_fly) {
            var _pl = _flash ? 1.0 : (0.30 + 0.30 * (0.5 + 0.5 * sin(current_time / 260)));
            draw_set_alpha(_pl);
            draw_set_color(_flash ? make_color_rgb(255, 236, 160) : _col);
            draw_ellipse(_cx - 34, _gy - 9, _cx + 34, _gy + 9, true);
            draw_ellipse(_cx - 30, _gy - 8, _cx + 30, _gy + 8, true);
            draw_set_alpha(1.0);
        }

        var _bright = _flash ? merge_color(_col, c_white, 0.6) : _col;
        var _prop   = trap_prop_sprite(_tr.name);
        if (_prop >= 0) {
            // Authored prop, bottom-centre anchored on the diagonal so it SITS on
            // the floor. Untinted: the art already carries the trap's identity, and
            // multiplying a filter colour over it muddies the palette. The filter
            // colour still reads through the armed ring and the charge pips.
            var _ps = 2;
            var _pw = sprite_get_width(_prop)  * _ps;
            var _ph = sprite_get_height(_prop) * _ps;
            draw_sprite_ext(_prop, 0, _cx - _pw * 0.5, _gy - _ph, _ps, _ps, 0, c_white, 1.0);
            if (_flash) {
                gpu_set_blendmode(bm_add);
                draw_sprite_ext(_prop, 0, _cx - _pw * 0.5, _gy - _ph, _ps, _ps, 0,
                                c_white, min(1.0, _tr.flash / 12.0) * 0.85);
                gpu_set_blendmode(bm_normal);
            }
        } else {
            // Fallback jaws: two rows of teeth facing each other across a sprung
            // plate. Kept so a trap without authored art still draws something.
            draw_set_color(merge_color(_bright, c_black, 0.45));
            draw_rectangle(_cx - 22, _gy - 3, _cx + 22, _gy + 3, false);
            draw_set_color(_bright);
            draw_rectangle(_cx - 22, _gy - 3, _cx + 22, _gy + 3, true);
            for (var _k = 0; _k < 5; _k++) {
                var _tx = _cx - 18 + _k * 9;
                draw_triangle(_tx, _gy - 3, _tx + 6, _gy - 3, _tx + 3, _gy - 13, false);
                draw_triangle(_tx, _gy + 3, _tx + 6, _gy + 3, _tx + 3, _gy + 13, false);
            }
        }
        if (_tr.charges > 1) {
            for (var _pi = 0; _pi < _tr.charges; _pi++) {
                draw_set_color(_col);
                draw_circle(_cx - 10 + _pi * 10, _gy + 22, 3, false);
            }
        }
        if (_flash) _tr.flash -= 1;
    }
    draw_set_color(c_white);
}

// ---- 3. Left-column reminder, under the buff row. Returns the height it used ----
// M: "we can keep the bear trap blocks melee box you created but it needs to be
// moved to the left handside and go below where buffs like SS reads."
function ui_draw_trap_info(_p, _x, _y, _cs) {
    if (!is_struct(_p) || !variable_struct_exists(_p, "class_id") || _p.class_id != 2) return 0;
    if (!variable_struct_exists(_p, "traps") || !is_array(_p.traps)) return 0;
    var _n = array_length(_p.traps);
    if (_n <= 0) return 0;

    var _w = 300, _rh = 42, _h = 26 + _n * _rh;
    draw_set_alpha(0.82);
    draw_set_color(make_color_rgb(12, 13, 20));
    draw_rectangle(_x, _y, _x + _w, _y + _h, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(58, 54, 44));
    draw_rectangle(_x, _y, _x + _w, _y + _h, true);

    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 142, 116));
    draw_text(_x + 10, _y + 5, "TRAPS SET  (" + string(_n) + "/" + string(trap_slots_max(_p)) + ")");

    for (var _t = 0; _t < _n; _t++) {
        var _tr = _p.traps[_t];
        var _ry = _y + 26 + _t * _rh;
        var _col   = trap_filter_color(_tr.filter);
        var _armed = trap_is_armed(_tr, _cs);
        draw_set_color(_armed ? merge_color(_col, c_white, 0.30) : _col);
        draw_text(_x + 10, _ry, ui_truncate(_tr.name, _w - 20));
        draw_set_color(_armed ? make_color_rgb(150, 210, 160) : make_color_rgb(118, 124, 144));
        draw_text(_x + 10, _ry + 20, ui_truncate(
            (_tr.block ? "blocks " : "punishes ") + trap_filter_label(_tr.filter)
            + (_armed ? "  - READY" : ""), _w - 20));
    }
    draw_set_font(-1);
    draw_set_color(c_white);
    return _h + 10;
}

// Visible rows in the SELL list. Was 7, which did not fit (M 08-08): rows start
// at y189 with a 126 pitch and a 117 height, so the 7th ran to y1062 - through
// the key legend at 1026 and out into the perimeter frame at 1050. A sell row
// holds three lines (name +12, stats +45, effect +75, ending ~+102), so the
// height can't be squeezed enough to fit seven without clipping the third line;
// six is the honest answer. Last row now ends at 936, clear of the confirm-bar
// band (960-1010). Read by the draw window, the Step scroll clamp AND the click
// hit-test - all five sites used to hardcode 7 independently.
function shop_sell_visible_rows() {
    return 6;
}

function ui_draw_option_row(_x1, _y1, _x2, _y2, _opt) {
    var _sel    = variable_struct_exists(_opt, "sel")    ? _opt.sel    : false;
    var _dim    = variable_struct_exists(_opt, "dim")    ? _opt.dim    : false;
    var _afford = variable_struct_exists(_opt, "afford") ? _opt.afford : true;
    var _accent = variable_struct_exists(_opt, "accent") ? _opt.accent : make_color_rgb(226, 198, 126);
    var _body   = variable_struct_exists(_opt, "body")   ? _opt.body   : "";
    var _tag    = variable_struct_exists(_opt, "tag")    ? _opt.tag    : "";

    // An unavailable row keeps its shape but loses its colour - it must still be
    // readable (the player needs to know the craft exists) without inviting a click.
    if (_dim) _accent = merge_color(_accent, make_color_rgb(60, 62, 76), 0.62);

    // ---- plate ----
    draw_set_color(_sel ? merge_color(make_color_rgb(18, 17, 26), _accent, 0.16)
                        : make_color_rgb(19, 18, 28));
    draw_rectangle(_x1, _y1, _x2, _y2, false);
    draw_set_color(_sel ? _accent : make_color_rgb(46, 48, 64));
    draw_rectangle(_x1, _y1, _x2, _y2, true);
    // Accent bar on the selected row's leading edge - carries the highlight even
    // for colour-blind readers, who can't rely on the fill tint alone.
    if (_sel) {
        draw_set_color(_accent);
        draw_rectangle(_x1, _y1, _x1 + 5, _y2, false);
    }

    var _tx = _x1 + 22;
    var _rx = _x2 - 20;

    // ---- cost chips ----
    // Built as a LIST first, then laid out against the real row height. The first
    // cut of this shipped a fixed 3-line stack (gold / dust / tag) starting at
    // y1+14, which needs 95px of row - Dorn's 84px temper rows pushed the third
    // chip straight through the bottom border into the row below (M screenshot,
    // 08-08). Rule: chips NEVER assume they fit. If the stack is taller than the
    // box, gold+dust collapse onto one line; the surviving stack is then centred.
    var _red = make_color_rgb(216, 104, 104);
    var _chip_t = [];   // text
    var _chip_c = [];   // colour
    var _has_g  = (variable_struct_exists(_opt, "gold") && _opt.gold > 0);
    var _has_d  = (variable_struct_exists(_opt, "dust") && _opt.dust > 0);
    var _rowh   = _y2 - _y1;
    var _cap    = max(1, (_rowh - 20) div 27);   // chip lines this row can hold

    // How many lines do we WANT? gold, dust, cost, tag.
    var _want = (_has_g ? 1 : 0) + (_has_d ? 1 : 0)
              + ((variable_struct_exists(_opt, "cost") && _opt.cost != "") ? 1 : 0)
              + ((_tag != "") ? 1 : 0);

    if (_has_g && _has_d && _want > _cap) {
        // Collapse the two currencies onto one line rather than dropping a chip -
        // the player must always be able to see the whole price.
        array_push(_chip_t, string(_opt.gold) + "g + " + string(_opt.dust) + " dust");
        array_push(_chip_c, _afford ? make_color_rgb(232, 200, 108) : _red);
    } else {
        if (_has_g) {
            array_push(_chip_t, string(_opt.gold) + "g");
            array_push(_chip_c, _afford ? make_color_rgb(232, 200, 108) : _red);
        }
        if (_has_d) {
            array_push(_chip_t, string(_opt.dust) + " dust");
            array_push(_chip_c, _afford ? make_color_rgb(126, 204, 224) : _red);
        }
    }
    if (variable_struct_exists(_opt, "cost") && _opt.cost != "") {
        array_push(_chip_t, _opt.cost);
        array_push(_chip_c, _afford ? make_color_rgb(198, 190, 214) : _red);
    }
    if (_tag != "") {
        array_push(_chip_t, _tag);
        array_push(_chip_c, variable_struct_exists(_opt, "tag_col") ? _opt.tag_col : make_color_rgb(140, 160, 195));
    }

    draw_set_font(ui_font(fnt_ui_small));
    draw_set_halign(fa_right); draw_set_valign(fa_top);
    var _chip_n = min(array_length(_chip_t), _cap);
    var _cost_w = 0;
    // Centre the surviving stack in the row so it can't touch either border.
    var _cy = _y1 + max(10, (_rowh - _chip_n * 27) div 2);
    for (var _ci = 0; _ci < _chip_n; _ci++) {
        draw_set_color(_chip_c[_ci]);
        draw_text(_rx, _cy, _chip_t[_ci]);
        _cost_w = max(_cost_w, string_width(_chip_t[_ci]));
        _cy += 27;
    }
    draw_set_halign(fa_left);

    // Text column stops clear of the widest chip (24px gutter), so a long craft
    // description can never run under the price - the collision this pass exists
    // to kill. Measured, not assumed.
    var _tw = (_rx - (_cost_w > 0 ? _cost_w + 24 : 0)) - _tx;

    // ---- title ----
    draw_set_font(ui_font(fnt_ui));
    var _tth = string_height("Ag");   // measured: the Large variant is taller
    draw_set_color(_sel ? merge_color(_accent, c_white, 0.35) : _accent);
    draw_text(_tx, _y1 + 8, ui_truncate(_opt.title, _tw));

    // ---- body ----
    // Offset measured under the title font (default resolves to the shipped
    // 41 exactly); at Large the fixed 41 ran the body onto the row border.
    if (_body != "") {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(_dim ? make_color_rgb(92, 96, 112)
                    : (_sel ? make_color_rgb(176, 182, 202) : make_color_rgb(134, 140, 160)));
        draw_text(_tx, _y1 + max(41, 12 + _tth), ui_truncate(_body, _tw));
    }
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_label_fit(cx, cy, str, box_w, box_h)
// Draws a label centered on (cx, cy) that always fits inside box_w x box_h: first
// tries one line at native scale; if too wide it wraps onto two balanced lines
// (split on the space nearest the middle) and uniformly scales them down to fit
// both the width and height. A single long word with no space is just scaled.
// Caller should set halign=center / valign=middle first (draw_text_transformed
// honours them). Used for ability button labels so long names like
// "Adrenaline Rush" stay readable and inside the button.
// ---------------------------------------------------------------------------
function ui_draw_label_fit(cx, cy, str, box_w, box_h) {
    var _w  = string_width(str);
    var _lh = string_height(str);

    // One line at native scale.
    if (_w <= box_w && _lh <= box_h) {
        draw_text(cx, cy, str);
        return;
    }

    // Find the space nearest the middle to split into two balanced lines.
    var _len   = string_length(str);
    var _best  = -1;
    var _bestd = _len;
    for (var _i = 1; _i <= _len; _i++) {
        if (string_char_at(str, _i) == " ") {
            var _d = abs(_i - _len / 2);
            if (_d < _bestd) { _bestd = _d; _best = _i; }
        }
    }

    // No space - single long word: scale the one line to fit.
    if (_best <= 0) {
        var _s1 = min(box_w / max(1, _w), box_h / max(1, _lh), 1);
        draw_text_transformed(cx, cy, str, _s1, _s1, 0);
        return;
    }

    // Two lines, scaled to fit width and the (two-line) height.
    var _line1 = string_copy(str, 1, _best - 1);
    var _line2 = string_copy(str, _best + 1, _len - _best);
    var _w2 = max(string_width(_line1), string_width(_line2));
    var _h2 = _lh * 2;
    var _s  = min(box_w / max(1, _w2), box_h / max(1, _h2), 1);
    var _half = (_lh * _s) / 2;
    draw_text_transformed(cx, cy - _half, _line1, _s, _s, 0);
    draw_text_transformed(cx, cy + _half, _line2, _s, _s, 0);
}

// ---------------------------------------------------------------------------
// ui_draw_sprite_cover(spr, subimg, x, y, w, h, alpha)
// Draws a sprite to exactly fill the (x,y,w,h) box WITHOUT distorting it:
// scales uniformly to cover the box and crops the centered overflow (CSS
// "object-fit: cover"). Use for portraits/art instead of draw_sprite_stretched,
// which squashes the image to the box aspect ratio.
// ---------------------------------------------------------------------------
// v_anchor picks WHERE the vertical crop bites: 0.5 = centered (default), 0 = keep the
// TOP of the art (portraits whose head reaches the canvas edge - centered crop scalps them).
function ui_draw_sprite_cover(spr, subimg, x, y, w, h, alpha, v_anchor = 0.5) {
    if (!sprite_exists(spr)) return;
    var _sw = sprite_get_width(spr);
    var _sh = sprite_get_height(spr);
    if (_sw <= 0 || _sh <= 0) return;
    var _scale = max(w / _sw, h / _sh);       // cover: largest scale that fills the box
    var _src_w = min(_sw, w / _scale);         // source sub-rect that maps onto the box
    var _src_h = min(_sh, h / _scale);
    var _src_l = (_sw - _src_w) * 0.5;         // centered crop (horizontal)
    var _src_t = (_sh - _src_h) * v_anchor;
    // draw_sprite_part_ext positions by the part's top-left and ignores origin.
    draw_sprite_part_ext(spr, subimg, _src_l, _src_t, _src_w, _src_h, x, y, _scale, _scale, c_white, alpha);
}

// Contain-fit: the WHOLE sprite scaled to fit inside the box, centered, letterboxed
// on the short axis (07-31, M: cover-crop was scalping portrait heads - "zoom out").
function ui_draw_sprite_contain(spr, subimg, x, y, w, h, alpha) {
    if (!sprite_exists(spr)) return;
    var _sw = sprite_get_width(spr);
    var _sh = sprite_get_height(spr);
    if (_sw <= 0 || _sh <= 0) return;
    var _scale = min(w / _sw, h / _sh);
    var _dx = x + (w - _sw * _scale) * 0.5;
    var _dy = y + (h - _sh * _scale) * 0.5;
    draw_sprite_part_ext(spr, subimg, 0, 0, _sw, _sh, _dx, _dy, _scale, _scale, c_white, alpha);
}

function ui_draw_ability_tooltip(x, anchor_bottom, ability, caster, panel_width = 480) {
    // ROUND 8 REBUILD (M: "match its height to the combat text box"): FIXED
    // BAND geometry - the card occupies exactly the log's vertical band
    // (210px tall, bottom at anchor_bottom), and the WIDTH absorbs what the
    // old height held: cost/class/AoE share one line, damage+hit another.
    var panel_w = panel_width;
    var panel_h = 198;   // matches the combat log band exactly (top y747)
    var padding = 18;
    var line_h  = 30;
    var _py     = anchor_bottom - panel_h;
    var _ew     = panel_w - padding * 2;

    // DETONATOR hover-explain (M 08-13: "detonator as a key word in
    // descriptions should be hoverable with a popup explanation"): hovering
    // the card of any detonator ability queues the full reaction table into
    // the shared status-tip slot (drawn topmost after the HUD).
    if (ability_is_detonator(ability)) {
        var _dmx = device_mouse_x_to_gui(0), _dmy = device_mouse_y_to_gui(0);
        if (_dmx >= x && _dmx <= x + panel_w && _dmy >= _py && _dmy <= anchor_bottom) {
            global.combat_status_tip = { x: _dmx, y: _dmy, se: {
                name: "DETONATOR", color: make_color_rgb(255, 200, 90), duration: 0, dur_noun: "",
                desc: "Detonators REACT with the target's strongest status on hit: Bleed bursts (+5 per remaining tick, consumed), Chill SHATTERS (+30%), Burn grants +40% crit, Stun guarantees the crit, Blind cannot miss, Poison spreads Mortality, Void heals you, Shock arcs to the others. Hexed targets take DOUBLED reactions." } };
        }
    }

    draw_set_font(ui_font(fnt_ui_small));
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);

    // ---- Content strings, computed up front ----
    var _ac_lbl  = ability_attack_class_label(ability_attack_class(ability));
    var _sch_lbl = school_label(ability_school(ability));
    var _class_line = (_sch_lbl != "" && _ac_lbl != "") ? (_sch_lbl + " - " + _ac_lbl)
                    : (_sch_lbl != "" ? _sch_lbl : _ac_lbl);
    var _is_aoe  = variable_struct_exists(ability, "is_aoe") && ability.is_aoe;
    var _has_dmg = variable_struct_exists(ability, "base_damage") && ability.base_damage > 0;

    var cost_str = "AP: " + string(ability.energy_cost);
    if (variable_struct_exists(ability, "secondary_cost") && ability.secondary_cost > 0) {
        var sec_label = "Resource";
        if (variable_struct_exists(caster, "souls"))            sec_label = "Souls";
        else if (variable_struct_exists(caster, "blood"))       sec_label = "Blood";
        else if (variable_struct_exists(caster, "preparation")) sec_label = "Prep";
        cost_str += " | " + sec_label + ": " + string(ability_secondary_cost_eff(ability, caster));
    }

    var _dmg_str = "";
    if (_has_dmg) {
        var dmg_type_str = "physical";
        if (variable_struct_exists(ability, "damage_type")) {
            if (ability.damage_type == 1)      dmg_type_str = "elemental";
            else if (ability.damage_type == 3) dmg_type_str = "blood";
            else if (ability.damage_type == 2) dmg_type_str = "drain";
        }
        _dmg_str = "Damage: " + string(ability.base_damage) + " (" + dmg_type_str + ")";
    }

    // Hit chance / always-hits (same math as before - stage1 accuracy, then
    // the selected target's dodge).
    var _hit_str = "Always hits";
    var _hit_col = c_lime;
    if (!(variable_struct_exists(ability, "guaranteed_hit") && ability.guaranteed_hit)) {
        var _acc_bonus = variable_struct_exists(caster, "acc") ? caster.acc : 0;
        var _blind_pen = round(combat_status_max(caster, "blind") * 100);
        var _stage1 = clamp(ability.base_acc + _acc_bonus - _blind_pen + rune_aspect_ranged_acc(ability), 5, 99);
        var _tgt_dodge = 0;
        if (instance_exists(obj_combat_controller)) {
            var _cc_tip = instance_find(obj_combat_controller, 0);
            if (variable_instance_exists(_cc_tip, "selected_target") && variable_instance_exists(_cc_tip, "combat_state")) {
                var _li_t  = 0;
                var _cbt_t = _cc_tip.combat_state.combatants;
                for (var _ti = 0; _ti < array_length(_cbt_t); _ti++) {
                    var _tc = _cbt_t[_ti];
                    if (_tc.is_player || _tc.is_defeated) continue;
                    if (_li_t == _cc_tip.selected_target) { _tgt_dodge = clamp(_tc.dodge, 0, 90); break; }
                    _li_t++;
                }
            }
        }
        var _net = round(_stage1 * (1 - _tgt_dodge / 100));
        _hit_str = (_tgt_dodge > 0)
            ? ("Hit: " + string(_net) + "%  (Acc " + string(_stage1) + "% - Dodge " + string(_tgt_dodge) + "%)")
            : ("Hit: " + string(_stage1) + "%");
        _hit_col = c_ltgray;
    }

    var effect_str = ability_effect_full(ability);
    var _ac_tag = ability_attack_class_tag(ability);
    if (_ac_tag != "") effect_str = (effect_str != "") ? (effect_str + " " + _ac_tag) : _ac_tag;

    // ---- Panel ----
    draw_set_alpha(0.92);
    draw_set_color(make_color_rgb(20, 25, 40));
    draw_rectangle(x, _py, x + panel_w, _py + panel_h, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(80, 120, 160));
    draw_rectangle(x, _py, x + panel_w, _py + panel_h, true);
    ui_draw_gothic_frame(x, _py, x + panel_w, _py + panel_h);

    var tx    = x + padding;
    var cur_y = _py + 14;

    // Icon, top-right.
    ui_draw_ability_icon(x + panel_w - padding - 48, _py + 14, 48, ability);

    // Row 1: name (fake bold).
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(40, 50, 70));
    draw_text(tx + 2, cur_y + 2, ability.name);
    draw_set_color(c_white);
    draw_text(tx, cur_y, ability.name);
    draw_set_font(ui_font(fnt_ui_small));
    cur_y += 38;

    // Row 2: cost | school-class | AoE - one line, segment-colored.
    var _r2x = tx;
    draw_set_color(c_yellow);
    draw_text(_r2x, cur_y, cost_str);
    _r2x += string_width(cost_str);
    if (_class_line != "") {
        draw_set_color(make_color_rgb(150, 175, 210));
        draw_text(_r2x, cur_y, "   |   " + _class_line);
        _r2x += string_width("   |   " + _class_line);
    }
    if (_is_aoe) {
        if (trait_active("Focused Power")) {
            draw_set_color(make_color_rgb(255, 150, 60));
            draw_text(_r2x, cur_y, "   |   Targets: SELECTED (Focused Power +50%)");
        } else {
            draw_set_color(make_color_rgb(255, 120, 120));
            draw_text(_r2x, cur_y, "   |   Targets: ALL enemies");
        }
    }
    cur_y += line_h;

    // Row 3: damage | hit - one line.
    var _r3x = tx;
    if (_dmg_str != "") {
        draw_set_color(c_white);
        draw_text(_r3x, cur_y, _dmg_str);
        _r3x += string_width(_dmg_str);
        draw_set_color(_hit_col);
        draw_text(_r3x, cur_y, "   |   " + _hit_str);
    } else {
        draw_set_color(_hit_col);
        draw_text(_r3x, cur_y, _hit_str);
    }
    cur_y += line_h + 4;

    // Effect text: wrapped into the remaining band, truncated with "..." if it
    // would spill past the footer ([V] carries the full breakdown).
    var _eff_bottom = _py + panel_h - padding - 26;
    if (effect_str != "") {
        var _fit_str = effect_str;
        var _guard = 0;
        while (string_height_ext(_fit_str, 26, _ew) > (_eff_bottom - cur_y) && _guard < 60) {
            var _cut = string_length(_fit_str);
            var _sp  = 0;
            for (var _ci = _cut; _ci >= 1; _ci--) {
                if (string_char_at(_fit_str, _ci) == " ") { _sp = _ci; break; }
            }
            if (_sp <= 1) break;
            _fit_str = string_copy(_fit_str, 1, _sp - 1) + "...";
            _guard++;
        }
        draw_set_color(c_white);
        draw_text_ext(tx, cur_y, _fit_str, 26, _ew);
    }

    // Footer hint, pinned to the band's bottom.
    draw_set_color(make_color_rgb(150, 160, 190));
    draw_text(tx, _py + panel_h - padding - 22, (input_device() == 2) ? "Hold an ability to examine" : "[V] Ability details");

    draw_set_font(-1);
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
}

// ---------------------------------------------------------------------------
// ui_draw_combat_hud(combat_state, player, ability_array, selected_ability_index, log_array)
// Master draw function - calls all component functions at their correct positions.
//
// Layout (native 1920x1080; combat fonts fnt_ui / fnt_ui_small / fnt_ui_title):
//   Top-left       Player HP bar         (30,  30) w375 h36
//   Below HP       Energy pips           (20,  56)
//   Below energy   Secondary resource    (20,  90) w250 h16
//   Top-center     Turn queue            (400, 10)
//   Bottom-center  Ability buttons       (160, 640)
//   Bottom-left    Combat log            (20,  200) w440 h280
//   Lower-right    Ability tooltip       x940 w320, bottom-anchored y630 (auto-h)
//   Top overlay    Telegraph warning     (full-width, only when active)
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// ui_boon_style(id) - visual style for a boon id: short abbreviation + badge
// color. Single source shared by the combat strip and (names only) the menu.
// Falls back to a neutral badge for unknown ids so new boons still render.
// ---------------------------------------------------------------------------
function ui_boon_style(id) {
    switch (id) {
        case "bloodlust":   return { abbr:"DMG",   col:make_color_rgb(200,  70,  60) };
        case "ironhide":    return { abbr:"HP+",   col:make_color_rgb(110, 130, 160) };
        case "duelist":     return { abbr:"CRIT",  col:make_color_rgb(220, 180,  70) };
        case "vampirism":   return { abbr:"LIFE",  col:make_color_rgb(180,  50,  90) };
        case "warding":     return { abbr:"WARD",  col:make_color_rgb( 80, 140, 200) };
        case "greed":       return { abbr:"GOLD",  col:make_color_rgb(210, 180,  60) };
        case "runic":       return { abbr:"DUST",  col:make_color_rgb(150, 110, 200) };
        case "executioner": return { abbr:"EXEC",  col:make_color_rgb(180,  60,  50) };
        case "aegis":       return { abbr:"AEGIS", col:make_color_rgb( 90, 170, 180) };
        case "glasscannon": return { abbr:"GLASS", col:make_color_rgb(220, 110,  70) };
        default:            return { abbr:"BOON",  col:make_color_rgb(150, 150, 170) };
    }
}

// ---------------------------------------------------------------------------
// ui_draw_active_boons(x, y) - vertical "BOONS" strip for the combat HUD.
// Boons are run-scoped (no per-turn duration) so this is a static legend:
// an abbr-badge + the boon's name per active boon. No-op when none active.
// ---------------------------------------------------------------------------
function ui_draw_active_boons(x, y) {
    if (!variable_global_exists("run_boons")) return;
    var _n = array_length(global.run_boons);
    if (_n == 0) return;

    draw_set_font(ui_font(fnt_ui_small));
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(150, 140, 110));
    draw_text(x, y, "BOONS");

    var _ry = y + 24;
    for (var _i = 0; _i < _n; _i++) {
        var _b = boon_get(global.run_boons[_i]);
        if (_b == undefined) continue;
        var _st = ui_boon_style(global.run_boons[_i]);

        // colored badge with short code
        draw_set_alpha(0.9);
        draw_set_color(_st.col);
        draw_roundrect(x, _ry, x + 60, _ry + 21, false);
        draw_set_alpha(1.0);
        draw_set_color(make_color_rgb(10, 10, 18));
        draw_roundrect(x, _ry, x + 60, _ry + 21, true);
        draw_set_color(c_white);
        draw_set_halign(fa_center);
        draw_set_valign(fa_middle);
        draw_text(x + 30, _ry + 11, _st.abbr);
        draw_set_halign(fa_left);
        draw_set_valign(fa_top);

        // boon name to the right of the badge
        draw_set_color(make_color_rgb(200, 205, 215));
        draw_text(x + 69, _ry + 2, _b.name);

        _ry += 27;
    }

    draw_set_font(-1);
    draw_set_alpha(1.0);
    draw_set_color(c_white);
}

// ---------------------------------------------------------------------------
// ui_curse_style(id) - short badge code + color for an active curse.
// Falls back to a neutral red badge for unknown ids.
// ---------------------------------------------------------------------------
function ui_curse_style(id) {
    switch (id) {
        case "frail":      return { abbr:"FRAIL", col:make_color_rgb(150, 110, 120) };
        case "famine":     return { abbr:"FMNE",  col:make_color_rgb(160, 130,  90) };
        case "exposed":    return { abbr:"EXPO",  col:make_color_rgb(200,  90,  80) };
        case "bloodprice": return { abbr:"BLD",   col:make_color_rgb(180,  40,  60) };
        case "savagery":   return { abbr:"SVG",   col:make_color_rgb(190,  70,  60) };
        case "withered":   return { abbr:"WTHR",  col:make_color_rgb(120, 130, 110) };
        case "doom":       return { abbr:"DOOM",  col:make_color_rgb(150,  50,  60) };
        case "damnation":  return { abbr:"DMN",   col:make_color_rgb(170,  50,  70) };
        case "ruin":       return { abbr:"RUIN",  col:make_color_rgb(140,  60,  70) };
        case "devilspact": return { abbr:"PACT",  col:make_color_rgb(190,  40,  50) };
        default:           return { abbr:"CURSE", col:make_color_rgb(170,  70,  80) };
    }
}

// ---------------------------------------------------------------------------
// ui_draw_active_curses(x, y) - vertical "CURSES" strip for the combat HUD.
// Mirrors ui_draw_active_boons (static legend; curses are run-scoped). No-op
// when none active. Returns the next free y so callers can stack panels.
// ---------------------------------------------------------------------------
function ui_draw_active_curses(x, y) {
    if (!variable_global_exists("run_curses")) return y;
    var _n = array_length(global.run_curses);
    if (_n == 0) return y;

    draw_set_font(ui_font(fnt_ui_small));
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(170, 90, 90));
    draw_text(x, y, "CURSES");

    var _ry = y + 24;
    for (var _i = 0; _i < _n; _i++) {
        var _c = curse_get(global.run_curses[_i]);
        if (_c == undefined) continue;
        var _st = ui_curse_style(global.run_curses[_i]);

        draw_set_alpha(0.9);
        draw_set_color(_st.col);
        draw_roundrect(x, _ry, x + 60, _ry + 21, false);
        draw_set_alpha(1.0);
        draw_set_color(make_color_rgb(10, 10, 18));
        draw_roundrect(x, _ry, x + 60, _ry + 21, true);
        draw_set_color(c_white);
        draw_set_halign(fa_center);
        draw_set_valign(fa_middle);
        draw_text(x + 30, _ry + 11, _st.abbr);
        draw_set_halign(fa_left);
        draw_set_valign(fa_top);

        draw_set_color(make_color_rgb(215, 180, 185));
        draw_text(x + 69, _ry + 2, _c.name);

        _ry += 27;
    }

    draw_set_font(-1);
    draw_set_alpha(1.0);
    draw_set_color(c_white);
    return _ry + 9;
}

// ---------------------------------------------------------------------------
// ui_draw_settings_overlay() - audio settings panel (Music + SFX sliders).
// Reads global.music_volume / global.sfx_volume / global.settings_cursor (see
// the audio_settings_* helpers in scr_stats). Called from the title + hub when
// global.settings_open. Draw-only; input is handled by audio_settings_handle_input.
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// ui_draw_pause_menu() - Resume / Settings / Quit to Title overlay. Drawn by each
// room controller (the persistent game_controller's Draw GUI doesn't fire). When
// the Settings sub-screen is open it is drawn instead (by ui_draw_settings_overlay).
// Geometry MUST match pause_menu_step()'s mouse hit-testing in scr_stats.
// ---------------------------------------------------------------------------
function ui_draw_pause_menu() {
    if (!variable_global_exists("pause_open") || !global.pause_open) return;
    // While Settings is open over the pause menu, let the settings overlay own the screen.
    if (variable_global_exists("settings_open") && global.settings_open) return;

    // Dim
    draw_set_alpha(0.72);
    draw_set_color(make_color_rgb(6, 8, 14));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    // Panel - height grows with the row list (the hub adds a "Save Game" row;
    // pause_menu_options is the single source, shared with the Step hit-test).
    var _labels  = pause_menu_options();
    var _n_rows  = array_length(_labels);
    var _pw = 540, _ph = 450 + (_n_rows - 3) * 84, _px = GUI_CX - _pw / 2, _py = 315;
    draw_set_color(make_color_rgb(20, 22, 34));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, false);
    draw_set_color(make_color_rgb(90, 110, 160));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, true);

    draw_set_halign(fa_center);
    draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(c_white);
    draw_text(GUI_CX, _py + 33, "Paused");

    var _cur     = global.pause_cursor;
    var _row_h   = 84, _first_y = 468, _bx0 = 735, _bx1 = 1185;
    draw_set_font(ui_font(fnt_ui));
    for (var _r = 0; _r < _n_rows; _r++) {
        var _ry = _first_y + _r * _row_h;
        var _on = (_r == _cur);
        draw_set_color(_on ? make_color_rgb(45, 55, 86) : make_color_rgb(26, 28, 40));
        draw_rectangle(_bx0, _ry, _bx1, _ry + 66, false);
        draw_set_color(_on ? make_color_rgb(120, 160, 230) : make_color_rgb(60, 66, 90));
        draw_rectangle(_bx0, _ry, _bx1, _ry + 66, true);
        draw_set_color(_on ? c_white : make_color_rgb(180, 188, 205));
        draw_text(GUI_CX, _ry + 18, ((_labels[_r] == "Quit Game") ? pause_quit_label() : _labels[_r]));
    }

    // "Game saved." flash for ~2s after the hub Save Game row is used.
    if (variable_global_exists("pause_saved_time") && current_time - global.pause_saved_time < 2000) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(120, 210, 150));
        draw_text(GUI_CX, _py + 84, "Game saved.");
    }

    // Controls legend - auto-scaled to fit inside the panel's inner width so it can
    // never spill past the side borders (panel is only _pw wide). Centered at GUI_CX,
    // and kept above the panel's bottom edge (_py + _ph). Shrink-to-fit is dynamic.
    var _legend    = "W/S: Navigate    Enter: Select    Esc: Resume";
    var _legend_pad = 36;
    draw_set_font(ui_font(fnt_ui_small));
    var _legend_sc  = min(1.0, (_pw - _legend_pad) / max(1, string_width(_legend)));
    var _legend_y   = _py + _ph - 39;
    draw_set_color(make_color_rgb(110, 118, 140));
    draw_text_transformed(GUI_CX, _legend_y, _legend, _legend_sc, _legend_sc, 0);
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// Coach-mark body keyword palette (M 08-13: "color coding certain words makes
// it easier to read" - the forge tips were walls of one-color text). Rules:
// [K] key chips cyan, rarity names their loot colors, gold/dust their resource
// colors, ALL-CAPS mechanic words (SMELT, CRAFT, TEMPER...) the title gold.
// Returns -1 for "keep the base body color".
// ---------------------------------------------------------------------------
function __ui_tip_word_color(_w) {
    var _core = string_lower(string_trim_punct_log(_w));
    if (string_length(_w) >= 3 && string_char_at(_w, 1) == "[") return make_color_rgb(110, 200, 220);
    if (_core == "common")    return make_color_rgb(165, 170, 180);
    if (_core == "uncommon")  return make_color_rgb(100, 205, 100);
    if (_core == "rare")      return make_color_rgb( 90, 155, 235);
    if (_core == "epic" || _core == "epics") return make_color_rgb(190, 110, 235);
    if (_core == "legendary" || _core == "legendaries") return make_color_rgb(235, 150, 60);
    if (_core == "gold")      return make_color_rgb(240, 200, 70);
    if (_core == "dust" || _core == "rune") return make_color_rgb(90, 195, 185);
    var _st = string_trim_punct_log(_w);
    if (string_length(_st) >= 2 && _st == string_upper(_st) && _st != string_lower(_st))
        return make_color_rgb(228, 200, 130);
    return -1;
}

// ui_tip_wrap_lines(txt, wrap_w) - word-wraps to the CURRENT font, honoring
// explicit \n. Returns [{words:[...], w:pixel_width}] so the tip box can size
// itself and center each line. Shared by measure + draw so they never drift.
function ui_tip_wrap_lines(_txt, _wrap_w) {
    var _lines = [];
    var _sp_w  = string_width(" ");
    var _paras = string_split(_txt, "\n");
    for (var _p = 0; _p < array_length(_paras); _p++) {
        var _words = string_split(_paras[_p], " ");
        var _cur = []; var _cw = 0;
        for (var _i = 0; _i < array_length(_words); _i++) {
            if (_words[_i] == "") continue;
            var _ww = string_width(_words[_i]);
            var _add = (_cw == 0) ? _ww : _cw + _sp_w + _ww;
            if (_cw > 0 && _add > _wrap_w) {
                array_push(_lines, { words: _cur, w: _cw });
                _cur = [_words[_i]]; _cw = _ww;
            } else {
                array_push(_cur, _words[_i]); _cw = _add;
            }
        }
        array_push(_lines, { words: _cur, w: _cw });
    }
    return _lines;
}

// ---------------------------------------------------------------------------
// ui_draw_tutorial_tip() - contextual onboarding coach-mark (see
// SYSTEMS_ONBOARDING.md). Draws a dimmed backdrop + a gothic-framed tip box for
// global.tutorial_active. Body wraps to the box and tints key words (see
// __ui_tip_word_color); box height adapts to the body. No-op when no tip is
// active. Call LAST in a surface's Draw so it sits on top.
// ---------------------------------------------------------------------------
function ui_draw_tutorial_tip() {
    if (!tutorial_is_active()) return;
    var _t = tutorial_get(global.tutorial_active);
    if (_t == undefined) { global.tutorial_active = ""; return; }

    // Dim the screen behind the tip.
    draw_set_alpha(0.78);
    draw_set_color(make_color_rgb(6, 8, 14));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    var _bw   = 990;
    var _wrap = _bw - 120;           // body wrap width (inside L/R padding)

    draw_set_halign(fa_center);
    draw_set_valign(fa_top);

    // Size the box to the wrapped body so long tips never overflow. The wrap
    // math is shared with the draw loop below (ui_tip_wrap_lines).
    draw_set_font(ui_font(fnt_ui));
    var _tls = ui_tip_wrap_lines(_t.body, _wrap);
    var _tlh = string_height("Ay") + 4;
    var _body_h = array_length(_tls) * _tlh;
    var _bh = 108 + _body_h + 78;    // title band + body + footer band
    var _bx = GUI_CX - _bw / 2;
    var _by = GUI_CY - _bh / 2;

    // Panel
    draw_set_color(make_color_rgb(18, 20, 30));
    draw_rectangle(_bx, _by, _bx + _bw, _by + _bh, false);

    // Title
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(228, 200, 130));
    draw_text(GUI_CX, _by + 33, _t.title);

    // Divider under the title
    draw_set_color(make_color_rgb(70, 64, 48));
    draw_line(_bx + 45, _by + 90, _bx + _bw - 45, _by + 90);

    // Body - wrapped, centered, keyword-tinted (M 08-13 readability pass).
    draw_set_font(ui_font(fnt_ui));
    draw_set_halign(fa_left);
    var _tip_base = make_color_rgb(205, 210, 222);
    var _tip_spw  = string_width(" ");
    for (var _tl = 0; _tl < array_length(_tls); _tl++) {
        var _ln = _tls[_tl];
        var _wx = GUI_CX - _ln.w / 2;
        var _wy = _by + 111 + _tl * _tlh;
        for (var _twi = 0; _twi < array_length(_ln.words); _twi++) {
            var _twc = __ui_tip_word_color(_ln.words[_twi]);
            draw_set_color((_twc == -1) ? _tip_base : _twc);
            draw_text(_wx, _wy, _ln.words[_twi]);
            _wx += string_width(_ln.words[_twi]) + _tip_spw;
        }
    }
    draw_set_halign(fa_center);

    // Footer hint
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(120, 128, 150));
    draw_text(GUI_CX, _by + _bh - 45, "Press any key or click to continue");

    // Ornate gothic rim (surrounds the box outward; box is centered with screen room).
    draw_set_font(-1);
    ui_draw_gothic_frame(_bx, _by, _bx + _bw, _by + _bh, 30);

    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
}

// Resolve a base UI font to its Small/Large variant per the Font Size setting
// (global.font_size_mode: 0 Small, 1 Default, 2 Large; persisted in settings.ini).
// Variant assets are looked up by NAME once and cached, so this compiles and
// runs before the variant fonts are imported - a missing variant just falls
// back to the base font (the selector still works, text simply doesn't change).
// Title font (Cinzel) is deliberately never scaled - it is decorative and its
// layouts are tuned to its fixed size.
// ui_font_dense(f) - font for DENSE, fixed-pitch panels (Bairc's stable/profile
// box: 30px rows, bars, chips). The LARGE accessibility size cannot fit those
// pitches (M 08-18 shot: rows and bars overlapping), so dense panels keep the
// base size in Large mode; the small mode still applies (it only tightens).
// ui_hint(kb, touch) - device-aware hint text (M 08-18: "did we remove all the keyboard
// commands on mobile? they waste space and are confusing"). Keyboard/pad callers keep the
// bracketed key; touch gets the tap wording ("" = draw nothing).
function ui_hint(_kb, _touch) { return (input_device() == 2) ? _touch : _kb; }
// ui_strip_keys(s) - drop "[X]" key tokens (and the doubled spaces they leave) for touch labels.
function ui_strip_keys(_s) {
    while (true) {
        var _a = string_pos("[", _s); if (_a <= 0) break;
        var _b = string_pos("]", _s); if (_b < _a) break;
        _s = string_delete(_s, _a, _b - _a + 1);
    }
    while (string_pos("  ", _s) > 0) _s = string_replace(_s, "  ", " ");
    while (string_length(_s) > 0 && string_char_at(_s, 1) == " ") _s = string_delete(_s, 1, 1);
    while (string_length(_s) > 0 && string_char_at(_s, string_length(_s)) == " ") _s = string_delete(_s, string_length(_s), 1);
    return _s;
}

function ui_font_dense(_f) {
    var _m = variable_global_exists("font_size_mode") ? global.font_size_mode : 1;
    if (_m == 2) return _f;
    return ui_font(_f);
}

function ui_font(_f) {
    var _m = variable_global_exists("font_size_mode") ? global.font_size_mode : 1;
    if (_m == 1) return _f;
    if (!variable_global_exists("__font_variants")) {
        global.__font_variants = [
            asset_get_index("fnt_ui_sm"),       asset_get_index("fnt_ui_lg"),
            asset_get_index("fnt_ui_small_sm"), asset_get_index("fnt_ui_small_lg"),
        ];
    }
    var _fv = global.__font_variants;
    var _r = -1;
    if (_f == fnt_ui)            _r = (_m == 0) ? _fv[0] : _fv[1];
    else if (_f == fnt_ui_small) _r = (_m == 0) ? _fv[2] : _fv[3];
    return (_r >= 0) ? _r : _f;
}

function ui_draw_settings_overlay() {
    audio_settings_init();
    video_settings_init();
    touch_settings_init();

    // Dim the screen behind the panel
    draw_set_alpha(0.78);
    draw_set_color(make_color_rgb(6, 8, 14));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    // Panel (tall enough for: Music, SFX, Hub Music, Dungeon Music, Menu Tick,
    // Fullscreen, Font Size, Tutorial Tips, On-screen D-pad, Pinch Zoom,
    // Reset Tutorial). The D-pad + Pinch Zoom rows only exist on touch
    // platforms - desktop/HTML5 skip them and the panel shrinks. On touch the
    // row pitches are squeezed (108->92 slider rows, 84->72 toggle rows, header
    // 150->136) so the 11th row fits the height-capped 1064 panel; highlight
    // bands are 66 tall, so 72 still leaves a 6px gap - measured, no overlap
    // (content bottom py+973 < reset flash py+986 < footer py+1022).
    var _has_dpad = touch_platform();
    var _pw = 840, _ph = _has_dpad ? 1064 : 1062;
    var _px = GUI_CX - _pw / 2;
    var _py = GUI_CY - _ph / 2;
    draw_set_color(make_color_rgb(18, 22, 36));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, false);
    draw_set_color(make_color_rgb(80, 140, 220));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, true);

    // Title
    draw_set_halign(fa_center);
    draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(c_white);
    draw_text(GUI_CX, _py + 39, "SETTINGS");

    var _row_y  = _py + (_has_dpad ? 136 : 150);
    var _row_h  = _has_dpad ? 92 : 108;    // slider-row pitch (squeezed on touch)
    var _pitch  = _has_dpad ? 72 : 84;     // toggle-row pitch (squeezed on touch)
    var _bar_x  = _px + 300;
    var _bar_w  = 420;
    var _bar_h  = 27;

    // --- Two slider rows: Music, SFX ---
    var _labels = ["Music", "Sound Effects"];
    var _vols   = [global.music_volume, global.sfx_volume];

    for (var _i = 0; _i < 2; _i++) {
        var _ry  = _row_y + _i * _row_h;
        var _sel = (global.settings_cursor == _i);

        // Selection highlight
        if (_sel) {
            draw_set_alpha(0.20);
            draw_set_color(make_color_rgb(80, 140, 220));
            draw_rectangle(_px + 30, _ry - 21, _px + _pw - 30, _ry + 45, false);
            draw_set_alpha(1.0);
        }

        // Label
        draw_set_halign(fa_left);
        draw_set_valign(fa_middle);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_sel ? c_white : make_color_rgb(170, 180, 200));
        draw_text(_px + 60, _ry + 12, (_sel ? "> " : "  ") + _labels[_i]);

        // Slider track
        var _by = _ry + 3;
        draw_set_color(make_color_rgb(35, 42, 60));
        draw_rectangle(_bar_x, _by, _bar_x + _bar_w, _by + _bar_h, false);
        // Slider fill
        var _fill = floor(_bar_w * clamp(_vols[_i], 0, 1));
        draw_set_color(_sel ? make_color_rgb(90, 170, 235) : make_color_rgb(60, 110, 150));
        if (_fill > 0) draw_rectangle(_bar_x, _by, _bar_x + _fill, _by + _bar_h, false);
        // Track border
        draw_set_color(make_color_rgb(70, 85, 110));
        draw_rectangle(_bar_x, _by, _bar_x + _bar_w, _by + _bar_h, true);

        // Percentage
        draw_set_halign(fa_left);
        draw_set_color(c_white);
        draw_text(_bar_x + _bar_w + 24, _by + _bar_h / 2, string(round(_vols[_i] * 100)) + "%");
    }

    // --- Rows 2/3: Hub Music / Dungeon Music track selectors (banshee jukebox,
    //     BANSHEE_BOTTLE_SPEC.md). A/D cycles Default + that pool's freed songs;
    //     the row is greyed until a spirit has been released. Per-save selection. ---
    banshee_init();
    var _trk_labels = ["Hub Music", "Dungeon Music"];
    var _trk_pools  = ["hub", "dungeon"];
    for (var _ti = 0; _ti < 2; _ti++) {
        var _ty   = _row_y + (2 + _ti) * _row_h;
        var _tsel2 = (global.settings_cursor == 2 + _ti);
        var _tpool = music_pool_unlocked(_trk_pools[_ti]);
        var _thas  = (array_length(_tpool) > 0);
        var _tcur  = music_selected_track(_trk_pools[_ti]);

        if (_tsel2) {
            draw_set_alpha(0.20);
            draw_set_color(make_color_rgb(80, 140, 220));
            draw_rectangle(_px + 30, _ty - 21, _px + _pw - 30, _ty + 45, false);
            draw_set_alpha(1.0);
        }
        draw_set_halign(fa_left);
        draw_set_valign(fa_middle);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_tsel2 ? c_white : make_color_rgb(170, 180, 200));
        draw_text(_px + 60, _ty + 12, (_tsel2 ? "> " : "  ") + _trk_labels[_ti]);

        // Value box - same footprint as the slider track so the column lines up.
        var _tby = _ty + 3;
        draw_set_color(make_color_rgb(35, 42, 60));
        draw_rectangle(_bar_x, _tby, _bar_x + _bar_w, _tby + _bar_h + 6, false);
        draw_set_color(_tsel2 ? make_color_rgb(120, 190, 255) : make_color_rgb(70, 85, 110));
        draw_rectangle(_bar_x, _tby, _bar_x + _bar_w, _tby + _bar_h + 6, true);
        draw_set_halign(fa_center);
        if (_thas) {
            var _tname = (_tcur == undefined) ? "Default" : _tcur.name;
            draw_set_color(_tcur == undefined ? c_white : make_color_rgb(150, 235, 235));
            // Shrink-to-fit: track names must never kiss the < > arrows.
            var _tval_sc = min(1.0, (_bar_w - 108) / max(1, string_width(_tname)));
            draw_text_transformed(_bar_x + _bar_w / 2, _tby + (_bar_h + 6) / 2, _tname, _tval_sc, _tval_sc, 0);
            draw_set_color(_tsel2 ? make_color_rgb(120, 190, 255) : make_color_rgb(90, 100, 125));
            draw_text(_bar_x + 21,          _tby + (_bar_h + 6) / 2, "<");
            draw_text(_bar_x + _bar_w - 21, _tby + (_bar_h + 6) / 2, ">");
        } else {
            draw_set_color(make_color_rgb(95, 100, 118));
            draw_text(_bar_x + _bar_w / 2, _tby + (_bar_h + 6) / 2, "No songs freed yet");
        }
        draw_set_halign(fa_left);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(140, 150, 170));
        draw_text(_bar_x + _bar_w + 24, _tby + (_bar_h + 6) / 2, _thas ? "(A/D)" : "(Maren)");
        draw_set_font(ui_font(fnt_ui));
    }

    // --- Fifth row: Menu Tick (nav glass ping) toggle ---
    var _tick_on = (!variable_global_exists("ui_tick_enabled")) || global.ui_tick_enabled;
    var _ky   = _row_y + 4 * _row_h;
    var _ksel = (global.settings_cursor == 4);
    if (_ksel) {
        draw_set_alpha(0.20);
        draw_set_color(make_color_rgb(80, 140, 220));
        draw_rectangle(_px + 30, _ky - 21, _px + _pw - 30, _ky + 45, false);
        draw_set_alpha(1.0);
    }
    draw_set_halign(fa_left);
    draw_set_valign(fa_middle);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_ksel ? c_white : make_color_rgb(170, 180, 200));
    draw_text(_px + 60, _ky + 12, (_ksel ? "> " : "  ") + "Menu Tick");
    var _kpx = _bar_x;
    var _kpy = _ky + 3;
    var _kpw = 138;
    var _kph = _bar_h + 6;
    draw_set_color(_tick_on ? make_color_rgb(50, 130, 90) : make_color_rgb(45, 50, 66));
    draw_rectangle(_kpx, _kpy, _kpx + _kpw, _kpy + _kph, false);
    draw_set_color(_ksel ? make_color_rgb(120, 190, 255) : make_color_rgb(70, 85, 110));
    draw_rectangle(_kpx, _kpy, _kpx + _kpw, _kpy + _kph, true);
    draw_set_halign(fa_center);
    draw_set_color(c_white);
    draw_text(_kpx + _kpw / 2, _kpy + _kph / 2, _tick_on ? "ON" : "OFF");
    draw_set_halign(fa_left);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(140, 150, 170));
    draw_text(_kpx + _kpw + 24, _kpy + _kph / 2, "(menu navigation sound)");
    draw_set_font(ui_font(fnt_ui));

    // --- Sixth row: Fullscreen toggle ---
    var _fry = _ky + _pitch;
    var _fsel = (global.settings_cursor == 5);
    if (_fsel) {
        draw_set_alpha(0.20);
        draw_set_color(make_color_rgb(80, 140, 220));
        draw_rectangle(_px + 30, _fry - 21, _px + _pw - 30, _fry + 45, false);
        draw_set_alpha(1.0);
    }
    draw_set_halign(fa_left);
    draw_set_valign(fa_middle);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_fsel ? c_white : make_color_rgb(170, 180, 200));
    draw_text(_px + 60, _fry + 12, (_fsel ? "> " : "  ") + "Fullscreen");

    // On/Off pill
    var _on   = global.fullscreen;
    var _pill_x = _bar_x;
    var _pill_y = _fry + 3;
    var _pill_w = 138;
    var _pill_h = _bar_h + 6;
    draw_set_color(_on ? make_color_rgb(50, 130, 90) : make_color_rgb(45, 50, 66));
    draw_rectangle(_pill_x, _pill_y, _pill_x + _pill_w, _pill_y + _pill_h, false);
    draw_set_color(_fsel ? make_color_rgb(120, 190, 255) : make_color_rgb(70, 85, 110));
    draw_rectangle(_pill_x, _pill_y, _pill_x + _pill_w, _pill_y + _pill_h, true);
    draw_set_halign(fa_center);
    draw_set_color(c_white);
    draw_text(_pill_x + _pill_w / 2, _pill_y + _pill_h / 2, _on ? "ON" : "OFF");
    draw_set_halign(fa_left);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(140, 150, 170));
    draw_text(_pill_x + _pill_w + 24, _pill_y + _pill_h / 2, "(F11)");

    // --- Seventh row: Font Size selector (Small / Default / Large; ui_font).
    //     Value box mirrors the music-selector footprint so the column lines up.
    //     Falls back to Default rendering until the variant fonts are imported. ---
    var _fnty  = _fry + _pitch;
    var _fnsel = (global.settings_cursor == 6);
    if (_fnsel) {
        draw_set_alpha(0.20);
        draw_set_color(make_color_rgb(80, 140, 220));
        draw_rectangle(_px + 30, _fnty - 21, _px + _pw - 30, _fnty + 45, false);
        draw_set_alpha(1.0);
    }
    draw_set_halign(fa_left);
    draw_set_valign(fa_middle);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_fnsel ? c_white : make_color_rgb(170, 180, 200));
    draw_text(_px + 60, _fnty + 12, (_fnsel ? "> " : "  ") + "Font Size");
    var _fnby = _fnty + 3;
    var _fs_mode = variable_global_exists("font_size_mode") ? global.font_size_mode : 1;
    var _fs_names = ["Small", "Default", "Large"];
    draw_set_color(make_color_rgb(35, 42, 60));
    draw_rectangle(_bar_x, _fnby, _bar_x + _bar_w, _fnby + _bar_h + 6, false);
    draw_set_color(_fnsel ? make_color_rgb(120, 190, 255) : make_color_rgb(70, 85, 110));
    draw_rectangle(_bar_x, _fnby, _bar_x + _bar_w, _fnby + _bar_h + 6, true);
    draw_set_halign(fa_center);
    draw_set_color(c_white);
    draw_text(_bar_x + _bar_w / 2, _fnby + (_bar_h + 6) / 2, _fs_names[_fs_mode]);
    draw_set_color(_fnsel ? make_color_rgb(120, 190, 255) : make_color_rgb(90, 100, 125));
    draw_text(_bar_x + 21,          _fnby + (_bar_h + 6) / 2, "<");
    draw_text(_bar_x + _bar_w - 21, _fnby + (_bar_h + 6) / 2, ">");
    draw_set_halign(fa_left);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(140, 150, 170));
    draw_text(_bar_x + _bar_w + 24, _fnby + (_bar_h + 6) / 2, "(A/D)");
    draw_set_font(ui_font(fnt_ui));

    // --- Eighth row: Tutorial Tips on/off toggle ---
    var _tut_on = (!variable_global_exists("tutorial_enabled")) || global.tutorial_enabled;
    var _try    = _fnty + _pitch;
    var _tsel   = (global.settings_cursor == 7);
    if (_tsel) {
        draw_set_alpha(0.20);
        draw_set_color(make_color_rgb(80, 140, 220));
        draw_rectangle(_px + 30, _try - 21, _px + _pw - 30, _try + 45, false);
        draw_set_alpha(1.0);
    }
    draw_set_halign(fa_left);
    draw_set_valign(fa_middle);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_tsel ? c_white : make_color_rgb(170, 180, 200));
    draw_text(_px + 60, _try + 12, (_tsel ? "> " : "  ") + "Tutorial Tips");

    var _tpx = _bar_x;
    var _tpy = _try + 3;
    var _tpw = 138;
    var _tph = _bar_h + 6;
    draw_set_color(_tut_on ? make_color_rgb(50, 130, 90) : make_color_rgb(45, 50, 66));
    draw_rectangle(_tpx, _tpy, _tpx + _tpw, _tpy + _tph, false);
    draw_set_color(_tsel ? make_color_rgb(120, 190, 255) : make_color_rgb(70, 85, 110));
    draw_rectangle(_tpx, _tpy, _tpx + _tpw, _tpy + _tph, true);
    draw_set_halign(fa_center);
    draw_set_color(c_white);
    draw_text(_tpx + _tpw / 2, _tpy + _tph / 2, _tut_on ? "ON" : "OFF");

    // --- Eighth row: On-screen D-pad (touch prefs, M 07-18 S25 batch). One row
    //     does both jobs because the panel is height-capped at 1080: A/D nudges
    //     the size (0.80-2.00, step 0.15), Enter toggles the pad entirely, and
    //     the slider reads OFF while disabled. Persisted by touch_settings_save.
    //     TOUCH PLATFORMS ONLY (M 07-28) - desktop/HTML5 skip straight to Reset. ---
    var _dry  = _try + _pitch;
    if (_has_dpad) {
    var _dsel = (global.settings_cursor == 8);
    if (_dsel) {
        draw_set_alpha(0.20);
        draw_set_color(make_color_rgb(80, 140, 220));
        draw_rectangle(_px + 30, _dry - 21, _px + _pw - 30, _dry + 45, false);
        draw_set_alpha(1.0);
    }
    draw_set_halign(fa_left);
    draw_set_valign(fa_middle);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_dsel ? c_white : make_color_rgb(170, 180, 200));
    draw_text(_px + 60, _dry + 12, (_dsel ? "> " : "  ") + "On-screen D-pad");
    var _dby = _dry + 3;
    if (global.touch_gamepad_off) {
        // Disabled: OFF pill in the toggle-row style; Enter/tap re-enables.
        draw_set_color(make_color_rgb(45, 50, 66));
        draw_rectangle(_bar_x, _dby, _bar_x + 138, _dby + _bar_h + 6, false);
        draw_set_color(_dsel ? make_color_rgb(120, 190, 255) : make_color_rgb(70, 85, 110));
        draw_rectangle(_bar_x, _dby, _bar_x + 138, _dby + _bar_h + 6, true);
        draw_set_halign(fa_center);
        draw_set_color(c_white);
        draw_text(_bar_x + 69, _dby + (_bar_h + 6) / 2, "OFF");
        draw_set_halign(fa_left);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(140, 150, 170));
        draw_text(_bar_x + 138 + 24, _dby + (_bar_h + 6) / 2, "(Enter: turn back on)");
        draw_set_font(ui_font(fnt_ui));
    } else {
        // Enabled: size slider in the volume-row style, x0.80..x2.00.
        var _dfrac = clamp((global.touch_pad_scale - TOUCH_PAD_SCALE_MIN)
                         / (TOUCH_PAD_SCALE_MAX - TOUCH_PAD_SCALE_MIN), 0, 1);
        draw_set_color(make_color_rgb(35, 42, 60));
        draw_rectangle(_bar_x, _dby, _bar_x + _bar_w, _dby + _bar_h, false);
        var _dfill = floor(_bar_w * _dfrac);
        draw_set_color(_dsel ? make_color_rgb(90, 170, 235) : make_color_rgb(60, 110, 150));
        if (_dfill > 0) draw_rectangle(_bar_x, _dby, _bar_x + _dfill, _dby + _bar_h, false);
        draw_set_color(make_color_rgb(70, 85, 110));
        draw_rectangle(_bar_x, _dby, _bar_x + _bar_w, _dby + _bar_h, true);
        draw_set_halign(fa_left);
        draw_set_color(c_white);
        draw_text(_bar_x + _bar_w + 24, _dby + _bar_h / 2, "x" + string_format(global.touch_pad_scale, 1, 2));
    }
    }   // end _has_dpad row

    // --- Ninth row: Pinch Zoom on/off (SYSTEMS_PINCH_ZOOM.md; touch platforms
    //     only, same gate as the D-pad row). OFF also hard-resets the zoom. ---
    var _pzy = _dry + 68;
    if (_has_dpad) {
        var _pz_on  = !global.pinch_zoom_off;
        var _pzsel  = (global.settings_cursor == 9);
        if (_pzsel) {
            draw_set_alpha(0.20);
            draw_set_color(make_color_rgb(80, 140, 220));
            draw_rectangle(_px + 30, _pzy - 21, _px + _pw - 30, _pzy + 45, false);
            draw_set_alpha(1.0);
        }
        draw_set_halign(fa_left);
        draw_set_valign(fa_middle);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_pzsel ? c_white : make_color_rgb(170, 180, 200));
        draw_text(_px + 60, _pzy + 12, (_pzsel ? "> " : "  ") + "Pinch Zoom");
        var _pzby = _pzy + 3;
        draw_set_color(_pz_on ? make_color_rgb(50, 130, 90) : make_color_rgb(45, 50, 66));
        draw_rectangle(_bar_x, _pzby, _bar_x + 138, _pzby + _bar_h + 6, false);
        draw_set_color(_pzsel ? make_color_rgb(120, 190, 255) : make_color_rgb(70, 85, 110));
        draw_rectangle(_bar_x, _pzby, _bar_x + 138, _pzby + _bar_h + 6, true);
        draw_set_halign(fa_center);
        draw_set_color(c_white);
        draw_text(_bar_x + 69, _pzby + (_bar_h + 6) / 2, _pz_on ? "ON" : "OFF");
        draw_set_halign(fa_left);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(140, 150, 170));
        draw_text(_bar_x + 138 + 24, _pzby + (_bar_h + 6) / 2, "(two-finger zoom)");
        draw_set_font(ui_font(fnt_ui));
    }

    // --- Tenth row: Reset Tutorial (re-show every tip). Takes the D-pad row's
    //     slot when the touch rows are hidden (desktop/HTML5). ---
    var _rry  = _has_dpad ? (_pzy + 68) : _dry;
    var _rsel = (global.settings_cursor == 10);
    if (_rsel) {
        draw_set_alpha(0.20);
        draw_set_color(make_color_rgb(80, 140, 220));
        draw_rectangle(_px + 30, _rry - 21, _px + _pw - 30, _rry + 45, false);
        draw_set_alpha(1.0);
    }
    draw_set_halign(fa_left);
    draw_set_valign(fa_middle);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_rsel ? c_white : make_color_rgb(170, 180, 200));
    draw_text(_px + 60, _rry + 12, (_rsel ? "> " : "  ") + "Reset Tutorial");
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(140, 150, 170));
    draw_text(_bar_x, _rry + 12, "[ Enter ] Re-show all tips");

    // Reset confirmation flash
    if (variable_global_exists("settings_reset_flash") && global.settings_reset_flash > 0) {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(120, 200, 140));
        draw_text(GUI_CX, _py + _ph - 78, "Tutorial reset - tips will show again.");
    }

    // -----------------------------------------------------------------------
    // TOUCH (M 07-17): these rows were nav+Enter only - unreachable on a phone.
    // Tap a volume TRACK to set that level directly; tap a music/toggle/reset row
    // to act on it (music cycles, toggles flip, reset fires) via the same key the
    // keyboard handler reads next frame. Touch only; the keyboard/pad path is
    // untouched. Row Y's reuse the exact locals the rows above were drawn at.
    // -----------------------------------------------------------------------
    // First-frame guard (M 08-26 Steam bug): the press that OPENED the panel is
    // still down on the panel's first drawn frame - the pause menu's "Settings"
    // row sits right on the Fullscreen band, so the opening click toggled
    // fullscreen through the overlay. Swallow any press on the first draw;
    // audio_settings_handle_input re-arms the guard when the panel closes.
    if (!variable_global_exists("settings_click_armed")) global.settings_click_armed = false;
    var _st_first_draw = !global.settings_click_armed;
    global.settings_click_armed = true;
    if (mouse_check_button_pressed(mb_left) && !_st_first_draw) {
        var _stmx   = device_mouse_x_to_gui(0);
        var _stmy   = device_mouse_y_to_gui(0);
        var _st_ys  = [ _row_y, _row_y + _row_h, _row_y + 2 * _row_h, _row_y + 3 * _row_h,
                        _ky, _fry, _fnty, _try, _dry, _pzy, _rry ];
        for (var _sri = 0; _sri < 11; _sri++) {
            // No D-pad/Pinch rows off-touch: their Y equals the Reset row's, so
            // skip 8-9 or a Reset tap would read as a toggle.
            if ((_sri == 8 || _sri == 9) && !_has_dpad) continue;
            var _sry = _st_ys[_sri];
            if (_stmx < _px + 30 || _stmx > _px + _pw - 30 || _stmy < _sry - 21 || _stmy > _sry + 45) continue;
            // Click-to-HIGHLIGHT, click-again-to-ACT (M 08-26: "1 click to
            // highlight, second click to select/confirm" - the title-menu
            // idiom): a press on an unselected row only moves the cursor; the
            // row's action fires on a second press on the already-selected row.
            if (global.settings_cursor != _sri) {
                global.settings_cursor = _sri;
                nav_tick();
                break;
            }
            if (_sri <= 1) {
                // Volume: tap on the track sets the level (delta via adjust reuses apply+save).
                if (_stmx >= _bar_x && _stmx <= _bar_x + _bar_w) {
                    var _tgt = clamp((_stmx - _bar_x) / _bar_w, 0, 1);
                    var _cur = (_sri == 0) ? global.music_volume : global.sfx_volume;
                    audio_settings_adjust(_sri, _tgt - _cur);
                }
            } else if (_sri <= 3) {
                touch_press(ord("D"));   // music selector: cycle to the next freed track
            } else if (_sri == 6) {
                touch_press(ord("D"));   // font size: cycle Small -> Default -> Large
            } else if (_sri == 8) {
                // D-pad row: tap the track to set the size directly while the pad is
                // on; tapping the rest of the row (or the OFF pill) toggles it.
                if (!global.touch_gamepad_off && _stmx >= _bar_x && _stmx <= _bar_x + _bar_w) {
                    var _dtgt = lerp(TOUCH_PAD_SCALE_MIN, TOUCH_PAD_SCALE_MAX,
                                     clamp((_stmx - _bar_x) / _bar_w, 0, 1));
                    touch_pad_scale_adjust(_dtgt - global.touch_pad_scale);
                } else {
                    touch_press(vk_enter);
                }
            } else {
                touch_press(vk_enter);   // Menu Tick / Fullscreen / Tutorial / Reset: act
            }
            break;
        }
    }

    // Footer hint
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 160, 185));
    if (input_device() == 2) {
        draw_text(GUI_CX, _py + _ph - 42, "Tap a row to select it - tap it again to change it - Back to close");
    } else {
        ui_draw_key_legend(GUI_CX, _py + _ph - 42, "W/S: Select    A/D or <-/->: Adjust / Toggle / Enter    Esc/O: Close");
    }

    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
    draw_set_font(-1);
}

// ui_draw_combat_overlay(...) - the hit-preview band + combat log. Split out of
// the HUD so the combat controller can draw it AFTER the battler sprites, keeping
// combat text on top of the sprites/shadows (text always has visual priority).
function ui_draw_combat_overlay(combat_state, player, ability_array, selected_ability_index, log_array) {
    // --- Ability tooltip (lower-right), deferred here from the HUD pass
    // (08-13, M shot): drawn AFTER the battler sprites so a near-station 2.5D
    // enemy can never stand on top of the card. Round 7 (M): WIDER + SHORTER -
    // it fills the gap beside the combat log (x1220-1890) and the extra width
    // means less wrapping, so it hugs the log's vertical band instead of
    // towering over the scene. Still bottom-anchored y945, grows upward.
    if (selected_ability_index >= 0 && selected_ability_index < array_length(ability_array)) {
        ui_draw_ability_tooltip(1220, 945, ability_array[selected_ability_index], player, 670);
    }

    // --- Ability hit preview (flashing, in the open band above the combat log) ---
    if (instance_exists(obj_combat_controller)) {
        var _cc_pv = instance_find(obj_combat_controller, 0);
        if (_cc_pv.player_turn && !_cc_pv.combat_over && !_cc_pv.show_loot_screen) {
            var _pv_ab    = ability_array[selected_ability_index];
            var _pv_live  = combat_living_enemies(combat_state);
            if (is_struct(_pv_ab) && array_length(_pv_live) > 0) {
                var _pv_mx  = device_mouse_x_to_gui(0);
                var _pv_my  = device_mouse_y_to_gui(0);
                var _pv_idx = clamp(_cc_pv.selected_target, 0, array_length(_pv_live) - 1);
                for (var _pvi = 0; _pvi < array_length(_pv_live); _pvi++) {
                    var _bx = (_pvi mod 2 == 0) ? 990 : 1485;
                    var _by = 96 + (_pvi div 2) * 132;   // keep in sync with Draw_64 _bar_row_gap (was stale 78 - hover previewed the WRONG enemy)
                    if (_pv_mx >= _bx && _pv_mx < _bx + 400 && _pv_my >= _by && _pv_my < _by + 42) {
                        _pv_idx = _pvi;
                        break;
                    }
                }
                var _pv_tgt = _pv_live[_pv_idx];
                var _pv_est = combat_estimate_hit(_pv_ab, player, _pv_tgt);

                // Only DAMAGE-dealing abilities get a preview - this shows the actual
                // stat-scaled hit (not the ability's base number). Defensive / utility
                // abilities (no direct damage) return -1 and show nothing at all.
                if (_pv_est >= 0) {
                    // Detonation: if a detonator (Snipe / Assassinate / Arcane Burst /
                    // Soul Nova) would pop a status the target is carrying, note the bonus.
                    var _det_txt = "";
                    if (ability_is_detonator(_pv_ab)) {
                        var _dp = combat_detonator_pick(_pv_tgt);
                        if (_dp.key != "" && _dp.idx >= 0 && _dp.idx < array_length(_pv_tgt.status_effects)) {
                            // Strip the "Detonate: " prefix - the "+ DETONATE" header below says it.
                            _det_txt = string_replace_all(status_detonation_text(_pv_tgt.status_effects[_dp.idx]), "Detonate: ", "");
                            // Hexed target: the reaction's numeric bonus is doubled.
                            if (combat_status_total(_pv_tgt, "hexed") > 0) _det_txt = "HEXED x2! " + _det_txt;
                        }
                    }

                    // NARROW vertical panel hugging the LEFT edge (x30, w290) so it clears
                    // the player sprite (drawn from x330) instead of running across it.
                    // Stacked just above the combat log. Reads top-down: label, big number,
                    // target, then the detonation note when one applies.
                    var _box_x = 30;
                    var _box_w = 290;
                    var _pad   = 12;
                    var _tw    = _box_w - _pad * 2;

                    var _det_h = 0;
                    if (_det_txt != "") {
                        draw_set_font(ui_font(fnt_ui_small));
                        _det_h = 10 + 24 + string_height_ext(_det_txt, 22, _tw);
                    }
                    var _box_h = _pad + 24 + 48 + 26 + _det_h + _pad;
                    var _box_y = 741 - _box_h;   // bottom ~6px above the log (y747)

                    draw_set_alpha(0.85);
                    draw_set_color(make_color_rgb(18, 16, 22));
                    draw_rectangle(_box_x, _box_y, _box_x + _box_w, _box_y + _box_h, false);
                    draw_set_alpha(1.0);
                    draw_set_color(make_color_rgb(120, 100, 50));
                    draw_rectangle(_box_x, _box_y, _box_x + _box_w, _box_y + _box_h, true);

                    draw_set_halign(fa_left);
                    draw_set_valign(fa_top);
                    var _ty = _box_y + _pad;

                    // Label
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(make_color_rgb(150, 140, 120));
                    draw_text(_box_x + _pad, _ty, "POTENTIAL DAMAGE");
                    _ty += 24;

                    // Big stat-scaled number (gently flashing for attention)
                    var _flash = 0.8 + 0.2 * (0.5 + 0.5 * sin(current_time / 220));
                    draw_set_alpha(_flash);
                    draw_set_font(fnt_ui_title);
                    draw_set_color(make_color_rgb(255, 210, 90));
                    var _num_str = string(_pv_est);
                    draw_text(_box_x + _pad, _ty, _num_str);
                    var _num_w = string_width(_num_str);   // measured in the title font
                    // "approx" hint trailing the big number (small, so the title font's
                    // missing '~' glyph is never an issue).
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(make_color_rgb(150, 140, 120));
                    draw_text(_box_x + _pad + _num_w + 10, _ty + 18, "approx.");
                    draw_set_alpha(1.0);
                    _ty += 48;

                    // Target
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(make_color_rgb(195, 200, 215));
                    draw_text(_box_x + _pad, _ty, "to " + _pv_tgt.name);
                    _ty += 26;

                    // Detonation note (when it applies)
                    if (_det_txt != "") {
                        _ty += 10;
                        draw_set_font(ui_font(fnt_ui_small));
                        draw_set_color(make_color_rgb(255, 150, 60));
                        draw_text(_box_x + _pad, _ty, "+ DETONATE");
                        _ty += 24;
                        draw_set_color(make_color_rgb(230, 185, 135));
                        draw_text_ext(_box_x + _pad, _ty, _det_txt, 22, _tw);
                    }
                    draw_set_valign(fa_top);
                }
            }
        }
    }

    // --- Combat log (bottom strip) ---
    // M 08-13: 210px held 6 rows + 12px of dead space (a 7th row never fit).
    // 198 = padding + exactly 6 rows - the whole band drops 12px, and the
    // stage above gains the room (PD box + ability card follow).
    ui_draw_combat_log(30, 747, 1170, 198, log_array);

    // --- Telegraph warning - drawn AFTER the log so it always sits on top,
    // centered and parked just above the log panel (log top = y735).
    var _tg_count = array_length(combat_state.combatants);
    for (var _tgi = 0; _tgi < _tg_count; _tgi++) {
        var _tgc = combat_state.combatants[_tgi];
        if (!_tgc.is_player && enemy_should_telegraph(_tgc, combat_state.round)) {
            ui_draw_telegraph_warning(_tgc.name, _tgc.telegraph_message);
            break; // Only one warning banner at a time
        }
    }
}

function ui_draw_combat_hud(combat_state, player, ability_array, selected_ability_index, log_array, draw_log = true) {

    // --- Turn queue (top-center, shifted LEFT so a 5-wide queue ends before the
    // enemy bar column at x990 instead of crossing the intent chips) ---
    ui_draw_turn_queue(430, 10, combat_state);

    // --- Player HP bar (top-left) ---
    // Conveyance (08-04): eased HP so ranged/spell hits drain when the bolt lands.
    // HUD reorder (M 08-15): LEVEL block now sits at the very top; HP below it.
    ui_draw_hp_bar(30, 58, 375, 36, combat_hp_vis(player), player.max_HP, "HP");

    // --- Energy pips (below HP bar) ---
    ui_draw_energy_pips(30, 102, player.energy, actor_turn_ap(player));

    // --- Secondary resource bar (below energy pips) ---
    // Determine which resource this class uses and pick a matching color
    var res_name  = "";
    var res_cur   = 0;
    var res_max   = 0;
    var res_color = c_white;

    if (variable_struct_exists(player, "souls")) {
        res_name  = "Souls";
        res_cur   = player.souls;
        res_max   = player.souls_max;
        res_color = c_purple;
    } else if (variable_struct_exists(player, "blood")) {
        res_name  = "Blood";
        res_cur   = player.blood;
        res_max   = player.blood_max;
        res_color = c_red;
    } else if (variable_struct_exists(player, "preparation")) {
        res_name  = "Preparation";
        res_cur   = player.preparation;
        res_max   = player.preparation_max;
        res_color = c_aqua;
    }

    if (res_name != "") {
        ui_draw_secondary_resource(30, 150, res_cur, res_max, res_name, res_color);
    }

    // --- Run level and XP bar (TOP of the column, M 08-15 reorder) ---
    // "Lv X" label leads the whole HUD; the XP bar rides beside it on the
    // same line so the block stays one row tall.
    if (variable_global_exists("run_level")) {
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(c_white);
        draw_set_halign(fa_left);
        draw_set_valign(fa_top);
        draw_text(30, 24, "Lv " + string(global.run_level));

        var _xb  = 130;
        var _xbw = 275;
        var _xbh = 12;
        var _xby = 34;

        if (global.run_level < 15 && variable_global_exists("run_xp")) {
            var _xp_lo    = xp_threshold(global.run_level);
            var _xp_hi    = xp_threshold(global.run_level + 1);
            var _xp_ratio = (_xp_hi > _xp_lo)
                            ? clamp((global.run_xp - _xp_lo) / (_xp_hi - _xp_lo), 0, 1)
                            : 1;
            draw_set_color(make_color_rgb(35, 45, 55));
            draw_rectangle(_xb, _xby, _xb + _xbw, _xby + _xbh, false);
            draw_set_color(make_color_rgb(60, 180, 200));
            var _xfill = floor(_xbw * _xp_ratio);
            if (_xfill > 0) draw_rectangle(_xb, _xby, _xb + _xfill, _xby + _xbh, false);
            draw_set_color(make_color_rgb(50, 70, 80));
            draw_rectangle(_xb, _xby, _xb + _xbw, _xby + _xbh, true);
        } else if (global.run_level >= 15) {
            // Full golden bar at max level - no text overlap
            draw_set_color(make_color_rgb(160, 125, 25));
            draw_rectangle(_xb, _xby, _xb + _xbw, _xby + _xbh, false);
            draw_set_color(make_color_rgb(80, 70, 90));
            draw_rectangle(_xb, _xby, _xb + _xbw, _xby + _xbh, true);
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(255, 200, 50));
            draw_text(_xb + _xbw * 0.5, _xby + _xbh * 0.5 + 2, "MAX");
            draw_set_halign(fa_left);
        }
        draw_set_font(-1);
    }

    // --- Active pet HP (below the level/XP block) ---
    // M 07-09: the over-head chip was awkward and hard to read - the companion's
    // vitals now live under the player's, as a slightly smaller green HP bar (same
    // widget as the player bar). Guard state + [G] toggle hint sit beside it for
    // guarded-stance Combatants. Buff icons/boons shift down while it's shown.
    var _hud_pet      = pet_active();
    var _hud_pet_show = (_hud_pet != undefined && !_hud_pet.is_egg);
    if (_hud_pet_show) {
        // M 08-15 reorder: the PET bar rides DIRECTLY below the secondary
        // resource - the buff/debuff row moved beside the AP pips, so the old
        // "gap under Lv that looked like pet debuffs" is gone entirely.
        ui_draw_hp_bar(30, 200, 280, 26, pet_hp(_hud_pet), pet_max_hp(_hud_pet), "PET");
        if (_hud_pet.archetype == PET_ARCH_COMBATANT && pet_stance(_hud_pet) == "guarded") {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_halign(fa_left);
            draw_set_valign(fa_middle);
            if (pet_hp(_hud_pet) <= 0) {
                draw_set_color(make_color_rgb(230, 95, 85));
                draw_text(322, 214, "DOWN");
            } else if (pet_guard_off(_hud_pet)) {
                draw_set_color(make_color_rgb(150, 156, 175));
                draw_text(322, 214, "CALLED OFF  [G]");
            } else {
                draw_set_color(make_color_rgb(140, 205, 150));
                draw_text(322, 214, "GUARDING  [G]");
            }
            draw_set_valign(fa_top);
            draw_set_font(-1);
        }
    }

    // --- Active player buff icons (below XP bar) ---
    // Each carries an `se` descriptor (name + one-line desc + duration noun + colour) so
    // it hover-explains itself like the typed debuff badges do. These buffs live on the
    // player struct (not status_effects[]), so they have no typed `kind` - the desc is
    // authored here. (Task: every status has a mouse-over explanation.)
    var _pbuffs = [];
    if (variable_struct_exists(player, "iron_skin_duration") && player.iron_skin_duration > 0) {
        var _is_col = make_color_rgb(80, 140, 220);
        array_push(_pbuffs, {
            label:    "IS",
            color:    _is_col,
            duration: player.iron_skin_duration,
            se: { name: "Iron Skin", color: _is_col, duration: player.iron_skin_duration, dur_noun: "turn",
                  desc: "Iron Skin: reduces the damage you take from each incoming hit while active." }
        });
    }
    if (variable_struct_exists(player, "bloodthorn_active") && player.bloodthorn_active) {
        var _bt_col = make_color_rgb(190, 55, 55);
        var _bt_val = variable_struct_exists(player, "bloodthorn_value") ? player.bloodthorn_value : 0;
        array_push(_pbuffs, {
            label:    "BT",
            color:    _bt_col,
            duration: player.bloodthorn_duration,
            se: { name: "Bloodthorn Aura", color: _bt_col, duration: player.bloodthorn_duration, dur_noun: "turn",
                  desc: "Bloodthorn Aura: reflects " + string(_bt_val) + " damage back at any enemy that strikes you." }
        });
    }
    if (variable_struct_exists(player, "blink_charges") && player.blink_charges > 0) {
        var _blk_col = make_color_rgb(70, 75, 210);
        array_push(_pbuffs, {
            label:    "BLK",
            color:    _blk_col,
            duration: player.blink_charges,
            se: { name: "Blink", color: _blk_col, duration: player.blink_charges, dur_noun: "charge",
                  desc: "Blink: staged evasion of your next incoming attacks - the first is fully dodged, then half damage, then quarter as the charges drop." }
        });
    }
    if (variable_struct_exists(player, "is_untargetable") && player.is_untargetable) {
        var _van_col = make_color_rgb(120, 70, 200);
        array_push(_pbuffs, {
            label:    "VAN",
            color:    _van_col,
            duration: player.untargetable_turns,
            se: { name: "Vanish", color: _van_col, duration: player.untargetable_turns, dur_noun: "charge",
                  desc: "Vanished: a chance to completely avoid each incoming attack (scales with Wisdom); your next strike also deals bonus damage." }
        });
    }
    if (variable_struct_exists(player, "soul_engine_active") && player.soul_engine_active) {
        var _eng_col   = make_color_rgb(180, 120, 235);
        var _eng_round = variable_struct_exists(player, "soul_engine_round") ? player.soul_engine_round : 0;
        var _eng_now   = variable_struct_exists(combat_state, "round") ? combat_state.round : _eng_round;
        var _eng_bonus = 3 * max(0, _eng_now - _eng_round);
        array_push(_pbuffs, {
            label:    "ENG",
            color:    _eng_col,
            duration: max(0, _eng_now - _eng_round),
            se: { name: "Soul Engine", color: _eng_col, duration: max(0, _eng_now - _eng_round), dur_noun: "turn",
                  desc: "Soul Engine: your spells currently deal +" + string(_eng_bonus) + " (grows +3 every turn, whole combat)." }
        });
    }
    if (variable_struct_exists(player, "glacial_ward_turns") && player.glacial_ward_turns > 0) {
        var _glw_col = make_color_rgb(140, 200, 235);
        array_push(_pbuffs, {
            label:    "GLW",
            color:    _glw_col,
            duration: player.glacial_ward_turns,
            se: { name: "Glacial Ward", color: _glw_col, duration: player.glacial_ward_turns, dur_noun: "turn",
                  desc: "Glacial Ward: melee enemies that strike you are Chilled (-30% damage; detonators shatter it)." }
        });
    }
    if (variable_struct_exists(player, "smoke_dodge_turns") && player.smoke_dodge_turns > 0) {
        var _smk_col = make_color_rgb(140, 145, 160);
        array_push(_pbuffs, {
            label:    "SMK",
            color:    _smk_col,
            duration: player.smoke_dodge_turns,
            se: { name: "Smoke Cover", color: _smk_col, duration: player.smoke_dodge_turns, dur_noun: "turn",
                  desc: "Smoke Cover: the smoke hides you too - +15% dodge while it lingers." }
        });
    }
    if (variable_struct_exists(player, "shadow_step_charges") && player.shadow_step_charges > 0) {
        var _ss_col = make_color_rgb(45, 155, 65);
        array_push(_pbuffs, {
            label:    "SS",
            color:    _ss_col,
            duration: player.shadow_step_charges,
            se: { name: "Shadow Step", color: _ss_col, duration: player.shadow_step_charges, dur_noun: "charge",
                  desc: "Shadow Step: a chance to dodge each of your next incoming attacks." }
        });
    }
    // The Arcanist's standing summon (class pass 08-13): reads as a buff with
    // its turns left; the hover explains the eat/detonate contract.
    if (variable_struct_exists(player, "summon") && is_struct(player.summon)) {
        var _smb = player.summon;
        var _smb_col = (_smb.kind == "golem") ? make_color_rgb(225, 120, 45)
                     : ((_smb.kind == "husk") ? make_color_rgb(230, 210, 70) : make_color_rgb(150, 130, 220));
        array_push(_pbuffs, {
            label:    (_smb.kind == "golem") ? "GLM" : ((_smb.kind == "husk") ? "HSK" : "EFG"),
            color:    _smb_col,
            duration: _smb.turns,
            se: { name: _smb.name, color: _smb_col, duration: _smb.turns, dur_noun: "turn",
                  desc: _smb.name + ": stands between you and them - eats "
                      + string(_smb.hits) + " more hit" + ((_smb.hits == 1) ? "" : "s")
                      + " meant for you. Press its button again to DETONATE."
                      + ((_smb.kind == "husk") ? " While it stands: +15% crit on Shock/Arcane casts." : "") }
        });
    }
    // Deployed traps read as buffs, exactly like SS (M 08-08): they ARE a
    // player-side timer, and the buff row is where the player already looks for
    // "what is currently true about me". The duration slot shows CHARGES, since
    // a trap has no turn count - it waits as long as it takes.
    if (variable_struct_exists(player, "traps") && is_array(player.traps)) {
        for (var _tpb = 0; _tpb < array_length(player.traps); _tpb++) {
            var _tpd = player.traps[_tpb];
            var _tpc = trap_filter_color(_tpd.filter);
            array_push(_pbuffs, {
                label:    trap_short_label(_tpd.name),
                color:    _tpc,
                duration: _tpd.charges,
                se: { name: _tpd.name, color: _tpc, duration: _tpd.charges, dur_noun: "charge",
                      desc: _tpd.name + ": set and waiting. Springs on the next "
                            + trap_filter_label(_tpd.filter)
                            + (_tpd.block ? " and BLOCKS it outright." : " - it does not block the attack.") }
            });
        }
    }
    // Typed debuffs/statuses applied to the player (poison, Sight Clouded/blind,
    // weaken, stun, ...) live in player.status_effects[] - surface them here too so
    // the player can actually see what's afflicting them and for how long.
    if (variable_struct_exists(player, "status_effects")) {
        var _pstat = status_icons_from(player.status_effects);
        for (var _psi = 0; _psi < array_length(_pstat); _psi++) array_push(_pbuffs, _pstat[_psi]);
    }
    // M 08-15 reorder: the buff/debuff row rides to the RIGHT of the AP pips,
    // directly below the HP bar - unmistakably the player's, and the left
    // column packs tight (Lv / HP / AP / resource / PET with no gap).
    if (array_length(_pbuffs) > 0) {
        ui_draw_status_icon_row(430, 96, _pbuffs);
    }
    var _hud_shift = _hud_pet_show ? 56 : 0;

    // --- Deployed-trap reminder (M 08-08: left-hand side, below the pet bar) ---
    // Returns the height it used so boons and curses slide down by exactly that
    // much - the trap block never overlaps them and never leaves a hole when the
    // board is empty.
    var _trap_h = ui_draw_trap_info(player, 30, _hud_pet_show ? 244 : 200, combat_state);

    // --- Active run boons + curses (left column, below the trap block) ---
    // Boons occupy a header (24px) + 27px per entry; stack curses just beneath them.
    ui_draw_active_boons(30, 290 + _hud_shift + _trap_h);
    var _boon_n = variable_global_exists("run_boons") ? array_length(global.run_boons) : 0;
    var _curse_y = 290 + _hud_shift + _trap_h + ((_boon_n > 0) ? (24 + 27 * _boon_n + 12) : 0);
    ui_draw_active_curses(30, _curse_y);

    // --- Ability buttons (bottom-center) ---
    ui_draw_ability_buttons(240, 990, ability_array, selected_ability_index, player);

    // --- Hit preview + combat log ---
    // Deferred to draw AFTER the battler sprites (so combat text always sits on top
    // of the sprites/shadows) when the caller passes draw_log=false; otherwise drawn
    // inline here. See ui_draw_combat_overlay.
    if (draw_log) {
        ui_draw_combat_overlay(combat_state, player, ability_array, selected_ability_index, log_array);
    }

    // --- Ability tooltip: MOVED to ui_draw_combat_overlay (08-13, M shot: a
    // near-station 2.5D enemy stood on top of the card - it must draw AFTER
    // the battler sprites, same deferral as the combat log). ---

    // NOTE: Target selection indicator (">") is drawn in Draw_64.gml alongside
    // the enemy HP bars - it needs selected_target from obj_combat_controller
    // directly and cannot be drawn here without passing it as a parameter.

    // NOTE: The telegraph warning banner moved to ui_draw_combat_overlay (after
    // the combat log draw) - here it rendered BEFORE the log and got buried
    // under it (M's screenshot 2026-07-07).
}

// ---------------------------------------------------------------------------
// ui_draw_ability_detail(ab)
// Full-screen "press Tab for the breakdown" popup for one ability - the elaborate
// view so the dense screens don't have to cram the text. Shows icon, attack class,
// AP + secondary-resource cost, cooldown, the full generated mechanics, the status-
// reaction table (for detonators), and flavor. Drawn on the GUI layer over whatever
// screen opened it; the caller gates input while it's up. See SYSTEMS_VIABILITY_PASS.md.
// ---------------------------------------------------------------------------
function ui_draw_ability_detail(ab, close_key_label = "Tab", scroll_y = 0) {
    if (!is_struct(ab)) return;

    // Dim the whole screen.
    draw_set_alpha(0.80); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    var _x1 = 390, _y1 = 144, _x2 = 1530, _y2 = 936;
    var _panel_bg = make_color_rgb(16, 17, 26);
    draw_set_color(_panel_bg);
    draw_rectangle(_x1, _y1, _x2, _y2, false);
    ui_draw_gothic_frame(_x1, _y1, _x2, _y2, 36);

    var _pad = 45;
    var _lx  = _x1 + _pad;
    var _rx  = _x2 - _pad;
    draw_set_halign(fa_left); draw_set_valign(fa_top);

    // Layout: a FIXED header (icon/name/cost/school) and a SCROLLABLE body below it.
    // The body is drawn shifted up by _scroll; afterwards we repaint the header and
    // footer strips with the panel colour to mask any overflow, then draw header/footer
    // on top. Scrolling keeps long ability writeups readable without a surface. (Task 11)
    var _body_top    = _y1 + _pad + 144 + 24;   // first body line (just below the header divider)
    var _body_bottom = _y2 - 66;                // footer strip begins here
    var _scroll      = max(0, scroll_y);

    var _detail_cat = ability_category(ab);

    // ========================= SCROLLABLE BODY =========================
    // The body renders onto a cached CLIPPING SURFACE the size of the visible band,
    // so overflow physically cannot escape it. (The old mask-strip approach only
    // covered ~28px below the body — once Woven Talents lengthened detonator
    // writeups, the tail drew over the frame and down the scrim.) Cleared to the
    // opaque panel colour so text antialiasing never lands on transparent pixels.
    var _surf_w = _x2 - _x1;
    var _surf_h = _body_bottom - _body_top;
    if (!variable_global_exists("ui_ability_detail_surf")) global.ui_ability_detail_surf = -1;
    if (!surface_exists(global.ui_ability_detail_surf))
        global.ui_ability_detail_surf = surface_create(_surf_w, _surf_h);
    surface_set_target(global.ui_ability_detail_surf);
    draw_clear(_panel_bg);
    // Coordinates are SURFACE-LOCAL until the reset below (origin = panel left / body top).
    _lx = _pad;
    _rx = _surf_w - _pad;
    var _y = -_scroll;

    // --- Mechanics (the canonical generated description) ---
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(120, 200, 140));
    draw_text(_lx, _y, "MECHANICS");
    _y += 36;
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(210, 214, 230));
    var _mech = ability_describe(ab);
    draw_text_ext(_lx, _y, _mech, -1, _rx - _lx);
    _y += string_height_ext(_mech, -1, _rx - _lx) + 27;

    // --- Woven talents (M 07-27: picks were invisible outside the loadout web;
    // the body already scrolls, so long weaves stay readable) ---
    var _wt_picks = ability_web_picks(ab.name);
    if (array_length(_wt_picks) > 0) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(190, 160, 240));
        draw_text(_lx, _y, "WOVEN TALENTS  (" + string(array_length(_wt_picks)) + " / 4)");
        _y += 36;
        var _wt_nodes = ability_web_nodes(ab);
        for (var _wi = 0; _wi < array_length(_wt_picks); _wi++) {
            // Picks store "id" or "id@param" (the Attunement school choice).
            var _wp    = _wt_picks[_wi];
            var _wp_at = string_pos("@", _wp);
            var _wp_id = (_wp_at > 0) ? string_copy(_wp, 1, _wp_at - 1) : _wp;
            var _wp_pr = (_wp_at > 0) ? string_copy(_wp, _wp_at + 1, string_length(_wp) - _wp_at) : "";
            var _wn = undefined;
            for (var _wj = 0; _wj < array_length(_wt_nodes); _wj++) {
                if (_wt_nodes[_wj].id == _wp_id) { _wn = _wt_nodes[_wj]; break; }
            }
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(226, 214, 245));
            var _wt_title = (_wn != undefined) ? _wn.title : _wp_id;
            if (_wp_pr != "") _wt_title += "  (" + string_upper(string_char_at(_wp_pr, 1)) + string_copy(_wp_pr, 2, string_length(_wp_pr) - 1) + ")";
            draw_text(_lx, _y, "-  " + _wt_title);
            _y += 33;
            if (_wn != undefined && _wn.label != "") {
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(150, 145, 175));
                draw_text_ext(_lx + 30, _y, _wn.label, -1, _rx - _lx - 30);
                _y += string_height_ext(_wn.label, -1, _rx - _lx - 30) + 9;
            }
        }
        _y += 18;
    }

    // --- P3 off-stat RIDER (08-01, M-approved): the ability's non-primary-stat
    //     bonus. Lit gold when the stat meets the threshold; grey while it's
    //     still a goal (the "you: N" makes the gear/level-up choice legible). ---
    var _rdr = ability_stat_rider(ab.name);
    if (_rdr != undefined) {
        var _rdr_have = ability_rider_stat_total(_rdr.stat);
        var _rdr_on   = (_rdr_have >= _rdr.at);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(_rdr_on ? make_color_rgb(245, 195, 80) : make_color_rgb(120, 126, 146));
        draw_text(_lx, _y, _rdr_on ? "RIDER - ACTIVE" : "RIDER - locked");
        _y += 36;
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_rdr_on ? make_color_rgb(230, 214, 170) : make_color_rgb(140, 146, 170));
        var _rdr_txt = _rdr.label + " at " + _rdr.stat + " " + string(_rdr.at) + "  (you: " + string(_rdr_have) + ")";
        draw_text_ext(_lx, _y, _rdr_txt, -1, _rx - _lx);
        _y += string_height_ext(_rdr_txt, -1, _rx - _lx) + 27;
    }

    // --- Role & same-category synergy (SYSTEMS_ABILITY_SYNERGY.md) ---
    var _cat_lbl = ability_category_label(_detail_cat);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(ability_category_color(_detail_cat));
    draw_text(_lx, _y, _cat_lbl + " - same-role synergy");
    _y += 36;
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(200, 204, 220));
    // Support abilities can be discounted all the way to 0 AP; every other role floors at 1.
    var _syn_floor_text = (_detail_cat == "support") ? "to 0 AP" : "1 less AP (minimum 1)";
    var _syn_text = "After you cast another " + _cat_lbl + " ability this turn, this one "
                  + "costs " + _syn_floor_text + ". Chaining same-role abilities makes minor "
                  + "buffs and supports worth casting together.";
    draw_text_ext(_lx, _y, _syn_text, -1, _rx - _lx);
    _y += string_height_ext(_syn_text, -1, _rx - _lx) + 27;

    // --- Status reactions table (detonators only) ---
    if (ability_is_detonator(ab)) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(190, 160, 240));
        draw_text(_lx, _y, "STATUS REACTIONS  (detonator - the purple diamond on its combat button)");
        _y += 36;
        var _reacts = [
            "Exposed (Vulnerable) - +12 damage (mark persists)",
            "Root / Frost - +30% damage, shatters",
            "Stun - guaranteed critical hit",
            "Weaken - +15% damage",
            "Blind - cannot miss",
            "Poison - applies Mortality (-40% healing, 4 turns)",
            "Bleed - bursts every remaining bleed tick",
            "Void DoT - heals you for 30% of the damage dealt",
        ];
        draw_set_color(make_color_rgb(200, 204, 220));
        for (var _ri = 0; _ri < array_length(_reacts); _ri++) {
            draw_text(_lx + 12, _y, "*  " + _reacts[_ri]);
            _y += 33;
        }
        _y += 12;
    }

    // --- Flavor (legacy authored line, if any) ---
    if (variable_struct_exists(ab, "desc_full") && ab.desc_full != "") {
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(140, 146, 170));
        draw_text_ext(_lx, _y, ab.desc_full, -1, _rx - _lx);
        _y += string_height_ext(ab.desc_full, -1, _rx - _lx);
    }

    // Publish the scroll range so the input handler can clamp (undo the scroll shift
    // to recover the natural content height; body drawing started at y = -_scroll).
    var _natural_h = _y + _scroll;
    var _visible_h = _body_bottom - _body_top;
    global.ui_ability_detail_max_scroll = max(0, _natural_h - _visible_h);

    surface_reset_target();
    draw_surface(global.ui_ability_detail_surf, _x1, _body_top);
    // Back to GUI coordinates for the fixed header / footer.
    _lx = _x1 + _pad;
    _rx = _x2 - _pad;

    // ========================= FIXED HEADER =========================
    var _hy = _y1 + _pad;
    ui_draw_ability_icon(_lx, _hy, 96, ab);
    var _tx = _lx + 96 + 27;
    draw_set_font(fnt_ui_title);
    draw_set_color(c_white);
    draw_text(_tx, _hy + 3, ab.name);
    var _name_w = string_width(ab.name);

    // Role-category chip, just to the RIGHT of the name (auto-positions, can't overlap).
    var _detail_clbl = ability_category_label(_detail_cat);
    draw_set_font(ui_font(fnt_ui));
    var _chip_x = _tx + _name_w + 27;
    var _chip_w = string_width(_detail_clbl);
    if (_chip_x + _chip_w > _rx) _chip_x = _rx - _chip_w;   // fallback: pin to right margin
    draw_set_color(ability_category_color(_detail_cat));
    draw_text(_chip_x, _hy + 12, _detail_clbl);

    // Cost / class line.
    var _ap  = variable_struct_exists(ab, "energy_cost") ? ab.energy_cost : 0;
    var _sec = variable_struct_exists(ab, "secondary_cost") ? ab.secondary_cost : 0;
    var _cls = variable_global_exists("chosen_class") ? global.chosen_class : 0;
    // Trunk discounts (P2, 08-05): out-of-combat surfaces fake a caster with just
    // the class_id - that is all the effective-cost helper reads.
    if (_sec > 0) _sec = ability_secondary_cost_eff(ab, { class_id: _cls });
    var _resname = (_cls == 0) ? "Souls" : ((_cls == 1) ? "Blood" : "Preparation");
    var _costline = string(_ap) + " AP";
    if (_sec > 0) _costline += "   +" + string(_sec) + " " + _resname;
    var _cd = ability_cooldown(ab);
    if (_cd > 0) _costline += "   *   " + string(_cd) + "-turn cooldown";
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(228, 190, 90));
    draw_text(_tx, _hy + 60, _costline);
    // DETONATOR tag in the FIXED header (07-31, M: the reactions table lives in
    // the scrollable body, so long writeups hid an ability's detonator nature
    // entirely). Right-aligned on the cost row, measured so it can never touch
    // the cost text; drops to the school row if the cost line runs long.
    if (ability_is_detonator(ab)) {
        var _det_lbl = "DETONATOR";
        var _det_w   = string_width(_det_lbl);
        draw_set_color(make_color_rgb(190, 160, 240));
        if (_rx - _det_w > _tx + string_width(_costline) + 30) {
            draw_text(_rx - _det_w, _hy + 60, _det_lbl);
        } else {
            draw_text(_rx - _det_w, _hy + 93, _det_lbl);
        }
    }
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 160, 190));
    // School prefix (SYSTEMS_ELEMENT_SCHOOLS.md §E), e.g. "Fire  (ranged/spell)".
    var _detail_school = school_label(ability_school(ab));
    var _detail_class  = ability_attack_class_tag(ab);
    if (_detail_school != "") _detail_class = _detail_school + " school  " + _detail_class;
    draw_text(_tx, _hy + 93, _detail_class);

    _hy += 144;
    draw_set_color(make_color_rgb(60, 64, 90));
    draw_line(_lx, _hy, _rx, _hy);

    // --- Scrollbar indicator (only when the body overflows) ---
    if (global.ui_ability_detail_max_scroll > 0) {
        var _track_x = _x2 - 30;
        var _track_y1 = _body_top;
        var _track_y2 = _body_bottom;
        var _track_h  = _track_y2 - _track_y1;
        draw_set_color(make_color_rgb(40, 44, 64));
        draw_rectangle(_track_x, _track_y1, _track_x + 6, _track_y2, false);
        var _thumb_h = max(40, _track_h * (_visible_h / _natural_h));
        var _thumb_y = _track_y1 + (_track_h - _thumb_h) * (_scroll / global.ui_ability_detail_max_scroll);
        draw_set_color(make_color_rgb(150, 130, 200));
        draw_rectangle(_track_x, _thumb_y, _track_x + 6, _thumb_y + _thumb_h, false);
    }

    // ========================= FOOTER =========================
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    {   // every device (08-19 mouse pass: desktop footer named keys only - no click-out)
        // TOUCH (M 07-18: inspecting an ability in combat "touching does nothing
        // to exit the inspection" - the only way out was the top-right chip).
        // The keyboard footer names keys a phone doesn't have, so replace it with
        // a real CLOSE button, and also accept a tap on the dimmed area outside
        // the panel. Both fire Esc, which every caller of this popup already
        // handles - no per-screen wiring. Fixing it HERE covers combat, the
        // loadout and the trainer at once, since all three share this function.
        // RIGHT end of the footer strip - the only corner free of collisions:
        //  - NOT footer-centred: combat's End Turn button is 750..1170 x 856..916
        //    and draws EARLIER in the frame, so (taps being non-consuming) a
        //    centred CLOSE would also end the player's turn. 1368 clears 1170.
        //  - NOT the panel's top-right: the role-category chip pins itself to the
        //    right margin (_rx - _chip_w) whenever the ability name is long.
        // The footer's keyboard hint is replaced by this on touch, so it's empty.
        var _cb_w = 132, _cb_h = 52;
        var _cb_x2 = _x2 - 30,       _cb_y1 = _y2 - 60;
        var _cb_x1 = _cb_x2 - _cb_w, _cb_y2 = _cb_y1 + _cb_h;
        var _cb_on = mouse_check_button(mb_left)
            && point_in_rectangle(device_mouse_x_to_gui(0), device_mouse_y_to_gui(0),
                                  _cb_x1, _cb_y1, _cb_x2, _cb_y2);
        draw_set_alpha(_cb_on ? 0.95 : 0.82);
        draw_set_color(_cb_on ? make_color_rgb(52, 44, 26) : make_color_rgb(14, 16, 24));
        draw_rectangle(_cb_x1, _cb_y1, _cb_x2, _cb_y2, false);
        draw_set_alpha(1.0);
        draw_set_color(_cb_on ? make_color_rgb(245, 195, 80) : make_color_rgb(150, 130, 90));
        draw_rectangle(_cb_x1, _cb_y1, _cb_x2, _cb_y2, true);
        draw_set_valign(fa_middle);
        draw_set_color(_cb_on ? make_color_rgb(245, 205, 120) : make_color_rgb(215, 200, 165));
        draw_text((_cb_x1 + _cb_x2) / 2, (_cb_y1 + _cb_y2) / 2 + 1, "CLOSE");
        draw_set_valign(fa_top);

        if (touch_tapped(_cb_x1, _cb_y1, _cb_x2, _cb_y2)) touch_press(vk_escape);
        // Tap-outside-to-dismiss: anywhere on the scrim that isn't the panel.
        else if (touch_tapped(GUI_XL, 0, GUI_XR, GUI_H)
                 && !point_in_rectangle(device_mouse_x_to_gui(0), device_mouse_y_to_gui(0),
                                        _x1, _y1, _x2, _y2)) {
            touch_press(vk_escape);
        }
    }
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_trait_detail(tr)
// Full-screen Tab breakdown for a TRAIT (mirrors ui_draw_ability_detail). Traits
// are simpler - name + full description + class requirement.
// ---------------------------------------------------------------------------
function ui_draw_trait_detail(tr) {
    if (!is_struct(tr)) return;
    draw_set_alpha(0.80); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    var _x1 = 450, _y1 = 240, _x2 = 1470, _y2 = 810;
    draw_set_color(make_color_rgb(16, 17, 26));
    draw_rectangle(_x1, _y1, _x2, _y2, false);
    ui_draw_gothic_frame(_x1, _y1, _x2, _y2, 36);

    var _pad = 45, _lx = _x1 + _pad, _rx = _x2 - _pad, _y = _y1 + _pad;
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    // Trait icon badge beside the title.
    ui_draw_trait_icon(_lx, _y + 2, 72, tr);
    var _hx = _lx + 92;
    draw_set_font(fnt_ui_title);
    draw_set_color(c_white);
    draw_text(_hx, _y, tr.name);
    _y += 66;
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(190, 160, 240));
    var _cr = variable_struct_exists(tr, "class_req") ? tr.class_req : -1;
    var _crn = (_cr == 0) ? "Arcanist only" : ((_cr == 1) ? "Bloodwarden only" : ((_cr == 2) ? "Shadowstrider only" : "Any class"));
    draw_text(_hx, _y, "TRAIT  *  " + _crn);
    _y += 45;
    draw_set_color(make_color_rgb(60, 64, 90));
    draw_line(_lx, _y, _rx, _y);
    _y += 24;

    // "In plain terms" - the colloquial half (what it means for you).
    var _tr_colloq = variable_struct_exists(tr, "effect_id") ? trait_colloquial(tr.effect_id) : "";
    if (_tr_colloq != "") {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(150, 205, 150));
        draw_text(_lx, _y, "IN PLAIN TERMS");
        _y += 33;
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(206, 222, 206));
        draw_text_ext(_lx, _y, _tr_colloq, -1, _rx - _lx);
        _y += string_height_ext(_tr_colloq, -1, _rx - _lx) + 33;
    }

    // "Mechanics" - the technical half (the trait's own description).
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 175, 230));
    draw_text(_lx, _y, "MECHANICS");
    _y += 33;
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(210, 214, 230));
    if (variable_struct_exists(tr, "description")) draw_text_ext(_lx, _y, tr.description, -1, _rx - _lx);

    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 160, 190));
    draw_text_outline((_x1 + _x2) / 2, _y2 - 39, ((input_device() == 2) ? "Tap Back to close" : "[Tab] or [Esc] - Close"));
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_potency_detail()
// Full-screen Tab breakdown for the POTENCY mechanic (Vex tab 4). Colloquial +
// technical, same two-section format as the trait/ability detail popups.
// ---------------------------------------------------------------------------
function ui_draw_potency_detail() {
    draw_set_alpha(0.80); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    var _x1 = 450, _y1 = 210, _x2 = 1470, _y2 = 876;
    draw_set_color(make_color_rgb(16, 17, 26));
    draw_rectangle(_x1, _y1, _x2, _y2, false);
    ui_draw_gothic_frame(_x1, _y1, _x2, _y2, 36);

    var _pad = 45, _lx = _x1 + _pad, _rx = _x2 - _pad, _y = _y1 + _pad;
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(160, 115, 225));
    draw_text(_lx, _y, "Trait Potency");
    _y += 66;
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(190, 160, 240));
    draw_text(_lx, _y, "PERMANENT UPGRADE  *  Vex the Trainer");
    _y += 45;
    draw_set_color(make_color_rgb(60, 64, 90));
    draw_line(_lx, _y, _rx, _y);
    _y += 24;

    // Colloquial half.
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 205, 150));
    draw_text(_lx, _y, "IN PLAIN TERMS");
    _y += 33;
    var _pd_colloq = "Pour your raw talent into a trait to make it truly matter. Every trait you own can climb five ranks - each one deepening what the trait already does - and the fifth rank TRANSCENDS it: the trait changes how it works, not just how hard. A handy little perk becomes a defining strength of your build.";
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(206, 222, 206));
    draw_text_ext(_lx, _y, _pd_colloq, -1, _rx - _lx);
    _y += string_height_ext(_pd_colloq, -1, _rx - _lx) + 33;

    // Technical half.
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 175, 230));
    draw_text(_lx, _y, "MECHANICS");
    _y += 33;
    var _pd_tech = "EVERY unlocked trait has 5 Potency ranks. Numeric traits gain +10% strength per rank (ranks 1-4); other traits deepen a bespoke effect shown on their row. Costs climb by rank: 1-2 are gold + rune dust, 3-4 sacrifice permanent stat points of your choice (Vex's Companion favor takes one off), and rank 5 - the TRANSCEND - asks an Epic or better item and transforms the trait's behavior. Stat and item sacrifices are permanent - spend with intent. Potency only works while the trait is equipped.";
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(210, 214, 230));
    draw_text_ext(_lx, _y, _pd_tech, -1, _rx - _lx);

    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 160, 190));
    draw_text_outline((_x1 + _x2) / 2, _y2 - 39, ((input_device() == 2) ? "Tap Back to close" : "[Tab] or [Esc] - Close"));
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_reweave_detail()
// Full-screen Tab breakdown for the REWEAVE mechanic (Vael's Reweave tab -
// moved from Vex, M 07-28). Same two-section format as the potency detail popup.
// ---------------------------------------------------------------------------
function ui_draw_reweave_detail() {
    draw_set_alpha(0.80); draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    var _x1 = 450, _y1 = 210, _x2 = 1470, _y2 = 876;
    draw_set_color(make_color_rgb(16, 17, 26));
    draw_rectangle(_x1, _y1, _x2, _y2, false);
    ui_draw_gothic_frame(_x1, _y1, _x2, _y2, 36);

    var _pad = 45, _lx = _x1 + _pad, _rx = _x2 - _pad, _y = _y1 + _pad;
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(160, 115, 225));
    draw_text(_lx, _y, "Reweaving");
    _y += 66;
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(190, 160, 240));
    draw_text(_lx, _y, "TALENT WEB RESPEC  *  Vael the Aesthete");
    _y += 45;
    draw_set_color(make_color_rgb(60, 64, 90));
    draw_line(_lx, _y, _rx, _y);
    _y += 24;

    // Colloquial half.
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 205, 150));
    draw_text(_lx, _y, "IN PLAIN TERMS");
    _y += 33;
    var _rd_colloq = "Regret a weave? Vael can pull the threads loose - reweaving is, after all, an art. Unweaving an ability's talent web returns every point you spent on it, ready to be woven down a different branch - your practice with the ability is never lost, only the choices you made with it.";
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(206, 222, 206));
    draw_text_ext(_lx, _y, _rd_colloq, -1, _rx - _lx);
    _y += string_height_ext(_rd_colloq, -1, _rx - _lx) + 33;

    // Technical half.
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 175, 230));
    draw_text(_lx, _y, "MECHANICS");
    _y += 33;
    var _rd_tech = "Unweaving one ability costs 100 gold + 10 rune dust and clears ALL of that ability's woven web picks. Talent Points are earned by lifetime casts (10 / 30 / 60 / 100), so the cleared picks immediately return as pending points - reweave them from the loadout screen ([M] on the ability's row). Cast progress is never reset.";
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(210, 214, 230));
    draw_text_ext(_lx, _y, _rd_tech, -1, _rx - _lx);

    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 160, 190));
    draw_text_outline((_x1 + _x2) / 2, _y2 - 39, ((input_device() == 2) ? "Tap Back to close" : "[Tab] or [Esc] - Close"));
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_compendium_sections()
// Data for the Compendium / Help tab. Each section is { title, entries[] },
// each entry is { term, text }. Data-driven so new mechanics are a quick add -
// append a section here and it shows up in the menu automatically.
// ---------------------------------------------------------------------------
function ui_compendium_sections() {
    return [
        {
            title: "Damage Types",
            entries: [
                { term: "How types work", text: "The Damage TYPE decides how a hit is mitigated. There are four: Physical, Elemental, Void, and Blood. Each maps to a default school (see Damage Schools) but the TYPE is what Armor / wards check." },
                { term: "Physical",     text: "Weapon-based damage, reduced by the target's Armor. The default for most strikes and shots." },
                { term: "Elemental",    text: "Reduced by the target's Elemental Resistance (wards) instead of Armor. Covers the Fire, Frost, Shock and Arcane schools." },
                { term: "Void",         text: "Its own type (the Void school). Bypasses ALL mitigation - neither Armor nor wards reduce it. Void/drain hits often siphon life or resources from the target." },
                { term: "Blood",        text: "Its own type (the Blood school). Bypasses Armor (and isn't stopped by wards either). Many blood abilities are self-fuelled - they cost some of your own HP or Blood to cast." },
                { term: "Poison & DoTs", text: "Poison and other damage-over-time ticks land UNMITIGATED regardless of the type that applied them - Armor and wards don't reduce DoT ticks. Only your CONSTITUTION softens them: -0.5% per point, up to 15%." },
                { term: "Stat resists",  text: "Minor, always-on: each point of CONSTITUTION gives 0.5% to shrug off a Stun (and softens DoTs, above), WISDOM 0.5% to shrug Silence or Weaken, DEXTERITY 0.5% to shrug Root - each capped at 15%." },
            ],
        },
        {
            title: "Damage Schools",
            entries: [
                { term: "What schools are", text: "A school is the FLAVOR of an ability's damage (its build identity), layered on top of the Damage Type. The Damage Type still decides mitigation - a Fire spell is still Elemental for Armor/wards. The eight schools: Fire, Frost, Shock, Arcane, Blood, Void, Shadow, Poison." },
                { term: "School-damage gear", text: "Some gear grants \"+X <school> damage\" - a flat bonus added to every damaging ability of that school. It's always FLAT (never a percentage), so schools point your build in a direction without exploding your damage. A piece can carry more than one school, and they stack." },
                { term: "Schools vs. Types",  text: "Damage TYPE (Physical / Elemental / Void / Blood) = how the hit is mitigated. SCHOOL (Fire / Frost / Shock / Arcane / Blood / Void / Shadow / Poison) = what flavor it is and which \"+X school damage\" gear buffs it. A school bonus is mitigated by the ability's own Type - e.g. a Poison-school bonus on a physical dart is reduced by Armor, while a poison DoT it leaves is unmitigated." },
                { term: "Frost & Shock",      text: "These schools have few dedicated abilities yet - for now they come mostly from Frostbound / Storm-touched weapons, which apply the matching status. More school content arrives over time." },
            ],
        },
        {
            title: "Attack Classes",
            entries: [
                { term: "Melee Attack", text: "A physical strike at close range. Stopped by ROOT and by STUN." },
                { term: "Ranged Attack",text: "A physical shot from a distance. Stopped by STUN, but ROOT does not affect it." },
                { term: "Melee Spell",  text: "A spell cast at close range. Stopped by ROOT, SILENCE and STUN." },
                { term: "Ranged Spell", text: "A spell cast from a distance. Stopped by SILENCE and STUN, but not ROOT." },
            ],
        },
        {
            title: "Status Effects",
            entries: [
                { term: "Damage over Time", text: "The target loses HP at the start of each of its turns for a set number of turns." },
                { term: "Vulnerable",       text: "The target takes increased damage from all sources while it lasts." },
                { term: "Weaken",           text: "The target deals reduced damage while it lasts." },
                { term: "Blind",            text: "Accuracy is greatly reduced - most attacks miss until it wears off." },
                { term: "Mortality",        text: "Reduces the healing the target receives. Useful against enemies that mend themselves." },
                { term: "Stun",             text: "The target skips its entire next turn - blocks every kind of action." },
                { term: "Root",             text: "Blocks MELEE actions (attacks and spells). Ranged actions still work." },
                { term: "Silence",          text: "Blocks SPELLS (melee and ranged). Weapon attacks still work." },
                { term: "Burn",             text: "A small fire damage-over-time. Applied by Flaming weapons; feeds the burn reaction." },
                { term: "Frost",            text: "Chills the target so its attacks hit softer. Applied by Frostbound weapons; feeds the frost shatter." },
                { term: "Shock",            text: "Leaves the target shocked, taking extra damage per hit. Applied by Storm-touched weapons; feeds the shock arc." },
            ],
        },
        {
            title: "Status Reactions",
            entries: [
                { term: "Detonators",   text: "Snipe, Assassinate, Arcane Burst, Soul Nova, Rupture, Bonebreaker and Rift are DETONATORS - when they hit a target carrying a status, they trigger a reaction based on that status (and usually consume it). Set up the status, then detonate. Rift detonates EVERY enemy it hits - the cascade turn. Elemental weapons (Flaming/Frostbound/Storm-touched) are an easy way to apply burn/frost/shock for these. When your selected ability would react, the target's status badge PULSES and the hit preview names the bonus." },
                { term: "Overwhelmed",  text: "An enemy carrying 2 or more DIFFERENT status effects is OVERWHELMED: it takes +15% damage from ALL sources while they last (shown as a gold OVW+ badge). Mixing statuses - a bleed plus a chill, a hex plus a poison - beats stacking one." },
                { term: "Hexed",        text: "Curse marks a foe Hexed: it takes +4 damage per hit, any detonation on it has its bonus DOUBLED, and each detonation spreads +2 damage-taken to every other enemy. The Control piece of a detonation build." },
                { term: "Poison",       text: "Detonating poison applies Mortality: the target's healing is cut for 4 turns. Utility, not burst - answers self-healing foes." },
                { term: "Bleed",        text: "Detonating bleed bursts every remaining bleed tick at once for bonus damage." },
                { term: "Burn",         text: "Detonating a burning target strikes with +40% critical chance." },
                { term: "Root / Frost", text: "Detonating a rooted (or frozen) target shatters it for +30% damage." },
                { term: "Shock",        text: "Detonating shock arcs about a third of the hit to every other enemy. Against a lone foe it instead lands a +25% crit empowered strike." },
                { term: "Vulnerable",   text: "Detonating an Exposed (Vulnerable) target adds a flat damage bonus. The mark is NOT consumed - it's a multi-hit window." },
                { term: "Stun",         text: "Detonating a stunned target is a guaranteed critical hit." },
                { term: "Weaken",       text: "Detonating a weakened target deals +15% damage." },
                { term: "Blind",        text: "Detonating a blinded target cannot miss." },
                { term: "Void",         text: "Detonating a void damage-over-time heals you for 30% of the damage dealt." },
            ],
        },
        {
            title: "AP / Turn Economy",
            entries: [
                { term: "Action Points (AP)", text: "You have 3 AP each turn (4 with the Bloodwarden Relentless trait). Abilities and items spend AP; bigger abilities cost more." },
                { term: "Using Items",        text: "A consumable costs 1 AP on your turn. On an enemy's turn you may use 1 item free, once per enemy turn." },
                { term: "Ending Your Turn",   text: "Any AP you don't spend becomes POISE (see below) rather than being wasted. AP refills back to 3 at the start of your next turn." },
                { term: "Poise",              text: "Each unspent AP at the end of your turn becomes 2 shield - up to 6 (8 with Relentless). This POISE guard lasts only until your next turn (it does not stack turn to turn), so a held turn braces you for a telegraphed blow. Attacking is still usually better - the low rate is a floor, not a plan. The legendary Aegis of the Unbroken Line raises the rate to 3 per AP and doubles the cap." },
                { term: "Armor",              text: "Percentage damage reduction with diminishing returns: your total Armor (base 1 + gear + boons) reduces every physical hit by Armor / (Armor + 15), capped at 60% - 5 Armor is 25% off, 10 is 40%, 15 is 50%. Iron Skin is a separate flat cut on top. A landed hit always deals at least 1. Armor is inherent to what you wear - HEAVY gear (plate, mail; STR/CON pieces) carries the most, MEDIUM (leather, hide; DEX pieces) some, and CLOTH (robes, hoods) none - cloth chest and helm pieces ward with Elem. Resist instead. Shields, of-Warding affixes and boons add more." },
                { term: "Interrupt",          text: "Land a STUN or ROOT on an enemy that is winding up a charged or heavy attack and you INTERRUPT it: the telegraph is answered AND you get 1 AP back (once per turn). Read the intent, punish it, keep tempo." },
            ],
        },
        {
            title: "Class Resources",
            entries: [
                { term: "What they are", text: "Each class banks a SECONDARY resource (cap 10) alongside AP. Generator abilities build it, payoff abilities spend it. Your bank carries between fights on the same floor - AP resets, the resource doesn't." },
                { term: "Souls (Arcanist)", text: "Built two ways: soul-generating spells (each states its gain - Soulfire +2, Void Drain +1, Scorch and Blazing Palm +1), and the Arcanist CLASS PASSIVE - +2 Souls every time one of your hits kills an enemy. Both can land in the same cast: a killing Soulfire logs +2 for the spell AND +2 for the kill. Spent by Arcane Burst, Soul Nova, Arcane Echo, Event Horizon and Soul Rend." },
                { term: "Blood (Bloodwarden)", text: "Built through bloodshed: +1 Blood each time YOU take a hit (class passive), plus blood-feeding abilities like Blood Leech. Spent by your crimson payoffs - many also cost HP, so the reserve is life you've set aside." },
                { term: "Preparation (Shadowstrider)", text: "+1 Preparation at the start of each of your turns while no trap of yours is armed (class passive), plus refunds from clean plays like Evasive Roll. Spent to power traps and executes." },
                { term: "OVERCHARGE", text: "Cast a resource-spending ability while your reserve is FULL (10) and it drains the WHOLE reserve: every point beyond what the ability itself uses adds +2 damage (or +2 healing / shield on self-spenders like Blood Surge and Sanguine Pact). The resource bar glows gold when full, and eligible abilities pulse gold on the combat bar. Abilities that scale with the reserve HELD (Soul Shield, Arcane Echo) don't Overcharge - they're already the hoard's payoff. Hoard, then cash out." },
            ],
        },
        {
            title: "Ability Synergy",
            entries: [
                { term: "Role Categories", text: "Every ability has a role: OFFENSE (deal damage), DEFENSE (protect yourself), SUPPORT (heal / buff / resource) or CONTROL (debuff / crowd-control). Buttons and ability rows are colour-coded - offense red, defense blue, support green, control purple." },
                { term: "Same-Role Discount", text: "After you cast an ability of a role this turn, every LATER ability of the SAME role costs 1 less AP. SUPPORT abilities can drop all the way to 0 AP (free); every other role floors at 1 AP. The first of each role pays full price; the discount resets at the start of your next turn." },
                { term: "Why it matters", text: "Stacking same-role abilities is cheaper, so minor buffs become worth casting together - e.g. a 1-AP support after another support this turn becomes FREE, and Bloodthorn Aura (2 AP) then Iron Skin (2-1 = 1 AP) is 3 AP for both, not 4. On the combat bar a discounted ability shows GREEN AP pips at its reduced cost." },
                { term: "What's discounted", text: "Only AP is reduced - secondary resources (Souls / Blood / Preparation) always cost full. Free (0-AP) abilities stay free. The discount stacks with other AP reductions like Quickcast." },
            ],
        },
        {
            title: "Timed Combat",
            entries: [
                { term: "The Guard Ring",  text: "When an enemy attack or spell comes for you, a ring CLOSES onto your hero. Press SPACE / click / tap (or pad A) as it lands: the GOLD core is a PERFECT parry - the blow is NEGATED and answered with a riposte (a riposte kill cancels the attack outright). The blue band is a GOOD block: half damage. Late, early or no press: the full hit. The ring is honest about danger - chip hits give a wide gold core, killing blows a razor-thin one. Read the intent chips." },
                { term: "The Strike Ring", text: "Your own damaging casts hang for a beat while an ember ring closes on the target. Press in the gold band for a TRUE STRIKE (+15% damage) - and ANY press turns an outright miss into a 50% glancing hit instead of a whiff. Cheap 1-AP casts have a generous gold band; 3-AP finishers demand real precision." },
                { term: "Your dodge kit",  text: "Blink, Shadow Step, Phantom Step and the like are the timing done FOR you - while one is armed, no guard ring appears and the ability resolves as always." },
                { term: "Modes",           text: "F1 in combat cycles ON / ASSIST / OFF (saved between sessions). ASSIST widens every window - built for touch screens. OFF returns combat to the classic untimed rules, enemy pressure included." },
            ],
        },
        {
            title: "Hit & Crit",
            entries: [
                { term: "Accuracy",        text: "First the attacker rolls to connect (its Accuracy, capped 5-99%). A failure is a MISS. Blind lowers Accuracy sharply." },
                { term: "Dodge",           text: "If an attack connects, the defender rolls their Dodge % to evade it - that's a DODGE (shown separately from a miss). High DEX raises Dodge, with diminishing returns." },
                { term: "Guaranteed Hit",  text: "Some abilities always land - they ignore accuracy, Dodge and Blind entirely." },
                { term: "Critical Hits",   text: "A successful crit deals bonus damage. Every ability rolls ONE of four crit types, each fed by a stat: Power (STR), Precision (DEX), Arcane (INT) or Effect (WIS) - so spells crit off INT, weapon strikes off STR or DEX." },
                { term: "Crit on gear",    text: "\"+X% crit\" from gear affixes is added to ALL crit rolls - physical, spell and effect alike. Typed sources say so: \"Phys crit\" (Keen runes, Bonefang affixes) only touches weapon attacks; \"Spell crit\" (the Surge rune aspect) only spells. The four Crit rows on your character sheet already include your gear bonus." },
            ],
        },
        {
            title: "Progression",
            entries: [
                { term: "Ascendance",  text: "Meta-progression earned across runs. Spend it to permanently strengthen your character between dives." },
                { term: "Run Leveling",text: "Within a run you gain XP from kills, leveling up and growing stronger for that dive only." },
                { term: "Traits",      text: "Passive perks trained at Vex. They cost gold plus a rarity-matched item and slot into your build." },
                { term: "Talent Webs", text: "Every ability has a web of 6 nodes in two branches - POWER (raw output) and TWIST (tempo and reliability) - each ending in a keystone. Casting an ability earns its Talent Points (at 10/30/60/100 lifetime casts), spent in the web from the loadout with M. You can only weave 4 of the 6 nodes, so every ability takes a shape - your Snipe isn't my Snipe. Regret a weave? Vael the Aesthete's REWEAVE tab unweaves an ability (100g + 10 dust) and returns its points." },
                { term: "The Whetstone", text: "A once-per-run altar in the dungeon: borrow one reachable unwoven web node on a slotted ability for the rest of the run, free - even past the 4-node cap." },
                { term: "Class Trunk", text: "Your class passive grows a TRUNK of five rows, opening at permanent levels 2 / 5 / 8 / 11 / 14. Each row offers two upgrades and you keep ONE - the other is lost for that character, so two max-level Arcanists still play differently. Nothing is spent: reaching the level IS the unlock. Choose it on the Abilities tab ([I], then A/D across to CLASS TRUNK). Regrets go to Vael: her Reweave tab UNMAKES the whole trunk for 500g + 50 dust, reopening every row." },
                { term: "Interrupted Dives", text: "If the game closes mid-run - a crash, a phone call, a quit to title - the dive is NOT lost: loading that save returns you to the dungeon where you left off, gear and gold intact. Iron rules apply: the run can only end in extraction or death, a re-entered fight starts fresh at the health you carried in, and a death stands the moment it lands. Ironwake does not forget." },
                { term: "The Iron Vow", text: "An oath sworn at character creation, never changeable after. THE IRON VOW grants three lives - every defeat consumes one, and the third death erases the save forever. THE UNBROKEN VOW grants one. A fallen character leaves only a gravestone on their save slot. Those who see IRONWAKE STANDS under a Vow are named for it: the Thrice-Tempered, or the Unbroken. (Not to be confused with the Descent's Hardcore severity, which risks only your gear.)" },
                { term: "Trait Potency", text: "Every unlocked trait climbs 5 ranks at Vex. Ranks 1-2 cost gold + dust, 3-4 sacrifice permanent stat points, and rank 5 TRANSCENDS the trait - an Epic+ offering transforms how it works (Last Stand triggers per floor, Phantom Step banks a true dodge, Battle Hardened loses its cap...). Potency only works while the trait is equipped." },
                { term: "Awakening Boost", text: "Clearing a dungeon at its highest unlocked Awakening for the first time lets the triumph SPREAD: a popup on your return offers +1 Awakening to one other dungeon of your choice (up to A5). Re-farming a cleared tier earns nothing - only new summits echo." },
                { term: "Beyond Infernal (A6+)", text: "Once Ironwake stands, the Awakenings never end. Clearing the frontier tier in ANY dungeon opens the next tier in ALL of them. Past A5 enemies compound +12% per tier, rewards keep pace, and loot's numbers grow beyond their old caps." },
                { term: "The Descent", text: "Unlocked by the triple-A5 win: an endless fall where every floor is a random dungeon, each deeper than the last. After each floor boss, RETREAT to bank everything or DESCEND for more. Depthforged finds scale with the floor - and leave with you. Spectral couriers can send finds home mid-fall; a hardcore severity lets death claim even worn gear (Salvager's Transcend defies it). Your deepest floor is remembered." },
                { term: "Endless Runes (IV+)", text: "Once Ironwake stands, Maren's combine loses its tier-III ceiling: three identical tier-III runes fuse into IV, three IVs into V, forever - each step doubling the gold + dust toll while the rune's power compounds. Fuel for breaking the Descent." },
                { term: "Rune Transmute & Blueprints", text: "Spare runes have two roads. Sable TRANSMUTES any 3 same-tier runes into one RANDOM rune of the next tier (a gamble, small fee). Petra brokers BLUEPRINTS: trade her 5 same-tier runes plus gold and she delivers the exact rune YOU choose at that tier after a few floor clears - her single order slot handles gear trades or blueprints, one at a time." },
                { term: "Sable's Cauldron", text: "Sable brews the full potion shelf - baseline salves and tonics for pocket change up through her alchemy exclusives. Three IDENTICAL potions fuse into their improved form on her Fusion tab, and ANY 3 mismatched potions can become a CHAOTIC BREW: what it does is rolled the moment you drink it - a rush of healing, a ward, vigor, ley-light... and sometimes it bites on the way down." },
                { term: "Legendary Sinks", text: "A legendary that bores you has three roads. Dorn RECASTS it for gold into a different legendary (he avoids ones you own). Maren SUNDERS it into parts - a Legendary Ingot, 50 dust and a tier-III rune. And Sable's CURSED REBIRTH feeds it to the dark: it returns with its numbers surged half again, still carrying its power - but branded with a real curse. Power with teeth." },
                { term: "The Legendary Forge", text: "All three vendors hold one piece of Ironwake's oldest craft. Dorn strikes the MYTHRIL FRAME (gold + a Legendary Ingot), Maren seals a RUNEHEART CORE (a tier-III+ rune + dust), and Sable distills QUINTESSENCE (three different specialty brews + gold). Bring all three to Dorn's forge [V]: choose its shape, choose its power, and NAME it - a legendary that exists nowhere else, because you made it." },
            ],
        },
        {
            title: "Item Rarities",
            entries: [
                { term: "Common",    text: "Plain gear with minimal or no affixes. The baseline drop." },
                { term: "Uncommon",  text: "A small affix or two - a modest step up from Common." },
                { term: "Rare",      text: "Several affixes; a meaningful upgrade worth equipping." },
                { term: "Epic",      text: "Strong, multi-affix gear that can anchor a build." },
                { term: "Legendary", text: "Hand-crafted uniques with build-defining powers. The rarest drops." },
                { term: "Requirements", text: "Rare and better weapons and heavy armor demand a minimum stat (STR/DEX/INT/CON) to wield - a greatsword needs STR, a bow needs DEX, a focus needs INT. If you don't meet it, the item can't be equipped; the requirement shows in red on its tooltip." },
            ],
        },
        {
            title: "Companions",
            entries: [
                { term: "Life & care", text: "Creatures grow through five stages (Hatchling to Awakened) by completing runs as your ACTIVE companion with a full growth bar (feed fills it). They have HUNGER (a well-fed pet banks growth; a starving one is benched) and an HP pool - intercepted blows hurt them, and at 0 HP they are injured and benched for the run. Food heals and feeds; treats deepen bond (2 per run)." },
                { term: "Bond", text: "Bond deepens by +1 per survived run with it active (+1 more ending the run well-fed); treats add more. Devoted at 10, Soul-bound at 18 - Soul-bound is required for the Awakened crossing (full clear at Awakening 5)." },
                { term: "Types", text: "FORTUNE grants passive boons (gold, loot find, luck) and never fights. WARRIOR strikes an enemy every turn and has stances (Aggressive / Guarded / Assist). GUARDIAN heals and shields you (Balanced / Mender / Warder / Cleanser stances)." },
                { term: "Gifts", text: "A raised creature makes exactly TWO permanent picks: its Stage-3 CAPSTONE (choose at Bairc's when it reaches Adult) and its Stage-4 AWAKENED SPLASH (one effect borrowed from a DIFFERENT type). Pending picks never expire - Bairc holds them until you choose. Wild-caught creatures roll theirs." },
                { term: "Corruption", text: "A corrupted creature PUSHES for 3 survived runs as your active companion: you pay -20% max HP and -10% damage while it pushes, and it gains a permanent +15% to its passive per run. CURE at Bairc's any time = keep the gains, lose the grand power. Fully corrupted = its passive is +45% forever, the burden ends, and it gains a grand boon: Fortune pays extra gold and loot find, a Warrior's strike becomes a cleave against ALL enemies, a Guardian heals AND shields every turn." },
            ],
        },
        {
            title: "Townsfolk",
            entries: [
                { term: "Affinity", text: "Camp folk remember you. Trading, feeding, gifting and quests all deepen affinity; gifts they LIKE or LOVE count for more. Higher tiers unlock each vendor's personal perks (discounts, extra stock, services)." },
                { term: "Gates", text: "Past Acquaintance, each new tier is a GATE: crossing takes a FAVOR - a gate quest posted on the tavern board. Finish it, then deepen the bond with them in camp." },
                { term: "Neglect & limits", text: "Friendships DECAY if ignored for many runs, and only a limited number of townsfolk can hold the deepest tiers at once - deepening one may demote another. Betrayal (robbing the one who trusts you) is remembered hardest of all." },
            ],
        },
    ];
}

// ---------------------------------------------------------------------------
// ui_draw_character_menu()
// Draws the full-screen character menu overlay.
// Called by each room controller's Draw GUI so it renders regardless of which
// room is active (obj_game_controller's own Draw GUI is unreliable when the
// object is persistent and sprite-less).
// ---------------------------------------------------------------------------
// =============================================================================
// SHARED TOAST (08-04, M standing order after the equip-notif collision: every
// transient floating notice draws TOPMOST with a measured backdrop box - never
// bare draw_text over a busy screen). Call at the END of the owning screen's
// Draw so nothing paints over it, and document the y-band in UI_BANDS.md.
// =============================================================================
function ui_draw_toast(_msg, _cx, _ytop, _alpha = 1.0, _txt_col = undefined) {
    if (_msg == "" || _alpha <= 0) return;
    if (_txt_col == undefined) _txt_col = make_color_rgb(100, 220, 130);
    draw_set_font(ui_font(fnt_ui));
    var _tw = string_width(_msg);
    var _th = string_height(_msg);
    var _x1 = _cx - _tw / 2 - 27, _x2 = _cx + _tw / 2 + 27;
    var _y2 = _ytop + _th + 24;
    draw_set_alpha(0.93 * _alpha);
    draw_set_color(make_color_rgb(16, 18, 30));
    draw_rectangle(_x1, _ytop, _x2, _y2, false);
    draw_set_alpha(_alpha);
    draw_set_color(make_color_rgb(245, 195, 80));
    draw_rectangle(_x1, _ytop, _x2, _y2, true);
    draw_set_halign(fa_center);
    draw_set_valign(fa_top);
    draw_set_color(_txt_col);
    draw_text(_cx, _ytop + 12, _msg);
    draw_set_halign(fa_left);
    draw_set_color(c_white);
    draw_set_alpha(1.0);
}

// =============================================================================
// FIND BANNER (M 08-18: "I have no idea when I got this pet ... whenever a pet,
// banshee bottle, or egg is found there should always be a popup"). A queued,
// topmost, boxed announcement - big colored name + icon - that every grant path
// pushes (pet_grant_from_source, banshee grants), ticked in gc Step (runs in every
// room), drawn LAST by each room controller's Draw GUI right after the trait toast.
// Non-blocking by design (no input stall risk before launch): 6.5s each, queued.
// =============================================================================
function find_banner_push(_kind, _name, _sub, _spr = -1) {
    if (!variable_global_exists("find_queue")) global.find_queue = [];
    array_push(global.find_queue, { kind: _kind, name: _name, sub: _sub, spr: _spr, t: -1 });
}
function find_banner_tick() {
    if (!variable_global_exists("find_queue") || array_length(global.find_queue) == 0) return;
    var _b = global.find_queue[0];
    if (_b.t < 0) {
        _b.t = 390;
        var _sting = (_b.kind == "banshee") ? snd_loot_unique : ((_b.kind == "egg") ? snd_egg_stir : snd_loot_legendary);
        if (audio_exists(_sting)) audio_play_sound(_sting, 1, false);
    }
    _b.t--;
    if (_b.t <= 0) array_delete(global.find_queue, 0, 1);
}
function find_banner_up() {
    return variable_global_exists("find_queue") && array_length(global.find_queue) > 0 && global.find_queue[0].t >= 0;
}
function ui_draw_find_banner() {
    if (!find_banner_up()) return;
    var _b  = global.find_queue[0];
    var _a  = min(1, _b.t / 30, (390 - _b.t + 1) / 8);   // fade in fast, out over the last 0.5s
    var _kc = make_color_rgb(150, 230, 170);               // pet found alive
    if (_b.kind == "egg")       _kc = make_color_rgb(255, 215, 120);
    if (_b.kind == "banshee")   _kc = make_color_rgb(140, 220, 235);
    if (_b.kind == "corrupted") _kc = make_color_rgb(205, 145, 245);
    draw_set_font(fnt_ui_title);
    var _nw = string_width(_b.name);
    draw_set_font(ui_font(fnt_ui_small));
    var _sw = string_width(_b.sub);
    var _icon = 96, _padx = 28;
    var _w  = max(_nw, _sw, 300) + _icon + _padx * 3;
    var _h  = 156;
    var _x0 = GUI_CX - _w / 2, _y0 = 200;
    var _pulse = 0.5 + 0.5 * sin(current_time / 160);
    draw_set_alpha(0.94 * _a);
    draw_set_color(make_color_rgb(14, 14, 24));
    draw_rectangle(_x0, _y0, _x0 + _w, _y0 + _h, false);
    draw_set_alpha(_a);
    draw_set_color(merge_color(make_color_rgb(200, 165, 80), c_white, 0.25 * _pulse));
    draw_rectangle(_x0, _y0, _x0 + _w, _y0 + _h, true);
    draw_rectangle(_x0 + 4, _y0 + 4, _x0 + _w - 4, _y0 + _h - 4, true);
    // Icon box (origin-aware centering; pet art is often feet-anchored).
    var _ix = _x0 + _padx, _iy = _y0 + (_h - _icon) / 2;
    draw_set_alpha(0.6 * _a); draw_set_color(make_color_rgb(30, 28, 44));
    draw_rectangle(_ix, _iy, _ix + _icon, _iy + _icon, false);
    draw_set_alpha(_a);
    if (_b.spr >= 0 && sprite_exists(_b.spr)) {
        var _sw2 = sprite_get_width(_b.spr), _sh2 = sprite_get_height(_b.spr);
        var _sc  = (_icon - 8) / max(1, max(_sw2, _sh2));
        var _fr  = (sprite_get_number(_b.spr) > 1) ? ((current_time div 120) mod sprite_get_number(_b.spr)) : 0;
        draw_sprite_ext(_b.spr, _fr,
            _ix + (_icon - _sw2 * _sc) / 2 + sprite_get_xoffset(_b.spr) * _sc,
            _iy + (_icon - _sh2 * _sc) / 2 + sprite_get_yoffset(_b.spr) * _sc,
            _sc, _sc, 0, c_white, _a);
    }
    var _tx = _ix + _icon + _padx;
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(235, 200, 120));
    draw_text(_tx, _y0 + 18, (_b.kind == "banshee") ? "Y O U ' V E   F O U N D   A   S O N G" : "Y O U ' V E   F O U N D");
    draw_set_font(fnt_ui_title);
    draw_set_color(_kc);
    draw_text(_tx, _y0 + 46, _b.name);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(200, 205, 215));
    draw_text(_tx, _y0 + 112, _b.sub);
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white); draw_set_alpha(1.0);
}

function ui_draw_character_menu() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!_gc.menu_open) return;
    var menu_tab = _gc.menu_tab;
    var items_used_this_turn = _gc.items_used_this_turn;

    // Tab 4 = Companion (M 07-08): the active pet's full profile, in the slot the
    // old in-menu Compendium vacated when it moved to the Journal (2026-07-04).
    var tab_names = ["Stats", "Equipment", "Abilities", "Consumables", "Companion"];

    draw_set_alpha(1.0);
    draw_set_color(c_white);

    // Full screen dark overlay
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(8, 10, 18));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    // Ornate gothic rim around the whole menu (matches the NPC shop overlays). Drawn
    // before the tab bar so the tabs sit cleanly on top of the top band.
    ui_draw_gothic_frame(30, 30, 1890, 1050, 30);

    // Tab bar at top (matches click zones in obj_game_controller Step). Count comes
    // from tab_names so removing/adding a tab can't strand this loop again - the
    // hardcoded 5 crashed here when the Compendium moved to the Journal (7-04).
    var _tab_w = 252;
    var _tab_h = 66;
    var _tab_y = 30;
    // Center the bar for however many tabs exist (the old fixed 306 start was
    // centered for FIVE tabs and skewed left once the Compendium moved out).
    var _tab_x0 = (GUI_W - (array_length(tab_names) * (_tab_w + 12) - 12)) / 2;
    draw_set_font(ui_font(fnt_ui));
    for (var _t = 0; _t < array_length(tab_names); _t++) {
        var _tx = _tab_x0 + _t * (_tab_w + 12);
        if (_t == menu_tab) {
            draw_set_color(make_color_rgb(30, 50, 90));
            draw_rectangle(_tx, _tab_y, _tx + _tab_w, _tab_y + _tab_h, false);
            draw_set_color(make_color_rgb(80, 140, 220));
            draw_rectangle(_tx, _tab_y, _tx + _tab_w, _tab_y + _tab_h, true);
        } else {
            draw_set_color(make_color_rgb(20, 25, 40));
            draw_rectangle(_tx, _tab_y, _tx + _tab_w, _tab_y + _tab_h, false);
            draw_set_color(make_color_rgb(50, 60, 80));
            draw_rectangle(_tx, _tab_y, _tx + _tab_w, _tab_y + _tab_h, true);
        }
        draw_set_halign(fa_center);
        draw_set_valign(fa_middle);
        draw_set_color((_t == menu_tab) ? c_white : make_color_rgb(140, 150, 170));
        draw_text(_tx + _tab_w / 2, _tab_y + _tab_h / 2, tab_names[_t]);
        // Class-trunk pending badge (P2, 08-05): an unlocked, unpicked trunk row
        // waits behind the Abilities tab - the gold count marks the milestone.
        if (_t == 2) {
            var _tb_pend = trunk_pending_count(variable_global_exists("chosen_class") ? global.chosen_class : 0);
            if (_tb_pend > 0) {
                draw_set_color(make_color_rgb(230, 190, 80));
                draw_circle(_tx + _tab_w - 20, _tab_y + 12, 13, false);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(30, 24, 10));
                draw_text(_tx + _tab_w - 20, _tab_y + 13, string(_tb_pend));
                draw_set_font(ui_font(fnt_ui));
            }
        }
    }
    draw_set_valign(fa_top);
    draw_set_halign(fa_left);
    draw_set_font(-1);

    // Get player reference if in combat
    var _player    = undefined;
    var _in_combat = instance_exists(obj_combat_controller);
    if (_in_combat) {
        var _ctrl = instance_find(obj_combat_controller, 0);
        _player = _ctrl.player;
    }

    // Out-of-combat fallback - build a read-only view from globals so Stats and
    // Abilities tabs are populated when the menu is opened in the hub or floor map.
    // Always copies chosen_stats and applies equipment bonuses - never mutates the global.
    if (_player == undefined && variable_global_exists("chosen_stats") && !is_undefined(global.chosen_stats)) {
        var _base        = global.chosen_stats;
        var _stats_view  = {
            class_id:    _base.class_id,
            class_name:  _base.class_name,
            STR:         _base.STR,
            DEX:         _base.DEX,
            CON:         _base.CON,
            INT:         _base.INT,
            WIS:         _base.WIS,
            CHA:         _base.CHA,
            free_points: _base.free_points,
        };
        var _sv_bonus = apply_equipment_stats(_stats_view);
        // Add run stat bonuses from XP leveling
        if (variable_global_exists("run_stat_bonuses")) {
            _stats_view.STR += global.run_stat_bonuses.STR;
            _stats_view.DEX += global.run_stat_bonuses.DEX;
            _stats_view.CON += global.run_stat_bonuses.CON;
            _stats_view.INT += global.run_stat_bonuses.INT;
            _stats_view.WIS += global.run_stat_bonuses.WIS;
            _stats_view.CHA += global.run_stat_bonuses.CHA;
        }
        // Add permanent meta-progression bonuses
        if (variable_global_exists("perm_str_bonus")) {
            _stats_view.STR += global.perm_str_bonus;
            _stats_view.DEX += global.perm_dex_bonus;
            _stats_view.CON += global.perm_con_bonus;
            _stats_view.INT += global.perm_int_bonus;
            _stats_view.WIS += global.perm_wis_bonus;
            _stats_view.CHA += global.perm_cha_bonus;
        }
        var _derived_view = stats_derive(_stats_view);
        // Use the shared out-of-combat max so the Stats tab matches the floor HUD
        // and the in-fight bar (includes Thick Skin's static +10% and boon/curse mults).
        var _sv_max_hp = out_of_combat_max_hp();
        _player = {
            class_id:  global.chosen_class,
            stats:     _stats_view,
            HP:        (variable_global_exists("run_current_hp") && global.run_current_hp > 0)
                           ? min(global.run_current_hp, _sv_max_hp)   // clamp: gear may have changed since capture
                           : _sv_max_hp,
            max_HP:    _sv_max_hp,
            abilities: abilities_resolve_player_loadout(global.chosen_class),
            dodge:     _derived_view.DODGE + _sv_bonus.dodge_flat,
        };
    }

    var _content_y = 135;
    var _pad       = 60;

    // ---- STATS TAB ----
    if (menu_tab == 0) {
        if (_player != undefined) {
            var _stats       = _player.stats;
            var _derived     = stats_derive(_stats);
            var _class_names = ["Arcanist", "Bloodwarden", "Shadowstrider"];
            var _class_id    = clamp(_player.stats.class_id, 0, 2);
            var _dc = make_color_rgb(120, 140, 170);
            var _hc = make_color_rgb(80, 110, 160);

            // Flat crit % added to EVERY school in actual combat: equipment crit_flat
            // ("of Ruin" affix / Keen runes -> player.stats.crit_bonus) plus the Duelist
            // boon. stats_derive only returns the stat-scaled portion, so the panel used
            // to under-report the real crit rate - fold the flat bonus in here.
            var _crit_flat = 0;
            if (_in_combat && variable_struct_exists(_player.stats, "crit_bonus")) {
                _crit_flat = _player.stats.crit_bonus;
            } else {
                _crit_flat = apply_equipment_stats({}).crit_flat;
            }
            _crit_flat += boon_value("duelist");

            // ---- Header: class + level + HP ----
            draw_set_font(fnt_ui_title);
            draw_set_color(make_color_rgb(80, 160, 220));
            var _class_str = _class_names[_class_id];
            draw_text(_pad, _content_y, _class_str);
            // Measure the class name in its (title) font so the level text can sit just
            // right of it instead of at a fixed x - a long name (Shadowstrider) in the
            // large title font used to run past the fixed offset and overlap "Level X".
            var _class_w = string_width(_class_str);
            // Character level = PERMANENT (meta) level (1 + perm points earned), which
            // persists in the hub - the old display showed run_level, which resets to 1
            // each run so the hub read "Level 1" forever. During an actual fight, also
            // show the per-dive level so in-run progression stays visible.
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(210, 200, 120));
            var _lvl_txt = "Level " + string(player_permanent_level());
            // In a run (on a floor or fighting), append the per-dive level so in-run
            // progression stays visible; the hub has no run, so it shows only the
            // permanent level.
            var _in_run = _in_combat || instance_exists(obj_floor_controller);
            if (_in_run && variable_global_exists("run_level")) {
                _lvl_txt += "   (Dive Lv " + string(global.run_level) + ")";
            }
            // Auto-position: just past the class name (+40 gap), but never left of the
            // original 450 offset so short names keep the familiar layout.
            var _lvl_x = _pad + max(450, _class_w + 40);
            draw_text(_lvl_x, _content_y + 9, _lvl_txt);
            draw_set_color(c_white);
            draw_text(_pad, _content_y + 66, "HP: " + string(_player.HP) + " / " + string(_player.max_HP));

            // ---- Epithet (expression #5): equipped title + [T] picker hint ----
            // Sits right of the HP readout on the same line; earned-count in the hint
            // so a fresh character knows titles exist before earning one.
            var _ep_txt = player_epithet_text();
            var _ep_n   = epithet_unlocked_count();
            draw_set_font(ui_font(fnt_ui_small));
            // Touch (07-24 audit): tapping the title line cycles it (simulated T);
            // the hint reads "tap" instead of "[T]" on a phone.
            var _ep_hint_verb = (input_device() == 2) ? "tap:" : "[T]";
            if (_ep_txt != "") {
                draw_set_color(make_color_rgb(205, 170, 235));
                draw_text(_lvl_x, _content_y + 69, "\"" + _ep_txt + "\"");
                draw_set_color(make_color_rgb(110, 100, 130));
                draw_text(_lvl_x + string_width("\"" + _ep_txt + "\"") + 30, _content_y + 72, _ep_hint_verb + " change title (" + string(_ep_n) + " earned)");
            } else {
                draw_set_color(make_color_rgb(110, 100, 130));
                draw_text(_lvl_x, _content_y + 72, (_ep_n > 0)
                    ? _ep_hint_verb + " choose a title (" + string(_ep_n) + " earned)"
                    : "Untitled - deeds in the dark earn titles");
            }
            if (input_device() == 2 && _ep_n > 0
                && touch_tapped(_lvl_x - 12, _content_y + 54, _lvl_x + 600, _content_y + 105)) {
                touch_press(ord("T"));
            }
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(c_white);

            // ---- Stat grid (two compact columns) ----
            // The shown value is the TOTAL (base + gear). Hovering a stat pops a small
            // box splitting it into Base vs Gear, because equip requirements test BASE
            // only - so a stat reading 17 (12 of it from gear) can still fail a 12 req.
            draw_set_font(ui_font(fnt_ui_small));
            var _stat_keys = ["STR", "DEX", "CON", "INT", "WIS", "CHA"];
            var _smx = device_mouse_x_to_gui(0);
            var _smy = device_mouse_y_to_gui(0);
            var _hover_stat = -1;
            for (var _s = 0; _s < 6; _s++) {
                var _sx  = _pad + floor(_s / 3) * 285;     // 0=STR/DEX/CON, 1=INT/WIS/CHA
                var _sy  = _content_y + 126 + (_s mod 3) * 48;
                var _is_hov = (_smx >= _sx && _smx <= _sx + 255 && _smy >= _sy && _smy <= _sy + 42);
                if (_is_hov) _hover_stat = _s;
                draw_set_color(_is_hov ? make_color_rgb(200, 215, 245) : make_color_rgb(140, 160, 200));
                draw_text(_sx, _sy, _stat_keys[_s] + ":");
                draw_set_color(c_white);
                draw_text(_sx + 84, _sy, string(variable_struct_get(_stats, _stat_keys[_s])));
            }
            // Base-vs-gear popup for the hovered stat (drawn after the grid so it sits on top).
            if (_hover_stat >= 0) {
                var _hs_key   = _stat_keys[_hover_stat];
                var _hs_total = variable_struct_get(_stats, _hs_key);
                var _hs_base  = player_base_stat(_hs_key);
                var _hs_gear  = _hs_total - _hs_base;
                var _hs_line2 = (_hs_gear != 0)
                              ? ((_hs_gear > 0 ? "Gear: +" : "Gear: ") + string(_hs_gear)) : "";
                var _hs_note  = "Equip requirements use Base.";
                var _hs_w = max(string_width(_hs_note), string_width(_hs_key + ": " + string(_hs_total))) + 36;
                var _hs_h = (_hs_line2 != "") ? 138 : 108;
                var _hs_x = _smx + 24;
                var _hs_y = _smy + 12;
                if (_hs_x + _hs_w > GUI_W) _hs_x = GUI_W - _hs_w - 6;
                draw_set_alpha(0.95);
                draw_set_color(make_color_rgb(18, 20, 32));
                draw_rectangle(_hs_x, _hs_y, _hs_x + _hs_w, _hs_y + _hs_h, false);
                draw_set_alpha(1.0);
                draw_set_color(make_color_rgb(120, 140, 190));
                draw_rectangle(_hs_x, _hs_y, _hs_x + _hs_w, _hs_y + _hs_h, true);
                var _hs_ty = _hs_y + 12;
                draw_set_color(c_white);
                draw_text(_hs_x + 18, _hs_ty, _hs_key + ": " + string(_hs_total));
                _hs_ty += 30;
                draw_set_color(make_color_rgb(180, 200, 235));
                draw_text(_hs_x + 18, _hs_ty, "Base: " + string(_hs_base));
                _hs_ty += 27;
                if (_hs_line2 != "") {
                    draw_set_color(make_color_rgb(150, 210, 160));
                    draw_text(_hs_x + 18, _hs_ty, _hs_line2);
                    _hs_ty += 27;
                }
                draw_set_color(make_color_rgb(140, 145, 165));
                draw_text(_hs_x + 18, _hs_ty, _hs_note);
                draw_set_font(ui_font(fnt_ui_small));
            }

            // ---- Offense ----
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_hc);
            draw_text(_pad, _content_y + 294, "-- Offense --------------");
            // Damage bonuses (left sub-column)
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(_dc);
            draw_text(_pad, _content_y + 333, "Phys abilities (STR):  +" + string(_derived.phys_dmg_bonus));
            draw_text(_pad, _content_y + 369, "Elem abilities (INT):  +" + string(_derived.elem_dmg_bonus));
            draw_text(_pad, _content_y + 405, "DoT / effects  (WIS):  +" + string(_derived.dot_dmg_bonus));
            draw_text(_pad, _content_y + 441, "All abilities  (CHA):  +" + string(_derived.cha_dmg_bonus));
            // Reach-gated weapon damage totals - flat dmg added to melee vs ranged abilities
            // only (SYSTEMS_WEAPON_ROLES.md §B). Read from apply_equipment_stats's per-reach
            // accumulators so this matches the cast resolver and the equip tab's per-weapon "+N dmg".
            var _wpn_bonus = apply_equipment_stats({});
            draw_text(_pad, _content_y + 477, "Melee Weapon dmg:   +" + string(_wpn_bonus.melee_dmg_bonus));
            draw_text(_pad, _content_y + 513, "Ranged Weapon dmg:  +" + string(_wpn_bonus.ranged_dmg_bonus));
            // Crit chances (right sub-column) - include the flat gear/Duelist bonus
            // AND the typed spell/phys crit gear (07-31): STR/DEX rows carry phys
            // crit, INT/WIS rows spell crit, mirroring combat_roll_crit exactly.
            // M 08-15 stats audit: plain PHYS/SPELL labels ("Arcane (INT)" read
            // as a mystery), and the SPELL rows now fold in the Surge rune
            // aspect so every socketed source shows in ONE total.
            var _crit_x  = _pad + 480;
            var _crit_ph = _wpn_bonus.crit_phys;
            var _crit_sp = _wpn_bonus.crit_spell + rune_aspect_value("spell_crit", undefined);
            draw_set_color(_dc);
            draw_text(_crit_x, _content_y + 333, "Phys Crit  (STR):  " + string(round(_derived.STR_crit_chance + _crit_flat + _crit_ph)) + "%");
            draw_text(_crit_x, _content_y + 369, "Phys Crit  (DEX):  " + string(round(_derived.DEX_crit_chance + _crit_flat + _crit_ph)) + "%");
            draw_text(_crit_x, _content_y + 405, "Spell Crit (INT):  " + string(round(_derived.INT_crit_chance + _crit_flat + _crit_sp)) + "%");
            draw_text(_crit_x, _content_y + 441, "Effect Crit (WIS): " + string(round(_derived.WIS_crit_chance + _crit_flat + _crit_sp)) + "%");
            draw_set_color(make_color_rgb(95, 105, 125));
            draw_text(_crit_x, _content_y + 480, "TOTALS: gear, runes, Surge aspects & Duelist");
            draw_text(_crit_x, _content_y + 507, "included - each ability adds its own base crit");

            // ---- Defense ----
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_hc);
            draw_text(_pad, _content_y + 576, "-- Defense --------------");
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(_dc);
            draw_text(_pad, _content_y + 615, "Dodge:           " + string(_derived.DODGE) + "%");
            draw_text(_pad, _content_y + 651, "Phys reduction:  " + string(_derived.phys_dmg_reduction) + "%");
            // M 08-15: Elemental Resist had NO row here despite gear/runes
            // granting it - Defense now states the total plainly.
            // 08-18 (M shot "text collision on status page"): the row sat on the crit
            // column at +615 - the same y as Accuracy. It lives in the Defense column
            // now, under Armor (+759 - the last free row above the Gold band).
            var _elr_tot = 1 + apply_equipment_stats({}).el_resist;
            draw_text(_pad, _content_y + 759, "Elem. Resist:    " + string(_elr_tot)
                + "  (" + string(round(100 * min(0.60, _elr_tot / (_elr_tot + 15)))) + "% off elemental hits)");
            draw_text(_pad, _content_y + 687, "Base HP:         " + string(_derived.HP) + "  (+" + string(apply_equipment_stats({}).bonus_max_hp) + " gear)");
            // #18: flat Armor from gear, with a hover explainer (it had no row here -
            // the stat existed only on tooltips and inside the combat math).
            // ARMOR REWORK (08-18): total pool (base 1 + gear + boons) -> % with
            // diminishing returns, armor/(armor+15) capped 60%. Row shows both.
            var _armor_val = 1 + apply_equipment_stats({}).armor + boon_flat_armor();
            var _armor_pct = round(100 * min(0.60, _armor_val / (_armor_val + 15)));
            var _arm_y   = _content_y + 723;
            var _arm_hov = (_smx >= _pad && _smx <= _pad + 400 && _smy >= _arm_y - 3 && _smy <= _arm_y + 30);
            draw_set_color(_arm_hov ? make_color_rgb(200, 215, 245) : _dc);
            draw_text(_pad, _arm_y, "Armor:           " + string(_armor_val) + "  (" + string(_armor_pct) + "% off physical hits)");
            draw_set_color(_dc);
            if (_arm_hov) {
                var _am_l1 = "Armor: " + string(_armor_val) + "  =  " + string(_armor_pct) + "% reduction";
                var _am_l2 = "Armor / (Armor + 15), capped at 60%:";
                var _am_l3 = "5 = 25%, 10 = 40%, 15 = 50%. Iron Skin";
                var _am_l4 = "cuts flat on top. A landed hit deals >= 1.";
                var _am_w  = max(string_width(_am_l2), string_width(_am_l3), string_width(_am_l4)) + 36;
                var _am_x  = _smx + 24, _am_y2 = _smy + 12;
                if (_am_x + _am_w > GUI_W) _am_x = GUI_W - _am_w - 6;
                draw_set_alpha(0.95);
                draw_set_color(make_color_rgb(18, 20, 32));
                draw_rectangle(_am_x, _am_y2, _am_x + _am_w, _am_y2 + 150, false);
                draw_set_alpha(1.0);
                draw_set_color(make_color_rgb(120, 140, 190));
                draw_rectangle(_am_x, _am_y2, _am_x + _am_w, _am_y2 + 150, true);
                draw_set_color(c_white);
                draw_text(_am_x + 18, _am_y2 + 12, _am_l1);
                draw_set_color(make_color_rgb(180, 200, 235));
                draw_text(_am_x + 18, _am_y2 + 48, _am_l2);
                draw_text(_am_x + 18, _am_y2 + 75, _am_l3);
                draw_text(_am_x + 18, _am_y2 + 102, _am_l4);
                draw_set_color(_dc);
            }
            // Accuracy - a flat bonus to each ability's to-hit, before the foe's dodge.
            draw_set_color(make_color_rgb(150, 180, 210));
            draw_text(_crit_x, _content_y + 615, "Accuracy:  +" + string(_derived.ACC_modifier) + "% to hit");
            ui_acc_cap_popup(_crit_x, _content_y + 615, 330);   // rune-cap hover (M 08-17)
            draw_set_color(make_color_rgb(95, 105, 125));
            draw_text(_crit_x, _content_y + 648, "Flat % added on top of each ability's");
            draw_text(_crit_x, _content_y + 675, "own hit chance (e.g. 85% + this, cap 99%).");
            draw_text(_crit_x, _content_y + 702, "Then the foe's Dodge rolls. Blind lowers it.");

            // ---- Fortune (07-31, M: gold/loot find were invisible) ----
            // Gold Find mirrors add_gold's gear + charisma sources; Scavenger /
            // Lucky Find / Beggar's Fortune multiply on top of the shown total.
            // Loot Find mirrors the drop-roll bonus points exactly.
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_hc);
            draw_text(_crit_x, _content_y + 741, "-- Fortune --------------");
            draw_set_font(ui_font(fnt_ui_small));
            var _ft_gf   = _wpn_bonus.gold_find + round(cha_gold_find() * 100);
            var _ft_loot = potion_loot_bonus_pts() + pet_active_boon_loot_pts()
                         + pet_active_lck_loot_pts() + pet_active_splash_loot_pts()
                         + pet_active_egg_bonus("loot");
            if (trait_active("Lucky Find")) _ft_loot += 5;
            draw_set_color(make_color_rgb(210, 190, 130));
            draw_text(_crit_x, _content_y + 780, "Gold Find:  +" + string(_ft_gf) + "%   (gear + charisma)");
            draw_set_color(make_color_rgb(160, 200, 170));
            draw_text(_crit_x, _content_y + 813, "Loot Find:  +" + string(round(_ft_loot)) + "%   (pets, potions, traits)");

            // ---- Footer ----
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(c_yellow);
            draw_text(_pad, _content_y + 810, "Gold: " + string(global.gold) + "g");
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(140, 160, 200));
            draw_text(_pad, _content_y + 846, "Run " + string(global.run_count + 1) + "  -  Floor " + string(global.current_floor));

            // =====================================================================
            // RIGHT COLUMN - portrait card + combat readiness + boons / effects
            // =====================================================================
            var _cardx1 = 1230, _cardx2 = 1800;
            var _cardy1 = _content_y, _cardy2 = _content_y + 360;

            // Card frame
            draw_set_color(make_color_rgb(18, 24, 38));
            draw_rectangle(_cardx1, _cardy1, _cardx2, _cardy2, false);
            draw_set_color(make_color_rgb(70, 90, 130));
            draw_rectangle(_cardx1, _cardy1, _cardx2, _cardy2, true);
            ui_draw_gothic_frame(_cardx1, _cardy1, _cardx2, _cardy2, 30);   // ornate portrait frame

            // South-facing player sprite (frame 0 = south in the 8-dir layout). Sprites
            // are top-left origin and vary in canvas size, so normalise to a target
            // height and centre inside the card.
            var _pspr = player_combat_sprite(_class_id);
            if (_pspr != -1 && sprite_exists(_pspr)) {
                var _psh = max(1, sprite_get_height(_pspr));
                var _psw = max(1, sprite_get_width(_pspr));
                var _pscale = 285 / _psh;
                var _pcx = (_cardx1 + _cardx2) / 2;
                var _pcy = (_cardy1 + _cardy2) / 2;
                draw_sprite_ext(_pspr, 0,
                    _pcx - (_psw * _pscale) / 2,
                    _pcy - (_psh * _pscale) / 2,
                    _pscale, _pscale, 0, c_white, 1.0);
            }

            // Skin / gender caption under the card
            var _skin_name = "Default look";
            if (variable_global_exists("player_skin") && global.player_skin != "default") {
                var _skd = vael_skin_get(global.player_skin);
                if (_skd != undefined) _skin_name = _skd.name;
            }
            var _gender_txt = (variable_global_exists("player_gender") && global.player_gender == "f") ? "Female" : "Male";
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(190, 200, 220));
            draw_text((_cardx1 + _cardx2) / 2, _cardy2 + 36, _skin_name + "  -  " + _gender_txt);
            draw_set_halign(fa_left);

            // ---- Combat readiness summary ----
            var _rx = _cardx1;
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_hc);
            draw_text(_rx, _content_y + 423, "-- Combat Readiness -----");
            // Primary crit school by class: Arcanist=Arcane, Bloodwarden=Power, Strider=Precision
            var _prim_lbls = ["Arcane (INT)", "Power (STR)", "Precision (DEX)"];
            var _prim_vals = [_derived.INT_crit_chance, _derived.STR_crit_chance, _derived.DEX_crit_chance];
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(c_white);
            draw_text(_rx, _content_y + 462, "HP:        " + string(_player.HP) + " / " + string(_player.max_HP));
            draw_text(_rx, _content_y + 495, "Dodge:     " + string(_derived.DODGE) + "%");
            draw_text(_rx, _content_y + 528, "Accuracy:  +" + string(_derived.ACC_modifier) + "% to hit");
            ui_acc_cap_popup(_rx, _content_y + 528, 300);   // rune-cap hover (M 08-17)
            draw_text(_rx, _content_y + 561, "Main crit: " + string(round(_prim_vals[_class_id] + _crit_flat)) + "%  (" + _prim_lbls[_class_id] + ")");

            // ---- Boons & Effects (right column, below readiness) ----
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_hc);
            draw_text(_rx, _content_y + 621, "-- Boons & Effects ------");
            draw_set_font(ui_font(fnt_ui_small));
            var _by    = _content_y + 660;
            var _any   = false;
            var _rows  = 0;
            var _rowmx = 6;   // vertical room cap for this column

            if (variable_global_exists("run_boons") && array_length(global.run_boons) > 0) {
                var _bn = array_length(global.run_boons);
                for (var _bi = 0; _bi < _bn && _rows < _rowmx; _bi++) {
                    var _bb = boon_get(global.run_boons[_bi]);
                    if (_bb == undefined) continue;
                    draw_set_color(make_color_rgb(190, 180, 140));
                    draw_text(_rx, _by, "+ " + _bb.name);
                    draw_set_color(make_color_rgb(120, 130, 110));
                    draw_text(_rx + 24, _by + 24, ui_truncate(_bb.desc, 540));
                    _by += 57; _rows++; _any = true;
                }
            }
            // Borrowed Memory (expression #6): the run-scoped other-class ability.
            if (variable_global_exists("run_borrowed_ability") && global.run_borrowed_ability != ""
                && _rows < _rowmx) {
                draw_set_color(make_color_rgb(170, 150, 230));
                draw_text(_rx, _by, "+ Borrowed Memory: " + global.run_borrowed_ability);
                draw_set_color(make_color_rgb(125, 115, 160));
                draw_text(_rx + 24, _by + 24, ui_truncate("A " + global.run_borrowed_class + " ability, yours until this run ends.", 540));
                _by += 57; _rows++; _any = true;
            }
            // Active run curses (devil's bargain) - red, with penalty text. The
            // REWARD side rides the name line (M 08-13: the stats tab showed the
            // curse but not its +1 loot-rarity-tier pay - the bargain looked
            // like a pure penalty here).
            if (variable_global_exists("run_curses") && array_length(global.run_curses) > 0) {
                var _cn = array_length(global.run_curses);
                for (var _ci = 0; _ci < _cn && _rows < _rowmx; _ci++) {
                    var _cc = curse_get(global.run_curses[_ci]);
                    if (_cc == undefined) continue;
                    var _cc_pay = "";
                    if (variable_struct_exists(_cc, "loot") && _cc.loot > 0) _cc_pay += "+" + string(_cc.loot) + " loot tier";
                    if (variable_struct_exists(_cc, "gold") && _cc.gold > 0) _cc_pay += (_cc_pay == "" ? "" : ", ") + "+" + string(round(_cc.gold * 100)) + "% gold";
                    if (variable_struct_exists(_cc, "dust") && _cc.dust > 0) _cc_pay += (_cc_pay == "" ? "" : ", ") + "+" + string(round(_cc.dust * 100)) + "% dust";
                    draw_set_color(make_color_rgb(210, 120, 120));
                    draw_text(_rx, _by, "! " + _cc.name);
                    if (_cc_pay != "") {
                        draw_set_color(make_color_rgb(190, 170, 120));
                        draw_text(_rx + 24 + string_width("! " + _cc.name), _by, "  pays " + _cc_pay);
                    }
                    draw_set_color(make_color_rgb(150, 110, 110));
                    draw_text(_rx + 24, _by + 24, ui_truncate(_cc.desc, 540));
                    _by += 57; _rows++; _any = true;
                }
            }
            // Active combat statuses on the player (only present during combat).
            if (variable_struct_exists(_player, "status_effects")) {
                var _se_list = _player.status_effects;
                for (var _ssi = 0; _ssi < array_length(_se_list) && _rows < _rowmx; _ssi++) {
                    var _ss   = _se_list[_ssi];
                    var _sst  = status_icon_style(_ss);
                    var _sdur = variable_struct_exists(_ss, "duration") ? _ss.duration : 0;
                    draw_set_color(_sst.color);
                    draw_text(_rx, _by, "- " + _ss.name + " (" + _sst.label + ")");
                    draw_set_color(make_color_rgb(150, 140, 120));
                    draw_text(_rx + 24, _by + 24, ui_truncate(status_effect_plain_text(_ss) + "  -  " + string(_sdur) + " turn" + (_sdur == 1 ? "" : "s") + " left", 540));
                    _by += 57; _rows++; _any = true;
                }
            }
            if (!_any) {
                draw_set_color(_dc);
                draw_text(_rx, _by, "None active.");
                draw_set_color(make_color_rgb(95, 105, 125));
                draw_text(_rx + 24, _by + 24, "Boons last all run; statuses a few turns.");
            }

            // ---- Bonds (thin affinity): compact one-line summary of relationships
            // past Stranger. The hub NPC panel is the primary surface; this is the
            // at-a-glance overview (replaced by the Phase-4 Journal). Left column,
            // below the Run/Floor footer.
            var _rel_str = "";
            var _rel_ids = affinity_npc_ids();
            for (var _ri = 0; _ri < array_length(_rel_ids); _ri++) {
                if (affinity_tier(_rel_ids[_ri]) >= 1) {
                    var _rn = _rel_ids[_ri];
                    _rn = string_upper(string_char_at(_rn, 1)) + string_delete(_rn, 1, 1);
                    if (_rel_str != "") _rel_str += "     ";
                    _rel_str += _rn + " - " + affinity_tier_name(_rel_ids[_ri]);
                }
            }
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_hc);
            draw_text(_pad, _content_y + 873, "-- Bonds ----------------");
            draw_set_font(ui_font(fnt_ui_small));
            if (_rel_str == "") {
                draw_set_color(make_color_rgb(120, 130, 150));
                draw_text(_pad, _content_y + 906, "No bonds yet - use the camp's services to grow them.");
            } else {
                draw_set_color(make_color_rgb(200, 185, 130));
                draw_text(_pad, _content_y + 906, ui_truncate(_rel_str, 1120));
            }

            // Guided tour overlay (07-31) - drawn last so it dims + annotates the
            // finished page. Step state lives on gc (stats_tour_step).
            ui_draw_stats_tour();
        } else {
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(120, 130, 150));
            draw_text(_pad, _content_y + 30, "No active character - start a run to view stats.");
        }
    }

    // ---- EQUIPMENT TAB ----
    // Restructured: a large detail/preview panel on the LEFT (big icon + full stats
    // of the selected slot) and a single-column slot LIST on the right. Ring 2 sits
    // directly under Ring 1 (equip_display_order). Both panels wear a gothic frame.
    if (menu_tab == 1) {
        var _slot_names = ["Melee Weapon", "Offhand", "Helm", "Chest", "Gloves", "Boots", "Amulet", "Ring 1", "Ranged Weapon", "Ring 2"];
        var _sel_pos    = clamp(_gc.equip_slot_selected, 0, EQUIP_SLOT_COUNT - 1);
        var _sel_inv    = equip_display_to_inv(_sel_pos);

        // Equip confirmation toast: moved to the END of this function (topmost,
        // boxed via ui_draw_toast) - it used to draw bare here at y110, under the
        // frame art and through the tab row (08-04 screenshot).

        // Stash / Pack counts in top-right
        var _stash_count = variable_global_exists("equipment_stash") ? array_length(global.equipment_stash) : 0;
        var _pack_count  = variable_global_exists("carried_items")   ? array_length(global.carried_items)   : 0;
        var _equip_in_hub = (room == rm_hub || room == rm_character_select);
        draw_set_halign(fa_right);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(120, 130, 150));
        if (_equip_in_hub) {
            draw_text(1850, 120, "Stash: " + string(_stash_count) + "   Pack: " + string(_pack_count));
        } else {
            draw_text(1850, 120, "Pack: " + string(_pack_count) + "  (stash left in town)");
        }
        draw_set_halign(fa_left);

        var _offhand_locked = hard_two_handed_equipped();   // greatsword/longbow lock the offhand; a caster staff does not (Task 10)

        // The right-hand FOUND-ITEMS column (pack) + which column is focused. When the
        // found column is focused, the left detail panel previews the highlighted found
        // item instead of the selected slot. (Sort: 0 rarity, 1 type.)
        var _ff       = (variable_instance_exists(_gc, "equip_found_focus") && _gc.equip_found_focus);
        var _fsort    = variable_instance_exists(_gc, "equip_found_sort") ? _gc.equip_found_sort : 0;
        var _found    = equip_found_list(_fsort);
        var _found_n  = array_length(_found);
        var _fcur     = clamp(variable_instance_exists(_gc, "equip_found_cursor") ? _gc.equip_found_cursor : 0, 0, max(0, _found_n - 1));
        // (Focus may be on the found column even when empty - the column still shows its
        //  "No items in your pack" state so A/D always reads as a working lateral swap.)

        // ===== LEFT DETAIL / PREVIEW PANEL (slightly narrowed to make room) =====
        var _dp_x1 = 60, _dp_y1 = 150, _dp_x2 = 560, _dp_y2 = 1012;
        draw_set_color(make_color_rgb(14, 17, 28));
        draw_rectangle(_dp_x1, _dp_y1, _dp_x2, _dp_y2, false);
        ui_draw_gothic_frame(_dp_x1, _dp_y1, _dp_x2, _dp_y2, 26);

        var _dp_cx   = (_dp_x1 + _dp_x2) / 2;
        // Detail item follows focus: a highlighted found item, else the selected slot.
        var _sel_item;
        var _detail_title;
        if (_ff && _found_n > 0) {
            _sel_item     = _found[_fcur].item;
            _detail_title = item_slot_label(_sel_item.slot) + "  (Pack)";
        } else {
            _sel_item     = (variable_global_exists("inventory") && array_length(global.inventory) > _sel_inv)
                            ? global.inventory[_sel_inv] : undefined;
            _detail_title = _slot_names[_sel_inv];
        }

        // Slot / item title
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(200, 180, 120));
        draw_text(_dp_cx, _dp_y1 + 26, _detail_title);

        // Large icon (or empty plate)
        var _big = 300;
        var _big_x = _dp_cx - _big / 2;
        var _big_y = _dp_y1 + 80;
        if (_sel_item != undefined) {
            ui_draw_item_icon(_big_x, _big_y, _big, _sel_item);
        } else {
            draw_set_color(make_color_rgb(12, 14, 22));
            draw_rectangle(_big_x, _big_y, _big_x + _big, _big_y + _big, false);
            draw_set_color(make_color_rgb(50, 56, 74));
            draw_rectangle(_big_x, _big_y, _big_x + _big, _big_y + _big, true);
            draw_set_valign(fa_middle);
            draw_set_color(make_color_rgb(70, 76, 96));
            draw_text(_dp_cx, _big_y + _big / 2, (_sel_inv == 1 && _offhand_locked) ? "LOCKED" : "EMPTY");
            draw_set_valign(fa_top);
        }

        // Detail text under the icon
        var _info_y = _big_y + _big + 28;
        var _info_w = (_dp_x2 - _dp_x1) - 56;
        if (_sel_item != undefined) {
            var _rcol = item_rarity_color(_sel_item.rarity);
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_rcol);
            draw_text_ext(_dp_cx, _info_y, _sel_item.name, 34, _info_w);
            _info_y += max(40, string_height_ext(_sel_item.name, 34, _info_w)) + 8;

            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(170, 150, 90));
            draw_text(_dp_cx, _info_y, item_rarity_name(_sel_item.rarity) + "  |  " + item_slot_label(_sel_item.slot));
            _info_y += 40;

            // Full stat string (wrapped, left-aligned within the panel)
            draw_set_halign(fa_left);
            draw_set_color(c_white);
            var _stat_str = ui_item_stat_str(_sel_item);
            draw_text_ext(_dp_x1 + 28, _info_y, _stat_str, 32, _info_w);
            _info_y += string_height_ext(_stat_str, 32, _info_w) + 10;

            // Requirement (green if met, red if not)
            var _req = item_stat_requirement(_sel_item);
            if (_req.value > 0 && _req.stat != "") {
                draw_set_color((player_base_stat(_req.stat) >= _req.value)
                    ? make_color_rgb(110, 190, 110) : make_color_rgb(230, 90, 90));
                draw_text(_dp_x1 + 28, _info_y, "Requires " + _req.stat + " " + string(_req.value));
                _info_y += 34;
            }
            var _dp_plr = item_perm_level_req(_sel_item);
            if (_dp_plr > 0) {
                draw_set_color((player_permanent_level() >= _dp_plr)
                    ? make_color_rgb(110, 190, 110) : make_color_rgb(230, 90, 90));
                draw_text(_dp_x1 + 28, _info_y, "Requires Permanent Level " + string(_dp_plr));
                _info_y += 34;
            }

            // Unique effect or flavor
            if (variable_struct_exists(_sel_item, "unique_desc") && _sel_item.unique_desc != "") {
                draw_set_color(make_color_rgb(255, 200, 50));
                draw_text_ext(_dp_x1 + 28, _info_y + 4, _sel_item.unique_desc, 30, _info_w);
                _info_y += string_height_ext(_sel_item.unique_desc, 30, _info_w) + 14;
            } else if (variable_struct_exists(_sel_item, "effect_desc") && _sel_item.effect_desc != "") {
                draw_set_color(make_color_rgb(140, 150, 175));
                draw_text_ext(_dp_x1 + 28, _info_y + 4, _sel_item.effect_desc, 30, _info_w);
                _info_y += string_height_ext(_sel_item.effect_desc, 30, _info_w) + 14;
            }

            // ===== SOCKETS (bottom-left) - the selected item's rune sockets, filled or
            // empty, drawn below the stats/flavor where there's plenty of room. =====
            item_ensure_sockets(_sel_item);
            var _scount = _sel_item.socket_count;
            if (_scount > 0) {
                var __rn_have = array_length(_sel_item.runes);
                // Divider
                _info_y += 6;
                draw_set_color(make_color_rgb(60, 66, 86));
                draw_line(_dp_x1 + 28, _info_y, _dp_x2 - 28, _info_y);
                _info_y += 16;
                // Header
                draw_set_halign(fa_left);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(180, 190, 215));
                draw_text(_dp_x1 + 28, _info_y, "Sockets (" + string(__rn_have) + "/" + string(_scount) + ")");
                _info_y += 38;
                // One row per socket: gem icon (or colored gem fallback) + rune effect,
                // or an empty-slot placeholder.
                var _sock_box = 40;
                var _sock_bx  = _dp_x1 + 34;
                for (var _si = 0; _si < _scount; _si++) {
                    var _sy = _info_y + _si * 54;
                    draw_set_color(make_color_rgb(28, 32, 46));
                    draw_rectangle(_sock_bx, _sy, _sock_bx + _sock_box, _sy + _sock_box, false);
                    draw_set_color(make_color_rgb(72, 80, 104));
                    draw_rectangle(_sock_bx, _sy, _sock_bx + _sock_box, _sy + _sock_box, true);
                    draw_set_valign(fa_middle);
                    if (_si < __rn_have) {
                        var _rn  = _sel_item.runes[_si];
                        var _ric = rune_icon_sprite(_rn.id);
                        if (_ric != -1) {
                            draw_sprite_stretched(_ric, 0, _sock_bx + 2, _sy + 2, _sock_box - 4, _sock_box - 4);
                        } else {
                            draw_set_color(rune_glyph_color(_rn.id));
                            draw_rectangle(_sock_bx + 9, _sy + 9, _sock_bx + _sock_box - 9, _sy + _sock_box - 9, false);
                        }
                        draw_set_color(make_color_rgb(150, 200, 255));
                        draw_text_ext(_sock_bx + _sock_box + 16, _sy + _sock_box / 2, rune_describe(_rn), 26, _info_w - _sock_box - 24);
                    } else {
                        draw_set_color(make_color_rgb(95, 102, 124));
                        draw_text(_sock_bx + _sock_box + 16, _sy + _sock_box / 2, "- Empty socket -");
                    }
                    draw_set_valign(fa_top);
                }
            }
        }
        draw_set_halign(fa_left);
        draw_set_valign(fa_top);

        // ===== MIDDLE SLOT LIST (the 10 equipped slots - the main show) =====
        // Narrowed on the right to make room for the found-items column. The per-row
        // rarity tag was dropped so item names get the full row width (rarity still reads
        // from the name colour). The gold "active" accent only shows when this column has
        // focus; when the found column is focused the selected slot dims to a blue accent.
        var _ls_x1 = 580, _ls_x2 = 1340, _ls_y1 = 150, _ls_y2 = 1012;
        draw_set_color(make_color_rgb(14, 17, 28));
        draw_rectangle(_ls_x1, _ls_y1, _ls_x2, _ls_y2, false);
        ui_draw_gothic_frame(_ls_x1, _ls_y1, _ls_x2, _ls_y2, 26);

        var _row_pad = 16;
        var _row_h   = (( _ls_y2 - _ls_y1) - _row_pad * 2) / EQUIP_SLOT_COUNT;
        for (var _r = 0; _r < EQUIP_SLOT_COUNT; _r++) {
            var _inv = equip_display_to_inv(_r);
            var _ry  = _ls_y1 + _row_pad + _r * _row_h;
            var _is_sel = (_r == _sel_pos);
            var _slot_locked = (_inv == 1 && _offhand_locked);
            if (_slot_locked) draw_set_alpha(0.4);

            // Row background + selection accent (gold when this column is focused, else blue)
            draw_set_color(_is_sel ? make_color_rgb(34, 54, 86) : make_color_rgb(18, 22, 36));
            draw_rectangle(_ls_x1 + 18, _ry + 3, _ls_x2 - 18, _ry + _row_h - 5, false);
            if (_is_sel) {
                draw_set_color(_ff ? make_color_rgb(70, 90, 130) : make_color_rgb(150, 120, 60));
                draw_rectangle(_ls_x1 + 18, _ry + 3, _ls_x2 - 18, _ry + _row_h - 5, true);
                draw_set_color(_ff ? make_color_rgb(90, 120, 170) : make_color_rgb(210, 175, 90));
                draw_rectangle(_ls_x1 + 18, _ry + 3, _ls_x1 + 24, _ry + _row_h - 5, false);
            }

            var _equipped = (variable_global_exists("inventory") && array_length(global.inventory) > _inv)
                            ? global.inventory[_inv] : undefined;

            // Icon badge
            var _icon_sz = _row_h - 22;
            if (_equipped != undefined) ui_draw_item_icon(_ls_x1 + 34, _ry + 8, _icon_sz, _equipped);

            // Slot name (small, top) + item name (rarity colour, below, full width)
            var _tx = _ls_x1 + 34 + _icon_sz + 22;
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(_is_sel ? make_color_rgb(210, 190, 130) : make_color_rgb(120, 130, 150));
            draw_text(_tx, _ry + 10, _slot_names[_inv]);

            draw_set_font(ui_font(fnt_ui));
            if (_equipped != undefined) {
                draw_set_color(item_rarity_color(_equipped.rarity));
                draw_text(_tx, _ry + 38, ui_truncate(_equipped.name, _ls_x2 - _tx - 30));
            } else {
                draw_set_color(_slot_locked ? make_color_rgb(150, 130, 90) : make_color_rgb(70, 76, 96));
                draw_text(_tx, _ry + 38, _slot_locked ? "Two-handed: offhand locked" : "- Empty -");
            }

            if (_slot_locked) draw_set_alpha(1.0);
        }

        // ===== RIGHT FOUND-ITEMS COLUMN (pack) =====
        // A browsable list of everything in your pack so you can inspect/equip without
        // cycling each slot. A/D (or left/right) or a click moves focus here; a corner
        // button toggles the rarity/type sort. Enter equips the highlighted item.
        var _fc_x1 = 1360, _fc_x2 = 1862, _fc_y1 = 150, _fc_y2 = 1012;
        draw_set_color(make_color_rgb(14, 17, 28));
        draw_rectangle(_fc_x1, _fc_y1, _fc_x2, _fc_y2, false);
        ui_draw_gothic_frame(_fc_x1, _fc_y1, _fc_x2, _fc_y2, 26);

        draw_set_halign(fa_left);
        draw_set_valign(fa_top);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(_ff ? make_color_rgb(210, 190, 130) : make_color_rgb(120, 130, 150));
        var _fc_hub = (room == rm_hub || room == rm_character_select);
        draw_text(_fc_x1 + 24, _fc_y1 + 18, (_fc_hub ? "Pack & Stash (" : "Found - Pack (") + string(_found_n) + ")");

        // Sort toggle button (top-right corner of the column)
        var _sb_x2 = _fc_x2 - 20, _sb_x1 = _fc_x2 - 210, _sb_y1 = _fc_y1 + 12, _sb_y2 = _fc_y1 + 52;
        draw_set_color(make_color_rgb(30, 40, 60));
        draw_rectangle(_sb_x1, _sb_y1, _sb_x2, _sb_y2, false);
        draw_set_color(make_color_rgb(90, 110, 150));
        draw_rectangle(_sb_x1, _sb_y1, _sb_x2, _sb_y2, true);
        draw_set_halign(fa_center);
        draw_set_valign(fa_middle);
        draw_set_color(make_color_rgb(185, 200, 225));
        draw_text((_sb_x1 + _sb_x2) / 2, (_sb_y1 + _sb_y2) / 2, (_fsort == 1) ? "Sort: Type" : "Sort: Rarity");
        draw_set_valign(fa_top);
        draw_set_halign(fa_left);

        // Creatures/eggs found THIS RUN go straight to Bairc - and carried
        // Banshee bottles ride along too (M 08-15: both belong in this strip).
        // Display-only, pinned to the column bottom; SCROLLABLE past 3 rows
        // (wheel over the strip, or the tap arrows) with a visible bar -
        // the old "+N more" dead line was a defect by the standing rule.
        var _rfp    = variable_global_exists("run_found_pets") ? global.run_found_pets : [];
        var _fs_rows = [];
        for (var _rfi = 0; _rfi < array_length(_rfp); _rfi++) {
            array_push(_fs_rows, { kind: "egg", label: _rfp[_rfi], right: "-> Bairc" });
        }
        var _fs_bb = variable_global_exists("banshee_carried") ? global.banshee_carried : 0;
        if (_fs_bb > 0) {
            array_push(_fs_rows, { kind: "bottle",
                label: "Banshee in a Bottle" + ((_fs_bb > 1) ? ("  x" + string(_fs_bb)) : ""),
                right: "-> Maren" });
        }
        var _rfp_n    = array_length(_fs_rows);
        var _rfp_show = min(_rfp_n, 3);
        var _rfp_h    = (_rfp_n > 0) ? (52 + _rfp_show * 30) : 0;
        var _fc_ly2   = _fc_y2 - _rfp_h;   // pack list's usable bottom

        if (_found_n == 0) {
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(70, 76, 96));
            draw_text(_fc_x1 + 24, _fc_y1 + 100, "No items in your pack.");
        } else {
            var _fl_y0    = _fc_y1 + 86;
            var _fl_rowh  = 60;
            var _fl_vis   = max(1, floor((_fc_ly2 - _fl_y0 - 14) / _fl_rowh));
            var _fl_first = ui_list_window("equip_found", _fcur, _found_n, _fl_vis);
            var _fl_last  = min(_found_n, _fl_first + _fl_vis);
            for (var _fi = _fl_first; _fi < _fl_last; _fi++) {
                var _fit  = _found[_fi].item;
                var _fry  = _fl_y0 + (_fi - _fl_first) * _fl_rowh;
                var _fsel = (_ff && _fi == _fcur);
                draw_set_color(_fsel ? make_color_rgb(34, 54, 86) : make_color_rgb(18, 22, 36));
                draw_rectangle(_fc_x1 + 16, _fry + 2, _fc_x2 - 16, _fry + _fl_rowh - 4, false);
                if (_fsel) {
                    draw_set_color(make_color_rgb(150, 120, 60));
                    draw_rectangle(_fc_x1 + 16, _fry + 2, _fc_x2 - 16, _fry + _fl_rowh - 4, true);
                    draw_set_color(make_color_rgb(210, 175, 90));
                    draw_rectangle(_fc_x1 + 16, _fry + 2, _fc_x1 + 22, _fry + _fl_rowh - 4, false);
                }
                var _fisz = _fl_rowh - 16;
                ui_draw_item_icon(_fc_x1 + 30, _fry + 6, _fisz, _fit);
                var _ftx = _fc_x1 + 30 + _fisz + 16;
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(120, 130, 150));
                draw_text(_ftx, _fry + 6, item_slot_label(_fit.slot));
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(item_rarity_color(_fit.rarity));
                draw_text(_ftx, _fry + 28, ui_truncate(_fit.name, _fc_x2 - _ftx - 24));
            }
            // Position readout (replaces "N more above/below") - selected row's place
            // in the full pack list, e.g. "7 / 23". Only when the list overflows.
            if (_found_n > _fl_vis) {
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(150, 160, 140));
                draw_set_halign(fa_center);
                draw_text((_fc_x1 + _fc_x2) / 2, _fc_ly2 - 28, string(_fcur + 1) + " / " + string(_found_n));
                draw_set_halign(fa_left);
            }
        }

        // Found eggs/creatures + carried bottles as ITEM-STYLE rows (display-only:
        // creatures live at Bairc's, bottles at Maren's). Mysterious eggs show a
        // code-drawn egg icon, bottles a code-drawn corked bottle. Overflow
        // scrolls (wheel / tap arrows) with a visible bar - never a dead "+N".
        if (_rfp_n > 0) {
            if (!variable_instance_exists(_gc, "equip_found_strip_scroll")) _gc.equip_found_strip_scroll = 0;
            var _fs_over = (_rfp_n > _rfp_show);
            _gc.equip_found_strip_scroll = clamp(_gc.equip_found_strip_scroll, 0, max(0, _rfp_n - _rfp_show));
            // Wheel over the strip band scrolls it (bairc-roster idiom: read in Draw).
            var _fs_mx = device_mouse_x_to_gui(0), _fs_my = device_mouse_y_to_gui(0);
            if (_fs_over && _fs_mx >= _fc_x1 && _fs_mx <= _fc_x2 && _fs_my >= _fc_ly2 && _fs_my <= _fc_y2) {
                if (mouse_wheel_down()) _gc.equip_found_strip_scroll = min(_rfp_n - _rfp_show, _gc.equip_found_strip_scroll + 1);
                if (mouse_wheel_up())   _gc.equip_found_strip_scroll = max(0, _gc.equip_found_strip_scroll - 1);
            }
            var _fs_scr = _gc.equip_found_strip_scroll;
            draw_set_color(make_color_rgb(60, 66, 90));
            draw_line(_fc_x1 + 16, _fc_ly2, _fc_x2 - 16, _fc_ly2);
            // Right inset for scrollbar + arrows when overflowing.
            var _fs_rx = _fs_over ? (_fc_x2 - 58) : (_fc_x2 - 24);
            for (var _ri = _fs_scr; _ri < min(_rfp_n, _fs_scr + _rfp_show); _ri++) {
                var _fsr = _fs_rows[_ri];
                var _egy = _fc_ly2 + 8 + (_ri - _fs_scr) * 30;
                // Row plate (matches the pack item rows above, slimmer)
                draw_set_color(make_color_rgb(20, 18, 32));
                draw_rectangle(_fc_x1 + 16, _egy, _fc_x2 - 16, _egy + 27, false);
                var _egcx = _fc_x1 + 32, _egcy = _egy + 14;
                if (_fsr.kind == "bottle") {
                    // Code-drawn corked bottle, faintly wailing (spectral violet).
                    draw_set_color(make_color_rgb(120, 90, 170));
                    draw_roundrect_ext(_egcx - 6, _egcy - 4, _egcx + 6, _egcy + 10, 4, 4, false);
                    draw_set_color(make_color_rgb(170, 140, 220));
                    draw_rectangle(_egcx - 2, _egcy - 9, _egcx + 2, _egcy - 4, false);
                    draw_set_color(make_color_rgb(150, 120, 90));
                    draw_rectangle(_egcx - 3, _egcy - 12, _egcx + 3, _egcy - 9, false);
                    draw_set_alpha(0.5 + 0.3 * sin(current_time / 300));
                    draw_set_color(make_color_rgb(210, 190, 250));
                    draw_circle(_egcx, _egcy + 2, 2, false);
                    draw_set_alpha(1.0);
                } else {
                    // Code-drawn egg icon: shell + highlight (no sprite dependency)
                    draw_set_color(make_color_rgb(214, 202, 176));
                    draw_ellipse(_egcx - 7, _egcy - 10, _egcx + 7, _egcy + 10, false);
                    draw_set_color(make_color_rgb(245, 240, 226));
                    draw_ellipse(_egcx - 3, _egcy - 7, _egcx + 1, _egcy - 2, false);
                    draw_set_color(make_color_rgb(90, 80, 66));
                    draw_ellipse(_egcx - 7, _egcy - 10, _egcx + 7, _egcy + 10, true);
                }
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color((_fsr.kind == "bottle") ? make_color_rgb(190, 165, 240) : make_color_rgb(200, 175, 245));
                draw_text(_fc_x1 + 52, _egy + 3, ui_truncate(_fsr.label, _fs_rx - (_fc_x1 + 52) - 90));
                draw_set_halign(fa_right);
                draw_set_color(make_color_rgb(120, 124, 138));
                draw_text(_fs_rx, _egy + 3, _fsr.right);
                draw_set_halign(fa_left);
            }
            if (_fs_over) {
                // Visible scrollbar (standing rule) + tappable arrows (touch path).
                ui_pb_scrollbar(_fc_x2 - 30, _fc_ly2 + 8, _fc_ly2 + 8 + _rfp_show * 30, _rfp_n, _rfp_show, _fs_scr);
                var _fs_ax = _fc_x2 - 52;
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(28, 34, 52));
                draw_rectangle(_fs_ax, _fc_ly2 + 6,  _fs_ax + 18, _fc_ly2 + 24, false);
                draw_rectangle(_fs_ax, _fc_y2 - 26,  _fs_ax + 18, _fc_y2 - 8,  false);
                draw_set_color(make_color_rgb(150, 160, 190));
                draw_set_halign(fa_center);
                draw_text(_fs_ax + 9, _fc_ly2 + 4,  "^");
                draw_text(_fs_ax + 9, _fc_y2 - 28,  "v");
                draw_set_halign(fa_left);
                if (touch_tapped(_fs_ax - 6, _fc_ly2, _fs_ax + 24, _fc_ly2 + 30))
                    _gc.equip_found_strip_scroll = max(0, _fs_scr - 1);
                if (touch_tapped(_fs_ax - 6, _fc_y2 - 32, _fs_ax + 24, _fc_y2 - 2))
                    _gc.equip_found_strip_scroll = min(_rfp_n - _rfp_show, _fs_scr + 1);
            }
        }

        // Bottom instruction line
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(80, 90, 110));
        if (_gc.equip_picker_open) {
            ui_draw_key_legend(960, 1035, "W/S: Navigate   Enter: Equip   Esc: Cancel");
        } else if (_ff) {
            ui_draw_key_legend(960, 1035, "W/S: Browse Pack   Enter: Equip   A/<-: Back to Slots   (click 'Sort' to reorder)");
        } else {
            ui_draw_key_legend(960, 1035, "W/S: Slots   Enter: Equip   U: Unequip   D/->: Browse Found Items");
            // Touch UNEQUIP (07-24 audit): tap-users could swap gear via the picker
            // but never EMPTY a slot - U had no touch path. Button only while the
            // selected slot actually holds an item (the U handler's own condition),
            // in the footer space the hidden key legend leaves free.
            if (input_device() == 2 && global.inventory[_sel_inv] != undefined) {
                draw_set_color(make_color_rgb(38, 24, 20));
                draw_rectangle(810, 972, 1110, 1032, false);
                draw_set_color(make_color_rgb(200, 120, 90));
                draw_rectangle(810, 972, 1110, 1032, true);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_valign(fa_middle);
                draw_set_color(make_color_rgb(235, 190, 165));
                draw_text(960, 1003, "UNEQUIP SLOT");
                draw_set_valign(fa_top);
                draw_set_font(ui_font(fnt_ui));
                if (touch_tapped(810, 972, 1110, 1032)) touch_press(ord("U"));
            }
        }
        draw_set_halign(fa_left);

        // --- EQUIP PICKER OVERLAY ---
        if (_gc.equip_picker_open) {
            // Filter by the item-TYPE the position accepts (Ring 2 -> "ring"), so rings
            // list for either ring position. Must match the Step picker.
            var _slot_name = equip_position_item_slot(_sel_inv);

            // Build filtered list (same order as Step)
            var _picker_items = [];
            var _picker_src   = [];
            // Stash is hub-only: during a run only the pack is shown (must match Step).
            var _picker_in_hub = (room == rm_hub || room == rm_character_select);
            if (_picker_in_hub && variable_global_exists("equipment_stash")) {
                for (var _pi = 0; _pi < array_length(global.equipment_stash); _pi++) {
                    if (global.equipment_stash[_pi].slot == _slot_name) {
                        array_push(_picker_items, global.equipment_stash[_pi]);
                        array_push(_picker_src, 0);   // 0 = stash
                    }
                }
            }
            if (variable_global_exists("carried_items")) {
                for (var _pi = 0; _pi < array_length(global.carried_items); _pi++) {
                    if (global.carried_items[_pi].slot == _slot_name) {
                        array_push(_picker_items, global.carried_items[_pi]);
                        array_push(_picker_src, 1);   // 1 = pack
                    }
                }
            }

            // Picker panel
            var _px      = 360;
            var _py      = 180;
            var _pw      = 1200;
            var _pk_row_h   = 108;
            var _visible = array_length(_picker_items);

            // The currently-equipped item lives in global.inventory (not in the
            // pack/stash lists above), so it never appears as a selectable row. Show
            // it as a dimmed, non-selectable "[Equipped]" row at the TOP so the player
            // always sees the FULL count for the slot - otherwise a duplicate of the
            // worn item (same name/stats sitting in the pack) reads as if the item
            // vanished. Selectable rows shift down one row while this is present.
            var _equipped_here = (variable_global_exists("inventory")
                && array_length(global.inventory) > _sel_inv)
                ? global.inventory[_sel_inv] : undefined;
            var _eq_extra = (_equipped_here != undefined) ? 1 : 0;
            var _list_y0  = _py + 48 + _eq_extra * _pk_row_h;   // top of the selectable list
            // Windowed list: the panel used to grow with the item count, so rows past
            // the screen bottom were selectable but invisible. Cap the panel to what
            // fits and slide an EDGE-TRIGGERED window (persistent equip_picker_scroll,
            // advanced in the Step handler; the mouse hit-test mirrors this).
            var _max_rows  = max(1, (1020 - _list_y0) div _pk_row_h);
            var _win_rows  = min(_visible, _max_rows);
            var _pk_scroll = clamp(_gc.equip_picker_scroll, 0, max(0, _visible - _max_rows));
            var _ph        = max(150, (_win_rows + _eq_extra) * _pk_row_h + 48);

            draw_set_alpha(0.97);
            draw_set_color(make_color_rgb(12, 15, 28));
            draw_rectangle(_px, _py, _px + _pw, _py + _ph, false);
            draw_set_alpha(1.0);
            draw_set_color(make_color_rgb(80, 140, 220));
            draw_rectangle(_px, _py, _px + _pw, _py + _ph, true);

            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(c_white);
            draw_text(_px + _pw / 2, _py + 12, "Choose " + _slot_names[_sel_inv]);
            draw_set_halign(fa_left);

            // Class restriction warning / equip_msg - measured BACKDROP BOX
            // (M 07-28 screenshot: bare text over the slot list behind was
            // illegible - "requires Level 4" tangled into the Helm row).
            if (variable_instance_exists(_gc, "equip_msg") && _gc.equip_msg != "") {
                draw_set_font(ui_font(fnt_ui_small));
                var _emw  = string_width(_gc.equip_msg);
                var _emh  = string_height(_gc.equip_msg);
                var _emx1 = _px + _pw / 2 - _emw / 2 - 24;
                var _emx2 = _px + _pw / 2 + _emw / 2 + 24;
                var _emy1 = _py + _ph + 3;
                var _emy2 = _emy1 + _emh + 18;
                draw_set_alpha(0.96);
                draw_set_color(make_color_rgb(30, 14, 8));
                draw_rectangle(_emx1, _emy1, _emx2, _emy2, false);
                draw_set_alpha(1.0);
                draw_set_color(make_color_rgb(255, 120, 60));
                draw_rectangle(_emx1, _emy1, _emx2, _emy2, true);
                draw_set_halign(fa_center);
                draw_text(_px + _pw / 2, _emy1 + 9, _gc.equip_msg);
                draw_set_halign(fa_left);
            }

            // Dimmed, non-selectable row for the item currently worn in this slot.
            if (_eq_extra) {
                var _eq_ry = _py + 48;
                draw_set_alpha(0.4);
                draw_set_color(make_color_rgb(16, 20, 34));
                draw_rectangle(_px + 6, _eq_ry, _px + _pw - 6, _eq_ry + _pk_row_h - 6, false);
                draw_set_alpha(1.0);
                var _eqcol = item_rarity_color(_equipped_here.rarity);
                ui_draw_item_icon(_px + 21, _eq_ry + 9, 48, _equipped_here);
                var _eqx = _px + 78;
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(merge_color(_eqcol, make_color_rgb(40, 44, 54), 0.45));
                ui_draw_stat_line_fit(_eqx, _eq_ry + 12, _equipped_here.name, (_px + _pw - 200) - _eqx);   // meme-length cursed titles shrink-to-fit
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(120, 128, 145));
                ui_draw_stat_str_stack(_eqx, _eq_ry + 45, _eq_ry + 38, ui_item_stat_str(_equipped_here), (_px + _pw - 200) - _eqx);
                draw_set_halign(fa_right);
                draw_set_color(make_color_rgb(120, 160, 210));
                draw_text(_px + _pw - 21, _eq_ry + 12, "[Equipped]");
                draw_set_halign(fa_left);
                // Thin divider between the worn item and the swap choices below.
                draw_set_color(make_color_rgb(50, 60, 84));
                draw_line(_px + 18, _eq_ry + _pk_row_h - 3, _px + _pw - 18, _eq_ry + _pk_row_h - 3);
            }

            if (_visible == 0) {
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(make_color_rgb(100, 110, 130));
                draw_text(_px + 30, _list_y0 + 12, _picker_in_hub
                    ? "No matching items in stash or pack."
                    : "No matching items in your pack.");
            } else {
                var _pc = variable_global_exists("chosen_class") ? global.chosen_class : -1;
                for (var _wr = 0; _wr < _win_rows; _wr++) {
                    var _ri     = _pk_scroll + _wr;
                    var _it     = _picker_items[_ri];
                    var _ry     = _list_y0 + _wr * _pk_row_h;
                    var _is_sel = (_ri == _gc.equip_picker_index);
                    var _src    = _picker_src[_ri];
                    var _it_cr  = variable_struct_exists(_it, "class_req") ? _it.class_req : -1;
                    var _locked = (_it_cr != -1 && _it_cr != _pc);

                    draw_set_alpha((_is_sel && !_locked) ? 0.9 : (_locked ? 0.28 : 0.45));
                    draw_set_color(_is_sel ? make_color_rgb(30, 50, 90) : make_color_rgb(18, 22, 38));
                    draw_rectangle(_px + 6, _ry, _px + _pw - 6, _ry + _pk_row_h - 6, false);
                    draw_set_alpha(1.0);

                    var _rcol = _locked ? make_color_rgb(80, 80, 90) : item_rarity_color(_it.rarity);
                    // Icon badge - drawn for locked items too so class-only gear still shows its art
                    ui_draw_item_icon(_px + 21, _ry + 9, 48, _it);
                    var _itx = _px + 78;
                    // Name
                    draw_set_font(ui_font(fnt_ui));
                    draw_set_color(_rcol);
                    ui_draw_stat_line_fit(_itx, _ry + 12, _it.name, (_px + _pw - 200) - _itx);   // meme-length cursed titles shrink-to-fit
                    // Stat string - stacks to 2 full-size lines instead of
                    // shrinking (08-11). When it takes both lines, the flavor
                    // row below yields its room (stats beat flavor in a picker;
                    // the hover tooltip still carries the flavor).
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(_locked ? make_color_rgb(70, 70, 80) : c_white);
                    var _st_ln = ui_draw_stat_str_stack(_itx, _ry + 45, _ry + 38, ui_item_stat_str(_it), (_px + _pw - 200) - _itx);
                    // Unique or flavor
                    if (_st_ln == 1 && !_locked && variable_struct_exists(_it, "unique_desc") && _it.unique_desc != "") {
                        draw_set_color(make_color_rgb(255, 200, 50));
                        draw_text(_itx, _ry + 75, _it.unique_desc);
                    } else if (_st_ln == 1 && !_locked && _it.effect_desc != "") {
                        draw_set_color(make_color_rgb(95, 105, 130));
                        draw_text(_itx, _ry + 75, _it.effect_desc);
                    }

                    // Source tag, rarity, class restriction
                    draw_set_halign(fa_right);
                    if (_locked) {
                        var _cr_names = ["Arcanist", "Bloodwarden", "Shadowstrider"];
                        draw_set_color(make_color_rgb(180, 80, 80));
                        draw_text(_px + _pw - 21, _ry + 12, "[" + _cr_names[_it_cr] + " only]");
                    } else {
                        draw_set_color((_src == 0) ? make_color_rgb(120, 200, 120) : make_color_rgb(200, 180, 100));
                        draw_text(_px + _pw - 21, _ry + 12, (_src == 0) ? "[Stash]" : "[Pack]");
                        draw_set_color(_rcol);
                        draw_text(_px + _pw - 21, _ry + 51, item_rarity_name(_it.rarity));
                        // Stat requirement - red when the current class can't meet it (equipping
                        // is hard-blocked). Visible in the list, not just the hover tooltip.
                        var _ereq = item_stat_requirement(_it);
                        if (_ereq.value > 0 && _ereq.stat != "") {
                            draw_set_color((player_base_stat(_ereq.stat) >= _ereq.value)
                                ? make_color_rgb(110, 170, 110) : make_color_rgb(225, 80, 80));
                            draw_text(_px + _pw - 21, _ry + 78, "Req " + string(_ereq.value) + " " + _ereq.stat);
                        }
                    }
                    draw_set_halign(fa_left);
                }

                // Position readout (replaces "N more above/below") when the list overflows
                // the visible window - shows the selected row's place in the full list.
                if (_visible > _max_rows) {
                    draw_set_halign(fa_center);
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(make_color_rgb(140, 160, 200));
                    draw_text(_px + _pw / 2, _list_y0 + _win_rows * _pk_row_h - 3, string(_gc.equip_picker_index + 1) + " / " + string(_visible));
                    draw_set_halign(fa_left);
                }

                // Hover tooltip for item in picker (windowed rows)
                var _hmx = device_mouse_x_to_gui(0);
                var _hmy = device_mouse_y_to_gui(0);
                if (_hmx >= _px + 6 && _hmx < _px + _pw - 6) {
                    for (var _tt_wr = 0; _tt_wr < _win_rows; _tt_wr++) {
                        var _tt_ry = _list_y0 + _tt_wr * _pk_row_h;
                        if (_hmy >= _tt_ry && _hmy < _tt_ry + _pk_row_h - 6) {
                            var _cur_eq = (variable_global_exists("inventory")
                                && array_length(global.inventory) > _sel_inv)
                                ? global.inventory[_sel_inv] : undefined;
                            ui_draw_item_tooltip(_hmx + 18, _hmy - 45, _picker_items[_pk_scroll + _tt_wr], _cur_eq);
                            break;
                        }
                    }
                }
            }
        }
    }

    // ---- ABILITIES TAB ----
    // Two-pane: a selectable list of the loadout on the LEFT, a full breakdown of
    // the highlighted ability on the RIGHT (mechanics, cost, school, and the CURRENT
    // damage it lands for with your equipped gear). Both panels gothic-framed.

    // ---- CLASS TRUNK VIEW (P2, 08-05): 5 permanently-exclusive this-or-that
    // rows gated by permanent level. Geometry MUST match the gc Step click
    // zones: rows y=252+r*142 h=126; box A x=260..1000, box B x=1090..1830.
    if (menu_tab == 2 && _gc.ability_page == 1) {
        var _tv_cls  = variable_global_exists("chosen_class") ? global.chosen_class : 0;
        var _tv_cat  = trunk_catalog(_tv_cls);
        var _tv_lvl  = player_permanent_level();
        var _tv_res  = (_tv_cls == 0) ? "Souls" : ((_tv_cls == 1) ? "Blood" : "Preparation");
        var _tv_pend = trunk_pending_count(_tv_cls);
        var _tv_x1 = 70, _tv_x2 = 1850, _tv_y1 = 170, _tv_y2 = 1012;
        draw_set_color(make_color_rgb(16, 15, 24));
        draw_rectangle(_tv_x1, _tv_y1, _tv_x2, _tv_y2, false);
        ui_draw_gothic_frame(_tv_x1, _tv_y1, _tv_x2, _tv_y2, 26);

        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(228, 190, 90));
        // Header offsets are TIGHTER than the other two pages on purpose: the five
        // node rows below are ABSOLUTE (252 + r*142, mirrored by the gc Step click
        // zones), so they did not move with the panel when it dropped to y170 to
        // clear the sub-tab chips (M 08-08). The header block has to fit the 82px
        // between the new panel top and the first row instead of pushing into it.
        draw_text(_tv_x1 + 30, _tv_y1 + 8, ((_tv_cls == 0) ? "ARCANIST" : ((_tv_cls == 1) ? "BLOODWARDEN" : "SHADOWSTRIDER")) + " CLASS TRUNK  -  how your " + _tv_res + " works");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(170, 180, 200));
        draw_set_halign(fa_right);
        draw_text(_tv_x2 - 30, _tv_y1 + 14, "Permanent Level " + string(_tv_lvl));
        draw_set_halign(fa_left);
        // Plain-language rules line (M 08-08: the old copy read as dev shorthand).
        // States what a pick costs you, what unlocks a row, and who can undo it.
        draw_set_color(_tv_pend > 0 ? make_color_rgb(230, 190, 80) : make_color_rgb(150, 140, 170));
        draw_text(_tv_x1 + 30, _tv_y1 + 46, (_tv_pend > 0)
            ? ("You have " + string(_tv_pend) + " choice" + (_tv_pend == 1 ? "" : "s") + " waiting. Each row gives two upgrades and you keep ONE - the other is lost for this character.")
            : "Each row offers two upgrades and you keep ONE - the other is lost for this character. New rows open as you gain permanent levels.");

        for (var _tr = 0; _tr < 5; _tr++) {
            var _row     = _tv_cat[_tr];
            var _ry      = 252 + _tr * 142;
            var _rlocked = (_tv_lvl < _row.lvl);
            var _rpick   = trunk_pick_get(_tv_cls, _tr);
            // Gate label column
            draw_set_font(ui_font(fnt_ui_small));
            // Status column: says what the row IS, not a bare adjective. A locked
            // row shows how many levels away it is - the player asked what "locked"
            // was waiting on and the screen never said (M 08-08).
            draw_set_color(_rlocked ? make_color_rgb(90, 95, 110) : make_color_rgb(210, 175, 90));
            draw_text(_tv_x1 + 24, _ry + 8, "LEVEL " + string(_row.lvl));
            if (_rlocked) {
                draw_set_color(make_color_rgb(110, 100, 90));
                draw_text(_tv_x1 + 24, _ry + 40, string(_row.lvl - _tv_lvl) + " levels away");
            } else if (_rpick == -1) {
                draw_set_color(make_color_rgb(230, 190, 80));
                draw_text(_tv_x1 + 24, _ry + 40, "PICK ONE");
            } else {
                draw_set_color(make_color_rgb(120, 170, 120));
                draw_text(_tv_x1 + 24, _ry + 40, "settled");
            }
            // The two node boxes
            for (var _sd = 0; _sd < 2; _sd++) {
                var _nd  = (_sd == 0) ? _row.a : _row.b;
                var _bx1 = (_sd == 0) ? 260 : 1090;
                var _bx2 = _bx1 + 740;
                var _cur     = (_gc.trunk_cursor == _tr && _gc.trunk_side == _sd);
                var _sel     = (!_rlocked && _rpick == -1 && _cur);
                var _ischose = (_rpick == _sd);
                var _issealed = (_rpick != -1 && _rpick != _sd);
                // Fill
                if (_ischose)       draw_set_color(make_color_rgb(52, 42, 20));
                else if (_issealed) draw_set_color(make_color_rgb(16, 16, 20));
                else if (_rlocked)  draw_set_color(make_color_rgb(20, 21, 28));
                else                draw_set_color(_sel ? make_color_rgb(30, 40, 62) : make_color_rgb(24, 28, 42));
                draw_rectangle(_bx1, _ry, _bx2, _ry + 126, false);
                // Border
                if (_ischose)       draw_set_color(make_color_rgb(210, 175, 90));
                else if (_sel)      draw_set_color((_gc.trunk_arm) ? make_color_rgb(220, 90, 80) : make_color_rgb(110, 160, 235));
                else if (_issealed) draw_set_color(make_color_rgb(60, 50, 50));
                else                draw_set_color(_rlocked ? make_color_rgb(45, 48, 60) : make_color_rgb(80, 90, 120));
                draw_rectangle(_bx1, _ry, _bx2, _ry + 126, true);
                // Cursor ring: drawn wherever the cursor IS, pickable or not.
                // It used to piggyback on _sel, so it vanished over settled
                // rows and the player lost track of the selector (M 08-11).
                // The inner border still tells the state (gold=taken etc.);
                // the double outer ring is purely "you are here".
                if (_cur) {
                    draw_set_color((_sel && _gc.trunk_arm) ? make_color_rgb(220, 90, 80) : make_color_rgb(110, 160, 235));
                    draw_rectangle(_bx1 - 2, _ry - 2, _bx2 + 2, _ry + 128, true);
                    draw_rectangle(_bx1 - 3, _ry - 3, _bx2 + 3, _ry + 129, true);
                }
                // Text
                var _tcol_t = _ischose ? make_color_rgb(240, 215, 140) : (_issealed || _rlocked ? make_color_rgb(110, 112, 125) : c_white);
                var _tcol_l = _ischose ? make_color_rgb(210, 195, 150) : (_issealed || _rlocked ? make_color_rgb(90, 92, 105) : make_color_rgb(180, 188, 205));
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(_tcol_t);
                draw_text(_bx1 + 20, _ry + 10, _nd.title + (_ischose ? "   -  YOU TOOK THIS" : (_issealed ? "   -  GIVEN UP" : "")));
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(_tcol_l);
                draw_text_ext(_bx1 + 20, _ry + 52, _nd.label, 26, _bx2 - _bx1 - 40);
            }
            // "OR" divider between open pair
            if (!_rlocked && _rpick == -1) {
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(150, 140, 170));
                draw_set_halign(fa_center);
                draw_text((1000 + 1090) / 2, _ry + 52, "OR");
                draw_set_halign(fa_left);
            }
        }

        // Footer strip: armed confirm vs hints
        var _tf_y = _tv_y2 - 54;
        if (_gc.trunk_arm) {
            var _fr  = _tv_cat[_gc.trunk_cursor];
            var _fnd = (_gc.trunk_side == 0) ? _fr.a : _fr.b;
            var _fot = (_gc.trunk_side == 0) ? _fr.b : _fr.a;
            draw_set_color(make_color_rgb(120, 40, 40));
            draw_rectangle(_tv_x1 + 24, _tf_y, _tv_x2 - 24, _tv_y2 - 12, false);
            draw_set_color(make_color_rgb(230, 120, 110));
            draw_rectangle(_tv_x1 + 24, _tf_y, _tv_x2 - 24, _tv_y2 - 12, true);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(c_white);
            draw_set_halign(fa_center); draw_set_valign(fa_middle);
            draw_text(GUI_CX, (_tf_y + _tv_y2 - 12) / 2,
                "Keep " + string_upper(_fnd.title) + " and give up " + string_upper(_fot.title) + " for good?     Enter: Yes, keep it     Esc: Not yet");
            draw_set_halign(fa_left); draw_set_valign(fa_top);
        } else {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(140, 150, 170));
            draw_set_halign(fa_center);
            draw_text(GUI_CX, _tf_y + 14, "W/S: Move    A/D: Change page    Enter: Take this one (asks first)    Esc: Back        Vael can undo the whole trunk for 500g + 50 dust");
            draw_set_halign(fa_left);
        }
        draw_set_font(-1);
    }

    // ---- TALENTS PAGE (08-08): a read-only ledger of every ability's talent
    // web - points earned, points still unspent, the next cast milestone, and
    // the nodes already woven. Weaving itself stays at the loadout screen (the
    // only place you can change your build), and this page says so plainly.
    // Left list geometry MIRRORS the ABILITIES page so the same click zones
    // in gc Step serve both (x86..684, rows from y166).
    if (menu_tab == 2 && _gc.ability_page == 2 && _player != undefined
        && array_length(_player.abilities) > 0) {
        var _tl_abs = _player.abilities;
        var _tl_cnt = array_length(_tl_abs);
        var _tl_cur = clamp(_gc.talent_cursor, 0, _tl_cnt - 1);

        // ===== LEFT: one row per ability, with its progress pips =====
        var _tl_x1 = 70, _tl_x2 = 700, _tl_y1 = 170, _tl_y2 = 1012;
        draw_set_color(make_color_rgb(14, 17, 28));
        draw_rectangle(_tl_x1, _tl_y1, _tl_x2, _tl_y2, false);
        ui_draw_gothic_frame(_tl_x1, _tl_y1, _tl_x2, _tl_y2, 26);

        var _tl_rh = min(108, 830 / _tl_cnt);
        for (var _tl = 0; _tl < _tl_cnt; _tl++) {
            var _tla   = _tl_abs[_tl];
            var _tly   = _tl_y1 + 16 + _tl * _tl_rh;
            var _tlsel = (_tl == _tl_cur);
            var _tlern = ability_web_mp_earned(_tla.name);
            var _tlpen = ability_web_mp_pending(_tla.name);

            draw_set_color(_tlsel ? make_color_rgb(40, 34, 70) : make_color_rgb(18, 22, 36));
            draw_rectangle(_tl_x1 + 16, _tly + 3, _tl_x2 - 16, _tly + _tl_rh - 5, false);
            if (_tlsel) {
                draw_set_color(make_color_rgb(180, 150, 235));
                draw_rectangle(_tl_x1 + 16, _tly + 3, _tl_x1 + 22, _tly + _tl_rh - 5, false);
            }
            var _tlic = _tl_rh - 24;
            ui_draw_ability_icon(_tl_x1 + 30, _tly + 10, _tlic, _tla);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_tlsel ? c_white : make_color_rgb(170, 180, 200));
            var _tltx = _tl_x1 + 30 + _tlic + 16;
            draw_text(_tltx, _tly + 12, ui_truncate(_tla.name, _tl_x2 - _tltx - 16));
            // Four pips = the four talent points this ability can ever earn.
            // Filled = spent on a node, ringed = earned and waiting to be spent.
            var _tlspent = array_length(ability_web_picks(_tla.name));
            for (var _tp = 0; _tp < ability_web_cap(); _tp++) {
                var _tpx = _tltx + 9 + _tp * 26;
                var _tpy = _tly + 58;
                if (_tp < _tlspent) {
                    draw_set_color(make_color_rgb(180, 150, 235));
                    draw_circle(_tpx, _tpy, 8, false);
                } else if (_tp < _tlern) {
                    draw_set_color(make_color_rgb(230, 190, 80));
                    draw_circle(_tpx, _tpy, 8, true);
                    draw_circle(_tpx, _tpy, 7, true);
                } else {
                    draw_set_color(make_color_rgb(70, 76, 95));
                    draw_circle(_tpx, _tpy, 8, true);
                }
            }
            if (_tlpen > 0) {
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(230, 190, 80));
                draw_text(_tltx + 9 + ability_web_cap() * 26 + 10, _tly + 46,
                          string(_tlpen) + " ready");
            }
        }

        // ===== RIGHT: the selected ability's web in full =====
        var _tw    = _tl_abs[_tl_cur];
        var _tw_nm = _tw.name;
        var _td_x1 = 740, _td_x2 = 1850, _td_y1 = 170, _td_y2 = 1012;
        draw_set_color(make_color_rgb(16, 18, 28));
        draw_rectangle(_td_x1, _td_y1, _td_x2, _td_y2, false);
        ui_draw_gothic_frame(_td_x1, _td_y1, _td_x2, _td_y2, 26);

        var _tdx = _td_x1 + 40, _tdw = (_td_x2 - 40) - _tdx;
        draw_set_halign(fa_left); draw_set_valign(fa_top);
        ui_draw_ability_icon(_tdx, _td_y1 + 40, 96, _tw);
        draw_set_font(fnt_ui_title);
        draw_set_color(c_white);
        draw_text(_tdx + 120, _td_y1 + 43, _tw_nm);

        var _tw_casts = ability_casts(_tw_nm);
        var _tw_next  = ability_web_next_threshold(_tw_nm);
        var _tw_pend  = ability_web_mp_pending(_tw_nm);
        var _tw_picks = ability_web_picks(_tw_nm);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(170, 180, 200));
        draw_text(_tdx + 120, _td_y1 + 100, "Cast " + string(_tw_casts) + " time" + (_tw_casts == 1 ? "" : "s"));

        // The one line that explains the whole system, in player words.
        var _tdy = _td_y1 + 150;
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(140, 150, 175));
        draw_text_ext(_tdx, _tdy,
            "Casting an ability teaches it. At 10, 30, 60 and 100 lifetime casts it earns a talent point, and points are woven into the ability's web at the loadout screen before a run.",
            26, _tdw);
        _tdy += 88;

        // Next milestone, or "fully taught".
        draw_set_font(ui_font(fnt_ui));
        if (_tw_next == -1) {
            draw_set_color(make_color_rgb(120, 170, 120));
            draw_text(_tdx, _tdy, "Fully taught - all 4 talent points earned.");
        } else {
            draw_set_color(make_color_rgb(200, 210, 230));
            draw_text(_tdx, _tdy, "Next talent point at " + string(_tw_next) + " casts  ("
                                  + string(_tw_next - _tw_casts) + " to go)");
        }
        _tdy += 48;

        if (_tw_pend > 0) {
            draw_set_color(make_color_rgb(52, 42, 20));
            draw_rectangle(_tdx, _tdy, _td_x2 - 40, _tdy + 62, false);
            draw_set_color(make_color_rgb(230, 190, 80));
            draw_rectangle(_tdx, _tdy, _td_x2 - 40, _tdy + 62, true);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(240, 215, 140));
            draw_text(_tdx + 18, _tdy + 16, string(_tw_pend) + " talent point"
                + (_tw_pend == 1 ? "" : "s") + " waiting - weave "
                + (_tw_pend == 1 ? "it" : "them") + " at the loadout screen.");
            _tdy += 78;
        }

        // Woven nodes, with what each one actually did.
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(180, 150, 235));
        draw_text(_tdx, _tdy, "WOVEN  " + string(array_length(_tw_picks)) + " / " + string(ability_web_cap()));
        _tdy += 40;
        if (array_length(_tw_picks) == 0) {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(120, 130, 150));
            draw_text_ext(_tdx, _tdy, "Nothing woven yet. This ability still does exactly what its description says.", 26, _tdw);
        } else {
            for (var _tn = 0; _tn < array_length(_tw_picks); _tn++) {
                var _tnode = ability_web_node_by_id(_tw, _tw_picks[_tn]);
                if (_tnode == undefined) continue;
                draw_set_color(make_color_rgb(24, 28, 42));
                draw_rectangle(_tdx, _tdy, _td_x2 - 40, _tdy + 84, false);
                draw_set_color(make_color_rgb(90, 78, 130));
                draw_rectangle(_tdx, _tdy, _td_x2 - 40, _tdy + 84, true);
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(make_color_rgb(214, 200, 240));
                draw_text(_tdx + 18, _tdy + 8, _tnode.title);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(160, 170, 195));
                draw_text_ext(_tdx + 18, _tdy + 44, _tnode.label, 24, _tdw - 36);
                _tdy += 92;
                // Footer owns y >= _td_y2-64: stop before a box can reach it.
                if (_tdy + 84 > _td_y2 - 64) break;
            }
        }

        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(140, 150, 170));
        draw_text(GUI_CX, _td_y2 - 40, "W/S: Move     A/D: Change page     Esc: Back");
        draw_set_halign(fa_left);
        draw_set_font(-1);
    } else if (menu_tab == 2 && _gc.ability_page == 2) {
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(120, 130, 150));
        draw_text(_pad, _content_y + 90, "No abilities to show - start a run to view your talents.");
    }

    // Drawn AFTER the three page panels: at y=104..142 the chip strip sits
    // right against the gothic frame's top ornament, which used to overdraw it.
    // ---- ABILITIES | CLASS TRUNK | TALENTS page chips (08-08 rework) ----
    // Three sub-pages walked with A/D, mirroring how the equipment sub-lists work.
    // Geometry MUST match the click zones in obj_game_controller Step: 3 chips
    // w=280 gap=10 centered on GUI_CX, y=104..142. Each carries its own "needs
    // your attention" badge so the player never has to open a page to find out.
    if (menu_tab == 2) {
        var _tc_cls  = variable_global_exists("chosen_class") ? global.chosen_class : 0;
        var _tc_w  = 280;
        var _tc_y0 = 104, _tc_y1 = 142;
        var _tc_bx = GUI_CX - (ABILITY_PAGE_COUNT * 290 - 10) / 2;
        var _tc_lbl = ["ABILITIES", "CLASS TRUNK", "TALENTS"];
        // Badge counts: trunk rows waiting on a choice, abilities holding unspent
        // talent points. 0 = no badge.
        var _tc_badge = [0, trunk_pending_count(_tc_cls), 0];
        if (_player != undefined) {
            for (var _tb = 0; _tb < array_length(_player.abilities); _tb++)
                if (ability_web_mp_pending(_player.abilities[_tb].name) > 0) _tc_badge[2]++;
        }
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_halign(fa_center); draw_set_valign(fa_middle);
        for (var _tc = 0; _tc < ABILITY_PAGE_COUNT; _tc++) {
            var _tcx  = _tc_bx + _tc * 290;
            var _tcon = (_gc.ability_page == _tc);
            // Active chip takes the page's own accent (blue list / gold trunk /
            // violet talents) so the colour tells you where you are.
            var _tcfill, _tcline;
            if (_tc == 1)      { _tcfill = make_color_rgb(56, 44, 24);  _tcline = make_color_rgb(210, 175, 90); }
            else if (_tc == 2) { _tcfill = make_color_rgb(38, 30, 60);  _tcline = make_color_rgb(160, 130, 225); }
            else               { _tcfill = make_color_rgb(30, 50, 90);  _tcline = make_color_rgb(80, 140, 220); }
            draw_set_color(_tcon ? _tcfill : make_color_rgb(20, 25, 40));
            draw_rectangle(_tcx, _tc_y0, _tcx + _tc_w, _tc_y1, false);
            draw_set_color(_tcon ? _tcline : make_color_rgb(50, 60, 80));
            draw_rectangle(_tcx, _tc_y0, _tcx + _tc_w, _tc_y1, true);
            draw_set_color(_tcon ? c_white : make_color_rgb(140, 150, 170));
            draw_text(_tcx + _tc_w / 2, (_tc_y0 + _tc_y1) / 2, _tc_lbl[_tc]);
            if (_tc_badge[_tc] > 0) {
                draw_set_color(make_color_rgb(230, 190, 80));
                draw_circle(_tcx + _tc_w - 18, _tc_y0 + 8, 12, false);
                draw_set_color(make_color_rgb(30, 24, 10));
                draw_text(_tcx + _tc_w - 18, _tc_y0 + 9, string(_tc_badge[_tc]));
            }
        }
        draw_set_halign(fa_left); draw_set_valign(fa_top);
        draw_set_font(-1);
    }

    if (menu_tab == 2 && _gc.ability_page == 0 && _player != undefined && array_length(_player.abilities) > 0) {
        var _abs  = _player.abilities;
        var _acnt = array_length(_abs);
        var _acur = clamp(_gc.ability_view_cursor, 0, _acnt - 1);

        // ===== LEFT: ability list =====
        var _al_x1 = 70, _al_x2 = 700, _al_y1 = 170, _al_y2 = 1012;
        draw_set_color(make_color_rgb(14, 17, 28));
        draw_rectangle(_al_x1, _al_y1, _al_x2, _al_y2, false);
        ui_draw_gothic_frame(_al_x1, _al_y1, _al_x2, _al_y2, 26);

        // Reserve a fixed strip at the bottom of the left pane for the CLASS
        // PASSIVE box (drawn after the list). The loadout list fits above it.
        var _al_list_y2 = _al_y2 - 168;
        var _alr_pad = 16;
        var _alr_h   = min(108, ((_al_list_y2 - _al_y1) - _alr_pad * 2) / _acnt);
        for (var _ab = 0; _ab < _acnt; _ab++) {
            var _a  = _abs[_ab];
            var _ay = _al_y1 + _alr_pad + _ab * _alr_h;
            var _asel = (_ab == _acur);

            draw_set_color(_asel ? make_color_rgb(34, 54, 86) : make_color_rgb(18, 22, 36));
            draw_rectangle(_al_x1 + 16, _ay + 3, _al_x2 - 16, _ay + _alr_h - 5, false);
            if (_asel) {
                draw_set_color(make_color_rgb(210, 175, 90));
                draw_rectangle(_al_x1 + 16, _ay + 3, _al_x1 + 22, _ay + _alr_h - 5, false);
            }

            var _aic = _alr_h - 24;
            ui_draw_ability_icon(_al_x1 + 30, _ay + 10, _aic, _a);

            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_asel ? c_white : make_color_rgb(170, 180, 200));
            draw_text(_al_x1 + 30 + _aic + 16, _ay + 12, ui_truncate(_a.name, _al_x2 - (_al_x1 + 30 + _aic + 16) - 16));
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(228, 190, 90));
            draw_text(_al_x1 + 30 + _aic + 16, _ay + 48, string(_a.energy_cost) + " AP");
        }

        // ---- CLASS PASSIVE (pinned bottom of the left pane; always shown) ----
        // The class resource passive (Arcanist on-kill Souls, etc.) fired in the
        // combat log but had no inspect surface anywhere - you couldn't read what
        // your class actually does (M 07-20). Pinned here so it shows regardless of
        // which ability is highlighted. chosen_class: 0 Arcanist / 1 Bloodwarden /
        // 2 Shadowstrider (same mapping as the cost line's resource name below).
        var _cp_y1 = _al_y2 - 160, _cp_y2 = _al_y2 - 16;
        var _cpi_cls, _cpi_desc;
        switch (variable_global_exists("chosen_class") ? global.chosen_class : 0) {
            case 1:  _cpi_cls = "Bloodwarden";
                     _cpi_desc = "+1 Blood each time you take a hit. Your crimson payoffs spend it. The CLASS TRUNK page grows this rule."; break;
            case 2:  _cpi_cls = "Shadowstrider";
                     _cpi_desc = "+1 Preparation at the start of each turn. Powers your traps and tools. The CLASS TRUNK page grows this rule."; break;
            default: _cpi_cls = "Arcanist";
                     _cpi_desc = "+2 Souls whenever one of your hits kills an enemy. Your soul-spending spells cash it in. The CLASS TRUNK page grows this rule."; break;
        }
        draw_set_color(make_color_rgb(20, 24, 40));
        draw_rectangle(_al_x1 + 16, _cp_y1, _al_x2 - 16, _cp_y2, false);
        draw_set_color(make_color_rgb(120, 90, 190));
        draw_rectangle(_al_x1 + 16, _cp_y1, _al_x2 - 16, _cp_y2, true);
        draw_set_halign(fa_left); draw_set_valign(fa_top);
        draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(180, 150, 235));
        draw_text(_al_x1 + 32, _cp_y1 + 12, "CLASS PASSIVE  -  " + _cpi_cls);
        draw_set_font(ui_font(fnt_ui)); draw_set_color(make_color_rgb(214, 200, 240));
        draw_text_ext(_al_x1 + 32, _cp_y1 + 46, _cpi_desc, 28, (_al_x2 - 16) - (_al_x1 + 32) - 16);

        // ===== RIGHT: breakdown of the selected ability =====
        var _ad = _abs[_acur];
        var _bd_x1 = 740, _bd_x2 = 1850, _bd_y1 = 170, _bd_y2 = 1012;
        draw_set_color(make_color_rgb(16, 18, 28));
        draw_rectangle(_bd_x1, _bd_y1, _bd_x2, _bd_y2, false);
        ui_draw_gothic_frame(_bd_x1, _bd_y1, _bd_x2, _bd_y2, 26);

        var _bpad = 40;
        var _blx  = _bd_x1 + _bpad;
        var _brx  = _bd_x2 - _bpad;
        var _by   = _bd_y1 + _bpad;

        // Header: icon + name + role chip
        ui_draw_ability_icon(_blx, _by, 96, _ad);
        var _btx = _blx + 96 + 24;
        draw_set_halign(fa_left); draw_set_valign(fa_top);
        draw_set_font(fnt_ui_title);
        draw_set_color(c_white);
        draw_text(_btx, _by + 3, _ad.name);
        var _cat = ability_category(_ad);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(ability_category_color(_cat));
        draw_text(_btx, _by + 60, ability_category_label(_cat));

        // Cost / cooldown line
        var _ap2  = variable_struct_exists(_ad, "energy_cost") ? _ad.energy_cost : 0;
        var _sec2 = variable_struct_exists(_ad, "secondary_cost") ? _ad.secondary_cost : 0;
        var _cls2 = variable_global_exists("chosen_class") ? global.chosen_class : 0;
        // Trunk discounts (P2, 08-05): show the effective cost (class_id is all
        // the helper reads from the caster).
        if (_sec2 > 0) _sec2 = ability_secondary_cost_eff(_ad, { class_id: _cls2 });
        var _resn = (_cls2 == 0) ? "Souls" : ((_cls2 == 1) ? "Blood" : "Preparation");
        var _cl2  = string(_ap2) + " AP";
        if (_sec2 > 0) _cl2 += "   +" + string(_sec2) + " " + _resn;
        var _cd2 = ability_cooldown(_ad);
        if (_cd2 > 0) _cl2 += "   *   " + string(_cd2) + "-turn cooldown";
        draw_set_halign(fa_right);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(228, 190, 90));
        draw_text(_brx, _by + 6, _cl2);
        draw_set_halign(fa_left);

        _by += 120;
        // School / attack-class line
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(150, 160, 190));
        var _sch = school_label(ability_school(_ad));
        var _acl = ability_attack_class_tag(_ad);
        if (_sch != "") {
            draw_set_color(school_color(ability_school(_ad)));
            _acl = _sch + " school  -  " + _acl;
        }
        draw_text(_blx, _by, _acl);
        _by += 38;

        // CURRENT DAMAGE WITH EQUIPMENT (estimate against an unmitigated target)
        var _dmg_derived = _in_combat ? _player.derived : out_of_combat_dmg_derived();
        var _est = combat_estimate_hit(_ad, { derived: _dmg_derived }, {});
        if (_est >= 0) {
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(235, 120, 90));
            draw_text(_blx, _by, "Current hit (with gear):  ~" + string(_est)
                + "    [base " + string(_ad.base_damage) + "]");
            _by += 44;
        } else if (variable_struct_exists(_ad, "effect_value") && _ad.effect_value != 0) {
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(120, 200, 160));
            draw_text(_blx, _by, _ad.effect_type + ":  " + string(_ad.effect_value));
            _by += 44;
        }

        draw_set_color(make_color_rgb(60, 64, 90));
        draw_line(_blx, _by, _brx, _by);
        _by += 22;

        // Mechanics
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(120, 200, 140));
        draw_text(_blx, _by, "MECHANICS");
        _by += 34;
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(210, 214, 230));
        var _mech2 = ability_describe(_ad);
        draw_text_ext(_blx, _by, _mech2, -1, _brx - _blx);
        _by += string_height_ext(_mech2, -1, _brx - _blx) + 28;

        // P3 off-stat rider line (08-01) - same data as the Tab popup.
        var _ld_rdr = ability_stat_rider(_ad.name);
        if (_ld_rdr != undefined) {
            var _ld_have = ability_rider_stat_total(_ld_rdr.stat);
            var _ld_on   = (_ld_have >= _ld_rdr.at);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(_ld_on ? make_color_rgb(245, 195, 80) : make_color_rgb(120, 126, 146));
            var _ld_txt = "Rider: " + _ld_rdr.label + " at " + _ld_rdr.stat + " " + string(_ld_rdr.at)
                + "  (you: " + string(_ld_have) + ")";
            draw_text_ext(_blx, _by, _ld_txt, -1, _brx - _blx);
            _by += string_height_ext(_ld_txt, -1, _brx - _blx) + 21;
        }

        // Flavor / full description
        if (variable_struct_exists(_ad, "desc_full") && _ad.desc_full != "") {
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(140, 146, 170));
            draw_text_ext(_blx, _by, _ad.desc_full, -1, _brx - _blx);
        }

        draw_set_halign(fa_left);
        draw_set_valign(fa_top);

        // Hint
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(80, 90, 110));
        ui_draw_key_legend(960, 1035, "W/S: Browse    A/D: Class Trunk / Talents    Q/E: Switch Tab    I / Esc: Close");
        draw_set_halign(fa_left);
    } else if (menu_tab == 2 && _gc.ability_page == 0) {
        // 08-08 collision fix: this fallback used to fire whenever the ABILITIES
        // page wasn't drawing, so it printed straight through the CLASS TRUNK
        // header. It belongs to page 0 only.
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(120, 130, 150));
        draw_text(_pad, _content_y + 30, "No abilities to show - start a run to view your loadout.");
    }

    // ---- CONSUMABLES TAB ----
    // Per-turn item limit state - declared at function scope because the tab-3
    // footer legend (further down, outside this block) reads them too.
    var _limit_reached = false;
    var _no_ap         = false;
    if (menu_tab == 3) {
        // Grouped view: identical consumables collapse to one "Name xN" row (the real
        // array still holds every entry). Step's nav/use map back through it.
        var _cgroups        = consumables_grouped();
        var _cons_count     = array_length(_cgroups);
        var _sub_open       = _gc.consumable_submenu_open;
        var _sub_cur        = _gc.consumable_submenu_cursor;

        // Determine per-turn item limit state
        if (_in_combat) {
            var _ctrl_lim = instance_find(obj_combat_controller, 0);
            if (!_ctrl_lim.player_turn && items_used_this_turn >= 1) {
                _limit_reached = true;
            }
            if (_ctrl_lim.player_turn && _ctrl_lim.player.energy < 1) {
                _no_ap         = true;
                _limit_reached = true;
            }
        }

        // Pack carry-cap readout (top-right of the tab) so the 10-slot limit and
        // any Pack Rat bonus are always visible.
        var _pack_n   = variable_global_exists("consumable_inventory") ? array_length(global.consumable_inventory) : 0;
        var _pack_cap = consumable_carry_cap();
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_halign(fa_right);
        draw_set_color((_pack_n >= _pack_cap) ? make_color_rgb(225, 120, 90) : make_color_rgb(150, 200, 200));
        draw_text(1335, _content_y, "Pack  " + string(_pack_n) + " / " + string(_pack_cap));
        draw_set_halign(fa_left);

        if (_cons_count == 0) {
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(100, 110, 130));
            draw_text(_pad, _content_y + 30, "No consumables in inventory.");
        } else {
            // Status header (combat only)
            if (_in_combat) {
                var _ctrl_hdr = instance_find(obj_combat_controller, 0);
                draw_set_font(ui_font(fnt_ui));
                if (_no_ap) {
                    draw_set_color(c_red);
                    draw_text(_pad, _content_y, "Not enough AP - items cost 1 AP to use.");
                } else if (!_ctrl_hdr.player_turn) {
                    if (_limit_reached) {
                        draw_set_color(c_red);
                        draw_text(_pad, _content_y, "Item use limit reached for this enemy turn.");
                    } else {
                        draw_set_color(c_yellow);
                        draw_text(_pad, _content_y, "Enemy turn - 1 item use remaining.");
                    }
                }
            }

            // Windowed list - keep the cursor on screen when there are many items.
            // Mouse hit-testing in obj_game_controller Step uses the same window math.
            var _cons_max_vis = 7;
            var _cons_first   = ui_list_window("consumables", _sub_cur, _cons_count, _cons_max_vis);
            var _cons_last    = min(_cons_count, _cons_first + _cons_max_vis);

            // Position readout (replaces "N more above/below") - "X / N" at the bottom
            // when the consumable list overflows one page.
            if (_cons_count > _cons_max_vis) {
                draw_set_halign(fa_center);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(120, 200, 200));
                draw_text(705, _content_y + 60 + _cons_max_vis * 120 - 18, string(_sub_cur + 1) + " / " + string(_cons_count));
                draw_set_halign(fa_left);
            }

            for (var _ci = _cons_first; _ci < _cons_last; _ci++) {
                var _c      = _cgroups[_ci].item;
                var _clabel = consumable_group_label(_cgroups[_ci]);
                var _cy2    = _content_y + 60 + (_ci - _cons_first) * 120;
                var _is_cur = (_sub_open && _ci == _sub_cur);

                // Background - highlighted when this row is the cursor
                if (_is_cur) {
                    draw_set_color(make_color_rgb(30, 80, 80));
                } else {
                    draw_set_color(make_color_rgb(20, 30, 48));
                }
                draw_rectangle(_pad, _cy2, 1350, _cy2 + 98, false);

                // Border
                if (_is_cur && !_limit_reached) {
                    draw_set_color(make_color_rgb(80, 220, 220));
                } else if (_is_cur && _limit_reached) {
                    draw_set_color(make_color_rgb(180, 60, 60));
                } else if (_sub_open) {
                    draw_set_color(make_color_rgb(30, 90, 90));
                } else {
                    draw_set_color(make_color_rgb(40, 140, 140));
                }
                draw_rectangle(_pad, _cy2, 1350, _cy2 + 98, true);

                // Icon (left side) - shifts the text right only when art exists
                var _csp     = ui_consumable_icon_sprite(_c.name, _c);   // struct enables brew variants
                var _c_dim   = (_sub_open && !_is_cur);
                var _has_csp = (_csp != -1 && sprite_exists(_csp));
                var _ctext_x = _has_csp ? _pad + 96 : _pad + 18;
                if (_has_csp) {
                    draw_set_alpha(_c_dim ? 0.4 : 1.0);
                    draw_sprite_stretched(_csp, 0, _pad + 12, _cy2 + 12, 72, 72);
                    draw_set_alpha(1.0);
                }

                // Name
                draw_set_font(ui_font(fnt_ui));
                if (_sub_open && !_is_cur) {
                    draw_set_color(make_color_rgb(50, 130, 130));
                } else if (_is_cur && _limit_reached) {
                    draw_set_color(make_color_rgb(200, 100, 100));
                } else if (_is_cur) {
                    draw_set_color(make_color_rgb(120, 255, 255));
                } else {
                    draw_set_color(make_color_rgb(80, 220, 220));
                }
                draw_text(_ctext_x, _cy2 + 12, _clabel);

                // Description
                draw_set_font(ui_font(fnt_ui_small));
                if (_sub_open && !_is_cur) {
                    draw_set_color(make_color_rgb(70, 80, 95));
                } else {
                    draw_set_color(c_white);
                }
                draw_text(_ctext_x, _cy2 + 54, _c.description);

                // Gold value
                draw_set_halign(fa_right);
                if (_sub_open && !_is_cur) {
                    draw_set_color(make_color_rgb(80, 90, 60));
                } else {
                    draw_set_color(c_yellow);
                }
                draw_text(1335, _cy2 + 12, string(_c.gold_value) + "g value");
                draw_set_halign(fa_left);
            }
        }
    }

    // ---- COMPANION TAB ---- (M 07-08; replaced the in-menu Compendium, which
    // lives in the Journal now.) Shows the ACTIVE pet's full profile via the
    // shared ui_draw_pet_detail modal - same sheet as Bairc's Tab popup.
    if (menu_tab == 4) {
        var _cmp_pet = pet_active();
        if (_cmp_pet != undefined) {
            ui_draw_pet_detail(_cmp_pet, true);   // #1: seamless inline page, not the modal
        } else {
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(150, 160, 185));
            draw_text(GUI_CX, 480, "No active companion.");
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(110, 120, 145));
            draw_text(GUI_CX, 530, "Equip one at the dungeon gate (Loadout > Companion), or visit Bairc to raise one.");
            draw_set_halign(fa_left);
        }
    }

    // Bottom instructions. Stats(0) uses the generic line; Equipment(1) and
    // Abilities(2) draw their own hints above; Consumables(3)/Compendium(4)
    // draw context-specific hints below. All footers share fnt_ui_small and the
    // (960,1035) bottom-center anchor so they read consistently at 1080p.
    draw_set_font(ui_font(fnt_ui_small));
    if (menu_tab == 0) {
        draw_set_halign(fa_center);
        draw_set_color(make_color_rgb(80, 90, 110));
        ui_draw_key_legend(960, 1035, "Q/E: Switch Tab   I / Esc: Close");
        draw_set_halign(fa_left);
    }
    if (menu_tab == 4) {
        draw_set_halign(fa_center);
        draw_set_color(make_color_rgb(80, 90, 110));
        ui_draw_key_legend(960, 1035, "Q/E: Switch Tab   I / Esc: Close");
        draw_set_halign(fa_left);
    }
    if (menu_tab == 3) {
        draw_set_halign(fa_center);
        if (_gc.consumable_submenu_open && array_length(global.consumable_inventory) > 0) {
            if (_no_ap) {
                draw_set_color(make_color_rgb(200, 80, 80));
                ui_draw_key_legend(960, 1035, "W/S: Navigate   Need 1 AP to use   Esc: Cancel");
            } else if (_limit_reached) {
                draw_set_color(make_color_rgb(200, 80, 80));
                ui_draw_key_legend(960, 1035, "W/S: Navigate   1 per turn limit   Esc: Cancel");
            } else {
                draw_set_color(make_color_rgb(80, 90, 110));
                ui_draw_key_legend(960, 1035, "W/S: Navigate   Enter: Use [-1 AP]   Esc: Cancel");
            }
        } else {
            draw_set_color(make_color_rgb(100, 200, 100));
            ui_draw_key_legend(960, 1035, "Enter: Browse Items   Q/E: Switch Tab   I: Close");
        }
        draw_set_halign(fa_left);
    }

    // Equip confirmation toast - LAST so it sits above every panel + the frame
    // (band 52-115, UI_BANDS.md; fades over its final 30 frames).
    if (variable_instance_exists(_gc, "equip_notif_timer") && _gc.equip_notif_timer > 0) {
        ui_draw_toast(_gc.equip_notif_msg, 960, 52, clamp(_gc.equip_notif_timer / 30.0, 0, 1.0));
    }
    ui_acc_cap_popup_flush();   // Accuracy rune-cap hover (armed at the stats rows) - topmost
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_npc_bond(npc_id, x, y)
// Compact bond readout for an NPC's own screen, so purchases/services are seen
// affecting the relationship live. "Bond: <Tier>" + a small progress bar, or a
// ready flag at a gate. Never shows the raw score (diegetic). Left-aligned at (x,y).
// ---------------------------------------------------------------------------
function ui_draw_npc_bond(npc_id, x, y) {
    var _tier = affinity_tier(npc_id);
    var _rdy  = affinity_gate_ready(npc_id);
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(210, 190, 130));
    draw_text(x, y, "Bond: " + affinity_tier_name(npc_id));
    if (_rdy) {
        // C7 (M-approved 07-09): the owed FAVOR shows right here - objective +
        // progress while the gate quest runs, "[B] Deepen" once it's done/unstarted.
        var _gq = npc_active_gate_quest(npc_id);
        if (_gq != undefined && _gq.state.progress < _gq.def.obj_target) {
            draw_set_color(make_color_rgb(150, 200, 230));
            draw_text(x + 210, y, ui_truncate("Favor: " + _gq.def.objective
                + "  (" + string(min(_gq.state.progress, _gq.def.obj_target)) + "/" + string(_gq.def.obj_target) + ")", 470));
        } else if (_gq != undefined) {
            draw_set_color(c_aqua);
            draw_text(x + 210, y, "Favor done - [B] Deepen");
        } else {
            draw_set_color(c_aqua);
            draw_text(x + 210, y, "Ready - [B] Deepen");
        }
    } else if (_tier < 4) {
        var _bx = x + 210, _by = y + 6, _bw = 220, _bh = 12;
        draw_set_color(make_color_rgb(40, 44, 56));
        draw_rectangle(_bx, _by, _bx + _bw, _by + _bh, false);
        draw_set_color(make_color_rgb(210, 190, 130));
        draw_rectangle(_bx, _by, _bx + _bw * affinity_progress_frac(npc_id), _by + _bh, false);
        draw_set_color(make_color_rgb(90, 96, 110));
        draw_rectangle(_bx, _by, _bx + _bw, _by + _bh, true);
    }
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// NPC ACTOR - the animated NPC sprite shown beside their screen. Idle loops; an
// action plays once (on a buy/forge/craft) then reverts to idle. Auto-resets when
// the NPC changes. No-ops until the sprites are imported (sprite_exists guard).
// Sprites: spr_npc_<id>_idle / spr_npc_<id>_action (id = dorn/sable/maren/vex/petra/vael).
// ---------------------------------------------------------------------------
function npc_actor_ensure() {
    if (!variable_global_exists("npc_actor") || !is_struct(global.npc_actor))
        global.npc_actor = { npc: "", mode: "idle", frame: 0 };
}
// Trigger the open NPC's action animation (call on a successful transaction).
function npc_actor_play_action() {
    npc_actor_ensure();
    global.npc_actor.mode  = "action";
    global.npc_actor.frame = 0;
}
function ui_draw_npc_actor(npc_id, x, y, scale) {
    npc_actor_ensure();
    var _a = global.npc_actor;
    if (_a.npc != npc_id) { _a.npc = npc_id; _a.mode = "idle"; _a.frame = 0; }

    var _idle = asset_get_index("spr_npc_" + npc_id + "_idle");
    var _act  = asset_get_index("spr_npc_" + npc_id + "_action");
    if (_idle == -1 || !sprite_exists(_idle)) { npc_hearts_draw(); return; }

    var _spr, _nf;
    if (_a.mode == "action" && _act != -1 && sprite_exists(_act)) { _spr = _act; _nf = sprite_get_number(_act); }
    else { _spr = _idle; _nf = sprite_get_number(_idle); }

    _a.frame += 0.105;   // ~6.3 fps (slowed ~30% from 0.15 per M)
    if (_a.mode == "action") {
        if (_a.frame >= _nf) { _a.mode = "idle"; _a.frame = 0; _spr = _idle; _nf = sprite_get_number(_idle); }
    } else if (_a.frame >= _nf) {
        _a.frame -= _nf;
    }
    // `scale` is now a TARGET ON-SCREEN HEIGHT (px): normalise the varied pro-mode
    // canvas sizes (92-124px) so every NPC renders the same size in its column.
    var _norm = scale / max(1, sprite_get_height(_spr));
    draw_sprite_ext(_spr, floor(_a.frame) mod _nf, x, y, _norm, _norm, 0, c_white, 1.0);

    // Tier-up heart burst originates at the actor's chest height.
    npc_hearts_consume_pending(x, y - scale * 0.6);
    npc_hearts_draw();
}

// ---------------------------------------------------------------------------
// ui_draw_npc_column(npc_id)
// Draws the reserved right-hand NPC column: a subtle backing + the animated actor
// standing in it. NPC screens shrink their content to ~x1500 to clear this column
// (x1530..1852). Call once per screen instead of ui_draw_npc_actor.
// ---------------------------------------------------------------------------
function ui_draw_npc_column(npc_id) {
    var _cx1 = 1530, _cy1 = 156, _cx2 = 1852, _cy2 = 1016;
    draw_set_alpha(0.30);
    draw_set_color(make_color_rgb(10, 11, 18));
    draw_rectangle(_cx1, _cy1, _cx2, _cy2, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(48, 52, 70));
    draw_line(_cx1, _cy1, _cx1, _cy2);     // left divider separating it from the content
    ui_draw_npc_actor(npc_id, (_cx1 + _cx2) / 2, _cy2 - 36, 300);   // 300 = target on-screen height (px)
}

// ---------------------------------------------------------------------------
// HEART-FLUTTER VFX - blue hearts (Acquaintance..Companion) / red (Lover) rise and
// fade on affinity TIER-UP (and later quest-complete / gifting). global.heart_pending
// holds the new tier; a draw consumes it and spawns the burst. spr_heart_fx is tinted.
// ---------------------------------------------------------------------------
function npc_hearts_consume_pending(x, y) {
    if (!variable_global_exists("heart_pending") || global.heart_pending == undefined) return;
    npc_hearts_spawn(x, y, global.heart_pending);
    global.heart_pending = undefined;
}
function npc_hearts_spawn(x, y, tier) {
    if (!variable_global_exists("hearts")) global.hearts = [];
    var _col = (tier >= 4) ? make_color_rgb(235, 70, 95) : make_color_rgb(95, 155, 240);
    for (var _i = 0; _i < 8; _i++) {
        array_push(global.hearts, {
            x: x + random_range(-50, 50), y: y + random_range(-20, 20),
            vx: random_range(-0.7, 0.7), vy: random_range(-3.4, -2.2),
            life: 0, maxlife: random_range(55, 90),
            col: _col, scale: random_range(0.8, 1.5), phase: random(6.28),
        });
    }
}
function npc_hearts_draw() {
    if (!variable_global_exists("hearts") || array_length(global.hearts) == 0) return;
    var _spr = asset_get_index("spr_heart_fx");
    var _alive = [];
    for (var _i = 0; _i < array_length(global.hearts); _i++) {
        var _h = global.hearts[_i];
        _h.life += 1;
        _h.x += _h.vx + sin((_h.life + _h.phase) * 0.13) * 0.9;
        _h.y += _h.vy;
        _h.vy += 0.03;
        var _al = 1.0 - (_h.life / _h.maxlife);
        if (_al > 0 && _h.life < _h.maxlife) {
            if (_spr != -1 && sprite_exists(_spr)) draw_sprite_ext(_spr, 0, _h.x, _h.y, _h.scale, _h.scale, 0, _h.col, _al);
            array_push(_alive, _h);
        }
    }
    global.hearts = _alive;
}

// ---------------------------------------------------------------------------
// ui_draw_shop_screen()
// Full-screen overlay for Petra's Supplies (shop_open==0) or Dorn's Forge
// (shop_open==1). Draws all purchasable rows, gold balance, and a notification
// line. Called by obj_hub_controller Draw_64 after the stash screen call.
// ---------------------------------------------------------------------------
function ui_draw_shop_screen() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc  = instance_find(obj_game_controller, 0);
    if (_gc.shop_open == -1) return;

    var _is_petra = (_gc.shop_open == 0);
    var _accent   = _is_petra ? make_color_rgb(60, 190, 190) : make_color_rgb(210, 130, 40);
    var _title    = _is_petra ? "PETRA'S SUPPLIES" : "DORN'S FORGE";

    // Full-screen dark cover
    draw_set_alpha(0.96);
    draw_set_color(make_color_rgb(8, 10, 18));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    // Title
    draw_set_halign(fa_center);
    draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(_accent);
    draw_text(960, 36, _title);   // y36 keeps the title inside the rim opening

    // Gold (top-right) - pulled in from x1875 to clear the right rim band
    draw_set_halign(fa_right);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(c_yellow);
    draw_text(1875, 36, "Gold: " + string(global.gold) + "g");
    // Reagent readout under the gold (M 08-26) - x stays right of every tab bar.
    ui_draw_reagent_readout(1875, 78);

    // --- Tab bar (auto-centred in the row; Petra has a 3rd Trade tab) ---
    // Tabs always centre on x960 regardless of count, so 2 or 3 tabs stay symmetric.
    var _tab_y   = 96;
    var _tab_h   = 42;
    var _tab_w   = 320;
    var _tab_gap = 24;
    // Dorn gained a 4th tab on 08-08 when TEMPER moved here from Maren's forge.
    // Tab COUNT must come from shop_tab_count() - the Step hit-tests lay their
    // boxes out from the same number, and a mismatch silently misroutes clicks.
    var _tab_labels  = _is_petra ? ["BUY", "SELL", "TRADE"] : ["BUY", "SELL", "REFORGE", "TEMPER"];
    // 3rd tab accent differs per NPC: Petra's Trade = violet, Dorn's Reforge = forge
    // copper; 4th (Dorn only) is the tempering gold used by the forge-result banner.
    var _tab_accents = [_accent, make_color_rgb(220, 155, 45),
                        _is_petra ? make_color_rgb(190, 120, 210) : make_color_rgb(210, 140, 70),
                        make_color_rgb(200, 170, 110)];
    var _tab_n     = array_length(_tab_labels);
    var _tab_total = _tab_n * _tab_w + (_tab_n - 1) * _tab_gap;
    var _tab_x0    = 960 - _tab_total / 2;
    draw_set_font(ui_font(fnt_ui));
    for (var _ti = 0; _ti < _tab_n; _ti++) {
        var _tx  = _tab_x0 + _ti * (_tab_w + _tab_gap);
        var _on  = (_gc.shop_tab == _ti);
        var _tac = _tab_accents[_ti];
        draw_set_color(_on ? make_color_rgb(18, 20, 28) : make_color_rgb(12, 14, 20));
        draw_rectangle(_tx, _tab_y, _tx + _tab_w, _tab_y + _tab_h, false);
        draw_set_color(_on ? _tac : make_color_rgb(34, 42, 52));
        draw_rectangle(_tx, _tab_y, _tx + _tab_w, _tab_y + _tab_h, true);
        draw_set_halign(fa_center);
        draw_set_color(_on ? _tac : make_color_rgb(70, 88, 100));
        draw_text(_tx + _tab_w / 2, _tab_y + 9, _tab_labels[_ti]);
        // Hover quick-ref (M 08-15): what lives behind this tab, on mouse-over.
        // Petra joined 08-15 (tour pass - her tabs were the only bare ones left).
        var _th_body = ui_tab_hint_body((_is_petra ? "petra:" : "dorn:") + _tab_labels[_ti]);
        if (_th_body != "") ui_tab_hover_stash(_tx, _tab_y, _tx + _tab_w, _tab_y + _tab_h, _tab_labels[_ti], _th_body);
    }
    draw_set_halign(fa_left);

    // Notification line (shifted below tab bar)
    if (_gc.shop_notification != "") {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        var _is_sold_notif = (string_pos("Sold for", _gc.shop_notification) > 0);
        var _is_bad_notif  = (string_pos("Not enough", _gc.shop_notification) > 0);
        var _notif_col;
        if (_is_bad_notif) {
            _notif_col = c_red;
        } else if (_is_sold_notif) {
            _notif_col = c_yellow;
        } else {
            _notif_col = make_color_rgb(100, 220, 120);
        }
        draw_set_color(_notif_col);
        draw_text(960, 156, _gc.shop_notification);
        draw_set_halign(fa_left);
    }

    // Ornate gothic rim around the whole overlay. Drawn here (common to both Buy/Sell
    // branches) - the band sits OUTSIDE the content opening (30,30)-(1890,1050), so the
    // list rows (x150..1770, y189+) never touch it. Title/gold raised to y36, hints to y1026.
    ui_draw_gothic_frame(30, 30, 1890, 1050, 30);

    // NPC bond readout (title row, far left) - watch purchases/services raise the
    // relationship live. Sits on the title line so it clears the tab bar below.
    ui_draw_npc_bond(_is_petra ? "petra" : "dorn", 90, 45);

    // Reserved right-hand NPC sprite column (content is narrowed to clear it).
    ui_draw_npc_column(_is_petra ? "petra" : "dorn");

    var _rx0  = 150;
    var _rw   = 1350;  // narrowed from 1620 to clear the right-hand NPC sprite column
    var _rh   = 117;   // tall enough for 3 lines (stats + unique desc / class tag)
    var _rgap = 9;
    var _ry0  = 189;   // shifted down to make room for tab bar

    // =========================================================================
    // SELL TAB
    // =========================================================================
    if (_gc.shop_tab == 1) {

        // Sell list via the shared sorted builder (rarity-DESC equipment, then
        // consumables) - MUST match the Step action handler's list exactly, so both
        // call shop_build_sell_list(). Equipped slots excluded.
        var _sl       = shop_build_sell_list();
        var _sl_items = _sl.items;
        var _sl_src   = _sl.src;
        var _sl_tags  = _sl.tags;
        var _sl_count = array_length(_sl_items);

        if (_sl_count == 0) {
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(90, 100, 120));
            draw_text(960, 480, "Nothing to sell.");
            draw_set_halign(fa_left);
        } else {
            var _sell_idx    = clamp(_gc.sell_index, 0, _sl_count - 1);
            var _sell_vis    = shop_sell_visible_rows();
            var _sell_scroll = clamp(_gc.sell_scroll, 0, max(0, _sl_count - _sell_vis));
            var _visible_end = min(_sell_scroll + _sell_vis, _sl_count);

            for (var _ri = _sell_scroll; _ri < _visible_end; _ri++) {
                var _ry    = _ry0 + (_ri - _sell_scroll) * (_rh + _rgap);
                var _it    = _sl_items[_ri];
                var _is_sel = (_ri == _sell_idx);

                // Sell price - the SAME formula the Step pays (affinity tier, Petra
                // rank, legendary floor, valuables) so the list never under-quotes.
                var _sprice = shop_sell_price(_it, (_gc.shop_open == 0) ? "petra" : "dorn");

                // Name color: rarity color for equipment, cyan for consumables
                var _name_col;
                if (variable_struct_exists(_it, "rarity")) {
                    _name_col = item_rarity_color(_it.rarity);
                } else {
                    _name_col = make_color_rgb(80, 210, 210);
                }

                // Row background
                draw_set_alpha(_is_sel ? 1.0 : 0.55);
                draw_set_color(_is_sel ? make_color_rgb(30, 26, 10) : make_color_rgb(14, 18, 28));
                draw_rectangle(_rx0, _ry, _rx0 + _rw, _ry + _rh, false);
                draw_set_alpha(1.0);
                draw_set_color(_is_sel ? make_color_rgb(200, 155, 40) : make_color_rgb(55, 50, 25));
                draw_rectangle(_rx0, _ry, _rx0 + _rw, _ry + _rh, true);

                // Icon badge - gear icon for equipment, consumable badge otherwise
                var _sell_is_equip = variable_struct_exists(_it, "slot");
                var _sell_tx = _rx0 + 24;
                if (_sell_is_equip) {
                    ui_draw_item_icon(_rx0 + 15, _ry + 12, 48, _it);
                    _sell_tx = _rx0 + 75;
                } else {
                    ui_draw_consumable_icon(_rx0 + 15, _ry + 12, 48, _it);
                    _sell_tx = _rx0 + 75;
                }
                // Name (truncated, not scaled - the class tag below positions off
                // the DRAWN width, so both must measure the same string).
                var _sell_nm = ui_truncate(_it.name, _rw - 420);
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(_name_col);
                draw_text(_sell_tx, _ry + 12, _sell_nm);
                // Class restriction appended after the name, buy-tab style (M 07-28:
                // the old dim corner tag read as invisible - he left the shop to
                // check inventory). Gold = your class can use it, red = another's.
                if (variable_struct_exists(_it, "class_req") && _it.class_req != -1) {
                    var _scr_names  = ["Arcanist", "Bloodwarden", "Shadowstrider"];
                    var _scr_my_cl  = variable_global_exists("chosen_class") ? global.chosen_class : -1;
                    var _scr_name_w = string_width(_sell_nm);   // measure while fnt_ui is set
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color((_it.class_req == _scr_my_cl) ? make_color_rgb(210, 175, 90) : make_color_rgb(225, 80, 80));
                    draw_text(_sell_tx + _scr_name_w + 18, _ry + 15,
                        "[" + _scr_names[clamp(_it.class_req, 0, 2)] + " only]");
                }
                // Stats or consumable description
                draw_set_font(ui_font(fnt_ui_small));
                // Shrink-to-fit so long affix/flavor strings clear the right-side
                // source-tag / price / class column (same fix as the Dorn buy rows).
                var _sell_max = (_rx0 + _rw - 180) - _sell_tx;
                if (_sell_is_equip) {
                    draw_set_color(c_white);
                    ui_draw_stat_line_fit(_sell_tx, _ry + 45, ui_item_stat_str(_it), _sell_max);
                    if (variable_struct_exists(_it, "unique_desc") && _it.unique_desc != "") {
                        draw_set_color(make_color_rgb(200, 160, 40));
                        ui_draw_stat_line_fit(_sell_tx, _ry + 75, _it.unique_desc, _sell_max);
                    } else if (_it.effect_desc != "") {
                        draw_set_color(make_color_rgb(95, 105, 130));
                        ui_draw_stat_line_fit(_sell_tx, _ry + 75, ui_sentence(_it.effect_desc), _sell_max);
                    }
                } else {
                    var _cdesc = variable_struct_exists(_it, "description") ? _it.description : "";
                    draw_set_color(make_color_rgb(130, 140, 155));
                    ui_draw_stat_line_fit(_sell_tx, _ry + 45, ui_sentence(_cdesc), _sell_max);
                }
                // Right side: source tag + sell price. (Class restriction moved up
                // next to the name, buy-tab style - the corner tag was too subtle.)
                draw_set_halign(fa_right);
                draw_set_color(make_color_rgb(155, 165, 180));
                draw_text(_rx0 + _rw - 24, _ry + 12, _sl_tags[_ri]);
                draw_set_color(c_yellow);
                draw_text(_rx0 + _rw - 24, _ry + 45, string(_sprice) + "g");
                draw_set_halign(fa_left);
            }

            // Scroll indicator. Was drawn at _ry0 + 7*(_rh+_rgap) + 6 = y1077 - off
            // the bottom of the screen, so it has never actually been visible; the
            // same off-by-one that ran the 7th row into the perimeter frame (M
            // 08-08). Right-aligned on the key-legend line instead, which is free
            // (the legend is centred on 960 and ends well short of x1450), and it
            // now names the WINDOW so the row that went away is accounted for.
            if (_sl_count > _sell_vis) {
                draw_set_halign(fa_right);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(95, 105, 125));
                draw_text_outline(1770, 1026, "Showing " + string(_sell_scroll + 1) + "-"
                    + string(_visible_end) + " of " + string(_sl_count) + "   W/S to scroll");
                draw_set_halign(fa_left);
            }
        }

        // Confirm bar for rare+ items (amber highlight)
        if (_gc.sell_confirm_name != "") {
            var _cf_y = 954;
            draw_set_color(make_color_rgb(50, 30, 8));
            draw_rectangle(150, _cf_y, 1770, _cf_y + 69, false);
            draw_set_color(make_color_rgb(220, 145, 40));
            draw_rectangle(150, _cf_y, 1770, _cf_y + 69, true);
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(c_white);
            draw_text(960, _cf_y + 18, _gc.shop_notification + "   [SPACE] Confirm   [ESC] Cancel");
            draw_set_halign(fa_left);
        }

        // Sell tab footer - swaps to confirm hint when rare+ item confirmation is pending
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(75, 85, 105));
        if (_gc.sell_confirm_name != "") {
            ui_draw_key_legend(960, 1026, "Space: Confirm   Esc: Cancel");
        } else {
            ui_draw_key_legend(960, 1026, "W/S: Navigate   Q/E: Buy/Sell   Enter: Sell   Esc: Close");
        }
        draw_set_halign(fa_left);
        draw_set_valign(fa_top);
        draw_set_alpha(1.0);
        return;
    }

    // =========================================================================
    // REFORGE TAB (Dorn only; shop_tab == 2) - two-panel ingot hoard + rework list
    // =========================================================================
    if (_gc.shop_tab == 2 && !_is_petra) {
        ui_draw_dorn_reforge(_gc);
        return;
    }

    // =========================================================================
    // TEMPER TAB (Dorn only; shop_tab == 3) - moved off Maren's forge 08-08
    // =========================================================================
    if (_gc.shop_tab == 3 && !_is_petra) {
        ui_draw_dorn_temper(_gc);
        return;
    }

    // =========================================================================
    // TREASURE TRADER TAB (Petra only; shop_tab == 2)
    // =========================================================================
    if (_gc.shop_tab == 2) {
        var _tt_accent = make_color_rgb(190, 120, 210);

        petra_orders_ensure();
        var _po_more = variable_instance_exists(_gc, "petra_place_more") && _gc.petra_place_more;
        if (petra_order_active() && !_po_more) {
            // --- THE LEDGER (two-order refactor 08-15): one status card per
            // order, stacked. COLLECT rides the FIRST ready card; a second
            // ledger line (station rank 1) offers PLACE ANOTHER ORDER below.
            var _po_n       = array_length(global.petra_orders);
            var _card_h     = (_po_n > 1) ? 300 : 320;
            var _card_y0    = (_po_n > 1) ? 200 : 300;
            var _card_gap   = 44;
            var _po_any_ready   = false;
            var _po_collect_drawn = false;
            var _pomx = device_mouse_x_to_gui(0), _pomy = device_mouse_y_to_gui(0);
            var _pop  = mouse_check_button_pressed(mb_left);
            for (var _poi = 0; _poi < _po_n; _poi++) {
                var _o     = global.petra_orders[_poi];
                var _cy0   = _card_y0 + _poi * (_card_h + _card_gap);
                var _cy1   = _cy0 + _card_h;
                var _ready = (_o.status == "ready");
                if (_ready) _po_any_ready = true;
                draw_set_color(make_color_rgb(16, 14, 24));
                draw_rectangle(360, _cy0, 1560, _cy1, false);
                draw_set_color(_ready ? make_color_rgb(120, 220, 140) : _tt_accent);
                draw_rectangle(360, _cy0, 1560, _cy1, true);

                draw_set_halign(fa_center);
                draw_set_font((_po_n > 1) ? ui_font(fnt_ui) : fnt_ui_title);
                draw_set_color(_ready ? make_color_rgb(120, 220, 140) : _tt_accent);
                draw_text(960, _cy0 + 24, (_ready ? "ORDER READY" : "ORDER IN PROGRESS")
                    + ((_po_n > 1) ? ("   -   ledger line " + string(_poi + 1)) : ""));

                draw_set_font(ui_font(fnt_ui));
                draw_set_color(c_white);
                if (petra_order_is_rune(_o)) {
                    var _bpd = rune_get(_o.rune_id);
                    draw_text(960, _cy0 + 96, "Blueprint:  " + ((_bpd != undefined) ? _bpd.name : _o.rune_id)
                        + " " + rune_tier_roman(_o.rune_tier));
                } else {
                    draw_text(960, _cy0 + 96, item_rarity_name(_o.input_tier) + "   ->   " + item_rarity_name(_o.output_tier));
                }

                if (!_ready) {
                    var _frac = clamp(_o.progress_floors / max(1, _o.cost_floors), 0, 1);
                    draw_set_color(make_color_rgb(40, 36, 52));
                    draw_rectangle(520, _cy0 + 156, 1400, _cy0 + 186, false);
                    draw_set_color(_tt_accent);
                    draw_rectangle(520, _cy0 + 156, 520 + (1400 - 520) * _frac, _cy0 + 186, false);
                    draw_set_color(make_color_rgb(120, 100, 140));
                    draw_rectangle(520, _cy0 + 156, 1400, _cy0 + 186, true);
                    draw_set_font(ui_font(fnt_ui));
                    draw_set_color(c_white);
                    draw_text(960, _cy0 + 202, string(_o.progress_floors) + " / " + string(_o.cost_floors) + " floors cleared"
                        + (_o.req_awakening > 0 ? ("    (A" + string(_o.req_awakening) + "+ only)") : "")
                        + (_o.dust_bias ? "    - roll-biased" : ""));
                } else {
                    draw_set_font(ui_font(fnt_ui));
                    draw_set_color(make_color_rgb(140, 220, 150));
                    draw_text(960, _cy0 + 148, "Petra has something for you.");
                    // Pulsing ready ring + a tappable COLLECT (the reveal moment -
                    // M 07-27 visual pass; Step consumes petra:collect). Only the
                    // FIRST ready card carries the button - Enter collects that one.
                    if (!_po_collect_drawn) {
                        _po_collect_drawn = true;
                        var _rd_p = frac(current_time / 900);
                        draw_set_alpha(0.5 * (1 - _rd_p));
                        draw_set_color(make_color_rgb(120, 220, 140));
                        draw_circle(960, _cy0 + 160, 26 + _rd_p * 70, true);
                        draw_set_alpha(1.0);
                        ui_confirm_button(790, _cy1 - 76, 1130, _cy1 - 16, "COLLECT  [Enter]", make_color_rgb(120, 210, 130),
                            _pomx, _pomy, _pop, "petra:collect");
                    }
                }
            }
            // Second ledger line open (station rank 1): the place-another button.
            if (petra_order_can_place()) {
                ui_confirm_button(740, 906, 1180, 958, "PLACE ANOTHER ORDER  [Space]", _tt_accent,
                    _pomx, _pomy, _pop, "petra:more");
            }

            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(160, 140, 180));
            var _po_leg = "";
            if (_po_any_ready)            _po_leg += "Enter: Collect    ";
            if (petra_order_can_place())  _po_leg += "Space: New Order    ";
            _po_leg += "C: Cancel newest    Q/E: Switch Tab    Esc: Close";
            ui_draw_key_legend(960, 1026, _po_leg);
            draw_set_halign(fa_left);

        } else {
            // --- No order: GEAR trade (mode 0) or RUNE BLUEPRINTS (mode 1) ---
            var _sel   = _gc.petra_trade_selected;
            var _sel_n = array_length(_sel);
            var _sel_rar = -1;
            if (_sel_n > 0) {
                var _f0 = global.equipment_stash[_sel[0]];
                if (is_struct(_f0) && variable_struct_exists(_f0, "rarity")) _sel_rar = _f0.rarity;
            }

            // Mode chip (M 07-27 rune blueprints) - Step consumes petra:mode; [R]
            // mirrors it. Sits left of the centered instruction line (which spans
            // roughly x510-1410, so x150-400 is clear).
            var _pt_mode = variable_instance_exists(_gc, "petra_trade_mode") ? _gc.petra_trade_mode : 0;
            var _pmx3 = device_mouse_x_to_gui(0), _pmy3 = device_mouse_y_to_gui(0);
            var _ppress3 = mouse_check_button_pressed(mb_left);
            // "-> X" reads as "switch to X" (bare "RUNES" looked like the CURRENT
            // mode - trade-tab cleanup, tour pass 08-15). Cycles three modes
            // since 08-26: GEAR -> RUNES -> REAGENTS.
            ui_confirm_button(150, 176, 400, 220,
                (_pt_mode == 0) ? "-> RUNES  [R]" : ((_pt_mode == 1) ? "-> REAGENTS  [R]" : "-> GEAR  [R]"),
                make_color_rgb(190, 120, 210), _pmx3, _pmy3, _ppress3, "petra:mode");

            if (_pt_mode == 2) {
            // ================= REAGENT SWAP MODE (M-locked 08-26) =================
            var _rg_cat = reagent_catalog();
            var _rg_n   = array_length(_rg_cat);
            var _rg_t   = petra_reagent_trade_terms();
            var _rg_ph  = variable_instance_exists(_gc, "petra_rg_phase") ? _gc.petra_rg_phase : 0;
            var _rg_gv  = variable_instance_exists(_gc, "petra_rg_give")  ? _gc.petra_rg_give  : 0;
            var _rg_gt  = variable_instance_exists(_gc, "petra_rg_get")   ? _gc.petra_rg_get   : 1;

            if (_gc.shop_notification == "" && _gc.petra_trade_notification == "") {
                draw_set_halign(fa_center);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(150, 130, 170));
                draw_text(960, 162, "Trade " + string(_rg_t.give) + " of one reagent for " + string(_rg_t.get)
                    + " of another - Petra keeps the difference."
                    + ((npc_rank("petra") >= 2) ? "  (Rank perk: 3-for-2.)" : ""));
                draw_set_halign(fa_left);
            }

            // Two columns: GIVE (left) and RECEIVE (right); the focused column
            // carries the accent border, matching the stash two-column idiom.
            var _rg_cols = [
                { x0: 190, x1: 790,  lab: "YOU GIVE " + string(_rg_t.give),    cur: _rg_gv, focus: (_rg_ph == 0), tag: "petra:rggive" },
                { x0: 860, x1: 1460, lab: "YOU RECEIVE " + string(_rg_t.get),  cur: _rg_gt, focus: (_rg_ph == 1), tag: "petra:rgget" }
            ];
            for (var _rc_i = 0; _rc_i < 2; _rc_i++) {
                var _rc = _rg_cols[_rc_i];
                draw_set_color(_rc.focus ? make_color_rgb(190, 120, 210) : make_color_rgb(60, 50, 70));
                draw_rectangle(_rc.x0, 236, _rc.x1, 780, true);
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(_rc.focus ? c_white : make_color_rgb(150, 140, 165));
                draw_text(_rc.x0 + 18, 246, _rc.lab);
                for (var _rr_i = 0; _rr_i < _rg_n; _rr_i++) {
                    var _rr  = _rg_cat[_rr_i];
                    var _rry = 300 + _rr_i * 118;
                    var _rr_sel = (_rc.cur == _rr_i);
                    // Greyed in the RECEIVE column when it matches the GIVE pick.
                    var _rr_dis = (_rc_i == 1 && _rr_i == _rg_gv);
                    if (_ppress3 && _pmx3 >= _rc.x0 && _pmx3 < _rc.x1 && _pmy3 >= _rry && _pmy3 < _rry + 108
                        && !_rr_dis
                        && !(variable_instance_exists(_gc, "petra_rg_ck") && _gc.petra_rg_ck)) {
                        input_inject(_rc.tag + string(_rr_i));
                    }
                    draw_set_alpha(_rr_dis ? 0.25 : (_rr_sel ? 0.95 : 0.5));
                    draw_set_color(_rr_sel && _rc.focus ? make_color_rgb(40, 28, 48) : make_color_rgb(18, 16, 26));
                    draw_rectangle(_rc.x0 + 10, _rry, _rc.x1 - 10, _rry + 108, false);
                    draw_set_alpha(_rr_dis ? 0.35 : 1.0);
                    draw_set_color(_rr_sel ? make_color_rgb(190, 120, 210) : make_color_rgb(55, 48, 66));
                    draw_rectangle(_rc.x0 + 10, _rry, _rc.x1 - 10, _rry + 108, true);
                    var _rr_spr = reagent_icon_sprite(_rr.id);
                    if (_rr_spr >= 0) draw_sprite_stretched(_rr_spr, 0, _rc.x0 + 24, _rry + 22, 64, 64);
                    draw_set_font(ui_font(fnt_ui));
                    draw_set_color(make_color_rgb(150, 205, 160));
                    draw_text(_rc.x0 + 104, _rry + 16, _rr.name);
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(make_color_rgb(140, 150, 170));
                    draw_text(_rc.x0 + 104, _rry + 58, "Held: " + string(reagent_count(_rr.id)));
                    draw_set_alpha(1.0);
                }
            }
            // TRADE button below the columns (hidden while the checkout popup is
            // up so its hover-inject can't queue a stale re-arm).
            if (!(variable_instance_exists(_gc, "petra_rg_ck") && _gc.petra_rg_ck)) {
                ui_confirm_button(660, 816, 990, 882, "TRADE  [Enter]", make_color_rgb(120, 210, 130),
                    _pmx3, _pmy3, _ppress3, "petra:rgtrade");
            }
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(160, 140, 180));
            ui_draw_key_legend(960, 1032, "A/D: Column    W/S: Pick    Enter: Trade    R: Mode    Esc: Back");
            draw_set_halign(fa_left);

            // Checkout popup (M 07-27 standing rule) - armed by the Step.
            if (variable_instance_exists(_gc, "petra_rg_ck") && _gc.petra_rg_ck) {
                var _rg_gvd = _rg_cat[clamp(_rg_gv, 0, _rg_n - 1)];
                var _rg_gtd = _rg_cat[clamp(_rg_gt, 0, _rg_n - 1)];
                ui_draw_checkout_confirm("TRADE REAGENTS",
                    string(_rg_t.give) + "x " + _rg_gvd.name + "  ->  " + string(_rg_t.get) + "x " + _rg_gtd.name
                    + "\nPetra keeps the difference. This cannot be undone.",
                    "petra:rgok", "petra:rgcancel");
            }
            } else if (_pt_mode == 1) {
            // ================= RUNE BLUEPRINT MODE =================
            var _pr_inv  = variable_global_exists("rune_inventory") ? global.rune_inventory : [];
            var _pr_sel  = variable_instance_exists(_gc, "petra_rune_sel") ? _gc.petra_rune_sel : [];
            var _pr_n_s  = array_length(_pr_sel);
            var _tlx = 150, _tlw = 1350, _trh = 90, _trg = 4, _ty0 = 224;   // MUST mirror the gear list

            if (_gc.petra_rune_phase == 0) {
                if (_gc.shop_notification == "" && _gc.petra_trade_notification == "") {
                    draw_set_halign(fa_center);
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(make_color_rgb(150, 130, 170));
                    draw_text(960, 162, "Trade 5 runes of the SAME tier for a BLUEPRINT - a rune YOU choose, delivered after floor clears.");
                }
                // Pick-slot diamond pips, matching the gear-mode readout
                // (trade-tab cleanup, tour pass 08-15).
                draw_set_font(ui_font(fnt_ui_small));
                var _pr_tier = (_pr_n_s > 0 && _pr_sel[0] < array_length(_pr_inv)) ? _pr_inv[_pr_sel[0]].tier : -1;
                var _pr_cap = (_pr_n_s == 0) ? "Pick 5 runes of one tier"
                    : ((_pr_tier > 0) ? ("Tier " + rune_tier_roman(_pr_tier) + "   ("
                        + string(petra_rune_order_gold(_pr_tier)) + "g, "
                        + string(petra_rune_order_floors()) + " floors)") : "");
                var _pr_pip = 16, _pr_pgap = 14;
                var _pr_pips_w = 5 * _pr_pip + 4 * _pr_pgap;
                var _pr_w  = _pr_pips_w + 20 + string_width(_pr_cap);
                var _pr_x  = 960 - _pr_w / 2;
                var _pr_cy = 203;
                for (var _pr_i = 0; _pr_i < 5; _pr_i++) {
                    var _pr_xc = _pr_x + _pr_i * (_pr_pip + _pr_pgap) + _pr_pip / 2;
                    draw_set_color(make_color_rgb(110, 80, 120));
                    draw_triangle(_pr_xc - _pr_pip / 2 - 3, _pr_cy, _pr_xc, _pr_cy - _pr_pip / 2 - 3, _pr_xc + _pr_pip / 2 + 3, _pr_cy, false);
                    draw_triangle(_pr_xc - _pr_pip / 2 - 3, _pr_cy, _pr_xc, _pr_cy + _pr_pip / 2 + 3, _pr_xc + _pr_pip / 2 + 3, _pr_cy, false);
                    draw_set_color((_pr_i < _pr_n_s) ? _tt_accent : make_color_rgb(24, 20, 28));
                    draw_triangle(_pr_xc - _pr_pip / 2, _pr_cy, _pr_xc, _pr_cy - _pr_pip / 2, _pr_xc + _pr_pip / 2, _pr_cy, false);
                    draw_triangle(_pr_xc - _pr_pip / 2, _pr_cy, _pr_xc, _pr_cy + _pr_pip / 2, _pr_xc + _pr_pip / 2, _pr_cy, false);
                }
                draw_set_halign(fa_left);
                draw_set_color((_pr_n_s == 0) ? make_color_rgb(150, 140, 165) : _tt_accent);
                draw_text(_pr_x + _pr_pips_w + 20, _pr_cy - string_height("Ag") / 2, _pr_cap);
                draw_set_font(ui_font(fnt_ui));

                var _pr_n = array_length(_pr_inv);
                if (_pr_n == 0) {
                    draw_set_halign(fa_center);
                    draw_set_font(ui_font(fnt_ui));
                    draw_set_color(make_color_rgb(90, 100, 120));
                    draw_text(960, 520, "You hold no unsocketed runes to trade.");
                    draw_set_halign(fa_left);
                } else {
                    var _pr_scr = clamp(_gc.petra_rune_scroll, 0, max(0, _pr_n - 8));
                    var _pr_end = min(_pr_scr + 8, _pr_n);
                    for (var _pri = _pr_scr; _pri < _pr_end; _pri++) {
                        var _prn = _pr_inv[_pri];
                        var _iy  = _ty0 + (_pri - _pr_scr) * (_trh + _trg);
                        var _hl  = (_pri == _gc.petra_rune_cursor);
                        var _chosen = false;
                        for (var _pk = 0; _pk < _pr_n_s; _pk++) { if (_pr_sel[_pk] == _pri) { _chosen = true; break; } }
                        var _locked = (_pr_n_s > 0 && !_chosen && _pr_inv[_pr_sel[0]].tier != _prn.tier);
                        // Row tap = toggle (Step consumes the absolute index).
                        if (_ppress3 && _pmx3 >= _tlx && _pmx3 < _tlx + _tlw && _pmy3 >= _iy && _pmy3 < _iy + _trh) {
                            input_inject("petra:runerow" + string(_pri));
                        }
                        draw_set_alpha(_hl ? 1.0 : (_locked ? 0.32 : 0.7));
                        draw_set_color(_chosen ? make_color_rgb(34, 26, 44) : (_hl ? make_color_rgb(24, 20, 32) : make_color_rgb(14, 14, 22)));
                        draw_rectangle(_tlx, _iy, _tlx + _tlw, _iy + _trh, false);
                        draw_set_alpha(1.0);
                        draw_set_color(_chosen ? _tt_accent : (_hl ? make_color_rgb(120, 90, 140) : make_color_rgb(44, 40, 56)));
                        draw_rectangle(_tlx, _iy, _tlx + _tlw, _iy + _trh, true);
                        // Checkbox + rune entry (icon/name/effect) + tier tag.
                        var _cbx = _tlx + 18, _cby = _iy + (_trh - 42) / 2;
                        draw_set_color(_chosen ? _tt_accent : make_color_rgb(70, 66, 84));
                        draw_rectangle(_cbx, _cby, _cbx + 42, _cby + 42, true);
                        if (_chosen) draw_rectangle(_cbx + 9, _cby + 9, _cbx + 33, _cby + 33, false);
                        ui_draw_rune_entry(_cbx + 78, _iy + 27, _prn);
                        draw_set_halign(fa_right);
                        draw_set_font(ui_font(fnt_ui_small));
                        draw_set_color(make_color_rgb(150, 145, 165));
                        draw_text(_tlx + _tlw - 24, _iy + 30, "Tier " + rune_tier_roman(_prn.tier));
                        draw_set_halign(fa_left);
                    }
                    if (_pr_n > 8) {
                        draw_set_halign(fa_center);
                        draw_set_font(ui_font(fnt_ui_small));
                        draw_set_color(make_color_rgb(110, 100, 130));
                        draw_text(960, _ty0 + 8 * (_trh + _trg) + 2, "(" + string(_pr_scr + 1) + "-" + string(_pr_end) + " of " + string(_pr_n) + ")");
                        draw_set_halign(fa_left);
                        // Visible scroll bar (standing rule) - trade-tab cleanup 08-15.
                        ui_pb_scrollbar(1508, _ty0, _ty0 + 8 * (_trh + _trg) - _trg, _pr_n, 8, _pr_scr);
                    }
                }
                if (_pr_n_s == 5) {
                    ui_confirm_button(710, 976, 1210, 1024, "CHOOSE BLUEPRINT  [Space]",
                        make_color_rgb(120, 210, 130), _pmx3, _pmy3, _ppress3, "petra:runego");
                } else {
                    draw_set_halign(fa_center);
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(make_color_rgb(160, 140, 180));
                    ui_draw_key_legend(960, 1032, "W/S: Move    Enter: Pick    Space: Choose Blueprint    R: Gear Trade    Esc: Close");
                    draw_set_halign(fa_left);
                }
            } else {
                // ---- Phase 1: choose the blueprint rune ----
                var _bp_pool = rune_blueprint_pool();
                var _bp_n    = array_length(_bp_pool);
                var _bp_tier = (_pr_n_s > 0 && _pr_sel[0] < array_length(_pr_inv)) ? _pr_inv[_pr_sel[0]].tier : 1;
                if (_gc.shop_notification == "" && _gc.petra_trade_notification == "") {
                    draw_set_halign(fa_center);
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(make_color_rgb(150, 130, 170));
                    draw_text(960, 162, "Choose the blueprint - Petra delivers it at tier " + rune_tier_roman(_bp_tier)
                        + " for " + string(petra_rune_order_gold(_bp_tier)) + "g after "
                        + string(petra_rune_order_floors()) + " floor clears.");
                    draw_set_halign(fa_left);
                }
                var _bp_scr = clamp(_gc.petra_rune_result_scroll, 0, max(0, _bp_n - 8));
                var _bp_end = min(_bp_scr + 8, _bp_n);
                for (var _bpi = _bp_scr; _bpi < _bp_end; _bpi++) {
                    var _bpr = rune_make(_bp_pool[_bpi], _bp_tier);
                    var _iy2 = _ty0 + (_bpi - _bp_scr) * (_trh + _trg);
                    var _hl2 = (_bpi == _gc.petra_rune_result_cursor);
                    if (_ppress3 && _pmx3 >= _tlx && _pmx3 < _tlx + _tlw && _pmy3 >= _iy2 && _pmy3 < _iy2 + _trh) {
                        input_inject("petra:runeres" + string(_bpi));
                    }
                    draw_set_alpha(_hl2 ? 1.0 : 0.7);
                    draw_set_color(_hl2 ? make_color_rgb(24, 20, 32) : make_color_rgb(14, 14, 22));
                    draw_rectangle(_tlx, _iy2, _tlx + _tlw, _iy2 + _trh, false);
                    draw_set_alpha(1.0);
                    draw_set_color(_hl2 ? _tt_accent : make_color_rgb(44, 40, 56));
                    draw_rectangle(_tlx, _iy2, _tlx + _tlw, _iy2 + _trh, true);
                    ui_draw_rune_entry(_tlx + 30, _iy2 + 27, _bpr);
                    draw_set_halign(fa_right);
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(_hl2 ? make_color_rgb(150, 200, 255) : make_color_rgb(120, 130, 150));
                    draw_text(_tlx + _tlw - 24, _iy2 + 30, _hl2 ? "Enter: Order" : "");
                    draw_set_halign(fa_left);
                }
                if (_bp_n > 8) {
                    draw_set_halign(fa_center);
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(make_color_rgb(110, 100, 130));
                    draw_text(960, _ty0 + 8 * (_trh + _trg) + 2, "(" + string(_bp_scr + 1) + "-" + string(_bp_end) + " of " + string(_bp_n) + ")");
                    draw_set_halign(fa_left);
                    // Visible scroll bar (standing rule) - trade-tab cleanup 08-15.
                    ui_pb_scrollbar(1508, _ty0, _ty0 + 8 * (_trh + _trg) - _trg, _bp_n, 8, _bp_scr);
                }
                draw_set_halign(fa_center);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(160, 140, 180));
                ui_draw_key_legend(960, 1032, "W/S: Move    Enter: Order    Esc: Back");
                draw_set_halign(fa_left);

                // Checkout popup for the armed order (M 07-27 standing rule).
                if (_gc.petra_trade_confirm && _bp_n > 0) {
                    var _bp_sel_id  = _bp_pool[clamp(_gc.petra_rune_result_cursor, 0, _bp_n - 1)];
                    var _bp_sel_def = rune_get(_bp_sel_id);
                    ui_draw_checkout_confirm("PLACE BLUEPRINT ORDER",
                        "5 tier-" + rune_tier_roman(_bp_tier) + " runes melt + " + string(petra_rune_order_gold(_bp_tier)) + "g.\n"
                        + "Petra delivers " + ((_bp_sel_def != undefined) ? _bp_sel_def.name : _bp_sel_id) + " "
                        + rune_tier_roman(_bp_tier) + " after " + string(petra_rune_order_floors())
                        + " floor clears. Gold is not refunded.",
                        "petra:placebp", "petra:cancelbp");
                }
            }
            } else {
            // ================= GEAR TRADE MODE (original) =================

            // Instruction - yields its line while a notification is showing (BOTH
            // channels draw at y156: shop_notification "Collected: ..." AND
            // petra_trade_notification - the roll-bias line used to overlap this).
            if (_gc.shop_notification == "" && _gc.petra_trade_notification == "") {
                draw_set_halign(fa_center);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(150, 130, 170));
                draw_text(960, 162, "Choose 3 items of the SAME tier to trade up - their affixes are destroyed.");
            } else {
                draw_set_halign(fa_center);
            }

            // Chosen summary + output preview - drawn pick-slot diamond pips
            // (craft-wizard idiom; "Chosen: 0 / 3" keyboard-symbol strings read
            // as unfinished dev work - trade-tab cleanup, tour pass 08-15).
            draw_set_font(ui_font(fnt_ui_small));
            var _pt_cap;
            if (_sel_n == 0) {
                draw_set_color(make_color_rgb(150, 130, 170));
                _pt_cap = "Pick 3 items of one tier";
            } else {
                var _srung = petra_ladder_for(_sel_rar);
                var _outtxt = (_srung != undefined)
                    ? ("  ->  " + item_rarity_name(_srung.out_rarity) + "    "
                        + string(floor(_srung.gold * petra_gold_mult())) + "g, "
                        + string(max(1, _srung.cost_floors - petra_delivery_reduction())) + " floors"
                        + (_srung.req_awk > 0 ? (" (A" + string(_srung.req_awk) + "+)") : ""))
                    : "  (this tier can't be traded up)";
                draw_set_color(_tt_accent);
                _pt_cap = item_rarity_name(_sel_rar) + _outtxt;
            }
            var _pt_pip = 16, _pt_pgap = 14;
            var _pt_pips_w = 3 * _pt_pip + 2 * _pt_pgap;
            var _pt_w  = _pt_pips_w + 20 + string_width(_pt_cap);
            var _pt_x  = 960 - _pt_w / 2;
            var _pt_cy = 203;
            for (var _pt_i = 0; _pt_i < 3; _pt_i++) {
                var _pt_xc = _pt_x + _pt_i * (_pt_pip + _pt_pgap) + _pt_pip / 2;
                draw_set_color(make_color_rgb(110, 80, 120));
                draw_triangle(_pt_xc - _pt_pip / 2 - 3, _pt_cy, _pt_xc, _pt_cy - _pt_pip / 2 - 3, _pt_xc + _pt_pip / 2 + 3, _pt_cy, false);
                draw_triangle(_pt_xc - _pt_pip / 2 - 3, _pt_cy, _pt_xc, _pt_cy + _pt_pip / 2 + 3, _pt_xc + _pt_pip / 2 + 3, _pt_cy, false);
                draw_set_color((_pt_i < _sel_n) ? _tt_accent : make_color_rgb(24, 20, 28));
                draw_triangle(_pt_xc - _pt_pip / 2, _pt_cy, _pt_xc, _pt_cy - _pt_pip / 2, _pt_xc + _pt_pip / 2, _pt_cy, false);
                draw_triangle(_pt_xc - _pt_pip / 2, _pt_cy, _pt_xc, _pt_cy + _pt_pip / 2, _pt_xc + _pt_pip / 2, _pt_cy, false);
            }
            draw_set_halign(fa_left);
            draw_set_color((_sel_n == 0) ? make_color_rgb(150, 140, 165) : _tt_accent);
            draw_text(_pt_x + _pt_pips_w + 20, _pt_cy - string_height("Ag") / 2, _pt_cap);
            draw_set_font(ui_font(fnt_ui));

            // Stash list (windowed to 8 rows)
            var _stash_n = array_length(global.equipment_stash);
            if (_stash_n == 0) {
                draw_set_halign(fa_center);
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(make_color_rgb(90, 100, 120));
                draw_text(960, 520, "Your stash has no equipment to trade.");
                draw_set_halign(fa_left);
            } else {
                var _tlx = 150, _tlw = 1350, _trh = 90, _trg = 4, _ty0 = 224;   // narrowed for the sprite column
                var _scroll  = clamp(_gc.petra_trade_scroll, 0, max(0, _stash_n - 8));
                var _vis_end = min(_scroll + 8, _stash_n);
                for (var _i = _scroll; _i < _vis_end; _i++) {
                    var _it  = global.equipment_stash[_i];
                    var _iy  = _ty0 + (_i - _scroll) * (_trh + _trg);
                    var _hl  = (_i == _gc.petra_trade_cursor);
                    var _chosen = false;
                    for (var _k = 0; _k < _sel_n; _k++) { if (_sel[_k] == _i) { _chosen = true; break; } }
                    // Row tap = toggle (Step consumes the absolute index) - the gear
                    // list had no touch path (M 07-27 visual/interactivity pass).
                    if (_ppress3 && _pmx3 >= _tlx && _pmx3 < _tlx + _tlw && _pmy3 >= _iy && _pmy3 < _iy + _trh
                        && !_gc.petra_trade_confirm) {
                        input_inject("petra:gearrow" + string(_i));
                    }
                    var _irar = (is_struct(_it) && variable_struct_exists(_it, "rarity")) ? _it.rarity : 0;
                    var _locked = (_sel_n > 0 && !_chosen && _irar != _sel_rar) || (petra_ladder_for(_irar) == undefined);

                    draw_set_alpha(_hl ? 1.0 : (_locked ? 0.32 : 0.7));
                    draw_set_color(_chosen ? make_color_rgb(34, 26, 44) : (_hl ? make_color_rgb(24, 20, 32) : make_color_rgb(14, 14, 22)));
                    draw_rectangle(_tlx, _iy, _tlx + _tlw, _iy + _trh, false);
                    draw_set_alpha(1.0);
                    draw_set_color(_chosen ? _tt_accent : (_hl ? make_color_rgb(120, 90, 140) : make_color_rgb(44, 40, 56)));
                    draw_rectangle(_tlx, _iy, _tlx + _tlw, _iy + _trh, true);

                    // selection checkbox
                    var _cbx = _tlx + 18, _cby = _iy + (_trh - 42) / 2;
                    draw_set_color(_chosen ? _tt_accent : make_color_rgb(70, 66, 84));
                    draw_rectangle(_cbx, _cby, _cbx + 42, _cby + 42, true);
                    if (_chosen) { draw_rectangle(_cbx + 9, _cby + 9, _cbx + 33, _cby + 33, false); }

                    // icon + name + stat line (sell-tab idiom; M 07-09: the row showed
                    // only the name - the stats are exactly what the trade destroys)
                    if (is_struct(_it)) ui_draw_item_icon(_cbx + 60, _iy + 21, 48, _it);
                    var _iname = (is_struct(_it) && variable_struct_exists(_it, "name")) ? _it.name : "Item";
                    draw_set_font(ui_font(fnt_ui));
                    draw_set_color(item_rarity_color(_irar));
                    draw_text(_cbx + 126, _iy + 12, _iname);
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(c_white);
                    if (is_struct(_it)) ui_draw_stat_line_fit(_cbx + 126, _iy + 48, ui_item_stat_str(_it), (_tlx + _tlw - 120) - (_cbx + 126));
                    draw_set_halign(fa_right);
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(make_color_rgb(150, 145, 165));
                    draw_text(_tlx + _tlw - 24, _iy + 30, item_rarity_name(_irar));
                    draw_set_halign(fa_left);
                }
                if (_stash_n > 8) {
                    draw_set_halign(fa_center);
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(make_color_rgb(110, 100, 130));
                    draw_text(960, _ty0 + 8 * (_trh + _trg) + 2, "(" + string(_scroll + 1) + "-" + string(_vis_end) + " of " + string(_stash_n) + ")");
                    draw_set_halign(fa_left);
                    // Visible scroll bar (standing rule) - trade-tab cleanup 08-15.
                    ui_pb_scrollbar(1508, _ty0, _ty0 + 8 * (_trh + _trg) - _trg, _stash_n, 8, _scroll);
                }
            }

            // Hints - swap to a tappable PLACE ORDER button once 3 are chosen (the
            // roll-bias choice lives in the popup; M 07-08 redesign + 07-27 touch).
            if (_sel_n == 3 && !_gc.petra_trade_confirm) {
                ui_confirm_button(710, 976, 1210, 1024, "PLACE ORDER  [Space]",
                    make_color_rgb(120, 210, 130), _pmx3, _pmy3, _ppress3, "petra:placeopen");
            } else {
                draw_set_halign(fa_center);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(160, 140, 180));
                ui_draw_key_legend(960, 1032, "W/S: Move    Enter: Select    Space: Place Order    Q/E: Tab    U: Station    Esc: Close");
                draw_set_halign(fa_left);
            }

            // PLACE-ORDER POPUP: pick the roll treatment AFTER choosing the 3
            // inputs, then confirm. petra_trade_lever = the highlighted choice.
            if (_gc.petra_trade_confirm && _sel_n == 3 && _sel_rar >= 0) {
                var _rung3 = petra_ladder_for(_sel_rar);
                if (_rung3 != undefined) {
                    // Taller than the 07-08 popup (was 795) - the CONFIRM button row
                    // sits below the two lever rows (which end at y711).
                    var _bp_x0 = 480, _bp_y0 = 285, _bp_x1 = 1440, _bp_y1 = 850;
                    draw_set_alpha(0.72); draw_set_color(c_black);
                    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
                    draw_set_alpha(0.97); draw_set_color(make_color_rgb(24, 19, 32));
                    draw_rectangle(_bp_x0, _bp_y0, _bp_x1, _bp_y1, false);
                    draw_set_alpha(1.0); draw_set_color(_tt_accent);
                    draw_rectangle(_bp_x0, _bp_y0, _bp_x1, _bp_y1, true);
                    draw_set_halign(fa_center);
                    draw_set_font(ui_font(fnt_ui));
                    draw_set_color(make_color_rgb(255, 225, 150));
                    draw_text(960, _bp_y0 + 27, "PLACE TRADE ORDER");
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color(make_color_rgb(200, 190, 215));
                    var _bp_gold = floor(_rung3.gold * petra_gold_mult());
                    var _bp_fl   = max(1, _rung3.cost_floors - petra_delivery_reduction());
                    draw_text_ext(960, _bp_y0 + 78,
                        "3 " + item_rarity_name(_sel_rar) + "  ->  1 " + item_rarity_name(_rung3.out_rarity)
                        + "   -   " + string(_bp_gold) + "g, delivered in " + string(_bp_fl) + " floor" + (_bp_fl == 1 ? "" : "s")
                        + ((_rung3.req_awk > 0) ? ("  (A" + string(_rung3.req_awk) + "+)") : "")
                        + "\nThe chosen items' affixes are destroyed. Gold is not refunded.", 30, (_bp_x1 - _bp_x0) - 60);

                    // Two roll-treatment rows: Standard / Roll-bias (+dust). Tappable
                    // (petra:lever0/1); the Step consumes them while the popup is up.
                    for (var _bi = 0; _bi < 2; _bi++) {
                        var _b_on  = (_bi == 1);
                        var _b_sel = (_gc.petra_trade_lever == _b_on);
                        var _b_y   = _bp_y0 + 204 + _bi * 120;
                        if (_ppress3 && _pmx3 >= _bp_x0 + 45 && _pmx3 < _bp_x1 - 45 && _pmy3 >= _b_y && _pmy3 < _b_y + 102) {
                            input_inject("petra:lever" + string(_bi));
                        }
                        draw_set_alpha(_b_sel ? 1.0 : 0.55);
                        draw_set_color(_b_sel ? make_color_rgb(38, 30, 52) : make_color_rgb(18, 16, 26));
                        draw_rectangle(_bp_x0 + 45, _b_y, _bp_x1 - 45, _b_y + 102, false);
                        draw_set_alpha(1.0);
                        draw_set_color(_b_sel ? _tt_accent : make_color_rgb(60, 54, 74));
                        draw_rectangle(_bp_x0 + 45, _b_y, _bp_x1 - 45, _b_y + 102, true);
                        draw_set_font(ui_font(fnt_ui));
                        draw_set_color(_b_sel ? c_white : make_color_rgb(150, 145, 165));
                        draw_text(960, _b_y + 15, _b_on ? ("Roll-bias  (+" + string(_rung3.dust) + " Rune Dust)") : "Standard roll");
                        draw_set_font(ui_font(fnt_ui_small));
                        draw_set_color(make_color_rgb(140, 150, 175));
                        draw_text(960, _b_y + 57, _b_on ? "Petra weights the outcome toward better affixes." : "A fair roll - no extra cost.");
                    }
                    // Lever rows end at y711; legend then the button sit below them.
                    draw_set_color(make_color_rgb(160, 140, 180));
                    draw_set_halign(fa_center);
                    draw_set_font(ui_font(fnt_ui_small));
                    ui_draw_key_legend(960, _bp_y1 - 118, "W/S: Choose    Esc: Cancel");
                    draw_set_halign(fa_left);
                    ui_confirm_button(_bp_x0 + 280, _bp_y1 - 84, _bp_x1 - 280, _bp_y1 - 24, "CONFIRM ORDER  [Enter]",
                        make_color_rgb(120, 210, 130), _pmx3, _pmy3, _ppress3, "petra:place");
                }
            }
            }   // end gear-mode branch (rune blueprint mode is the sibling above)
        }

        // Notification (shared by both states)
        if (_gc.petra_trade_notification != "") {
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui));
            var _ntc = make_color_rgb(100, 220, 120);
            if (string_pos("Need", _gc.petra_trade_notification) > 0) _ntc = c_red;
            else if (string_pos("Cancel the order", _gc.petra_trade_notification) > 0
                  || string_pos("Affixes destroyed", _gc.petra_trade_notification) > 0) _ntc = c_yellow;
            draw_set_color(_ntc);
            draw_text_ext(960, 156, _gc.petra_trade_notification, 30, 1500);
            draw_set_halign(fa_left);
        }

        // --- ORDER REVEAL popup (M 07-28: examine what was traded and what came
        // back, instead of the yield vanishing into a stash stack). Drawn over
        // whichever trade state shows; Step is modal while it's open.
        if (variable_instance_exists(_gc, "petra_reveal_open") && _gc.petra_reveal_open
            && variable_global_exists("petra_reveal") && global.petra_reveal != undefined) {
            var _rv = global.petra_reveal;
            draw_set_alpha(0.75); draw_set_color(c_black);
            draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
            draw_set_alpha(0.97); draw_set_color(make_color_rgb(20, 16, 28));
            draw_rectangle(420, 240, 1500, 900, false);
            draw_set_alpha(1.0); draw_set_color(_tt_accent);
            draw_rectangle(420, 240, 1500, 900, true);
            draw_rectangle(426, 246, 1494, 894, true);
            draw_set_halign(fa_center);
            draw_set_font(fnt_ui_title);
            draw_set_color(make_color_rgb(255, 225, 150));
            draw_text(960, 268, "ORDER FULFILLED");

            // TRADED (left) -> RECEIVED (right), the Dorn reveal idiom.
            var _rv_in = string(_rv.input_count) + " x " + (_rv.is_rune
                ? ("tier-" + rune_tier_roman(_rv.input_tier) + " runes")
                : (item_rarity_name(_rv.input_tier) + " items"));
            ui_reforge_card(470, 348, 900, 800, "TRADED", make_color_rgb(120, 118, 130),
                _rv_in, _rv.is_rune ? 3 : _rv.input_tier,
                _rv.dust_bias ? "melted down  -  roll-biased order" : "melted down");
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(200, 190, 170));
            draw_text(960, 560, "->");
            if (_rv.is_rune) {
                ui_reforge_card(1020, 348, 1450, 800, "RECEIVED", make_color_rgb(120, 210, 130),
                    rune_title(_rv.rune), 3, rune_effect(_rv.rune));
            } else {
                var _rv_rar = variable_struct_exists(_rv.item, "rarity") ? clamp(_rv.item.rarity, 0, 4) : 0;
                ui_reforge_card(1020, 348, 1450, 800, "RECEIVED", make_color_rgb(120, 210, 130),
                    _rv.item.name, _rv_rar, ui_item_stat_str(_rv.item));
                // Codex splash (when authored) sits in the card's lower half.
                var _rv_art = item_splash_sprite(item_base_name(_rv.item));
                if (_rv_art != -1 && sprite_exists(_rv_art)) {
                    draw_sprite_ext(_rv_art, 0, 1235 - 64, 648, 1, 1, 0, c_white, 1.0);
                }
            }
            ui_confirm_button(795, 820, 1125, 880, "DONE  [Enter]", make_color_rgb(120, 210, 130),
                device_mouse_x_to_gui(0), device_mouse_y_to_gui(0), mouse_check_button_pressed(mb_left), "petra:revealdone");
            draw_set_halign(fa_left);
        }

        ui_draw_checkout_vfx();   // Gigapack collect burst (fired at collect)
        draw_set_font(-1);
        return;
    }

    // =========================================================================
    // BUY TAB - unchanged buy content
    // =========================================================================

    // -------------------------------------------------------------------------
    // PETRA - consumables (+ optional special) then pet feeds; windowed if long.
    // -------------------------------------------------------------------------
    if (_is_petra) {
        var _buy_list = petra_buy_list();
        var _buy_n    = array_length(_buy_list);
        // COMPACT rows (87px vs Dorn's 117) so Petra's whole typical stock - 4
        // consumables + special + 4 feeds - fits on ONE page with no scrolling
        // (M 2026-07-03: the "more wares below" hint read as clutter). The window
        // only engages past 8 entries (several discovered preferred feeds).
        var _prh      = 84;
        var _prgap    = 6;
        var _max_vis  = 9;    // 4 consumables + limited special + 3 basics + premium all fit
        // Edge-triggered window: driven by the persistent buy_scroll the Step handler
        // advances only when the cursor reaches an edge (so the selector moves, not the list).
        var _win0     = clamp(_gc.buy_scroll, 0, max(0, _buy_n - _max_vis));
        var _win1     = min(_buy_n, _win0 + _max_vis);

        for (var _ri = _win0; _ri < _win1; _ri++) {
            var _entry   = _buy_list[_ri];
            var _it      = _entry.it;
            var _price   = _entry.price;
            var _is_feed = (_entry.kind == "feed");
            var _is_reag = (_entry.kind == "reagent");   // limited RNG lots (08-26)
            var _ry      = _ry0 + (_ri - _win0) * (_prh + _prgap);
            var _is_sel  = (_ri == _gc.shop_index);

            draw_set_alpha(_is_sel ? 1.0 : 0.55);
            draw_set_color(_is_sel ? make_color_rgb(16, 42, 50) : make_color_rgb(14, 18, 28));
            draw_rectangle(_rx0, _ry, _rx0 + _rw, _ry + _prh, false);
            draw_set_alpha(1.0);
            // Feed rows take a warm accent to read as a different category from
            // consumables; reagent lots go dungeon-green.
            var _acc_on  = _is_reag ? make_color_rgb(150, 205, 160) : (_is_feed ? make_color_rgb(150, 200, 110) : make_color_rgb(55, 170, 170));
            var _acc_off = _is_reag ? make_color_rgb(60, 95, 70)    : (_is_feed ? make_color_rgb(70, 96, 50)     : make_color_rgb(38, 75, 85));
            draw_set_color(_is_sel ? _acc_on : _acc_off);
            draw_rectangle(_rx0, _ry, _rx0 + _rw, _ry + _prh, true);

            if (_is_reag) {
                var _rgi = reagent_icon_sprite(_it.id);
                if (_rgi >= 0) draw_sprite_stretched(_rgi, 0, _rx0 + 15, _ry + 14, 58, 58);
            } else if (_is_feed) {
                // Feed icon (falls back to a +N growth badge if the sprite isn't imported).
                var _fic = pet_feed_icon(_it.id);
                if (_fic >= 0) {
                    var _fis = 58 / max(1, max(sprite_get_width(_fic), sprite_get_height(_fic)));
                    draw_sprite_ext(_fic, 0, _rx0 + 44, _ry + 43, _fis, _fis, 0, c_white, 1);
                } else {
                    // Treats are bond-only (growth 0) - badge says "+1 bond", not "+0 grow" (#4).
                    var _f_is_treat = variable_struct_exists(_it, "bond") && _it.bond > 0;
                    draw_set_color(make_color_rgb(30, 40, 24));
                    draw_rectangle(_rx0 + 15, _ry + 14, _rx0 + 73, _ry + 72, false);
                    draw_set_color(make_color_rgb(150, 200, 110));
                    draw_rectangle(_rx0 + 15, _ry + 14, _rx0 + 73, _ry + 72, true);
                    draw_set_halign(fa_center); draw_set_font(ui_font(fnt_ui));
                    draw_text(_rx0 + 44, _ry + 18, "+" + string(_f_is_treat ? _it.bond : _it.growth));
                    draw_set_font(ui_font(fnt_ui_small)); draw_set_color(make_color_rgb(120, 150, 100));
                    draw_text(_rx0 + 44, _ry + 44, _f_is_treat ? "bond" : "grow");
                    draw_set_halign(fa_left);
                }
            } else {
                ui_draw_consumable_icon(_rx0 + 15, _ry + 14, 58, _it);
            }

            // Name
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_is_reag ? make_color_rgb(150, 205, 160)
                         : (_is_feed ? make_color_rgb(180, 220, 130) : ui_consumable_name_color(_it)));   // Genie Lamp golds
            draw_text(_rx0 + 90, _ry + 9, _it.name);

            // Description / blurb
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(130, 160, 170));
            draw_text(_rx0 + 90, _ry + 49, ui_sentence((_is_feed || _is_reag) ? _it.blurb : _it.description));

            draw_set_halign(fa_right);
            if (_is_reag) {
                // Limited-lot badge + held count (top-right, reagent-green).
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(make_color_rgb(255, 155, 30));
                draw_text(_rx0 + _rw - 225, _ry + 9, "[LIMITED - " + string(_entry.qty) + " left]");
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(150, 205, 160));
                draw_text(_rx0 + _rw - 24, _ry + 9, "Held: " + string(reagent_count(_it.id)));
            } else if (_is_feed) {
                // Owned-in-pouch count (top-right).
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(150, 200, 140));
                draw_text(_rx0 + _rw - 24, _ry + 9, "Owned: " + string(pet_feed_pouch_count(_it.id)));
            } else if (_entry.special) {
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(make_color_rgb(255, 155, 30));
                draw_text(_rx0 + _rw - 225, _ry + 9, "[LIMITED - " + string(global.petra_special_qty) + " left]");
            }

            // Price (right-aligned)
            var _can_afford = (global.gold >= _price);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_can_afford ? c_yellow : make_color_rgb(180, 80, 80));
            draw_text(_rx0 + _rw - 24, _ry + 47, string(_price) + "g");
            draw_set_halign(fa_left);
        }

        // Position readout (replaces "N more above/below") - shows the selected row's
        // place in the full list, e.g. "12 / 20". Only when the list overflows one page.
        if (_buy_n > _max_vis) {
            draw_set_halign(fa_center); draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(120, 160, 160));
            // Touch: clamp the readout above the ACTIONS chip (top ~y1005) so it isn't covered.
            var _sm_below_y = _ry0 + _max_vis * (_prh + _prgap) - 4;
            if (input_device() == 2) _sm_below_y = min(_sm_below_y, 992);
            draw_text(_rx0 + _rw / 2, _sm_below_y, string(_gc.shop_index + 1) + " / " + string(_buy_n));
            draw_set_halign(fa_left);
        }

    // -------------------------------------------------------------------------
    // DORN - rotating gear list; sold entries appear greyed with SOLD tag
    // -------------------------------------------------------------------------
    } else {
        var _dorn_count = array_length(global.dorn_stock);

        for (var _ri = 0; _ri < _dorn_count; _ri++) {
            var _ry      = _ry0 + _ri * (_rh + _rgap);
            var _entry   = global.dorn_stock[_ri];
            var _is_sold = _entry.sold;
            var _is_sel  = (!_is_sold && _ri == _gc.shop_index);

            draw_set_alpha(_is_sold ? 0.28 : (_is_sel ? 1.0 : 0.55));
            draw_set_color(_is_sel ? make_color_rgb(38, 28, 10) : make_color_rgb(14, 18, 28));
            draw_rectangle(_rx0, _ry, _rx0 + _rw, _ry + _rh, false);
            draw_set_alpha(1.0);
            draw_set_color(_is_sold ? make_color_rgb(45, 45, 45)
                         : (_is_sel ? make_color_rgb(195, 135, 38) : make_color_rgb(75, 58, 28)));
            draw_rectangle(_rx0, _ry, _rx0 + _rw, _ry + _rh, true);

            if (_is_sold) {
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(make_color_rgb(65, 65, 65));
                draw_text(_rx0 + 24, _ry + 15, _entry.item.name);
                draw_set_halign(fa_right);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(75, 75, 75));
                draw_text(_rx0 + _rw - 24, _ry + 42, "SOLD");
                draw_set_halign(fa_left);
            } else {
                var _rcol = item_rarity_color(_entry.item.rarity);
                // Icon badge
                ui_draw_item_icon(_rx0 + 15, _ry + 12, 48, _entry.item);
                // Name + rarity right-aligned
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(_rcol);
                draw_text(_rx0 + 75, _ry + 12, _entry.item.name);
                // Class restriction tag appended after the name (gold = your class can
                // use it, red = locked to another). Standardized with the loadout tooltip.
                var _dorn_cr = variable_struct_exists(_entry.item, "class_req") ? _entry.item.class_req : -1;
                if (_dorn_cr != -1) {
                    var _dorn_cr_names = ["Arcanist", "Bloodwarden", "Shadowstrider"];
                    var _dorn_my_cl    = variable_global_exists("chosen_class") ? global.chosen_class : -1;
                    // Measure the name while fnt_ui (its draw font) is still set -
                    // measuring after the switch to fnt_ui_small landed the tag
                    // mid-name on long items ("Vaultstone Wand of Gre[Arcanist...").
                    var _dorn_name_w = string_width(_entry.item.name);
                    draw_set_font(ui_font(fnt_ui_small));
                    draw_set_color((_dorn_cr == _dorn_my_cl) ? make_color_rgb(210, 175, 90) : make_color_rgb(225, 80, 80));
                    draw_text(_rx0 + 75 + _dorn_name_w + 18, _ry + 15,
                        "[" + _dorn_cr_names[clamp(_dorn_cr, 0, 2)] + " only]");
                    draw_set_font(ui_font(fnt_ui));
                }
                draw_set_halign(fa_right);
                draw_set_font(ui_font(fnt_ui_small));
                draw_text(_rx0 + _rw - 24, _ry + 12, "[" + item_rarity_name(_entry.item.rarity) + "]");
                draw_set_halign(fa_left);
                // Stat string. Long rare/legendary affix lists used to run into the
                // right-aligned price/type column (M 07-17) - shrink-to-fit like the
                // picker/stash lists, reserving room for the Req tag + the price.
                draw_set_color(c_white);
                var _dstat    = ui_item_stat_str(_entry.item);
                var _dreq     = item_stat_requirement(_entry.item);
                var _has_req   = (_dreq.value > 0 && _dreq.stat != "");
                var _req_str   = _has_req ? ("Req " + string(_dreq.value) + " " + _dreq.stat) : "";
                var _req_w     = _has_req ? string_width(_req_str) : 0;
                var _stat_max  = (_rw - 140 - 75) - (_has_req ? (_req_w + 27) : 0);   // keep clear of the price
                ui_draw_stat_line_fit(_rx0 + 75, _ry + 45, _dstat, _stat_max);
                // Stat requirement appended, red if the current class can't meet it (hard-blocked).
                if (_has_req) {
                    draw_set_color((player_base_stat(_dreq.stat) >= _dreq.value)
                        ? make_color_rgb(110, 170, 110) : make_color_rgb(225, 80, 80));
                    draw_text(_rx0 + 75 + min(string_width(_dstat), _stat_max) + 27, _ry + 45, _req_str);
                }
                // Flavor or unique below stats (shrink-to-fit to clear the right-aligned slot label).
                var _desc_max = (_rw - 180) - 75;
                if (variable_struct_exists(_entry.item, "unique_desc") && _entry.item.unique_desc != "") {
                    draw_set_color(make_color_rgb(255, 200, 50));
                    ui_draw_stat_line_fit(_rx0 + 75, _ry + 75, _entry.item.unique_desc, _desc_max);
                } else if (_entry.item.effect_desc != "") {
                    draw_set_color(make_color_rgb(95, 105, 130));
                    ui_draw_stat_line_fit(_rx0 + 75, _ry + 75, _entry.item.effect_desc, _desc_max);
                }
                // Slot label right-aligned (so buyers know wep/arm/boot at a glance)
                if (variable_struct_exists(_entry.item, "slot")) {
                    draw_set_halign(fa_right);
                    draw_set_color(make_color_rgb(120, 135, 160));
                    draw_text(_rx0 + _rw - 24, _ry + 75, item_slot_label(_entry.item.slot));
                    draw_set_halign(fa_left);
                }
                // Price right-aligned (CHA-discounted)
                var _eprice     = cha_price(_entry.price);
                var _can_afford = (global.gold >= _eprice);
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(_can_afford ? c_yellow : make_color_rgb(180, 80, 80));
                draw_set_halign(fa_right);
                draw_text(_rx0 + _rw - 24, _ry + 45, string(_eprice) + "g");
                draw_set_halign(fa_left);
            }
        }
    }

    // Buy tab footer (raised to clear the bottom rim band). Dorn's affix rework now
    // lives on its own REFORGE tab (Q/E, or the [R] shortcut) - nudge toward it.
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    if (!_is_petra && input_device() != 2) {
        draw_set_color(make_color_rgb(220, 185, 110));
        draw_text_outline(960, 993, "[R] or Q/E -> REFORGE tab: rework an item's affixes with your ingots");
    }
    draw_set_color(make_color_rgb(75, 85, 105));
    ui_draw_key_legend(960, 1026, "W/S: Navigate   Q/E: Tab   Enter: Buy   F: Gift   U: Station   Esc: Close     (purchases go to your stash)");
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_dorn_temper(_gc)
// TEMPER tab (Dorn only; shop_tab == 3). Moved off Maren's forge menu 08-08:
// working rough metal toward its finish is smith work, not rune work.
//
// LEFT  = every piece below 100% quality, as shared option rows (name in its
//         rarity colour, live stat line beneath, step fee and quality tag right).
// RIGHT = a live BEFORE/AFTER preview of the step being offered, built by
//         running item_compare_rows against a hypothetical +10% copy. M's report
//         was "i spent the resources and they stayed at the % they were at and
//         gained no improvements" - showing the delta BEFORE the purchase (and
//         again in the reveal popup after) is the answer to that.
//
// Frame, tab bar, title, gold, bond and the Dorn sprite are drawn by
// ui_draw_shop_overlay before this runs. Input lives in the gc Step handler and
// its row geometry MUST mirror this one: 7 visible, pitch 90, top y255.
// Bands: rows 255-879, preview 255-890, confirm bar 906-960, legend 1026.
// ---------------------------------------------------------------------------
function ui_draw_dorn_temper(_gc) {
    // The tab can be entered by a CLICK inside another tab's input block, which
    // exits before this tab's Step handler runs - so Draw can legitimately be the
    // first code to touch these. Create them here rather than trusting Step order.
    if (!variable_instance_exists(_gc, "temper_index"))  _gc.temper_index  = 0;
    if (!variable_instance_exists(_gc, "temper_scroll")) _gc.temper_scroll = 0;
    var _list = item_picker_candidates_temperable();
    var _n    = array_length(_list);
    var _gold = make_color_rgb(200, 170, 110);

    draw_set_halign(fa_left); draw_set_valign(fa_top);

    if (_n == 0) {
        draw_set_font(ui_font(fnt_ui));
        draw_set_halign(fa_center);
        draw_set_color(make_color_rgb(150, 156, 175));
        draw_text(960, 470, "Nothing to temper.");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(115, 120, 140));
        draw_text(960, 520, "Every piece you carry is already at its full quality - or still DORMANT,");
        draw_text(960, 552, "and a sleeping legendary must be woken at Maren's Runesmithing before it can be finished.");
        draw_set_color(make_color_rgb(80, 90, 110));
        ui_draw_key_legend(960, 1026, "Q/E: Switch Tab   Esc: Close");
        draw_set_halign(fa_left);
        return;
    }

    var _vis  = 7;
    var _win0 = clamp(_gc.temper_scroll, 0, max(0, _n - _vis));
    var _win1 = min(_n, _win0 + _vis);
    var _sel  = clamp(_gc.temper_index, 0, _n - 1);
    if (!variable_global_exists("rune_dust")) global.rune_dust = 0;

    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(140, 146, 168));
    draw_text(60, 219, "Rough gear - each step works a piece +" + string((npc_rank("dorn") >= 2) ? 12 : 10) + "% toward its true potential:");

    for (var _i = _win0; _i < _win1; _i++) {
        var _ry  = 255 + (_i - _win0) * 90;
        var _it  = _list[_i].item;
        var _fee = temper_fee(_it);
        var _can = (global.gold >= _fee.gold && global.rune_dust >= _fee.dust);
        ui_draw_option_row(60, _ry, 900, _ry + 84, {
            // Worn gear says so (M 08-11: EQUIPPED at every NPC function).
            title:   _it.name + (item_is_equipped(_it) ? "   [EQUIPPED]" : ""),
            body:    ui_item_stat_str(_it),
            sel:     (_i == _sel),
            gold:    _fee.gold,
            dust:    _fee.dust,
            afford:  _can,
            accent:  item_rarity_color(variable_struct_exists(_it, "rarity") ? _it.rarity : 0),
            // The stat line already ends in "[Quality N%]" - repeating it here was
            // pure noise. Show what the step BUYS instead (M 08-08).
            tag:     string(_it.quality) + "% -> " + string(min(100, _it.quality + ((npc_rank("dorn") >= 2) ? 12 : 10))) + "%",
            tag_col: make_color_rgb(150, 158, 182)
        });
    }
    // Scroll position, so a long stash doesn't hide how much is left below.
    if (_n > _vis) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(105, 112, 132));
        draw_text(60, 888, "Showing " + string(_win0 + 1) + "-" + string(_win1) + " of " + string(_n));
    }

    // ---- preview panel ----
    var _px1 = 940, _px2 = 1860, _py1 = 255, _py2 = 890;
    draw_set_color(make_color_rgb(15, 16, 24));
    draw_rectangle(_px1, _py1, _px2, _py2, false);
    draw_set_color(make_color_rgb(52, 50, 44));
    draw_rectangle(_px1, _py1, _px2, _py2, true);

    var _it2  = _list[_sel].item;
    var _fee2 = temper_fee(_it2);
    var _can2 = (global.gold >= _fee2.gold && global.rune_dust >= _fee2.dust);
    // The hypothetical next step: a copy taken +10%, never the real item.
    var _next = item_shallow_copy(_it2);
    _next.quality = min(100, _it2.quality + ((npc_rank("dorn") >= 2) ? 12 : 10));

    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(140, 132, 112));
    draw_text(_px1 + 30, _py1 + 20, "IF DORN WORKS THIS PIECE");
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(item_rarity_color(variable_struct_exists(_it2, "rarity") ? _it2.rarity : 0));
    draw_text(_px1 + 30, _py1 + 52, ui_truncate(_it2.name, 860));
    draw_set_color(make_color_rgb(48, 50, 66));
    draw_line(_px1 + 30, _py1 + 100, _px2 - 30, _py1 + 100);

    var _rows = item_compare_rows(_it2, _next);
    var _rn   = array_length(_rows);
    var _lx = _px1 + 30, _av = 1362, _bv = 1438, _mid = 1400;
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(118, 124, 148));
    draw_set_halign(fa_right); draw_text(_av, _py1 + 116, "NOW");
    draw_set_halign(fa_left);  draw_text(_bv, _py1 + 116, "AFTER");
    draw_set_color(make_color_rgb(48, 50, 66));
    draw_line(_lx, _py1 + 148, _px2 - 30, _py1 + 148);

    var _ry2 = _py1 + 162;
    for (var _r = 0; _r < _rn; _r++) {
        var _rw = _rows[_r];
        draw_set_halign(fa_left);
        draw_set_color(make_color_rgb(138, 146, 170));
        draw_text(_lx, _ry2, ui_truncate(_rw.label, 200));
        draw_set_halign(fa_right);
        draw_set_color(make_color_rgb(142, 148, 166));
        draw_text(_av, _ry2, ui_truncate(_rw.a, 180));
        draw_set_halign(fa_center);
        draw_set_color(make_color_rgb(88, 94, 116));
        draw_text(_mid, _ry2, "->");
        draw_set_halign(fa_left);
        draw_set_color((_rw.better > 0) ? make_color_rgb(120, 210, 130)
                    : ((_rw.better < 0) ? make_color_rgb(214, 112, 112)
                                        : make_color_rgb(215, 220, 232)));
        draw_text(_bv, _ry2, ui_truncate(_rw.b, 400));
        _ry2 += 33;
    }

    // Honest footnote. A small roll genuinely cannot move its own stat (rounding),
    // and pretending otherwise is what made tempering feel broken - so say where
    // the gain actually lands instead.
    draw_set_halign(fa_left);
    draw_set_color(make_color_rgb(120, 126, 146));
    draw_text_ext(_lx, min(_ry2 + 24, _py2 - 96),
        "FINISH is quality's own reward: every completed 10% adds flat max HP, scaled by the piece's rarity. "
        + "A small stat roll may not change on its own - the finish always does.", 28, (_px2 - 30) - _lx);

    // ---- confirm bar (band 906-960) ----
    draw_set_color(_can2 ? merge_color(make_color_rgb(18, 17, 26), _gold, 0.18) : make_color_rgb(22, 16, 16));
    draw_rectangle(940, 906, 1860, 960, false);
    draw_set_color(_can2 ? _gold : make_color_rgb(120, 66, 66));
    draw_rectangle(940, 906, 1860, 960, true);
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_can2 ? _gold : make_color_rgb(206, 116, 116));
    draw_text(1400, 916, _can2
        ? ("Temper to " + string(_next.quality) + "%   -   " + string(_fee2.gold) + "g + " + string(_fee2.dust) + " dust")
        : ("Need " + string(_fee2.gold) + "g + " + string(_fee2.dust) + " dust"));

    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(80, 90, 110));
    ui_draw_key_legend(960, 1026, "W/S: Choose a piece   Enter: Temper it   Q/E: Switch Tab   Esc: Close");
    draw_set_halign(fa_left);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_dorn_reforge(_gc)
// REFORGE tab (Dorn only; shop_tab == 2). LEFT panel = the player's ingot hoard by
// rarity tier; RIGHT panel = every affix-bearing item they can rework, each row
// enterable only when they own an ingot of that item's tier OR higher. The gothic
// frame, tab bar, title, gold, bond and Dorn sprite are already drawn by
// ui_draw_shop_overlay before this runs; the rework result uses shop_notification
// (drawn at y156 by the shared block). Input/rework logic is in the gc Step handler.
// ---------------------------------------------------------------------------
function ui_draw_dorn_reforge(_gc) {
    reforge_ingots_ensure();

    // THE LEGENDARY FORGE overlay (M locked 07-28) owns the tab while open.
    if (variable_instance_exists(_gc, "forge_open") && _gc.forge_open) {
        ui_draw_legendary_forge(_gc);
        return;
    }

    // PATTERN BOOK overlays (08-11): the craft wizard and the book browser own
    // the tab while open (forge idiom). The smelt study popup draws ON TOP of
    // the tab instead - see the end of this function.
    if (variable_instance_exists(_gc, "pb_craft_open") && _gc.pb_craft_open) {
        ui_draw_pattern_craft(_gc);
        // The craft checkout rides the shared dorn_ck popup - keep it topmost.
        ui_draw_checkout_vfx();
        if (variable_instance_exists(_gc, "dorn_ck_open") && _gc.dorn_ck_open) {
            ui_draw_checkout_confirm(_gc.dorn_ck_title, _gc.dorn_ck_body, "dorn:ok", "dorn:cancel");
        }
        return;
    }
    if (variable_instance_exists(_gc, "pb_book_open") && _gc.pb_book_open) {
        ui_draw_pattern_book(_gc);
        return;
    }

    // Two-step rework screen (stage machine lives in the gc Step handler): while a
    // rework is being confirmed / forged / revealed, it replaces both panels.
    if (variable_instance_exists(_gc, "reforge_stage") && _gc.reforge_stage > 0
        && _gc.reforge_target != undefined) {
        ui_draw_reforge_confirm(_gc);
        return;
    }

    var _copper   = make_color_rgb(210, 140, 70);
    var _panel_bg = make_color_rgb(16, 16, 22);
    var _panel_ln = make_color_rgb(70, 55, 40);

    // ---- LEFT PANEL: YOUR INGOTS ------------------------------------------
    var _lx0 = 150, _lx1 = 600, _ly0 = 189, _ly1 = 900;
    draw_set_color(_panel_bg); draw_rectangle(_lx0, _ly0, _lx1, _ly1, false);
    draw_set_color(_panel_ln); draw_rectangle(_lx0, _ly0, _lx1, _ly1, true);
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_copper);
    draw_text((_lx0 + _lx1) / 2, _ly0 + 15, "YOUR INGOTS");

    // 07-29 layout fix (M screenshot): the old 96px rows ran the Epic/Legendary
    // tiers straight into the LEGENDARY FORGE block (the Legendary row was drawn
    // UNDER the buttons and read as missing). Compressed rows + the forge block
    // pushed below the full 5-row list; a FUSE verb joins the panel.
    var _tier_names = ["Common", "Uncommon", "Rare", "Epic", "Legendary"];
    for (var _t = 0; _t < 5; _t++) {
        var _cnt = global.reforge_ingots[_t];
        var _has = (_cnt > 0);
        var _iry = _ly0 + 54 + _t * 70;
        draw_set_alpha(_has ? 1.0 : 0.32);
        draw_sprite_stretched(reforge_ingot_sprite(_t), 0, _lx0 + 30, _iry, 52, 52);
        draw_set_alpha(1.0);
        draw_set_halign(fa_left);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_has ? item_rarity_color(_t) : make_color_rgb(88, 88, 100));
        draw_text(_lx0 + 100, _iry + 12, _tier_names[_t]);
        draw_set_halign(fa_right);
        draw_set_color(_has ? c_white : make_color_rgb(78, 78, 90));
        draw_text(_lx1 - 24, _iry + 12, "x" + string(_cnt));
    }
    draw_set_halign(fa_left);

    var _dfx = device_mouse_x_to_gui(0), _dfy = device_mouse_y_to_gui(0);
    var _dfp = mouse_check_button_pressed(mb_left);

    // Fuse verb (M 07-29): 3 same-tier ingots -> 1 of the next tier up.
    var _cmb_t = reforge_combine_tier();
    ui_confirm_button(_lx0 + 24, 610, _lx1 - 24, 654,
        (_cmb_t >= 0) ? ("Fuse 3 " + _tier_names[_cmb_t] + " -> 1 " + _tier_names[_cmb_t + 1] + "  [C]")
                      : "Fuse 3 same-tier -> 1  [C]",
        (_cmb_t >= 0) ? make_color_rgb(190, 160, 90) : make_color_rgb(90, 90, 105),
        _dfx, _dfy, _dfp, "dorn:combine");

    // ---- THE LEGENDARY FORGE (M locked 07-28): component readout + verbs ----
    forge_components_ensure();
    draw_set_color(_panel_ln);
    draw_line(_lx0 + 18, 668, _lx1 - 18, 668);
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(255, 225, 150));
    draw_text((_lx0 + _lx1) / 2, 678, "THE LEGENDARY FORGE");
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(170, 160, 190));
    // Component readout + verb (08-11 reword, M: "what mithril frame? theres no
    // mention of this elsewhere" - the bare "Strike Frame" button gave no hint
    // it was a forge PART or that Dorn's part is called the Mythril Frame).
    draw_text((_lx0 + _lx1) / 2, 712, "Parts held:  Frame x" + string(global.forge_comp_frame)
        + "   Core x" + string(global.forge_comp_core)
        + "   Quint. x" + string(global.forge_comp_quint));
    draw_set_halign(fa_left);
    ui_confirm_button(_lx0 + 24, 740, _lx1 - 24, 784, "Forge part: Mythril Frame  [G]",
        make_color_rgb(210, 140, 70), _dfx, _dfy, _dfp, "dorn:frame");
    ui_confirm_button(_lx0 + 24, 792, _lx1 - 24, 836, forge_components_ready() ? "FORGE  [V]" : "Forge - needs all 3  [V]",
        forge_components_ready() ? make_color_rgb(120, 210, 130) : make_color_rgb(90, 90, 105), _dfx, _dfy, _dfp, "dorn:forge");

    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 135, 110));
    draw_text_ext(_lx0 + 24, _ly1 - 56, "A higher-tier ingot reworks its own tier or below - a Legendary ingot works on anything.", 24, (_lx1 - _lx0) - 48);

    // ---- RIGHT PANEL: REWORK GEAR -----------------------------------------
    var _rx0 = 630, _rx1 = 1500, _ry0 = 189, _ry1 = 900;
    draw_set_color(_panel_bg); draw_rectangle(_rx0, _ry0, _rx1, _ry1, false);
    draw_set_color(_panel_ln); draw_rectangle(_rx0, _ry0, _rx1, _ry1, true);
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_copper);
    draw_text((_rx0 + _rx1) / 2, _ry0 + 15, "REWORK GEAR");
    draw_set_halign(fa_left);

    var _list = item_picker_candidates_affixed();
    var _n    = array_length(_list);
    if (_n == 0) {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(120, 120, 135));
        draw_text_ext((_rx0 + _rx1) / 2, _ry0 + 240,
            "No affix-bearing gear to rework.\n(Equipped gear must be unequipped first.)", 34, (_rx1 - _rx0) - 80);
        draw_set_halign(fa_left);
    } else {
        var _cur  = clamp(_gc.reforge_index, 0, _n - 1);
        var _vis  = 5;   // 08-11: row 6's band now hosts the Pattern Book verbs
        // Edge-triggered window: driven by the persistent reforge_scroll the Step handler
        // advances only when the cursor hits an edge (so the selector moves, not the list).
        var _win0 = clamp(_gc.reforge_scroll, 0, max(0, _n - _vis));
        var _win1 = min(_n, _win0 + _vis);
        var _row_h = 96, _row_y0 = _ry0 + 66;
        var _name_maxw = (_rx1 - 300) - (_rx0 + 30);   // reserve a right zone for status text

        for (var _i = _win0; _i < _win1; _i++) {
            var _c    = _list[_i];
            var _rar  = variable_struct_exists(_c.item, "rarity") ? clamp(_c.item.rarity, 0, 4) : 0;
            // Legendaries recast for gold (07-28) - never dimmed by the ingot gate.
            var _able = (_rar >= 4) || (reforge_ingot_tier_for(_rar) >= 0);
            var _sel  = (_i == _cur);
            var _ry   = _row_y0 + (_i - _win0) * (_row_h + 6);

            draw_set_alpha(_sel ? 1.0 : 0.85);
            draw_set_color(_sel ? make_color_rgb(38, 32, 24) : make_color_rgb(20, 20, 26));
            draw_rectangle(_rx0 + 12, _ry, _rx1 - 12, _ry + _row_h, false);
            draw_set_alpha(1.0);
            draw_set_color(_sel ? _copper : make_color_rgb(48, 44, 40));
            draw_rectangle(_rx0 + 12, _ry, _rx1 - 12, _ry + _row_h, true);

            // Name (rarity-colored, dimmed when unaffordable) - truncate to its zone.
            draw_set_font(ui_font(fnt_ui));
            var _disp = _c.label;
            if (string_width(_disp) > _name_maxw) {
                while (string_length(_disp) > 1 && string_width(_disp + "...") > _name_maxw) {
                    _disp = string_copy(_disp, 1, string_length(_disp) - 1);
                }
                _disp += "...";
            }
            var _nc = item_rarity_color(_rar);
            draw_set_color(_able ? _nc : merge_color(_nc, c_black, 0.55));
            draw_text(_rx0 + 30, _ry + 12, _disp);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(_able ? make_color_rgb(150, 150, 165) : make_color_rgb(95, 95, 108));
            draw_text(_rx0 + 30, _ry + 54, "[" + item_rarity_name(_rar) + "]");

            draw_set_halign(fa_right);
            if (_rar >= 4) {
                // Legendaries RECAST for gold (M 07-28) - no ingot gate.
                draw_set_color(make_color_rgb(230, 210, 150));
                draw_text(_rx1 - 30, _ry + 32, _sel ? ("Enter: Recast (" + string(legendary_recast_cost()) + "g)") : "Recastable");
            } else if (_able) {
                draw_set_color(make_color_rgb(120, 210, 130));
                draw_text(_rx1 - 30, _ry + 32, _sel ? "Enter: Rework" : "Ready");
            } else {
                draw_set_color(make_color_rgb(190, 90, 80));
                draw_text(_rx1 - 30, _ry + 32, "Need " + item_rarity_name(_rar) + "+ ingot");
            }
            draw_set_halign(fa_left);
        }

        // Position readout instead of "N more above/below" - shows where the selected
        // row sits in the whole list (e.g. "23 / 56"). Only when the list overflows.
        if (_n > _vis) {
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(150, 130, 100));
            draw_text((_rx0 + _rx1) / 2, _ry1 - 30, string(_cur + 1) + " / " + string(_n));
            draw_set_halign(fa_left);
        }
    }

    // PATTERN BOOK verb row (08-11): smelt fodder in, the book to browse, the
    // craft wizard out. Occupies the band the 6th gear row used to hold.
    ui_confirm_button(_rx0 + 12, 772, _rx0 + 292, 824, "SMELT  [T]",
        make_color_rgb(190, 160, 90), _dfx, _dfy, _dfp, "dorn:smelt");
    ui_confirm_button(_rx0 + 304, 772, _rx0 + 584, 824, "PATTERN BOOK  [B]",
        make_color_rgb(210, 140, 70), _dfx, _dfy, _dfp, "dorn:book");
    ui_confirm_button(_rx0 + 596, 772, _rx1 - 12, 824, "CRAFT  [N]",
        make_color_rgb(120, 210, 130), _dfx, _dfy, _dfp, "dorn:craft");

    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(75, 85, 105));
    ui_draw_key_legend(960, 1026, "W/S: Gear   Enter: Rework   T: Smelt   B: Book   N: Craft   G: Frame   V: Forge   U: Station   Q/E: Tab   Esc: Close");
    draw_set_halign(fa_left);

    // Mythril Frame checkout popup + component-craft burst (drawn topmost).
    ui_draw_checkout_vfx();
    if (variable_instance_exists(_gc, "dorn_ck_open") && _gc.dorn_ck_open) {
        ui_draw_checkout_confirm(_gc.dorn_ck_title, _gc.dorn_ck_body, "dorn:ok", "dorn:cancel");
    }
    // Smelt study popup (08-11): modal over the tab, topmost of all.
    if (variable_instance_exists(_gc, "pb_smelt_open") && _gc.pb_smelt_open) {
        ui_draw_pb_smelt(_gc);
    }
    // TIER UNLOCK toast (M 08-16) - boxed, topmost, per the toast standard;
    // set by pattern_smelt_commit when a study crosses 1 / 3 / 6.
    if (variable_global_exists("dorn_toast_timer") && global.dorn_toast_timer > 0
        && variable_global_exists("dorn_toast_msg") && global.dorn_toast_msg != "") {
        ui_draw_toast(global.dorn_toast_msg, 960, 176, min(1, global.dorn_toast_timer / 40));
        global.dorn_toast_timer--;
    }

    draw_set_valign(fa_top);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_confirm_button(x0, y0, x1, y1, txt, col, mx, my, press, tag)
// Shared tappable button: draws the box AND hit-tests it (touch rule: hit-tests
// live in Draw). A press inside injects `tag` for the owning Step handler to
// consume next frame. Reused by the Dorn rework screen + Petra's visual pass.
// ---------------------------------------------------------------------------
function ui_confirm_button(_x0, _y0, _x1, _y1, _txt, _col, _mx, _my, _press, _tag) {
    var _hov = (_mx >= _x0 && _mx < _x1 && _my >= _y0 && _my < _y1);
    draw_set_color(_hov ? make_color_rgb(40, 42, 36) : make_color_rgb(24, 26, 32));
    draw_rectangle(_x0, _y0, _x1, _y1, false);
    draw_set_color(_col);
    draw_rectangle(_x0, _y0, _x1, _y1, true);
    draw_set_halign(fa_center);
    // 08-18 (M shot: Pattern Book UP/DOWN/CLOSE spilled below their boxes): the label was
    // pinned at +12 in the full font whatever the box height. Now vertically CENTRED, and
    // steps down to the base-size (dense) font, then fnt_ui_small, when the box is short -
    // so Large-font mode never overflows a 36px button.
    var _bh = (_y1 - _y0);
    draw_set_font(ui_font(fnt_ui));
    if (string_height("Ag") > _bh - 6) draw_set_font(ui_font_dense(fnt_ui));
    if (string_height("Ag") > _bh - 6) draw_set_font(ui_font_dense(fnt_ui_small));
    // Width guard too: a long label in a narrow box drops to the small font.
    if (string_width(_txt) > (_x1 - _x0) - 12) draw_set_font(ui_font_dense(fnt_ui_small));
    draw_set_valign(fa_middle);
    draw_text((_x0 + _x1) / 2, (_y0 + _y1) / 2 + 1, _txt);
    draw_set_valign(fa_top);
    draw_set_halign(fa_left);
    if (_hov && _press) input_inject(_tag);
}

// ui_draw_reagent_readout(right_x, y) - right-aligned "Reagents: N" chip with
// the reagent glyph, for NPC screen/shop headers next to gold + dust (M 08-26:
// reagents need their own icon readout on every trade screen). Label green like
// the dungeon growth they come from; value stays gold like the other readouts.
// The glyph resolves by STRING so this ships ahead of the sprite import - add
// spr_icon_reagent to global.__sprite_includes when the art lands.
function ui_draw_reagent_readout(_rx, _ry) {
    reagents_ensure();
    var _rr_num = " " + string(reagent_total());
    draw_set_halign(fa_right);
    draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(200, 180, 130));
    draw_text(_rx, _ry, _rr_num);
    draw_set_color(make_color_rgb(150, 205, 160));
    draw_text(_rx - string_width(_rr_num), _ry, "Reagents:");
    var _rr_spr = asset_get_index("spr_icon_reagent");
    if (_rr_spr >= 0) {
        draw_sprite_stretched(_rr_spr, 0,
            _rx - string_width(_rr_num) - string_width("Reagents:") - 30, _ry - 2, 24, 24);
    }
    draw_set_halign(fa_left);
}

// ui_draw_text_fit(x, y, txt, maxw) - one-line left-aligned shrink-to-fit text:
// scales down instead of overflowing (08-26: crafted names may run 60-72 chars).
function ui_draw_text_fit(_x, _y, _txt, _maxw) {
    var _sc = min(1.0, _maxw / max(1, string_width(_txt)));
    draw_text_transformed(_x, _y + (1 - _sc) * string_height("Ag") * 0.35, _txt, _sc, _sc, 0);
}

// ui_arrow_button(x0,y0,x1,y1, dir, col, mx,my,press, tag) - scroll button with
// a DRAWN triangle glyph instead of a text label (M 08-26: the Pattern Book's
// UP/DOWN labels never sat cleanly in their boxes across font sizes - a glyph
// can't crop or fall to a mismatched fallback font). dir: -1 = up, +1 = down.
// Hover + inject idiom identical to ui_confirm_button.
function ui_arrow_button(_x0, _y0, _x1, _y1, _dir, _col, _mx, _my, _press, _tag) {
    var _hov = (_mx >= _x0 && _mx < _x1 && _my >= _y0 && _my < _y1);
    draw_set_color(_hov ? make_color_rgb(40, 42, 36) : make_color_rgb(24, 26, 32));
    draw_rectangle(_x0, _y0, _x1, _y1, false);
    draw_set_color(_col);
    draw_rectangle(_x0, _y0, _x1, _y1, true);
    var _acx = (_x0 + _x1) / 2, _acy = (_y0 + _y1) / 2;
    var _ahw = min(_x1 - _x0, _y1 - _y0) * 0.28;   // triangle half-width
    draw_set_color(_hov ? c_white : _col);
    if (_dir < 0) draw_triangle(_acx - _ahw, _acy + _ahw * 0.7, _acx + _ahw, _acy + _ahw * 0.7, _acx, _acy - _ahw * 0.8, false);
    else          draw_triangle(_acx - _ahw, _acy - _ahw * 0.7, _acx + _ahw, _acy - _ahw * 0.7, _acx, _acy + _ahw * 0.8, false);
    if (_hov && _press) input_inject(_tag);
}

// ---------------------------------------------------------------------------
// CHECKOUT VFX (M 07-28: the code-drawn rings read "runescape level" - reuse
// the owned spell VFX Gigapack instead). One transient additive burst shared by
// the hub checkout moments (Dorn's reforge reveal, Sable's cauldron, Petra's
// collect, Vael's reweave). Fire with ui_checkout_vfx(spr, x, y); each owning
// overlay draw calls ui_draw_checkout_vfx() LAST. Frame/scale idiom mirrors the
// combat impact VFX (obj_combat_controller Draw_64).
// ---------------------------------------------------------------------------
function ui_checkout_vfx(_spr, _x, _y) {
    global.checkout_vfx = { spr: _spr, x: _x, y: _y, t: 0, tmax: 30 };
}
function ui_draw_checkout_vfx() {
    if (!variable_global_exists("checkout_vfx")) return;
    var _v = global.checkout_vfx;
    if (_v == undefined) return;
    var _n  = max(1, sprite_get_number(_v.spr));
    var _pr = clamp(_v.t / _v.tmax, 0, 1);
    var _fr = clamp(floor(_pr * _n), 0, _n - 1);
    var _target = lerp(300, 210, _pr);   // on-screen px, shrinks over life
    var _sc = _target / max(1, sprite_get_width(_v.spr));
    gpu_set_blendmode(bm_add);
    draw_set_alpha(min(1.0, (_v.tmax - _v.t) / 10));
    draw_sprite_ext(_v.spr, _fr, _v.x - _target / 2, _v.y - _target / 2, _sc, _sc, 0, c_white, 1.0);
    gpu_set_blendmode(bm_normal);
    draw_set_alpha(1.0);
    _v.t++;
    if (_v.t >= _v.tmax) global.checkout_vfx = undefined;
}

// ---------------------------------------------------------------------------
// ui_draw_checkout_confirm(title, body, ok_tag, cancel_tag)
// STANDING RULE (M 07-27): every "checkout" confirm at a store/shop/trainer is
// a bordered overlay popup with CONFIRM/CANCEL buttons - never a question
// squeezed into the bottom notification line. Dims the screen, draws the boxed
// prompt, and hit-tests both buttons (injecting ok_tag / cancel_tag for the
// owning Step handler). The owning Step MUST treat the armed state as MODAL -
// swallow nav/tabs/row toggles - so the pending action can't shift under the
// popup (the old inline confirm let Enter deselect the rune it was committing).
// ---------------------------------------------------------------------------
function ui_draw_checkout_confirm(_title, _body, _ok_tag, _cancel_tag, _subject = "", _subject_col = undefined, _subject_sub = "") {
    draw_set_alpha(0.66);
    draw_set_color(make_color_rgb(6, 8, 14));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    var _x0 = 560, _y0 = 372, _x1 = 1360;
    var _cbw = (_x1 - _x0) - 90;
    // MEASURE pass (M 08-26 shot: a long craft body ran UNDER the buttons):
    // replicate the font stack below, find where the body ends, and grow the
    // panel past its shipped 708 floor when the content asks for more room.
    draw_set_font(ui_font(fnt_ui_small));
    var _clh   = max(30, string_height("Ag") + 2);   // wrap line pitch, font-measured
    var _m_cby = _y0 + 84;
    if (_subject != "") {
        draw_set_font(ui_font(fnt_ui));
        if (string_width(_subject) > _cbw) draw_set_font(ui_font(fnt_ui_small));
        _m_cby = _y0 + 72 + string_height("Ag") + 6;
        draw_set_font(ui_font(fnt_ui_small));
        _m_cby += (_subject_sub != "") ? (string_height("Ag") + 12) : 8;
    }
    var _y1 = clamp(max(708, _m_cby + string_height_ext(_body, _clh, _cbw) + 114), 708, 1044);
    draw_set_alpha(0.97);
    draw_set_color(make_color_rgb(22, 20, 30));
    draw_rectangle(_x0, _y0, _x1, _y1, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(200, 170, 110));
    draw_rectangle(_x0, _y0, _x1, _y1, true);
    draw_rectangle(_x0 + 6, _y0 + 6, _x1 - 6, _y1 - 6, true);
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(255, 225, 150));
    // Shrink-to-fit title: a 72-char crafted name rides in here (08-26 cap x3).
    var _t_sc = min(1.0, ((_x1 - _x0) - 48) / max(1, string_width(_title)));
    draw_text_transformed((_x0 + _x1) / 2, _y0 + 24, _title, _t_sc, _t_sc, 0);
    draw_set_font(ui_font(fnt_ui_small));
    var _cby = _y0 + 84;
    if (_subject != "") {
        // Colored SUBJECT block (M 08-14: "runes/items should be color coded in
        // these popups so the player knows what is being disassembled") - the
        // thing being consumed, in its own color, its effect line under it; the
        // body then reads as a plain sentence, no more mid-phrase line break.
        draw_set_font(ui_font(fnt_ui));
        if (string_width(_subject) > _cbw) draw_set_font(ui_font(fnt_ui_small));   // 3-name lists
        draw_set_color((_subject_col == undefined) ? make_color_rgb(228, 222, 240) : _subject_col);
        draw_text((_x0 + _x1) / 2, _y0 + 72, _subject);
        _cby = _y0 + 72 + string_height("Ag") + 6;
        draw_set_font(ui_font(fnt_ui_small));
        if (_subject_sub != "") {
            draw_set_color(make_color_rgb(152, 158, 174));
            draw_text((_x0 + _x1) / 2, _cby, _subject_sub);
            _cby += string_height("Ag") + 12;
        } else {
            _cby += 8;
        }
    }
    draw_set_color(make_color_rgb(210, 214, 228));
    draw_text_ext((_x0 + _x1) / 2, _cby, _body, _clh, _cbw);
    draw_set_halign(fa_left);
    var _ccx = device_mouse_x_to_gui(0), _ccy = device_mouse_y_to_gui(0);
    var _ccp = mouse_check_button_pressed(mb_left);
    ui_confirm_button(_x0 + 60, _y1 - 90, (_x0 + _x1) / 2 - 30, _y1 - 24, "CONFIRM  [Enter]",
        make_color_rgb(120, 210, 130), _ccx, _ccy, _ccp, _ok_tag);
    ui_confirm_button((_x0 + _x1) / 2 + 30, _y1 - 90, _x1 - 60, _y1 - 24, "CANCEL  [Esc]",
        make_color_rgb(190, 120, 110), _ccx, _ccy, _ccp, _cancel_tag);
}

// ---------------------------------------------------------------------------
// ui_reforge_card(x0, y0, x1, y1, tag, tagcol, label, rar, stat)
// One item card for the rework screen: framed box, small tag header, then the
// item's name (rarity-colored, wrapped), tier line, and stat string. Pass
// label == "" to draw just the empty frame (caller fills it - the "?" card).
// ---------------------------------------------------------------------------
function ui_reforge_card(_x0, _y0, _x1, _y1, _tag, _tagcol, _label, _rar, _stat) {
    draw_set_color(make_color_rgb(20, 20, 27)); draw_rectangle(_x0, _y0, _x1, _y1, false);
    draw_set_color(_tagcol);                    draw_rectangle(_x0, _y0, _x1, _y1, true);
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(_tagcol);
    draw_text((_x0 + _x1) / 2, _y0 + 14, _tag);
    if (_label != "") {
        // MEASURED stack (M 08-16: "reforge rework has text coverings and runs
        // out of text boxes"): a long affixed name used to overrun the fixed
        // [Rarity] line at +148, and a long stat list ran past the card bottom
        // into the buttons. Name -> rarity -> stats now flow from measured
        // heights; the stat block steps down to the small font (then a tighter
        // line pitch) until it fits the card, and finally trims lines with "..".
        var _cw = (_x1 - _x0) - 60;
        var _cx = (_x0 + _x1) / 2;
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(item_rarity_color(_rar));
        var _nh = string_height_ext(_label, 34, _cw);
        draw_text_ext(_cx, _y0 + 64, _label, 34, _cw);
        var _ry = _y0 + 64 + _nh + 12;
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(140, 145, 160));
        draw_text(_cx, _ry, "[" + item_rarity_name(_rar) + "]");
        var _sy    = _ry + 46;
        var _avail = (_y1 - 16) - _sy;
        var _sf = ui_font(fnt_ui), _slh = 36;
        draw_set_font(_sf);
        if (string_height_ext(_stat, _slh, _cw) > _avail) { _sf = ui_font(fnt_ui_small); _slh = 30; draw_set_font(_sf); }
        if (string_height_ext(_stat, _slh, _cw) > _avail) { _slh = 26; }
        var _st = _stat, _guard = 0, _trimmed = false;
        while (string_height_ext(_st + (_trimmed ? "\n.." : ""), _slh, _cw) > _avail && string_length(_st) > 8 && _guard++ < 60) {
            var _nl = 0;
            for (var _ci = string_length(_st); _ci >= 1; _ci--) if (string_char_at(_st, _ci) == "\n") { _nl = _ci; break; }
            _st = (_nl > 1) ? string_copy(_st, 1, _nl - 1) : string_copy(_st, 1, string_length(_st) - 12);
            _trimmed = true;
        }
        if (_trimmed) _st += "\n..";
        draw_set_color(make_color_rgb(205, 210, 225));
        draw_text_ext(_cx, _sy, _st, _slh, _cw);
    }
    draw_set_halign(fa_left);
}

// ---------------------------------------------------------------------------
// ui_draw_reforge_confirm(_gc)
// Dorn's two-step rework screen (M 07-27: a legendary got rerolled in one click
// - destructive spends get a confirmation + reveal now, standing rule). Stage 1:
// BEFORE card + ingot cost + COMMIT/BACK. Stage 2: forge animation over a veiled
// result (snd_forge rang at commit). Stage 3: the reworked item revealed.
// Geometry: content panel 150-1500 (center 825) like the two list panels; the
// legend stays screen-centered at 960 like every shop screen. Buttons hit-test
// HERE and inject reforge:commit / reforge:back / reforge:done for the Step.
// ---------------------------------------------------------------------------
function ui_draw_reforge_confirm(_gc) {
    var _copper = make_color_rgb(210, 140, 70);
    var _stage  = _gc.reforge_stage;
    var _item   = _gc.reforge_target;
    var _t      = _gc.reforge_anim_t;

    var _px0 = 150, _px1 = 1500, _py0 = 189, _py1 = 900;
    var _pcx = (_px0 + _px1) / 2;   // 825 - the content panel's center, NOT 960
    draw_set_color(make_color_rgb(14, 13, 20)); draw_rectangle(_px0, _py0, _px1, _py1, false);
    draw_set_color(make_color_rgb(70, 55, 40)); draw_rectangle(_px0, _py0, _px1, _py1, true);

    // LEGENDARY RECAST (M 07-28): same screen, different words + a gold cost
    // instead of an ingot.
    var _recast = variable_instance_exists(_gc, "reforge_is_recast") && _gc.reforge_is_recast;
    draw_set_halign(fa_center);
    draw_set_font(fnt_ui_title);
    draw_set_color(_copper);
    draw_text(_pcx, _py0 + 20, (_stage == 1) ? (_recast ? "RECAST THIS LEGENDARY?" : "REWORK THIS GEAR?")
        : ((_stage == 2) ? "DORN WORKS THE FORGE..." : (_recast ? "RECAST" : "REFORGED")));

    if (_stage == 1) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(200, 150, 110));
        draw_text(_pcx, _py0 + 72, _recast
            ? "It melts down entirely and returns as a DIFFERENT legendary. This cannot be undone."
            : "Its affixes are melted and re-struck. This cannot be undone.");
    }

    // BEFORE card (left). From stage 2 on this is the SNAPSHOT taken at commit -
    // the live item struct has already been rerolled underneath the veil.
    var _b_lab, _b_rar, _b_stat;
    if (_stage >= 2 && _gc.reforge_before != undefined) {
        _b_lab = _gc.reforge_before.label; _b_rar = _gc.reforge_before.rar; _b_stat = _gc.reforge_before.stat;
    } else {
        _b_lab  = _item.name;
        _b_rar  = variable_struct_exists(_item, "rarity") ? clamp(_item.rarity, 0, 4) : 0;
        _b_stat = ui_item_stat_str(_item);
    }
    // Cards trimmed 40px each on the gutter side (M 08-26: "the two boxes cover
    // the ingot in the middle") - the center gutter grew 170 -> 250px so the
    // ingot + its cost line never sit on the card borders at any font size.
    var _lc0 = 190, _lc1 = 700, _cy0 = 290, _cy1 = 790;
    ui_reforge_card(_lc0, _cy0, _lc1, _cy1, (_stage >= 2) ? "BEFORE" : "YOUR GEAR",
        make_color_rgb(120, 118, 130), _b_lab, _b_rar, _b_stat);

    // Center column: arrow + the ingot this rework costs (spent tier once rolled).
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(200, 190, 170));
    draw_text(_pcx, 500, "->");
    if (_recast) {
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(230, 210, 150));
        draw_text(_pcx, 560, string(legendary_recast_cost()) + "g");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(160, 145, 120));
        draw_text(_pcx, 604, (_stage >= 2) ? "paid to the forge" : "the forge's price");
    } else {
        var _cost_tier = (_stage >= 2) ? _gc.reforge_spent_tier : reforge_ingot_tier_for(_b_rar);
        if (_cost_tier >= 0) {
            draw_sprite_stretched(reforge_ingot_sprite(_cost_tier), 0, _pcx - 36, 534, 72, 72);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(item_rarity_color(_cost_tier));
            // Wrapped inside the widened 250px gutter between the cards.
            draw_text_ext(_pcx, 618, ((_stage >= 2) ? "spent 1 " : "costs 1 ") + item_rarity_name(_cost_tier) + " ingot", 26, 230);
        }
    }

    // AFTER card (right): "?" until the reveal, then the reworked item in green.
    var _rc0 = 950, _rc1 = 1460;
    var _rcx = (_rc0 + _rc1) / 2, _rcy = (_cy0 + _cy1) / 2;
    if (_stage == 3) {
        ui_reforge_card(_rc0, _cy0, _rc1, _cy1, "NEW", make_color_rgb(120, 210, 130),
            _item.name, _b_rar, ui_item_stat_str(_item));
        // Reveal burst: one golden ring + fading flash, driven past frame 90.
        var _rv = _t - 90;
        if (_rv >= 0 && _rv < 45) {
            draw_set_alpha(0.55 * (1 - _rv / 45));
            draw_set_color(make_color_rgb(255, 220, 130));
            draw_circle(_rcx, _rcy, 40 + _rv * 7, true);
            draw_set_alpha(1.0);
        }
    } else {
        ui_reforge_card(_rc0, _cy0, _rc1, _cy1, "AFTER", merge_color(_copper, c_black, 0.35), "", 0, "");
        draw_set_font(fnt_ui_title);
        draw_set_color(merge_color(_copper, c_black, (_stage == 2) ? 0.1 : 0.45));
        draw_text(_rcx, _rcy - 60, "?");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(130, 120, 110));
        draw_text_ext(_rcx, _rcy + 20, "Dorn will melt and re-strike its affixes.\nThe result is uncertain.", 30, (_rc1 - _rc0) - 80);
        // Forge animation (M 07-28: Gigapack over code rings): the fiery Gigapack
        // burst loops over the veiled card while Dorn works, with a soft copper
        // ring underneath for continuity.
        if (_stage == 2) {
            for (var _ri = 0; _ri < 2; _ri++) {
                var _rt = (_t * 2 + _ri * 45) mod 90;
                draw_set_alpha(0.35 * (1 - _rt / 90));
                draw_set_color(_copper);
                draw_circle(_rcx, _rcy, 24 + _rt * 2.4, true);
            }
            draw_set_alpha(1.0);
            var _ff_n  = max(1, sprite_get_number(spr_vfx_fire));
            var _ff_fr = (_t div 3) mod _ff_n;
            var _ff_sz = 260;
            var _ff_sc = _ff_sz / max(1, sprite_get_width(spr_vfx_fire));
            gpu_set_blendmode(bm_add);
            draw_sprite_ext(spr_vfx_fire, _ff_fr, _rcx - _ff_sz / 2, _rcy - _ff_sz / 2, _ff_sc, _ff_sc, 0, c_white, 0.9);
            gpu_set_blendmode(bm_normal);
        }
    }

    // Buttons (inside the panel, under the cards) - tap targets per the touch rule.
    var _mx = device_mouse_x_to_gui(0), _my = device_mouse_y_to_gui(0);
    var _press = mouse_check_button_pressed(mb_left);
    if (_stage == 1) {
        ui_confirm_button(390, 816, 760, 882, "COMMIT  [Enter]", make_color_rgb(120, 210, 130), _mx, _my, _press, "reforge:commit");
        ui_confirm_button(890, 816, 1260, 882, "BACK  [Esc]",    make_color_rgb(190, 120, 110), _mx, _my, _press, "reforge:back");
    } else if (_stage == 3) {
        ui_confirm_button(640, 816, 1010, 882, "DONE  [Enter]",  make_color_rgb(120, 210, 130), _mx, _my, _press, "reforge:done");
    }

    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(75, 85, 105));
    ui_draw_key_legend(960, 1026, (_stage == 1) ? "Enter: Commit    Esc: Back"
        : ((_stage == 2) ? "Enter: Skip" : "Enter: Done"));
    draw_set_halign(fa_left);
    ui_draw_checkout_vfx();   // Gigapack reveal burst (fired at stage 3 entry)
    draw_set_valign(fa_top);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_legendary_forge(_gc)
// THE LEGENDARY FORGE (M locked 07-28): pick a slot, pick one of the five
// forge-exclusive effects, NAME the piece, see it born. Owns the Dorn content
// area while _gc.forge_open. Row taps inject forge:row<i>; DONE = forge:done.
// ---------------------------------------------------------------------------
function ui_draw_legendary_forge(_gc) {
    var _px0 = 150, _px1 = 1500, _py0 = 189, _py1 = 900;
    var _pcx = (_px0 + _px1) / 2;
    draw_set_color(make_color_rgb(16, 13, 22)); draw_rectangle(_px0, _py0, _px1, _py1, false);
    draw_set_color(make_color_rgb(120, 90, 50)); draw_rectangle(_px0, _py0, _px1, _py1, true);
    draw_rectangle(_px0 + 6, _py0 + 6, _px1 - 6, _py1 - 6, true);

    draw_set_halign(fa_center);
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(255, 225, 150));
    draw_text(_pcx, _py0 + 20, "THE LEGENDARY FORGE");
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(180, 160, 130));
    draw_text(_pcx, _py0 + 72, (_gc.forge_phase == 0) ? "What shape shall it take?"
        : ((_gc.forge_phase == 1) ? "What power shall it hold?"
        : ((_gc.forge_phase == 2) ? "Name it - there will be exactly one." : "It is done.")));
    draw_set_halign(fa_left);

    var _fmx = device_mouse_x_to_gui(0), _fmy = device_mouse_y_to_gui(0);
    var _fmp = mouse_check_button_pressed(mb_left);

    if (_gc.forge_phase == 0 || _gc.forge_phase == 1) {
        var _slot_names = { weapon: "Weapon", ranged_weapon: "Ranged Weapon", offhand: "Off-hand",
                            helm: "Helm", chest: "Chest", gloves: "Gloves", boots: "Boots",
                            amulet: "Amulet", ring: "Ring" };
        var _rows  = (_gc.forge_phase == 0) ? forge_slot_list() : forge_effect_catalog();
        var _n     = array_length(_rows);
        var _rx0   = 400, _rx1 = 1250, _ry0 = 285;
        var _pitch = (_gc.forge_phase == 0) ? 64 : 96;
        for (var _i = 0; _i < _n; _i++) {
            var _ry = _ry0 + _i * _pitch;
            var _on = (_i == _gc.forge_cursor);
            if (_fmp && _fmx >= _rx0 && _fmx < _rx1 && _fmy >= _ry && _fmy < _ry + _pitch - 8) {
                input_inject("forge:row" + string(_i));
            }
            draw_set_color(_on ? make_color_rgb(40, 32, 22) : make_color_rgb(20, 18, 26));
            draw_rectangle(_rx0, _ry, _rx1, _ry + _pitch - 8, false);
            draw_set_color(_on ? make_color_rgb(255, 225, 150) : make_color_rgb(52, 48, 44));
            draw_rectangle(_rx0, _ry, _rx1, _ry + _pitch - 8, true);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_on ? c_white : make_color_rgb(180, 175, 165));
            if (_gc.forge_phase == 0) {
                draw_text(_rx0 + 24, _ry + 12, variable_struct_get(_slot_names, _rows[_i]));
            } else {
                draw_text(_rx0 + 24, _ry + 9, _rows[_i].name);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(140, 135, 125));
                draw_text(_rx0 + 24, _ry + 45, _rows[_i].desc);
            }
        }
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(120, 105, 85));
        ui_draw_key_legend(960, 1026, "W/S: Choose    Enter: Next    Esc: Back");
        draw_set_halign(fa_left);
    } else if (_gc.forge_phase == 2) {
        // Naming: the typed name, a blinking caret, the chosen shape + power.
        var _fx   = forge_effect_catalog()[clamp(_gc.forge_fx_pick, 0, 4)];
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(170, 160, 150));
        draw_text(_pcx, 380, string_upper(forge_slot_list()[clamp(_gc.forge_slot_pick, 0, 8)])
            + "   -   " + _fx.name + ": " + _fx.desc);
        draw_set_color(make_color_rgb(30, 26, 36));
        draw_rectangle(_pcx - 360, 470, _pcx + 360, 550, false);
        draw_set_color(make_color_rgb(255, 225, 150));
        draw_rectangle(_pcx - 360, 470, _pcx + 360, 550, true);
        draw_set_font(fnt_ui_title);
        draw_set_color(item_rarity_color(4));
        var _caret = ((current_time div 400) mod 2 == 0) ? "|" : " ";
        // Shrink-to-fit: the cap grew 20 -> 60 (M 08-26), so long names scale
        // down inside the 720px entry box instead of running out of it.
        var _fn_txt = keyboard_string + _caret;
        var _fn_sc  = min(1.0, 680 / max(1, string_width(_fn_txt)));
        draw_text_transformed(_pcx, 490 + (1 - _fn_sc) * 18, _fn_txt, _fn_sc, _fn_sc, 0);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(140, 130, 115));
        draw_text(_pcx, 590, "Type its name (max 60).   Enter: FORGE IT    Esc: back");
        draw_set_halign(fa_left);
    } else {
        // Result: the born legendary, examined like every other reveal.
        if (_gc.forge_result != undefined) {
            ui_reforge_card(_pcx - 280, 320, _pcx + 280, 790, "FORGED", make_color_rgb(120, 210, 130),
                _gc.forge_result.name, 4, ui_item_stat_str(_gc.forge_result));
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(200, 180, 130));
            draw_text(_pcx, 700, _gc.forge_result.unique_desc);
            draw_set_halign(fa_left);
        }
        ui_confirm_button(_pcx - 165, 812, _pcx + 165, 872, "DONE  [Enter]", make_color_rgb(120, 210, 130),
            _fmx, _fmy, _fmp, "forge:done");
    }

    ui_draw_checkout_vfx();   // the forging burst (fired at commit)
    draw_set_valign(fa_top);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_consumable_overflow()
// Modal shown mid-run when the pack is full and another consumable is picked
// up. Resolves global.consumable_overflow one item at a time (see
// consumable_overflow_step). Drawn over both the combat and floor screens.
// ---------------------------------------------------------------------------
function ui_draw_consumable_overflow() {
    if (!consumable_overflow_pending()) return;
    if (!variable_global_exists("consumable_overflow_cursor")) global.consumable_overflow_cursor = 0;
    if (!variable_global_exists("consumable_overflow_armed"))  global.consumable_overflow_armed  = -1;

    // Dim the whole screen
    draw_set_alpha(0.72);
    draw_set_color(make_color_rgb(6, 8, 14));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    var _new     = global.consumable_overflow[0];
    var _groups  = consumables_grouped();
    var _options = array_length(_groups) + 1;
    var _cur     = clamp(global.consumable_overflow_cursor, 0, _options - 1);
    var _armed   = global.consumable_overflow_armed;

    // Two-panel layout (M 07-08 redesign): YOUR PACK on the left, a DISCARDING
    // panel on the right that spells out exactly what is lost - one unit of a
    // stack, never the stack - plus an explicit second-press confirm.
    var _pw = 1160;
    var _ph = 720;
    var _px = (GUI_W - _pw) / 2;
    var _py = (GUI_H - _ph) / 2;

    draw_set_color(make_color_rgb(18, 20, 32));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, false);
    draw_set_color(make_color_rgb(200, 110, 80));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, true);

    draw_set_halign(fa_center);
    draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(230, 130, 90));
    draw_text(_px + _pw / 2, _py + 18, "PACK FULL  (" + string(consumable_carry_cap()) + "/"
        + string(consumable_carry_cap()) + ")");

    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(200, 210, 225));
    draw_text(_px + _pw / 2, _py + 70, "Picked up  " + _new.name + ".  Choose ONE thing to give up.");

    var _pending_extra = array_length(global.consumable_overflow) - 1;
    if (_pending_extra > 0) {
        draw_set_color(make_color_rgb(150, 160, 180));
        draw_text(_px + _pw / 2, _py + 96, "(" + string(_pending_extra) + " more pickup" + (_pending_extra == 1 ? "" : "s") + " waiting)");
    }

    // ---- LEFT: pack list (windowed; follows the cursor) --------------------
    var _lx0 = _px + 36, _lx1 = _px + 630;
    var _list_y = _py + 132;
    var _row_h  = 52;
    var _max_rows = max(1, ((_py + _ph - 76) - _list_y) div _row_h);
    var _scroll   = clamp(_cur - _max_rows + 1, 0, max(0, _options - _max_rows));

    draw_set_halign(fa_left);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(140, 150, 175));
    draw_text(_lx0, _list_y - 30, "YOUR PACK");
    draw_set_font(ui_font(fnt_ui));

    for (var _wr = 0; _wr < min(_max_rows, _options - _scroll); _wr++) {
        var _i = _scroll + _wr;
        var _is_sel = (_i == _cur);
        var _is_arm = (_i == _armed);
        var _ry = _list_y + _wr * _row_h;

        if (_is_sel || _is_arm) {
            draw_set_alpha(0.9);
            draw_set_color(_is_arm ? make_color_rgb(70, 30, 26) : make_color_rgb(60, 40, 30));
            draw_rectangle(_lx0 - 10, _ry - 4, _lx1, _ry + _row_h - 10, false);
            draw_set_alpha(1.0);
            if (_is_arm) {
                draw_set_color(make_color_rgb(230, 120, 90));
                draw_rectangle(_lx0 - 10, _ry - 4, _lx1, _ry + _row_h - 10, true);
            }
        }

        if (_i < array_length(_groups)) {
            ui_draw_consumable_icon(_lx0, _ry - 2, 40, _groups[_i].item);
            draw_set_color(_is_sel ? make_color_rgb(120, 230, 230) : ui_consumable_name_color(_groups[_i].item));   // Genie Lamp golds
            draw_text(_lx0 + 54, _ry + 4, consumable_group_label(_groups[_i]));
        } else {
            ui_draw_consumable_icon(_lx0, _ry - 2, 40, _new);
            draw_set_color(_is_sel ? make_color_rgb(230, 160, 120) : make_color_rgb(180, 130, 100));
            draw_text(_lx0 + 54, _ry + 4, "Leave " + _new.name + " behind");
        }
    }
    if (_scroll > 0 || _scroll + _max_rows < _options) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(140, 150, 175));
        if (_scroll > 0) ui_draw_scroll_more(_lx0, _list_y - 8, true, "more");
        if (_scroll + _max_rows < _options) ui_draw_scroll_more(_lx0, _list_y + _max_rows * _row_h - 4, false, "more");
    }

    // ---- RIGHT: DISCARDING panel - what the armed choice actually costs ----
    var _dx0 = _px + 672, _dx1 = _px + _pw - 36;
    var _dy0 = _list_y - 30, _dy1 = _py + _ph - 90;
    draw_set_color(make_color_rgb(28, 16, 16));
    draw_rectangle(_dx0, _dy0, _dx1, _dy1, false);
    draw_set_color((_armed != -1) ? make_color_rgb(220, 100, 80) : make_color_rgb(70, 55, 55));
    draw_rectangle(_dx0, _dy0, _dx1, _dy1, true);

    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(230, 130, 90));
    draw_text((_dx0 + _dx1) / 2, _dy0 + 12, "DISCARDING");
    if (_armed == -1) {
        draw_set_color(make_color_rgb(120, 105, 105));
        draw_text_ext((_dx0 + _dx1) / 2, _dy0 + 130,
            "Nothing yet.\n\nHighlight what you can spare and press Enter - it appears here first, and NOTHING is lost until you confirm.", 30, (_dx1 - _dx0) - 60);
    } else if (_armed < array_length(_groups)) {
        var _ag = _groups[_armed];
        ui_draw_consumable_icon((_dx0 + _dx1) / 2 - 33, _dy0 + 60, 66, _ag.item);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(240, 150, 120));
        draw_text((_dx0 + _dx1) / 2, _dy0 + 144, "1x  " + _ag.item.name);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(200, 190, 190));
        draw_text_ext((_dx0 + _dx1) / 2, _dy0 + 195,
            ((_ag.count > 1)
                ? ("Only ONE is discarded - " + string(_ag.count - 1) + " stay" + (_ag.count - 1 == 1 ? "s" : "") + " in your pack.")
                : "That is your last one.")
            + "\n\n" + _new.name + " takes its place.", 30, (_dx1 - _dx0) - 60);
    } else {
        ui_draw_consumable_icon((_dx0 + _dx1) / 2 - 33, _dy0 + 60, 66, _new);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(240, 150, 120));
        draw_text((_dx0 + _dx1) / 2, _dy0 + 144, _new.name);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(200, 190, 190));
        draw_text_ext((_dx0 + _dx1) / 2, _dy0 + 195,
            "The new pickup is left behind.\n\nYour pack is unchanged.", 30, (_dx1 - _dx0) - 60);
    }

    // ---- Footer legend ------------------------------------------------------
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color((_armed != -1) ? make_color_rgb(230, 150, 110) : c_gray);
    ui_draw_key_legend(_px + _pw / 2, _py + _ph - 40, (_armed != -1)
        ? "Enter: CONFIRM discard    Esc: Cancel    W/S: Change choice"
        : "W/S: Navigate    Enter: Select    Enter again: Confirm");

    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_consumable_groups(arr)
// Collapse identical consumables (matched by name) into display groups so the
// stash lists show "Healing Potion  x4" on ONE row instead of four rows
// (M 07-29: the per-copy listing read as messy). Preserves first-seen order.
// Returns [{ item, count, first_index }]; first_index is the source array slot
// a move operation should take a copy from.
// ---------------------------------------------------------------------------
function ui_consumable_groups(arr) {
    var _g = [];
    for (var _i = 0; _i < array_length(arr); _i++) {
        var _it = arr[_i];
        var _found = -1;
        for (var _j = 0; _j < array_length(_g); _j++) {
            if (_g[_j].item.name == _it.name) { _found = _j; break; }
        }
        if (_found == -1) array_push(_g, { item: _it, count: 1, first_index: _i });
        else              _g[_found].count++;
    }
    return _g;
}

// ---------------------------------------------------------------------------
// ui_draw_stash_screen()
// Two-column stash management overlay drawn in the hub.
// Left column: items taken on the run (at risk). Right: safe stash.
// Called by obj_hub_controller Draw_64 after the history overlay.
// ---------------------------------------------------------------------------
function ui_draw_stash_screen() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!_gc.stash_mode_open) return;

    // Full-screen opaque cover
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(8, 10, 18));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);

    // Title
    draw_set_halign(fa_center);
    draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(c_white);
    draw_text(960, 30, "ITEM STASH");   // y30 keeps the title inside the rim opening

    // Subtitle warning (pushed clear of the large title font's descenders so they
    // no longer collide just under the title)
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(180, 150, 80));
    draw_text(960, 102, "Equipped gear is always safe.   Carried items are lost on death (1 random salvage).");
    draw_set_halign(fa_left);

    // Category tab bar (Q/E) - equipment, consumables, and the MISC holdings
    // ledger (M 08-26). Geometry is mirrored by the mouse handling in
    // obj_game_controller Step (tabs x = 503 + t*315, 285 wide, y138..190).
    var _tab = _gc.stash_mode_tab;
    var _tab_labels = ["EQUIPMENT", "CONSUMABLES", "MISC"];
    draw_set_font(ui_font(fnt_ui));
    for (var _t = 0; _t < 3; _t++) {
        var _tx = 503 + _t * 315;   // 285 wide + 30 gap, trio centered on x960
        var _on = (_tab == _t);
        draw_set_color(_on ? make_color_rgb(30, 50, 80) : make_color_rgb(18, 22, 38));
        draw_rectangle(_tx, 138, _tx + 285, 190, false);
        draw_set_color(_on ? make_color_rgb(80, 160, 220) : make_color_rgb(45, 55, 75));
        draw_rectangle(_tx, 138, _tx + 285, 190, true);
        draw_set_halign(fa_center);
        draw_set_valign(fa_middle);
        draw_set_color(_on ? c_white : make_color_rgb(150, 150, 170));
        draw_text(_tx + 143, 164, _tab_labels[_t]);
    }
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);

    var _ly      = 204;
    var _col_w   = 855;
    var _row_h   = 75;
    var _max_bot = 1020;
    var _list_top = _ly + 45;
    var _rows_visible = max(1, floor((_max_bot - _list_top) / _row_h));

    // ---- MISC TAB (M 08-26): one read-only holdings list - reagents, dust,
    // ingots, forge parts, banshees, freed songs, unhatched eggs. Single
    // centered panel (x360-1560), cursor + VISIBLE scrollbar, no move verbs.
    // Row source = stash_misc_rows() (scr_stats); Step mirrors this geometry.
    if (_tab == 2) {
        var _mx0 = 360, _mx1 = 1560;
        draw_set_color(make_color_rgb(80, 160, 220));
        draw_rectangle(_mx0, _ly, _mx1, _max_bot, true);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(150, 205, 160));
        draw_text(_mx0 + 15, _ly + 9, "HOLDINGS  (safe - reagents, parts, songs, eggs)");
        var _mrows = stash_misc_rows();
        var _mn    = array_length(_mrows);
        var _mscr  = clamp(_gc.stash_scroll, 0, max(0, _mn - _rows_visible));
        var _my_   = _list_top;
        for (var _mi = _mscr; _mi < _mn; _mi++) {
            if (_my_ + _row_h > _max_bot) break;
            var _mr    = _mrows[_mi];
            var _m_sel = (_gc.stash_mode_index == _mi);
            draw_set_alpha(_m_sel ? 0.9 : 0.5);
            draw_set_color(_m_sel ? make_color_rgb(30, 50, 80) : make_color_rgb(18, 22, 38));
            draw_rectangle(_mx0 + 6, _my_, _mx1 - 40, _my_ + _row_h - 3, false);
            draw_set_alpha(1.0);
            // 44px icon; spr -1 (art not yet imported) = diamond placeholder.
            if (_mr.spr >= 0) {
                draw_sprite_stretched(_mr.spr, 0, _mx0 + 16, _my_ + 12, 44, 44);
            } else {
                draw_set_color(_mr.col);
                draw_rectangle(_mx0 + 26, _my_ + 22, _mx0 + 50, _my_ + 46, true);
                draw_line(_mx0 + 26, _my_ + 34, _mx0 + 38, _my_ + 22);
                draw_line(_mx0 + 38, _my_ + 22, _mx0 + 50, _my_ + 34);
            }
            var _mtx = _mx0 + 76;
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_mr.col);
            ui_draw_text_fit(_mtx, _my_ + 8, _mr.name + ((_mr.count >= 0) ? ("  x" + string(_mr.count)) : ""), (_mx1 - 52) - _mtx);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(140, 150, 170));
            ui_draw_stat_line_fit(_mtx, _my_ + 39, _mr.sub, (_mx1 - 52) - _mtx);
            _my_ += _row_h;
        }
        // VISIBLE scrollbar (M's standing scroll rule) + position readout.
        ui_pb_scrollbar(_mx1 - 28, _list_top, _max_bot - 3, _mn, _rows_visible, _mscr);
        if (_mn > _rows_visible) {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_halign(fa_center);
            draw_set_color(make_color_rgb(150, 170, 200));
            draw_text((_mx0 + _mx1) / 2, _max_bot + 2, string(_gc.stash_mode_index + 1) + " / " + string(_mn));
            draw_set_halign(fa_left);
        }
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(c_gray);
        ui_draw_key_legend(960, 1026, "Q/E: Category   W/S: Navigate   Esc: Close");
        draw_set_halign(fa_left);
        return;
    }

    // Both columns show only the active tab's category. Consumables collapse
    // into name-groups with an xN count (one row per kind, not per copy).
    var _left_items   = (_tab == 0) ? global.carried_items   : global.consumable_inventory;
    var _right_items  = (_tab == 0) ? global.equipment_stash : global.consumable_stash;
    var _left_groups  = (_tab == 1) ? ui_consumable_groups(_left_items)  : undefined;
    var _right_groups = (_tab == 1) ? ui_consumable_groups(_right_items) : undefined;

    var _left_active  = (_gc.stash_mode_side == 0);
    var _right_active = (_gc.stash_mode_side == 1);

    // ---- LEFT COLUMN ----
    var _lx = 45;
    draw_set_color(_left_active ? make_color_rgb(80, 160, 220) : make_color_rgb(45, 55, 75));
    draw_rectangle(_lx, _ly, _lx + _col_w, _max_bot, true);

    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(200, 100, 80));
    draw_text(_lx + 15, _ly + 9, "TAKING ON RUN  (at risk)");

    // Scroll window: keep the selection in view (the list follows the cursor
    // instead of the cursor scrolling off-screen). Only the active side tracks
    // the cursor; the inactive side shows from the top.
    var _left_n      = (_tab == 1) ? array_length(_left_groups) : array_length(_left_items);
    // Active column uses the edge-triggered stash_scroll (cursor moves, list shifts only
    // at the edges); the inactive column just shows from the top.
    var _left_scroll = 0;
    if (_left_active) {
        _left_scroll = clamp(_gc.stash_scroll, 0, max(0, _left_n - _rows_visible));
    }

    var _item_y = _list_top;
    for (var _i = _left_scroll; _i < _left_n; _i++) {
        if (_item_y + _row_h > _max_bot) break;
        var _cnt    = (_tab == 1) ? _left_groups[_i].count : 1;
        var _it     = (_tab == 1) ? _left_groups[_i].item  : _left_items[_i];
        var _is_sel = (_left_active && _gc.stash_mode_index == _i);

        draw_set_alpha(_is_sel ? 0.9 : 0.5);
        draw_set_color(_is_sel ? make_color_rgb(30, 50, 80) : make_color_rgb(18, 22, 38));
        draw_rectangle(_lx + 6, _item_y, _lx + _col_w - 6, _item_y + _row_h - 3, false);
        draw_set_alpha(1.0);

        var _col = (_tab == 1) ? ui_consumable_name_color(_it) : item_rarity_color(_it.rarity);   // Genie Lamp golds on the consumables tab
        if (_tab == 0) ui_draw_item_icon(_lx + 12, _item_y + 8, 30, _it);
        else           ui_draw_consumable_icon(_lx + 12, _item_y + 8, 30, _it);
        var _stl_tx = _lx + 51;
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_col);
        ui_draw_text_fit(_stl_tx, _item_y + 8, _it.name + ((_cnt > 1) ? ("  x" + string(_cnt)) : ""), (_lx + _col_w - 12) - _stl_tx);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(140, 150, 170));
        // Shrink-to-fit so a busy many-affix item can't run off the column edge
        // (M 07-17: was plain draw_text, overflowed sideways).
        ui_draw_stat_line_fit(_stl_tx, _item_y + 39, (_tab == 1) ? _it.description : ui_item_stat_str(_it), (_lx + _col_w - 12) - _stl_tx);

        _item_y += _row_h;
    }
    // Active column: position readout at the bottom (e.g. "12 / 40"), matching the other
    // lists. Inactive column: a plain "more" below when its list overflows the view.
    if (_left_active) {
        if (_left_n > _rows_visible) {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_halign(fa_center);
            draw_set_color(make_color_rgb(150, 170, 200));
            draw_text(_lx + _col_w / 2, _max_bot + 2, string(_gc.stash_mode_index + 1) + " / " + string(_left_n));
            draw_set_halign(fa_left);
        }
    } else if (_left_scroll + _rows_visible < _left_n) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_halign(fa_right);
        draw_set_color(make_color_rgb(150, 170, 200));
        ui_draw_scroll_more(_lx + _col_w - 14, _max_bot + 2, false, "more");
        draw_set_halign(fa_left);
    }
    if (array_length(_left_items) == 0) {
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(70, 80, 100));
        draw_text(_lx + 18, _ly + 57, (_tab == 0) ? "No equipment in pack." : "No consumables in pack.");
    }

    // ---- RIGHT COLUMN ----
    var _rx = 1020;
    draw_set_color(_right_active ? make_color_rgb(80, 160, 220) : make_color_rgb(45, 55, 75));
    draw_rectangle(_rx, _ly, _rx + _col_w, _max_bot, true);

    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(100, 200, 100));
    draw_text(_rx + 15, _ly + 9, "STASH  (safe)");

    var _right_n      = (_tab == 1) ? array_length(_right_groups) : array_length(_right_items);
    var _right_scroll = 0;
    if (_right_active) {
        _right_scroll = clamp(_gc.stash_scroll, 0, max(0, _right_n - _rows_visible));
    }

    _item_y = _list_top;
    for (var _i = _right_scroll; _i < _right_n; _i++) {
        if (_item_y + _row_h > _max_bot) break;
        var _cnt    = (_tab == 1) ? _right_groups[_i].count : 1;
        var _it     = (_tab == 1) ? _right_groups[_i].item  : _right_items[_i];
        var _is_sel = (_right_active && _gc.stash_mode_index == _i);

        draw_set_alpha(_is_sel ? 0.9 : 0.5);
        draw_set_color(_is_sel ? make_color_rgb(30, 50, 80) : make_color_rgb(18, 22, 38));
        draw_rectangle(_rx + 6, _item_y, _rx + _col_w - 6, _item_y + _row_h - 3, false);
        draw_set_alpha(1.0);

        var _col = (_tab == 1) ? ui_consumable_name_color(_it) : item_rarity_color(_it.rarity);   // Genie Lamp golds on the consumables tab
        if (_tab == 0) ui_draw_item_icon(_rx + 12, _item_y + 8, 30, _it);
        else           ui_draw_consumable_icon(_rx + 12, _item_y + 8, 30, _it);
        var _str_tx = _rx + 51;
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_col);
        ui_draw_text_fit(_str_tx, _item_y + 8, _it.name + ((_cnt > 1) ? ("  x" + string(_cnt)) : ""), (_rx + _col_w - 12) - _str_tx);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(140, 150, 170));
        // Shrink-to-fit so a busy many-affix item can't run off the column edge (M 07-17).
        ui_draw_stat_line_fit(_str_tx, _item_y + 39, (_tab == 1) ? _it.description : ui_item_stat_str(_it), (_rx + _col_w - 12) - _str_tx);

        _item_y += _row_h;
    }
    if (_right_active) {
        if (_right_n > _rows_visible) {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_halign(fa_center);
            draw_set_color(make_color_rgb(150, 170, 200));
            draw_text(_rx + _col_w / 2, _max_bot + 2, string(_gc.stash_mode_index + 1) + " / " + string(_right_n));
            draw_set_halign(fa_left);
        }
    } else if (_right_scroll + _rows_visible < _right_n) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_halign(fa_right);
        draw_set_color(make_color_rgb(150, 170, 200));
        ui_draw_scroll_more(_rx + _col_w - 14, _max_bot + 2, false, "more");
        draw_set_halign(fa_left);
    }
    if (array_length(_right_items) == 0) {
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(70, 80, 100));
        draw_text(_rx + 18, _ly + 57, (_tab == 0) ? "No equipment in stash." : "No consumables in stash.");
    }

    // Footer (raised to clear the bottom rim band)
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(c_gray);
    ui_draw_key_legend(960, 1026, "Q/E: Category   A/D: Switch Side   W/S: Navigate   Enter: Move Item   Esc: Close");
    draw_set_halign(fa_left);

    // HOARD STRIP (M 07-17): the tracked currencies no single vendor lists in full -
    // Reforge Ingots per tier + banked Banshees - shown here so you can witness the
    // whole hoard of wealth. (Chip bar is suppressed on the stash, so the bottom is
    // free.) Only drawn when you actually hold something.
    var _hoard_ing = reforge_ingot_total();
    var _hoard_ban = variable_global_exists("banshee_banked") ? global.banshee_banked : 0;
    if (_hoard_ing > 0 || _hoard_ban > 0) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_valign(fa_middle);
        var _hy = 1052, _hsz = 26, _hlbl = "HOARD:  ";
        var _hw = string_width(_hlbl);
        for (var _ht = 0; _ht < 5; _ht++) if (global.reforge_ingots[_ht] > 0) _hw += _hsz + 4 + string_width("x" + string(global.reforge_ingots[_ht])) + 18;
        if (_hoard_ban > 0) _hw += _hsz + 4 + string_width("x" + string(_hoard_ban)) + 18;
        var _hx = 960 - _hw / 2;
        draw_set_halign(fa_left);
        draw_set_color(make_color_rgb(200, 185, 140));
        draw_text(_hx, _hy, _hlbl); _hx += string_width(_hlbl);
        for (var _ht = 0; _ht < 5; _ht++) {
            if (global.reforge_ingots[_ht] <= 0) continue;
            draw_sprite_stretched(reforge_ingot_sprite(_ht), 0, _hx, _hy - _hsz / 2, _hsz, _hsz);
            _hx += _hsz + 4;
            draw_set_color(item_rarity_color(_ht));
            var _hl = "x" + string(global.reforge_ingots[_ht]);
            draw_text(_hx, _hy, _hl); _hx += string_width(_hl) + 18;
        }
        if (_hoard_ban > 0) {
            draw_sprite_stretched(spr_icon_banshee_bottle, 0, _hx, _hy - _hsz / 2, _hsz, _hsz);
            _hx += _hsz + 4;
            draw_set_color(make_color_rgb(150, 235, 235));
            draw_text(_hx, _hy, "x" + string(_hoard_ban));
        }
        draw_set_valign(fa_top);
        draw_set_halign(fa_left);
    }
    draw_set_valign(fa_top);

    // Ornate gothic rim around the whole overlay. Columns (x105..960 / 1020..1875) and the
    // y123..1020 lists sit inside the opening (30,30)-(1890,1050); the tooltip draws on top after.
    ui_draw_gothic_frame(30, 30, 1890, 1050, 30);

    // Hover tooltip - scan both columns for the moused-over item
    var _hmx_st = device_mouse_x_to_gui(0);
    var _hmy_st = device_mouse_y_to_gui(0);
    var _st_hover = undefined;
    // Start at each column's scroll offset so hover matches the drawn rows
    // (scanning from 0 misattributed tooltips whenever a list was scrolled).
    var _hy_l = _list_top;
    for (var _sthi = _left_scroll; _sthi < array_length(_left_items) && _st_hover == undefined; _sthi++) {
        if (_hy_l + _row_h > _max_bot) break;
        if (_hmx_st >= _lx + 6 && _hmx_st < _lx + _col_w - 6
                && _hmy_st >= _hy_l && _hmy_st < _hy_l + _row_h - 3) {
            _st_hover = _left_items[_sthi];
        }
        _hy_l += _row_h;
    }
    if (_st_hover == undefined) {
        var _hy_r = _list_top;
        for (var _sthi = _right_scroll; _sthi < array_length(_right_items); _sthi++) {
            if (_hy_r + _row_h > _max_bot) break;
            if (_hmx_st >= _rx + 6 && _hmx_st < _rx + _col_w - 6
                    && _hmy_st >= _hy_r && _hmy_st < _hy_r + _row_h - 3) {
                _st_hover = _right_items[_sthi];
                break;
            }
            _hy_r += _row_h;
        }
    }
    if (_st_hover != undefined) {
        ui_draw_item_tooltip(_hmx_st + 21, _hmy_st - 30, _st_hover, undefined);
        // Alt+click on an equipment item opens the comparison panel
        if (mouse_check_button_pressed(mb_left) && keyboard_check(vk_alt)
                && variable_struct_exists(_st_hover, "slot")) {
            _gc.comparison_item     = _st_hover;
            _gc.comparison_equipped = undefined;
            if (variable_global_exists("inventory")) {
                var _cmp_si = comparison_target_index(_st_hover);   // ring-aware target
                if (_cmp_si >= 0 && _cmp_si < array_length(global.inventory)) {
                    _gc.comparison_equipped = global.inventory[_cmp_si];
                }
            }
            _gc.comparison_open = true;
        }
    }

    draw_set_alpha(1.0);
    draw_set_font(-1);
}


// ---------------------------------------------------------------------------
// ui_draw_credits_page()
// Shared full-screen asset-credits page: scrim, header, sections, thank-you,
// gothic rim. Callers add their own key-legend footer. Drawn by the title
// screen's CREDITS menu option AND the ending's credits stage (WIN_STATE_SPEC).
// Content of record: CREDITS.md.
// ---------------------------------------------------------------------------
function ui_draw_credits_page() {
    draw_set_alpha(0.84);
    draw_set_color(make_color_rgb(6, 7, 12));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    draw_set_halign(fa_center);
    draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(130, 195, 255));
    draw_text(960, 84, "CREDITS");

    draw_set_alpha(0.4);
    draw_set_color(make_color_rgb(60, 100, 160));
    draw_rectangle(585, 176, 1335, 179, false);
    draw_set_alpha(1.0);

    var _cred = [
        ["MUSIC", [
            "Sara Garrard - \"Shadows/Infinity: Battle Zone\" & \"Infinity Crystal\"  (sonatina.itch.io)",
            "Alex Coldfire - \"Viking March\" & \"Rainy Memories\""]],
        ["SOUND", [
            "Chequered Ink - 400 Sounds Pack",
            "TomMusic - Free Fantasy SFX & Ambience"]],
        ["ICONS & EFFECTS", [
            "Batareya - ability, trait & item icon packs",
            "Will Tice / unTied Games - pixel art effects & projectiles",
            "Willibab - Simple Pixel Spell Effects",
            "Frostwindz - Necromancer VFX",
            "CraftPix - potion & mineral icons",
            "CaptainSkeleto - magic tome icons",
            "Medieval Weapons Pack - shield icons"]],
        ["FONTS", [
            "EB Garamond & Cinzel Decorative - SIL Open Font License"]],
        ["ART TOOLS", [
            "Character, item & world art created with PixelLab & MidJourney"]],
    ];

    // Tightened 08-11 (46/37/22 -> 42/33/16): the three new VFX-pack credit
    // rows pushed the old pitch past the gothic frame (thank-you line at y1078).
    // 14 rows / 5 sections now end at y966 - measured, inside the frame.
    var _cy = 208;
    for (var _cs = 0; _cs < array_length(_cred); _cs++) {
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(228, 190, 90));
        draw_text(960, _cy, _cred[_cs][0]);
        _cy += 42;
        var _crows = _cred[_cs][1];
        for (var _cr = 0; _cr < array_length(_crows); _cr++) {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(200, 205, 220));
            draw_text(960, _cy, _crows[_cr]);
            _cy += 33;
        }
        _cy += 16;
    }

    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 160, 185));
    draw_text(960, _cy + 6, "Thank you to every creator who shares their work.");

    draw_set_font(-1);
    ui_draw_gothic_frame(30, 30, 1890, 1050, 30);
    draw_set_halign(fa_left);
    draw_set_alpha(1.0);
}

// ---------------------------------------------------------------------------
// _cmp_stat_name(st)  - readable display name for an affix stat_type string
// ---------------------------------------------------------------------------
function _cmp_stat_name(st) {
    switch (st) {
        case "STR":       return "Strength";
        case "DEX":       return "Dexterity";
        case "CON":       return "Constitution";
        case "INT":       return "Intelligence";
        case "WIS":       return "Wisdom";
        case "CHA":       return "Charisma";
        case "armor":     return "Armor";
        case "el_resist": return "Elem. Resist";
        case "crit":      return "Crit Chance";
        case "dodge":     return "Dodge";
        case "max_hp":    return "Max HP";
        case "gold_find": return "Gold Find";
        default:          return st;
    }
}


// ---------------------------------------------------------------------------
// ui_draw_comparison_panel(new_item, equipped_item)
// Stat-delta comparison overlay centered on screen.
// equipped_item may be undefined (slot is empty).
// ---------------------------------------------------------------------------
function ui_draw_comparison_panel(new_item, equipped_item) {
    // Collect stat totals from each item's affixes
    var _sn = {};
    var _se = {};
    if (variable_struct_exists(new_item, "affixes")) {
        for (var _i = 0; _i < array_length(new_item.affixes); _i++) {
            var _a = new_item.affixes[_i];
            if (!variable_struct_exists(_sn, _a.stat_type)) _sn[$ _a.stat_type] = 0;
            _sn[$ _a.stat_type] += _a.stat_value;
        }
    }
    if (equipped_item != undefined && variable_struct_exists(equipped_item, "affixes")) {
        for (var _i = 0; _i < array_length(equipped_item.affixes); _i++) {
            var _a = equipped_item.affixes[_i];
            if (!variable_struct_exists(_se, _a.stat_type)) _se[$ _a.stat_type] = 0;
            _se[$ _a.stat_type] += _a.stat_value;
        }
    }

    // Union of all stat keys from both items
    var _keys = [];
    var _kn = variable_struct_get_names(_sn);
    for (var _i = 0; _i < array_length(_kn); _i++) array_push(_keys, _kn[_i]);
    var _ke = variable_struct_get_names(_se);
    for (var _i = 0; _i < array_length(_ke); _i++) {
        var _dup = false;
        for (var _j = 0; _j < array_length(_keys); _j++) {
            if (_keys[_j] == _ke[_i]) { _dup = true; break; }
        }
        if (!_dup) array_push(_keys, _ke[_i]);
    }

    // Panel sizing - grows with row count
    var _row_h  = 42;
    var _hdr_h  = 117;
    var _ftr_h  = 39;
    var _nrows  = max(array_length(_keys), 1);
    var _pw     = 750;
    var _ph     = _hdr_h + _nrows * _row_h + _ftr_h;
    var _px     = GUI_CX - _pw / 2;
    var _py     = GUI_CY - _ph / 2;
    var _mid    = _px + _pw / 2;
    var _cl     = _px + 15;
    var _cr     = _mid + 15;

    // Background and border
    draw_set_alpha(0.96);
    draw_set_color(make_color_rgb(10, 12, 22));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(90, 90, 140));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, true);

    // Title bar
    draw_set_color(make_color_rgb(55, 60, 95));
    draw_rectangle(_px, _py, _px + _pw, _py + 33, false);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(c_white);
    draw_set_halign(fa_center);
    draw_text(GUI_CX, _py + 6, "ITEM COMPARISON");

    // New item header (left half)
    var _nc = variable_struct_exists(new_item, "rarity") ? item_rarity_color(new_item.rarity) : c_white;
    draw_set_halign(fa_left);
    draw_set_color(_nc);
    ui_draw_stat_line_fit(_cl, _py + 39, new_item.name, _pw / 2 - 60);   // cursed titles shrink-to-fit
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(105, 115, 140));
    draw_text(_cl, _py + 66, string_upper(item_slot_title(new_item.slot)));
    draw_set_color(make_color_rgb(70, 190, 70));
    draw_text(_cl, _py + 90, "[New]");

    // Equipped item header (right half)
    if (equipped_item != undefined) {
        var _ec = variable_struct_exists(equipped_item, "rarity") ? item_rarity_color(equipped_item.rarity) : c_white;
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_ec);
        ui_draw_stat_line_fit(_cr, _py + 39, equipped_item.name, _pw / 2 - 60);   // cursed titles shrink-to-fit
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(105, 115, 140));
        draw_text(_cr, _py + 66, string_upper(item_slot_title(equipped_item.slot)));
        draw_set_color(make_color_rgb(200, 180, 60));
        draw_text(_cr, _py + 90, "[Equipped]");
    } else {
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(95, 95, 105));
        draw_text(_cr, _py + 39, "Nothing Equipped");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(65, 65, 75));
        draw_text(_cr, _py + 66, string_upper(item_slot_title(new_item.slot)) + " slot empty");
    }

    // Divider lines
    draw_set_color(make_color_rgb(55, 60, 95));
    draw_line(_px + 9, _py + _hdr_h - 6, _px + _pw - 9, _py + _hdr_h - 6);
    draw_line(_mid,    _py + _hdr_h,      _mid,           _py + _ph - _ftr_h);

    // Stat rows
    draw_set_font(ui_font(fnt_ui_small));
    if (array_length(_keys) == 0) {
        draw_set_color(make_color_rgb(95, 95, 115));
        draw_set_halign(fa_center);
        draw_text(GUI_CX, _py + _hdr_h + 12, "No stat affixes");
    }
    for (var _i = 0; _i < array_length(_keys); _i++) {
        var _sk    = _keys[_i];
        var _ry    = _py + _hdr_h + _i * _row_h;
        var _nv    = variable_struct_exists(_sn, _sk) ? _sn[$ _sk] : 0;
        var _ev    = variable_struct_exists(_se, _sk) ? _se[$ _sk] : 0;
        var _delta = _nv - _ev;

        // Stat name
        draw_set_color(make_color_rgb(170, 175, 200));
        draw_set_halign(fa_left);
        draw_text(_cl, _ry + 9, _cmp_stat_name(_sk));

        // New item value (right edge of left half)
        draw_set_color(c_white);
        draw_set_halign(fa_right);
        draw_text(_mid - 12, _ry + 9, (_nv > 0 ? "+" : "") + string(_nv));

        // Equipped value (left edge of right half)
        if (equipped_item != undefined) {
            draw_set_color(make_color_rgb(150, 150, 160));
            draw_set_halign(fa_left);
            draw_text(_cr, _ry + 9, (_ev > 0 ? "+" : "") + string(_ev));
        }

        // Delta (far right, color-coded)
        if (_delta != 0) {
            var _dcol = (_delta > 0) ? make_color_rgb(80, 230, 80) : make_color_rgb(230, 80, 80);
            draw_set_color(_dcol);
            draw_set_halign(fa_right);
            // #9: drawn triangle instead of the letter arrow "^ / v".
            draw_text(_px + _pw - 28, _ry + 9, ((_delta > 0) ? "+" : "") + string(_delta));
            draw_set_halign(fa_left);
            ui_draw_scroll_more(_px + _pw - 24, _ry + 9, _delta > 0);
            draw_set_halign(fa_right);
        } else {
            draw_set_color(make_color_rgb(120, 120, 135));
            draw_set_halign(fa_right);
            draw_text(_px + _pw - 12, _ry + 9, "=");
        }
    }

    // Footer close hint
    draw_set_halign(fa_center);
    draw_set_color(make_color_rgb(95, 95, 115));
    draw_text(GUI_CX, _py + _ph - 30, "Alt+Click or ESC to close");

    draw_set_halign(fa_left);
    draw_set_alpha(1.0);
    draw_set_font(-1);
}


// ---------------------------------------------------------------------------
// ui_draw_trainer_screen()
// Full-screen overlay for Vex the Trainer (trainer_open). Four sections:
//   tab 0 Stats - 200g + a Rare+ item per +1 permanent stat
//   tab 1 Trait Slots - 800g / 2000g for +1 / +2 active-trait slots
//   tab 2 Abilities - 500g to unlock a non-starter ability into the loadout pool
//   tab 4 Potency - V2 tiered ranks on EVERY trait, rank 5 = Transcend
//   tab 5 Reweave - unweave an ability's talent web (100g + 10 dust)
// Drawn by obj_hub_controller Draw_64 after ui_draw_shop_screen().
// ---------------------------------------------------------------------------
function ui_draw_trainer_screen() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "trainer_open") || !_gc.trainer_open) return;

    var _accent   = make_color_rgb(160, 115, 225);   // Vex violet
    var _class_id = variable_global_exists("chosen_class") ? global.chosen_class : 0;
    var _class_names = ["Arcanist", "Bloodwarden", "Shadowstrider"];

    // Full-screen dark cover
    draw_set_alpha(0.97);
    draw_set_color(make_color_rgb(8, 8, 16));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);

    // Title + gold
    draw_set_valign(fa_top);
    draw_set_halign(fa_center);
    draw_set_font(fnt_ui_title);
    draw_set_color(_accent);
    draw_text(960, 36, "VEX THE TRAINER");   // y36 keeps the title inside the rim opening
    draw_set_halign(fa_right);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(c_yellow);
    draw_text(1875, 36, "Gold: " + string(global.gold) + "g");        // pulled in to clear the right band
    ui_draw_reagent_readout(1875, 72);   // reagent readout under the gold (M 08-26)

    // Bond readout - watch services raise the relationship live.
    ui_draw_npc_bond("vex", 90, 45);
    ui_draw_npc_column("vex");   // reserved right-hand sprite column

    // --- Tab bar (5 tabs - REWEAVE moved to Vael, M 07-28; geometry MUST match
    // the mouse hit-test in obj_game_controller Step_0: 60 + t*360, 345 wide) ---
    var _tab_labels = ["STATS", "TRAIT SLOTS", "ABILITIES", "TRAITS", "POTENCY"];
    for (var _t = 0; _t < 5; _t++) {
        var _tx  = 60 + _t * 360;
        var _on  = (_gc.trainer_tab == _t);
        draw_set_color(_on ? make_color_rgb(28, 18, 48) : make_color_rgb(12, 13, 20));
        draw_rectangle(_tx, 96, _tx + 345, 144, false);
        draw_set_color(_on ? _accent : make_color_rgb(40, 40, 60));
        draw_rectangle(_tx, 96, _tx + 345, 144, true);
        draw_set_halign(fa_center);
        draw_set_color(_on ? c_white : make_color_rgb(95, 95, 125));
        draw_set_font(ui_font(fnt_ui));
        if (string_width(_tab_labels[_t]) > 325) draw_set_font(ui_font(fnt_ui_small));
        draw_text(_tx + 172, 108, _tab_labels[_t]);
    }
    draw_set_font(ui_font(fnt_ui));

    // --- Notification line ---
    if (_gc.trainer_notification != "") {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        var _is_warn = (string_pos("Beware", _gc.trainer_notification) > 0);
        var _is_bad  = (string_pos("Not enough", _gc.trainer_notification) > 0
                     || string_pos("Need", _gc.trainer_notification) > 0
                     || string_pos("need a", _gc.trainer_notification) > 0);
        draw_set_color(_is_warn ? make_color_rgb(255, 90, 90)
                     : (_is_bad ? make_color_rgb(230, 130, 70)
                                : make_color_rgb(120, 220, 140)));
        // y154: the notification owns the tab-bar..rows band (144..225) alone - the
        // class lines below yield to it, and the "more above" arrow sits at y201
        // (M screenshot 07-10: all three stacked on the abilities tab).
        draw_text(960, 154, _gc.trainer_notification);
    } else if (variable_global_exists("duelist_tokens") && global.duelist_tokens > 0) {
        // DUELIST ARTS (DESIGN_DUELIST_CHALLENGE.md): the hidden ladder reveals
        // itself here once the first token is held. Rides the idle notification
        // band (y154) - the two are mutually exclusive, so nothing collides.
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(205, 140, 95));
        var _da_t = global.duelist_tokens;
        var _da_line = "DUELIST ARTS  -  Tokens " + string(min(_da_t, 3)) + "/3:   "
            + "Measured Riposte" + ((_da_t >= 1) ? " [LEARNED]" : " [?]") + "    "
            + "Duelist's Poise"  + ((_da_t >= 2) ? " [LEARNED]" : " [a 2nd token...]") + "    "
            + "The Ashen Blade"  + ((_da_t >= 3) ? " [YOURS]"   : " [a 3rd token...]");
        draw_text(960, 154, _da_line);
        draw_set_font(ui_font(fnt_ui));
    }

    var _rx0 = 180;
    var _rx1 = 1500;   // narrowed from 1740 to clear the right-hand NPC sprite column
    var _ry0 = 225;
    var _rh  = 87;
    var _rgap = 9;

    draw_set_halign(fa_left);

    // =====================================================================
    // TAB 0: PERMANENT STAT UPGRADES
    // =====================================================================
    if (_gc.trainer_tab == 0) {
        var _stat_keys  = ["perm_str_bonus","perm_dex_bonus","perm_con_bonus","perm_int_bonus","perm_wis_bonus","perm_cha_bonus"];
        var _stat_names = ["STR","DEX","CON","INT","WIS","CHA"];
        for (var _i = 0; _i < 6; _i++) {
            var _ry  = _ry0 + _i * (_rh + _rgap);
            var _sel = (_gc.trainer_cursor == _i);
            var _cur = variable_global_exists(_stat_keys[_i]) ? variable_global_get(_stat_keys[_i]) : 0;

            draw_set_color(_sel ? make_color_rgb(26, 18, 44) : make_color_rgb(14, 15, 24));
            draw_rectangle(_rx0, _ry, _rx1, _ry + _rh, false);
            draw_set_color(_sel ? _accent : make_color_rgb(38, 40, 60));
            draw_rectangle(_rx0, _ry, _rx1, _ry + _rh, true);

            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_sel ? c_white : make_color_rgb(180, 185, 210));
            draw_text(_rx0 + 24, _ry + 9, _stat_names[_i] + "   (current permanent bonus: +" + string(_cur) + ")");
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(120, 125, 150));
            draw_text(_rx0 + 24, _ry + 45, "Raise this stat permanently by +1.");

            // Scaling price (08-11): same formula the purchase gate charges -
            // quadratic gold on stats bought + escalating trade rarity.
            draw_set_halign(fa_right);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(c_yellow);
            draw_text(_rx1 - 24, _ry + 9, string(vex_price(cha_price(vex_stat_base_cost())))
                + "g  +  1 " + item_rarity_name(vex_stat_rarity_req()) + "+ item");
            draw_set_halign(fa_left);
        }

        // Trade-item readout (rarity requirement follows the price ladder)
        var _req_rar = vex_stat_rarity_req();
        var _trade   = trainer_find_item(_req_rar);
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        if (_trade != undefined) {
            draw_set_color(make_color_rgb(120, 200, 140));
            draw_text(960, 840, "Trade item ready: " + _trade.item.name + "  (" + item_rarity_name(_trade.rarity) + ")  -  the lowest-value " + item_rarity_name(_req_rar) + "+ item is used.");
        } else {
            draw_set_color(make_color_rgb(210, 120, 70));
            draw_text(960, 840, "No " + item_rarity_name(_req_rar) + " or better item in your stash/pack to trade.");
        }
        draw_set_halign(fa_left);
    }

    // =====================================================================
    // TAB 1: TRAIT SLOT EXPANSION
    // =====================================================================
    else if (_gc.trainer_tab == 1) {
        var _bts   = variable_global_exists("bonus_trait_slots") ? global.bonus_trait_slots : 0;
        var _total = 2 + _bts;

        // Single action row (kept at the standard first-row position so the mouse
        // hit-test in obj_game_controller Step matches).
        var _ry  = _ry0;
        var _maxed = (_bts >= 4);   // M 07-16: cap raised +2 -> +4 bought slots
        var _slot_ladder = [800, 2000, 4000, 8000];
        var _cost  = cha_price(_slot_ladder[clamp(_bts, 0, 3)]);

        draw_set_color(_maxed ? make_color_rgb(16, 18, 26) : make_color_rgb(26, 18, 44));
        draw_rectangle(_rx0, _ry, _rx1, _ry + _rh, false);
        draw_set_color(_maxed ? make_color_rgb(40, 42, 58) : _accent);
        draw_rectangle(_rx0, _ry, _rx1, _ry + _rh, true);

        if (_maxed) {
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(110, 200, 130));
            draw_text(_rx0 + 24, _ry + 27, "All trait slots purchased - you have the maximum of 6.");
        } else {
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(c_white);
            draw_text(_rx0 + 24, _ry + 9, "Unlock trait slot #" + string(_total + 1));
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(120, 125, 150));
            draw_text(_rx0 + 24, _ry + 45, "Permanently raises your base active-trait slots to " + string(_total + 1) + ".");
            draw_set_halign(fa_right);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(c_yellow);
            draw_text(_rx1 - 24, _ry + 27, string(_cost) + "g");
            draw_set_halign(fa_left);
        }

        // Info block (below the row - text only, no hit-test)
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(190, 160, 240));
        draw_text(960, 420, "Active Trait Slots:  " + string(_total) + "   (base 2  +  " + string(_bts) + " purchased)");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(120, 125, 150));
        draw_text(960, 468, "Buy extra slots to equip more traits at once. Maximum +4 (6 total).");
        draw_set_color(make_color_rgb(90, 95, 120));
        draw_text(960, 504, "Stacks on top of Crown of the Hollow King while it is equipped.");
        draw_set_halign(fa_left);
    }

    // =====================================================================
    // TAB 2: ABILITY UNLOCKS
    // =====================================================================
    else if (_gc.trainer_tab == 2) {
        // Drawn only while no notification shows - the two share the header band
        // and collided with the confirm prompt + scroll arrow (M screenshot 07-10).
        if (_gc.trainer_notification == "") {
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(140, 145, 175));
            draw_text(960, 162, "Class: " + _class_names[clamp(_class_id, 0, 2)] + "   -   unlocked abilities can be slotted in your loadout.");
            draw_set_halign(fa_left);
        }

        var _locked = class_vex_purchasable(_class_id);
        if (array_length(_locked) == 0) {
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(110, 200, 130));
            draw_text(960, 495, "Every purchasable ability for this class is unlocked.");
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(110, 115, 145));
            draw_text(960, 540, "Some abilities unlock through progression instead - see your loadout.");
            draw_set_halign(fa_left);
        } else {
            // Taller rows than the other tabs so the full ability description fits
            // on two wrapped lines without spilling outside the row.
            var _ab_rh      = 117;
            var _ab_max_vis = 6;
            var _ab_scroll  = loadout_list_scroll(_gc.trainer_cursor, array_length(_locked), _ab_max_vis);
            for (var _i = _ab_scroll; _i < min(array_length(_locked), _ab_scroll + _ab_max_vis); _i++) {
                var _ab   = _locked[_i];
                var _ry   = _ry0 + (_i - _ab_scroll) * (_ab_rh + _rgap);
                var _sel  = (_gc.trainer_cursor == _i);
                var _cost = ability_unlock_cost(_ab.name);

                draw_set_color(_sel ? make_color_rgb(26, 18, 44) : make_color_rgb(14, 15, 24));
                draw_rectangle(_rx0, _ry, _rx1, _ry + _ab_rh, false);
                draw_set_color(_sel ? _accent : make_color_rgb(38, 40, 60));
                draw_rectangle(_rx0, _ry, _rx1, _ry + _ab_rh, true);

                // Role-category accent bar on the left edge (SYSTEMS_ABILITY_SYNERGY.md).
                draw_set_color(ability_category_color(ability_category(_ab)));
                draw_rectangle(_rx0, _ry, _rx0 + 6, _ry + _ab_rh, false);

                // Ability icon - 84x84 badge on the left of the row
                draw_set_alpha(1.0);
                ui_draw_ability_icon(_rx0 + 17, _ry + 17, 84, _ab);
                var _ab_textx = _rx0 + 17 + 84 + 18;

                draw_set_font(ui_font(fnt_ui));
                draw_set_color(_sel ? c_white : make_color_rgb(180, 185, 210));
                draw_text(_ab_textx, _ry + 12, _ab.name);
                // Canonical full description (damage clause + every effect) plus the
                // melee/ranged attack-class tag - the complete explanation, wrapped.
                var _ab_desc = ability_describe(_ab);
                var _ab_tag  = ability_attack_class_tag(_ab);
                if (_ab_tag != "") _ab_desc = (_ab_desc != "") ? (_ab_desc + "  " + _ab_tag) : _ab_tag;
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(135, 142, 170));
                draw_text_ext(_ab_textx, _ry + 48, _ab_desc, -1, (_rx1 - 24) - _ab_textx);

                draw_set_halign(fa_right);
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(c_yellow);
                draw_text(_rx1 - 24, _ry + 12, "[" + string(_ab.energy_cost) + " AP]   " + string(_cost) + "g");
                draw_set_halign(fa_left);
            }
            draw_set_font(ui_font(fnt_ui_small));
            if (_ab_scroll > 0) {
                draw_set_halign(fa_center); draw_set_color(_accent);
                ui_draw_scroll_more(960, _ry0 - 24, true, "more above");
            }
            if (_ab_scroll + _ab_max_vis < array_length(_locked)) {
                draw_set_halign(fa_center); draw_set_color(_accent);
                ui_draw_scroll_more(960, _ry0 + _ab_max_vis * (_ab_rh + _rgap) - 3, false, "more below");
            }
            draw_set_halign(fa_left);
        }
    }

    // =====================================================================
    // TAB 3: TRAIT UNLOCKS (gold + a rarity-matched item)
    // =====================================================================
    else if (_gc.trainer_tab == 3) {
        // Same yield-to-notification rule as the abilities tab (07-10).
        if (_gc.trainer_notification == "") {
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(140, 145, 175));
            draw_text(960, 162, "Class: " + _class_names[clamp(_class_id, 0, 2)]
                + "   -   unlocked traits can be equipped at the Dungeon Gate.");
            draw_set_halign(fa_left);
        }

        var _tr_locked = trait_vex_purchasable(_class_id);
        if (array_length(_tr_locked) == 0) {
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(110, 200, 130));
            draw_text(960, 495, "Every trait available to this class is unlocked.");
            draw_set_halign(fa_left);
        } else {
            // Window to 7 rows (not 8) so the bottom trade-item readout at y=626
            // never overlaps the last row. Must match the Step hit-test window.
            var _tr_max_vis = 7;
            var _tr_scroll  = loadout_list_scroll(_gc.trainer_cursor, array_length(_tr_locked), _tr_max_vis);
            for (var _i = _tr_scroll; _i < min(array_length(_tr_locked), _tr_scroll + _tr_max_vis); _i++) {
                var _tt    = _tr_locked[_i];
                var _ry    = _ry0 + (_i - _tr_scroll) * (_rh + _rgap);
                var _sel   = (_gc.trainer_cursor == _i);
                var _tcost = trait_unlock_cost(_tt.name);
                var _afford = (global.gold >= _tcost.gold) && trainer_has_item(_tcost.min_rarity);

                draw_set_color(_sel ? make_color_rgb(26, 18, 44) : make_color_rgb(14, 15, 24));
                draw_rectangle(_rx0, _ry, _rx1, _ry + _rh, false);
                draw_set_color(_sel ? _accent : make_color_rgb(38, 40, 60));
                draw_rectangle(_rx0, _ry, _rx1, _ry + _rh, true);

                // Trait icon badge (selected row full, others slightly dimmed).
                draw_set_alpha(_sel ? 1.0 : 0.8);
                ui_draw_trait_icon(_rx0 + 12, _ry + 11, 64, _tt);
                draw_set_alpha(1.0);

                draw_set_font(ui_font(fnt_ui));
                draw_set_color(_sel ? c_white : make_color_rgb(180, 185, 210));
                draw_text(_rx0 + 92, _ry + 9, _tt.name
                    + (_tt.class_req != -1 ? "   (" + _class_names[clamp(_tt.class_req, 0, 2)] + ")" : ""));
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(120, 125, 150));
                draw_text(_rx0 + 92, _ry + 45, _tt.description);

                draw_set_halign(fa_right);
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(_afford ? c_yellow : make_color_rgb(150, 90, 90));
                draw_text(_rx1 - 24, _ry + 9, string(_tcost.gold) + "g");
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(_afford ? make_color_rgb(180, 160, 230) : make_color_rgb(150, 90, 90));
                draw_text(_rx1 - 24, _ry + 45, "+ 1 " + _tcost.item_label + " item");
                draw_set_halign(fa_left);
            }
            draw_set_font(ui_font(fnt_ui_small));
            if (_tr_scroll > 0) {
                draw_set_halign(fa_center); draw_set_color(_accent);
                ui_draw_scroll_more(960, _ry0 - 24, true, "more above");
            }
            if (_tr_scroll + _tr_max_vis < array_length(_tr_locked)) {
                draw_set_halign(fa_center); draw_set_color(_accent);
                ui_draw_scroll_more(960, _ry0 + _tr_max_vis * (_rh + _rgap) - 3, false, "more below");
            }
            draw_set_halign(fa_left);
        }

        // Trade-item readout (mirrors the Stats tab)
        var _tr_sel_cost = (array_length(_tr_locked) > 0)
            ? trait_unlock_cost(_tr_locked[clamp(_gc.trainer_cursor, 0, array_length(_tr_locked) - 1)].name)
            : { gold:0, min_rarity:2, item_label:"Rare" };
        var _tr_trade = trainer_find_item(_tr_sel_cost.min_rarity);
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        if (_tr_trade != undefined) {
            draw_set_color(make_color_rgb(120, 200, 140));
            draw_text(960, 939, "Trade item ready: " + _tr_trade.item.name + "  ("
                + item_rarity_name(_tr_trade.rarity) + ")  -  lowest-value " + _tr_sel_cost.item_label + "+ item is used.");
        } else {
            draw_set_color(make_color_rgb(210, 120, 70));
            draw_text(960, 939, "No " + _tr_sel_cost.item_label + " or better item in your stash/pack for this trait.");
        }
        draw_set_halign(fa_left);
    }

    // =====================================================================
    // TAB 4: TRAIT POTENCY (stat sacrifice)
    // =====================================================================
    else if (_gc.trainer_tab == 4) {
        // POTENCY V2 (SYSTEMS_POTENCY_V2.md): EVERY unlocked trait, windowed to
        // 8 rows (matching the Step_0 hit-test). Rank knob + Transcend from
        // trait_potency_info; next-rank cost from trait_potency_rank_cost.
        var _ups = trait_upgradable_list();
        // "LEARNED TRAITS" header (M 07-27): says out loud that this list is only
        // what you've unlocked - not the whole trait catalog.
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(190, 160, 240));
        draw_text(_rx0, 201, "LEARNED TRAITS  (" + string(array_length(_ups)) + ")  -  new traits join as you unlock them");
        var _pt_vis    = min(8, array_length(_ups));
        var _pt_scroll = loadout_list_scroll(_gc.trainer_cursor, array_length(_ups), 8);
        for (var _i = 0; _i < _pt_vis; _i++) {
            var _pidx = _pt_scroll + _i;
            var _up   = _ups[_pidx];
            var _ry   = _ry0 + _i * (_rh + _rgap);
            var _sel  = (_gc.trainer_cursor == _pidx);
            var _tier = trait_potency_tier(_up.name);
            var _pnfo = trait_potency_info(_up.name);

            draw_set_color(_sel ? make_color_rgb(26, 18, 44) : make_color_rgb(14, 15, 24));
            draw_rectangle(_rx0, _ry, _rx1, _ry + _rh, false);
            draw_set_color(_sel ? _accent : (_tier >= 5 ? make_color_rgb(150, 120, 60) : make_color_rgb(38, 40, 60)));
            draw_rectangle(_rx0, _ry, _rx1, _ry + _rh, true);

            // Trait icon badge (this list carries only name/effect, so resolve
            // the full trait struct for its effect_id).
            draw_set_alpha(_sel ? 1.0 : 0.8);
            ui_draw_trait_icon(_rx0 + 12, _ry + 11, 64, trait_get_by_name(_up.name));
            draw_set_alpha(1.0);

            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_sel ? c_white : make_color_rgb(180, 185, 210));
            draw_text(_rx0 + 92, _ry + 9, _up.name + ((_tier >= 5) ? ("   -   " + _pnfo.tname) : ""));
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(120, 125, 150));
            // Sub-line: the rank knob (or the Transcend once owned), fitted so it
            // can never run under the pips at +810.
            ui_draw_stat_line_fit(_rx0 + 92, _ry + 45,
                "Rank " + string(_tier) + "/5   -   " + ((_tier >= 5) ? _pnfo.teffect : _pnfo.knob),
                _rx0 + 800 - (_rx0 + 92));

            // Rank pips (the 5th glows gold - it is the Transcend)
            for (var _p = 0; _p < 5; _p++) {
                var _px2 = _rx0 + 810 + _p * 27;
                draw_set_color(_p < _tier ? ((_p == 4) ? make_color_rgb(230, 190, 90) : _accent) : make_color_rgb(45, 47, 66));
                draw_rectangle(_px2, _ry + 12, _px2 + 20, _ry + 33, false);
            }

            draw_set_halign(fa_right);
            if (_tier >= 5) {
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(make_color_rgb(230, 190, 90));
                draw_text(_rx1 - 24, _ry + 9, "TRANSCENDED");
            } else {
                var _pc = trait_potency_rank_cost(_tier + 1);
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(c_yellow);
                if (_pc.kind == "gold")
                    draw_text(_rx1 - 24, _ry + 9, "Rank " + string(_tier + 1) + ": " + string(vex_price(cha_price(_pc.gold))) + "g + " + string(_pc.dust) + " dust");
                else if (_pc.kind == "stats")
                    draw_text(_rx1 - 24, _ry + 9, "Rank " + string(_tier + 1) + ": " + string(_pc.points) + " stat points");
                else
                    draw_text(_rx1 - 24, _ry + 9, "TRANSCEND: an Epic offering");
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(120, 125, 150));
                draw_text(_rx1 - 24, _ry + 45, (_pc.kind == "item") ? _pnfo.tname : "Enter: raise the rank");
            }
            draw_set_halign(fa_left);
        }
        // Windowing notes (the 26-trait list runs past the 8 visible rows).
        if (_pt_scroll > 0 || _pt_scroll + _pt_vis < array_length(_ups)) {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(150, 160, 140));
            // Centered at 960 (tabs 2/3 idiom) - the LEARNED TRAITS header owns
            // the left slot at (_rx0, 201) since 07-28.
            if (_pt_scroll > 0) { draw_set_halign(fa_center); draw_text(960, 201, "^  more above"); draw_set_halign(fa_left); }
            if (_pt_scroll + _pt_vis < array_length(_ups) && !_gc.trainer_confirm)
                draw_text(_rx0, _ry0 + _pt_vis * (_rh + _rgap) + 3,
                    "v  " + string(array_length(_ups) - _pt_scroll - _pt_vis) + " more below");
        }

        // Confirmation bar (non-refundable sacrifice)
        if (_gc.trainer_confirm) {
            draw_set_color(make_color_rgb(50, 12, 12));
            draw_rectangle(_rx0, 987, _rx1, 1041, false);
            draw_set_color(make_color_rgb(200, 60, 60));
            draw_rectangle(_rx0, 987, _rx1, 1041, true);
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(255, 110, 110));
            draw_text_outline(960, 1002, "Beware, what you are about to do can not be undone.   [ Space ] Confirm     [ Esc ] Cancel");
            draw_set_halign(fa_left);
        }
    }

    // (TAB 5 REWEAVE moved to Vael the Aesthete - M 07-28. See ui_draw_vael_screen.)

    // --- Controls hint (raised to clear the bottom rim band). Hidden while the
    // sacrifice confirm bar is up: the bar (987-1041) owns this band and lists
    // its own keys (M 07-27 screenshot: the two collided). ---
    if (!(variable_instance_exists(_gc, "trainer_confirm") && _gc.trainer_confirm)) {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(70, 75, 100));
        ui_draw_key_legend(960, 1026, "W/S: Navigate    Q/E: Section    Enter: Buy / Select    Tab: Examine    F: Gift    U: Station    Esc: Close");
        draw_set_halign(fa_left);
    }

    // Ornate gothic rim around the whole overlay (matches the other NPC shops).
    ui_draw_gothic_frame(30, 30, 1890, 1050, 30);

    // --- Tab detail popup over the Abilities (2) / Traits (3) / Potency (4) tabs ---
    if (variable_instance_exists(_gc, "vex_detail_open") && _gc.vex_detail_open) {
        if (_gc.trainer_tab == 2) {
            var _vd_ap = class_vex_purchasable(_class_id);
            if (_gc.trainer_cursor < array_length(_vd_ap)) ui_draw_ability_detail(_vd_ap[_gc.trainer_cursor], "Tab",
                variable_instance_exists(_gc, "vex_detail_scroll") ? _gc.vex_detail_scroll : 0);
        } else if (_gc.trainer_tab == 3) {
            var _vd_tp = trait_vex_purchasable(_class_id);
            if (_gc.trainer_cursor < array_length(_vd_tp)) ui_draw_trait_detail(_vd_tp[_gc.trainer_cursor]);
        } else if (_gc.trainer_tab == 4) {
            ui_draw_potency_detail();
        }
    }

    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_trainer_statpick()
// Vex tab-4 popup: choose a stat to sacrifice 5 permanent points from for a trait
// potency upgrade. Lists all 6 stats with available points (base allocation +
// bought bonus). Geometry MUST match the hit-testing in obj_game_controller/
// Step_0 (_sp_px 440, _sp_pw 400, _sp_y0 250, _sp_rh 40). No-op when not open.
// ---------------------------------------------------------------------------
function ui_draw_trainer_statpick() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "trainer_statpick_open") || !_gc.trainer_statpick_open) return;

    var _stats = ["STR", "DEX", "CON", "INT", "WIS", "CHA"];
    var _alloc = (variable_instance_exists(_gc, "trainer_statpick_alloc")
                  && array_length(_gc.trainer_statpick_alloc) == 6)
                 ? _gc.trainer_statpick_alloc : [0, 0, 0, 0, 0, 0];
    var _total = 0;
    for (var _t = 0; _t < 6; _t++) _total += _alloc[_t];

    // Geometry MUST match the hit-testing in obj_game_controller/Step_0.
    var _px = 630, _pw = 660, _py = 255, _ph = 660, _y0 = 375, _rh = 60;
    var _minus_x = _px + _pw - 144;
    var _plus_x  = _px + _pw - 72;

    // Dim + panel
    draw_set_alpha(0.6); draw_set_color(c_black);
    draw_rectangle(0, 0, display_get_gui_width(), display_get_gui_height(), false);
    draw_set_alpha(0.97); draw_set_color(make_color_rgb(22, 20, 34));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, false);
    draw_set_alpha(1.0); draw_set_color(make_color_rgb(150, 140, 180));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, true);

    // Header - wrapped so long trait names never overflow the panel.
    var _hw = _pw - 48;
    draw_set_halign(fa_center); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(255, 225, 150));
    draw_text_ext(_px + _pw / 2, _py + 18, "Raise " + _gc.trainer_statpick_trait + " potency", -1, _hw);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(c_ltgray);
    var _spd_need = (variable_instance_exists(_gc, "trainer_statpick_need"))
        ? _gc.trainer_statpick_need : vex_potency_points();
    draw_text_ext(_px + _pw / 2, _py + 66, "Spend " + string(_spd_need) + " points total - use - / + on any stats"
        + (affinity_at_least("vex", 3) ? "  (Vex's favor: one less)" : ""), -1, _hw);
    draw_set_halign(fa_left);

    // Stat rows: name - (have N) - [-] alloc [+]
    for (var _i = 0; _i < 6; _i++) {
        var _ry    = _y0 + _i * _rh;
        var _have  = stat_available_points(_stats[_i]);
        var _a     = _alloc[_i];
        var _sel   = (_gc.trainer_statpick_cursor == _i);
        var _can_inc = (_total < 5 && _a < _have);

        if (_sel) {
            draw_set_alpha(0.30); draw_set_color(make_color_rgb(120, 100, 160));
            draw_rectangle(_px + 24, _ry, _px + _pw - 24, _ry + _rh - 6, false);
            draw_set_alpha(1.0);
            draw_set_color(make_color_rgb(170, 150, 210));
            draw_rectangle(_px + 24, _ry, _px + _pw - 24, _ry + _rh - 6, true);
        }

        // Stat name + how many points are available to spend.
        draw_set_halign(fa_left); draw_set_valign(fa_top);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color((_have > 0) ? c_white : make_color_rgb(120, 90, 90));
        draw_text(_px + 42, _ry + 12, _stats[_i]);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(120, 125, 150));
        draw_text(_px + 144, _ry + 12, "have " + string(_have));

        // [-] button (dim when nothing allocated).
        draw_set_color(_a > 0 ? make_color_rgb(200, 120, 120) : make_color_rgb(70, 60, 70));
        draw_rectangle(_minus_x, _ry + 9, _minus_x + 48, _ry + _rh - 15, true);
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        draw_text(_minus_x + 24, _ry + 12, "-");

        // Allocated amount.
        draw_set_color(_a > 0 ? c_yellow : make_color_rgb(120, 120, 140));
        draw_text((_minus_x + 48 + _plus_x) / 2, _ry + 12, string(_a));

        // [+] button (dim when capped by availability or the 5-point total).
        draw_set_color(_can_inc ? make_color_rgb(130, 200, 140) : make_color_rgb(60, 70, 60));
        draw_rectangle(_plus_x, _ry + 9, _plus_x + 48, _ry + _rh - 15, true);
        draw_text(_plus_x + 24, _ry + 12, "+");
        draw_set_halign(fa_left);
    }

    // Running total.
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_total == _spd_need ? make_color_rgb(120, 210, 130) : c_yellow);
    draw_text(_px + _pw / 2, _y0 + 6 * _rh + 9, "Total: " + string(_total) + " / " + string(_spd_need));
    draw_set_halign(fa_left);

    // Footer / confirm
    if (variable_instance_exists(_gc, "trainer_statpick_confirm") && _gc.trainer_statpick_confirm) {
        draw_set_alpha(0.95); draw_set_color(make_color_rgb(120, 40, 40));
        draw_rectangle(_px + 24, _py + _ph - 105, _px + _pw - 24, _py + _ph - 54, false);
        draw_set_alpha(1.0);
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small)); draw_set_color(c_white);
        draw_text_ext(_px + _pw / 2, _py + _ph - 96, "Sacrifice these " + string(_spd_need) + " points permanently? Cannot be undone.", -1, _pw - 60);
        draw_set_color(c_ltgray);
        ui_draw_key_legend(_px + _pw / 2, _py + _ph - 36, "Enter: Confirm     Esc: Back");
    } else {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small)); draw_set_color(c_ltgray);
        ui_draw_key_legend(_px + _pw / 2, _py + _ph - 45, "W/S: Stat    A/D or -/+: Adjust    Enter: Confirm    Esc: Cancel");
    }

    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white); draw_set_alpha(1.0);
    draw_set_font(-1);
}

// Number of Maren/Sable list rows that fit on screen at once (rows start at y=285,
// 72px each; the list must clear the notification y=999 + controls y=1026). Drives the
// scroll windowing in both the Maren input block (Step) and ui_draw_maren_screen.
// ---------------------------------------------------------------------------
// Vendor list metrics (M 08-14 font-size pass). Every vendor list row was a
// hard 72px pitch / 66px box tuned for the default font - the Large variants
// ran text onto the row borders (Maren Forge screenshot). These return the
// shipped numbers VERBATIM on Default/Small so those modes stay bit-identical,
// and grow from MEASURED font heights on Large. Draw AND the Step mouse
// hit-tests must both read them - never hard-code 72/66/9 again.
// ---------------------------------------------------------------------------
function ui_vendor_row_pitch() {
    // Measured in EVERY font mode (M 08-15: default rode the borders too -
    // "assign a dynamic fix to all border encasings to always space a margin").
    var _pf = draw_get_font();
    draw_set_font(ui_font(fnt_ui));       var _th = string_height("Ag");
    draw_set_font(ui_font(fnt_ui_small)); var _bh = string_height("Ag");
    draw_set_font(_pf);
    // title line (4px inset) + gap + TWO effect lines + breathing room + 6px row
    // gap. (08-16, M: "item text should wrap into another row instead of
    // shrinking and becoming unreadable" - long descs like Devil Wine / chaotic
    // brews now wrap to a 2nd line, so every vendor row reserves it; lists
    // scroll, so fewer visible rows is the accepted trade.)
    return max(72, 4 + _th + 3 + 2 * _bh + 4 + 6);
}
// Body-line pitch for wrapped 2nd lines (measured small font).
function ui_vendor_body_lh() {
    var _pf = draw_get_font();
    draw_set_font(ui_font(fnt_ui_small)); var _bh = string_height("Ag");
    draw_set_font(_pf);
    return _bh;
}

// y-offset of the body/effect line under a row's title line, measured so the
// title can never overrun it (was a hard +38 tuned for the default font).
function ui_vendor_body_dy() {
    var _pf = draw_get_font();
    draw_set_font(ui_font(fnt_ui)); var _th = string_height("Ag");
    draw_set_font(_pf);
    return max(38, 6 + _th);
}

// Rows that fit the pixel budget the shipped layout gave _n_default rows
// (n * 72). Shrinks as the measured pitch grows.
function ui_vendor_visible_rows(_n_default = 9) {
    return max(3, (_n_default * 72) div ui_vendor_row_pitch());
}

function maren_visible_rows() { return ui_vendor_visible_rows(9); }

// Bottom-of-list "Showing X-Y of N (W/S to scroll)" hint + edge chevrons, drawn when a
// windowed Maren list overflows. _scroll = first visible index, _vis = window size.
function ui_maren_scroll_hint(_scroll, _vis, _count) {
    if (_count <= _vis) return;
    var _first = _scroll + 1;
    var _last  = min(_scroll + _vis, _count);
    draw_set_halign(fa_center);
    draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(165, 150, 195));
    var _txt = "Showing " + string(_first) + "-" + string(_last) + " of " + string(_count) + "   (W/S to scroll)";
    draw_text(960, 948, _txt);
    // #9: real triangles flank the line instead of the letter arrows "^ / v".
    draw_set_halign(fa_left);
    var _hw = string_width(_txt) / 2;
    if (_scroll > 0)             ui_draw_scroll_more(960 - _hw - 28, 948, true);
    if (_scroll + _vis < _count) ui_draw_scroll_more(960 + _hw + 14, 948, false);
}

// Draws one Maren list-row background (row index _i) and returns the text baseline y.
// _x1 lets a screen narrow the row (e.g. the Socket-gear list, so the item detail
// panel can sit clear to its right instead of overlapping the rows).
function ui_maren_row(_i, _selected, _base_y = 285, _x1 = 1500) {
    var _mp = ui_vendor_row_pitch();   // measured: title + 2 body lines (08-16)
    var _ry = _base_y + _i * _mp;
    draw_set_color(_selected ? make_color_rgb(45, 38, 66) : make_color_rgb(20, 18, 30));
    draw_rectangle(300, _ry, _x1, _ry + (_mp - 6), false);
    draw_set_color(_selected ? make_color_rgb(150, 110, 220) : make_color_rgb(45, 42, 62));
    draw_rectangle(300, _ry, _x1, _ry + (_mp - 6), true);
    return _ry + 15;
}

// Code-drawn faceted gem glyph (fallback when a rune's sprite icon isn't imported).
// Centered at (_cx,_cy), radius _r, in theme color _col.
function ui_draw_gem_glyph(_cx, _cy, _r, _col) {
    var _top = merge_color(_col, c_white, 0.45);
    var _dk  = merge_color(_col, c_black, 0.40);
    var _wx  = _r * 0.8;
    // Diamond body (upper + lower triangles).
    draw_set_color(_col);
    draw_triangle(_cx, _cy - _r, _cx - _wx, _cy, _cx + _wx, _cy, false);
    draw_triangle(_cx - _wx, _cy, _cx + _wx, _cy, _cx, _cy + _r, false);
    // Bright top facet.
    draw_set_color(_top);
    draw_triangle(_cx, _cy - _r, _cx - _r * 0.42, _cy - _r * 0.18, _cx + _r * 0.42, _cy - _r * 0.18, false);
    // Darker lower half for depth.
    draw_set_color(_dk);
    draw_triangle(_cx - _wx, _cy, _cx + _wx, _cy, _cx, _cy + _r, false);
    // Outline + center crease.
    draw_set_color(merge_color(_col, c_white, 0.25));
    draw_line(_cx, _cy - _r, _cx - _wx, _cy);
    draw_line(_cx - _wx, _cy, _cx, _cy + _r);
    draw_line(_cx, _cy + _r, _cx + _wx, _cy);
    draw_line(_cx + _wx, _cy, _cx, _cy - _r);
    draw_line(_cx - _wx, _cy, _cx + _wx, _cy);
}

// Code-drawn "gear dissolving to dust" glyph, style-matched to ui_draw_gem_glyph
// (flat facets + highlight). A sword lying DIAGONALLY (hilt up-right, tip
// down-left, so the whole silhouette reads) whose lower half is mid-dissolve:
// the blade breaks into drifting fragments and square motes that sink and fade.
// Sable Salvage-Gear badge.
function ui_draw_dissolve_glyph(_cx, _cy, _r, _col) {
    var _top = merge_color(_col, c_white, 0.50);
    var _dk  = merge_color(_col, c_black, 0.40);
    var _c   = 0.70711;
    // Blade axis (t: hilt -1 .. tip +1) and across-axis (w) unit vectors.
    var _ax = -_c, _ay = _c, _px = _c, _py = _c;

    // Pommel disc at the hilt end, with a small bright stud.
    var _pmx = _cx + _ax * (-0.92 * _r), _pmy = _cy + _ay * (-0.92 * _r);
    draw_set_color(_dk);
    draw_circle(_pmx, _pmy, _r * 0.13, false);
    draw_set_color(_top);
    draw_circle(_pmx, _pmy, _r * 0.05, false);

    // Grip: slim quad from the pommel to the guard.
    var _g0 = -0.84 * _r, _g1 = -0.56 * _r, _gw = 0.06 * _r;
    draw_set_color(_dk);
    draw_triangle(_cx + _ax*_g0 + _px*(-_gw), _cy + _ay*_g0 + _py*(-_gw),
                  _cx + _ax*_g0 + _px*( _gw), _cy + _ay*_g0 + _py*( _gw),
                  _cx + _ax*_g1 + _px*( _gw), _cy + _ay*_g1 + _py*( _gw), false);
    draw_triangle(_cx + _ax*_g0 + _px*(-_gw), _cy + _ay*_g0 + _py*(-_gw),
                  _cx + _ax*_g1 + _px*( _gw), _cy + _ay*_g1 + _py*( _gw),
                  _cx + _ax*_g1 + _px*(-_gw), _cy + _ay*_g1 + _py*(-_gw), false);

    // Crossguard: bar across the axis, lit on its upper edge.
    var _c0 = -0.56 * _r, _c1 = -0.44 * _r, _cw = 0.42 * _r;
    draw_set_color(_col);
    draw_triangle(_cx + _ax*_c0 + _px*(-_cw), _cy + _ay*_c0 + _py*(-_cw),
                  _cx + _ax*_c0 + _px*( _cw), _cy + _ay*_c0 + _py*( _cw),
                  _cx + _ax*_c1 + _px*( _cw), _cy + _ay*_c1 + _py*( _cw), false);
    draw_triangle(_cx + _ax*_c0 + _px*(-_cw), _cy + _ay*_c0 + _py*(-_cw),
                  _cx + _ax*_c1 + _px*( _cw), _cy + _ay*_c1 + _py*( _cw),
                  _cx + _ax*_c1 + _px*(-_cw), _cy + _ay*_c1 + _py*(-_cw), false);
    draw_set_color(_top);
    draw_line(_cx + _ax*_c0 + _px*(-_cw), _cy + _ay*_c0 + _py*(-_cw),
              _cx + _ax*_c0 + _px*( _cw), _cy + _ay*_c0 + _py*( _cw));

    // Blade, intact from the guard to just past centre: two-tone (lit edge on
    // one side, steel on the other), tapering toward the break.
    var _b0 = -0.44 * _r, _b1 = 0.14 * _r;
    var _bw0 = 0.15 * _r, _bw1 = 0.11 * _r;
    draw_set_color(_col);
    draw_triangle(_cx + _ax*_b0 + _px*(-_bw0), _cy + _ay*_b0 + _py*(-_bw0),
                  _cx + _ax*_b0,               _cy + _ay*_b0,
                  _cx + _ax*_b1,               _cy + _ay*_b1, false);
    draw_triangle(_cx + _ax*_b0 + _px*(-_bw0), _cy + _ay*_b0 + _py*(-_bw0),
                  _cx + _ax*_b1,               _cy + _ay*_b1,
                  _cx + _ax*_b1 + _px*(-_bw1), _cy + _ay*_b1 + _py*(-_bw1), false);
    draw_set_color(_top);
    draw_triangle(_cx + _ax*_b0,               _cy + _ay*_b0,
                  _cx + _ax*_b0 + _px*( _bw0), _cy + _ay*_b0 + _py*( _bw0),
                  _cx + _ax*_b1 + _px*( _bw1), _cy + _ay*_b1 + _py*( _bw1), false);
    draw_triangle(_cx + _ax*_b0,               _cy + _ay*_b0,
                  _cx + _ax*_b1 + _px*( _bw1), _cy + _ay*_b1 + _py*( _bw1),
                  _cx + _ax*_b1,               _cy + _ay*_b1, false);

    // Below the break the blade continues as detached, fading fragments.
    var _fr = [
        { t0: 0.22, t1: 0.40, w: 0.095, a: 0.85 },
        { t0: 0.50, t1: 0.62, w: 0.075, a: 0.60 },
        { t0: 0.72, t1: 0.80, w: 0.050, a: 0.35 }
    ];
    for (var _fi = 0; _fi < array_length(_fr); _fi++) {
        var _f  = _fr[_fi];
        var _f0 = _f.t0 * _r, _f1 = _f.t1 * _r, _fw = _f.w * _r;
        draw_set_alpha(_f.a);
        draw_set_color((_fi mod 2 == 0) ? _col : _top);
        draw_triangle(_cx + _ax*_f0 + _px*(-_fw), _cy + _ay*_f0 + _py*(-_fw),
                      _cx + _ax*_f0 + _px*( _fw), _cy + _ay*_f0 + _py*( _fw),
                      _cx + _ax*_f1 + _px*( _fw), _cy + _ay*_f1 + _py*( _fw), false);
        draw_triangle(_cx + _ax*_f0 + _px*(-_fw), _cy + _ay*_f0 + _py*(-_fw),
                      _cx + _ax*_f1 + _px*( _fw), _cy + _ay*_f1 + _py*( _fw),
                      _cx + _ax*_f1 + _px*(-_fw), _cy + _ay*_f1 + _py*(-_fw), false);
    }
    draw_set_alpha(1.0);

    // Motes shear off the dissolve zone and sink straight down, fading (loop).
    for (var _mi = 0; _mi < 6; _mi++) {
        var _mt = frac(current_time / 1500 + _mi / 6);
        var _mr = frac(sin((_mi + 1) * 91.7) * 4371.23);
        var _mtt = (0.20 + _mr * 0.55) * _r;                 // spawn along the broken zone
        var _mx = _cx + _ax * _mtt + (_mr * 2 - 1) * _r * 0.08;
        var _my = _cy + _ay * _mtt + _mt * _r * 0.65;
        var _ms = max(1.5, _r * 0.10 * (1 - _mt * 0.6));
        draw_set_alpha(0.9 * (1 - _mt));
        draw_set_color((_mi mod 2 == 0) ? _col : _top);
        draw_rectangle(_mx - _ms * 0.5, _my - _ms * 0.5, _mx + _ms * 0.5, _my + _ms * 0.5, false);
    }
    draw_set_alpha(1.0);
}

// Code-drawn "rune shattering" glyph, same facet language: the gem split down
// the middle, each half fully faceted (bright top facet, body, dark underside,
// and a lit fresh-break face along the crack), halves shoved apart with a gap
// that breathes; stray shards ring the break with pulsing glints.
// Sable Salvage-Runes badge.
function ui_draw_shatter_glyph(_cx, _cy, _r, _col) {
    var _top = merge_color(_col, c_white, 0.50);
    var _lit = merge_color(_col, c_white, 0.28);
    var _dk  = merge_color(_col, c_black, 0.40);
    var _g   = _r * (0.13 + 0.035 * sin(current_time / 600));   // crack gap breathes
    var _wx  = _r * 0.78;

    // ---- Left half (displaced down-left) ----
    var _lx = _cx - _g, _ly = _cy + _g * 0.55;
    // Top facet (catches light), body, dark underside - mirrors the intact gem.
    draw_set_color(_lit);
    draw_triangle(_lx, _ly - _r, _lx - _wx * 0.5, _ly - _r * 0.30, _lx, _ly - _r * 0.30, false);
    draw_set_color(_col);
    draw_triangle(_lx - _wx * 0.5, _ly - _r * 0.30, _lx - _wx, _ly, _lx, _ly, false);
    draw_triangle(_lx - _wx * 0.5, _ly - _r * 0.30, _lx, _ly, _lx, _ly - _r * 0.30, false);
    draw_set_color(_dk);
    draw_triangle(_lx - _wx, _ly, _lx, _ly, _lx - _r * 0.10, _ly + _r * 0.72, false);
    // Fresh-break face: thin bright sliver along the crack edge.
    draw_set_color(_top);
    draw_triangle(_lx, _ly - _r, _lx, _ly + _r * 0.55, _lx - _r * 0.07, _ly - _r * 0.15, false);

    // ---- Right half (displaced up-right) ----
    var _rx = _cx + _g, _ry = _cy - _g * 0.55;
    draw_set_color(_top);
    draw_triangle(_rx, _ry - _r, _rx + _wx * 0.5, _ry - _r * 0.30, _rx, _ry - _r * 0.30, false);
    draw_set_color(_lit);
    draw_triangle(_rx + _wx * 0.5, _ry - _r * 0.30, _rx + _wx, _ry, _rx, _ry, false);
    draw_triangle(_rx + _wx * 0.5, _ry - _r * 0.30, _rx, _ry, _rx, _ry - _r * 0.30, false);
    draw_set_color(_col);
    draw_triangle(_rx + _wx, _ry, _rx, _ry, _rx + _r * 0.10, _ry + _r * 0.72, false);
    // Fresh-break face on this side too.
    draw_set_color(_top);
    draw_triangle(_rx, _ry - _r, _rx, _ry + _r * 0.55, _rx + _r * 0.07, _ry - _r * 0.15, false);

    // ---- Shards ringing the break: varied sizes/orientations, fading outward ----
    var _shd = [
        { x: -0.92, y: -0.62, s: 0.16, f: 0, a: 0.95 },
        { x:  0.88, y: -0.72, s: 0.13, f: 1, a: 0.85 },
        { x:  1.00, y:  0.35, s: 0.17, f: 2, a: 0.80 },
        { x: -1.05, y:  0.30, s: 0.11, f: 1, a: 0.65 },
        { x:  0.35, y:  0.95, s: 0.10, f: 0, a: 0.55 }
    ];
    for (var _si = 0; _si < array_length(_shd); _si++) {
        var _s  = _shd[_si];
        var _sx = _cx + _s.x * _r, _sy = _cy + _s.y * _r, _ss = _s.s * _r;
        draw_set_alpha(_s.a);
        draw_set_color(_s.f == 0 ? _top : (_s.f == 1 ? _col : _lit));
        // Orientation varies with the index so they read as tumbling pieces.
        if (_si mod 3 == 0) {
            draw_triangle(_sx, _sy - _ss, _sx + _ss, _sy + _ss * 0.4, _sx - _ss * 0.5, _sy + _ss, false);
        } else if (_si mod 3 == 1) {
            draw_triangle(_sx - _ss, _sy, _sx + _ss * 0.6, _sy - _ss * 0.7, _sx + _ss * 0.4, _sy + _ss, false);
        } else {
            draw_triangle(_sx + _ss, _sy - _ss * 0.3, _sx - _ss * 0.4, _sy - _ss, _sx - _ss * 0.2, _sy + _ss * 0.9, false);
        }
    }
    draw_set_alpha(1.0);

    // ---- Glints: two tiny sparkles pulsing near the break ----
    for (var _gi = 0; _gi < 2; _gi++) {
        var _ga = 0.5 + 0.5 * sin(current_time / 450 + _gi * 2.4);
        var _gx = _cx + (_gi == 0 ? -_r * 0.30 : _r * 0.42);
        var _gy = _cy + (_gi == 0 ? -_r * 0.70 : _r * 0.55);
        draw_set_alpha(_ga * 0.9);
        draw_set_color(c_white);
        draw_rectangle(_gx - 1, _gy - 1, _gx + 1, _gy + 1, false);
        draw_set_alpha(_ga * 0.4);
        draw_rectangle(_gx - 2.5, _gy - 2.5, _gx + 2.5, _gy + 2.5, false);
    }
    draw_set_alpha(1.0);
}

// Draws a rune as an equipment-style entry: gem icon, then the rune NAME on the top
// line and its STAT EFFECT below (mirroring how gear rows read). _ty is the row text
// baseline returned by ui_maren_row. Optionally tags the rune's domain on the right.
function ui_draw_rune_entry(_x, _ty, rune, _show_domain_tag = false, _row_x1 = 1620) {
    var _ry   = _ty - 15;
    var _icon = rune_icon_sprite(rune.id);
    var _gcol = rune_glyph_color(rune.id);
    var _isz  = 48;
    var _ix   = _x + 6;
    var _iy   = _ry + 9;
    if (_icon != -1) {
        draw_sprite_stretched(_icon, 0, _ix, _iy, _isz, _isz);
    } else {
        ui_draw_gem_glyph(_ix + _isz * 0.5, _iy + _isz * 0.5, _isz * 0.5, _gcol);
    }
    var _tx = _ix + _isz + 18;
    draw_set_halign(fa_left);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_gcol);
    draw_text(_tx, _ry + 4, rune_title(rune));
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(225, 230, 240));
    draw_text(_tx, _ry + ui_vendor_body_dy(), rune_effect(rune));
    if (_show_domain_tag) {
        var _def   = rune_get(rune.id);
        var _isasp = (_def != undefined && _def.domain == "aspect");
        draw_set_halign(fa_right);
        draw_set_color(make_color_rgb(130, 125, 150));
        draw_text(_row_x1 - 24, _ry + 24, _isasp ? "Aspect" : "Gear");
        draw_set_halign(fa_left);
    }
    draw_set_font(ui_font(fnt_ui));
}

// ---------------------------------------------------------------------------
// ui_draw_maren_screen()
// Maren the Runesmith - rune socketing overlay (Phase 1: Socket gear + Runes).
// Layout constants MUST match the Maren input block in obj_game_controller Step.
// ---------------------------------------------------------------------------
function ui_draw_maren_screen() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "maren_open") || !_gc.maren_open) return;

    // Full-screen overlay
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(10, 8, 16));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);

    // Title + dust
    draw_set_halign(fa_center);
    draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(180, 150, 230));
    draw_text(960, 36, "Maren the Runesmith");
    draw_set_halign(fa_right);
    draw_set_font(ui_font(fnt_ui_small));
    var _dust = variable_global_exists("rune_dust") ? global.rune_dust : 0;
    // "Rune Dust:" label in light purple, value stays gold (M 2026-07-07,
    // every NPC dust readout - contrasts the gold like the resource itself).
    var _dust_num = " " + string(_dust);
    draw_set_color(make_color_rgb(200, 180, 130));
    draw_text(1860, 48, _dust_num);
    draw_set_color(make_color_rgb(195, 155, 255));
    draw_text(1860 - string_width(_dust_num), 48, "Rune Dust:");
    // #19 polish: dust glyph beside the label (shrine-header idiom, 24px).
    draw_sprite_stretched(spr_icon_dust, 0,
        1860 - string_width(_dust_num) - string_width("Rune Dust:") - 30, 46, 24, 24);
    // Player gold, under the dust readout - with the coin glyph (#19 polish).
    draw_set_color(c_yellow);
    var _mg_txt = "Gold: " + string(global.gold) + "g";
    draw_text(1860, 84, _mg_txt);
    draw_sprite_stretched(spr_icon_gold, 0, 1860 - string_width(_mg_txt) - 30, 82, 24, 24);
    ui_draw_reagent_readout(1860, 120);   // reagents under gold (M 08-26); x clears the y105 tab bar
    draw_set_halign(fa_left);

    // Bond readout - watch socketing raise the relationship live.
    ui_draw_npc_bond("maren", 90, 45);
    ui_draw_npc_column("maren");   // sprite column (rows + socket tooltip narrowed to clear it)

    // Tab bar (5 tabs) - x=368+t*240, y=105, w=225, h=60 (MUST match the mouse
    // hit-test in obj_game_controller Step's maren block)
    var _tab_labels = ["Socket Gear", "Aspects", "Runesmithing", "Runes", "Spirits"];   // Forge -> Runesmithing (M 08-15)
    // Hover quick-refs per tab (M 08-15) - stashed here, drawn topmost by the
    // hub controller's ui_draw_tab_tip() call.
    for (var _th = 0; _th < 5; _th++) {
        var _thx = 368 + _th * 240;
        var _thb = ui_tab_hint_body("maren:" + _tab_labels[_th]);
        if (_thb != "") ui_tab_hover_stash(_thx, 105, _thx + 225, 165, _tab_labels[_th], _thb);
    }
    draw_set_font(ui_font(fnt_ui));
    for (var _t = 0; _t < 5; _t++) {
        var _tx  = 368 + _t * 240;
        var _on  = (_gc.maren_tab == _t);
        draw_set_color(_on ? make_color_rgb(45, 35, 70) : make_color_rgb(22, 20, 34));
        draw_rectangle(_tx, 105, _tx + 225, 165, false);
        draw_set_color(_on ? make_color_rgb(150, 110, 220) : make_color_rgb(55, 50, 75));
        draw_rectangle(_tx, 105, _tx + 225, 165, true);
        draw_set_halign(fa_center);
        draw_set_valign(fa_middle);
        draw_set_color(_on ? c_white : make_color_rgb(150, 150, 170));
        // Tabs are narrower at 5-up: shrink-to-fit so "Socket Gear" can't kiss the borders.
        var _tab_sc = min(1.0, (225 - 24) / max(1, string_width(_tab_labels[_t])));
        draw_text_transformed(_tx + 113, 135, _tab_labels[_t], _tab_sc, _tab_sc, 0);
    }
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui));

    var _row_y0 = 285;
    var _list_x = 300;
    var _list_x2 = 1500;   // ui_maren_row boxes end at 1500 - 1620 left the right-
                           // aligned hints/costs hanging outside their row borders
    var _cursor = _gc.maren_cursor;
    var _scroll = _gc.maren_scroll;          // first visible row (list windowing)
    var _vis    = maren_visible_rows();

    if (_gc.maren_tab == 0) {
        // -------- SOCKET GEAR TAB --------
        var _slots = maren_socketable_slots();

        if (_gc.maren_phase == 0) {
            // Breadcrumb
            draw_set_color(make_color_rgb(140, 130, 165));
            draw_text(_list_x, 225, "Choose a piece of gear to socket:");
            // DEEP SOCKET chip (rank 2, M-locked 08-15): tappable [D] hint,
            // right-aligned on the breadcrumb band. Grey once spent this run.
            if (npc_rank("maren") >= 2) {
                var _ds_spent = variable_global_exists("deep_socket_used") && global.deep_socket_used;
                draw_set_font(ui_font(fnt_ui_small));
                var _ds_txt = _ds_spent ? "[D] Deep Socket - spent this run"
                                        : "[D] DEEP SOCKET: +1 socket  (400g + 80 dust, once per run)";
                if (input_device() == 2) _ds_txt = ui_strip_keys(_ds_txt);   // it's a tap button on touch
                var _ds_w  = string_width(_ds_txt) + 28;
                var _ds_x1 = 1500, _ds_x0 = _ds_x1 - _ds_w, _ds_y0 = 216, _ds_y1 = 254;
                draw_set_color(_ds_spent ? make_color_rgb(20, 20, 26) : make_color_rgb(35, 28, 52));
                draw_rectangle(_ds_x0, _ds_y0, _ds_x1, _ds_y1, false);
                draw_set_color(_ds_spent ? make_color_rgb(55, 52, 66) : make_color_rgb(150, 110, 220));
                draw_rectangle(_ds_x0, _ds_y0, _ds_x1, _ds_y1, true);
                draw_set_halign(fa_center);
                draw_set_color(_ds_spent ? make_color_rgb(110, 108, 125) : make_color_rgb(210, 185, 255));
                draw_text((_ds_x0 + _ds_x1) / 2, _ds_y0 + 8, _ds_txt);
                draw_set_halign(fa_left);
                if (touch_tapped(_ds_x0, _ds_y0, _ds_x1, _ds_y1)) touch_press(ord("D"));
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(make_color_rgb(140, 130, 165));
            }

            if (array_length(_slots) == 0) {
                draw_set_color(make_color_rgb(120, 115, 140));
                draw_text(_list_x, _row_y0 + 12, "No socketed gear equipped. Uncommon+ items have sockets - equip some first.");
            }
            // Narrow the item rows so the detail panel sits clear to their RIGHT instead
            // of overlapping them (the old panel landed on top of the rows). Rows now end
            // at x=1130; the tooltip is placed at x=1165 (its adaptive width fits on-screen
            // without the auto-flip that caused the overlap).
            var _socket_row_x1 = 990;   // narrowed so rows + tooltip clear the sprite column
            var _slot_n = array_length(_slots);
            for (var _i = _scroll; _i < min(_slot_n, _scroll + _vis); _i++) {
                var _it = global.inventory[_slots[_i]];
                item_ensure_sockets(_it);
                var _ty = ui_maren_row(_i - _scroll, _i == _cursor, _row_y0, _socket_row_x1);
                draw_set_color(item_rarity_color(_it.rarity));
                ui_draw_stat_line_fit(_list_x + 24, _ty, _it.name, (_socket_row_x1 - 190) - (_list_x + 24));   // clears the socket count
                // Socket count right-aligned within the narrowed row.
                draw_set_halign(fa_right);
                draw_set_color(make_color_rgb(170, 160, 190));
                draw_text(_socket_row_x1 - 18, _ty,
                    string(array_length(_it.runes)) + " / " + string(_it.socket_count) + " sockets");
                draw_set_halign(fa_left);
            }
            ui_maren_scroll_hint(_scroll, _vis, _slot_n);
            // Full gear breakdown for the highlighted piece (same panel the inventory
            // uses) so you can judge an item's stats before deciding to socket it. Sits to
            // the right of the narrowed rows.
            if (array_length(_slots) > 0) {
                var _sel_i  = clamp(_cursor, 0, array_length(_slots) - 1);
                var _sel_it = global.inventory[_slots[_sel_i]];
                ui_draw_item_tooltip(1015, 255, _sel_it, undefined);
            }
        } else if (_gc.maren_phase == 1) {
            var _it = global.inventory[_gc.maren_item_sel];
            item_ensure_sockets(_it);
            draw_set_color(make_color_rgb(140, 130, 165));
            draw_text(_list_x, 225, "Sockets on ");
            draw_set_color(item_rarity_color(_it.rarity));
            ui_draw_stat_line_fit(_list_x + 135, 225, _it.name, 1500 - (_list_x + 135));
            draw_set_color(make_color_rgb(110, 105, 130));
            draw_text(_list_x, 252, "Enter a filled socket to remove its rune, or an empty socket to add one.");

            var _filled = array_length(_it.runes);
            for (var _s = 0; _s < _it.socket_count; _s++) {
                var _ty2 = ui_maren_row(_s, _s == _cursor);
                if (_s < _filled) {
                    ui_draw_rune_entry(_list_x, _ty2, _it.runes[_s]);
                    draw_set_halign(fa_right);
                    draw_set_color(make_color_rgb(150, 110, 110));
                    draw_text(_list_x2 - 24, _ty2, "Enter: remove");
                    draw_set_halign(fa_left);
                } else {
                    draw_set_color(make_color_rgb(110, 110, 130));
                    draw_text(_list_x + 24, _ty2, "[ Empty Socket ]");
                    draw_set_halign(fa_right);
                    draw_set_color(make_color_rgb(120, 160, 120));
                    draw_text(_list_x2 - 24, _ty2, "Enter: add rune");
                    draw_set_halign(fa_left);
                }
            }
        } else {
            // phase 2 - choose a gear rune to socket
            var _gear = rune_inventory_indices("gear");
            draw_set_color(make_color_rgb(140, 130, 165));
            draw_text(_list_x, 225, "Choose a gear rune to socket:");
            if (array_length(_gear) == 0) {
                draw_set_color(make_color_rgb(120, 115, 140));
                draw_text(_list_x, _row_y0 + 12, "No gear runes in inventory.");
            }
            var _gear_n = array_length(_gear);
            for (var _g = _scroll; _g < min(_gear_n, _scroll + _vis); _g++) {
                var _rn = global.rune_inventory[_gear[_g]];
                var _ty3 = ui_maren_row(_g - _scroll, _g == _cursor);
                ui_draw_rune_entry(_list_x, _ty3, _rn);
            }
            ui_maren_scroll_hint(_scroll, _vis, _gear_n);
        }
    } else if (_gc.maren_tab == 1) {
        // -------- ASPECTS TAB (character Aspect slots) --------
        var _slots_n = variable_global_exists("aspect_slots") ? global.aspect_slots : 2;
        var _socked  = variable_global_exists("aspect_runes") ? global.aspect_runes : [];
        var _cap     = aspect_slot_cap();

        if (_gc.maren_phase == 0) {
            draw_set_color(make_color_rgb(140, 130, 165));
            draw_text(_list_x, 225, "Aspect slots (" + string(array_length(_socked)) + " / " + string(_slots_n) + " filled). Buff action categories in combat.");
            draw_text(_list_x, 252, "Enter a filled slot to remove its rune, an empty slot to add one.");

            // One row per unlocked slot, then an optional unlock row.
            for (var _s = 0; _s < _slots_n; _s++) {
                var _tya = ui_maren_row(_s, _s == _cursor);
                if (_s < array_length(_socked)) {
                    ui_draw_rune_entry(_list_x, _tya, _socked[_s]);
                    draw_set_halign(fa_right);
                    draw_set_color(make_color_rgb(150, 110, 110));
                    draw_text(_list_x2 - 24, _tya, "Enter: remove");
                    draw_set_halign(fa_left);
                } else {
                    draw_set_color(make_color_rgb(110, 110, 130));
                    draw_text(_list_x + 24, _tya, "[ Empty Aspect Slot ]");
                    draw_set_halign(fa_right);
                    draw_set_color(make_color_rgb(120, 160, 120));
                    draw_text(_list_x2 - 24, _tya, "Enter: add aspect rune");
                    draw_set_halign(fa_left);
                }
            }
            // Unlock-next-slot row
            if (_slots_n < _cap) {
                var _cost = aspect_slot_unlock_cost();
                var _tyu = ui_maren_row(_slots_n, _slots_n == _cursor);
                var _afford = (global.gold >= _cost.gold)
                              && variable_global_exists("rune_dust") && global.rune_dust >= _cost.dust;
                draw_set_color(_afford ? make_color_rgb(150, 220, 150) : make_color_rgb(150, 120, 120));
                draw_text(_list_x + 24, _tyu, "[ Unlock +1 Aspect Slot ]");
                draw_set_halign(fa_right);
                draw_set_color(make_color_rgb(200, 180, 130));
                draw_text(_list_x2 - 24, _tyu, string(_cost.gold) + "g  +  " + string(_cost.dust) + " Dust");
                draw_set_halign(fa_left);
            }
        } else {
            // phase 1 - choose an aspect rune to socket
            var _asp = rune_inventory_indices("aspect");
            draw_set_color(make_color_rgb(140, 130, 165));
            draw_text(_list_x, 225, "Choose an aspect rune to socket:");
            if (array_length(_asp) == 0) {
                draw_set_color(make_color_rgb(120, 115, 140));
                draw_text(_list_x, _row_y0 + 12, "No aspect runes in inventory.");
            }
            var _asp_n = array_length(_asp);
            for (var _a = _scroll; _a < min(_asp_n, _scroll + _vis); _a++) {
                var _rna = global.rune_inventory[_asp[_a]];
                var _tya2 = ui_maren_row(_a - _scroll, _a == _cursor);
                ui_draw_rune_entry(_list_x, _tya2, _rna);
            }
            ui_maren_scroll_hint(_scroll, _vis, _asp_n);
        }
    } else if (_gc.maren_tab == 2) {
        // -------- FORGE TAB (Combine / Split / Craft Flagship) --------
        if (_gc.maren_phase == 0) {
            draw_set_color(make_color_rgb(140, 130, 165));
            draw_text(_list_x, 225, "Runesmithing - choose your craft:");
            // 08-08 typography pass (M: "a list of all monochrome options that feel
            // unappealing to read through"). Every row goes through the shared
            // ui_draw_option_row standard now - violet TITLE (Maren's colour) over a
            // dim body - and carries a READINESS TAG. Availability used to be
            // invisible until you pressed Enter and got an error beep; the count sits
            // on the row now, and a craft you can't perform dims instead of lying.
            // TEMPER moved to Dorn (08-08): working rough metal toward its finish is
            // smith work, not rune work. Maren keeps AWAKEN, which is arcane.
            var _mrn_acc = make_color_rgb(196, 158, 246);
            var _mrn_lo  = make_color_rgb(148, 150, 170);
            var _n_comb  = array_length(rune_combine_groups());
            var _n_rune  = array_length(global.rune_inventory);
            var _n_leg   = array_length(item_picker_candidates_by_rarity(4));
            var _n_core  = array_length(maren_core_candidates());
            var _n_dorm  = array_length(item_picker_candidates_dormant());
            var _menu = [
                { name: "Combine", n: _n_comb,
                  desc: "Fuse three matching runes into one of the next tier - greater power drawn from sacrifice.",
                  tag: (_n_comb > 0) ? (string(_n_comb) + " set" + ((_n_comb == 1) ? "" : "s") + " ready") : "no triples" },
                { name: "Split", n: _n_rune,
                  desc: "Shatter a rune one tier down and reclaim its dust. What is unmade never comes back whole.",
                  tag: (_n_rune > 0) ? (string(_n_rune) + " held") : "no runes" },
                { name: "Craft Flagship", n: 1,
                  desc: "Forge a legendary Quickcast or Echo rune from raw dust and gold - Maren's masterwork.",
                  tag: "" },
                { name: "Sunder Legendary", n: _n_leg,
                  desc: "Break a legendary into parts: a Legendary Ingot, 50 dust and a tier-III rune. Fuel for better things.",
                  tag: (_n_leg > 0) ? (string(_n_leg) + " spare") : "none spare" },
                { name: "Forge Runeheart Core", n: _n_core,
                  desc: "Seal a tier-III+ rune and 40 dust into Maren's share of the LEGENDARY FORGE.",
                  tag: (_n_core > 0) ? (string(_n_core) + " eligible") : "no tier-III+" },
                { name: "Awaken Legendary", n: _n_dorm,
                  desc: "Wake a DORMANT legendary to its true strength - 300g, 60 dust and two epics fed to the fire.",
                  tag: (_n_dorm > 0) ? (string(_n_dorm) + " sleeping") : "none dormant" }
            ];
            var _fmp = ui_vendor_row_pitch();
            for (var _fm = 0; _fm < array_length(_menu); _fm++) {
                var _fry = 285 + _fm * _fmp;
                var _fok = (_menu[_fm].n > 0);
                ui_draw_option_row(300, _fry, 1500, _fry + (_fmp - 6), {
                    title:   _menu[_fm].name,
                    body:    _menu[_fm].desc,
                    sel:     (_fm == _cursor),
                    dim:     !_fok,
                    accent:  _mrn_acc,
                    tag:     _menu[_fm].tag,
                    tag_col: _fok ? make_color_rgb(130, 200, 150) : _mrn_lo
                });
            }
            draw_set_font(ui_font(fnt_ui));
        } else if (_gc.maren_phase == 1) {
            // Combine: 3-of-a-kind groups
            var _groups = rune_combine_groups();
            draw_set_color(make_color_rgb(140, 130, 165));
            // Post-win the source-tier cap lifts (rune_combine_tier_cap) - say so, and
            // pre-win tell the player where the ceiling is instead of silently hiding
            // tier-III triples from the list.
            draw_text(_list_x, 225, (rune_combine_tier_cap() > 3)
                ? "Combine - choose a set of three to fuse  (endless tiers unlocked):"
                : "Combine - choose a set of three to fuse  (tier III cap - lifts when Ironwake stands):");
            if (array_length(_groups) == 0) {
                draw_set_color(make_color_rgb(120, 115, 140));
                draw_text(_list_x, _row_y0 + 12, "No 3-of-a-kind runes (same type AND tier) available.");
            }
            var _grp_n = array_length(_groups);
            for (var _gi = _scroll; _gi < min(_grp_n, _scroll + _vis); _gi++) {
                var _grp  = _groups[_gi];
                var _ccst = rune_combine_cost(_grp.tier);
                var _tyc  = ui_maren_row(_gi - _scroll, _gi == _cursor);
                // Gem icon + name + effect of the source rune (equipment-style row).
                ui_draw_rune_entry(_list_x, _tyc, rune_make(_grp.id, _grp.tier));
                // Right column: fuse action on the name row, cost on the effect row, so
                // the two lines mirror the gem's name/effect rows and stay inside the row.
                var _caff = (global.gold >= _ccst.gold) && (_dust >= _ccst.dust);
                draw_set_halign(fa_right);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(150, 200, 255));
                draw_text(_list_x2 - 24, _tyc - 11,
                    "x3  ->  " + _grp.name + " " + rune_tier_roman(_grp.tier + 1)
                    + "   (have " + string(_grp.count) + ")");
                draw_set_color(_caff ? make_color_rgb(200, 180, 130) : make_color_rgb(165, 110, 110));
                draw_text(_list_x2 - 24, _tyc + 23, string(_ccst.gold) + "g  +  " + string(_ccst.dust) + " Dust");
                draw_set_halign(fa_left);
            }
            ui_maren_scroll_hint(_scroll, _vis, _grp_n);
        } else if (_gc.maren_phase == 2) {
            // Split: any owned rune
            draw_set_color(make_color_rgb(140, 130, 165));
            draw_text(_list_x, 225, "Split - choose a rune to break down (" + string(rune_split_cost().gold) + "g):");
            if (array_length(global.rune_inventory) == 0) {
                draw_set_color(make_color_rgb(120, 115, 140));
                draw_text(_list_x, _row_y0 + 12, "No runes to split.");
            }
            var _split_n = array_length(global.rune_inventory);
            for (var _si = _scroll; _si < min(_split_n, _scroll + _vis); _si++) {
                var _sr  = global.rune_inventory[_si];
                var _tys = ui_maren_row(_si - _scroll, _si == _cursor);
                ui_draw_rune_entry(_list_x, _tys, _sr);
                draw_set_halign(fa_right);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(200, 180, 130));
                var _db = rune_split_dust(_sr.tier);
                var _yield = (_sr.tier > 1)
                    ? (_sr.name + " " + rune_tier_roman(_sr.tier - 1) + "  +  " + string(_db) + " Dust")
                    : (string(_db) + " Dust");
                draw_text(_list_x2 - 24, _tys + 23, "->  " + _yield);
                draw_set_halign(fa_left);
            }
            ui_maren_scroll_hint(_scroll, _vis, _split_n);
        } else if (_gc.maren_phase == 4) {
            // -------- RUNEHEART CORE sacrifice list (LEGENDARY FORGE) --------
            forge_components_ensure();
            draw_set_color(make_color_rgb(140, 130, 165));
            draw_text(_list_x, 225, "Runeheart Core - choose the tier-III+ rune to seal (40 dust; held: "
                + string(global.forge_comp_core) + "):");
            var _mc_cands = maren_core_candidates();
            var _mc_n = array_length(_mc_cands);
            if (_mc_n == 0) {
                draw_set_color(make_color_rgb(120, 115, 140));
                draw_text(_list_x, _row_y0 + 12, "No unsocketed tier-III or better runes.");
            }
            for (var _mi = _scroll; _mi < min(_mc_n, _scroll + _vis); _mi++) {
                var _mrn = global.rune_inventory[_mc_cands[_mi]];
                var _tym = ui_maren_row(_mi - _scroll, _mi == _cursor);
                ui_draw_rune_entry(_list_x, _tym, _mrn);
                draw_set_halign(fa_right);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(212, 150, 245));
                draw_text(_list_x2 - 24, _tym + 23, "->  Runeheart Core   (40 Dust)");
                draw_set_halign(fa_left);
            }
            ui_maren_scroll_hint(_scroll, _vis, _mc_n);
        } else {
            // Craft Flagship
            var _flags = rune_flagship_ids();
            var _fc    = flagship_craft_cost();
            draw_set_color(make_color_rgb(140, 130, 165));
            draw_text(_list_x, 225, "Craft Flagship - forge a tier III rune (" + string(_fc.gold) + "g  +  " + string(_fc.dust) + " Dust):");
            var _faff = (global.gold >= _fc.gold) && (_dust >= _fc.dust);
            for (var _fi = 0; _fi < array_length(_flags); _fi++) {
                var _tyfl = ui_maren_row(_fi, _fi == _cursor);
                // Gem icon + name + effect of the tier-III flagship being forged.
                // Chase recipes (Cascade/Bastion, C6) show GREYED with their unlock
                // condition until earned - maren_craft_flagship enforces the same gate.
                var _flock = flagship_unlock_text(_flags[_fi]);
                if (_flock != "") draw_set_alpha(0.45);
                ui_draw_rune_entry(_list_x, _tyfl, rune_make(_flags[_fi], 3));
                draw_set_alpha(1.0);
                draw_set_halign(fa_right);
                draw_set_font(ui_font(fnt_ui_small));
                if (_flock != "") {
                    draw_set_color(make_color_rgb(205, 120, 100));
                    draw_text(_list_x2 - 24, _tyfl + 23, _flock);
                } else {
                    draw_set_color(_faff ? make_color_rgb(200, 180, 130) : make_color_rgb(165, 110, 110));
                    draw_text(_list_x2 - 24, _tyfl + 23, string(_fc.gold) + "g  +  " + string(_fc.dust) + " Dust");
                }
                draw_set_halign(fa_left);
            }
        }
    } else if (_gc.maren_tab == 3) {
        // -------- RUNES TAB (read-only owned list) --------
        draw_set_color(make_color_rgb(140, 130, 165));
        draw_text(_list_x, 225, "Runes owned (" + string(array_length(global.rune_inventory)) + "):");
        if (array_length(global.rune_inventory) == 0) {
            draw_set_color(make_color_rgb(120, 115, 140));
            draw_text(_list_x, _row_y0 + 12, "No runes yet. Elites and bosses drop them.");
        }
        var _runes_n = array_length(global.rune_inventory);
        for (var _r = _scroll; _r < min(_runes_n, _scroll + _vis); _r++) {
            var _rn2  = global.rune_inventory[_r];
            var _ty4 = ui_maren_row(_r - _scroll, _r == _cursor);
            ui_draw_rune_entry(_list_x, _ty4, _rn2, true, _list_x2);
        }
        ui_maren_scroll_hint(_scroll, _vis, _runes_n);
    } else {
        // -------- SPIRITS TAB (Banshee in a Bottle, BANSHEE_BOTTLE_SPEC.md) --------
        banshee_init();
        draw_set_color(make_color_rgb(140, 130, 165));
        draw_text(_list_x, 225, "Bottled spirits - Maren frees them; their parting songs become yours:");

        // The single action row: bottle icon + banked count (the release action).
        var _sp_ty = ui_maren_row(0, _cursor == 0);
        draw_sprite_stretched(spr_icon_banshee_bottle, 0, _list_x + 18, _sp_ty - 9, 48, 48);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color((global.banshee_banked > 0) ? make_color_rgb(150, 235, 235) : make_color_rgb(120, 130, 145));
        draw_text(_list_x + 84, _sp_ty - 11, "Banshee in a Bottle  x" + string(global.banshee_banked));
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(135, 130, 155));
        draw_text(_list_x + 84, _sp_ty + 23, (global.banshee_banked > 0)
            ? "Release a spirit - a random new song joins your Settings music."
            : "A very rare dungeon find. Survive the trip out and bring one here.");
        draw_set_halign(fa_right);
        draw_set_color(make_color_rgb(150, 200, 255));
        draw_text(_list_x2 - 24, _sp_ty - 11, ui_hint("[Enter] Release", "Tap: Release"));
        draw_set_halign(fa_left);

        // Song ledger below the action row. M 07-16: list ONLY songs already freed -
        // no "???" placeholder rows and no x/total count. How many songs exist (and
        // that a new bottle always holds one) stays a mystery until each is revealed.
        var _sp_cat = music_track_catalog();
        var _sp_y   = _sp_ty + 90;
        var _sp_shown = 0;
        var _sp_total_owned = 0;
        for (var _sp_i = 0; _sp_i < array_length(_sp_cat); _sp_i++)
            if (music_track_owned(_sp_cat[_sp_i].id)) _sp_total_owned++;
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(180, 150, 230));
        draw_text(_list_x, _sp_y, "Songs freed:");
        draw_set_font(ui_font(fnt_ui_small));
        if (_sp_total_owned == 0) {
            draw_set_color(make_color_rgb(95, 100, 118));
            draw_text(_list_x + 24, _sp_y + 48, "None yet - every bottle carries a song no one living has heard.");
            _sp_shown = 1;
        } else {
            for (var _sp_i = 0; _sp_i < array_length(_sp_cat); _sp_i++) {
                var _sp_t = _sp_cat[_sp_i];
                if (!music_track_owned(_sp_t.id)) continue;
                draw_set_color(make_color_rgb(205, 210, 222));
                draw_text(_list_x + 24, _sp_y + 48 + _sp_shown * 39,
                    "\"" + _sp_t.name + "\"   -   " + ((_sp_t.pool == "both") ? "Hub & Dungeon music"
                        : ((_sp_t.pool == "hub") ? "Hub music" : "Dungeon music")));
                _sp_shown++;
            }
            // A veiled hint that the collection is not complete - count kept secret.
            if (_sp_total_owned < array_length(_sp_cat)) {
                draw_set_color(make_color_rgb(95, 100, 118));
                draw_text(_list_x + 24, _sp_y + 48 + _sp_shown * 39,
                    "...more spirits still wander the dark.");
                _sp_shown++;
            }
        }
        draw_set_color(make_color_rgb(120, 115, 140));
        draw_text(_list_x + 24, _sp_y + 48 + _sp_shown * 39 + 15,
            "Freed songs are chosen in Settings (Hub Music / Dungeon Music). Extra spirits leave 25 Rune Dust.");
        draw_set_font(ui_font(fnt_ui));
    }

    // Notification line
    if (_gc.maren_notification != "") {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(220, 200, 150));
        draw_text(960, 999, _gc.maren_notification);
        draw_set_halign(fa_left);
    }

    // Controls hint (raised to clear the bottom rim band)
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(70, 70, 95));
    ui_draw_key_legend(960, 1026, "W/S: Navigate    Q/E: Tab    Enter: Select    F: Gift    U: Station    Esc: Back / Close");
    draw_set_halign(fa_left);

    // Ornate gothic rim around the whole overlay. Opening (30,30)-(1890,1050) keeps the
    // band fully on-screen while containing the title, currency, tabs and content. Drawn
    // last so it sits on top.
    ui_draw_gothic_frame(30, 30, 1890, 1050, 30);

    // Runeheart Core checkout popup (LEGENDARY FORGE, M locked 07-28) + the
    // component-forged burst - drawn over the rim like the confirm modal below.
    ui_draw_checkout_vfx();
    if (variable_instance_exists(_gc, "maren_ck_open") && _gc.maren_ck_open) {
        ui_draw_checkout_confirm(_gc.maren_ck_title, _gc.maren_ck_body, "maren:ok", "maren:cancel");
    }

    // Confirm modal - a pending gold-costing / destructive action (drawn topmost, over
    // the rim) so socketing/unsocketing/forging always asks before spending or destroying.
    if (variable_instance_exists(_gc, "maren_confirm") && _gc.maren_confirm != undefined) {
        var _cf = _gc.maren_confirm;
        draw_set_alpha(0.82);
        draw_set_color(c_black);
        draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
        draw_set_alpha(1.0);

        var _cbx0 = 480; var _cbx1 = 1440; var _cby0 = 372; var _cby1 = 708;
        draw_set_color(make_color_rgb(20, 16, 30));
        draw_rectangle(_cbx0, _cby0, _cbx1, _cby1, false);
        draw_set_color(make_color_rgb(180, 140, 230));
        draw_rectangle(_cbx0, _cby0, _cbx1, _cby1, true);

        draw_set_halign(fa_center);
        draw_set_valign(fa_top);
        draw_set_font(fnt_ui_title);
        draw_set_color(make_color_rgb(210, 185, 240));
        draw_text(GUI_CX, _cby0 + 30, "Confirm");

        draw_set_font(ui_font(fnt_ui));
        draw_set_color(c_white);
        draw_text_ext(GUI_CX, _cby0 + 102, _cf.message, 36, _cbx1 - _cbx0 - 90);

        if (_cf.warn != "") {
            draw_set_font(ui_font(fnt_ui_small));
            // Destruction warnings glow red; benign notes (rune returns safely) stay amber.
            var _cf_danger = (string_pos("DESTROY", _cf.warn) > 0);
            draw_set_color(_cf_danger ? make_color_rgb(235, 110, 95) : make_color_rgb(220, 190, 120));
            draw_text_ext(GUI_CX, _cby0 + 192, _cf.warn, 32, _cbx1 - _cbx0 - 90);
        }

        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(c_ltgray);
        ui_draw_key_legend(GUI_CX, _cby1 - 48, "Enter: Confirm        Esc: Cancel");
        draw_set_halign(fa_left);
        draw_set_valign(fa_top);
    }

    // Banshee release ceremony - the lore popup (Bairc-event idiom): the bottle
    // opens, the banshee rises with her melodic scream, then the reward reveal.
    // Timer is stepped (and input swallowed) by the maren block in gc Step.
    if (variable_instance_exists(_gc, "banshee_release_open") && _gc.banshee_release_open) {
        draw_set_alpha(0.85);
        draw_set_color(make_color_rgb(4, 8, 12));
        draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
        draw_set_alpha(1.0);

        var _br_frames = 17;                                     // spr_banshee_release frame count
        var _br_frame  = min(_br_frames - 1, _gc.banshee_release_timer div 5);   // ~12fps, holds the last frame
        var _br_done   = (_gc.banshee_release_timer >= _br_frames * 5);

        // Panel sized around the 3x-scaled 128px animation canvas.
        var _br_bw = 990, _br_bh = 852;
        var _br_bx = GUI_CX - _br_bw / 2;
        var _br_by = GUI_CY - _br_bh / 2;
        draw_set_color(make_color_rgb(14, 18, 26));
        draw_rectangle(_br_bx, _br_by, _br_bx + _br_bw, _br_by + _br_bh, false);

        draw_set_halign(fa_center);
        draw_set_valign(fa_top);
        draw_set_font(fnt_ui_title);
        draw_set_color(make_color_rgb(150, 235, 235));
        draw_text(GUI_CX, _br_by + 33, "The Bottle Opens");
        draw_set_color(make_color_rgb(70, 64, 48));
        draw_line(_br_bx + 45, _br_by + 90, _br_bx + _br_bw - 45, _br_by + 90);

        // The animation, pixel-scaled 3x (384px) and centered. A soft cyan glow
        // swells under the ghost as the ceremony progresses.
        var _br_ax = GUI_CX - 192;
        var _br_ay = _br_by + 120;
        var _br_glow = 0.10 + 0.20 * min(1, _gc.banshee_release_timer / 40)
                     + 0.04 * sin(_gc.banshee_release_timer * 0.15);
        draw_set_alpha(_br_glow);
        draw_set_color(make_color_rgb(90, 200, 210));
        draw_ellipse(GUI_CX - 210, _br_ay + 60, GUI_CX + 210, _br_ay + 350, false);
        draw_set_alpha(1.0);
        draw_sprite_stretched(spr_banshee_release, _br_frame, _br_ax, _br_ay, 384, 384);

        // Reveal band under the animation (after the banshee has fully risen).
        if (_br_done && _gc.banshee_release_result != undefined) {
            var _br_res = _gc.banshee_release_result;
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(205, 210, 222));
            if (_br_res.kind == "track") {
                draw_text_ext(GUI_CX, _br_by + 546,
                    "The spirit unwinds into the rafters, and her scream softens into song.\nMaren bows her head. \"Go on, then. Sing somewhere kinder.\"", 36, _br_bw - 120);
                draw_set_font(fnt_ui_title);
                draw_set_color(make_color_rgb(150, 235, 235));
                draw_text(GUI_CX, _br_by + 660, "\"" + _br_res.track.name + "\"");
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(200, 180, 130));
                draw_text(GUI_CX, _br_by + 717, "New " + ((_br_res.track.pool == "both") ? "Hub & Dungeon"
                        : ((_br_res.track.pool == "hub") ? "Hub" : "Dungeon"))
                    + " music track - choose it in Settings.");
            } else {
                draw_text_ext(GUI_CX, _br_by + 546,
                    "The spirit unwinds into the rafters - but you already know every song she knew.\nShe leaves what dust a grateful ghost can gather.", 36, _br_bw - 120);
                draw_set_font(fnt_ui_title);
                draw_set_color(make_color_rgb(195, 155, 255));
                draw_text(GUI_CX, _br_by + 660, "+" + string(_br_res.amount) + " Rune Dust");
            }
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(120, 128, 150));
            draw_text(GUI_CX, _br_by + _br_bh - 45, (input_device() == 2) ? "Tap to continue" : "Press any key to continue");
        } else {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(120, 128, 150));
            draw_text(GUI_CX, _br_by + _br_bh - 45, (input_device() == 2) ? "Tap to skip" : "Press any key to skip");
        }

        // Ornate gothic rim, matching the tutorial-tip / Bairc popup dressing.
        draw_set_font(-1);
        ui_draw_gothic_frame(_br_bx, _br_by, _br_bx + _br_bw, _br_by + _br_bh, 30);
        draw_set_halign(fa_left);
    }

    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_sable_screen()
// Sable the Alchemist - Salvage / Brew / Upgrade overlay. Reuses ui_maren_row
// for row geometry. Layout constants MUST match the Sable input block in
// obj_game_controller Step. (Tabs at x=345+t*200, mirroring Maren's 3-tab bar.)
// ---------------------------------------------------------------------------
function ui_draw_sable_screen() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "sable_open") || !_gc.sable_open) return;

    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(10, 14, 12));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);

    // Title + dust
    draw_set_halign(fa_center);
    draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(150, 210, 170));
    draw_text(960, 36, "Sable the Alchemist");
    draw_set_halign(fa_right);
    draw_set_font(ui_font(fnt_ui_small));
    var _dust = variable_global_exists("rune_dust") ? global.rune_dust : 0;
    // "Rune Dust:" label in light purple, values stay gold (M 2026-07-07).
    var _sd_gold = "        Gold: " + string(global.gold);   // gap holds the coin glyph (#19)
    var _sd_dnum = " " + string(_dust);
    draw_set_color(make_color_rgb(200, 180, 130));
    draw_text(1860, 48, _sd_gold);
    var _sd_x = 1860 - string_width(_sd_gold);
    draw_text(_sd_x, 48, _sd_dnum);
    draw_set_color(make_color_rgb(195, 155, 255));
    draw_text(_sd_x - string_width(_sd_dnum), 48, "Rune Dust:");
    // #19 polish: dust/gold glyphs beside the readouts (shrine-header idiom, 24px).
    draw_sprite_stretched(spr_icon_dust, 0,
        _sd_x - string_width(_sd_dnum) - string_width("Rune Dust:") - 30, 46, 24, 24);
    draw_sprite_stretched(spr_icon_gold, 0, _sd_x + 8, 46, 24, 24);
    ui_draw_reagent_readout(1860, 78);   // reagents under the dust/gold line (M 08-26)
    draw_set_halign(fa_left);

    // Bond readout - watch salvage/brew raise the relationship live.
    ui_draw_npc_bond("sable", 90, 45);
    ui_draw_npc_column("sable");   // reserved right-hand sprite column

    // Tab bar (4 tabs) - x=368+t*300, y=105, w=285 (centred on x960, matches Maren)
    var _tab_labels = ["Salvage", "Brew", "Upgrade", "Rebirth"];
    draw_set_font(ui_font(fnt_ui));
    for (var _t = 0; _t < 4; _t++) {
        var _tx = 368 + _t * 300;
        var _on = (_gc.sable_tab == _t);
        draw_set_color(_on ? make_color_rgb(30, 50, 38) : make_color_rgb(20, 28, 24));
        draw_rectangle(_tx, 105, _tx + 285, 165, false);
        draw_set_color(_on ? make_color_rgb(110, 200, 140) : make_color_rgb(50, 70, 58));
        draw_rectangle(_tx, 105, _tx + 285, 165, true);
        draw_set_halign(fa_center);
        draw_set_valign(fa_middle);
        draw_set_color(_on ? c_white : make_color_rgb(150, 160, 150));
        draw_text(_tx + 143, 135, _tab_labels[_t]);
        // Hover quick-ref (M 08-15).
        var _thb2 = ui_tab_hint_body("sable:" + _tab_labels[_t]);
        if (_thb2 != "") ui_tab_hover_stash(_tx, 105, _tx + 285, 165, _tab_labels[_t], _thb2);
    }
    draw_set_halign(fa_left);
    draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui));

    // Rotating alchemist quip (M 2026-07-06): one line of Sable flavor, new each run.
    var _sq = [
        "Everything melts down to something useful.",
        "Careful with the green ones. Or don't be - I need the notes.",
        "Dust is just potential with the shape burned off.",
        "Three of anything is a recipe. One of anything is a shame.",
        "If it hums, brew it. If it screams, label it.",
        "The cauldron remembers every recipe. Unfortunately, so do I."
    ];
    var _sq_i = (variable_global_exists("run_count") ? global.run_count : 0) mod array_length(_sq);
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(110, 150, 125));
    draw_text(960, 186, "\"" + _sq[_sq_i] + "\"");
    draw_set_halign(fa_left);
    draw_set_font(ui_font(fnt_ui));

    // Soft gold shimmer for dust PAYOUTS (gains glitter; costs stay flat).
    var _dust_shimmer = merge_color(make_color_rgb(200, 180, 130), make_color_rgb(255, 235, 160),
        0.5 + 0.5 * sin(current_time / 400));

    // --- Ever-present cauldron, bottom-left (M 2026-07-07): living set-dressing on
    // EVERY tab - things go in, things come out. Sits in the free strip left of the
    // rows (x<300) so no list length can overlap it. Static sprite + code-driven
    // bubble/wisp loop; a true PixelLab animation can replace the overlay next cycle.
    var _cld = asset_get_index("spr_sable_cauldron");
    if (_cld != -1) {
        var _cld_s  = 1.6;
        var _cld_cx = 168;
        var _cld_cy = 1042 - sprite_get_height(_cld) * _cld_s * 0.5;   // centre origin
        var _cld_n  = sprite_get_number(_cld);
        // 210ms/frame (~1.9s loop) - 125ms read as speed-running the boil (M).
        var _cld_f  = (_cld_n > 1) ? floor(current_time / 210) mod _cld_n : 0;
        draw_sprite_ext(_cld, _cld_f, _cld_cx, _cld_cy, _cld_s, _cld_s, 0, c_white, 1);

        // Code-drawn bubble/wisp loop: FALLBACK only, for a single-frame sprite.
        // Once the PixelLab bubbling animation is imported the sprite carries its
        // own smoke and this whole block stays dormant.
        if (_cld_n <= 1) {
            // Liquid surface band (sprite-space: the brew sits just above centre).
            var _liq_y = _cld_cy - sprite_get_height(_cld) * _cld_s * 0.17;
            var _liq_w = sprite_get_width(_cld) * _cld_s * 0.26;

            // Bubbles: five staggered surface pops, each respawning at a hashed x.
            for (var _cbi = 0; _cbi < 5; _cbi++) {
                var _cbt = frac(current_time / 1100 + _cbi * 0.2);
                var _cbc = floor(current_time / 1100 + _cbi * 0.2);
                var _cbr = frac(sin(_cbc * 12.9898 + _cbi * 78.233) * 43758.5453);
                draw_set_alpha((1 - _cbt) * 0.55);
                draw_set_color(make_color_rgb(150, 200, 255));
                draw_circle(_cld_cx + (_cbr * 2 - 1) * _liq_w,
                    _liq_y - _cbt * 6 * _cld_s, (1 + 3 * _cbt) * _cld_s, true);
            }
            // Wisps: translucent vapor puffs drifting up off the rim, swaying.
            for (var _cwi = 0; _cwi < 4; _cwi++) {
                var _cwt = frac(current_time / 2600 + _cwi / 4);
                draw_set_alpha((1 - _cwt) * 0.16);
                draw_set_color(make_color_rgb(170, 210, 255));
                draw_circle(_cld_cx + sin(current_time / 700 + _cwi * 2.1) * _liq_w * 0.6,
                    _liq_y - 4 * _cld_s - _cwt * 90 * _cld_s, (4 + 10 * _cwt) * _cld_s, false);
            }
            draw_set_alpha(1.0);
        }
    }

    var _row_y0 = 285;
    var _list_x = 300;
    var _list_x2 = 1500;   // narrowed from 1620 to clear the right-hand NPC sprite column
    var _cursor = _gc.sable_cursor;

    if (_gc.sable_tab == 0) {
        // -------- SALVAGE TAB --------
        if (_gc.sable_phase == 0) {
            draw_set_color(make_color_rgb(140, 160, 145));
            draw_text(_list_x, 225, "Salvage unwanted loot into rune dust - or transmute spare runes:");
            // Equipment-style menu rows (icon + title + blurb) instead of bare text.
            var _menu = [
                { title: "Salvage Gear",
                  desc:  "Unequipped pack & stash pieces melt down to rune dust." },
                { title: "Salvage Runes & Aspects",
                  desc:  "Unsocketed runes scrapped whole - nothing comes back." },
                { title: "Transmute Runes",
                  desc:  "Melt any 3 same-tier runes into ONE RANDOM rune of the next tier." }
            ];
            for (var _sm = 0; _sm < 3; _sm++) {
                var _tys = ui_maren_row(_sm, _sm == _cursor);
                var _sry = _tys - 15;
                // Verb badges (M-approved 07-28 PixelLab picks, replacing the
                // code-drawn glyphs M called "SNES basic"): melting sword /
                // shattered rune / transmutation gem. 48px, centered like the
                // old glyphs were.
                var _badge_spr = (_sm == 0) ? spr_badge_salvage_gear
                               : ((_sm == 1) ? spr_badge_scrap_rune : spr_badge_transmute);
                draw_sprite_stretched(_badge_spr, 0, _list_x + 6, _sry + 9, 48, 48);
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(make_color_rgb(190, 220, 195));
                draw_text(_list_x + 72, _sry + 4, _menu[_sm].title);
                draw_set_font(ui_font(fnt_ui_small));
                draw_set_color(make_color_rgb(130, 150, 138));
                draw_text(_list_x + 72, _sry + ui_vendor_body_dy(), _menu[_sm].desc);
                draw_set_font(ui_font(fnt_ui));
            }
        } else if (_gc.sable_phase == 1) {
            // Gear list (WINDOWED - pack + stash can exceed the panel height). Keep
            // SABLE_SALVAGE_VIS in sync with the mouse hit-test in obj_game_controller Step.
            var _gear = sable_salvageable_gear();
            draw_set_color(make_color_rgb(140, 160, 145));
            // Dust-per-rarity header reads the REAL payout function so it can
            // never drift from the mechanic again (M 08-14 shot: said 1/2/5/
            // 10/20 while sable_salvage_gear_dust paid 1/3/12/30/75).
            var _sdl = "";
            for (var _sdr = 0; _sdr <= 4; _sdr++) {
                _sdl += ((_sdr > 0) ? " / " : "") + item_rarity_name(_sdr) + " " + string(sable_salvage_gear_dust(_sdr));
            }
            draw_text(_list_x, 225, "Choose gear to salvage (dust: " + _sdl + "):");
            var _gn = array_length(_gear);
            if (_gn == 0) {
                draw_set_color(make_color_rgb(120, 130, 122));
                draw_text(_list_x, _row_y0 + 12, "No unequipped gear to salvage.");
            } else {
                var _svis   = ui_vendor_visible_rows(9);
                var _gfirst = ui_list_window("sable_gear", _cursor, _gn, _svis);
                var _glast  = min(_gn, _gfirst + _svis);
                for (var _gi = _gfirst; _gi < _glast; _gi++) {
                    var _it  = _gear[_gi].item;
                    var _tyg = ui_maren_row(_gi - _gfirst, _gi == _cursor);
                    // Icon badge like Dorn's shop rows (M 2026-07-06: bare text lists).
                    ui_draw_item_icon(_list_x + 6, _tyg - 6, 48, _it);
                    draw_set_color(item_rarity_color(_it.rarity));
                    ui_draw_stat_line_fit(_list_x + 72, _tyg,
                        _it.name + "  (" + item_rarity_name(_it.rarity) + ")",
                        (_list_x2 - 320) - (_list_x + 72));   // clears the right dust column
                    draw_set_halign(fa_right);
                    draw_set_color(_dust_shimmer);
                    draw_text(_list_x2 - 24, _tyg, "+" + string(sable_salvage_gear_dust(_it.rarity)) + " Dust  [" + _gear[_gi].source + "]");
                    draw_set_halign(fa_left);
                }
                ui_draw_sable_scroll_hint(_list_x2 - 24, _gfirst, _glast, _gn);
            }
        } else if (_gc.sable_phase == 2) {
            // Rune list (WINDOWED - see the gear list note above; keep _svis in sync
            // with the mouse hit-test in obj_game_controller Step).
            draw_set_color(make_color_rgb(140, 160, 145));
            draw_text(_list_x, 225, "Choose a rune to scrap for dust (I 6 / II 16 / III 40):");
            var _rinv = variable_global_exists("rune_inventory") ? global.rune_inventory : [];
            var _rn_n = array_length(_rinv);
            if (_rn_n == 0) {
                draw_set_color(make_color_rgb(120, 130, 122));
                draw_text(_list_x, _row_y0 + 12, "No unsocketed runes to scrap.");
            } else {
                var _svis2  = ui_vendor_visible_rows(9);
                var _rfirst = ui_list_window("sable_rune", _cursor, _rn_n, _svis2);
                var _rlast  = min(_rn_n, _rfirst + _svis2);
                for (var _ri = _rfirst; _ri < _rlast; _ri++) {
                    var _rn  = _rinv[_ri];
                    var _tyr = ui_maren_row(_ri - _rfirst, _ri == _cursor);
                    ui_draw_rune_entry(_list_x, _tyr, _rn);
                    draw_set_halign(fa_right);
                    draw_set_color(_dust_shimmer);
                    draw_text(_list_x2 - 24, _tyr, "+" + string(sable_salvage_rune_dust(_rn.tier)) + " Dust");
                    draw_set_halign(fa_left);
                }
                ui_draw_sable_scroll_hint(_list_x2 - 24, _rfirst, _rlast, _rn_n);
            }
        } else {
            // -------- TRANSMUTE list (phase 3, M 07-27 rune sink) -------- same
            // windowed rune list; Enter TOGGLES a rune into the 3-pick, picked rows
            // ring teal with a PICKED tag. The 3rd pick arms the checkout POPUP
            // (drawn at the end of this function); commit lives in the Step modal.
            // Guarded: on the very first open the Step block may not have lazy-inited
            // the selection array yet this frame.
            var _t_sel  = variable_instance_exists(_gc, "sable_trans_sel") ? _gc.sable_trans_sel : [];
            var _t_n    = array_length(_t_sel);
            draw_set_color(make_color_rgb(140, 160, 145));
            draw_text(_list_x, 225, "Transmute - pick 3 SAME-TIER runes to melt into 1 RANDOM next-tier rune  ("
                + string(_t_n) + " / 3 picked"
                + ((rune_combine_tier_cap() <= 3) ? ", tier III cap until Ironwake stands" : "") + "):");
            var _rinv3  = variable_global_exists("rune_inventory") ? global.rune_inventory : [];
            var _rn3_n  = array_length(_rinv3);
            if (_rn3_n == 0) {
                draw_set_color(make_color_rgb(120, 130, 122));
                draw_text(_list_x, _row_y0 + 12, "No unsocketed runes to transmute.");
            } else {
                var _svis3  = ui_vendor_visible_rows(9);
                var _tfirst = ui_list_window("sable_trans", _cursor, _rn3_n, _svis3);
                var _tlast  = min(_rn3_n, _tfirst + _svis3);
                for (var _ti = _tfirst; _ti < _tlast; _ti++) {
                    var _tr  = _rinv3[_ti];
                    var _tyt = ui_maren_row(_ti - _tfirst, _ti == _cursor);
                    var _picked = false;
                    for (var _tp = 0; _tp < _t_n; _tp++) { if (_t_sel[_tp] == _ti) { _picked = true; break; } }
                    if (_picked) {
                        // Teal pick-ring over the row frame (geometry from
                        // ui_vendor_row_pitch - MUST match ui_maren_row).
                        var _tmp  = ui_vendor_row_pitch();
                        var _try0 = _row_y0 + (_ti - _tfirst) * _tmp;
                        var _try1 = _try0 + (_tmp - 6);
                        draw_set_color(make_color_rgb(90, 200, 190));
                        draw_rectangle(300, _try0, _list_x2, _try1, true);
                        draw_rectangle(301, _try0 + 1, _list_x2 - 1, _try1 - 1, true);
                    }
                    ui_draw_rune_entry(_list_x, _tyt, _tr);
                    draw_set_halign(fa_right);
                    if (_picked) {
                        draw_set_color(make_color_rgb(90, 200, 190));
                        draw_text(_list_x2 - 24, _tyt, "PICKED");
                    } else {
                        draw_set_color(make_color_rgb(130, 150, 138));
                        draw_text(_list_x2 - 24, _tyt, "->  random " + rune_tier_roman(_tr.tier + 1)
                            + "   (" + string(sable_transmute_cost(_tr.tier)) + "g)");
                    }
                    draw_set_halign(fa_left);
                }
                ui_draw_sable_scroll_hint(_list_x2 - 24, _tfirst, _tlast, _rn3_n);
            }
        }
    } else if (_gc.sable_tab == 1) {
        // -------- BREW TAB -------- (WINDOWED since the 07-28 catalog expansion -
        // 18 recipes; keep _bvis in sync with the Step mouse mapping.)
        var _brew = sable_brew_catalog_priced();
        var _slots_used = variable_global_exists("consumable_inventory") ? array_length(global.consumable_inventory) : 0;
        draw_set_color(make_color_rgb(140, 160, 145));
        draw_text(_list_x, 225, "Brew a potion  (you hold " + string(_slots_used) + " consumables):");
        var _b_n    = array_length(_brew);
        var _bvis   = ui_vendor_visible_rows(9);
        var _bfirst = ui_list_window("sable_brew", _cursor, _b_n, _bvis);
        var _blast  = min(_b_n, _bfirst + _bvis);
        for (var _bi = _bfirst; _bi < _blast; _bi++) {
            var _b   = _brew[_bi];
            var _tyb = ui_maren_row(_bi - _bfirst, _bi == _cursor);
            var _aff = (global.gold >= _b.gold) && (_dust >= _b.dust);
            // Equipment-style row: icon + name + effect description. Desc gets a
            // measured clear width so long lines (Devil Wine) never run under
            // the right-aligned price (M 08-13 shot).
            var _b_price = string(_b.gold) + "g  +  " + string(_b.dust) + " Dust";
            draw_set_font(ui_font(fnt_ui_small));
            var _b_desc_w = (_list_x2 - 24) - string_width(_b_price) - 18 - (_list_x + 72);
            ui_draw_consumable_entry(_list_x, _tyb, _b,
                _aff ? make_color_rgb(190, 220, 195) : make_color_rgb(120, 130, 122), _b_desc_w);
            draw_set_halign(fa_right);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(_aff ? make_color_rgb(200, 180, 130) : make_color_rgb(150, 120, 110));
            draw_text(_list_x2 - 24, _tyb + 12, _b_price);
            draw_set_halign(fa_left);
        }
        ui_draw_sable_scroll_hint(_list_x2 - 24, _bfirst, _blast, _b_n);

    } else if (_gc.sable_tab == 2) {
        // -------- UPGRADE TAB (Potion Fusion) --------
        // Presented like the Rebirth tab: accent title + a framed how-it-works
        // diagram, with the fusable rows below (M 2026-07-07: was bare text).
        var _groups = sable_upgrade_groups();
        var _ucost = sable_upgrade_cost();

        // -------- CHAOTIC BREW pick-3 list (M 07-28) -------- replaces the tab
        // content while open. Rows base y285, pitch 72, 9 visible, window
        // "sable_chaos" - MUST match the Step click mapping.
        if (variable_instance_exists(_gc, "sable_chaos_open") && _gc.sable_chaos_open) {
            var _ch_sel = _gc.sable_chaos_sel;
            // Shared pick-3 list serves BOTH the Chaotic Brew and the forge's
            // Quintessence (sable_chaos_kind).
            var _ck_quint = variable_instance_exists(_gc, "sable_chaos_kind") && _gc.sable_chaos_kind == "quint";
            draw_set_font(ui_font(fnt_ui));
            var _cb_title = _ck_quint ? "Quintessence" : "Chaotic Brew";
            draw_text_outline(_list_x, 225, _cb_title,
                _ck_quint ? make_color_rgb(55, 22, 78) : make_color_rgb(16, 50, 48),
                _ck_quint ? make_color_rgb(212, 150, 245) : make_color_rgb(130, 225, 215));
            draw_set_color(make_color_rgb(140, 160, 145));
            draw_text(_list_x + string_width(_cb_title) + 12, 225,
                (_ck_quint ? "- pick 3 DIFFERENT specialty brews to distill" : "- pick ANY 3 potions to melt down")
                + "  (" + string(array_length(_ch_sel)) + " / 3 picked):");
            // Combined stash+pouch pool (08-11) - MUST match the Step handler's
            // list, since the picked indices are combined-pool indices.
            var _ch_inv = sable_potion_pool();
            var _ch_n   = array_length(_ch_inv);
            if (_ch_n == 0) {
                draw_set_color(make_color_rgb(120, 130, 122));
                draw_text(_list_x, 297, "No potions in your pouch or stash.");
            } else {
                var _ch_vis   = ui_vendor_visible_rows(9);
                var _ch_first = ui_list_window("sable_chaos", _cursor, _ch_n, _ch_vis);
                var _ch_last  = min(_ch_n, _ch_first + _ch_vis);
                for (var _ci2 = _ch_first; _ci2 < _ch_last; _ci2++) {
                    var _cp  = _ch_inv[_ci2].it;
                    var _cp_stash = (_ch_inv[_ci2].src == 0);
                    var _tyc = ui_maren_row(_ci2 - _ch_first, _ci2 == _cursor);
                    var _cpicked = false;
                    for (var _cps = 0; _cps < array_length(_ch_sel); _cps++) {
                        if (_ch_sel[_cps] == _ci2) { _cpicked = true; break; }
                    }
                    if (_cpicked) {
                        // Teal pick-ring over the row frame (geometry from
                        // ui_vendor_row_pitch - MUST match ui_maren_row).
                        var _cmp  = ui_vendor_row_pitch();
                        var _cry0 = 285 + (_ci2 - _ch_first) * _cmp;
                        var _cry1 = _cry0 + (_cmp - 6);
                        draw_set_color(make_color_rgb(90, 200, 190));
                        draw_rectangle(300, _cry0, _list_x2, _cry1, true);
                        draw_rectangle(301, _cry0 + 1, _list_x2 - 1, _cry1 - 1, true);
                    }
                    var _cp_lbl = (_cp_stash ? "STASH  -  " : "")
                        + (_cpicked ? "PICKED" : "Enter: pick");
                    draw_set_font(ui_font(fnt_ui_small));
                    var _cp_desc_w = (_list_x2 - 24) - string_width(_cp_lbl) - 18 - (_list_x + 72);
                    ui_draw_consumable_entry(_list_x, _tyc, _cp, make_color_rgb(190, 220, 195), _cp_desc_w);
                    draw_set_halign(fa_right);
                    draw_set_font(ui_font(fnt_ui_small));
                    // Stash rows say where they live - the pool mixes both.
                    draw_set_color(_cpicked ? make_color_rgb(90, 200, 190) : make_color_rgb(115, 135, 122));
                    draw_text(_list_x2 - 24, _tyc, _cp_lbl);
                    draw_set_halign(fa_left);
                }
                ui_draw_sable_scroll_hint(_list_x2 - 24, _ch_first, _ch_last, _ch_n);
            }
        } else {

        draw_set_font(ui_font(fnt_ui));
        var _uf_title = "Potion Fusion";
        draw_text_outline(_list_x, 225, _uf_title, make_color_rgb(20, 52, 30), make_color_rgb(150, 230, 170));
        draw_set_color(make_color_rgb(140, 160, 145));
        draw_text(_list_x + string_width(_uf_title) + 12, 225, "- fuse three identical potions into their improved form:");
        draw_set_halign(fa_right);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(200, 180, 130));
        draw_text(_list_x2, 231, string(_ucost.gold) + "g  +  " + string(_ucost.dust) + " Dust per fuse");
        draw_set_halign(fa_left);

        // (The old how-it-works diagram panel is GONE - M 08-15: it repeated the
        // header line word for word and pushed the list down; the rows' own
        // 3-icons -> 1-icon readout already SHOWS the mechanic. Rows reclaimed
        // the space and start at the standard y285 - keep the Step hit-test in
        // sync.)

        // Empty state: a ghost row where the fusable groups would list (row 1 -
        // the CHAOTIC BREW row owns row 0 when there are no groups).
        if (array_length(_groups) == 0) {
            var _gh_p  = ui_vendor_row_pitch();
            var _gh_y0 = 285 + _gh_p;
            var _gh_y1 = _gh_y0 + (_gh_p - 6);
            draw_set_alpha(0.55);
            draw_set_color(make_color_rgb(16, 22, 19));
            draw_rectangle(_list_x, _gh_y0, _list_x2, _gh_y1, false);
            draw_set_color(make_color_rgb(52, 70, 60));
            draw_rectangle(_list_x, _gh_y0, _list_x2, _gh_y1, true);
            draw_set_alpha(1.0);
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(110, 130, 118));
            draw_text((_list_x + _list_x2) / 2, _gh_y0 + 10, "No potion held three times over - nothing to fuse yet.");
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(85, 100, 90));
            draw_text((_list_x + _list_x2) / 2, _gh_y0 + ui_vendor_body_dy() + 4, "Brew or buy duplicates of a standard potion, then bring them back.");
            draw_set_halign(fa_left);
            draw_set_font(ui_font(fnt_ui));
        }
        for (var _ui = 0; _ui < array_length(_groups); _ui++) {
            var _g   = _groups[_ui];
            var _tyu = ui_maren_row(_ui, _ui == _cursor);   // base 285 since 08-15 (diagram panel removed)
            // Fusable needs 3 held AND the fee (recipes list always since 07-28).
            var _uaff = (_g.count >= 3) && (global.gold >= _ucost.gold) && (_dust >= _ucost.dust);
            var _ucol = _uaff ? make_color_rgb(190, 220, 195) : make_color_rgb(120, 130, 122);
            if (_g.count < 3) draw_set_alpha(0.55);
            // Show the RESULT potion equipment-style (icon + name + effect); the recipe
            // (fuse 3x source) sits on the right.
            var _utmpl = sable_elite_template(_g.to);
            if (_utmpl != undefined) {
                ui_draw_consumable_entry(_list_x, _tyu, _utmpl, _ucol);
            } else {
                draw_set_color(_ucol);
                draw_text(_list_x + 24, _tyu, _g.to);
            }
            // Fusion preview (M 2026-07-06): the recipe drawn as icons - three
            // source potions, an arrow, the improved result - instead of a sentence.
            var _fus_src = undefined;
            for (var _fi = 0; _fi < array_length(global.consumable_inventory); _fi++) {
                if (global.consumable_inventory[_fi].name == _g.from) { _fus_src = global.consumable_inventory[_fi]; break; }
            }
            // Recipes list even at 0 held (07-28) - fall back to the catalog
            // template so the source icons never vanish from the row.
            if (_fus_src == undefined) {
                var _fus_pools = [global.consumables_standard, global.consumables_elite];
                for (var _fp = 0; _fp < 2 && _fus_src == undefined; _fp++) {
                    for (var _fj = 0; _fj < array_length(_fus_pools[_fp]); _fj++) {
                        if (_fus_pools[_fp][_fj].name == _g.from) { _fus_src = _fus_pools[_fp][_fj]; break; }
                    }
                }
            }
            var _fus_x = _list_x2 - 24 - 170;   // [34px x3, 26px pitch] + arrow + 44px result
            var _fus_y = _tyu - 4;
            if (_fus_src != undefined) {
                for (var _fi3 = 0; _fi3 < 3; _fi3++) {
                    ui_draw_consumable_icon(_fus_x + _fi3 * 26, _fus_y, 34, _fus_src);
                }
            }
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(make_color_rgb(150, 200, 160));
            draw_text(_fus_x + 96, _fus_y + 4, "->");
            if (_utmpl != undefined) {
                ui_draw_consumable_icon(_fus_x + 126, _fus_y - 5, 44, _utmpl);
            }
            // Held count, left of the icon block - "have N / 3" reads as a goal
            // when short (07-28 always-visible recipes).
            draw_set_halign(fa_right);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color((_g.count >= 3) ? make_color_rgb(180, 195, 205) : make_color_rgb(210, 160, 110));
            draw_text(_fus_x - 15, _tyu + 12, "have " + string(_g.count) + " / 3");
            draw_set_halign(fa_left);
            draw_set_alpha(1.0);
        }

        // The always-present CHAOTIC BREW row (M 07-28) - teal. Enter opens the
        // pick-3 list above (Step: sable_chaos_open).
        var _chaos_i = array_length(_groups);
        var _tyc2 = ui_maren_row(_chaos_i, _chaos_i == _cursor);   // base 285 since 08-15
        // Real icon (M pick 07-28) with the old gem glyph as pre-reload fallback.
        // Backdrop plate (M 08-04: the dark art was near-invisible on the dark row).
        draw_set_color(make_color_rgb(34, 40, 46));
        draw_rectangle(_list_x + 2, _tyc2 - 10, _list_x + 58, _tyc2 + 46, false);
        draw_set_color(make_color_rgb(70, 140, 134));
        draw_rectangle(_list_x + 2, _tyc2 - 10, _list_x + 58, _tyc2 + 46, true);
        var _cb_ic = asset_get_index("spr_icon_consumable_chaotic_brew");
        if (_cb_ic >= 0) draw_sprite_stretched(_cb_ic, 0, _list_x + 6, _tyc2 - 6, 48, 48);
        else ui_draw_gem_glyph(_list_x + 30, _tyc2 + 18, 20, make_color_rgb(90, 200, 190));
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(130, 225, 215));
        draw_text(_list_x + 72, _tyc2 - 9, "CHAOTIC BREW");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(115, 150, 145));
        draw_text(_list_x + 72, _tyc2 + 21, "Fuse ANY 3 potions - mismatched welcome. The result is... experimental.");
        draw_set_halign(fa_right);
        var _chc2 = sable_chaotic_cost();
        draw_set_color(make_color_rgb(200, 180, 130));
        draw_text(_list_x2 - 24, _tyc2 + 6, string(_chc2.gold) + "g  +  " + string(_chc2.dust) + " Dust");
        draw_set_halign(fa_left);

        // QUINTESSENCE row (LEGENDARY FORGE component, M locked 07-28) - violet.
        var _quint_i = array_length(_groups) + 1;
        var _tyq = ui_maren_row(_quint_i, _quint_i == _cursor);   // base 285 since 08-15
        // Real icon (M pick 07-28; NEW resource - needs one GM project reload,
        // the glyph draws until then).
        // Backdrop plate (same dark-on-dark treatment as the Chaotic Brew row).
        draw_set_color(make_color_rgb(38, 32, 46));
        draw_rectangle(_list_x + 2, _tyq - 10, _list_x + 58, _tyq + 46, false);
        draw_set_color(make_color_rgb(140, 95, 170));
        draw_rectangle(_list_x + 2, _tyq - 10, _list_x + 58, _tyq + 46, true);
        var _qt_ic = asset_get_index("spr_icon_consumable_quintessence");
        if (_qt_ic >= 0) draw_sprite_stretched(_qt_ic, 0, _list_x + 6, _tyq - 6, 48, 48);
        else ui_draw_gem_glyph(_list_x + 30, _tyq + 18, 20, make_color_rgb(190, 120, 230));
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(212, 150, 245));
        draw_text(_list_x + 72, _tyq - 9, "DISTILL QUINTESSENCE");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(150, 130, 160));
        forge_components_ensure();
        draw_text(_list_x + 72, _tyq + 21, "Boil ANY 3 potions into Sable's share of the LEGENDARY FORGE.   (held: "
            + string(global.forge_comp_quint) + ")");
        draw_set_halign(fa_right);
        draw_set_color(make_color_rgb(200, 180, 130));
        draw_text(_list_x2 - 24, _tyq + 6, string(forge_quint_cost()) + "g");
        draw_set_halign(fa_left);
        draw_set_font(ui_font(fnt_ui));
        }   // end fusion-list branch (chaos pick list is the sibling above)
    } else {
        // -------- REBIRTH TAB (07-29 restructure - M: the cost table + stacked
        // rows read as one busy wall). Three clearly-separated FUNCTION cards;
        // Enter opens the shared item picker for the chosen craft. Cards sit at
        // the STANDARD y285 row base so the shared mouse mapping lines up (the
        // old layout drew rows at ~y446 while clicks mapped from y285). --------
        draw_set_font(ui_font(fnt_ui));
        var _ar_title = "Alchemical Rebirth";
        var _ar_tw    = string_width(_ar_title);   // measured in fnt_ui BEFORE the font swap
        draw_text_outline(_list_x, 225, _ar_title, make_color_rgb(55, 22, 78), make_color_rgb(212, 150, 245));
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(150, 140, 160));
        ui_draw_stat_line_fit(_list_x + _ar_tw + 18, 231,
            "- \"Despite what Dorn would have you believe, there are many ways to make... or unmake an item.\"",
            1500 - (_list_x + _ar_tw + 18) - 12);
        draw_set_font(ui_font(fnt_ui));

        // 07-31 (M): rows carry only the NAME - the crammed one-line cost walls
        // moved into a roomy DETAIL PANEL below that expands the selected craft.
        var _reb_n = array_length(item_picker_candidates_class_specific());
        var _sq_n  = array_length(item_picker_candidates_statreq());
        var _cr_n  = array_length(item_picker_candidates_by_rarity(4));
        var _cu  = alch_rebirth_cost(1);
        var _cra = alch_rebirth_cost(2);
        var _ce  = alch_rebirth_cost(3);
        var _rb_cards = [
            { title: "CLASS REBIRTH", tcol: make_color_rgb(190, 220, 195), n: _reb_n,
              tag:  "Trade a class item across class lines",
              body: "Sacrifice one of your class's items. An item of the SAME slot and rarity rises in its place - but belonging to a random OTHER class. The way to arm a build your own drops never feed.",
              costs: [ "Uncommon:   " + string(_cu.dust)  + " dust + " + string(_cu.gold)  + "g",
                       "Rare:       " + string(_cra.dust) + " dust + " + string(_cra.gold) + "g",
                       "Epic:       " + string(_ce.dust)  + " dust + " + string(_ce.gold)  + "g" ] },
            { title: "ATTUNEMENT REBIRTH", tcol: make_color_rgb(160, 200, 235), n: _sq_n,
              tag:  "Re-roll an item's stat requirement",
              body: "Re-set an item's STAT REQUIREMENT to a random other stat. Repeatable - keep rolling until the gear fits the hands that hold it.",
              costs: [ "Rare:       " + string(statreq_rebirth_cost(2)) + "g",
                       "Epic:       " + string(statreq_rebirth_cost(3)) + "g",
                       "Legendary:  " + string(statreq_rebirth_cost(4)) + "g" ] },
            { title: "CURSED REBIRTH", tcol: make_color_rgb(220, 140, 150), n: _cr_n,
              tag:  "An Inequivalent Exchange... feed a legendary to the dark",
              body: "Surrender a legendary to what waits below the cauldron. It returns REBORN - a surge of power ROLLED by the dark, never twice the same - but it keeps something of yours: a curse, also rolled, that no craft can lift. Feed it again and the asks multiply.",
              costs: [ "First feed:  " + string(cha_price(700)) + "g + 2 potions + 1 epic (or 3 rares / 12 uncommons)",
                       "Every curse on the item raises the ask x1.5" ] },
        ];
        for (var _rbi = 0; _rbi < 3; _rbi++) {
            var _rby   = ui_maren_row(_rbi, _rbi == _cursor, 285);
            var _rb_on = (_rb_cards[_rbi].n > 0);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_rb_on ? _rb_cards[_rbi].tcol : make_color_rgb(115, 118, 122));
            draw_text(_list_x + 24, _rby - 9, _rb_cards[_rbi].title + "   (" + string(_rb_cards[_rbi].n) + " eligible)");
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(145, 152, 148));
            draw_text(_list_x + 24, _rby + 21, _rb_cards[_rbi].tag);
            draw_set_font(ui_font(fnt_ui));
        }

        // ---- Detail panel: the selected craft, explained with room to breathe.
        // Rows end at 285+2*72+66=495; notification owns y999 (UI_BANDS). ----
        var _dp_c   = _rb_cards[clamp(_cursor, 0, 2)];
        var _dpx1 = 300, _dpx2 = 1500, _dpy1 = 531, _dpy2 = 963;
        draw_set_color(make_color_rgb(16, 14, 24));
        draw_rectangle(_dpx1, _dpy1, _dpx2, _dpy2, false);
        draw_set_color(make_color_rgb(150, 110, 220));
        draw_rectangle(_dpx1, _dpy1, _dpx2, _dpy2, true);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_dp_c.tcol);
        draw_text(_dpx1 + 30, _dpy1 + 24, _dp_c.title);
        draw_set_color(make_color_rgb(205, 210, 220));
        var _dp_ty = _dpy1 + 75;
        draw_text_ext(_dpx1 + 30, _dp_ty, _dp_c.body, 39, _dpx2 - _dpx1 - 60);
        _dp_ty += string_height_ext(_dp_c.body, 39, _dpx2 - _dpx1 - 60) + 30;
        draw_set_color(make_color_rgb(200, 180, 130));
        draw_text(_dpx1 + 30, _dp_ty, "COST");
        _dp_ty += 36;
        draw_set_color(make_color_rgb(185, 190, 200));
        for (var _dci = 0; _dci < array_length(_dp_c.costs); _dci++) {
            draw_text(_dpx1 + 54, _dp_ty, _dp_c.costs[_dci]);
            _dp_ty += 33;
        }
        _dp_ty += 15;
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color((_dp_c.n > 0) ? make_color_rgb(150, 200, 160) : make_color_rgb(170, 120, 120));
        draw_text(_dpx1 + 30, _dp_ty, (_dp_c.n > 0)
            ? string(_dp_c.n) + " eligible item" + ((_dp_c.n == 1) ? "" : "s") + " - press Enter to choose one."
            : "No eligible items right now.");
        draw_set_font(ui_font(fnt_ui));
    }

    // Notification (ERRORS only since 08-15 - successes use the result toast).
    // Bottom-clamped above the legend so the two can never collide (M shot:
    // the cauldron line sat on top of "W/S: Navigate...").
    if (_gc.sable_notification != "") {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(180, 220, 160));
        var _sn_h = string_height("Ag");
        draw_text(960, min(999, 1014 - _sn_h), _gc.sable_notification);
        draw_set_halign(fa_left);
    }

    // Controls hint (raised to clear the bottom rim band)
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(70, 95, 78));
    ui_draw_key_legend(960, 1026, "W/S: Navigate    Q/E: Tab    Enter: Select    F: Gift    U: Station    Esc: Back / Close");
    draw_set_halign(fa_left);

    // Ornate gothic rim around the whole overlay (see Maren screen for geometry notes).
    ui_draw_gothic_frame(30, 30, 1890, 1050, 30);

    // Checkout confirm popup (M 07-27 standing rule) - drawn LAST, over the rim.
    // Salvage gear / scrap rune / transmute / fusion / chaotic all arm it; the
    // Step block is modal while it's up ("sable:ok" / "sable:cancel").
    if (variable_instance_exists(_gc, "sable_confirm") && _gc.sable_confirm) {
        ui_draw_checkout_confirm(
            variable_instance_exists(_gc, "sable_confirm_title") ? _gc.sable_confirm_title : "CONFIRM",
            variable_instance_exists(_gc, "sable_confirm_body")  ? _gc.sable_confirm_body  : "",
            "sable:ok", "sable:cancel",
            variable_instance_exists(_gc, "sable_confirm_subject")     ? _gc.sable_confirm_subject     : "",
            variable_instance_exists(_gc, "sable_confirm_subject_col") ? _gc.sable_confirm_subject_col : undefined,
            variable_instance_exists(_gc, "sable_confirm_subject_sub") ? _gc.sable_confirm_subject_sub : "");
    }
    ui_draw_checkout_vfx();   // Gigapack cauldron burst (fired at commit)

    // Result TOAST + transmute rune-reveal card (M 08-15) - drawn TOPMOST per
    // the toast standard. The toast is the boxed one-liner; the card shows the
    // new rune's icon, colored name and effect so the player SEES what they got.
    if (variable_instance_exists(_gc, "sable_toast_timer") && _gc.sable_toast_timer > 0
        && _gc.sable_toast_msg != "") {
        var _sta = min(1, _gc.sable_toast_timer / 40);   // fade over the last 40f
        ui_draw_toast(_gc.sable_toast_msg, 960, 176, _sta);
    }
    if (variable_instance_exists(_gc, "sable_result_rune") && _gc.sable_result_rune != undefined
        && _gc.sable_result_timer > 0) {
        var _rrx0 = 660, _rry0 = 400, _rrx1 = 1260, _rry1 = 520;
        draw_set_alpha(0.96);
        draw_set_color(make_color_rgb(20, 24, 22));
        draw_rectangle(_rrx0, _rry0, _rrx1, _rry1, false);
        draw_set_alpha(1.0);
        draw_set_color(make_color_rgb(245, 195, 80));
        draw_rectangle(_rrx0, _rry0, _rrx1, _rry1, true);
        draw_rectangle(_rrx0 + 4, _rry0 + 4, _rrx1 - 4, _rry1 - 4, true);
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(200, 185, 150));
        draw_text((_rrx0 + _rrx1) / 2, _rry0 + 12, "THE CAULDRON YIELDS");
        draw_set_halign(fa_left);
        // Rune row (icon + colored name + effect) via the standard entry drawer.
        ui_draw_rune_entry(_rrx0 + 40, _rry0 + 57, _gc.sable_result_rune);
    }

    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_vael_screen()
// Vael the Aesthete - transmog/skin selection overlay. Single list of skins with
// a live preview of the highlighted skin. Reuses ui_maren_row for row geometry.
// Layout constants MUST match the Vael input block in obj_game_controller Step.
// ---------------------------------------------------------------------------
function ui_draw_vael_screen() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "vael_open") || !_gc.vael_open) return;

    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(16, 12, 18));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);

    // Title + gold
    draw_set_halign(fa_center);
    draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(make_color_rgb(210, 170, 230));
    draw_text(960, 36, "Vael the Aesthete");
    draw_set_halign(fa_right);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(230, 210, 150));
    draw_text(1860, 48, "Gold: " + string(global.gold));
    ui_draw_reagent_readout(1860, 78);   // reagents under the gold (M 08-26); clears the y96 tabs
    draw_set_halign(fa_left);

    // Bond readout - watch transmog purchases raise the relationship live.
    ui_draw_npc_bond("vael", 90, 45);
    ui_draw_npc_column("vael");   // sprite column (skin preview box narrowed to clear it)

    // --- Tabs: Skins | Portrait | Tints | Reweave (geometry MUST match the Vael
    //     input block: 4 headers centred on x960, 960 + (t-1.5)*240, +-108) ---
    var _vtab = variable_instance_exists(_gc, "vael_tab") ? _gc.vael_tab : 0;
    var _vtab_names = ["Skins", "Portrait", "Tints", "Reweave"];
    draw_set_font(ui_font(fnt_ui));
    // Tab group centred on x960 (matching the centred title above) and dropped to
    // y96 so the title's descenders clear the tab tops (M 2026-07-06: the group
    // sat centred on 1080 and its top row touched "Vael the Aesthete").
    for (var _vt = 0; _vt < 4; _vt++) {
        var _vtx   = 960 + (_vt - 1.5) * 240;
        var _vt_on = (_vt == _vtab);
        draw_set_color(_vt_on ? make_color_rgb(60, 46, 86) : make_color_rgb(26, 22, 34));
        draw_rectangle(_vtx - 108, 96, _vtx + 108, 144, false);
        draw_set_color(_vt_on ? make_color_rgb(160, 120, 230) : make_color_rgb(60, 54, 78));
        draw_rectangle(_vtx - 108, 96, _vtx + 108, 144, true);
        draw_set_halign(fa_center);
        draw_set_color(_vt_on ? c_white : make_color_rgb(150, 140, 165));
        draw_text(_vtx, 108, _vtab_names[_vt]);
        draw_set_halign(fa_left);
    }

    // The Portrait / Tints / Reweave tabs are self-contained - draw them and
    // return early, so the skins list + detail panel below only run for tab 0.
    if (_vtab == 1) {
        ui_draw_vael_portrait_tab(_gc);
        return;
    }
    if (_vtab == 2) {
        ui_draw_vael_tints_tab(_gc);
        return;
    }
    if (_vtab == 3) {
        ui_draw_vael_reweave_tab(_gc);
        return;
    }

    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(160, 140, 175));
    draw_text(300, 165, "Transmog - change your combat look. Owned skins switch freely; locked skins need a milestone.");

    var _catalog = vael_skin_catalog_visible();   // class-gated view (08-14)
    var _count   = array_length(_catalog);
    var _cursor  = clamp(_gc.vael_cursor, 0, _count - 1);
    var _active  = variable_global_exists("player_skin") ? global.player_skin : "default";

    // Windowed list (left column, x200..800). Scroll derivation MUST match the Vael
    // input block in obj_game_controller Step (vael_list_scroll).
    var _vis    = ui_vendor_visible_rows(10);   // 10 default; fewer on Large (M 08-14 font pass)
    var _scroll = ui_list_window("vael_skins", _cursor, _count, _vis);
    var _list_y = 225;
    var _row_h  = ui_vendor_row_pitch();

    for (var _v = 0; _v < _vis; _v++) {
        var _i = _scroll + _v;
        if (_i >= _count) break;
        var _sk = _catalog[_i];
        var _ry = _list_y + _v * _row_h;
        var _is_cur   = (_i == _cursor);
        var _owned    = vael_skin_owned(_sk.id);
        var _equipped = (_sk.id == _active);
        var _unlocked = vael_skin_unlocked(_sk);

        // Row frame (narrowed to 1160 so it clears the preview box at x1190)
        draw_set_color(_is_cur ? make_color_rgb(45, 38, 66) : make_color_rgb(20, 18, 30));
        draw_rectangle(300, _ry, 1160, _ry + (_row_h - 6), false);
        draw_set_color(_is_cur ? make_color_rgb(150, 110, 220) : make_color_rgb(45, 42, 62));
        draw_rectangle(300, _ry, 1160, _ry + (_row_h - 6), true);
        var _ty = _ry + 15;

        // Mini swatch (guard missing art -> small placeholder dot) - normalised to a
        // ~51px icon centred in the row's swatch box, accounting for top-left origin.
        if (_sk.sprite != undefined && _sk.sprite != -1 && sprite_exists(_sk.sprite)) {
            var _sw_sc = 51 / max(1, sprite_get_height(_sk.sprite));
            var _sw_w  = sprite_get_width(_sk.sprite)  * _sw_sc;
            var _sw_h  = sprite_get_height(_sk.sprite) * _sw_sc;
            draw_sprite_ext(_sk.sprite, player_sprite_frame(_sk.sprite), 345 - _sw_w / 2, (_ry + 33) - _sw_h / 2, _sw_sc, _sw_sc, 0, c_white, _unlocked ? 1 : 0.4);
        } else if (_sk.sprite != undefined) {
            draw_set_color(make_color_rgb(40, 38, 55));
            draw_rectangle(324, _ry + 12, 366, _ry + 54, false);
        }

        // Name (+ gender marker), greyed if locked
        var _name_col = _equipped ? make_color_rgb(180, 240, 180)
                      : (_unlocked ? (_owned ? make_color_rgb(210, 200, 220) : make_color_rgb(195, 185, 205))
                                   : make_color_rgb(120, 112, 132));
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_name_col);
        var _gtag = (variable_struct_exists(_sk, "gender") && _sk.gender == "f") ? " (F)"
                  : ((variable_struct_exists(_sk, "gender") && _sk.gender == "m") ? "" : "");
        draw_text(387, _ty, _sk.name + _gtag);

        // Right-side short status
        draw_set_halign(fa_right);
        draw_set_font(ui_font(fnt_ui_small));
        if (_equipped) {
            draw_set_color(make_color_rgb(150, 230, 150)); draw_text(1145, _ty, "EQUIPPED");
        } else if (_owned) {
            draw_set_color(make_color_rgb(160, 200, 240)); draw_text(1145, _ty, "OWNED");
        } else if (!_unlocked) {
            draw_set_color(make_color_rgb(150, 110, 120)); draw_text(1145, _ty, "LOCKED");
        } else {
            var _vdisc = vael_cosmetic_price(_sk.gold);   // Friend perk + Atelier rank
            draw_set_color((global.gold >= _vdisc) ? make_color_rgb(230, 210, 150) : make_color_rgb(170, 120, 120));
            draw_text(1145, _ty, string(_vdisc) + "g");
        }
        draw_set_halign(fa_left);
    }

    // Scroll indicator
    if (_count > _vis) {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(110, 95, 130));
        draw_text(750, _list_y + _vis * _row_h + 3,
            string(_scroll + 1) + "-" + string(min(_scroll + _vis, _count)) + " of " + string(_count) + "   (W/S)");
        draw_set_halign(fa_left);
    }

    // ----- Detail / preview panel (right, x1260..1875) -----
    var _sel = _catalog[_cursor];
    var _sel_unlocked = vael_skin_unlocked(_sel);
    draw_set_color(make_color_rgb(18, 14, 22));
    draw_rectangle(1190, 225, 1510, 930, false);
    draw_set_color(make_color_rgb(70, 55, 90));
    draw_rectangle(1190, 225, 1510, 930, true);

    // Resolve preview sprite (default look is gender-aware; missing art = placeholder)
    var _prev_spr = _sel.sprite;
    var _prev_missing = false;
    if (_prev_spr == undefined) {
        var _cid = variable_global_exists("chosen_class") ? clamp(global.chosen_class, 0, 2) : 0;
        var _gen = variable_global_exists("player_gender") ? global.player_gender : "m";
        var _cls_m = [spr_arcanist, spr_bloodwarden, spr_shadowstrider];
        _prev_spr = _cls_m[_cid];
        if (_gen == "f") {
            var _ff = asset_get_index(["spr_arcanist_f", "spr_bloodwarden_f", "spr_shadowstrider_f"][_cid]);
            if (_ff != -1 && sprite_exists(_ff)) _prev_spr = _ff;
        }
    } else if (_prev_spr == -1 || !sprite_exists(_prev_spr)) {
        _prev_missing = true;
    }

    if (!_prev_missing) {
        // Centre + size-normalise: these sprites have a top-left origin and varied
        // canvas sizes (92-108px), so scale to a target height and offset by half.
        var _pv_cx = 1350, _pv_cy = 525;   // recentred for the narrowed preview box
        var _pv_sc = 315 / max(1, sprite_get_height(_prev_spr));
        var _pv_w  = sprite_get_width(_prev_spr)  * _pv_sc;
        var _pv_h  = sprite_get_height(_prev_spr) * _pv_sc;
        draw_sprite_ext(_prev_spr, player_sprite_frame(_prev_spr), _pv_cx - _pv_w / 2, _pv_cy - _pv_h / 2, _pv_sc, _pv_sc, 0, c_white, _sel_unlocked ? 1 : 0.45);
    } else {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(90, 80, 105));
        draw_text(1350, 525, "(art pending)");
        draw_set_halign(fa_left);
    }

    // Name + gender
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(225, 205, 235));
    draw_text(1350, 705, _sel.name);

    // Description (centred in the narrowed box)
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 140, 165));
    draw_text_ext(1350, 753, _sel.desc, -1, 290);
    draw_set_halign(fa_left);

    // Status / requirement line
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui));
    if (_sel.id == _active) {
        draw_set_color(make_color_rgb(150, 230, 150)); draw_text(1350, 882, "Equipped");
    } else if (vael_skin_owned(_sel.id)) {
        draw_set_color(make_color_rgb(160, 200, 240)); draw_text(1350, 882, "Owned - Enter to wear");
    } else if (!_sel_unlocked) {
        draw_set_color(make_color_rgb(220, 130, 130)); draw_text(1350, 882, "Locked - " + vael_skin_req_text(_sel));
    } else {
        draw_set_color((global.gold >= _sel.gold) ? make_color_rgb(230, 210, 150) : make_color_rgb(190, 130, 130));
        draw_text(1350, 882, string(_sel.gold) + "g - Enter to buy");
    }
    draw_set_halign(fa_left);

    // Notification
    if (_gc.vael_notification != "") {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(220, 190, 230));
        draw_text(960, 999, _gc.vael_notification);
        draw_set_halign(fa_left);
    }

    // Controls hint (raised to clear the bottom rim band)
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(90, 75, 100));
    ui_draw_key_legend(960, 1026, "W/S: Navigate    Enter: Buy / Wear    Q/E: Switch tab    F: Gift    U: Station    Esc: Close");
    draw_set_halign(fa_left);

    // Ornate gothic rim around the whole overlay (see Maren screen for geometry notes).
    // The skin detail panel (x1260..1875) sits inside the opening; the rim band is outside.
    ui_draw_gothic_frame(30, 30, 1890, 1050, 30);

    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_vael_tints_tab(gc)
// Tints tab of the Vael overlay (expression #4) - purchased spell palettes that
// recolor a school everywhere school_color() is consulted, plus the cast VFX
// blend in combat. List geometry mirrors the Skins tab and MUST match the Tints
// input block in obj_game_controller Step (x300..1160, y225, row 72, 10 visible).
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// ui_draw_vael_reweave_tab(_gc)
// REWEAVE (moved from Vex trainer tab 5, M 07-28 - "the term fits her"):
// unweave an ability's talent web for gold + dust; its picks return as Talent
// Points. Rows x300..1500, y225, 72 pitch, 10 visible, window "vael_reweave" -
// geometry MUST match the vael_tab == 3 hit-test in obj_game_controller Step.
// Confirm = the shared checkout popup (vael:ok / vael:cancel).
// ---------------------------------------------------------------------------
function ui_draw_vael_reweave_tab(_gc) {
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(160, 140, 175));
    draw_text(300, 165, "Reweave - Vael unpicks an ability's talent web; every woven pick returns as Talent Points.  [Tab] How it works");

    var _rw_list = vael_reweave_rows();   // trunk row first when picks exist (P2, 08-05)
    var _rw_n    = array_length(_rw_list);
    var _rw_g    = cha_price(100);

    // Dust on hand (gold already lives in the header).
    draw_set_halign(fa_right);
    draw_set_color(make_color_rgb(150, 175, 230));
    draw_text(1500, 165, "Rune dust: " + string(variable_global_exists("rune_dust") ? global.rune_dust : 0));
    draw_set_halign(fa_left);

    if (_rw_n == 0) {
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(120, 124, 138));
        draw_text(300, 243, "No woven webs to reweave.");
        draw_set_font(ui_font(fnt_ui_small));
        draw_text(300, 285, "Spend Talent Points on an ability from the dungeon-gate loadout ([M] on its row), then Vael can unweave them here.");
    } else {
        var _cursor = clamp(_gc.vael_rw_cursor, 0, _rw_n - 1);
        var _vis    = ui_vendor_visible_rows(10);
        var _rw_p   = ui_vendor_row_pitch();
        var _scroll = ui_list_window("vael_reweave", _cursor, _rw_n, _vis);
        for (var _v = 0; _v < _vis; _v++) {
            var _i = _scroll + _v;
            if (_i >= _rw_n) break;
            var _rw = _rw_list[_i];
            var _ry = 225 + _v * _rw_p;
            var _on = (_i == _cursor);
            var _rt = variable_struct_exists(_rw, "is_trunk") && _rw.is_trunk;
            // The trunk row wears the trunk's amber; ability rows keep Vael's violet.
            if (_rt) draw_set_color(_on ? make_color_rgb(56, 44, 24) : make_color_rgb(28, 24, 16));
            else     draw_set_color(_on ? make_color_rgb(45, 38, 66) : make_color_rgb(20, 18, 30));
            draw_rectangle(300, _ry, 1500, _ry + (_rw_p - 6), false);
            if (_rt) draw_set_color(_on ? make_color_rgb(210, 175, 90) : make_color_rgb(90, 75, 45));
            else     draw_set_color(_on ? make_color_rgb(150, 110, 220) : make_color_rgb(45, 42, 62));
            draw_rectangle(300, _ry, 1500, _ry + (_rw_p - 6), true);
            var _rw_bd = ui_vendor_body_dy() + 2;   // measured (was hard +40)
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_on ? c_white : make_color_rgb(180, 175, 200));
            draw_text(324, _ry + 6, _rw.name);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(130, 125, 150));
            draw_text(324, _ry + _rw_bd, _rt
                ? (string(_rw.picks) + " sealed class choice" + (_rw.picks == 1 ? "" : "s") + " - reopens every row")
                : (string(_rw.picks) + " pick" + (_rw.picks == 1 ? "" : "s") + " woven"));
            draw_set_halign(fa_right);
            draw_set_color(make_color_rgb(230, 210, 150));
            draw_text(1476, _ry + 6, _rt ? (string(cha_price(500)) + "g + 50 dust") : (string(_rw_g) + "g + 10 dust"));
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(130, 125, 150));
            draw_text(1476, _ry + _rw_bd, _on ? (_rt ? "Enter: unmake" : "Enter: unweave") : "");
            draw_set_halign(fa_left);
        }
        // Scroll bar (standing rule: overflowing lists show a visible bar).
        if (_rw_n > _vis) {
            var _sb_y0 = 225, _sb_y1 = 225 + _vis * _rw_p - 6;
            draw_set_color(make_color_rgb(34, 38, 50));
            draw_rectangle(1506, _sb_y0, 1512, _sb_y1, false);
            var _sb_h = _sb_y1 - _sb_y0;
            draw_set_color(make_color_rgb(150, 110, 220));
            draw_rectangle(1506, _sb_y0 + _sb_h * (_scroll / _rw_n), 1512,
                _sb_y0 + _sb_h * (min(_rw_n, _scroll + _vis) / _rw_n), false);
        }
    }

    // Mechanic explainer ([Tab]) + the checkout popup - drawn last, on top.
    if (variable_instance_exists(_gc, "vael_rw_detail") && _gc.vael_rw_detail) {
        ui_draw_reweave_detail();
    }
    if (variable_instance_exists(_gc, "vael_confirm") && _gc.vael_confirm) {
        ui_draw_checkout_confirm(_gc.vael_confirm_title, _gc.vael_confirm_body, "vael:ok", "vael:cancel");
    }
    ui_draw_checkout_vfx();   // Gigapack unweave burst (fired at commit)
}

function ui_draw_vael_tints_tab(_gc) {
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(160, 140, 175));
    draw_text(300, 165, "Spell tints - recolor a school's log words, numbers and cast flare. Enter on an equipped tint reverts it.");

    var _catalog = vael_tint_catalog();
    var _count   = array_length(_catalog);
    var _cursor  = clamp(_gc.vael_tint_cursor, 0, _count - 1);

    var _vis    = ui_vendor_visible_rows(10);   // 10 default; fewer on Large (M 08-14 font pass)
    var _scroll = ui_list_window("vael_tints", _cursor, _count, _vis);
    var _list_y = 225;
    var _row_h  = ui_vendor_row_pitch();

    for (var _v = 0; _v < _vis; _v++) {
        var _i = _scroll + _v;
        if (_i >= _count) break;
        var _tn = _catalog[_i];
        var _ry = _list_y + _v * _row_h;
        var _is_cur   = (_i == _cursor);
        var _owned    = vael_tint_owned(_tn.id);
        var _equipped = (school_tint_id(_tn.school) == _tn.id);

        draw_set_color(_is_cur ? make_color_rgb(45, 38, 66) : make_color_rgb(20, 18, 30));
        draw_rectangle(300, _ry, 1160, _ry + (_row_h - 6), false);
        draw_set_color(_is_cur ? make_color_rgb(150, 110, 220) : make_color_rgb(45, 42, 62));
        draw_rectangle(300, _ry, 1160, _ry + (_row_h - 6), true);
        var _ty = _ry + 15;

        // Color swatch: the tint itself, ringed in the school's BASE color so the
        // before/after reads at a glance.
        draw_set_color(_tn.color);
        draw_circle(345, _ry + 33, 17, false);
        draw_set_color(school_base_color(_tn.school));
        draw_circle(345, _ry + 33, 21, true);

        // Name + school tag
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_equipped ? make_color_rgb(180, 240, 180) : make_color_rgb(210, 200, 220));
        draw_text(387, _ty, _tn.name);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(_tn.color);
        draw_text(720, _ty + 3, school_label(_tn.school));

        // Right-side status
        draw_set_halign(fa_right);
        if (_equipped) {
            draw_set_color(make_color_rgb(150, 230, 150)); draw_text(1145, _ty, "EQUIPPED");
        } else if (_owned) {
            draw_set_color(make_color_rgb(160, 200, 240)); draw_text(1145, _ty, "OWNED");
        } else {
            var _tdisc = vael_cosmetic_price(_tn.gold);   // Friend perk + Atelier rank
            draw_set_color((global.gold >= _tdisc) ? make_color_rgb(230, 210, 150) : make_color_rgb(170, 120, 120));
            draw_text(1145, _ty, string(_tdisc) + "g");
        }
        draw_set_halign(fa_left);
    }

    if (_count > _vis) {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(110, 95, 130));
        draw_text(750, _list_y + _vis * _row_h + 3,
            string(_scroll + 1) + "-" + string(min(_scroll + _vis, _count)) + " of " + string(_count) + "   (W/S)");
        draw_set_halign(fa_left);
    }

    // ----- Detail / preview panel (matches the skins panel box) -----
    var _sel = _catalog[_cursor];
    draw_set_color(make_color_rgb(18, 14, 22));
    draw_rectangle(1190, 225, 1510, 930, false);
    draw_set_color(make_color_rgb(70, 55, 90));
    draw_rectangle(1190, 225, 1510, 930, true);

    // Big swatch + a sample combat-log line rendered in the tint so the buyer sees
    // exactly what changes.
    draw_set_color(_sel.color);
    draw_circle(1350, 375, 60, false);
    draw_set_color(school_base_color(_sel.school));
    draw_circle(1350, 375, 66, true);
    // Looping sample spell VFX in the candidate tint (M 07-08): the impact burst
    // replays over the swatch so the color is seen IN MOTION, not just as paint.
    var _tint_fx = asset_get_index("spr_fx_impact");
    if (_tint_fx != -1 && sprite_exists(_tint_fx)) {
        var _tf_n  = max(1, sprite_get_number(_tint_fx));
        var _tf_fr = (current_time div 90) mod _tf_n;
        var _tf_sc = 132 / max(1, sprite_get_height(_tint_fx));
        draw_sprite_ext(_tint_fx, _tf_fr,
            1350 - (sprite_get_width(_tint_fx)  * _tf_sc) / 2,
            375  - (sprite_get_height(_tint_fx) * _tf_sc) / 2,
            _tf_sc, _tf_sc, 0, _sel.color, 0.95);
    }

    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 140, 165));
    draw_text(1350, 480, "Base " + school_label(_sel.school) + ":");
    draw_set_color(school_base_color(_sel.school));
    draw_text(1350, 516, school_label(_sel.school) + " strike (+12)!");
    draw_set_color(make_color_rgb(150, 140, 165));
    draw_text(1350, 573, "Tinted:");
    draw_set_color(_sel.color);
    draw_text(1350, 609, school_label(_sel.school) + " strike (+12)!");

    // Name + description
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(225, 205, 235));
    draw_text(1350, 705, _sel.name);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 140, 165));
    draw_text_ext(1350, 753, _sel.desc, -1, 290);

    // Status line
    draw_set_font(ui_font(fnt_ui));
    if (school_tint_id(_sel.school) == _sel.id) {
        draw_set_color(make_color_rgb(150, 230, 150)); draw_text(1350, 882, "Equipped - Enter to revert");
    } else if (vael_tint_owned(_sel.id)) {
        draw_set_color(make_color_rgb(160, 200, 240)); draw_text(1350, 882, "Owned - Enter to equip");
    } else {
        var _sdisc = vael_cosmetic_price(_sel.gold);
        draw_set_color((global.gold >= _sdisc) ? make_color_rgb(230, 210, 150) : make_color_rgb(190, 130, 130));
        draw_text(1350, 882, string(_sdisc) + "g - Enter to buy");
    }
    draw_set_halign(fa_left);

    // Notification + controls + rim (same shell as the skins tab)
    if (_gc.vael_notification != "") {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(220, 190, 230));
        draw_text(960, 999, _gc.vael_notification);
        draw_set_halign(fa_left);
    }
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(90, 75, 100));
    ui_draw_key_legend(960, 1026, "W/S: Navigate    Enter: Buy / Equip / Revert    Q/E: Switch tab    F: Gift    U: Station    Esc: Close");
    draw_set_halign(fa_left);

    ui_draw_gothic_frame(30, 30, 1890, 1050, 30);

    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_vael_portrait_tab(gc)
// Portrait tab of the Vael overlay - a carousel over global.portrait_sprites.
// Changing to a NEW portrait costs 100g (charged in the Vael input block in
// obj_game_controller Step). Title/gold/tabs are already drawn by the caller.
// ---------------------------------------------------------------------------
function ui_draw_vael_portrait_tab(_gc) {
    var _ports  = global.portrait_sprites;
    var _pcount = array_length(_ports);
    if (_pcount <= 0) return;
    var _cur    = clamp(_gc.vael_portrait_cursor, 0, _pcount - 1);
    var _active = clamp(variable_global_exists("chosen_portrait") ? global.chosen_portrait : 0, 0, _pcount - 1);

    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(160, 140, 175));
    draw_set_halign(fa_center);
    draw_text(960, 165, "Choose a new portrait. Switching to a different one costs 100g.");

    // Center portrait (large)
    var _main_w = 450, _main_h = 450;
    var _main_x = GUI_CX - _main_w / 2;
    var _main_y = 252;
    draw_set_color(make_color_rgb(18, 14, 22));
    draw_rectangle(_main_x - 6, _main_y - 6, _main_x + _main_w + 6, _main_y + _main_h + 6, false);
    draw_sprite_stretched(_ports[_cur], 0, _main_x, _main_y, _main_w, _main_h);
    draw_set_color((_cur == _active) ? make_color_rgb(150, 230, 150) : make_color_rgb(160, 120, 230));
    draw_rectangle(_main_x - 6, _main_y - 6, _main_x + _main_w + 6, _main_y + _main_h + 6, true);
    ui_draw_gothic_frame(_main_x - 6, _main_y - 6, _main_x + _main_w + 6, _main_y + _main_h + 6, 33);   // ornate portrait frame

    // Side thumbnails (prev / next), dimmed
    if (_pcount > 1) {
        var _thumb_w = 180, _thumb_h = 180, _thumb_y = _main_y + 135;
        var _prev = (_cur - 1 + _pcount) mod _pcount;
        var _next = (_cur + 1) mod _pcount;
        draw_sprite_stretched_ext(_ports[_prev], 0, _main_x - _thumb_w - 42, _thumb_y, _thumb_w, _thumb_h, c_white, 0.5);
        draw_sprite_stretched_ext(_ports[_next], 0, _main_x + _main_w + 42, _thumb_y, _thumb_w, _thumb_h, c_white, 0.5);
    }

    // Counter - the gothic frame's band extends ~33px BELOW the portrait rect
    // (to y~741), so these lines start at +51/+96 to clear it (07-13 report:
    // "3 / 60" was drawn into the frame's bottom filigree).
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(180, 165, 195));
    draw_text(960, _main_y + _main_h + 51, string(_cur + 1) + " / " + string(_pcount));

    // Status / confirm line
    if (_cur == _active) {
        draw_set_color(make_color_rgb(150, 230, 150));
        draw_text(960, _main_y + _main_h + 96, "Your current portrait");
    } else if (global.gold >= 100) {
        draw_set_color(make_color_rgb(230, 210, 150));
        draw_text(960, _main_y + _main_h + 96, "100g  -  Enter to set as your portrait");
    } else {
        draw_set_color(make_color_rgb(190, 130, 130));
        draw_text(960, _main_y + _main_h + 96, "Not enough gold (need 100g)");
    }

    // Notification
    if (_gc.vael_notification != "") {
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(220, 190, 230));
        draw_text(960, 999, _gc.vael_notification);
    }

    // Controls (raised to clear the bottom rim band)
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(90, 75, 100));
    ui_draw_key_legend(960, 1026, "A/D: Browse    Q/E: Switch tab    Enter: Set (100g)    Esc: Close");
    draw_set_halign(fa_left);

    // Ornate gothic rim around the whole overlay (matches the skins tab + Maren/Sable).
    ui_draw_gothic_frame(30, 30, 1890, 1050, 30);

    draw_set_valign(fa_top);
    draw_set_alpha(1.0);
    draw_set_font(-1);
}

// Stateless scroll window for the Vael skin list - shared by draw + input so the
// visible rows and mouse hit-testing always agree.
function vael_list_scroll(_cursor, _count, _vis) {
    if (_count <= _vis) return 0;
    return clamp(_cursor - floor(_vis / 2), 0, _count - _vis);
}

// ---------------------------------------------------------------------------
// ui_draw_item_picker()
// The shared "choose an item to sacrifice + confirm" modal (see
// SYSTEMS_ITEM_PICKER.md). Layers over whatever screen opened it; geometry MUST
// match item_picker_step() hit-testing in scr_stats. No-op when not open.
// ---------------------------------------------------------------------------
function ui_draw_item_picker() {
    if (!variable_global_exists("item_picker") || !global.item_picker.open) return;
    var _p = global.item_picker;
    var _n = array_length(_p.candidates);

    // Geometry - MUST stay in sync with item_picker_step() hit-testing (scr_stats).
    var _px = 330, _py = 165, _pw = 1260, _ph = 750;
    // Left list column.
    var _lx0 = _px + 24;            // row highlight left edge
    var _lx1 = _px + 606;           // row highlight right edge
    var _ly0 = _py + 129;           // first row top
    var _rh  = 57;                  // row pitch
    // Right detail pane.
    var _dvx = _px + 624;           // divider x
    var _dx  = _px + 648;           // detail content left
    var _dr  = _px + _pw - 30;      // detail content right
    // Confirm/footer band.
    var _cby0 = _py + _ph - 114, _cby1 = _py + _ph - 60;

    // Dim the whole screen, then the panel.
    draw_set_alpha(0.6); draw_set_color(c_black);
    draw_rectangle(0, 0, display_get_gui_width(), display_get_gui_height(), false);
    draw_set_alpha(0.97); draw_set_color(make_color_rgb(22, 22, 34));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, false);
    draw_set_alpha(1.0); draw_set_color(make_color_rgb(150, 150, 180));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, true);

    // Header
    draw_set_halign(fa_center); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(255, 225, 150));
    draw_text(_px + _pw / 2, _py + 21, item_picker_prompt());
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(c_ltgray);
    draw_text(_px + _pw / 2, _py + 63, "(only items you didn't pick are safe - nothing is lost until you confirm)");
    draw_set_halign(fa_left);

    if (_n == 0) {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(220, 120, 120));
        draw_text(_px + _pw / 2, _py + _ph / 2, "No qualifying item to give up.");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(c_ltgray);
        draw_text_outline(_px + _pw / 2, _py + _ph - 45, "Esc / Right-click: back");
        draw_set_halign(fa_left); draw_set_valign(fa_top);
        draw_set_font(-1);
        return;
    }

    // Vertical divider between list and detail pane.
    draw_set_color(make_color_rgb(60, 64, 86));
    draw_line(_dvx, _py + 114, _dvx, _cby0 - 12);

    // --- Left: windowed list of 8 rows (icon + name + value) -----------------
    draw_set_font(ui_font(fnt_ui_small));
    var _vis = min(8, _n);
    for (var _r = 0; _r < _vis; _r++) {
        var _idx = _p.scroll + _r;
        if (_idx >= _n) break;
        var _c  = _p.candidates[_idx];
        var _ry = _ly0 + _r * _rh;
        if (_idx == _p.cursor) {
            draw_set_alpha(0.30); draw_set_color(make_color_rgb(90, 110, 160));
            draw_rectangle(_lx0, _ry, _lx1, _ry + 51, false);
            draw_set_alpha(1.0);
            draw_set_color(make_color_rgb(120, 150, 210));
            draw_rectangle(_lx0, _ry, _lx1, _ry + 51, true);
        }
        // Inline icon per source: gear (0/1) the full item icon; potions (10) the
        // cyan consumable badge; runes (11) their gem art; feed (12) Petra's feed
        // art; trinkets (13) the catalog icon. (M 07-28: the gift list drew icons
        // for gear only - runes and most other rows sat iconless.)
        if (is_struct(_c.item) && _c.source < 10) {
            ui_draw_item_icon(_lx0 + 6, _ry + 5, 42, _c.item);
        } else if (_c.source == 10 && is_struct(_c.item)) {
            ui_draw_consumable_icon(_lx0 + 6, _ry + 5, 42, _c.item);
        } else if (_c.source == 11 && is_struct(_c.item)) {
            var _pk_ric = rune_icon_sprite(_c.item.id);
            if (_pk_ric != -1) draw_sprite_stretched(_pk_ric, 0, _lx0 + 6, _ry + 5, 42, 42);
            else ui_draw_gem_glyph(_lx0 + 27, _ry + 26, 21, rune_glyph_color(_c.item.id));
        } else if (_c.source == 12 && is_string(_c.item)) {
            var _pk_fic = pet_feed_icon(_c.item);   // feed candidates carry the id string
            if (_pk_fic != -1) draw_sprite_stretched(_pk_fic, 0, _lx0 + 6, _ry + 5, 42, 42);
        } else if (_c.source == 13 && is_struct(_c.item) && variable_struct_exists(_c.item, "id")) {
            var _pk_tic = asset_get_index("spr_icon_trinket_" + _c.item.id);
            if (_pk_tic >= 0) draw_sprite_stretched(_pk_tic, 0, _lx0 + 6, _ry + 5, 42, 42);
        }
        draw_set_halign(fa_left); draw_set_valign(fa_top);
        draw_set_color(item_rarity_color(_c.rarity));
        // Truncate the label to the space LEFT of the measured price (M 08-04
        // screenshot: "(asks DEX 14)" ran into "140g" on long re-attune rows).
        var _pk_val = string(_c.value) + "g";
        var _pk_lw  = (_lx1 - 12 - string_width(_pk_val) - 12) - (_lx0 + 60);
        draw_text(_lx0 + 60, _ry + 12, ui_truncate(_c.label, max(60, _pk_lw)));
        draw_set_halign(fa_right); draw_set_color(c_ltgray);
        draw_text(_lx1 - 12, _ry + 12, _pk_val);
        // Worn gear says so in every NPC picker (M 08-11) - lower line, clear
        // of the label/price line above.
        if (is_struct(_c.item) && _c.source < 10 && item_is_equipped(_c.item)) {
            draw_set_color(make_color_rgb(210, 175, 90));
            draw_text(_lx1 - 12, _ry + 30, "EQUIPPED");
        }
        draw_set_halign(fa_left);
    }
    // Scroll hints
    if (_p.scroll > 0) {
        draw_set_halign(fa_center); draw_set_color(make_color_rgb(120, 140, 170));
        ui_draw_scroll_more((_lx0 + _lx1) / 2, _ly0 - 27, true, "more");
    }
    if (_p.scroll + _vis < _n) {
        draw_set_halign(fa_center); draw_set_color(make_color_rgb(120, 140, 170));
        ui_draw_scroll_more((_lx0 + _lx1) / 2, _ly0 + _vis * _rh + 3, false, "more");
    }
    draw_set_halign(fa_left);

    // --- Right: detail pane for the selected item ----------------------------
    var _cur = _p.candidates[_p.cursor];
    var _it  = _cur.item;
    // Gift-picker non-gear candidates (consumables 10 / runes 11 / feed 12 / trinkets
    // 13) get a SIMPLE pane - the gear pane below reads slots/affixes they don't have.
    if (_cur.source >= 10) {
        var _gdy = _py + 129;
        draw_set_halign(fa_left); draw_set_valign(fa_top);
        // Detail icon badge, per source (trinkets got one 07-06; potions/runes/feed
        // joined 07-28 - M: "in the gift screen runes and many objects icons dont
        // show up"). Resolved sprite + border color per category; label shifts
        // right beside it. draw_sprite_stretched fills the frame regardless of the
        // sprite's origin (the old draw_sprite_ext anchored on the ORIGIN, so
        // centered-origin trinkets floated outside the box - M 07-09 screenshot).
        var _tk_x = _dx;
        var _gp_spr = -1;
        var _gp_bcol = make_color_rgb(120, 130, 160);
        var _gp_glyph = false, _gp_gcol = c_white;
        if (_cur.source == 13 && is_struct(_cur.item) && variable_struct_exists(_cur.item, "id")) {
            _gp_spr  = asset_get_index("spr_icon_trinket_" + _cur.item.id);
            _gp_bcol = make_color_rgb(255, 200, 80);
        } else if (_cur.source == 10 && is_struct(_cur.item)) {
            _gp_spr  = ui_consumable_icon_sprite(_cur.item.name, _cur.item);   // struct enables brew variants
            _gp_bcol = make_color_rgb(80, 200, 200);
        } else if (_cur.source == 11 && is_struct(_cur.item)) {
            _gp_spr  = rune_icon_sprite(_cur.item.id);
            _gp_gcol = rune_glyph_color(_cur.item.id);
            _gp_bcol = _gp_gcol;
            _gp_glyph = (_gp_spr == -1);   // pre-import fallback: the gem glyph
        } else if (_cur.source == 12 && is_string(_cur.item)) {
            _gp_spr  = pet_feed_icon(_cur.item);
            _gp_bcol = make_color_rgb(150, 200, 120);
        }
        if ((_gp_spr >= 0 && sprite_exists(_gp_spr)) || _gp_glyph) {
            draw_set_color(make_color_rgb(20, 18, 12));
            draw_rectangle(_dx, _gdy, _dx + 96, _gdy + 96, false);
            if (_gp_glyph) ui_draw_gem_glyph(_dx + 48, _gdy + 48, 40, _gp_gcol);
            else draw_sprite_stretched(_gp_spr, 0, _dx + 4, _gdy + 4, 88, 88);
            draw_set_color(_gp_bcol);
            draw_rectangle(_dx, _gdy, _dx + 96, _gdy + 96, true);
            _tk_x = _dx + 114;
        }
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_cur.source == 13 ? make_color_rgb(255, 200, 80) : c_white);
        draw_text_ext(_tk_x, _gdy, _cur.label, -1, _dr - _tk_x);
        _gdy += max(string_height_ext(_cur.label, -1, _dr - _tk_x), (_tk_x > _dx) ? 96 : 0) + 15;
        draw_set_font(ui_font(fnt_ui_small));
        if (_cur.source == 13) {
            draw_set_color(make_color_rgb(200, 180, 140));
            var _tk_flav = ui_sentence(_cur.item.flavor);
            draw_text_ext(_dx, _gdy, _tk_flav, 27, _dr - _dx);
            _gdy += string_height_ext(_tk_flav, 27, _dr - _dx) + 15;
            draw_set_color(make_color_rgb(150, 160, 185));
            draw_text_ext(_dx, _gdy, "A signature gift - someone in town would treasure this.", 27, _dr - _dx);
        } else if (_cur.source == 11 && is_struct(_it)) {
            // Runes carry no .desc - show the socket effect like Maren's list does.
            draw_set_color(make_color_rgb(170, 176, 195));
            draw_text_ext(_dx, _gdy, rune_effect(_it), 27, _dr - _dx);
        } else if (is_struct(_it) && variable_struct_exists(_it, "desc")) {
            draw_set_color(make_color_rgb(170, 176, 195));
            draw_text_ext(_dx, _gdy, ui_sentence(_it.desc), 27, _dr - _dx);
        }
    } else if (is_struct(_it)) {
        var _rar  = variable_struct_exists(_it, "rarity") ? _it.rarity : 0;
        var _rcol = item_rarity_color(_rar);
        var _dy   = _py + 129;

        // Big icon + name/rarity/slot header.
        ui_draw_item_icon(_dx, _dy, 96, _it);
        draw_set_halign(fa_left); draw_set_valign(fa_top);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_rcol);
        draw_text_ext(_dx + 114, _dy + 3, _it.name, -1, _dr - (_dx + 114));
        // MEASURED name height (M 08-04 screenshot: long cursed-legendary names
        // wrap to 2 lines and the fixed-offset rarity/slot lines struck through
        // line 2). Single-line names keep the original y39 layout exactly.
        var _pk_nh = string_height_ext(_it.name, -1, _dr - (_dx + 114));
        var _pk_hy = _dy + 3 + max(_pk_nh, 33) + 3;
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(150, 160, 185));
        draw_text(_dx + 114, _pk_hy, item_rarity_name(_rar));
        if (item_is_equipped(_it)) {
            draw_set_color(make_color_rgb(210, 175, 90));
            draw_text(_dx + 114 + string_width(item_rarity_name(_rar)) + 18, _pk_hy, "EQUIPPED");
        }
        if (variable_struct_exists(_it, "slot")) {
            draw_set_color(make_color_rgb(110, 120, 150));
            draw_text(_dx + 114, _pk_hy + 30, string_upper(item_slot_title(_it.slot)));
        }
        // Class restriction (gold = usable by your class, red = locked to another).
        var _pk_cr = variable_struct_exists(_it, "class_req") ? _it.class_req : -1;
        if (_pk_cr != -1) {
            var _pk_names = ["Arcanist", "Bloodwarden", "Shadowstrider"];
            var _pk_name  = (_pk_cr >= 0 && _pk_cr <= 2) ? _pk_names[_pk_cr] : "Unknown";
            var _pk_my    = variable_global_exists("chosen_class") ? global.chosen_class : -1;
            draw_set_color((_pk_cr == _pk_my) ? make_color_rgb(210, 175, 90) : make_color_rgb(225, 80, 80));
            draw_text(_dx + 114, _pk_hy + 57, _pk_name + " only");
        }

        var _cy = max(_dy + 117, _pk_hy + 78);
        draw_set_color(make_color_rgb(50, 55, 80));
        draw_line(_dx, _cy, _dr, _cy);
        _cy += 12;

        // Full stat string (primary + affixes), wrapped.
        draw_set_font(ui_font(fnt_ui_small));
        var _statstr = ui_item_stat_str(_it);
        draw_set_color(c_white);
        draw_text_ext(_dx, _cy, _statstr, -1, _dr - _dx);
        _cy += string_height_ext(_statstr, -1, _dr - _dx) + 9;

        // Stat requirement (red if the would-be wearer can't meet it). Shown here so the
        // equip gate is visible in the picker, not only on the loadout hover-tooltip.
        var _preq = item_stat_requirement(_it);
        if (_preq.value > 0 && _preq.stat != "") {
            var _reqstr = "Requires " + string(_preq.value) + " " + _preq.stat;
            draw_set_color((player_base_stat(_preq.stat) >= _preq.value)
                ? make_color_rgb(110, 170, 110) : make_color_rgb(225, 80, 80));
            draw_text_ext(_dx, _cy, _reqstr, -1, _dr - _dx);
            _cy += string_height_ext(_reqstr, -1, _dr - _dx) + 9;
        }
        var _pk_plr = item_perm_level_req(_it);
        if (_pk_plr > 0) {
            var _pk_reqstr = "Requires Permanent Level " + string(_pk_plr);
            draw_set_color((player_permanent_level() >= _pk_plr)
                ? make_color_rgb(110, 170, 110) : make_color_rgb(225, 80, 80));
            draw_text_ext(_dx, _cy, _pk_reqstr, -1, _dr - _dx);
            _cy += string_height_ext(_pk_reqstr, -1, _dr - _dx) + 9;
        }

        // Unique effect.
        if (variable_struct_exists(_it, "unique_desc") && _it.unique_desc != "") {
            draw_set_color(make_color_rgb(255, 200, 50));
            draw_text_ext(_dx, _cy, _it.unique_desc, -1, _dr - _dx);
            _cy += string_height_ext(_it.unique_desc, -1, _dr - _dx) + 9;
        }

        // Flavor / description.
        var _flavor = "";
        if      (variable_struct_exists(_it, "effect_desc") && _it.effect_desc != "")
            _flavor = _it.effect_desc;
        else if (variable_struct_exists(_it, "description"))
            _flavor = _it.description;
        // (skipped when a deep affix stack has eaten the room - the fee block
        // below must never reach the confirm bar)
        if (_flavor != "" && _cy < _cby0 - 180) {
            draw_set_color(make_color_rgb(110, 120, 145));
            draw_text_ext(_dx, _cy, _flavor, -1, _dr - _dx);
            _cy += string_height_ext(_flavor, -1, _dr - _dx) + 12;
        }

        // Reassemblage fee preview (08-04): the cursed-rebirth picker shows THE
        // EXACT ASK for the selected offering (it scales x1.5 per curse already
        // on it) before the confirm arms - red when the dark can't be paid.
        if (_p.purpose == "cursed_rebirth") {
            var _pkf     = cursed_rebirth_fee(_it);
            var _pk_pts  = cursed_rebirth_pts_avail(_it);
            var _pkp     = (variable_global_exists("consumable_inventory") && is_array(global.consumable_inventory))
                           ? array_length(global.consumable_inventory) : 0;
            var _pk_ok   = (global.gold >= _pkf.gold && _pk_pts >= _pkf.uncommons && _pkp >= _pkf.potions);
            draw_set_color(make_color_rgb(50, 55, 80));
            draw_line(_dx, _cy + 3, _dr, _cy + 3);
            _cy += 15;
            draw_set_color(make_color_rgb(220, 140, 150));
            draw_text(_dx, _cy, "THE DARK ASKS" + ((_pkf.feeds > 0) ? ("   (curse " + string(_pkf.feeds + 1) + ")") : ""));
            _cy += 30;
            draw_set_color(_pk_ok ? make_color_rgb(185, 190, 200) : make_color_rgb(225, 110, 110));
            var _pk_ask = string(_pkf.gold) + "g  +  " + string(_pkf.potions) + " potion" + ((_pkf.potions == 1) ? "" : "s")
                + "  +  " + string(_pkf.epics) + " epic" + ((_pkf.epics == 1) ? "" : "s") + " (or "
                + string(_pkf.rares) + " rares / " + string(_pkf.uncommons) + " uncommons, mixed freely)";
            draw_text_ext(_dx, _cy, _pk_ask, 27, _dr - _dx);
            _cy += string_height_ext(_pk_ask, 27, _dr - _dx) + 6;
            draw_set_color(make_color_rgb(120, 110, 135));
            draw_text_ext(_dx, _cy, _pk_ok
                ? "You choose exactly which pieces burn - next screen."
                : "You cannot pay the dark's price for this offering.", 27, _dr - _dx);
        }

        // Temper fee preview (08-04 tempering): the smith's ask for the
        // selected piece, red when short.
        if (_p.purpose == "maren_temper") {
            var _tpv = temper_fee(_it);
            var _tpv_dust = variable_global_exists("rune_dust") ? global.rune_dust : 0;
            var _tpv_ok = (global.gold >= _tpv.gold && _tpv_dust >= _tpv.dust);
            draw_set_color(make_color_rgb(50, 55, 80));
            draw_line(_dx, _cy + 3, _dr, _cy + 3);
            _cy += 15;
            draw_set_color(make_color_rgb(200, 170, 110));
            draw_text(_dx, _cy, "MAREN ASKS  (per +10%)");
            _cy += 30;
            draw_set_color(_tpv_ok ? make_color_rgb(185, 190, 200) : make_color_rgb(225, 110, 110));
            draw_text(_dx, _cy, string(_tpv.gold) + "g  +  " + string(_tpv.dust) + " dust");
        }

        // Awaken fee preview (08-05): the forge's ask for waking the dormant
        // piece - the epics themselves are hand-picked on the next screen.
        if (_p.purpose == "maren_awaken") {
            var _awp_dust = variable_global_exists("rune_dust") ? global.rune_dust : 0;
            var _awp_eps  = array_length(cursed_rebirth_gear_of_rarity(3, _it));
            var _awp_ok   = (global.gold >= 300 && _awp_dust >= 60 && _awp_eps >= 2);
            draw_set_color(make_color_rgb(50, 55, 80));
            draw_line(_dx, _cy + 3, _dr, _cy + 3);
            _cy += 15;
            draw_set_color(make_color_rgb(200, 170, 110));
            draw_text(_dx, _cy, "MAREN ASKS");
            _cy += 30;
            draw_set_color(_awp_ok ? make_color_rgb(185, 190, 200) : make_color_rgb(225, 110, 110));
            draw_text(_dx, _cy, "300g  +  60 dust  +  2 epics to burn");
            _cy += 30;
            draw_set_color(make_color_rgb(120, 110, 135));
            draw_text_ext(_dx, _cy, _awp_ok
                ? "You choose exactly which epics burn - next screen."
                : "You cannot pay the forge's price yet.", 27, _dr - _dx);
        }
        draw_set_valign(fa_top);
    }

    // --- Footer / confirm bar ------------------------------------------------
    if (_p.confirm) {
        draw_set_alpha(0.95); draw_set_color(make_color_rgb(120, 40, 40));
        draw_rectangle(_px + 30, _cby0, _px + _pw - 30, _cby1, false);
        draw_set_alpha(1.0);
        draw_set_halign(fa_center); draw_set_valign(fa_top);
        draw_set_font(ui_font(fnt_ui_small)); draw_set_color(c_white);
        draw_text(_px + _pw / 2, _cby0 + 14,
            item_picker_verb() + " " + _cur.label + "? This cannot be undone.");
        draw_set_color(c_ltgray);
        ui_draw_key_legend(_px + _pw / 2, _py + _ph - 36, "Enter: Confirm     Esc: Back");
    } else {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small)); draw_set_color(c_ltgray);
        ui_draw_key_legend(_px + _pw / 2, _py + _ph - 36, "W/S: Select     Enter: Choose     Esc: Cancel");
    }

    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white); draw_set_alpha(1.0);
    draw_set_font(-1);
}

// ---------------------------------------------------------------------------
// ui_draw_reagent_picker()
// CURSED-REBIRTH REAGENT STAGE (M 08-05): the multi-select "what exactly burns"
// modal. Junkiest gear + cheapest potions come PRE-TICKED to meet the ask;
// every row toggles; the SEAL bar arms then commits. Geometry MUST match
// reagent_picker_step() hit-testing (scr_stats). No-op when closed.
// ---------------------------------------------------------------------------
function ui_draw_reagent_picker() {
    if (!variable_global_exists("reagent_picker") || global.reagent_picker == undefined) return;
    var _p = global.reagent_picker;
    var _n = array_length(_p.rows);
    var _t = reagent_picker_tally();
    // Purpose skin: Sable's dark exchange (violet) vs Maren's forge fuel (amber).
    var _aw = (_p.purpose == "maren_awaken");

    // Geometry - MUST stay in sync with reagent_picker_step() (scr_stats).
    var _px = 330, _py = 165, _pw = 1260, _ph = 750;
    var _lx0 = _px + 24, _lx1 = _px + 606, _ly0 = _py + 129, _rh = 57;
    var _dvx = _px + 624, _dx = _px + 648, _dr = _px + _pw - 30;
    var _cby0 = _py + _ph - 114, _cby1 = _py + _ph - 60;

    // Dim + panel - same frame as the picker, shifted violet for the dark.
    draw_set_alpha(0.6); draw_set_color(c_black);
    draw_rectangle(0, 0, display_get_gui_width(), display_get_gui_height(), false);
    draw_set_alpha(0.97); draw_set_color(_aw ? make_color_rgb(30, 24, 18) : make_color_rgb(24, 20, 32));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, false);
    draw_set_alpha(1.0); draw_set_color(_aw ? make_color_rgb(200, 170, 110) : make_color_rgb(150, 115, 175));
    draw_rectangle(_px, _py, _px + _pw, _py + _ph, true);

    // Header
    draw_set_halign(fa_center); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(_aw ? make_color_rgb(255, 205, 110) : make_color_rgb(215, 160, 235));
    draw_text(_px + _pw / 2, _py + 21, _aw ? "FUEL FOR THE CRUCIBLE" : "AN INEQUIVALENT EXCHANGE...");
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(c_ltgray);
    draw_text(_px + _pw / 2, _py + 63, _aw
        ? "Tick what burns in Maren's runesmithing crucible - everything unticked is safe. Nothing burns until you light it."
        : "Tick what feeds the dark - everything unticked is safe. Nothing burns until you seal it.");
    draw_set_halign(fa_left);

    // Divider
    draw_set_color(make_color_rgb(60, 64, 86));
    draw_line(_dvx, _py + 114, _dvx, _cby0 - 12);

    // --- Left: reagent rows (8 visible, scrollbar when more) -----------------
    draw_set_font(ui_font(fnt_ui_small));
    var _vis = min(8, _n);
    for (var _r = 0; _r < _vis; _r++) {
        var _idx = _p.scroll + _r;
        if (_idx >= _n) break;
        var _rw = _p.rows[_idx];
        var _ry = _ly0 + _r * _rh;
        if (_rw.sel) {   // ticked = it burns: dark ember wash
            draw_set_alpha(0.25); draw_set_color(make_color_rgb(150, 60, 70));
            draw_rectangle(_lx0, _ry, _lx1, _ry + 51, false);
            draw_set_alpha(1.0);
        }
        if (_idx == _p.cursor && !_p.confirm) {
            draw_set_color(make_color_rgb(120, 150, 210));
            draw_rectangle(_lx0, _ry, _lx1, _ry + 51, true);
        }
        // Checkbox
        var _bx = _lx0 + 8, _by = _ry + 14;
        draw_set_color(_rw.sel ? make_color_rgb(230, 120, 130) : make_color_rgb(110, 115, 140));
        draw_rectangle(_bx, _by, _bx + 24, _by + 24, true);
        if (_rw.sel) {
            draw_line_width(_bx + 5, _by + 13, _bx + 10, _by + 19, 3);
            draw_line_width(_bx + 10, _by + 19, _bx + 20, _by + 5, 3);
        }
        // Icon
        if (_rw.kind == "gear") ui_draw_item_icon(_lx0 + 42, _ry + 5, 42, _rw.item);
        else                    ui_draw_consumable_icon(_lx0 + 42, _ry + 5, 42, _rw.item);
        // Right-side tag: essence worth for gear, "potion" for potions; sell
        // value under it. Name truncates against the widest tag.
        var _tag = (_rw.kind == "gear") ? ("+" + string(_rw.pts)) : "potion";
        draw_set_halign(fa_right);
        draw_set_color((_rw.kind == "gear") ? make_color_rgb(190, 140, 220) : make_color_rgb(80, 200, 200));
        draw_text(_lx1 - 12, _ry + 4, _tag);
        draw_set_color(c_gray);
        draw_text(_lx1 - 12, _ry + 27, string(_rw.value) + "g");
        draw_set_halign(fa_left);
        draw_set_color((_rw.kind == "gear") ? item_rarity_color(_rw.rarity) : make_color_rgb(140, 220, 220));
        var _nw = (_lx1 - 12 - string_width("potion") - 12) - (_lx0 + 96);
        draw_text(_lx0 + 96, _ry + 12, ui_truncate(_rw.label, max(60, _nw)));
    }
    // Visible scrollbar (scrollable-list rule: a BAR, not just hints).
    if (_n > _vis) {
        var _sb_x0 = _lx1 + 8, _sb_x1 = _lx1 + 16;
        var _sb_y0 = _ly0, _sb_y1 = _ly0 + _vis * _rh - 6;
        draw_set_color(make_color_rgb(50, 52, 72));
        draw_rectangle(_sb_x0, _sb_y0, _sb_x1, _sb_y1, false);
        var _th = max(36, (_sb_y1 - _sb_y0) * _vis / _n);
        var _ty = _sb_y0 + (_sb_y1 - _sb_y0 - _th) * _p.scroll / max(1, _n - _vis);
        draw_set_color(make_color_rgb(150, 120, 180));
        draw_rectangle(_sb_x0, _ty, _sb_x1, _ty + _th, false);
    }

    // --- Right: the offering + the ask, tallied live -------------------------
    var _cy = _py + 129;
    var _tgt = _p.target.item;
    // Mega-cursed names (a celebrated flex) can wrap deep - drop a font size
    // past 3 lines so the tallies below never reach the seal bar.
    draw_set_font(ui_font(fnt_ui));
    if (string_height_ext(_p.target.label, -1, _dr - _dx) > 108) draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(item_rarity_color(variable_struct_exists(_tgt, "rarity") ? _tgt.rarity : 4));
    draw_text_ext(_dx, _cy, _p.target.label, -1, _dr - _dx);
    _cy += string_height_ext(_p.target.label, -1, _dr - _dx) + 9;
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(_aw ? make_color_rgb(200, 170, 110) : make_color_rgb(220, 140, 150));
    draw_text(_dx, _cy, _aw ? "MAREN ASKS"
        : ("THE DARK ASKS" + ((_p.fee.feeds > 0) ? ("   (curse " + string(_p.fee.feeds + 1) + ")") : "")));
    _cy += 36;

    // Gold (+ dust for the forge) - taken automatically at the seal; shown so
    // the full price reads here.
    draw_set_color(make_color_rgb(185, 190, 200));
    if (_aw) {
        var _rp_dust = variable_global_exists("rune_dust") ? global.rune_dust : 0;
        draw_text(_dx, _cy, string(_p.fee.gold) + "g  +  " + string(_p.fee.dust) + " dust   (you hold "
            + string(global.gold) + "g, " + string(_rp_dust) + " dust)");
    } else {
        draw_text(_dx, _cy, string(_p.fee.gold) + "g   (you hold " + string(global.gold) + "g)");
    }
    _cy += 42;

    var _bar_w = _dr - _dx;
    // Gear bar: essence for the dark, a straight epic count for the forge.
    draw_set_color(_aw ? make_color_rgb(200, 170, 110) : make_color_rgb(190, 140, 220));
    draw_text(_dx, _cy, (_aw ? "EPICS TO BURN   " : "GEAR ESSENCE   ") + string(_t.pts) + " / " + string(_p.goal));
    _cy += 30;
    draw_set_color(make_color_rgb(40, 36, 54));
    draw_rectangle(_dx, _cy, _dx + _bar_w, _cy + 15, false);
    draw_set_color((_t.pts >= _p.goal) ? (_aw ? make_color_rgb(200, 150, 60) : make_color_rgb(170, 110, 210))
                                       : (_aw ? make_color_rgb(120, 90, 50)  : make_color_rgb(120, 70, 90)));
    draw_rectangle(_dx, _cy, _dx + _bar_w * min(1, _t.pts / max(1, _p.goal)), _cy + 15, false);
    draw_set_color(make_color_rgb(90, 80, 110));
    draw_rectangle(_dx, _cy, _dx + _bar_w, _cy + 15, true);
    _cy += 27;
    if (!_aw) {
        draw_set_color(make_color_rgb(120, 110, 135));
        draw_text(_dx, _cy, "uncommon 1  -  rare 4  -  epic 12   (mix freely)");
        _cy += 42;
    } else {
        _cy += 15;
    }

    // Potions bar (the dark only - the forge takes no potions).
    if (!_aw) {
        draw_set_color(make_color_rgb(80, 200, 200));
        draw_text(_dx, _cy, "POTIONS   " + string(_t.pot) + " / " + string(_p.fee.potions));
        _cy += 30;
        draw_set_color(make_color_rgb(36, 48, 54));
        draw_rectangle(_dx, _cy, _dx + _bar_w, _cy + 15, false);
        draw_set_color((_t.pot >= _p.fee.potions) ? make_color_rgb(70, 180, 180) : make_color_rgb(70, 110, 120));
        draw_rectangle(_dx, _cy, _dx + _bar_w * min(1, _t.pot / max(1, _p.fee.potions)), _cy + 15, false);
        draw_set_color(make_color_rgb(80, 100, 110));
        draw_rectangle(_dx, _cy, _dx + _bar_w, _cy + 15, true);
        _cy += 42;
    }

    // Warnings: overshoot (no refunds) / ask not yet met. Gated on remaining
    // room so a deep name stack can never push them into the seal bar.
    if ((_t.pts > _p.goal || _t.pot > _p.fee.potions) && _cy < _cby0 - 60) {
        var _ov = _aw ? "More than the ask is ticked - the forge takes all of it."
                      : "More than the ask is ticked - the dark keeps the excess.";
        draw_set_color(make_color_rgb(220, 180, 110));
        draw_text_ext(_dx, _cy, _ov, 27, _dr - _dx);
        _cy += string_height_ext(_ov, 27, _dr - _dx) + 9;
    }
    if (!_t.ok && _cy < _cby0 - 40) {
        draw_set_color(make_color_rgb(225, 110, 110));
        draw_text_ext(_dx, _cy, "The ask is not yet met - tick more reagents.", 27, _dr - _dx);
    }

    // --- Footer: SEAL bar / armed confirm / key legend -----------------------
    if (_p.confirm) {
        draw_set_alpha(0.95); draw_set_color(make_color_rgb(120, 40, 40));
        draw_rectangle(_px + 30, _cby0, _px + _pw - 30, _cby1, false);
        draw_set_alpha(1.0);
        draw_set_halign(fa_center);
        draw_set_color(c_white);
        var _gear_ct = 0;
        for (var _ci = 0; _ci < _n; _ci++) if (_p.rows[_ci].sel && _p.rows[_ci].kind == "gear") _gear_ct++;
        draw_text(_px + _pw / 2, _cby0 + 14, _aw
            ? ("Burn " + string(_gear_ct) + " epic" + ((_gear_ct == 1) ? "" : "s") + " + " + string(_p.fee.gold)
                + "g + " + string(_p.fee.dust) + " dust to wake it? This cannot be undone.")
            : ("Feed " + string(_gear_ct) + " gear + " + string(_t.pot)
                + " potion" + ((_t.pot == 1) ? "" : "s") + " + " + string(_p.fee.gold) + "g to the dark? This cannot be undone."));
        draw_set_color(c_ltgray);
        ui_draw_key_legend(_px + _pw / 2, _py + _ph - 36, "Enter: Confirm     Esc: Back");
    } else {
        var _bx0 = _px + 330, _bx1 = _px + _pw - 330;
        draw_set_alpha(_t.ok ? 0.92 : 0.45);
        draw_set_color(_t.ok ? (_aw ? make_color_rgb(110, 76, 30) : make_color_rgb(96, 44, 110)) : make_color_rgb(50, 46, 60));
        draw_rectangle(_bx0, _cby0, _bx1, _cby1, false);
        draw_set_alpha(1.0);
        draw_set_color((_p.cursor == _n) ? c_white : (_aw ? make_color_rgb(200, 175, 130) : make_color_rgb(170, 150, 190)));
        draw_rectangle(_bx0, _cby0, _bx1, _cby1, true);
        draw_set_halign(fa_center);
        draw_set_color(_t.ok ? c_white : c_gray);
        draw_text(_px + _pw / 2, _cby0 + 14, _aw ? "LIGHT THE FORGE" : "SEAL THE EXCHANGE");
        draw_set_color(c_ltgray);
        ui_draw_key_legend(_px + _pw / 2, _py + _ph - 36, "W/S: Move     Enter: Tick / Seal     Esc: Cancel");
    }

    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white); draw_set_alpha(1.0);
    draw_set_font(-1);
}

// =============================================================================
// FORGE RESULT REVEAL (07-31, M: "SHOW THE PLAYER WHAT THEY JUST DID") - one
// shared popup fired after ANY craft/forge/trade resolves. The new item gets a
// full card: splash art (or framed icon), rarity-colored name, stat line,
// unique effect, plus an optional "was" comparison line and extra reward lines.
//
// global.forge_result = undefined | {
//     title:  "CURSED REBIRTH",          // banner (craft name)
//     flavor: "The dark accepts...",     // one-line sub, "" for none
//     item:   struct|undefined,          // NEW item -> card
//     prev:   struct|undefined,          // old item -> compact comparison
//     lines:  array<string>,             // extra rewards (sunder etc.)
//     accent: color                      // banner color
// }
//
// Modal rules: obj_game_controller Step clears Enter/Space/Esc while it's open
// (one press closes) and the craft screens' input blocks stand down, so a
// close press/tap can never fall through to the screen underneath.
// =============================================================================
function ui_draw_forge_result() {
    if (!variable_global_exists("forge_result") || global.forge_result == undefined) return;
    var _fr = global.forge_result;

    // ---- measure content height first (UI collision rule: no fixed boxes) ----
    var _pw   = 900;
    var _cx   = GUI_CX;
    var _inw  = _pw - 120;                       // inner text width
    var _has_item = (_fr.item != undefined);
    var _art  = -1;
    if (_has_item) {
        // Splash art is BASE-NAME art (codex): right for legendaries whose
        // dedicated icon matches it, WRONG for generic gear whose seeded icon
        // variant looks nothing like the base splash (M 08-14 temper shot: the
        // card showed "a different item" which then "reverted" in inventory).
        // Rule now mirrors ui_draw_item_icon's pinned-art rule: only un-cursed
        // unique_effect legendaries show the big splash; everything else (and
        // cursed rebirths, whose look the dark re-rolled) shows its OWN icon.
        // (08-16: clone_item stamps unique_effect = "" on EVERY item, so the bare
        // exists-check let generic gear through - the card showed the codex
        // splash while the inventory showed the seeded icon. Now "" != legendary.)
        if (variable_struct_exists(_fr.item, "unique_effect") && _fr.item.unique_effect != ""
            && !(variable_struct_exists(_fr.item, "cursed") && _fr.item.cursed)) {
            _art = item_splash_sprite(item_base_name(_fr.item));
            if (_art < 0 && variable_struct_exists(_fr.item, "splash_base"))
                _art = item_splash_sprite(_fr.item.splash_base);
        }
    }
    var _art_h = _has_item ? ((_art >= 0 && sprite_exists(_art)) ? 264 : 132) : 0;

    draw_set_font(ui_font(fnt_ui));
    var _stat_txt = _has_item ? ui_item_stat_str(_fr.item) : "";
    var _uniq_txt = (_has_item && variable_struct_exists(_fr.item, "unique_desc")
                     && _fr.item.unique_desc != "") ? _fr.item.unique_desc : "";
    var _uniq_h   = (_uniq_txt != "") ? string_height_ext(_uniq_txt, 33, _inw) + 12 : 0;
    // BEFORE/AFTER table (M 08-08). Measured, never a fixed box: the row count
    // varies by craft (a temper moves 2 rows, a cursed rebirth can move 7).
    // Falls back to the old grey "was:" line if nothing comparable came back.
    draw_set_font(ui_font(fnt_ui_small));
    var _cmp   = (_fr.prev != undefined && _has_item) ? item_compare_rows(_fr.prev, _fr.item) : [];
    var _cmp_n = array_length(_cmp);
    draw_set_font(ui_font(fnt_ui));
    var _prev_h   = (_fr.prev == undefined) ? 0 : ((_cmp_n > 0) ? (60 + _cmp_n * 33) : 66);
    var _lines_h  = array_length(_fr.lines) * 36;

    var _ph = 96                                  // banner + flavor
            + (_fr.flavor != "" ? 15 : 0)         // flavor line clears the art (M 08-04 shot)
            + _art_h + 15
            + (_has_item ? 48 : 0)                // item name
            + (_stat_txt != "" ? 39 : 0)
            + _uniq_h
            + _prev_h
            + _lines_h
            + 108;                                // gap + button + pad
    var _py1 = GUI_CY - _ph / 2;
    var _px1 = _cx - _pw / 2, _px2 = _cx + _pw / 2, _py2 = _py1 + _ph;

    // ---- scrim + panel ----
    draw_set_alpha(0.72);
    draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(14, 13, 22));
    draw_rectangle(_px1, _py1, _px2, _py2, false);
    ui_draw_gothic_frame(_px1, _py1, _px2, _py2, 27);

    // ---- banner + flavor ----
    draw_set_halign(fa_center); draw_set_valign(fa_top);
    draw_set_font(fnt_ui_title);
    draw_set_color(_fr.accent);
    draw_text(_cx, _py1 + 27, _fr.title);
    // Art starts BELOW the flavor line when one is present (fnt_ui_small line at
    // y78 spans to ~y105 - the fixed y96 art start struck through it, M 08-04).
    var _fy = _py1 + ((_fr.flavor != "") ? 111 : 96);
    if (_fr.flavor != "") {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(165, 160, 185));
        ui_draw_stat_line_fit_center(_cx, _py1 + 78, _fr.flavor, _pw - 90);
    }

    // ---- item card ----
    if (_has_item) {
        if (_art >= 0 && sprite_exists(_art)) {
            ui_draw_sprite_contain(_art, 0, _cx - 132, _fy, 264, 264, 1.0);
        } else {
            ui_draw_item_icon(_cx - 66, _fy, 132, _fr.item);
        }
        _fy += _art_h + 15;
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(item_rarity_color(variable_struct_exists(_fr.item, "rarity") ? _fr.item.rarity : 0));
        ui_draw_stat_line_fit_center(_cx, _fy, _fr.item.name, _pw - 90);
        _fy += 48;
        if (_stat_txt != "") {
            draw_set_color(make_color_rgb(215, 220, 232));
            ui_draw_stat_line_fit_center(_cx, _fy, _stat_txt, _pw - 90);
            _fy += 39;
        }
        if (_uniq_txt != "") {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(228, 190, 90));
            draw_text_ext(_cx, _fy, _uniq_txt, 33, _inw);
            _fy += _uniq_h;
        }
    }

    // ---- BEFORE / AFTER side-by-side ----
    if (_fr.prev != undefined && _cmp_n > 0) {
        // Column geometry (all measured off _cx so it stays centred with the panel):
        //   label   left  at _cx-400, truncated 150 -> ends _cx-250
        //   before  right at _cx-38,  truncated 200 -> starts _cx-238 (12px clear)
        //   arrow   centre at _cx
        //   after   left  at _cx+38,  truncated 340 -> ends _cx+378 (inside _px2-60)
        var _cl = _cx - 400, _cav = _cx - 38, _cbv = _cx + 38;
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(118, 124, 148));
        draw_set_halign(fa_right); draw_text(_cav, _fy + 6, "BEFORE");
        draw_set_halign(fa_left);  draw_text(_cbv, _fy + 6, "AFTER");
        draw_set_color(make_color_rgb(52, 56, 76));
        draw_line(_cl, _fy + 40, _px2 - 60, _fy + 40);

        var _cry = _fy + 54;
        for (var _ci = 0; _ci < _cmp_n; _ci++) {
            var _cr = _cmp[_ci];
            draw_set_halign(fa_left);
            draw_set_color(make_color_rgb(138, 146, 170));
            draw_text(_cl, _cry, ui_truncate(_cr.label, 150));
            draw_set_halign(fa_right);
            draw_set_color(make_color_rgb(142, 148, 166));
            draw_text(_cav, _cry, ui_truncate(_cr.a, 200));
            draw_set_halign(fa_center);
            draw_set_color(make_color_rgb(88, 94, 116));
            draw_text(_cx, _cry, "->");
            draw_set_halign(fa_left);
            // Green = this craft improved it, red = it cost you something, plain
            // white = changed sideways or not at all. An all-white table is a
            // truthful answer to "what did my gold buy?" - it bought nothing.
            draw_set_color((_cr.better > 0) ? make_color_rgb(120, 210, 130)
                        : ((_cr.better < 0) ? make_color_rgb(214, 112, 112)
                                            : make_color_rgb(215, 220, 232)));
            draw_text(_cbv, _cry, ui_truncate(_cr.b, 340));
            _cry += 33;
        }
        draw_set_halign(fa_center);
        _fy += _prev_h;
    } else if (_fr.prev != undefined) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(125, 130, 150));
        ui_draw_stat_line_fit_center(_cx, _fy + 6, "was:  " + _fr.prev.name, _pw - 90);
        ui_draw_stat_line_fit_center(_cx, _fy + 36, ui_item_stat_str(_fr.prev), _pw - 90);
        _fy += _prev_h;
    }

    // ---- extra reward lines ----
    if (array_length(_fr.lines) > 0) {
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(205, 210, 225));
        for (var _li = 0; _li < array_length(_fr.lines); _li++) {
            draw_text(_cx, _fy, _fr.lines[_li]);
            _fy += 36;
        }
    }

    // ---- close affordance (device-aware). Actual closing lives in gc Step. ----
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(150, 160, 190));
    if (input_device() == 2) {
        ui_draw_touch_continue(_cx, _py2 - 93, "TAKE IT");
    } else {
        draw_text(_cx, _py2 - 57, (input_device() == 1) ? "A: Take it" : "Enter: Take it");
    }

    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_color(c_white); draw_set_alpha(1.0);
    draw_set_font(-1);
}

// Centered variant of ui_draw_stat_line_fit (that one is left-anchored; the
// reveal card centers everything). Caller sets font/colour first.
function ui_draw_stat_line_fit_center(cx, y, txt, max_w) {
    var _w = string_width(txt);
    var _prev_ha = draw_get_halign();
    draw_set_halign(fa_center);
    if (_w > max_w && _w > 0) {
        var _sc = max_w / _w;
        draw_text_transformed(cx, y, txt, _sc, _sc, 0);
    } else {
        draw_text(cx, y, txt);
    }
    draw_set_halign(_prev_ha);
}

// forge_result_open(title, flavor, item, prev, lines, accent) - one-call fire.
function forge_result_open(_title, _flavor, _item, _prev, _lines, _accent) {
    global.forge_result = {
        title:  _title,
        flavor: _flavor,
        item:   _item,
        prev:   _prev,
        lines:  _lines,
        accent: _accent
    };
    // No audio here - every craft site already plays its own commit sound.
}

// =============================================================================
// STATS-PAGE GUIDED TOUR (07-31, M: "like in most games... next goes through
// the stats page quickly explaining it, skip exits"). Six steps, each dimming
// the page except one highlighted region and pinning an explainer card nearby.
// Runs in the calling gc's scope: reads stats_tour_step; input (Enter/Space/N =
// next, Esc = skip, pad A/B) is handled in obj_game_controller Step with the
// keys cleared. Touch: NEXT / SKIP buttons fire simulated keys from here (hit-
// tests live in Draw per the input rule).
// =============================================================================
// TALENT-WEB GUIDED TOUR (M 08-04): the stats-tour treatment for the talent
// web - dim everything but the step's region, gold pulse border, measured
// explainer card that auto-slides off its own highlight rect. State lives on
// gc (talent_tour_step, advanced in gc Step); drawn at the END of the hub's
// web-view block so it sits over the whole web. Geometry mirrors the web
// panel (460..1460 x 130..985; root 960,305; columns x740/x1180).
function ui_draw_talent_tour() {
    if (!instance_exists(obj_game_controller)) return;
    if (!variable_instance_exists(obj_game_controller.id, "talent_tour_step")) return;
    if (obj_game_controller.talent_tour_step < 0) return;
    var _ts = clamp(obj_game_controller.talent_tour_step, 0, 4);
    var _steps = [
        { r: [870, 235, 1050, 350],  cx: 470, cy: 400,
          t: "Every ability grows",
          b: "This is the ability's TALENT WEB. Casting the ability earns it Talent Points at cast milestones - the spells you actually USE are the ones that deepen. The root is the ability itself; everything around it is what it can become." },
        { r: [466, 143, 1454, 240],  cx: 690, cy: 300,
          t: "Talent Points",
          b: "This line is the ledger: points ready to spend, how many nodes are woven (6 max), your lifetime casts, and how many casts until the next point arrives. No gold involved - only practice." },
        { r: [472, 350, 950, 762],   cx: 970, cy: 380,
          t: "The POWER branch",
          b: "Straight, honest upgrades - bigger numbers, harder hits, tier by tier downward. Each node needs the one above it woven first." },
        { r: [970, 350, 1448, 762],  cx: 480, cy: 380,
          t: "The TWIST branch",
          b: "Twists CHANGE how the ability behaves rather than just scaling it. The mid-tier cross-link lets a build bridge both branches - and the double-ringed tier-3 KEYSTONES are the build-defining picks." },
        { r: [472, 764, 1448, 972],  cx: 690, cy: 300,
          t: "Weave it",
          b: "Select a node and press Enter to STAGE it - nothing is permanent yet. SAVE & CLOSE weaves your staged picks forever; CLOSE discards them. Regret a finished weave? Vael's REWEAVE unpicks a web and returns every point." },
    ];
    var _sd = _steps[_ts];
    var _hr = _sd.r;

    // Dim everything EXCEPT the highlight rect.
    draw_set_alpha(0.68);
    draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, _hr[1], false);
    draw_rectangle(GUI_XL, _hr[3], GUI_XR, GUI_H, false);
    draw_rectangle(GUI_XL, _hr[1], _hr[0], _hr[3], false);
    draw_rectangle(_hr[2], _hr[1], GUI_XR, _hr[3], false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(245, 195, 80));
    draw_set_alpha(0.65 + 0.35 * sin(current_time / 250));
    draw_rectangle(_hr[0], _hr[1], _hr[2], _hr[3], true);
    draw_rectangle(_hr[0] - 1, _hr[1] - 1, _hr[2] + 1, _hr[3] + 1, true);
    draw_set_alpha(1.0);

    // Explainer card (measured; slides off its own rect - stats-tour rule).
    var _cw = 540;
    draw_set_font(ui_font(fnt_ui_small));
    var _bh = string_height_ext(_sd.b, 30, _cw - 60);
    var _ch = 63 + _bh + 24 + 54 + 9 + 24 + 12;
    var _cx1 = _sd.cx, _cy1 = _sd.cy;
    if (_cx1 + _cw > 1860) _cx1 = 1860 - _cw;
    if (_cy1 + _ch > 1040) _cy1 = 1040 - _ch;
    if (_cx1 < _hr[2] && _cx1 + _cw > _hr[0] && _cy1 < _hr[3] && _cy1 + _ch > _hr[1]) {
        if (_hr[1] - 12 - _ch >= 30)        _cy1 = _hr[1] - 12 - _ch;
        else if (_hr[3] + 12 + _ch <= 1040) _cy1 = _hr[3] + 12;
    }
    var _cx2 = _cx1 + _cw, _cy2 = _cy1 + _ch;
    draw_set_alpha(0.97);
    draw_set_color(make_color_rgb(16, 18, 30));
    draw_rectangle(_cx1, _cy1, _cx2, _cy2, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(245, 195, 80));
    draw_rectangle(_cx1, _cy1, _cx2, _cy2, true);
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(245, 210, 140));
    draw_text(_cx1 + 30, _cy1 + 18, _sd.t + "   (" + string(_ts + 1) + "/5)");
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(210, 214, 228));
    draw_text_ext(_cx1 + 30, _cy1 + 63, _sd.b, 30, _cw - 60);

    // Buttons: NEXT (Enter) / SKIP (Esc) - tappable, with kb/pad hints.
    var _bt_y1 = _cy1 + 63 + _bh + 24, _bt_y2 = _bt_y1 + 54;
    var _nx1 = _cx1 + 30,  _nx2 = _nx1 + 216;
    var _sx1 = _nx2 + 24,  _sx2 = _sx1 + 168;
    draw_set_color(make_color_rgb(28, 44, 66));
    draw_rectangle(_nx1, _bt_y1, _nx2, _bt_y2, false);
    draw_set_color(make_color_rgb(80, 160, 220));
    draw_rectangle(_nx1, _bt_y1, _nx2, _bt_y2, true);
    draw_set_color(make_color_rgb(34, 30, 40));
    draw_rectangle(_sx1, _bt_y1, _sx2, _bt_y2, false);
    draw_set_color(make_color_rgb(120, 110, 130));
    draw_rectangle(_sx1, _bt_y1, _sx2, _bt_y2, true);
    draw_set_halign(fa_center); draw_set_valign(fa_middle);
    draw_set_color(c_white);
    draw_text((_nx1 + _nx2) / 2, (_bt_y1 + _bt_y2) / 2, (_ts == 4) ? "FINISH" : "NEXT");
    draw_set_color(make_color_rgb(180, 175, 190));
    draw_text((_sx1 + _sx2) / 2, (_bt_y1 + _bt_y2) / 2, "SKIP");
    draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(120, 130, 155));
    var _hint = "Enter: Next    Esc: Skip";
    if (input_device() == 1) _hint = "A: Next    B: Skip";
    if (input_device() == 2) _hint = "";
    if (_hint != "") draw_text((_cx1 + _cx2) / 2, _bt_y2 + 9, _hint);
    draw_set_halign(fa_left);
    if (touch_tapped(_nx1, _bt_y1, _nx2, _bt_y2)) touch_press(vk_enter);
    if (touch_tapped(_sx1, _bt_y1, _sx2, _bt_y2)) touch_press(vk_escape);

    draw_set_color(c_white); draw_set_alpha(1.0); draw_set_font(-1);
}

function ui_draw_stats_tour() {
    // Tour state lives on gc; this draws from whichever room controller owns the
    // menu (hub/floor/combat), so it must NOT read stats_tour_step bare (1.0.2
    // live crash: floor controller had no such variable).
    if (!instance_exists(obj_game_controller)) return;
    if (!variable_instance_exists(obj_game_controller.id, "stats_tour_step")) return;
    if (obj_game_controller.stats_tour_step < 0) return;
    var _ts = clamp(obj_game_controller.stats_tour_step, 0, 5);
    // Highlight rects {x1,y1,x2,y2} + card anchor per step (1920x1080 GUI).
    // Re-measured 08-04 against the live stats layout (content_y=135; left col
    // x60, mid col x540, right col x1230) so every edge lands in a GAP between
    // text lines - the 07-31 rects sliced the ranged-dmg line, crit notes,
    // armor row and Fortune header mid-line.
    var _steps = [
        { r: [42, 246, 648, 417],    cx: 700,  cy: 258,
          t: "Your six core stats",
          b: "The number shown is Base + Gear combined. Hover any stat to see the split - equip requirements test your BASE stat only, so gear can't carry you into gear." },
        { r: [42, 423, 528, 682],    cx: 600,  cy: 430,
          t: "Damage bonuses",
          b: "Each ability type scales off its own stat: STR drives Phys, INT drives Elemental, WIS drives DoTs and effects, CHA adds a little to everything. Weapon damage adds only to abilities that match the weapon's reach (melee or ranged)." },
        { r: [522, 423, 1196, 676],  cx: 60,   cy: 430,
          t: "Critical chance",
          b: "Four crit rates - every ability rolls the ONE matching its style: Power (STR), Precision (DEX), Arcane (INT) or Effect (WIS). Gear crit adds on top: \"Crit (all)\" boosts every roll; Spell Crit and Phys Crit boost only their half." },
        { r: [42, 699, 1196, 912],   cx: 600,  cy: 260,
          t: "Staying alive",
          b: "Dodge avoids a hit outright. Phys reduction shaves a percentage, then Armor subtracts a flat amount (a landed hit always deals at least 1). Accuracy is your to-hit bonus, applied before the foe's dodge. DEX also buys INITIATIVE - high DEX draws first each fight, slow foes act last." },
        { r: [522, 870, 1196, 982],  cx: 60,   cy: 690,
          t: "Fortune",
          b: "Gold Find boosts every coin you pick up; Loot Find raises the chance enemies drop equipment at all. Gear affixes, charisma, companions, potions and traits all feed these numbers." },
        // Copy rewritten 08-13 (M shot: "bad incoherent ai language") - plain
        // and concrete, like the other five steps.
        { r: [1212, 120, 1818, 1010], cx: 570, cy: 400,
          t: "Your character at a glance",
          b: "The right column shows where you stand right now: your HP, dodge, accuracy and main crit, plus every boon, curse and status currently on you. If your numbers ever look wrong mid-run, check here first." },
    ];
    var _sd = _steps[_ts];
    var _hr = _sd.r;

    // Dim everything EXCEPT the highlight rect (four surrounding panels).
    draw_set_alpha(0.68);
    draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, _hr[1], false);                 // above
    draw_rectangle(GUI_XL, _hr[3], GUI_XR, GUI_H, false);             // below
    draw_rectangle(GUI_XL, _hr[1], _hr[0], _hr[3], false);            // left
    draw_rectangle(_hr[2], _hr[1], GUI_XR, _hr[3], false);            // right
    draw_set_alpha(1.0);
    // Highlight border (pulsing gold).
    draw_set_color(make_color_rgb(245, 195, 80));
    draw_set_alpha(0.65 + 0.35 * sin(current_time / 250));
    draw_rectangle(_hr[0], _hr[1], _hr[2], _hr[3], true);
    draw_rectangle(_hr[0] - 1, _hr[1] - 1, _hr[2] + 1, _hr[3] + 1, true);
    draw_set_alpha(1.0);

    // ---- Explainer card (measured height; never collides by construction). ----
    var _cw   = 560;
    draw_set_font(ui_font(fnt_ui_small));
    var _bh   = string_height_ext(_sd.b, 30, _cw - 60);
    // Top-down: title 63, body _bh, gap 24, buttons 54, gap 9, hint 24, pad 12.
    var _ch   = 63 + _bh + 24 + 54 + 9 + 24 + 12;
    var _cx1  = _sd.cx, _cy1 = _sd.cy;
    if (_cx1 + _cw > 1860) _cx1 = 1860 - _cw;
    if (_cy1 + _ch > 1040) _cy1 = 1040 - _ch;
    // Never cover the very thing being highlighted (M 08-04 screenshots: the
    // crit and Fortune cards sat ON their own gold rects). If the MEASURED card
    // intersects the highlight rect, slide it fully above it (below if there's
    // no room above). Dimmed regions are fair game - only the rect is sacred.
    if (_cx1 < _hr[2] && _cx1 + _cw > _hr[0] && _cy1 < _hr[3] && _cy1 + _ch > _hr[1]) {
        if (_hr[1] - 12 - _ch >= 30)      _cy1 = _hr[1] - 12 - _ch;
        else if (_hr[3] + 12 + _ch <= 1040) _cy1 = _hr[3] + 12;
    }
    var _cx2 = _cx1 + _cw, _cy2 = _cy1 + _ch;
    draw_set_alpha(0.97);
    draw_set_color(make_color_rgb(16, 18, 30));
    draw_rectangle(_cx1, _cy1, _cx2, _cy2, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(245, 195, 80));
    draw_rectangle(_cx1, _cy1, _cx2, _cy2, true);
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(245, 210, 140));
    draw_text(_cx1 + 30, _cy1 + 18, _sd.t + "   (" + string(_ts + 1) + "/6)");
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(210, 214, 228));
    draw_text_ext(_cx1 + 30, _cy1 + 63, _sd.b, 30, _cw - 60);

    // ---- Buttons: NEXT (Enter) / SKIP (Esc) - tappable, with kb/pad hints. ----
    var _bt_y1 = _cy1 + 63 + _bh + 24, _bt_y2 = _bt_y1 + 54;
    var _nx1 = _cx1 + 30,  _nx2 = _nx1 + 216;
    var _sx1 = _nx2 + 24,  _sx2 = _sx1 + 168;
    draw_set_color(make_color_rgb(28, 44, 66));
    draw_rectangle(_nx1, _bt_y1, _nx2, _bt_y2, false);
    draw_set_color(make_color_rgb(80, 160, 220));
    draw_rectangle(_nx1, _bt_y1, _nx2, _bt_y2, true);
    draw_set_color(make_color_rgb(34, 30, 40));
    draw_rectangle(_sx1, _bt_y1, _sx2, _bt_y2, false);
    draw_set_color(make_color_rgb(120, 110, 130));
    draw_rectangle(_sx1, _bt_y1, _sx2, _bt_y2, true);
    draw_set_halign(fa_center); draw_set_valign(fa_middle);
    draw_set_color(c_white);
    draw_text((_nx1 + _nx2) / 2, (_bt_y1 + _bt_y2) / 2, (_ts == 5) ? "FINISH" : "NEXT");
    draw_set_color(make_color_rgb(180, 175, 190));
    draw_text((_sx1 + _sx2) / 2, (_bt_y1 + _bt_y2) / 2, "SKIP");
    draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(120, 130, 155));
    var _hint = "Enter: Next    Esc: Skip";
    if (input_device() == 1) _hint = "A: Next    B: Skip";
    if (input_device() == 2) _hint = "";
    if (_hint != "") draw_text((_cx1 + _cx2) / 2, _bt_y2 + 9, _hint);
    draw_set_halign(fa_left);
    if (touch_tapped(_nx1, _bt_y1, _nx2, _bt_y2)) touch_press(vk_enter);
    if (touch_tapped(_sx1, _bt_y1, _sx2, _bt_y2)) touch_press(vk_escape);

    draw_set_color(c_white); draw_set_alpha(1.0); draw_set_font(-1);
}

// =============================================================================
// NPC STATION GUIDED TOURS (M-locked 08-15: framework + all SEVEN keepers in
// one pass). The stats-tour spotlight applied to every camp station: the first
// time each NPC's screen opens (once ever - tutorial_seen "tour_<npc>"), the
// screen dims except one highlighted region per step, with a measured explainer
// card pinned nearby. State lives on gc (npc_tour_step / npc_tour_npc, advanced
// in gc Step - every NPC input block stands down while a tour runs). Drawn as
// the LAST call in obj_hub_controller Draw_64, above the screens and tab tips.
// Rects are measured against each screen's real drawn geometry (tab bars, row
// regions, panels) so every edge lands on a drawn box edge, not mid-text.
// =============================================================================
function npc_tour_steps(_npc) {
    switch (_npc) {
        case "dorn": return [
            // Tab bar: 4 tabs, y96..138, x284 + t*344, 320 wide.
            { r: [278, 90, 1622, 144],  cx: 680, cy: 320,
              t: "Dorn's four counters",
              b: "Everything at the forge lives behind these tabs: BUY from his stock, SELL what you've outgrown, REFORGE to reroll and craft, TEMPER to finish rough steel. Q/E or a click switches counters - and resting the mouse on a tab reminds you what lives there." },
            // Buy rows region (x150..1500, y189+, 117px rows; 3 rows high so the
            // card always fits beneath it).
            { r: [144, 183, 1506, 562], cx: 960, cy: 600,
              t: "The stock rotates",
              b: "Dorn restocks between runs, and his shelf improves as your Awakening climbs. Names are tinted by rarity - and a price you can't pay yet is a reason to descend, not to despair." },
            { r: [946, 90, 1278, 144],  cx: 500, cy: 320,
              t: "Reforge, smelt, craft",
              b: "Three crafts share this counter. REWORK rerolls an unequipped item's affixes with a Reforge Ingot. SMELT destroys a piece for an ingot - Dorn STUDIES one of its affixes into his PATTERN BOOK and keeps its ICON for the art page. CRAFT then builds an item to YOUR design from everything he has studied - wearing any smelted icon that fits its slot." },
            { r: [1290, 90, 1622, 144], cx: 500, cy: 320,
              t: "Finish rough steel",
              b: "Loot drops UNFINISHED - the QUALITY tag says how much of its true power it delivers. Each temper step buys +10% quality for gold and rune dust, plus a little bonus max HP. A raw legendary barely beats a finished epic - temper what you love." },
            // Bond readout, title row far left.
            { r: [78, 30, 470, 86],     cx: 550, cy: 320,
              t: "Trade builds trust",
              b: "Every purchase and service warms Dorn to you, and friendship gates unlock his perks. His STATION can grow too: back on the camp carousel, [U] or the STATION chip buys station ranks - a deeper shelf first, a hotter temper after." },
        ];
        case "petra": return [
            // Tab bar: 3 tabs, y96..138, x456 + t*344, 320 wide.
            { r: [450, 90, 1470, 144],  cx: 640, cy: 320,
              t: "Petra's three counters",
              b: "BUY supplies for the road, SELL spare loot for gold, and TRADE - her treasure ladder, where lesser goods become better ones. Q/E or a click switches counters." },
            { r: [144, 183, 1506, 562], cx: 960, cy: 600,
              t: "Stock the road ahead",
              b: "Potions, creature feed and supplies for your next descent, restocked between runs. Watch her SPECIAL shelf - now and then it carries something the road rarely offers." },
            { r: [1138, 90, 1470, 144], cx: 500, cy: 320,
              t: "The treasure ladder",
              b: "Hand Petra 3 items of the SAME tier and she trades them up to the next one - delivered after you clear floors, not on the spot. [R] flips to RUNE mode: 5 same-tier runes buy a BLUEPRINT, a rune YOU choose. What you trade in is destroyed, so feed the ladder what you don't love." },
            { r: [78, 30, 470, 86],     cx: 550, cy: 320,
              t: "A merchant remembers",
              b: "Purchases and trades deepen Petra's bond, and gates cross with a favor. Her STATION ranks ([U] or the STATION chip on the camp carousel) keep her special shelf stocked every visit - and make you a FAVORED CLIENT: faster deliveries, finer goods." },
        ];
        case "vex": return [
            // Tab bar: 5 tabs, y96..144, x60 + t*360, 345 wide.
            { r: [54, 90, 1851, 150],   cx: 680, cy: 330,
              t: "The trainer's ledger",
              b: "Vex sells permanence: STATS raise a core stat forever, TRAIT SLOTS widen what you can carry, ABILITIES and TRAITS teach new tools, POTENCY sharpens traits you own. Everything bought here survives every death." },
            { r: [54, 90, 411, 150],    cx: 620, cy: 330,
              t: "Permanent stats",
              b: "Each +1 is yours on every future run. The price climbs as you buy - gold plus a trade item whose rarity escalates. Your BASE stats also gate what gear you can equip, so these purchases open doors gear alone can't." },
            { r: [414, 90, 771, 150],   cx: 900, cy: 330,
              t: "Room to carry more",
              b: "You bring a limited set of traits into the dungeon. Each expansion here adds one more slot, permanently - up to four in all. Expensive, and worth every coin to a build that wants one more piece." },
            { r: [774, 90, 1491, 150],  cx: 300, cy: 330,
              t: "New tools",
              b: "Abilities and traits learned here join your collection forever - then you choose which to bring on the LOADOUT screen before each run. Learning is permanent; only the loadout is per-run." },
            { r: [1494, 90, 1851, 150], cx: 700, cy: 330,
              t: "Trait potency",
              b: "Sharpen a trait you already own. Potency tiers cost gold, rune dust - and 5 points carved from any stat you choose. A good trait at high potency is a build-defining one." },
        ];
        case "maren": return [
            // Tab bar: 5 tabs, y105..165, x368 + t*240, 225 wide.
            { r: [362, 99, 1559, 171],  cx: 680, cy: 340,
              t: "The runeworks",
              b: "Five benches. SOCKET GEAR seats runes in your equipment; ASPECTS seats them in YOU; RUNESMITHING is Maren's craft menu; RUNES is your pouch; SPIRITS frees the bottled songs you find below." },
            { r: [362, 99, 599, 171],   cx: 680, cy: 340,
              t: "Sockets are free to try",
              b: "Uncommon and better gear carries sockets. Seat a GEAR rune for flat stats, and pull it back out whenever you like - nothing is ever lost by experimenting. Rarer gear carries more sockets." },
            { r: [602, 99, 839, 171],   cx: 900, cy: 340,
              t: "Aspect runes",
              b: "ASPECT runes socket into your character, not your gear: per-school damage like Ember and Rime, Surge, Serration and the flagships. They follow you into every run no matter what you wear." },
            { r: [842, 99, 1079, 171],  cx: 300, cy: 340,
              t: "Runesmithing",
              b: "Maren's craft bench: COMBINE three identical runes into one a tier higher, SPLIT one back down, AWAKEN a dormant legendary, and seal her RUNEHEART CORE share of the Legendary Forge." },
            // SPIRITS tab (5th, x1328..1553) - the tour SWITCHES to it (tab: 4) so the
            // bottle list is on screen while it explains (M 08-18: "should take you to
            // banshee spirits tab as well and inform players of unlocking songs").
            { r: [1322, 99, 1559, 171], cx: 700, cy: 340, tab: 4,
              t: "Banshee spirits - unlock songs",
              b: "Deep in the dungeons you may find a BANSHEE IN A BOTTLE. Bring it here: Maren frees the spirit in a short ceremony, and its parting SONG joins your music - new tracks for the camp, the garden and the fight. Every bottle you free unlocks another; the Settings music rows let you pick which one plays." },
            // Dust + gold readout, top right.
            { r: [1500, 38, 1890, 116], cx: 900, cy: 340, tab: 0,
              t: "Dust is the coin here",
              b: "Nearly everything Maren does runs on RUNE DUST - Sable melts unwanted gear into it, so when dust runs short, visit the apothecary first. Maren's STATION rank ([U] or the STATION chip on the camp carousel) cuts the dust her combines ask." },
        ];
        case "sable": return [
            // Tab bar: 4 tabs, y105..165, x368 + t*300, 285 wide.
            { r: [362, 99, 1559, 171],  cx: 680, cy: 340,
              t: "The apothecary",
              b: "Four stations. SALVAGE melts gear into rune dust; BREW turns her book into potions; UPGRADE refines runes and recipes; REBIRTH is her dark exchange. Q/E or a click moves between them." },
            { r: [362, 99, 659, 171],   cx: 700, cy: 340,
              t: "Everything melts down",
              b: "Unequipped gear breaks down into RUNE DUST - the fuel for Maren's runework and Sable's own brewing. Rarer pieces pay more. Salvage is the graceful end for gear you've outgrown." },
            { r: [662, 99, 959, 171],   cx: 900, cy: 340,
              t: "Potions and worse",
              b: "Sable brews what her book knows - and her CHAOTIC BREW fuses potions you already know into one unstable draught: every part applies when you drink it, plus a downside. Three of anything is a recipe." },
            { r: [962, 99, 1259, 171],  cx: 340, cy: 340,
              t: "Refinements",
              b: "TRANSMUTE three runes into a different rune of the same tier - shaping, not gambling - and improve what Sable already knows how to make." },
            { r: [1262, 99, 1559, 171], cx: 340, cy: 340,
              t: "The dark exchange",
              b: "CURSED REBIRTH feeds a legendary to the dark: it returns stronger, but branded with a real curse. Her QUINTESSENCE share of the Legendary Forge distills here too, from three different specialty brews. This bench does not refund - read twice before you commit." },
        ];
        case "vael": return [
            // Tab bar: 4 tabs, y96..144, centred on 960, 216 wide, 240 pitch.
            { r: [486, 90, 1434, 150],  cx: 640, cy: 330,
              t: "The atelier",
              b: "Vael deals in appearances - and one second chance. SKINS re-dress your combat look, PORTRAIT repaints your face, TINTS re-color what you wear... and REWEAVE, the odd one out, unpicks a woven talent web." },
            // Skins list column.
            { r: [294, 219, 1166, 1000], cx: 1220, cy: 260,
              t: "Skins",
              b: "Your combat look, cataloged. Owned skins switch freely; locked rows name the milestone that earns them. Pick a row to preview it on the right - purely cosmetic, your numbers never change." },
            { r: [726, 90, 954, 150],   cx: 400, cy: 330,
              t: "A new face",
              b: "For 100 gold Vael repaints your portrait - the face your menus and dialogues show. It changes nothing but your looks, and you can come back and change it again whenever you like." },
            { r: [1206, 90, 1434, 150], cx: 640, cy: 330,
              t: "The reweave",
              b: "Regret a finished talent web? REWEAVE unpicks every woven node and returns every talent point to spend again. The casts that earned those points are never lost - only their arrangement." },
        ];
        case "bairc": return [
            // Stable roster panel (left column, above the garden band).
            { r: [143, 240, 747, 708],  cx: 800, cy: 300,
              t: "The stable",
              b: "Every creature and egg in your care lives here. The colored edge is its calling - gold BOON blesses your runs, red COMBATANT fights beside you, green GUARDIAN shields you - and the bright green frame marks your ACTIVE companion." },
            // Detail + actions panel (right).
            { r: [750, 210, 1642, 966], cx: 160, cy: 300,
              t: "Tend what you keep",
              b: "Select a creature to see its whole life: hunger, health, bond, growth and gifts. FEED it from your pouch - Petra sells feed - and set it ACTIVE to bring it below. Growth fills at camp; a RUN is what evolves a creature that's ready." },
            // Garden band at the stable panel's foot.
            { r: [143, 710, 747, 906],  cx: 800, cy: 560,
              t: "His garden",
              b: "Creatures you ENTRUST to Bairc are not gone - they live on in his plot, and every resident quickens growth for the whole stable. The stones at the garden's edge remember the ones the dungeon took." },
            // Stable header / capacity counter.
            { r: [143, 240, 747, 302],  cx: 800, cy: 380,
              t: "Mind the crowding",
              b: "The stable holds only so many. Past capacity, every creature's growth slows - entrust spares to the garden instead of hoarding. Bairc's STATION rank ([U] or the STATION chip on the camp carousel) also slows hunger for the whole roster." },
        ];
    }
    return [];
}

function ui_draw_npc_tour() {
    // Tour state lives on gc (stats-tour rule: never read the step bare - this
    // draws from the hub controller, which has no such variable).
    if (!instance_exists(obj_game_controller)) return;
    if (!variable_instance_exists(obj_game_controller.id, "npc_tour_step")) return;
    if (obj_game_controller.npc_tour_step < 0) return;
    var _steps = npc_tour_steps(obj_game_controller.npc_tour_npc);
    var _tot   = array_length(_steps);
    if (_tot == 0) return;
    var _ts = clamp(obj_game_controller.npc_tour_step, 0, _tot - 1);
    var _sd = _steps[_ts];
    var _hr = _sd.r;

    // Dim everything EXCEPT the highlight rect (four surrounding panels).
    draw_set_alpha(0.68);
    draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, _hr[1], false);                 // above
    draw_rectangle(GUI_XL, _hr[3], GUI_XR, GUI_H, false);             // below
    draw_rectangle(GUI_XL, _hr[1], _hr[0], _hr[3], false);            // left
    draw_rectangle(_hr[2], _hr[1], GUI_XR, _hr[3], false);            // right
    draw_set_alpha(1.0);
    // Highlight border (pulsing gold).
    draw_set_color(make_color_rgb(245, 195, 80));
    draw_set_alpha(0.65 + 0.35 * sin(current_time / 250));
    draw_rectangle(_hr[0], _hr[1], _hr[2], _hr[3], true);
    draw_rectangle(_hr[0] - 1, _hr[1] - 1, _hr[2] + 1, _hr[3] + 1, true);
    draw_set_alpha(1.0);

    // Explainer card (measured; slides off its own rect - stats-tour rule).
    var _cw = 560;
    draw_set_font(ui_font(fnt_ui_small));
    var _bh = string_height_ext(_sd.b, 30, _cw - 60);
    var _ch = 63 + _bh + 24 + 54 + 9 + 24 + 12;
    var _cx1 = _sd.cx, _cy1 = _sd.cy;
    if (_cx1 + _cw > 1860) _cx1 = 1860 - _cw;
    if (_cy1 + _ch > 1040) _cy1 = 1040 - _ch;
    if (_cx1 < _hr[2] && _cx1 + _cw > _hr[0] && _cy1 < _hr[3] && _cy1 + _ch > _hr[1]) {
        if (_hr[1] - 12 - _ch >= 30)        _cy1 = _hr[1] - 12 - _ch;
        else if (_hr[3] + 12 + _ch <= 1040) _cy1 = _hr[3] + 12;
    }
    var _cx2 = _cx1 + _cw, _cy2 = _cy1 + _ch;
    draw_set_alpha(0.97);
    draw_set_color(make_color_rgb(16, 18, 30));
    draw_rectangle(_cx1, _cy1, _cx2, _cy2, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(245, 195, 80));
    draw_rectangle(_cx1, _cy1, _cx2, _cy2, true);
    draw_set_halign(fa_left); draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(245, 210, 140));
    draw_text(_cx1 + 30, _cy1 + 18, _sd.t + "   (" + string(_ts + 1) + "/" + string(_tot) + ")");
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(210, 214, 228));
    draw_text_ext(_cx1 + 30, _cy1 + 63, _sd.b, 30, _cw - 60);

    // Buttons: NEXT (Enter) / SKIP (Esc) - tappable, with kb/pad hints.
    var _bt_y1 = _cy1 + 63 + _bh + 24, _bt_y2 = _bt_y1 + 54;
    var _nx1 = _cx1 + 30,  _nx2 = _nx1 + 216;
    var _sx1 = _nx2 + 24,  _sx2 = _sx1 + 168;
    draw_set_color(make_color_rgb(28, 44, 66));
    draw_rectangle(_nx1, _bt_y1, _nx2, _bt_y2, false);
    draw_set_color(make_color_rgb(80, 160, 220));
    draw_rectangle(_nx1, _bt_y1, _nx2, _bt_y2, true);
    draw_set_color(make_color_rgb(34, 30, 40));
    draw_rectangle(_sx1, _bt_y1, _sx2, _bt_y2, false);
    draw_set_color(make_color_rgb(120, 110, 130));
    draw_rectangle(_sx1, _bt_y1, _sx2, _bt_y2, true);
    draw_set_halign(fa_center); draw_set_valign(fa_middle);
    draw_set_color(c_white);
    draw_text((_nx1 + _nx2) / 2, (_bt_y1 + _bt_y2) / 2, (_ts == _tot - 1) ? "FINISH" : "NEXT");
    draw_set_color(make_color_rgb(180, 175, 190));
    draw_text((_sx1 + _sx2) / 2, (_bt_y1 + _bt_y2) / 2, "SKIP");
    draw_set_valign(fa_top);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(120, 130, 155));
    var _hint = "Enter: Next    Esc: Skip";
    if (input_device() == 1) _hint = "A: Next    B: Skip";
    if (input_device() == 2) _hint = "";
    if (_hint != "") draw_text((_cx1 + _cx2) / 2, _bt_y2 + 9, _hint);
    draw_set_halign(fa_left);
    if (touch_tapped(_nx1, _bt_y1, _nx2, _bt_y2)) touch_press(vk_enter);
    if (touch_tapped(_sx1, _bt_y1, _sx2, _bt_y2)) touch_press(vk_escape);

    draw_set_color(c_white); draw_set_alpha(1.0); draw_set_font(-1);
}

// =============================================================================
// BAIRC'S GARDEN SCENE (M design-locked 08-15 late): the full-screen grounds.
// 4800px night garden under a wandering camera; everything here is code-drawn
// set dressing (band-garden idiom, scaled up) except the pets themselves.
// Verb chips double as tap targets injecting the tags gc Step consumes; the
// keyboard hotkeys act on the nearest visible instance (input-parity rule).
// =============================================================================
function ui_draw_garden_scene() {
    if (!instance_exists(obj_game_controller)) return;
    var _gc = instance_find(obj_game_controller, 0);
    if (!variable_instance_exists(_gc, "garden_open") || !_gc.garden_open) return;
    garden_ensure();
    var _cam = _gc.garden_cam_x;
    var _W   = garden_world_w();
    var _t   = current_time / 1000;
    var _mx  = device_mouse_x_to_gui(0), _my = device_mouse_y_to_gui(0);

    // ---- SKY: layered night gradient + moon + hashed stars ----
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(7, 9, 18));    draw_rectangle(GUI_XL, 0,   GUI_XR, 300,  false);
    draw_set_color(make_color_rgb(10, 13, 24));  draw_rectangle(GUI_XL, 300, GUI_XR, 480,  false);
    draw_set_color(make_color_rgb(14, 18, 30));  draw_rectangle(GUI_XL, 480, GUI_XR, 640,  false);
    for (var _si = 0; _si < 60; _si++) {
        var _sh  = frac(sin(_si * 71.3) * 34781.7);
        var _sh2 = frac(sin(_si * 13.9) * 91733.1);
        var _sx  = frac(_sh - _cam * 0.18 / _W) * 1920;
        var _sy  = _sh2 * 560;
        draw_set_alpha(0.25 + 0.45 * abs(sin(_t * (0.6 + _sh) + _si)));
        draw_set_color(make_color_rgb(200, 210, 235));
        draw_rectangle(_sx, _sy, _sx + 2, _sy + 2, false);
    }
    draw_set_alpha(1.0);
    var _moon_x = 760 - _cam * 0.30;
    draw_set_color(make_color_rgb(222, 224, 210));
    draw_circle(_moon_x, 190, 58, false);
    draw_set_color(make_color_rgb(14, 16, 28));
    draw_circle(_moon_x - 22, 176, 50, false);   // waning crescent bite

    // ---- Distant treeline (parallax 0.55) ----
    draw_set_color(make_color_rgb(11, 16, 20));
    for (var _tr = 0; _tr < 40; _tr++) {
        var _th  = frac(sin(_tr * 37.7) * 15731.3);
        var _tx  = _tr * 130 - frac(_cam * 0.55 / 5200) * 5200;
        if (_tx < -160 || _tx > 2080) continue;
        var _tth = 90 + _th * 130;
        draw_triangle(_tx - 70, 660, _tx + 70, 660, _tx, 660 - _tth, false);
    }

    // ---- Ground: earth bands + lanes + hashed grass tufts ----
    draw_set_color(make_color_rgb(18, 24, 20));  draw_rectangle(GUI_XL, 640, GUI_XR, 820,  false);   // back lane
    draw_set_color(make_color_rgb(22, 29, 23));  draw_rectangle(GUI_XL, 820, GUI_XR, 1000, false);   // front lane
    draw_set_color(make_color_rgb(16, 20, 17));  draw_rectangle(GUI_XL, 1000, GUI_XR, GUI_H, false); // foreground earth
    draw_set_color(make_color_rgb(30, 38, 30));
    draw_line(GUI_XL, 820, GUI_XR, 820);
    for (var _gt = 0; _gt < 200; _gt++) {
        var _gh  = frac(sin(_gt * 57.1) * 24631.9);
        var _gwx = _gh * _W;
        var _gsx = _gwx - _cam;
        if (_gsx < -20 || _gsx > 1940) continue;
        var _gy  = 660 + frac(sin(_gt * 11.7) * 7351.3) * 380;
        draw_set_color(make_color_rgb(28, 40, 28));
        draw_line(_gsx, _gy, _gsx - 3, _gy - 9);
        draw_line(_gsx, _gy, _gsx + 3, _gy - 10);
    }

    // ---- Entrance gate (world x 140) ----
    var _eg = 140 - _cam;
    if (_eg > -220 && _eg < 2140) {
        draw_set_color(make_color_rgb(38, 32, 30));
        draw_rectangle(_eg - 90, 560, _eg - 66, 900, false);
        draw_rectangle(_eg + 66, 560, _eg + 90, 900, false);
        draw_rectangle(_eg - 110, 530, _eg + 110, 566, false);
        draw_set_color(make_color_rgb(240, 190, 110));
        draw_set_alpha(0.5 + 0.2 * sin(_t * 2.1));
        draw_circle(_eg - 78, 600, 10, false);
        draw_circle(_eg + 78, 600, 10, false);
        draw_set_alpha(1.0);
    }

    // ---- Memorial grove (world x 4150+): every stone remembered ----
    var _mems = bairc_memorials();
    var _mgn  = min(array_length(_mems), 8);
    for (var _mg = 0; _mg < _mgn; _mg++) {
        var _mgx = 4180 + (_mg mod 4) * 110 + ((_mg div 4) * 55) - _cam;
        var _mgy = 780 + (_mg div 4) * 120;
        if (_mgx < -40 || _mgx > 1960) continue;
        draw_set_color(make_color_rgb(66, 70, 82));
        draw_roundrect_ext(_mgx - 16, _mgy - 44, _mgx + 16, _mgy, 10, 10, false);
        draw_set_color(make_color_rgb(30, 33, 42));
        draw_roundrect_ext(_mgx - 16, _mgy - 44, _mgx + 16, _mgy, 10, 10, true);
        draw_line(_mgx - 8, _mgy - 28, _mgx + 8, _mgy - 28);
    }
    if (array_length(_mems) > 0 && 4390 - _cam > -200 && 4390 - _cam < 2100) {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(110, 118, 132));
        draw_text(4390 - _cam, 660, "the quiet corner");
        draw_set_halign(fa_left);
    }

    // ---- Ornaments in their plots (+ glowing rings in placement mode) ----
    var _anch = garden_decor_anchors();
    var _has_jar = false, _has_wheel = false;
    for (var _ai = 0; _ai < array_length(_anch); _ai++) {
        var _aid = garden_decor_at(_ai);
        var _ax  = _anch[_ai].x - _cam;
        var _ay  = _anch[_ai].y;
        if (_aid == "jar")   _has_jar = true;
        if (_aid == "wheel") _has_wheel = true;
        if (_ax < -160 || _ax > 2080) continue;
        if (_aid == "") {
            // Empty plot: visible ONLY while choosing a spot - glowing ring + number.
            if (_gc.garden_place_pick != "") {
                draw_set_alpha(0.55 + 0.30 * sin(_t * 3 + _ai));
                draw_set_color(make_color_rgb(150, 220, 160));
                draw_ellipse(_ax - 52, _ay - 18, _ax + 52, _ay + 14, true);
                draw_ellipse(_ax - 46, _ay - 15, _ax + 46, _ay + 11, true);
                draw_set_alpha(1.0);
                draw_set_halign(fa_center);
                draw_set_font(ui_font(fnt_ui));
                draw_text(_ax, _ay - 62, "[" + string(_ai + 1) + "]");
                draw_set_halign(fa_left);
                if (touch_tapped(_ax - 60, _ay - 70, _ax + 60, _ay + 20)) input_inject("garden:plot" + string(_ai));
            }
            continue;
        }
        // Each ornament: a small code-drawn vignette.
        switch (_aid) {
            case "lantern":
                draw_set_color(make_color_rgb(70, 72, 80));
                draw_rectangle(_ax - 10, _ay - 76, _ax + 10, _ay, false);
                draw_rectangle(_ax - 22, _ay - 96, _ax + 22, _ay - 72, false);
                draw_set_color(make_color_rgb(26, 27, 34));
                draw_rectangle(_ax - 22, _ay - 96, _ax + 22, _ay - 72, true);
                draw_set_color(make_color_rgb(250, 200, 120));
                draw_set_alpha(0.55 + 0.25 * sin(_t * 2.4 + _ai));
                draw_rectangle(_ax - 12, _ay - 92, _ax + 12, _ay - 76, false);
                draw_set_alpha(0.10 + 0.05 * sin(_t * 2.4 + _ai));
                draw_circle(_ax, _ay - 84, 90, false);
                draw_set_alpha(1.0);
                break;
            case "gate":
                draw_set_color(make_color_rgb(52, 38, 40));
                draw_rectangle(_ax - 64, _ay - 130, _ax - 48, _ay, false);
                draw_rectangle(_ax + 48, _ay - 130, _ax + 64, _ay, false);
                draw_rectangle(_ax - 80, _ay - 150, _ax + 80, _ay - 126, false);
                draw_rectangle(_ax - 70, _ay - 120, _ax + 70, _ay - 106, false);
                draw_set_alpha(0.20 + 0.10 * sin(_t * 1.4));
                draw_set_color(make_color_rgb(170, 120, 220));
                draw_rectangle(_ax - 46, _ay - 104, _ax + 46, _ay - 4, false);
                draw_set_alpha(1.0);
                break;
            case "basin":
                draw_set_color(make_color_rgb(72, 74, 84));
                draw_rectangle(_ax - 8, _ay - 40, _ax + 8, _ay, false);
                draw_ellipse(_ax - 42, _ay - 58, _ax + 42, _ay - 34, false);
                draw_set_color(make_color_rgb(30, 44, 60));
                draw_ellipse(_ax - 34, _ay - 54, _ax + 34, _ay - 38, false);
                draw_set_color(make_color_rgb(222, 224, 210));
                draw_set_alpha(0.7);
                draw_circle(_ax + 8 * sin(_t * 0.7), _ay - 46, 6, false);
                draw_set_alpha(1.0);
                break;
            case "bloom":
                for (var _bf = 0; _bf < 7; _bf++) {
                    var _bfx = _ax - 42 + _bf * 14;
                    var _bfy = _ay - 6 - frac(sin(_bf * 91.7 + _ai) * 351.3) * 16;
                    draw_set_color(make_color_rgb(30, 44, 32));
                    draw_line(_bfx, _ay, _bfx, _bfy);
                    draw_set_alpha(0.65 + 0.30 * sin(_t * 1.8 + _bf));
                    draw_set_color((_bf mod 2 == 0) ? make_color_rgb(150, 110, 220) : make_color_rgb(110, 180, 235));
                    draw_circle(_bfx, _bfy, 5, false);
                    draw_set_alpha(1.0);
                }
                break;
            case "ward":
                draw_set_color(make_color_rgb(60, 70, 58));
                draw_roundrect_ext(_ax - 20, _ay - 66, _ax + 20, _ay, 12, 12, false);
                draw_circle(_ax, _ay - 74, 16, false);
                draw_set_color(make_color_rgb(40, 52, 40));
                draw_roundrect_ext(_ax - 20, _ay - 66, _ax + 20, _ay, 12, 12, true);
                // Eyes blink every few seconds.
                if (frac(_t * 0.21 + _ai * 0.37) > 0.06) {
                    draw_set_color(make_color_rgb(160, 230, 170));
                    draw_circle(_ax - 6, _ay - 76, 2, false);
                    draw_circle(_ax + 6, _ay - 76, 2, false);
                }
                break;
            case "chimes":
                draw_set_color(make_color_rgb(48, 40, 36));
                draw_rectangle(_ax - 3, _ay - 120, _ax + 3, _ay, false);
                draw_rectangle(_ax - 40, _ay - 124, _ax + 40, _ay - 116, false);
                for (var _ch = 0; _ch < 4; _ch++) {
                    var _chx = _ax - 28 + _ch * 19;
                    var _chs = 5 * sin(_t * 1.1 + _ch * 1.7);
                    draw_set_color(make_color_rgb(210, 205, 190));
                    draw_line(_chx, _ay - 116, _chx + _chs, _ay - 76 + _ch * 4);
                    draw_rectangle(_chx + _chs - 2, _ay - 76 + _ch * 4, _chx + _chs + 2, _ay - 56 + _ch * 4, false);
                }
                break;
            case "wheel":
                draw_set_color(make_color_rgb(52, 44, 38));
                draw_circle(_ax, _ay - 40, 34, true);
                for (var _ws = 0; _ws < 4; _ws++) {
                    var _wa = _t * 0.8 + _ws * pi / 2;
                    draw_line(_ax - cos(_wa) * 32, _ay - 40 - sin(_wa) * 32,
                              _ax + cos(_wa) * 32, _ay - 40 + sin(_wa) * 32);
                }
                draw_set_color(make_color_rgb(70, 60, 50));
                draw_rectangle(_ax - 6, _ay - 40, _ax + 6, _ay, false);
                break;
            case "jar":
                draw_set_color(make_color_rgb(120, 140, 150));
                draw_roundrect_ext(_ax - 14, _ay - 40, _ax + 14, _ay, 8, 8, true);
                draw_set_color(make_color_rgb(90, 96, 104));
                draw_rectangle(_ax - 10, _ay - 46, _ax + 10, _ay - 40, false);
                for (var _fj = 0; _fj < 3; _fj++) {
                    draw_set_alpha(0.5 + 0.5 * sin(_t * 3 + _fj * 2.1));
                    draw_set_color(make_color_rgb(220, 235, 140));
                    draw_circle(_ax - 8 + _fj * 8, _ay - 12 - 8 * frac(sin(_fj * 7.3) * 133.7) - 4 * sin(_t + _fj), 2, false);
                    draw_set_alpha(1.0);
                }
                break;
        }
    }

    // ---- THE POND (world x 1250, front lane) ----
    var _pd = 1250 - _cam;
    if (_pd > -420 && _pd < 2340) {
        draw_set_color(make_color_rgb(20, 32, 44));
        draw_ellipse(_pd - 340, 830, _pd + 340, 972, false);
        draw_set_color(make_color_rgb(34, 56, 74));
        draw_ellipse(_pd - 310, 842, _pd + 310, 960, false);
        draw_set_color(make_color_rgb(80, 120, 146));
        draw_ellipse(_pd - 340, 830, _pd + 340, 972, true);
        // Moon glint drifting on the surface.
        draw_set_alpha(0.45);
        draw_set_color(make_color_rgb(150, 190, 215));
        var _gl = _pd - 60 + 40 * sin(_t * 0.5);
        draw_line(_gl - 26, 890, _gl + 26, 890);
        draw_line(_gl - 12, 898, _gl + 30, 898);
        draw_set_alpha(1.0);
        // Koi shadows: three slow orbiters; a fresh crumb pulls them in.
        var _crumb_live = (current_time - _gc.garden_crumb_t < 4500);
        for (var _ko = 0; _ko < 3 + (_has_wheel ? 2 : 0); _ko++) {
            var _ka  = _t * (0.35 + 0.1 * _ko) + _ko * 2.2;
            var _kx  = _pd + cos(_ka) * (170 - _ko * 30);
            var _ky  = 901 + sin(_ka) * (38 - _ko * 6);
            if (_crumb_live) {
                var _kt = clamp((current_time - _gc.garden_crumb_t) / 900, 0, 1);
                _kx = lerp(_kx, _gc.garden_crumb_x - _cam + cos(_ka * 3) * 26, _kt);
                _ky = lerp(_ky, 900 + sin(_ka * 3) * 12, _kt);
            }
            draw_set_alpha(0.55);
            draw_set_color(make_color_rgb(16, 24, 34));
            draw_ellipse(_kx - 20, _ky - 6, _kx + 20, _ky + 6, false);
            draw_set_alpha(0.8);
            draw_set_color((_ko mod 2 == 0) ? make_color_rgb(200, 120, 90) : make_color_rgb(200, 190, 175));
            draw_ellipse(_kx - 14, _ky - 4, _kx + 6, _ky + 4, false);
            draw_set_alpha(1.0);
        }
        // Tap the water itself = toss a crumb.
        if (touch_tapped(_pd - 340, 830, _pd + 340, 972)) input_inject("garden:crumb");
    }

    // ---- THE CAIRN (world x 2650) ----
    var _cn = 2650 - _cam;
    var _stacked = variable_global_exists("garden_cairn") ? global.garden_cairn : 0;
    if (_cn > -200 && _cn < 2120) {
        // Flat base stone.
        draw_set_color(make_color_rgb(58, 62, 70));
        draw_ellipse(_cn - 56, 866, _cn + 56, 890, false);
        // The stack: stones shrink as they rise; the newest wobbles as it lands.
        var _sy0 = 866;
        for (var _st = 0; _st < _stacked; _st++) {
            var _sw2 = 44 - _st * 7;
            var _sh3 = 22 - _st * 2;
            var _wob = 0;
            for (var _gfw = 0; _gfw < array_length(_gc.garden_fx); _gfw++) {
                var _gfs2 = _gc.garden_fx[_gfw];
                if (_gfs2.kind == "stone" && _st == _stacked - 1 && current_time - _gfs2.t0 < 700) {
                    _wob = 4 * sin((current_time - _gfs2.t0) / 60) * (1 - (current_time - _gfs2.t0) / 700);
                }
            }
            draw_set_color(make_color_rgb(74 + _st * 6, 78 + _st * 6, 88 + _st * 6));
            draw_ellipse(_cn - _sw2 + _wob, _sy0 - _sh3, _cn + _sw2 + _wob, _sy0, false);
            draw_set_color(make_color_rgb(34, 36, 44));
            draw_ellipse(_cn - _sw2 + _wob, _sy0 - _sh3, _cn + _sw2 + _wob, _sy0, true);
            _sy0 -= _sh3 - 4;
        }
        // Loose stones waiting at the base.
        for (var _ls = 0; _ls < 5 - _stacked; _ls++) {
            var _lsx = _cn - 120 - _ls * 44;
            draw_set_color(make_color_rgb(64, 66, 76));
            draw_ellipse(_lsx - 18, 886, _lsx + 18, 902, false);
            draw_set_color(make_color_rgb(34, 36, 44));
            draw_ellipse(_lsx - 18, 886, _lsx + 18, 902, true);
        }
        // Completed cairn: a soft standing shimmer.
        if (_stacked >= 5) {
            draw_set_alpha(0.18 + 0.08 * sin(_t * 1.6));
            draw_set_color(make_color_rgb(160, 230, 170));
            draw_circle(_cn, 800, 70, false);
            draw_set_alpha(1.0);
        }
        if (touch_tapped(_cn - 170, 760, _cn + 90, 910)) input_inject("garden:stone");
    }

    // ---- Forage sparkles (3/day, seeded) ----
    var _spots = garden_forage_spots();
    for (var _fs = 0; _fs < array_length(_spots); _fs++) {
        var _fsp = _spots[_fs];
        if (_fsp.taken) continue;
        var _fx = _fsp.x - _cam;
        if (_fx < -40 || _fx > 1960) continue;
        var _tw = 0.5 + 0.5 * sin(_t * 4 + _fs * 2.3);
        draw_set_alpha(0.55 + 0.45 * _tw);
        draw_set_color(make_color_rgb(240, 230, 150));
        draw_line(_fx - 8 - 4 * _tw, _fsp.y, _fx + 8 + 4 * _tw, _fsp.y);
        draw_line(_fx, _fsp.y - 8 - 4 * _tw, _fx, _fsp.y + 8 + 4 * _tw);
        draw_circle(_fx, _fsp.y, 3, false);
        draw_set_alpha(1.0);
        if (touch_tapped(_fx - 34, _fsp.y - 34, _fx + 34, _fsp.y + 34)) input_inject("garden:forage" + string(_fsp.idx));
    }

    // ---- RESIDENTS: the entrusted creatures, two lanes, dwell-and-stroll ----
    var _dn  = array_length(bairc_donated());
    var _dsh = min(_dn, 16);
    var _crumb_on = (current_time - _gc.garden_crumb_t < 4500);
    var _hover_name = "";
    for (var _pass = 0; _pass < 2; _pass++) {          // 0 = back lane, 1 = front
        for (var _ri = 0; _ri < _dsh; _ri++) {
            var _back = (_ri mod 2 == 1);
            if (_back != (_pass == 0)) continue;
            var _rd  = bairc_donated()[_ri];
            var _rp  = { is_egg: false, species: _rd.species, stage: _rd.stage };
            var _rsp = pet_sprite(_rp, "s");
            if (_rsp < 0) continue;
            // Home x matches the Step's petting math EXACTLY.
            var _rh  = frac(sin((_ri + 1) * 91.17) * 47453.25);
            var _rx  = 260 + _rh * (_W - 620);
            // Dwell-and-stroll (band idiom): eased sine, clamped to linger.
            var _rsw = sin(_t * (0.14 + 0.04 * (_ri mod 3)) + _ri * 2.13);
            var _rmv = clamp((_rsw + 0.55) / 1.10, 0, 1);
            _rmv = _rmv * _rmv * (3 - 2 * _rmv);
            _rx += lerp(-130, 130, _rmv);
            // A fresh crumb draws anyone near the pond to the bank.
            if (_crumb_on && abs(_rx - 1250) < 760) {
                var _rct = clamp((current_time - _gc.garden_crumb_t) / 1200, 0, 1);
                _rx = lerp(_rx, 1250 + ((_rx < 1250) ? -380 : 380), _rct * 0.8);
            }
            var _ry  = _back ? 800 : 952;
            var _rsx = _rx - _cam;
            if (_rsx < -100 || _rsx > 2020) continue;
            // Idle bob + drink-dip at the pond.
            var _rbob = 2 * sin(_t * 2 + _ri);
            var _rfit = pet_sprite_fit(_rsp, _rsx, _ry + _rbob, (_back ? 74 : 104) * pet_stage_size_mult(_rd.stage));   // stage-scaled (08-19)
            draw_sprite_ext(_rsp, pet_anim_frame(_rsp), _rfit.x, _rfit.y,
                _rfit.scale, _rfit.scale, 0, c_white, _back ? 0.92 : 1);
            // Hover: name label; tap: a pet on the head.
            var _hw = _back ? 50 : 68;
            if (_mx >= _rsx - _hw && _mx <= _rsx + _hw && _my >= _ry - 130 && _my <= _ry + 10) {
                _hover_name = _rd.name + "  (" + pet_stage_name(_rd.stage) + ")";
                if (touch_tapped(_rsx - _hw, _ry - 130, _rsx + _hw, _ry + 10)) input_inject("garden:pet" + string(_ri));
            }
        }
    }
    if (_dn > _dsh && 60 - 0 < 2000) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(120, 130, 120));
        draw_text(30, 1002, "+" + string(_dn - _dsh) + " more live deeper in the grounds");
    }

    // ---- Ambient fireflies (more near a placed jar) ----
    var _ffn = 20 + (_has_jar ? 16 : 0);
    for (var _ff = 0; _ff < _ffn; _ff++) {
        var _fh  = frac(sin(_ff * 43.7) * 82631.1);
        var _ffx = frac(_fh + _t * 0.008 * (1 + _fh)) * _W - _cam;
        if (_ffx < -10 || _ffx > 1930) continue;
        var _ffy = 620 + frac(sin(_ff * 17.3) * 5417.9) * 400 + 14 * sin(_t * 1.1 + _ff);
        draw_set_alpha(0.35 + 0.55 * abs(sin(_t * 1.9 + _ff * 1.3)));
        draw_set_color(make_color_rgb(215, 235, 140));
        draw_circle(_ffx, _ffy, 2, false);
        draw_set_alpha(1.0);
    }

    // ---- Reaction FX: rising hearts + pond ripples ----
    for (var _xf = 0; _xf < array_length(_gc.garden_fx); _xf++) {
        var _fx2 = _gc.garden_fx[_xf];
        var _age = (current_time - _fx2.t0) / 1000;
        var _fxx = _fx2.x - _cam;
        if (_fxx < -100 || _fxx > 2020) continue;
        if (_fx2.kind == "hearts" && _age < 2.2) {
            for (var _hh = 0; _hh < 3; _hh++) {
                var _ht = _age - _hh * 0.25;
                if (_ht < 0 || _ht > 1.6) continue;
                var _hx = _fxx + 12 * sin(_ht * 5 + _hh * 2) + (_hh - 1) * 16;
                var _hy = _fx2.y - 90 - _ht * 70;
                draw_set_alpha(clamp(1.4 - _ht, 0, 1) * 0.9);
                draw_set_color(make_color_rgb(235, 110, 140));
                draw_circle(_hx - 4, _hy, 5, false);
                draw_circle(_hx + 4, _hy, 5, false);
                draw_triangle(_hx - 8, _hy + 2, _hx + 8, _hy + 2, _hx, _hy + 13, false);
            }
            draw_set_alpha(1.0);
        } else if (_fx2.kind == "ripple" && _age < 1.8) {
            draw_set_alpha(clamp(1 - _age / 1.8, 0, 1) * 0.6);
            draw_set_color(make_color_rgb(150, 190, 215));
            draw_ellipse(_fxx - 20 - _age * 90, _fx2.y - 6 - _age * 26,
                         _fxx + 20 + _age * 90, _fx2.y + 6 + _age * 26, true);
            draw_set_alpha(1.0);
        }
    }

    // ---- Verb chips above the features currently on screen ----
    draw_set_font(ui_font(fnt_ui_small));
    if (_pd > -240 && _pd < 2160) ui_garden_chip(_pd, 792, "[1] TOSS A CRUMB", "garden:crumb");
    if (_cn > -240 && _cn < 2160) {
        ui_garden_chip(_cn, 726, (_stacked >= 5) ? "the cairn holds" : "[2] PLACE A STONE  (" + string(_stacked) + "/5)",
            (_stacked >= 5) ? "" : "garden:stone");
    }
    for (var _fs2 = 0; _fs2 < array_length(_spots); _fs2++) {
        var _fc2 = _spots[_fs2];
        if (_fc2.taken) continue;
        var _fx3 = _fc2.x - _cam;
        if (_fx3 > -120 && _fx3 < 2040) ui_garden_chip(_fx3, _fc2.y - 66, "[3] FORAGE", "garden:forage" + string(_fc2.idx));
    }
    if (_hover_name != "") {
        draw_set_halign(fa_center);
        draw_set_color(make_color_rgb(210, 220, 210));
        draw_text(_mx, _my - 42, _hover_name);
        draw_set_color(make_color_rgb(150, 165, 150));
        draw_text(_mx, _my - 18, ui_hint("[E] or click to pet", "Tap to pet"));
        draw_set_halign(fa_left);
    }

    // ---- HUD frame: title, leave/shop chips, position strip, hints, gold ----
    draw_set_alpha(0.55);
    draw_set_color(c_black);
    draw_rectangle(GUI_XL, 0, GUI_XR, 66, false);
    draw_rectangle(GUI_XL, 1032, GUI_XR, GUI_H, false);
    draw_set_alpha(1.0);
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(170, 210, 170));
    draw_text(960, 16, "B A I R C ' S   G A R D E N");
    draw_set_halign(fa_left);
    ui_garden_chip(140, 16, "[Esc] LEAVE", "garden:leave");
    ui_garden_chip(1700, 16, "[B] ORNAMENTS", "garden:shop");
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_halign(fa_right);
    draw_set_color(c_yellow);
    draw_text(1560, 22, "Gold: " + string(global.gold) + "   Dust: " + string(variable_global_exists("rune_dust") ? global.rune_dust : 0));
    // 08-18 (M screenshot "text collisions all over baircs garden"): the 48px bottom band
    // (1032-1080) is fixed-height, so it draws in the DENSE font (base size even in Large
    // mode), the hint row sits at the top of the band with the position strip UNDER it,
    // and the chips are measured (ui_garden_chip) so they never spill past GUI_H.
    draw_set_font(ui_font_dense(fnt_ui_small));
    draw_set_halign(fa_center); draw_set_valign(fa_top);
    draw_set_color(make_color_rgb(130, 145, 132));
    // Device-aware hint (08-18 mobile pass): on touch the letter keys mean nothing -
    // the verbs are the tappable chips over the pond / cairn / forage spots.
    var _gh_hint = (input_device() == 2)
        ? "Drag to wander        Tap a creature to pet        Tap a chip to act        "
        : "A/D or drag: wander        [E] pet        [1] pond    [2] cairn    [3] forage        ";
    draw_text(960, 1035, _gh_hint + string(_dn) + " resident" + ((_dn == 1) ? "" : "s"));
    draw_set_halign(fa_left);
    // Position strip: where the camera window sits over the grounds (below the hint row).
    draw_set_color(make_color_rgb(30, 38, 32));
    draw_rectangle(660, 1066, 1260, 1073, false);
    draw_set_color(make_color_rgb(150, 200, 150));
    var _ps0 = 660 + 600 * (_cam / _W);
    draw_rectangle(_ps0, 1066, _ps0 + 600 * (1920 / _W), 1073, false);
    // Music selector chip (bottom-right): cycles the garden's track pool.
    ui_garden_chip(1700, 1036, "[M] MUSIC", "garden:music", true);

    // ---- Notice toast (standard boxed toast, topmost of the scene HUD) ----
    if (_gc.garden_notice != "" && _gc.garden_notice_t > 0) {
        ui_draw_toast(_gc.garden_notice, 960, 120, min(1, _gc.garden_notice_t / 30));
    }
    // ---- WORK-IN-PROGRESS banner (M 08-18): every entry, 8s, fades over its last second.
    //      Boxed + measured (longest line sets the width) so it never spills. ----
    if (variable_instance_exists(_gc, "garden_wip_t") && _gc.garden_wip_t > 0) {
        var _wa  = min(1, _gc.garden_wip_t / 60);
        var _wl1 = "THE GARDEN IS A WORK IN PROGRESS";
        var _wl2 = "Apologies for the rough edges - big things are coming for customizing your pet garden.";
        var _wl3 = "Please bear with us.";
        draw_set_font(ui_font(fnt_ui));
        var _ww = max(string_width(_wl1), string_width(_wl2), string_width(_wl3)) + 60;
        var _wh = 132;
        var _wx = 960 - _ww / 2, _wy = 165;
        draw_set_alpha(0.90 * _wa); draw_set_color(make_color_rgb(14, 20, 16));
        draw_rectangle(_wx, _wy, _wx + _ww, _wy + _wh, false);
        draw_set_alpha(_wa); draw_set_color(make_color_rgb(150, 205, 150));
        draw_rectangle(_wx, _wy, _wx + _ww, _wy + _wh, true);
        draw_set_halign(fa_center); draw_set_valign(fa_top);
        draw_set_color(make_color_rgb(235, 220, 150)); draw_text(960, _wy + 16, _wl1);
        draw_set_color(make_color_rgb(215, 225, 215)); draw_text(960, _wy + 54, _wl2);
        draw_text(960, _wy + 88, _wl3);
        draw_set_halign(fa_left); draw_set_alpha(1.0);
    }

    // ---- ORNAMENT SHOP overlay ----
    if (_gc.garden_shop_open) {
        draw_set_alpha(0.62); draw_set_color(c_black);
        draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
        draw_set_alpha(1.0);
        var _sx0 = 520, _sy0 = 150, _sx1 = 1400, _sy1 = 936;
        draw_set_color(make_color_rgb(14, 18, 15));
        draw_rectangle(_sx0, _sy0, _sx1, _sy1, false);
        draw_set_color(make_color_rgb(110, 170, 120));
        draw_rectangle(_sx0, _sy0, _sx1, _sy1, true);
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(190, 230, 190));
        draw_text((_sx0 + _sx1) / 2, _sy0 + 16, "ORNAMENTS FOR THE GARDEN");
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(120, 140, 125));
        draw_text((_sx0 + _sx1) / 2, _sy0 + 52, "\"It could use a little something. They notice, you know.\"");
        draw_set_halign(fa_left);
        var _sc_cat = garden_decor_catalog();
        var _srow_y = _sy0 + 96, _srow_h = 82;
        for (var _sr = 0; _sr < array_length(_sc_cat); _sr++) {
            var _sd = _sc_cat[_sr];
            var _sry = _srow_y + _sr * _srow_h;
            var _son = (_sr == _gc.garden_shop_cur);
            var _splaced = garden_decor_placed(_sd.id);
            if (touch_tapped(_sx0 + 16, _sry, _sx1 - 16, _sry + _srow_h - 8)) input_inject("garden:shoprow" + string(_sr));
            draw_set_color(_son ? make_color_rgb(26, 38, 28) : make_color_rgb(18, 22, 19));
            draw_rectangle(_sx0 + 16, _sry, _sx1 - 16, _sry + _srow_h - 8, false);
            draw_set_color(_splaced ? make_color_rgb(70, 110, 80) : (_son ? make_color_rgb(150, 210, 160) : make_color_rgb(48, 62, 50)));
            draw_rectangle(_sx0 + 16, _sry, _sx1 - 16, _sry + _srow_h - 8, true);
            draw_set_font(ui_font(fnt_ui));
            draw_set_color(_splaced ? make_color_rgb(120, 140, 125) : c_white);
            draw_text(_sx0 + 36, _sry + 8, _sd.name);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(130, 145, 132));
            draw_text(_sx0 + 36, _sry + 44, _sd.blurb);
            draw_set_halign(fa_right);
            if (_splaced) {
                draw_set_color(make_color_rgb(120, 200, 140));
                draw_text(_sx1 - 36, _sry + 12, "PLACED");
            } else {
                draw_set_color(c_yellow);
                draw_text(_sx1 - 36, _sry + 12, string(_sd.gold) + "g + " + string(_sd.dust) + " dust");
            }
            draw_set_halign(fa_left);
        }
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(130, 150, 135));
        ui_draw_key_legend((_sx0 + _sx1) / 2, _sy1 - 30, "W/S: Browse    Enter: Choose    Esc: Close");
        draw_set_halign(fa_left);
    }

    // ---- Fade-in from black ----
    if (_gc.garden_fade > 0) {
        draw_set_alpha(_gc.garden_fade / 24);
        draw_set_color(c_black);
        draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
        draw_set_alpha(1.0);
    }

    draw_set_color(c_white); draw_set_alpha(1.0); draw_set_font(-1);
    draw_set_halign(fa_left); draw_set_valign(fa_top);
}

// Small tappable verb chip, centred on x. Empty tag = label only (no action).
function ui_garden_chip(_cx, _cy, _label, _tag, _dense = false) {
    // _dense: bottom-band chips use the base-size font so the fixed 48px band fits Large mode.
    // Touch: the chip IS the button - drop the "[1]" / "[Esc]" key tokens from the label.
    if (input_device() == 2) _label = ui_strip_keys(_label);
    draw_set_font(_dense ? ui_font_dense(fnt_ui_small) : ui_font(fnt_ui_small));
    var _w  = string_width(_label) + 26;
    var _th = string_height(_label);
    var _x0 = _cx - _w / 2, _x1 = _cx + _w / 2, _y1 = _cy + max(34, _th + 14);
    draw_set_alpha(0.85);
    draw_set_color(make_color_rgb(12, 18, 14));
    draw_rectangle(_x0, _cy, _x1, _y1, false);
    draw_set_alpha(1.0);
    draw_set_color((_tag == "") ? make_color_rgb(60, 72, 62) : make_color_rgb(110, 170, 120));
    draw_rectangle(_x0, _cy, _x1, _y1, true);
    draw_set_halign(fa_center);
    draw_set_color((_tag == "") ? make_color_rgb(120, 135, 122) : make_color_rgb(190, 230, 190));
    draw_set_valign(fa_top);
    draw_text(_cx, _cy + ((_y1 - _cy) - _th) / 2, _label);
    draw_set_halign(fa_left);
    if (_tag != "" && touch_tapped(_x0, _cy, _x1, _y1)) input_inject(_tag);
}

// =============================================================================
// PATTERN BOOK DRAW (M design-locked 08-11, SYSTEMS_REFORGE_CRAFT.md).
// Three overlays on Dorn's reforge tab. All rows/buttons hit-test HERE and
// inject pbsm:/pbbk:/pb: tags for the gc Step (touch rule: hit-tests in Draw).
// =============================================================================

// Slot-dispatch icon resolve for an icon proxy / art entry / item.
function ui_pb_slot_icon(_p) {
    if (!is_struct(_p) || !variable_struct_exists(_p, "slot")) return -1;
    switch (_p.slot) {
        case "weapon": case "ranged_weapon": return ui_weapon_icon_sprite(_p);
        case "offhand": return ui_offhand_icon_sprite(_p);
        case "chest":   return ui_chest_icon_sprite(_p);
        case "helm":    return ui_helm_icon_sprite(_p);
        case "gloves":  return ui_gloves_icon_sprite(_p);
        case "boots":   return ui_boots_icon_sprite(_p);
        case "ring":    return ui_ring_icon_sprite(_p);
        case "amulet":  return ui_amulet_icon_sprite(_p);
    }
    return -1;
}

// Shared vertical scroll bar (standing rule: overflowing lists show a VISIBLE
// track + proportional thumb). No-op when everything fits.
function ui_pb_scrollbar(_x, _y0, _y1, _total, _vis, _win0) {
    if (_total <= _vis) return;
    draw_set_color(make_color_rgb(40, 38, 48));
    draw_rectangle(_x, _y0, _x + 8, _y1, false);
    var _h  = _y1 - _y0;
    var _th = max(24, _h * _vis / _total);
    var _ty = _y0 + (_h - _th) * (_win0 / max(1, _total - _vis));
    draw_set_color(make_color_rgb(150, 130, 100));
    draw_rectangle(_x, _ty, _x + 8, _ty + _th, false);
}

// Three-pip blueprint tier readout (filled up to the unlocked tier).
function ui_pb_pips(_x, _y, _tier) {
    for (var _i = 0; _i < 3; _i++) {
        var _on = (_tier >= _i + 1);
        draw_set_color(_on ? make_color_rgb(230, 195, 120) : make_color_rgb(60, 58, 70));
        draw_rectangle(_x + _i * 20, _y, _x + _i * 20 + 13, _y + 13, false);
        draw_set_color(make_color_rgb(28, 26, 36));
        draw_rectangle(_x + _i * 20, _y, _x + _i * 20 + 13, _y + 13, true);
    }
}

// ---------------------------------------------------------------------------
// ui_draw_pattern_book(_gc) - the BOOK browse overlay: every affix family with
// its tier pips + progress, and the art page count. Read-only reference.
// ---------------------------------------------------------------------------
function ui_draw_pattern_book(_gc) {
    var _copper   = make_color_rgb(210, 140, 70);
    var _panel_bg = make_color_rgb(16, 16, 22);
    var _x0 = 150, _x1 = 1500, _y0 = 189, _y1 = 900;
    // Panel CENTER is 825, not screen-center 960 (M 08-15: the 960-centred
    // header ran out the panel's RIGHT side at every font size).
    var _pbcx = (_x0 + _x1) / 2;
    draw_set_color(_panel_bg); draw_rectangle(_x0, _y0, _x1, _y1, false);
    draw_set_color(_copper);   draw_rectangle(_x0, _y0, _x1, _y1, true);
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(255, 225, 150));
    draw_text(_pbcx, _y0 + 15, "THE PATTERN BOOK");
    pattern_book_ensure();
    // Two authored lines (M 08-13: the old draw_text_ext wrap split "Art page:
    // 0 icons unlocked" mid-phrase and read as rough text).
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(170, 160, 190));
    var _hdr1 = "Blueprints from SMELTING gear - 1 study unlocks Uncommon crafts, 3 unlock Rare, 6 unlock Epic. Rarer fodder teaches faster.";
    var _hdr2 = "Art page: " + string(array_length(global.pattern_book.art)) + " icon" + ((array_length(global.pattern_book.art) == 1) ? "" : "s") + " unlocked.";
    // One-line gate is 1130, not panel-width: the centred line must also stay
    // clear of the UP arrow button at x >= _x1-96 (2*(1404-825) - margin).
    if (string_width(_hdr1) <= 1130) {
        draw_text(_pbcx, _y0 + 44, _hdr1);
        draw_text(_pbcx, _y0 + 70, _hdr2);
    } else {
        // Large font: the one-liner outgrows the panel - split at the authored
        // comma and fold the short art-page count onto line 2. Rows stay +108.
        draw_text(_pbcx, _y0 + 40, "Blueprints from SMELTING gear - 1 study unlocks Uncommon crafts,");
        draw_text(_pbcx, _y0 + 68, "3 unlock Rare, 6 unlock Epic. Rarer fodder teaches faster.   " + _hdr2);
    }
    draw_set_halign(fa_left);

    var _cat  = pattern_family_catalog();
    var _n    = array_length(_cat);
    var _vis  = 10;   // MUST mirror the Step's pb_book handler
    var _win0 = clamp(_gc.pb_book_scroll, 0, max(0, _n - _vis));
    var _row_y0 = _y0 + 108, _pitch = 56;
    var _mx = device_mouse_x_to_gui(0), _my = device_mouse_y_to_gui(0);
    var _mp = mouse_check_button_pressed(mb_left);
    var _bk_cur = variable_instance_exists(_gc, "pb_book_cursor") ? _gc.pb_book_cursor : -1;
    for (var _i = _win0; _i < min(_n, _win0 + _vis); _i++) {
        var _e  = _cat[_i];
        var _ry = _row_y0 + (_i - _win0) * _pitch;
        var _tier = pattern_fam_tier(_e.stat_name);
        var _is_cur = (_i == _bk_cur);
        draw_set_color(_is_cur ? make_color_rgb(28, 36, 56) : make_color_rgb(20, 20, 26));
        draw_rectangle(_x0 + 20, _ry, _x1 - 40, _ry + _pitch - 6, false);
        // Blue active-selection highlight (M 08-13), same idiom as every other
        // key-navigated list in the game.
        draw_set_color(_is_cur ? make_color_rgb(95, 145, 225) : make_color_rgb(48, 44, 40));
        draw_rectangle(_x0 + 20, _ry, _x1 - 40, _ry + _pitch - 6, true);
        if (_is_cur) draw_rectangle(_x0 + 21, _ry + 1, _x1 - 41, _ry + _pitch - 7, true);
        draw_set_font(ui_font(fnt_ui));
        // Three states (M 08-15: "my eye isnt caught by lines ive started"):
        // unlocked tier = white, STARTED (any study banked) = warm parchment,
        // untouched = grey.
        var _pbp = pattern_fam_get(_e.stat_name);
        var _pbc = (_tier > 0) ? c_white
                 : ((_pbp.p1 > 0) ? make_color_rgb(226, 214, 178) : make_color_rgb(110, 110, 125));
        draw_set_color(_pbc);
        draw_text(_x0 + 44, _ry + 10, _e.label);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(130, 125, 150));
        draw_text(_x0 + 420, _ry + 14, (_e.kind == "school") ? "Caster Affix (amulet/ring)" : "Stat Affix");
        ui_pb_pips(_x0 + 790, _ry + 16, _tier);
        draw_set_halign(fa_right);
        draw_set_color((_tier >= 3) ? make_color_rgb(230, 195, 120) : make_color_rgb(150, 150, 165));
        draw_text(_x1 - 60, _ry + 14, pattern_fam_progress_text(_e.stat_name));
        draw_set_halign(fa_left);
    }
    ui_pb_scrollbar(_x1 - 32, _row_y0, _row_y0 + _vis * _pitch - 6, _n, _vis, _win0);

    // Touch scroll arrows + close (bare W/S and Esc are keyboard-only).
    // Triangle-glyph buttons (M 08-26: the UP/DOWN text labels were cropped
    // poorly in their boxes) - a drawn glyph fits every font-size mode.
    ui_arrow_button(_x1 - 96, _y0 + 10, _x1 - 48, _y0 + 46, -1, make_color_rgb(150, 130, 100), _mx, _my, _mp, "pbbk:up");
    ui_arrow_button(_x1 - 96, _y1 - 46, _x1 - 48, _y1 - 10,  1, make_color_rgb(150, 130, 100), _mx, _my, _mp, "pbbk:dn");
    ui_confirm_button(_pbcx - 150, _y1 - 44, _pbcx + 150, _y1 - 4, "CLOSE  [Esc]", make_color_rgb(190, 120, 110), _mx, _my, _mp, "pbbk:close");
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(75, 85, 105));
    ui_draw_key_legend(960, 1026, "W/S: Scroll   Esc: Close");
    draw_set_halign(fa_left);
}

// ---------------------------------------------------------------------------
// ui_draw_pattern_craft(_gc) - the CRAFT wizard. Phases 0-4 are pick lists
// (slot / rarity / base stat / affixes / art), 5 is naming, 6 the result card.
// Geometry MUST mirror the Step handler: 9 visible rows, pitch 56, top y300.
// ---------------------------------------------------------------------------
function ui_draw_pattern_craft(_gc) {
    var _copper   = make_color_rgb(210, 140, 70);
    var _panel_bg = make_color_rgb(16, 16, 22);
    var _x0 = 150, _x1 = 1500, _y0 = 189, _y1 = 900;
    draw_set_color(_panel_bg); draw_rectangle(_x0, _y0, _x1, _y1, false);
    draw_set_color(_copper);   draw_rectangle(_x0, _y0, _x1, _y1, true);

    var _ph    = _gc.pb_craft_phase;
    var _slots = forge_slot_list();
    var _slot  = _slots[clamp(_gc.pb_slot_pick, 0, array_length(_slots) - 1)];
    var _rar   = 1 + _gc.pb_rar_pick;
    var _mx = device_mouse_x_to_gui(0), _my = device_mouse_y_to_gui(0);
    var _mp = mouse_check_button_pressed(mb_left);

    var _titles = ["CRAFT: CHOOSE THE TYPE", "CHOOSE THE SLOT", "CHOOSE THE QUALITY",
                   "CHOOSE YOUR BLUEPRINTS", "CHOOSE ITS ART", "NAME IT", "YOUR CRAFT"];
    var _pccx = (_x0 + _x1) / 2;   // panel centre 825, not screen 960 (M 08-15)
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(255, 225, 150));
    draw_text(_pccx, _y0 + 15, _titles[clamp(_ph, 0, 6)]);

    // Breadcrumb: the choices locked in so far. Title-cased segments joined by
    // small drawn diamonds - "melee weapon  >  Uncommon" read as keyboard-symbol
    // dev text, not finished UI (M 08-15 shot).
    var _segs = [];
    if (_ph >= 2) array_push(_segs, item_slot_title(_slot));
    if (_ph >= 3) array_push(_segs, item_rarity_name(_rar));
    if (_ph >= 4 && array_length(_gc.pb_affix_picks) > 0) {
        var _cl = "";
        for (var _ci = 0; _ci < array_length(_gc.pb_affix_picks); _ci++) {
            var _cfe = pattern_family_entry(_gc.pb_affix_picks[_ci]);
            if (_cfe != undefined) _cl += ((_cl == "") ? "" : ", ") + _cfe.label;
        }
        array_push(_segs, _cl);
    }
    if (array_length(_segs) > 0) {
        draw_set_font(ui_font(fnt_ui_small));
        var _seg_gap = 36;
        var _bw = 0;
        for (var _si = 0; _si < array_length(_segs); _si++) {
            _bw += string_width(_segs[_si]) + ((_si > 0) ? _seg_gap : 0);
        }
        if (_bw > (_x1 - _x0) - 60) {
            // 3 affix labels at Large font can outgrow the panel - fall back to
            // one truncated comma line (M 08-14 font pass).
            var _crumb = "";
            for (var _si = 0; _si < array_length(_segs); _si++) {
                _crumb += ((_si > 0) ? ", " : "") + _segs[_si];
            }
            draw_set_color(make_color_rgb(190, 180, 205));
            draw_text(_pccx, _y0 + 52, ui_truncate(_crumb, (_x1 - _x0) - 60));
        } else {
            draw_set_halign(fa_left);
            var _bx  = _pccx - _bw / 2;
            var _bcy = _y0 + 52 + string_height("Ag") / 2;
            for (var _si = 0; _si < array_length(_segs); _si++) {
                if (_si > 0) {
                    var _dx = _bx + _seg_gap / 2;
                    draw_set_color(make_color_rgb(150, 130, 100));
                    draw_triangle(_dx - 5, _bcy, _dx, _bcy - 5, _dx + 5, _bcy, false);
                    draw_triangle(_dx - 5, _bcy, _dx, _bcy + 5, _dx + 5, _bcy, false);
                    _bx += _seg_gap;
                }
                draw_set_color(make_color_rgb(190, 180, 205));
                draw_text(_bx, _y0 + 52, _segs[_si]);
                _bx += string_width(_segs[_si]);
            }
        }
    }
    draw_set_halign(fa_left);

    // ---- NAMING (phase 5): the forge's typed-entry idiom + RANDOM button.
    if (_ph == 5) {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(170, 160, 190));
        draw_text(960, 380, "Type its name, or let the book roll one. Enter forges it.");
        draw_set_color(make_color_rgb(30, 28, 40));
        draw_rectangle(560, 460, 1360, 530, false);
        draw_set_color(_copper);
        draw_rectangle(560, 460, 1360, 530, true);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(c_white);
        var _caret = (current_time mod 1000 < 500) ? "|" : " ";
        // Shrink-to-fit: cap grew 24 -> 72 (M 08-26) - scale inside the 800px box.
        var _cw_ntxt = keyboard_string + _caret;
        var _cw_nsc  = min(1.0, 760 / max(1, string_width(_cw_ntxt)));
        draw_text_transformed(960, 478 + (1 - _cw_nsc) * 12, _cw_ntxt, _cw_nsc, _cw_nsc, 0);
        ui_confirm_button(560, 580, 940, 636, "RANDOM NAME", make_color_rgb(150, 130, 100), _mx, _my, _mp, "pb:rand");
        ui_confirm_button(980, 580, 1360, 636, "FORGE IT  [Enter]", make_color_rgb(120, 210, 130), _mx, _my, _mp, "pb:ok");
        // WEAPON OPTION CHIPS (M design-locked 08-26 parity). Typing owns the
        // letters, so the chips ride Tab / arrows; taps inject pb:twoh/pb:school.
        var _pb5_legend = "Type: Name   Enter: Forge   Esc: Back";
        if (_slot == "weapon" || _slot == "ranged_weapon") {
            var _pb5_2h = variable_instance_exists(_gc, "pb_opt_twoh") && _gc.pb_opt_twoh;
            ui_confirm_button(560, 664, 940, 716,
                "TWO-HANDED: " + (_pb5_2h ? "ON" : "OFF") + "  [Tab]",
                _pb5_2h ? make_color_rgb(120, 210, 130) : make_color_rgb(120, 110, 95),
                _mx, _my, _mp, "pb:twoh");
            if (_slot == "ranged_weapon") {
                var _pb5_sch = variable_instance_exists(_gc, "pb_opt_school") ? _gc.pb_opt_school : "";
                ui_confirm_button(980, 664, 1360, 716,
                    "DAMAGE: " + ((_pb5_sch == "") ? "Physical" : school_label(_pb5_sch)) + "  [< >]",
                    (_pb5_sch == "") ? make_color_rgb(150, 140, 120) : school_color(_pb5_sch),
                    _mx, _my, _mp, "pb:school");
            }
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(150, 140, 165));
            draw_text(960, 740, (_slot == "ranged_weapon")
                ? "Two-handed trades your offhand for ~+80% damage (a schooled 2H is a STAFF - keeps focus/tome offhands)."
                : "Two-handed trades your offhand for ~+80% damage.");
            _pb5_legend = "Type: Name   Tab: 2H   " + ((_slot == "ranged_weapon") ? "</>: School   " : "") + "Enter: Forge   Esc: Back";
        }
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(75, 85, 105));
        ui_draw_key_legend(960, 1026, _pb5_legend);
        draw_set_halign(fa_left);
        return;
    }

    // ---- RESULT (phase 6): reveal card + HONE (M design-locked 08-26 - the
    // numbers-reroll charged gold on often-zero-width bands; HONE buys +1 on a
    // chosen line up to the mastery ceiling, fee 100g x (points honed + 1)).
    if (_ph == 6) {
        var _it = _gc.pb_result;
        if (_it == undefined) return;
        ui_reforge_card(660, 300, 1260, 640, "PATTERN-CRAFTED", make_color_rgb(120, 210, 130),
            _it.name, _it.rarity, ui_item_stat_str(_it));
        var _ic = ui_pb_slot_icon((variable_struct_exists(_it, "icon_as") && is_struct(_it.icon_as)) ? _it.icon_as : _it);
        if (_ic != -1 && sprite_exists(_ic)) draw_sprite_stretched(_ic, 0, 924, 556, 72, 72);
        var _hn_open = variable_instance_exists(_gc, "pb_hone_pick") && _gc.pb_hone_pick >= 0;
        var _hn_fee  = pattern_hone_fee(_it);
        if (_hn_open) {
            // HONE panel: one row per crafted line, current value -> ceiling.
            var _hn_rows = pattern_hone_rows(_it);
            var _hn_y0   = 664;
            draw_set_color(make_color_rgb(16, 16, 22));
            draw_rectangle(560, _hn_y0, 1360, _hn_y0 + array_length(_hn_rows) * 60 + 84, false);
            draw_set_color(make_color_rgb(190, 160, 90));
            draw_rectangle(560, _hn_y0, 1360, _hn_y0 + array_length(_hn_rows) * 60 + 84, true);
            for (var _hi = 0; _hi < array_length(_hn_rows); _hi++) {
                var _hr  = _hn_rows[_hi];
                var _hry = _hn_y0 + 12 + _hi * 60;
                var _hsel = (_gc.pb_hone_pick == _hi);
                if (_mp && _mx >= 570 && _mx < 1350 && _my >= _hry && _my < _hry + 54) input_inject("pbhn:row" + string(_hi));
                draw_set_alpha(_hsel ? 0.95 : 0.55);
                draw_set_color(_hsel ? make_color_rgb(40, 34, 18) : make_color_rgb(20, 20, 27));
                draw_rectangle(570, _hry, 1350, _hry + 54, false);
                draw_set_alpha(1.0);
                draw_set_color(_hsel ? make_color_rgb(190, 160, 90) : make_color_rgb(60, 55, 45));
                draw_rectangle(570, _hry, 1350, _hry + 54, true);
                draw_set_font(ui_font(fnt_ui));
                draw_set_color(c_white);
                draw_text(590, _hry + 12, _hr.label);
                draw_set_halign(fa_right);
                var _hdone = (_hr.value >= _hr.cap);
                draw_set_color(_hdone ? make_color_rgb(120, 210, 130) : make_color_rgb(230, 195, 120));
                draw_text(1330, _hry + 12, _hdone ? ("+" + string(_hr.value) + "  (finished)")
                                                  : ("+" + string(_hr.value) + "  ->  cap +" + string(_hr.cap)));
                draw_set_halign(fa_left);
            }
            var _hn_by = _hn_y0 + array_length(_hn_rows) * 60 + 20;
            ui_confirm_button(580, _hn_by, 950, _hn_by + 52, "HONE +1  (" + string(_hn_fee) + "g)  [Enter]",
                make_color_rgb(120, 210, 130), _mx, _my, _mp, "pbhn:buy");
            ui_confirm_button(990, _hn_by, 1340, _hn_by + 52, "BACK  [Esc]",
                make_color_rgb(190, 120, 110), _mx, _my, _mp, "pbhn:close");
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(75, 85, 105));
            ui_draw_key_legend(960, 1026, "W/S: Line   Enter: Hone +1   Esc: Back");
            draw_set_halign(fa_left);
        } else {
            ui_confirm_button(560, 700, 940, 756, "HONE +1 (" + string(_hn_fee) + "g)  [R]",
                make_color_rgb(190, 160, 90), _mx, _my, _mp, "pb:hone");
            ui_confirm_button(980, 700, 1360, 756, "DONE  [Enter]", make_color_rgb(120, 210, 130), _mx, _my, _mp, "pb:done");
            draw_set_halign(fa_center);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(170, 160, 190));
            draw_text(960, 780, "HONE buys +1 on a line of your choosing, up to its mastery ceiling - the fee grows with every point.");
            draw_set_color(make_color_rgb(75, 85, 105));
            ui_draw_key_legend(960, 1026, "R: Hone   Enter: Done");
            draw_set_halign(fa_left);
        }
        return;
    }

    // ---- PICK LISTS (phases 0-4, 08-15 v2). Rows: 9 visible, pitch 56, top
    // y300. Order: type > slot > quality > blueprints > art. The blueprint
    // list shows ONLY what the player has unlocked for the chosen quality
    // (M: "list all of your unlocked blueprints nothing else").

    // RULE HINTS (M design-locked 08-26 transparency): the wizard teaches its
    // own rules where they apply. DENSE font in the fixed band under the
    // breadcrumb so Large mode cannot spill into the rows.
    if (_ph == 2 || _ph == 3) {
        draw_set_halign(fa_center);
        draw_set_font(ui_font_dense(fnt_ui_small));
        draw_set_color(make_color_rgb(150, 140, 165));
        if (_ph == 2) {
            draw_text(_pccx, 272, "Your FIRST core-stat pick (Might, Grace, ...) becomes the item's BASE LINE: +3 / +5 / +7 by quality.");
        } else {
            var _hint_bud   = pattern_affix_budget(_rar);
            var _hint_picks = array_length(_gc.pb_affix_picks);
            var _hint_spare = max(0, _hint_bud - _hint_picks);
            if (_hint_spare > 0 && _hint_picks > 0) {
                draw_set_color(make_color_rgb(230, 195, 120));
                draw_text(_pccx, 292, string(_hint_picks) + " of " + string(_hint_bud)
                    + " picks - each unused slot CONCENTRATES your picks: they roll x"
                    + string(1 + 0.5 * _hint_spare) + ".");
            } else {
                draw_text(_pccx, 292, (_hint_picks == 0)
                    ? ("Up to " + string(_hint_bud) + " picks. Fewer picks CONCENTRATE: each unused slot multiplies the rest x1.5.")
                    : "All pick slots used - full spread, no concentration.");
            }
        }
        draw_set_halign(fa_left);
    }
    var _rows = [];   // { label, right, desc, rar (-1 or rarity tint), dim, icon (sprite or -1), picked }
    if (_ph == 0) {
        // Category: weapons / armor / jewelry (M: "read organically").
        var _cats0 = pattern_craft_categories();
        for (var _i0 = 0; _i0 < array_length(_cats0); _i0++) {
            array_push(_rows, { label: _cats0[_i0].label, right: _cats0[_i0].desc, desc: "", rar: -1, dim: false, icon: -1, picked: false });
        }
    } else if (_ph == 1) {
        var _cats1 = pattern_craft_categories();
        var _cs1 = _cats1[clamp(_gc.pb_cat_pick, 0, array_length(_cats1) - 1)].slots;
        for (var _is = 0; _is < array_length(_cs1); _is++) {
            array_push(_rows, { label: _cs1[_is].label, right: "", desc: "", rar: -1, dim: false, icon: -1, picked: false });
        }
    } else if (_ph == 2) {
        // Quality: gates which blueprints appear next (1/3/6 studies = U/R/E).
        for (var _i1 = 0; _i1 < 3; _i1++) {
            var _rr = 1 + _i1;
            var _rf = pattern_craft_fee(_rr);
            var _pks = pattern_affix_budget(_rr);
            array_push(_rows, { label: item_rarity_name(_rr),
                right: string(_rf.gold) + "g,  " + string(_rf.dust) + " dust,  1 " + item_rarity_name(_rf.ingot_rar)
                    + " or better ingot,  " + string(pattern_craft_reagents(_rr)) + " reagent" + ((pattern_craft_reagents(_rr) == 1) ? "" : "s")
                    + " (have " + string(reagent_total()) + ")     " + string(_pks) + " blueprint pick" + ((_pks == 1) ? "" : "s"),
                desc: "", rar: _rr, dim: false, icon: -1, picked: false });
        }
    } else if (_ph == 3) {
        // YOUR BLUEPRINTS: only families unlocked for this quality. A core
        // stat pick (STR..CHA) becomes the item's base line; the rest roll as
        // affixes. Caster (school) blueprints only fit jewelry. Each row carries
        // the family's what-it-does line (M 08-15: "Hexruin, Shadows, Vitality
        // should say what the affix means/does").
        var _cat3 = pattern_family_catalog();
        var _caster = (_slot == "amulet" || _slot == "ring");
        for (var _i3 = 0; _i3 < array_length(_cat3); _i3++) {
            var _e3 = _cat3[_i3];
            if (_e3.kind == "school" && !_caster) continue;
            var _t3 = pattern_fam_tier(_e3.stat_name);
            if (_t3 < _rar) continue;
            var _pk = false;
            for (var _j3 = 0; _j3 < array_length(_gc.pb_affix_picks); _j3++) {
                if (_gc.pb_affix_picks[_j3] == _e3.stat_name) { _pk = true; break; }
            }
            array_push(_rows, { label: _e3.label,
                right: "Rolls " + pattern_band_text(_e3.stat_name, _rar, _t3),
                desc: pattern_family_desc(_e3.stat_name), rar: -1,
                dim: false, icon: -1, picked: _pk });
        }
    } else {   // phase 4: art page for the slot family (weapons pool, jewelry pools)
        array_push(_rows, { label: "Dorn's plain work", right: "The standard face for its name", desc: "", rar: -1, dim: false, icon: -1, picked: false });
        var _arts = pattern_art_for_slot(_slot);
        for (var _i4 = 0; _i4 < array_length(_arts); _i4++) {
            var _pr4 = pattern_art_proxy(_arts[_i4]);
            array_push(_rows, { label: _arts[_i4].label, right: item_rarity_name(_arts[_i4].rarity),
                desc: "", rar: _arts[_i4].rarity, dim: false, icon: ui_pb_slot_icon(_pr4), picked: false });
        }
        // Where icons COME FROM (M 08-26: the pool read as arbitrary) - one
        // hint line between the breadcrumb (y241-265) and the rows (y300).
        // DENSE font: the band is fixed-height, so Large mode must not spill.
        draw_set_halign(fa_center);
        draw_set_font(ui_font_dense(fnt_ui_small));
        draw_set_color((array_length(_arts) == 0) ? make_color_rgb(230, 195, 120) : make_color_rgb(150, 140, 165));
        draw_text(_pccx, 272, "Every item you SMELT leaves its icon here - any collected icon that fits this slot family can be its look.");
        draw_set_halign(fa_left);
    }

    var _n    = array_length(_rows);
    // Phase 3 rows are taller (grey what-it-does subrow) so fewer fit - the
    // Step handler MUST mirror _vis (6 on phase 3, 9 elsewhere).
    var _vis    = (_ph == 3) ? 6 : 9;
    var _cur  = clamp(_gc.pb_cursor, 0, max(0, _n - 1));
    var _win0 = clamp(_gc.pb_scroll, 0, max(0, _n - _vis));
    var _row_y0 = (_ph == 3) ? 320 : 300;
    var _pitch  = (_ph == 3) ? 84 : 56;
    var _boxh   = (_ph == 3) ? 76 : 50;
    if (_ph == 3) {
        // Pick budget as drawn diamond pips - filled = chosen, hollow = open
        // (M 08-15: "0 / 2 - Enter toggles" keyboard-symbol strings read as
        // unfinished dev work).
        var _budget = pattern_affix_budget(_rar);
        var _used   = array_length(_gc.pb_affix_picks);
        draw_set_font(ui_font(fnt_ui_small));
        var _cap = (_used >= _budget) ? "Every blueprint slot is filled"
                 : ((_used == 0) ? "Choose up to " + string(_budget) + " blueprint" + ((_budget == 1) ? "" : "s")
                                 : string(_budget - _used) + " blueprint slot" + ((_budget - _used == 1) ? "" : "s") + " still open");
        var _pip = 16, _pgap = 14;
        var _pips_w = _budget * _pip + (_budget - 1) * _pgap;
        var _pw = _pips_w + 20 + string_width(_cap);
        var _px = _pccx - _pw / 2;
        var _pcy = 288;
        for (var _pi = 0; _pi < _budget; _pi++) {
            var _pxc = _px + _pi * (_pip + _pgap) + _pip / 2;
            draw_set_color(make_color_rgb(110, 92, 66));
            draw_triangle(_pxc - _pip / 2 - 3, _pcy, _pxc, _pcy - _pip / 2 - 3, _pxc + _pip / 2 + 3, _pcy, false);
            draw_triangle(_pxc - _pip / 2 - 3, _pcy, _pxc, _pcy + _pip / 2 + 3, _pxc + _pip / 2 + 3, _pcy, false);
            draw_set_color((_pi < _used) ? make_color_rgb(120, 210, 130) : make_color_rgb(24, 22, 28));
            draw_triangle(_pxc - _pip / 2, _pcy, _pxc, _pcy - _pip / 2, _pxc + _pip / 2, _pcy, false);
            draw_triangle(_pxc - _pip / 2, _pcy, _pxc, _pcy + _pip / 2, _pxc + _pip / 2, _pcy, false);
        }
        draw_set_halign(fa_left);
        draw_set_color(make_color_rgb(190, 180, 205));
        draw_text(_px + _pips_w + 20, _pcy - string_height("Ag") / 2, _cap);
    }
    for (var _i = _win0; _i < min(_n, _win0 + _vis); _i++) {
        var _r  = _rows[_i];
        var _ry = _row_y0 + (_i - _win0) * _pitch;
        var _sel = (_i == _cur);
        draw_set_color(_sel ? make_color_rgb(38, 32, 24) : make_color_rgb(20, 20, 26));
        draw_rectangle(_x0 + 20, _ry, _x1 - 40, _ry + _boxh, false);
        draw_set_color(_r.picked ? make_color_rgb(120, 210, 130) : (_sel ? _copper : make_color_rgb(48, 44, 40)));
        draw_rectangle(_x0 + 20, _ry, _x1 - 40, _ry + _boxh, true);
        var _tx = _x0 + 44;
        if (_r.icon != -1 && sprite_exists(_r.icon)) {
            draw_sprite_stretched(_r.icon, 0, _x0 + 32, _ry + 3, 44, 44);
            _tx = _x0 + 92;
        }
        draw_set_font(ui_font(fnt_ui));
        var _lab_col = _r.dim ? make_color_rgb(110, 110, 125) : (_r.picked ? make_color_rgb(160, 230, 170) : c_white);
        if (_ph == 2 && _r.rar >= 0 && !_r.dim) _lab_col = item_rarity_color(_r.rar);
        draw_set_color(_lab_col);
        draw_text(_tx, _ry + ((_ph == 3) ? 6 : 10), _r.label);
        // The family's what-it-does line, indented under the name (mirrors the
        // smelt-study popup's subrow idiom).
        if (_r.desc != "") {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(128, 128, 148));
            draw_text(_tx + 18, _ry + 42, ui_truncate(_r.desc, (_x1 - 260) - (_tx + 18)));
        }
        if (_r.right != "") {
            draw_set_halign(fa_right);
            draw_set_font(ui_font(fnt_ui_small));
            var _rt_col = _r.dim ? make_color_rgb(95, 95, 108) : make_color_rgb(150, 150, 165);
            if (_ph == 4 && _r.rar >= 0 && !_r.dim) _rt_col = item_rarity_color(_r.rar);
            if (_ph == 3 && !_r.dim) _rt_col = make_color_rgb(200, 175, 120);
            draw_set_color(_rt_col);
            draw_text(_x1 - 60, _ry + ((_ph == 3) ? 10 : 14), _r.right);
            draw_set_halign(fa_left);
        }
        // Picked state: drawn green check + CHOSEN, not a "[CHOSEN]" text tag.
        if (_ph == 3 && _r.picked) {
            draw_set_halign(fa_right);
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(120, 210, 130));
            draw_text(_x1 - 60, _ry + 42, "CHOSEN");
            var _ckx = _x1 - 60 - string_width("CHOSEN") - 26;
            var _cky = _ry + 42 + string_height("Ag") / 2;
            draw_line_width(_ckx, _cky, _ckx + 5, _cky + 6, 3);
            draw_line_width(_ckx + 5, _cky + 6, _ckx + 15, _cky - 7, 3);
            draw_set_halign(fa_left);
        }
        // Touch: tap any row (selects; tap acts like Enter in the Step handler).
        if (_mp && _mx >= _x0 + 20 && _mx < _x1 - 40 && _my >= _ry && _my < _ry + _boxh) {
            input_inject("pb:row" + string(_i));
        }
    }
    ui_pb_scrollbar(_x1 - 32, _row_y0, _row_y0 + (_vis - 1) * _pitch + _boxh, _n, _vis, _win0);
    if (_n == 0 && _ph == 3) {
        draw_set_halign(fa_center);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(make_color_rgb(120, 120, 135));
        draw_text_ext(_pccx, 420, "No blueprints unlocked for " + item_rarity_name(_rar) + " work yet.\nSmelt gear to study its affixes. Rarer fodder teaches faster.", 34, 900);
        draw_set_halign(fa_left);
    }

    // CONTINUE out of the blueprint phase with a partial pick (concentration).
    if (_ph == 3) {
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(make_color_rgb(128, 128, 148));
        // Truncate against the CONTINUE button's left edge (overlap audit).
        draw_text(_x0 + 30, _y1 - 40, ui_truncate("Open slots pour their power into the blueprints you do choose.",
            (_x1 - 380) - (_x0 + 30)));
    }
    if (_ph == 3 && array_length(_gc.pb_affix_picks) >= 1) {
        ui_confirm_button(_x1 - 360, _y1 - 46, _x1 - 160, _y1 - 8,
            "CONTINUE  [Space]", make_color_rgb(120, 210, 130), _mx, _my, _mp, "pb:cont");
    }

    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(75, 85, 105));
    ui_draw_key_legend(960, 1026, (_ph == 3) ? "W/S: Choose   Enter/Tap: Toggle   Space: Continue   Esc: Back"
                                             : "W/S: Choose   Enter/Tap: Pick   Esc: Back");
    draw_set_halign(fa_left);
}

// ---------------------------------------------------------------------------
// ui_draw_pb_smelt(_gc) - the SMELT study popup: the fodder is chosen; pick
// which of its affix families to study (or take just the ingot). Bordered
// modal per the checkout standing rule - commit and cancel are buttons.
// ---------------------------------------------------------------------------
function ui_draw_pb_smelt(_gc) {
    var _it = _gc.pb_smelt_item;
    if (_it == undefined) return;
    var _fams = pattern_item_families(_it);
    var _rows = array_length(_fams) + 1;
    var _x0 = 510, _x1 = 1410;
    var _y1 = 780;
    // Rows are 76px tall so the grey family-desc subrow sits INSIDE the border
    // (M 08-13 shot: at 48px the 27px-tall subrow straddled the box bottom).
    // The old 210px dead band above the buttons is reclaimed to pay for it.
    var _list_h = _rows * 84;
    var _y0 = _y1 - 120 - _list_h - 130;
    draw_set_alpha(0.66);
    draw_set_color(make_color_rgb(6, 8, 14));
    draw_rectangle(GUI_XL, 0, GUI_XR, GUI_H, false);
    draw_set_alpha(0.97);
    draw_set_color(make_color_rgb(22, 20, 30));
    draw_rectangle(_x0, _y0, _x1, _y1, false);
    draw_set_alpha(1.0);
    draw_set_color(make_color_rgb(200, 170, 110));
    draw_rectangle(_x0, _y0, _x1, _y1, true);
    draw_rectangle(_x0 + 6, _y0 + 6, _x1 - 6, _y1 - 6, true);
    draw_set_halign(fa_center);
    draw_set_font(ui_font(fnt_ui));
    draw_set_color(make_color_rgb(255, 225, 150));
    draw_text((_x0 + _x1) / 2, _y0 + 22, "SMELT " + string_upper(_it.name) + "?");
    draw_set_font(ui_font(fnt_ui_small));
    draw_set_color(make_color_rgb(210, 214, 228));
    draw_text_ext((_x0 + _x1) / 2, _y0 + 62,
        "It is DESTROYED for " + string(pattern_smelt_fee()) + "g: +1 " + item_rarity_name(clamp(_it.rarity, 0, 4))
        + " Reforge Ingot, its icon art joins the book, and Dorn studies ONE affix family from it:",
        max(26, string_height("Ag") + 2), (_x1 - _x0) - 80);
    draw_set_halign(fa_left);

    var _mx = device_mouse_x_to_gui(0), _my = device_mouse_y_to_gui(0);
    var _mp = mouse_check_button_pressed(mb_left);
    var _row_y0 = _y0 + 130;
    for (var _i = 0; _i < _rows; _i++) {
        var _ry  = _row_y0 + _i * 84;
        var _sel = (_i == _gc.pb_smelt_pick);
        var _lb; var _rt; var _ok = true;
        if (_i < array_length(_fams)) {
            var _fe = pattern_family_entry(_fams[_i]);
            var _pv = pattern_study_preview(_fams[_i], clamp(_it.rarity, 0, 4));
            _lb = (_fe != undefined) ? _fe.label : _fams[_i];
            _rt = _pv.text;
            _ok = _pv.ok;
        } else {
            _lb = "Just the ingot";
            _rt = "No study";
        }
        draw_set_color(_sel ? make_color_rgb(38, 32, 24) : make_color_rgb(20, 20, 26));
        draw_rectangle(_x0 + 30, _ry, _x1 - 30, _ry + 76, false);
        draw_set_color(_sel ? make_color_rgb(210, 140, 70) : make_color_rgb(48, 44, 40));
        draw_rectangle(_x0 + 30, _ry, _x1 - 30, _ry + 76, true);
        draw_set_font(ui_font(fnt_ui));
        draw_set_color(_ok ? c_white : make_color_rgb(120, 120, 135));
        draw_text(_x0 + 50, _ry + 6, _lb);
        // What the family actually DOES (M 08-13: "youre just choosing the
        // vernacular like 'insight' or 'ruin'") - grey subrow under the name.
        var _fd = (_i < array_length(_fams)) ? pattern_family_desc(_fams[_i])
                                             : "The ore alone - no blueprint study.";
        if (_fd != "") {
            draw_set_font(ui_font(fnt_ui_small));
            draw_set_color(make_color_rgb(128, 128, 148));
            draw_text(_x0 + 50, _ry + 42, _fd);
        }
        draw_set_halign(fa_right);
        draw_set_font(ui_font(fnt_ui_small));
        draw_set_color(_ok ? make_color_rgb(150, 210, 160) : make_color_rgb(190, 120, 110));
        draw_text(_x1 - 50, _ry + 26, _rt);
        draw_set_halign(fa_left);
        if (_mp && _mx >= _x0 + 30 && _mx < _x1 - 30 && _my >= _ry && _my < _ry + 76) {
            input_inject("pbsm:row" + string(_i));
        }
    }
    ui_confirm_button(_x0 + 50, _y1 - 90, (_x0 + _x1) / 2 - 20, _y1 - 24, "SMELT  [Enter]",
        make_color_rgb(120, 210, 130), _mx, _my, _mp, "pbsm:ok");
    ui_confirm_button((_x0 + _x1) / 2 + 20, _y1 - 90, _x1 - 50, _y1 - 24, "CANCEL  [Esc]",
        make_color_rgb(190, 120, 110), _mx, _my, _mp, "pbsm:cancel");
}
